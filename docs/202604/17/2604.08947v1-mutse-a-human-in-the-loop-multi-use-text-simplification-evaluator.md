---
title: "MuTSE: A Human-in-the-Loop Multi-use Text Simplification Evaluator"
title_zh: MuTSE：一种人在回路的多用途文本简化评估器
authors: "Rares-Alexandru Roscan, Gabriel Petre1, Adrian-Marius Dumitran, Angela-Liliana Dumitran"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08947v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 用于智能辅导系统和NLP研究的文本简化评估器
tldr: MuTSE是一个人机协作的文本简化评估系统，旨在解决大语言模型在不同提示策略和架构下输出难以系统评估的问题。该系统支持多模型与多提示词的交叉组合评估，并集成了一种带有线性偏差启发式的语义对齐引擎，通过可视化方式降低定性分析的认知负担，助力教育工作者和研究人员高效构建高质量的简化文本数据集。
selection_source: fresh_fetch
motivation: 现有的计算脚本和对话界面缺乏系统化的多维度框架，难以支持对大模型在文本简化任务中不同提示词与模型组合的对比评估。
method: 开发了一个交互式Web应用，支持多模型与多提示词组合的并发执行，并引入带有线性偏差启发式的分层语义对齐引擎进行可视化映射。
result: 实现了源句与简化句的实时对比矩阵生成，显著降低了定性分析的认知负荷，并支持可重复的结构化标注。
conclusion: MuTSE为LLM生成的文本简化提供了高效的评估工具，促进了智能辅导系统和NLP数据集的构建。
---

## 摘要
随着大语言模型（LLMs）在文本简化领域日益普及，针对不同提示策略和架构系统性地评估其输出，仍是自然语言处理（NLP）研究和智能导师系统（ITS）中一个关键的方法论挑战。由于缺乏用于文本对比分析的结构化、可视化框架，开发鲁棒的提示词往往受到阻碍。研究人员通常依赖静态计算脚本，而教育工作者则受限于标准的对话界面，这两种范式都不支持对“提示-模型”排列组合进行系统性的多维度评估。为了解决这些局限性，我们推出了 MuTSE，这是一个交互式的人在回路 Web 应用程序，旨在简化针对任意 CEFR 等级目标的 LLM 生成文本简化结果的评估流程。该系统支持并发执行 $P \times M$ 种提示-模型排列组合，并实时生成全面的对比矩阵。通过集成一种由线性偏差启发式算法（$λ$）增强的新型分层语义对齐引擎，MuTSE 能够将源句子与其简化后的对应句子进行可视化映射，从而降低定性分析相关的认知负荷，并为下游 NLP 数据集的构建提供可重复的结构化标注。

## Abstract
As Large Language Models (LLMs) become increasingly prevalent in text simplification, systematically evaluating their outputs across diverse prompting strategies and architectures remains a critical methodological challenge in both NLP research and Intelligent Tutoring Systems (ITS). Developing robust prompts is often hindered by the absence of structured, visual frameworks for comparative text analysis. While researchers typically rely on static computational scripts, educators are constrained to standard conversational interfaces -- neither paradigm supports systematic multi-dimensional evaluation of prompt-model permutations. To address these limitations, we introduce \textbf{MuTSE}\footnote{The project code and the demo have been made available for peer review at the following anonymized URL. https://osf.io/njs43/overview?view_only=4b4655789f484110a942ebb7788cdf2a, an interactive human-in-the-loop web application designed to streamline the evaluation of LLM-generated text simplifications across arbitrary CEFR proficiency targets. The system supports concurrent execution of $P \times M$ prompt-model permutations, generating a comprehensive comparison matrix in real-time. By integrating a novel tiered semantic alignment engine augmented with a linearity bias heuristic ($λ$), MuTSE visually maps source sentences to their simplified counterparts, reducing the cognitive load associated with qualitative analysis and enabling reproducible, structured annotation for downstream NLP dataset construction.

---

## 论文详细总结（自动生成）

以下是对论文《MuTSE: A Human-in-the-Loop Multi-use Text Simplification Evaluator》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着大语言模型（LLM）在文本简化任务中的广泛应用，如何系统性地评估不同模型（M）在不同提示策略（P）下的输出质量，成为了一个方法论挑战。
*   **研究背景**：
    *   **研究者困境**：通常依赖静态脚本或代码（如 EASSE），缺乏直观的可视化对比和对齐工具。
    *   **教育者困境**：受限于标准对话界面（如 ChatGPT），难以进行多维度、多模型的横向对比。
    *   **评估痛点**：现有的自动指标（如 BLEU、SARI）无法完全取代人工定性分析，而人工分析多个变体时认知负荷极高。

### 2. 方法论与核心技术
MuTSE 是一个基于 Web 的交互式“人在回路”（Human-in-the-Loop）系统，其核心思想是通过自动化对齐和并行生成来辅助人工评估。
*   **并行推理模型 ($P \times M$)**：采用异步并发架构（FastAPI + Together AI），支持同时运行多个提示词与多个模型的组合，将生成延迟控制在最慢单个模型的推理时间内。
*   **分层语义对齐引擎**：为了将源句与简化句精准映射，设计了三级回退机制：
    1.  **第一级（语义层）**：使用多语言 Transformer 模型（如 MiniLM）计算余弦相似度。
    2.  **第二级（词汇层）**：若语义向量失效，回退至基于字符级 n-grams 的 TF-IDF 混合表示。
    3.  **第三级（位置层）**：若前两级均失效，则根据句子在文档中的相对位置进行结构化对齐。
*   **线性偏差启发式算法 ($\lambda$)**：
    *   **公式**：$Score(i, j) = CosineSim(S_{orig}, S_{simp}) - |Pos_{rel}(i) - Pos_{rel}(j)| \times \lambda$
    *   **原理**：通过引入位置惩罚因子 $\lambda$，强制对齐遵循文本的单调进展，有效解决了语义相似但位置偏离导致的误报问题。用户可实时调节 $\lambda$（0-2）来优化可视化效果。

### 3. 实验设计与场景
该论文属于**系统描述类论文**，重点在于工具的实现与功能验证，而非单一算法的性能打榜。
*   **应用场景**：针对 CEFR（欧洲语言共同参考框架）等级（如 A2、B1）的文本简化评估。
*   **集成模型**：通过 Together AI 接入了 Llama 3、DeepSeek V3、Qwen 2.5 等主流开源 LLM。
*   **对比基准**：系统内置了 Flesch-Kincaid Grade Level（年级水平）和 Flesch Reading Ease（阅读易读性）等传统可读性指标作为参考基准。
*   **功能验证**：展示了如何通过 UI 切换提示词、模型，并实时观察语义对齐图表的变化。

### 4. 资源与算力
*   **算力实现**：系统后端不直接承担重型推理，而是通过 **Together AI 的无服务器（Serverless）端点**调用云端算力。
*   **本地需求**：语义对齐引擎（SBERT）设计为轻量级，可在标准 CPU 架构上运行，无需专用 GPU 即可实现本地部署。
*   **持久化**：采用轻量级本地 JSON 存储，无需复杂的数据库配置。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了系统处理 $P \times M$ 矩阵的能力（例如 5 个预设提示词与多个模型的组合）。
*   **充分性评价**：作为工具类论文，其功能演示涵盖了从生成、对齐、标注到导出的全流程。虽然没有大规模的人工标注一致性实验（Inter-annotator agreement），但其提供的自定义标注框架（支持 1-100 滑块、权重分配等）为后续的客观实验提供了基础设施。

### 6. 主要结论与发现
*   **降低认知负荷**：通过实时语义对齐和侧边栏对比，显著减少了研究人员在追踪复杂文本转换时的视觉疲劳。
*   **线性偏差的有效性**：引入 $\lambda$ 参数能显著修正小参数嵌入模型在处理高度压缩文本时的对齐错误。
*   **科研助力**：系统支持导出结构化的 JSON/CSV 数据，为构建高质量的 NLP 简化数据集（用于微调或奖励模型训练）提供了便捷路径。

### 7. 优点与亮点
*   **人在回路的深度集成**：不仅是生成工具，更是一个允许用户自定义评估维度（如流畅度、意义保留度）的标注平台。
*   **高度模块化**：用户无需修改代码即可通过 UI 添加新模型 ID 或自定义提示词。
*   **前端交互优化**：对齐图表的重算在客户端完成，调节参数时具有零延迟的视觉反馈。

### 8. 不足与局限
*   **扩展性限制**：目前使用本地 JSON 存储，不适合大型实验室的多用户并发协作（需升级为关系型数据库）。
*   **部署门槛**：虽然使用了云端推理，但本地环境配置（Python/Node.js 依赖）对非技术背景的教育工作者仍有一定难度。
*   **任务局限**：目前的线性偏差算法主要针对单调的文本简化，若用于语序大幅调整的机器翻译（MT）评估，可能需要重新校准 $\lambda$ 启发式策略。

（完）

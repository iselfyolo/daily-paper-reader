---
title: Cross-Platform Domain Adaptation for Multi-Modal MOOC Learner Satisfaction Prediction
title_zh: 针对多模态慕课学习者满意度预测的跨平台领域自适应
authors: "Jakub Kowalski, Magdalena Piotrowska"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13247v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 使用大模型编码器进行 MOOC 学习者满意度预测
tldr: 本研究针对MOOC平台间学习者满意度预测的领域偏移问题，提出了跨平台领域自适应框架ADAPT-MS。该框架结合LLM文本编码与行为轨迹建模，通过领域对抗训练对齐跨平台表示，并利用潜变量校准层修正平台评分偏差。实验证明，ADAPT-MS在无监督和少样本场景下均显著优于现有基准，有效解决了多模态数据缺失和平台差异带来的挑战。
selection_source: fresh_fetch
motivation: 解决MOOC满意度预测模型在不同平台间因评论风格、行为日志模式和评分标准差异导致的性能显著下降问题。
method: 提出ADAPT-MS框架，利用LLM和MLP编码多模态数据，通过对抗训练对齐特征，并引入校准层和门控融合处理评分偏差与模态缺失。
result: 在三大MOOC平台数据集上，该方法在无监督设置下RMSE达到0.66，在1000个样本的少样本设置下达到0.60，优于所有对比基准。
conclusion: ADAPT-MS通过有效的特征对齐和偏差校准，为跨平台MOOC学习者满意度预测提供了一种稳健且高效的解决方案。
---

## 摘要
从慕课（MOOC）评论和行为日志中预测学习者满意度，对于课程质量改进和平台运营具有重要价值。在实践中，由于评论风格、学习者群体、行为日志模式以及平台特定的评分规范存在领域偏移，在一个平台上训练的模型在部署到另一个平台时性能会显著下降。我们研究了在目标平台标签有限或缺失的情况下，多模态慕课满意度预测的跨平台领域自适应问题。我们提出了 ADAPT-MS，这是一个平台自适应框架，它：(i) 使用冻结的大语言模型（LLM）编码器对评论文本进行编码，并使用规范词汇表 MLP 对行为轨迹进行编码；(ii) 通过带有梯度反转的领域对抗训练来对齐跨平台表示；(iii) 通过潜变量校准层纠正平台特定的评分偏差；(iv) 通过带有模态丢弃（modality dropout）的门控融合处理缺失的行为模态。在涵盖三个主要平台的跨平台慕课数据集上的实验表明，ADAPT-MS 在无监督设置（零标注目标样本）下实现了 0.66 的目标平台均方根误差（RMSE），在有 1000 个标注目标样本的情况下实现了 0.60 的 RMSE，优于包括朴素池化、无校准的领域对抗对齐以及全量微调在内的强基准模型。消融实验证实了各组件的独立贡献，少样本自适应曲线表明，即使只有 50 个标注目标样本，模型也能实现稳定的提升。

## Abstract
Learner satisfaction prediction from MOOC reviews and behavioral logs is valuable for course quality improvement and platform operations. In practice, models trained on one platform degrade significantly when deployed on another due to domain shift in review style, learner population, behavioral logging schemas, and platform-specific rating norms. We study \textbf{cross-platform domain adaptation} for multi-modal MOOC satisfaction prediction under limited or absent target-platform labels. We propose \textbf{ADAPT-MS}, a platform-adaptive framework that (i) encodes review text with a frozen LLM encoder and behavioral traces with a canonical-vocabulary MLP, (ii) aligns cross-platform representations via domain-adversarial training with gradient reversal, (iii) corrects platform-specific rating bias through a latent-variable calibration layer, and (iv) handles missing behavioral modalities via gated fusion with modality dropout. Experiments on a multi-platform MOOC dataset spanning three major platforms demonstrate that ADAPT-MS achieves target-platform RMSE of 0.66 in the unsupervised setting (zero labeled target samples) and 0.60 with 1000 labeled target samples, outperforming strong baselines including naive pooling, domain-adversarial alignment without calibration, and full fine-tuning. Ablation studies confirm the independent contribution of each component, and few-shot adaptation curves demonstrate stable improvement even with as few as 50 labeled target samples.

---

## 论文详细总结（自动生成）

### 针对多模态慕课学习者满意度预测的跨平台领域自适应论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是**大规模开放在线课程（MOOC）中学习者满意度的预测问题**。虽然现有的多模态模型在单一平台内表现良好，但在跨平台部署时会遭遇严重的性能退化。这种退化主要源于四个维度的“领域偏移（Domain Shift）”：
*   **语言风格偏移**：不同平台的评论用词、正式程度和文化表达不同。
*   **人群与课程分布偏移**：平台间的学习者人口统计特征和学科分布存在差异。
*   **行为日志模式偏移**：不同平台对同一行为（如“观看视频”）的记录粒度和定义不同。
*   **评分标准漂移**：不同平台的评分文化不同（例如，某些平台默认给5星，而另一些平台则较为严苛）。

#### 2. 论文提出的方法论：ADAPT-MS 框架
为了解决上述偏移，作者提出了 **ADAPT-MS** 框架，其核心思想是通过特征对齐和标签校准来实现跨平台迁移：
*   **多模态编码**：
    *   **文本**：使用冻结的预训练大语言模型（RoBERTa-base）提取评论的语义向量，以保留通用语言特征并防止对源平台词汇过拟合。
    *   **行为**：将原始日志映射为包含观看时长、测验次数、论坛互动等6个维度的“规范化行为特征向量”，并通过 MLP 编码。
*   **鲁棒融合机制**：采用**门控融合（Gated Fusion）**结合**模态丢弃（Modality Dropout）**。在训练中随机丢弃行为模态，使模型在目标平台缺失行为数据时仍能保持稳健。
*   **领域对抗对齐**：引入**梯度反转层（GRL）**和领域判别器，通过对抗训练迫使编码器提取平台无关的通用特征。
*   **平台特定评分校准**：设计了一个潜变量校准层，通过仿射变换（$y = ax + b$）修正目标平台的评分偏差。在无监督情况下，通过匹配源平台预测值与目标平台历史统计值的均值和方差来实现校准。

#### 3. 实验设计
*   **数据集**：涵盖三个主流 MOOC 平台（A、B、C），包含 48 万次选课记录、9500 万条行为事件和 180 万条评论。
*   **实验场景**：
    1.  **无监督迁移**：目标平台完全无标签（$k=0$）。
    2.  **少样本自适应**：目标平台有极少量标签（$k=50, 200, 1000$）。
    3.  **成对迁移**：在不同平台对之间进行交叉验证。
*   **对比基准（Benchmark）**：
    *   **Source-Only**：仅在源平台训练。
    *   **Pool-NoAdapt**：合并所有源平台数据但不做自适应。
    *   **Fine-Tune**：全参数微调。
    *   **DANN-Only**：仅使用领域对抗对齐，不进行校准。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 (40GB)** GPU。
*   **时长**：
    *   主模型训练：每个 Fold 约 **1.5 小时**。
    *   文本嵌入预计算：每个平台约 30 分钟。
    *   少样本校准更新：每个设置耗时少于 5 分钟。
*   **优化器**：Adam，学习率 $3 \times 10^{-4}$，Batch Size 为 256。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **消融实验**：逐一移除校准层、对抗对齐、门控融合等组件，验证了各部分的独立贡献。
*   **参数敏感性分析**：探讨了对齐权重 $\lambda$ 对结果的影响。
*   **时间切分**：采用基于时间的划分（前 70% 训练，后 15% 测试），有效防止了时间数据泄露。
*   **多平台验证**：在 A、B、C 三个具有显著统计差异的平台间进行交叉测试，证明了方法的普适性。

#### 6. 主要结论与发现
*   **性能提升**：在无监督设置下，ADAPT-MS 的 RMSE 达到 0.66，比朴素池化方法提升了约 13%-16%。
*   **校准的重要性**：校准层对性能的提升至关重要，尤其是在目标平台评分普遍偏高（如平台 C）的情况下。
*   **少样本效率**：仅需 50 个目标平台样本，模型性能就能显著提升，且比全参数微调更稳定。
*   **模态互补性**：文本模态是基础，但加入规范化的行为特征能进一步降低误差，尤其是在处理极端评分时。

#### 7. 优点
*   **针对性强**：精准识别并解决了 MOOC 场景下特有的“评分标准漂移”问题。
*   **架构优雅**：结合了冻结 LLM 的稳定性与轻量级校准层的灵活性，适合实际生产环境中的快速部署。
*   **鲁棒性高**：通过模态丢弃技术，解决了教育数据中常见的行为日志缺失或不兼容问题。

#### 8. 不足与局限
*   **日志依赖**：虽然提出了规范化特征，但如果目标平台完全缺失某些核心行为（如视频快进/回放），模型性能仍会受限。
*   **词汇局限**：领域对抗训练难以完全消除平台特有的 UI 术语（如“XP点数”）带来的干扰。
*   **语言限制**：目前实验主要集中在单一语言环境，未探讨跨语言（如中文平台到英文平台）的迁移挑战。
*   **公平性考量**：虽然论文提到了公平性审计，但未在实验中给出具体的量化评估，校准过程是否会引入针对特定群体的偏差仍需研究。

（完）

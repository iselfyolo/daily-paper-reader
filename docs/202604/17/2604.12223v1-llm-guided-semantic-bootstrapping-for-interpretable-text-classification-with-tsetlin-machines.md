---
title: LLM-Guided Semantic Bootstrapping for Interpretable Text Classification with Tsetlin Machines
title_zh: 基于大语言模型引导语义自举的 Tsetlin 机可解释文本分类
authors: "Jiechao Gao, Rohan Kumar Yadav, Yuangang Li, Yuandong Pan, Jie Wang, Ying Liu, Michael Lepech"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12223v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: LLM生成子意图，通过三阶段课程引导合成数据创建
tldr: 本研究针对预训练语言模型不透明且计算成本高，以及Tsetlin Machine（TM）等符号模型缺乏语义泛化能力的问题，提出了一种LLM引导的语义引导框架。该框架通过LLM生成子意图并构建三阶段课程学习合成数据，利用非否定TM提取可解释的语义线索并注入真实数据。该方法无需嵌入或运行时LLM调用，使符号模型具备了预训练语义先验，在保持高效和可解释性的同时，实现了与BERT相当的分类性能。
selection_source: fresh_fetch
motivation: 旨在结合预训练语言模型的语义理解能力与符号模型的可解释性，解决传统符号模型在处理复杂文本语义时表现不足的问题。
method: 通过LLM生成多阶段合成数据，利用非否定TM提取关键语义线索并将其注入到符号模型中，实现知识从大模型到符号模型的迁移。
result: 实验表明该方法在多个分类任务上显著优于传统TM，且在保持全符号化和高效性的同时，取得了与BERT模型相媲美的准确率。
conclusion: 该框架成功地将LLM的语义先验转化为可解释的符号逻辑，为构建兼具高性能与高透明度的文本分类系统提供了新路径。
---

## 摘要
BERT 等预训练语言模型 (PLM) 提供了强大的语义表示，但成本高昂且不透明；而 Tsetlin 机 (TM) 等符号模型虽然具有透明性，但缺乏语义泛化能力。我们提出了一种语义自举框架，将 LLM 知识转化为符号形式，从而将可解释性与语义能力相结合。给定一个类别标签，LLM 会生成子意图，通过三阶段课程（种子、核心、增强）引导合成数据的创建，从而扩展语义多样性。非否定 Tsetlin 机 (NTM) 从这些示例中学习，提取高置信度的文字 (literals) 作为可解释的语义线索。将这些线索注入真实数据，使 TM 能够将子句逻辑与 LLM 推断的语义对齐。我们的方法不需要嵌入或运行时 LLM 调用，但能为符号模型配备预训练的语义先验。在多个文本分类任务中，该方法在可解释性和准确性上均优于原始 TM，在保持完全符号化和高效性的同时，实现了与 BERT 相当的性能。

## Abstract
Pretrained language models (PLMs) like BERT provide strong semantic representations but are costly and opaque, while symbolic models such as the Tsetlin Machine (TM) offer transparency but lack semantic generalization. We propose a semantic bootstrapping framework that transfers LLM knowledge into symbolic form, combining interpretability with semantic capacity. Given a class label, an LLM generates sub-intents that guide synthetic data creation through a three-stage curriculum (seed, core, enriched), expanding semantic diversity. A Non-Negated TM (NTM) learns from these examples to extract high-confidence literals as interpretable semantic cues. Injecting these cues into real data enables a TM to align clause logic with LLM-inferred semantics. Our method requires no embeddings or runtime LLM calls, yet equips symbolic models with pretrained semantic priors. Across multiple text classification tasks, it improves interpretability and accuracy over vanilla TM, achieving performance comparable to BERT while remaining fully symbolic and efficient.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-Guided Semantic Bootstrapping for Interpretable Text Classification with Tsetlin Machines》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对文本分类中**“性能”与“可解释性”的权衡**问题展开研究。
*   **背景**：预训练语言模型（如 BERT）语义表达能力强，但属于“黑盒”模型，计算成本高且难以解释；而 Tsetlin 机（TM）作为一种基于命题逻辑的符号模型，具有极高的透明度和推理效率，但由于依赖布尔词袋（BoW）表示，缺乏语义泛化能力（例如无法识别近义词）。
*   **核心问题**：如何在不引入复杂嵌入（Embedding）或运行时调用大模型（LLM）的前提下，将 LLM 的语义知识注入到符号化的 TM 模型中，使其既具备深度语义理解能力，又保持完全的可解释性。

### 2. 方法论：核心思想与技术细节
论文提出了一种**LLM 引导的语义自举框架**，主要分为三个阶段：
1.  **LLM 引导的子意图发现与合成数据生成**：
    *   利用 LLM 将类别标签分解为更细粒度的“子意图”（Sub-intents，如“因剧情而好评”）。
    *   采用**三阶段课程学习**生成合成数据：**种子阶段（Seed）**生成规范示例；**核心阶段（Core）**增加结构变化；**增强阶段（Enriched）**引入同义词和修饰语，扩展词汇多样性。
2.  **预训练非否定 Tsetlin 机（NTM）**：
    *   设计了一种 NTM 变体，其子句仅包含正向文字（Non-Negated Literals），公式为 $C_{\kappa j}(X) = \bigwedge_{x_k \in I_{\kappa j}} x_k$。
    *   引入**增强型 Type I 反馈**（奖励概率设为 1.0），使模型快速收敛并锁定高置信度的语义文字。
3.  **语义特征注入与 TM 微调**：
    *   从 NTM 中提取与子意图关联的高置信度文字作为“语义线索”。
    *   将这些线索作为额外的布尔特征注入真实数据的 BoW 表示中。
    *   最后在增强后的数据上训练标准 TM，实现逻辑子句与 LLM 推断语义的对齐。

### 3. 实验设计
*   **数据集**：涵盖三个领域共 6 个基准数据集：
    *   主题分类：AG News, R8, R52。
    *   情感分析：IMDb, SST-2。
    *   生物医学：HoC (Hallmarks of Cancer)。
*   **Benchmark（对比方法）**：
    *   传统方法：BoW TF-IDF, fastText。
    *   深度学习：char-CNN, RNN/LSTM (含 GloVe 版本), BERT (Base/Large)。
    *   符号模型：原始 TM, 增强了 GloVe 嵌入的 TM (GloVe)。

### 4. 资源与算力
*   **LLM 部分**：使用了 Azure OpenAI API 提供的 **gpt-4o**（2024年5月版）进行子意图发现和数据生成。设置了 $p=0.9$ 的核采样和 $0.7$ 的温度。
*   **TM 训练部分**：论文**未明确说明**训练 TM 所使用的具体硬件（如 GPU 型号、数量）或具体的训练时长。但强调了该方法在推理阶段无需 LLM 调用，具有极高的运行效率。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同性质的数据集上进行了测试，每组实验运行 5 次并取平均值（含标准差）。
*   **充分性评价**：
    *   **优点**：跨领域覆盖较广，对比了从简单统计到最先进 Transformer 的多种模型，结果具有代表性。
    *   **不足**：论文提到未进行完整的消融实验（例如改变合成样本数量、超参数 $s$ 的敏感度分析等），这在一定程度上限制了对各组件贡献的精确评估。

### 6. 主要结论与发现
*   **性能提升**：LLM 引导的 TM 在所有数据集上均显著优于原始 TM。在 SST-2 上比原始 TM 提升了 9.63%，在 R52 上提升了近 10%。
*   **比肩 BERT**：在 AG News、R8 和 R52 等任务上，该方法的准确率已非常接近 BERT（差距通常在 1% 左右），同时保持了符号模型的轻量化。
*   **泛化能力**：通过案例研究证明，该方法能有效处理原始 TM 无法识别的稀有词或组合结构（如将“immunosuppression”分解为“immune”和“suppression”进行逻辑匹配）。

### 7. 优点（亮点）
*   **全符号化推理**：推理阶段完全基于布尔逻辑，不需要向量计算或 Embedding 层，适合资源受限的实时环境。
*   **高可解释性**：模型输出的是人类可读的逻辑子句（如：`plot ∧ twist ∧ ending -> Positive`），决策过程透明。
*   **知识迁移新路径**：提供了一种将 LLM 的隐性知识转化为显性符号规则的有效方案，无需在推理时依赖昂贵的 LLM。

### 8. 不足与局限
*   **LLM 依赖性**：框架的效果高度依赖 LLM 生成合成数据的质量，可能引入 LLM 自带的偏见或幻觉。
*   **表达力权衡**：NTM 为了提高可解释性舍弃了否定文字，这可能削弱模型处理复杂对比逻辑（如“虽然...但是...”）的能力。
*   **超参数敏感性**：TM 模型对超参数（如 $s$ 值、子句数量）较为敏感，论文尚未给出系统性的调优指南。
*   **任务局限**：目前主要集中在文本分类，尚未扩展到更复杂的序列标注或生成任务。

（完）

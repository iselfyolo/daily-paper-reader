---
title: "RPS: Information Elicitation with Reinforcement Prompt Selection"
title_zh: RPS：基于强化提示词选择的信息引导
authors: "Tao Wang, Jingyao Lu, Xibo Wang, Haonan Huang, Su Yao, Zhiqiang Hu, Xingyan Chen, Enmao Diao"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13817v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 用于辅导系统的大模型信息引导
tldr: 本研究针对大语言模型在开放式对话中难以引导用户提供隐藏或不完整信息的问题，提出了强化提示选择（RPS）框架。该框架将提示词选择建模为序列决策问题，利用强化学习策略从提示池中动态选择最有效的引导语。通过合成实验和新构建的法律领域基准数据集 IELegal 的验证，RPS 在获取关键信息方面显著优于静态和随机基准，提升了对话系统的交互效率。
selection_source: fresh_fetch
motivation: 旨在解决用户在与AI对话时因隐私、歧义或犹豫而隐瞒关键信息，导致模型难以获取完整上下文的挑战。
method: 提出一种轻量级的强化学习框架RPS，将提示词选择视为序列决策过程，通过学习策略动态从提示池中选取最优引导语。
result: 在合成实验和基于真实法律案例构建的 IELegal 基准测试中，RPS 的信息获取效果均显著优于静态提示和随机基准。
conclusion: 实验证明自适应提示选择策略能有效增强 LLM 引导用户表达隐藏信息的能力，对法律、医疗等专业对话场景具有重要价值。
---

## 摘要
大语言模型（LLMs）在对话生成和推理方面展现出卓越的能力，但在开放式对话中引导用户已知但隐藏的信息方面，其效果仍然有限。在许多交互式人工智能应用中，如个人助手、辅导系统以及法律或临床支持，用户往往由于隐私顾虑、歧义或社交顾虑而保留敏感或不确定的信息。这使得 LLMs 难以收集完整且与上下文相关的输入。在这项工作中，我们定义了开放式对话场景下的信息引导问题，并提出了强化提示词选择（RPS），这是一个轻量级的强化学习框架，将提示词选择建模为一个序列决策问题。为了在受控环境下分析这一问题，我们设计了一个合成实验，其中强化学习智能体的表现优于随机查询基准，展示了基于策略的方法在自适应信息引导方面的潜力。基于这一见解，RPS 在提示词池上学习一种策略，通过对话自适应地引导用户提供隐藏或表达不完整的信息。我们还引入了 IELegal，这是一个根据真实法律案件文件构建的新基准数据集，模拟了旨在揭示案件相关事实的基于对话的信息引导任务。在此设置下，RPS 的表现优于静态提示词基准，证明了自适应提示词选择在 LLM 驱动的对话系统中引导关键信息的有效性。

## Abstract
Large language models (LLMs) have shown remarkable capabilities in dialogue generation and reasoning, yet their effectiveness in eliciting user-known but concealed information in open-ended conversations remains limited. In many interactive AI applications, such as personal assistants, tutoring systems, and legal or clinical support, users often withhold sensitive or uncertain information due to privacy concerns, ambiguity, or social hesitation. This makes it challenging for LLMs to gather complete and contextually relevant inputs. In this work, we define the problem of information elicitation in open-ended dialogue settings and propose Reinforcement Prompt Selection (RPS), a lightweight reinforcement learning framework that formulates prompt selection as a sequential decision-making problem. To analyze this problem in a controlled setting, we design a synthetic experiment, where a reinforcement learning agent outperforms a random query baseline, illustrating the potential of policy-based approaches for adaptive information elicitation. Building on this insight, RPS learns a policy over a pool of prompts to adaptively elicit concealed or incompletely expressed information from users through dialogue. We also introduce IELegal, a new benchmark dataset constructed from real legal case documents, which simulates dialogue-based information elicitation tasks aimed at uncovering case-relevant facts. In this setting, RPS outperforms static prompt baselines, demonstrating the effectiveness of adaptive prompt selection for eliciting critical information in LLM-driven dialogue systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《RPS: Information Elicitation with Reinforcement Prompt Selection》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**信息引导（Information Elicitation, IE）**问题。尽管大语言模型（LLM）擅长对话，但在面对用户**有意隐瞒或表达不全**的信息（如法律咨询中的不利事实、医疗问诊中的敏感症状）时，往往难以通过常规对话挖掘出完整真相。
*   **研究动机**：用户常因隐私、自我保护或认知偏差而选择性披露信息（倾向于说正面信息 $I^+$，隐瞒负面信息 $I^-$）。
*   **核心挑战**：如何设计一种机制，让模型能根据对话进展，自适应地选择最优的提问策略（Prompt），从而最大化地挖掘出用户隐藏的关键事实。

### 2. 方法论：核心思想与关键技术
论文提出了**强化提示选择（Reinforcement Prompt Selection, RPS）**框架，将信息引导建模为一个**序列决策问题**。

*   **核心思想**：不再依赖单一的静态提示词，也不进行高成本的逐 Token 强化学习生成，而是从一个**预定义的专家提示词池**中，根据当前对话状态动态选择最合适的策略。
*   **关键技术细节**：
    *   **状态（State）**：由当前已提取的信息 $\hat{I}_{t-1}$ 和对话历史 $D_{1:t-1}$ 组成。
    *   **动作（Action）**：从提示词池 $\mathcal{A}$ 中选择一个特定的引导策略（如“对抗性质疑”或“细节佐证”）。
    *   **奖励函数（Reward）**：采用**归一化信息增益**。公式定义为：$r_t = \frac{d(\hat{I}_{t-1}, I) - d(\hat{I}_t, I)}{d(\hat{I}_{t-1}, I)}$。其中 $d$ 是估计信息与真实信息之间的距离。这种归一化设计能稳定优化过程，防止在信息缺口缩小后奖励信号消失。
    *   **算法流程**：在每个回合，策略网络 $\pi_\theta$ 选择一个提示词，LLM 基于该提示词生成查询 $q_t$，用户反馈后，系统提取新事实并更新状态，最后根据信息覆盖率的提升计算奖励并更新策略网络（使用 DQN 算法）。

### 3. 实验设计
论文设计了两个层面的实验：
*   **合成实验（GMM 场景）**：
    *   使用**高斯混合模型（GMM）**模拟用户的信息分布。
    *   设置了“无偏用户”和“有偏用户”（倾向于隐藏某些簇的数据）两种模式。
    *   **Benchmark**：随机查询基准。
*   **真实领域实验（IELegal 基准）**：
    *   **数据集**：基于 CAIL2018 刑事案例构建了 **IELegal**（包含 1000 个基础案例和 1000 个增强案例）。
    *   **对比方法**：
        1.  **静态策略**：探索性搜集、精确证据确认、佐证细节验证、对抗性披露提问。
        2.  **Normal**：无特定提示词的默认对话。
        3.  **Adaptive Generative Questioning**：由 LLM 实时生成提问策略。
        4.  **GRIPS & RLPrompt**：现有的自动化提示词优化/搜索方法。

### 4. 资源与算力
*   **模型使用**：
    *   使用 **GPT-4o-mini** 进行事实提取和用户模拟。
    *   使用 **Qwen2.5-0.5B** 作为 RLPrompt 的策略网络。
    *   使用 **Qwen-32B** 作为 GRIPS 的评估器。
    *   使用 **Sentence-BERT** 计算语义相似度奖励。
*   **算力细节**：文中明确提到 GMM 实验兼容 **纯 CPU 执行**。对于 LLM 实验，虽然列出了模型规模和超参数（如 Batch Size 64, 训练 500 个 Episode），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在 2000 个法律案例上进行了测试，每组实验运行 10 次独立重复以确保统计可靠性（提供 95% 置信区间）。
*   **充分性**：实验涵盖了从理论模拟（GMM）到实际应用（法律）的跨越。对比了多种基准，包括手动设计的专家策略和最先进的自动提示词优化算法。
*   **客观性**：通过引入“有偏用户”模拟真实的人类心理（自我呈现理论），使得实验环境比传统的协作式对话更具挑战性且更贴近现实。

### 6. 主要结论与发现
*   **RPS 显著占优**：在 IELegal 数据集上，RPS 的信息获取效率和最终覆盖率均显著超过所有静态基准和自动化基准（如 GRIPS）。
*   **对抗策略的必要性**：实验证明，当用户试图隐瞒信息时，传统的“探索性”提问效果有限，而 RPS 能学会在关键时刻切换到“对抗性”或“精确确认”策略。
*   **轻量化优势**：相比于逐 Token 生成提示词的 RLPrompt，RPS 通过选择预定义策略，既保证了领域知识的集成，又大幅降低了计算开销。

### 7. 优点（亮点）
*   **问题定义新颖**：首次系统性地定义了开放式对话中的“隐藏信息引导”问题，并结合了社会心理学背景。
*   **轻量且高效**：将 RL 作用于提示词池而非 Token 空间，解决了 LLM 强化学习训练难、推理慢的问题。
*   **高质量基准**：贡献了 IELegal 数据集，为法律 AI 领域提供了专门测试“事实挖掘能力”的工具。

### 8. 不足与局限
*   **领域覆盖有限**：目前仅在法律领域验证，尚未扩展到医疗、心理咨询等同样存在信息隐瞒的领域。
*   **数据集规模**：2000 个案例对于深度强化学习来说规模尚可，但对于覆盖复杂的法律场景可能仍显不足。
*   **静态提示词池依赖**：RPS 的表现上限受限于预定义的提示词池质量，如果池中缺乏有效的策略，模型无法通过自学习创造出全新的引导方式。
*   **时间动态性缺失**：GMM 合成实验未考虑对话随时间演进的动态变化特征。

（完）

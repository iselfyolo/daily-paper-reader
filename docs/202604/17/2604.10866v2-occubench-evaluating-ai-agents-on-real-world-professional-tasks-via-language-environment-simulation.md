---
title: "OccuBench: Evaluating AI Agents on Real-World Professional Tasks via Language Environment Simulation"
title_zh: OccuBench：通过语言环境模拟评估 AI 智能体在真实世界专业任务中的表现
authors: "Xiaomeng Hu, Yinger Zhang, Fei Huang, Jianhong Tu, Yang Su, Lianghao Deng, Yuxuan Liu, Yantao Liu, Dayiheng Liu, Tsung-Yi Ho"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10866v2"
tags: ["query:ds-gen"]
score: 9.0
evidence: 使用LLM生成基准评估实例的多智能体合成流水线
tldr: OccuBench是一个针对AI智能体在真实专业任务中表现的评估基准，涵盖10个行业和65个专业领域的100个场景。它通过语言环境模拟器（LES）利用大模型生成工具响应，解决了专业领域公共环境匮乏的难题。该基准从任务完成度和环境鲁棒性两个维度评估了15种前沿模型，揭示了模型在不同行业的职业能力分布及对隐性故障的处理挑战，为系统性评估智能体职业能力提供了新框架。
selection_source: fresh_fetch
motivation: 现有基准测试因缺乏特定领域的公开模拟环境，无法有效评估AI智能体在数百个专业职业领域中的实际工作表现。
method: 引入OccuBench基准，采用大模型驱动的语言环境模拟器（LES）模拟专业工具交互，并通过多智能体合成流水线确保评估实例的可解性与多样性。
result: 实验显示没有模型能在所有行业中胜出，且智能体在应对隐性数据降级故障时比显性错误更困难，而模型规模和推理能力的提升能显著增强表现。
conclusion: OccuBench提供了首个系统性的跨行业职业任务评估体系，证明了环境模拟质量对评估可靠性的关键作用，并揭示了当前前沿模型在专业领域的能力差异。
---

## 摘要
AI 智能体被期望在数百个职业领域（从急诊科分诊到核反应堆安全监测，再到海关进口处理）执行专业工作，然而现有的基准测试只能在少数存在公开环境的领域中评估智能体。我们推出了 OccuBench，这是一个涵盖 10 个行业类别和 65 个专业领域的 100 个真实世界专业任务场景的基准测试。它通过语言环境模拟器（LESs）实现，该模拟器利用大语言模型（LLM）驱动的工具响应生成来模拟特定领域的环境。我们的多智能体合成流水线能够自动生成评估实例，并确保其可解性、经过校准的难度以及基于文档的多样性。OccuBench 从两个互补的维度评估智能体：跨专业领域的任务完成度，以及在受控故障注入（显式错误、隐式数据降级和混合故障）下的环境鲁棒性。我们评估了来自 8 个模型家族的 15 个前沿模型，并发现：(1) 没有单一模型能在所有行业中占据主导地位，因为每个模型都有独特的职业能力画像；(2) 隐式故障（数据截断、字段缺失）比显式错误（超时、500 错误）和混合故障更难处理，因为它们缺乏明显的错误信号，需要智能体自主检测数据降级；(3) 更大的模型、更新的世代以及更高的推理投入（reasoning effort）能持续提升性能。GPT-5.2 在从最小到最大推理投入下性能提升了 27.5 分；(4) 强大的智能体并不一定是强大的环境模拟器。模拟器质量对于基于 LES 的评估可靠性至关重要。OccuBench 提供了首个针对 AI 智能体在专业职业任务上的系统性跨行业评估。

## Abstract
AI agents are expected to perform professional work across hundreds of occupational domains (from emergency department triage to nuclear reactor safety monitoring to customs import processing), yet existing benchmarks can only evaluate agents in the few domains where public environments exist. We introduce OccuBench, a benchmark covering 100 real-world professional task scenarios across 10 industry categories and 65 specialized domains, enabled by Language Environment Simulators (LESs) that simulate domain-specific environments through LLM-driven tool response generation. Our multi-agent synthesis pipeline automatically produces evaluation instances with guaranteed solvability, calibrated difficulty, and document-grounded diversity. OccuBench evaluates agents along two complementary dimensions: task completion across professional domains and environmental robustness under controlled fault injection (explicit errors, implicit data degradation, and mixed faults). We evaluate 15 frontier models across 8 model families and find that: (1) no single model dominates all industries, as each has a distinct occupational capability profile; (2) implicit faults (truncated data, missing fields) are harder than both explicit errors (timeouts, 500s) and mixed faults, because they lack overt error signals and require the agent to independently detect data degradation; (3) larger models, newer generations, and higher reasoning effort consistently improve performance. GPT-5.2 improves by 27.5 points from minimal to maximum reasoning effort; and (4) strong agents are not necessarily strong environment simulators. Simulator quality is critical for LES-based evaluation reliability. OccuBench provides the first systematic cross-industry evaluation of AI agents on professional occupational tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《OccuBench: Evaluating AI Agents on Real-World Professional Tasks via Language Environment Simulation》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
AI 智能体在医疗分诊、核反应堆监控、海关处理等高价值专业领域的应用潜力巨大，但现有的评估基准（如 WebArena, SWE-bench）主要局限于 Web 浏览、代码或简单 API 调用。
*   **核心痛点**：
    *   **不可测试的领域**：大多数专业领域涉及企业内部系统，无公开 API，且真实操作具有不可逆的风险。
    *   **扩展成本极高**：为每个新领域构建真实的模拟环境需要巨大的工程投入。
    *   **缺乏鲁棒性评估**：现有基准多在“理想路径”下测试，忽略了真实世界中 API 超时、数据缺失等环境噪声。
*   **整体含义**：论文旨在通过“语言环境模拟”打破环境限制，系统性地评估 AI 智能体在 100 个真实专业场景下的职业能力和抗干扰能力。

### 2. 论文提出的方法论
核心思想是**语言环境模拟器（Language Environment Simulator, LES）**，即利用 LLM 本身存储的领域知识来模拟环境反馈。
*   **核心组件**：一个 LES 由配置 $c = (\text{系统提示词, 工具图谱, 初始状态, 状态描述})$ 定义。LLM 根据智能体的动作（工具调用）生成结构化的 JSON 响应，并维护隐含的环境状态。
*   **多智能体合成流水线**：
    1.  **场景生成**：基于 65 个专业领域的参考文档，生成非重叠的子话题。
    2.  **实例构建**：自动生成任务指令、工具定义、标准解法和评分细则（Rubric）。
    3.  **质量控制**：通过多次执行验证任务的“可解性”，过滤掉太简单（100% 成功）或不可解（0% 成功）的实例。
*   **故障注入机制**：在评估时向 LES 注入三类故障：
    *   **E1 (显式错误)**：如 HTTP 500、超时，有明确错误信号。
    *   **E2 (隐式降级)**：如数据截断、字段缺失，无错误信号，需智能体自主检测。
    *   **E3 (混合故障)**：上述两者的结合。

### 3. 实验设计
*   **场景与规模**：涵盖 10 个行业类别（商业、IT、工业、医疗、农业等），100 个专业场景，共 382 个经过验证的评估实例。
*   **Benchmark 构成**：
    *   **任务完成度 (CR)**：在干净环境（E0）下的成功率。
    *   **鲁棒性得分 (R)**：在故障环境（E1-E3）下的表现衰减情况。
*   **对比方法**：评估了 15 个前沿模型，包括 GPT-5.2、Claude 4/4.5/4.6 系列、Gemini 3.1 系列、Qwen 3.5 系列、DeepSeek V3.2、Kimi K2.5、GLM-5 等。

### 4. 资源与算力
*   **算力说明**：论文未明确指出训练模型所用的具体 GPU 型号和时长，因为该研究主要侧重于**评估**而非模型训练。
*   **评估成本**：实验涉及大量多轮对话推理。文中提到使用了 Gemini-3-Flash-Preview 作为默认的 LES 模拟器，并对比了 Qwen 3.5 Plus 和 GPT-5.2 作为模拟器的效果。

### 5. 实验数量与充分性
*   **实验规模**：对 15 个模型在 382 个实例上进行了全量测试，并针对故障注入（不同故障频率和持续时间）、推理投入（Reasoning Effort）进行了消融实验。
*   **充分性与公平性**：
    *   **跨行业覆盖**：实验涵盖了从核能安全到鱼塘水质控制的广泛领域，避免了单一领域的偏差。
    *   **模拟器一致性测试**：通过交叉验证不同模拟器对模型排名的影响（一致性达 85.7%），证明了 LES 评估方法的可靠性。
    *   **客观性**：采用基于 Rubric 的自动评分器，并结合多智能体修复模块确保基准本身的质量。

### 6. 论文的主要结论与发现
1.  **无全能模型**：GPT-5.2 在农业和科学领域领先，但在商业领域不如 Qwen 3.5 Plus；Claude 4.6 在运输领域表现优异。每个模型都有独特的“职业能力画像”。
2.  **隐式故障是“杀手”**：模型处理隐式数据降级（E2）的能力远弱于处理显式错误（E1）。因为隐式故障缺乏错误信号，大多数智能体会盲目接受错误数据导致失败。
3.  **规模与推理效应**：模型越大、世代越新，表现越好。特别是**推理投入（Thinking Effort）**的增加能显著提升性能（GPT-5.2 提升了 27.5 个百分点）。
4.  **强智能体 $\neq$ 强模拟器**：GPT-5.2 虽然作为智能体表现最强，但作为模拟器时质量较差（容易捏造规则或遗漏实体），说明模拟能力与执行能力是解耦的。

### 7. 优点
*   **开创性视角**：解决了专业领域因缺乏公开环境而无法评估的长期难题。
*   **低成本高扩展**：将环境构建从工程问题转变为配置问题，极大降低了 Benchmark 的开发门槛。
*   **深度鲁棒性测试**：首次系统性地引入了针对智能体工具调用过程中的隐式故障评估。
*   **真实职业映射**：每个场景对应真实的职业角色，使评估结果具有实际的行业参考价值。

### 8. 不足与局限
*   **逻辑模拟而非数据模拟**：LES 模拟的是业务逻辑（如：检查了什么），而非精确的数值计算（如：财务报表里的每一分钱是否绝对准确）。
*   **模拟器偏差风险**：评估结果高度依赖于模拟器 LLM 的水平。如果模拟器本身对某个领域有偏见或知识错误，会直接影响评估的公正性。
*   **应用限制**：目前主要针对文本/代码交互的工具环境，对于需要实时物理反馈或复杂多模态交互的场景支持有限。

（完）

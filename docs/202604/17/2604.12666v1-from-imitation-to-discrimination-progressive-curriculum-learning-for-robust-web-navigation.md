---
title: "From Imitation to Discrimination: Progressive Curriculum Learning for Robust Web Navigation"
title_zh: 从模仿到辨别：面向鲁棒网络导航的渐进式课程学习
authors: "Chuang Peng, Wei Zhang, Renshuai Tao, Xinhao Zhang, Jian Yang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12666v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 通过双智能体共识流水线合成数据
tldr: 针对文本Web智能体在复杂HTML环境下辨别力差和泛化性弱的问题，本文提出了Triton数据集（590k实例）和一种渐进式课程学习框架。通过结构语义硬负采样和双智能体共识机制构建高质量数据，并依次利用SFT、ORPO和GRPO训练模型。实验表明，Triton-GRPO-32B在Mind2Web任务上显著超越了GPT-4.5等闭源模型，证明了专业化数据课程在Web导航中的关键作用。
selection_source: fresh_fetch
motivation: 现有的监督微调方法在处理真实世界复杂HTML时，缺乏对相似干扰项的辨别能力且泛化性能有限。
method: 提出Triton数据集并采用从模仿到辨别再到长期一致性的渐进式课程学习，结合了硬负采样、ORPO偏好优化和GRPO强化学习。
result: "Triton-GRPO-32B在Mind2Web评测中达到58.7%的单步成功率，以超过16%的优势领先于GPT-4.5和Claude-4.5。"
conclusion: 针对Web导航任务，精心设计的专业化数据课程比单纯增加模型参数规模更为有效。
---

## 摘要
基于文本的网络智能体为自主网络导航提供了极高的计算效率，但由于真实世界 HTML 的噪声和异构特性，开发鲁棒的智能体仍具挑战。标准的监督微调（SFT）方法在两个关键维度上存在缺陷：一是缺乏辨别能力，无法拒绝密集页面中看似合理但错误的元素；二是对未见过的网站布局泛化能力有限。为了应对这些挑战，我们引入了 Triton 数据集（59 万个实例）和一种渐进式训练课程。Triton 通过结构-语义难负例挖掘（显式挖掘拓扑相似的干扰项）以及双智能体共识流水线（通过严格验证合成多样的跨域任务）构建而成。基于此，我们的渐进式课程产出了三个模型：用于基础模仿的 Triton-SFT-32B，通过优势比偏好优化（ORPO）实现鲁棒辨别的 Triton-ORPO-32B，以及通过群体相对策略优化（GRPO）实现长程一致性的 Triton-GRPO-32B。在 Mind2Web 上的实证评估表明，Triton-GRPO-32B 在开源模型中达到了最先进的性能，单步成功率达 58.7%，超过 GPT-4.5 (42.4%) 和 Claude-4.5 (41.4%) 16% 以上，验证了在网络导航任务中，专门的数据课程比单纯的参数规模更为重要。

## Abstract
Text-based web agents offer computational efficiency for autonomous web navigation, yet developing robust agents remains challenging due to the noisy and heterogeneous nature of real-world HTML. Standard Supervised Fine-Tuning (SFT) approaches fail in two critical dimensions: they lack discrimination capabilities to reject plausible but incorrect elements in densely populated pages, and exhibit limited generalization to unseen website layouts. To address these challenges, we introduce the Triton dataset (590k instances) and a progressive training curriculum. Triton is constructed via Structural-Semantic Hard Negative Mining, which explicitly mines topologically similar distractors, and a Dual-Agent Consensus pipeline that synthesizes diverse cross-domain tasks with strict verification. Building upon this foundation, our progressive curriculum produces three models: Triton-SFT-32B for basic imitation, Triton-ORPO-32B for robust discrimination via Odds Ratio Preference Optimization, and Triton-GRPO-32B for long-horizon consistency through Group Relative Policy Optimization. Empirical evaluation on Mind2Web demonstrates that Triton-GRPO-32B achieves state-of-the-art performance among open-source models with 58.7% Step Success Rate, surpassing GPT-4.5 (42.4%) and Claude-4.5 (41.4%) by over 16%, validating that specialized data curriculum outweighs raw parameter scale for web navigation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Triton** 的研究项目，旨在提升文本 Web 智能体在复杂真实世界环境中的导航鲁棒性。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管基于文本的 Web 智能体（Agent）比多模态模型更具计算效率，但在处理真实 HTML 页面时面临两大挑战：
    1.  **辨别力缺失**：标准监督微调（SFT）仅学习“如何操作”，导致模型在面对密集页面中相似的干扰元素时容易产生幻觉（选错目标）。
    2.  **泛化性受限**：模型难以适应未见过的网站布局和异构的 DOM 结构。
*   **核心目标**：通过构建高质量的辨别性数据集和渐进式训练课程，使中等规模的开源模型能够超越顶级的闭源大模型。

### 2. 方法论
论文提出了 **Triton 框架**，包含数据构建和训练课程两个核心部分：
*   **Triton 数据集构建（590k 实例）**：
    *   **结构-语义难负例挖掘**：利用 **DOM 树编辑距离（TED）** 计算拓扑相似度，结合属性杰卡德相似度（Jaccard Similarity），挖掘出结构最相似的干扰元素，强制模型进行细粒度推理。
    *   **反事实拒绝采样**：通过指令扰动（如更换不存在的实体或不匹配的操作），训练模型在目标缺失时输出特殊的 `None` 标记，从而划定决策边界。
    *   **双智能体共识流水线**：使用 Qwen3-Coder-480B 作为生成器合成任务，使用 Qwen3-32B 作为验证器进行“盲测”验证，只有两者预测一致的样本才被保留，确保了 96% 的人工验证合格率。
*   **渐进式训练课程**：
    *   **阶段 I (SFT)**：基础模仿学习，注入 HTML 语法和指令遵循知识。
    *   **阶段 II (ORPO)**：使用**优势比偏好优化（Odds Ratio Preference Optimization）**。通过模型自身的错误生成偏好对，惩罚高概率的辨别错误，无需额外的奖励模型。
    *   **阶段 III (GRPO)**：使用**群体相对策略优化（Group Relative Policy Optimization）**。通过分层奖励函数（格式奖励、元素选择奖励、F1 分数、完美匹配奖励）来增强长程执行的一致性。

### 3. 实验设计
*   **数据集与场景**：主要在 **Mind2Web** 基准测试上进行，该测试包含跨任务（Cross-Task）、跨网站（Cross-Website）和跨领域（Cross-Domain）三个难度递增的泛化子集。
*   **Benchmark 指标**：元素准确率（Ele. Acc）、操作 F1（Op. F1）、单步成功率（Step SR）以及综合评分（Composite Score）。
*   **对比方法**：
    *   **闭源模型**：GPT-4.5-preview、Claude-4.5、Gemini-2.5-pro、Grok-3 等。
    *   **开源模型**：DeepSeek-V3 (671B)、Qwen3 系列、Llama-3.1 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H200 GPU**（每张显存 141GB）。
*   **训练细节**：
    *   SFT 阶段：3 epoch，学习率 5e-5，Batch Size 128。
    *   ORPO 阶段：学习率 5e-6，Batch Size 64。
    *   GRPO 阶段：学习率 1e-6，群体规模 G=5。
    *   上下文长度：8192 tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，涵盖了数十个主流开源和闭源模型。
*   **消融实验**：非常充分。分别验证了难负例挖掘（+Dis）、合成数据（+Vis）、ORPO 优化和 GRPO 优化的增量贡献。
*   **分析深度**：包含了超参数敏感性分析（如负采样比例 K 和拒绝样本量 G 的影响）以及合成数据的 Scaling Law 分析。实验设计客观，通过跨领域子集严格评估了模型的泛化能力。

### 6. 主要结论与发现
*   **性能突破**：Triton-GRPO-32B 达到了 **58.7% 的单步成功率**，显著超过了 GPT-4.5 (42.4%) 和 Claude-4.5 (41.4%)。
*   **规模非唯一论**：仅有 32B 参数的 Triton 模型性能是 671B 参数的 DeepSeek-V3 (25.2%) 的两倍以上，证明了**专业化数据课程的质量优于单纯的参数规模**。
*   **课程有效性**：SFT 提供了基础知识，ORPO 减少了幻觉（辨别力），GRPO 提升了复杂任务的稳定性（一致性），三者具有累加效应。

### 7. 优点
*   **数据质量控制严苛**：引入双智能体共识机制，有效解决了自动合成数据中的幻觉问题。
*   **训练策略科学**：将辨别力训练（ORPO）和一致性训练（GRPO）解耦并顺序执行，符合人类学习规律。
*   **实用性强**：针对文本 Agent 的痛点（相似元素干扰）提出了基于拓扑结构的硬核解决方案。

### 8. 不足与局限
*   **模态缺失**：模型仅处理 HTML 文本，无法感知像素级信息（如颜色、空间遮挡或复杂的视觉验证码）。
*   **静态环境限制**：Mind2Web 基于静态快照，无法完全模拟真实网络中的动态交互（如网络延迟、实时弹窗）。
*   **计算开销**：GRPO 阶段需要进行多次轨迹采样，相比标准 SFT，训练时的计算资源消耗更高。

（完）

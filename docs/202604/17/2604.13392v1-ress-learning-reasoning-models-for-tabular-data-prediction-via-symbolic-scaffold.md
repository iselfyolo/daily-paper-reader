---
title: "ReSS: Learning Reasoning Models for Tabular Data Prediction via Symbolic Scaffold"
title_zh: ReSS：通过符号支架学习用于表格数据预测的推理模型
authors: "Chenlang Yi, Gang Li, Zizhan Xiong, Tue Minh Cao, Yanmin Gong, My T. Thai, Tianbao Yang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13392v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 可扩展的数据策展与生成推理路径
tldr: ReSS是一个结合符号逻辑与神经推理的框架，旨在提升表格数据预测的准确性与可解释性。它利用决策树提取决策路径作为“符号支架”，引导大语言模型生成符合逻辑的自然语言推理过程。通过这种方式构建高质量数据集并微调模型，辅以数据增强策略，ReSS在医疗和金融领域显著提升了预测性能，并确保了推理过程的忠实度与一致性。
selection_source: fresh_fetch
motivation: 针对表格数据预测中符号模型语义表达不足及大语言模型推理缺乏逻辑一致性的挑战，寻求一种兼顾逻辑严密性与语义表达能力的方案。
method: 利用决策树提取决策路径作为符号支架，引导LLM生成逻辑严密的推理文本，并通过支架不变的数据增强策略进行模型微调。
result: "在医疗和金融基准测试中，ReSS训练的模型比传统决策树和标准微调方法性能提升高达10%，且推理过程表现出更高的忠实度。"
conclusion: ReSS成功弥合了符号逻辑与神经推理的鸿沟，为高风险领域的表格数据预测提供了一种兼顾高精度与可解释性的有效框架。
---

## 摘要
表格数据在医疗和金融等高风险领域仍然普遍存在，在这些领域中，预测模型被期望同时提供高准确性以及忠实且人类可理解的推理。虽然符号模型提供了可验证的逻辑，但它们缺乏语义表达能力。与此同时，通用大语言模型（LLM）通常需要专门的微调才能掌握特定领域的表格推理。为了应对可扩展数据整理和推理一致性的双重挑战，我们提出了 ReSS，这是一个连接符号推理模型和神经推理模型的系统框架。ReSS 利用决策树模型提取实例级的决策路径作为符号支架。这些支架与输入特征和标签一起，引导 LLM 生成严格遵循底层决策逻辑的有据可依的自然语言推理。由此产生的高质量数据集被用于将预训练的 LLM 微调为专门的表格推理模型，并通过支架不变的数据增强策略进一步增强，以提高泛化能力和可解释性。为了严格评估忠实度，我们引入了包括幻觉率、解释必要性和解释充分性在内的定量指标。在医疗和金融基准测试上的实验结果表明，经 ReSS 训练的模型相比传统决策树和标准微调方法提升了高达 10%，同时产生了忠实且一致的推理。

## Abstract
Tabular data remains prevalent in high-stakes domains such as healthcare and finance, where predictive models are expected to provide both high accuracy and faithful, human-understandable reasoning. While symbolic models offer verifiable logic, they lack semantic expressiveness. Meanwhile, general-purpose LLMs often require specialized fine-tuning to master domain-specific tabular reasoning. To address the dual challenges of scalable data curation and reasoning consistency, we propose ReSS, a systematic framework that bridges symbolic and neural reasoning models. ReSS leverages a decision-tree model to extract instance-level decision paths as symbolic scaffolds. These scaffolds, alongside input features and labels, guide an LLM to generate grounded natural-language reasoning that strictly adheres to the underlying decision logic. The resulting high-quality dataset is used to fine-tune a pretrained LLM into a specialized tabular reasoning model, further enhanced by a scaffold-invariant data augmentation strategy to improve generalization and explainability. To rigorously assess faithfulness, we introduce quantitative metrics including hallucination rate, explanation necessity, and explanation sufficiency. Experimental results on medical and financial benchmarks demonstrate that ReSS-trained models improve traditional decision trees and standard fine-tuning approaches up to $10\%$ while producing faithful and consistent reasoning

---

## 论文详细总结（自动生成）

这是一份关于论文《ReSS: Learning Reasoning Models for Tabular Data Prediction via Symbolic Scaffold》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在医疗和金融等高风险领域，表格数据预测不仅要求高准确性，还要求模型提供**忠实（Faithful）且人类可理解的推理过程**。
*   **现有挑战：** 传统的符号模型（如决策树）逻辑严密但缺乏语义表达能力（仅是数值比较）；通用大语言模型（LLM）语义丰富但容易产生幻觉，且推理过程往往与最终预测缺乏逻辑一致性。
*   **研究目标：** 提出一种名为 **ReSS** 的框架，旨在结合符号逻辑的严谨性和神经模型的语义表达力，训练出既准又懂逻辑的表格推理模型。

### 2. 方法论：核心思想与关键技术
ReSS 的核心思想是利用决策树的路径作为“**符号支架（Symbolic Scaffold）**”来引导 LLM 的推理。
*   **符号支架提取：** 首先在表格数据上训练决策树，为每个正确预测的样本提取从根节点到叶节点的决策路径（如：`年龄 > 28.5 且 BMI > 9.65`）。
*   **推理数据策展：** 将原始特征、标签和符号支架输入给一个通用 LLM（如 GPT-4），引导其生成严格遵循支架逻辑的自然语言推理步骤（CoT）。
*   **支架不变的数据增强：** 为了提高泛化性，在保持决策路径和标签不变的前提下，对输入特征进行扰动（例如在阈值范围内改变数值或将非关键特征设为“未知”），生成更多样化的推理数据。
*   **模型微调：** 使用上述高质量的推理数据集，对预训练 LLM（如 Qwen-2.5-3B）进行监督微调（SFT），使其成为专门的表格推理模型。

### 3. 实验设计
*   **数据集：** 涵盖医疗和金融领域的四个真实数据集：
    *   **AD（阿尔茨海默病）**：三分类任务。
    *   **Diabetes（糖尿病）**、**Creditg（信用风险）**、**HomeLoan（房贷审批）**：二分类任务。
*   **对比方法（Baselines）：**
    *   传统机器学习：决策树、XGBoost。
    *   深度学习：TabNet、TabPFN。
    *   LLM 相关：直接微调（Direct SFT）、直接推理策展（DRC+SFT）、直接强化学习（Direct RL）。
*   **评估指标：** 除了准确率，还引入了**幻觉率（FEH/FVH/CH）**、**解释充分性**和**解释必要性**（通过掩码特征观察性能变化）来衡量推理的忠实度。

### 4. 资源与算力
*   **模型：** 使用 GPT-4.1 作为教师模型生成推理数据，使用 **Qwen-2.5-3B-Instruct** 作为基础模型进行微调。
*   **算力说明：** 论文提到了超参数设置（如 AdamW 优化器、学习率 1e-5、Batch Size 为 2、训练 20 轮等），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个数据集上进行了多次重复实验（3 个随机种子取平均），并进行了详尽的消融实验。
*   **充分性评估：** 实验设计较为充分。不仅对比了预测准确率，还深入探讨了：
    *   **领域知识的作用**：通过脱敏实验证明了 LLM 引入领域知识的价值。
    *   **SFT 与 RL 的对比**：探讨了强化学习在 ReSS 框架下的增益。
    *   **忠实度量化**：通过特征掩码实验验证了推理步骤是否真的是模型决策的依据。
*   **公平性：** 对比了不同规模的 LLM（如统一模型与特定任务模型），实验设置相对客观。

### 6. 主要结论与发现
*   **性能提升：** ReSS 训练的模型在准确率上一致优于传统决策树和标准微调方法，部分数据集提升高达 **10%**。
*   **高忠实度：** 幻觉率极低（特征存在和数值幻觉几乎为 0），且特征掩码实验证明生成的推理过程与模型决策具有强因果关系。
*   **知识修正：** 发现 ReSS 训练的 LLM 有时能利用领域知识修正决策树本身的错误逻辑。
*   **泛化性：** 支架不变的数据增强策略显著提升了模型在分布外数据上的表现。

### 7. 优点（亮点）
*   **逻辑与语义的融合：** 巧妙地利用决策树解决了 LLM 在表格数据上推理不透明、不一致的问题。
*   **自动化的数据生产：** 提供了一种可扩展的方法来获取高质量的领域推理数据，减少了对人类专家的依赖。
*   **严谨的评估体系：** 提出的“解释必要性”和“充分性”指标为推理模型的忠实度评估提供了新标准。

### 8. 不足与局限
*   **对决策树的依赖：** 如果初始决策树的性能极差，提取的符号支架可能具有误导性，限制了模型上限。
*   **教师模型依赖：** 推理质量高度依赖于生成数据的教师模型（如 GPT-4）的领域知识水平。
*   **数据集规模：** 虽然涵盖了关键领域，但实验的数据集数量（4 个）相对较少，且主要集中在中小规模表格数据上。
*   **实时性：** 生成长推理链（CoT）会增加推理阶段的计算开销和延迟。

（完）

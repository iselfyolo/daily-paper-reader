---
title: "EMGFlow: Robust and Efficient Surface Electromyography Synthesis via Flow Matching"
title_zh: EMGFlow：基于流匹配的鲁棒且高效的表面肌电信号合成
authors: "Boxuan Jiang, Chenyun Dai, Can Han"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13685v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 通过流匹配和生成模型进行合成数据生成
tldr: 针对表面肌电信号（sEMG）手势识别中数据稀缺和多样性不足的问题，本文提出了首个基于流匹配（Flow Matching）的条件生成框架EMGFlow。该方法克服了GAN训练不稳定和扩散模型推理效率低的问题，在三个基准数据集上展现了优于传统增强方法及生成模型的性能，为肌电控制系统提供了一种高效且稳健的数据增强方案。
selection_source: fresh_fetch
motivation: 现有的sEMG生成方法如GAN和扩散模型在训练稳定性或推理效率方面存在局限，难以有效解决数据稀缺和受试者多样性不足的问题。
method: 提出EMGFlow框架，首次将流匹配和连续时间生成模型应用于sEMG合成，并利用高级数值求解器优化生成动力学。
result: 在三个基准数据集上的评估显示，EMGFlow在保真度和下游任务效用上均优于GAN和扩散模型，且实现了更好的质量与效率权衡。
conclusion: 流匹配为解决肌电控制系统中的数据瓶颈提供了一个极具潜力且高效的新范式。
---

## 摘要
基于深度学习的表面肌电信号（sEMG）手势识别常受限于数据稀缺和受试者多样性不足。虽然利用生成对抗网络（GAN）和扩散模型生成合成数据已成为一种极具前景的数据增强策略，但这些方法往往在训练稳定性或推理效率方面面临挑战。为填补这一空白，我们提出了 EMGFlow，一个条件 sEMG 生成框架。据我们所知，这是首项探讨流匹配（Flow Matching, FM）和连续时间生成建模在 sEMG 领域应用的研究。为了在三个基准 sEMG 数据集上验证 EMGFlow，我们采用了一套统一的评估协议，整合了基于特征的保真度、分布几何以及下游效用。广泛的评估表明，EMGFlow 的性能优于传统的增强方法和 GAN 基准；在“合成训练-真实测试”（TSTR）协议下，其独立效用也强于本文对比的扩散模型基准。此外，通过先进的数值求解器和针对性的时间采样优化生成动力学，EMGFlow 实现了更优的质量与效率权衡。综上所述，这些结果表明流匹配是解决肌电控制系统中数据瓶颈的一种高效且极具前景的范式。我们的代码开源于：https://github.com/Open-EXG/EMGFlow。

## Abstract
Deep learning-based surface electromyography (sEMG) gesture recognition is frequently bottlenecked by data scarcity and limited subject diversity. While synthetic data generation via Generative Adversarial Networks (GANs) and diffusion models has emerged as a promising augmentation strategy, these approaches often face challenges regarding training stability or inference efficiency. To bridge this gap, we propose EMGFlow, a conditional sEMG generation framework. To the best of our knowledge, this is the first study to investigate the application of Flow Matching (FM) and continuous-time generative modeling in the sEMG domain. To validate EMGFlow across three benchmark sEMG datasets, we employ a unified evaluation protocol integrating feature-based fidelity, distributional geometry, and downstream utility. Extensive evaluations show that EMGFlow outperforms conventional augmentation and GAN baselines, and provides stronger standalone utility than the diffusion baselines considered here under the train-on-synthetic test-on-real (TSTR) protocol. Furthermore, by optimizing generation dynamics through advanced numerical solvers and targeted time sampling, EMGFlow achieves improved quality-efficiency trade-offs. Taken together, these results suggest that Flow Matching is a promising and efficient paradigm for addressing data bottlenecks in myoelectric control systems. Our code is available at: https://github.com/Open-EXG/EMGFlow.

---

## 论文详细总结（自动生成）

### 论文总结：EMGFlow —— 基于流匹配的鲁棒且高效的表面肌电信号合成

#### 1. 核心问题与整体含义（研究动机和背景）
表面肌电信号（sEMG）是实现人机交互和假肢控制的关键技术，但深度学习模型在这一领域面临严重的**数据瓶颈**：大规模采集成本高、标注困难，且受试者之间存在显著的个体差异。
*   **现有技术的局限：** 传统数据增强（如平移、加噪）多样性不足；生成对抗网络（GAN）训练不稳定且易发生模式崩溃；扩散模型（Diffusion Models）虽然质量高，但推理时需要数百步迭代，效率极低。
*   **研究目标：** 引入**流匹配（Flow Matching, FM）**技术，旨在构建一个既能稳定训练、又能高效生成高质量、高多样性 sEMG 信号的框架，解决肌电识别中的数据稀缺问题。

#### 2. 论文提出的方法论
EMGFlow 是首个将连续时间生成模型（流匹配）应用于 sEMG 合成的框架。
*   **核心思想：** 不同于扩散模型的加噪/去噪过程，流匹配学习的是一个**时间相关的速度场（Velocity Field）**，直接描述从简单先验分布（高斯噪声）到复杂数据分布（真实 sEMG）的连续运输动力学。
*   **关键技术细节：**
    *   **条件流匹配目标函数：** 通过线性插值路径 $x_t = (1-t)x_0 + tx_1$ 定义概率路径，训练网络 $v_\theta$ 预测目标速度 $(x_1 - x_0)$。
    *   **AdaGN 条件注入：** 采用自适应组归一化（Adaptive GroupNorm）将手势标签信息注入 U-Net 骨干网络，增强类别一致性。
    *   **Logit-Normal 时间采样：** 在训练时非均匀地采样时间点 $t$，重点优化轨迹中间阶段，提高生成质量。
    *   **ODE 求解器推理：** 在推理阶段，通过数值求解常微分方程（ODE）生成样本。支持 Euler、Heun 和 RK4 等多种求解器，实现生成精度与速度的权衡。
    *   **无分类器指导（CFG）：** 引入指导权重 $w$ 来调节生成样本的类别倾向性。

#### 3. 实验设计
*   **数据集：** 使用了 Ninapro 项目的三个基准数据集：**DB2**（40名受试者，49种手势）、**DB4**（10名受试者，52种手势）、**DB7**（20名受试者，40种手势）。
*   **评估协议：**
    1.  **数据增强（Augmentation）：** 合成数据 + 真实数据共同训练，测试真实数据。
    2.  **合成训练-真实测试（TSTR）：** 仅用合成数据训练分类器，在真实数据上测试（衡量独立效用的严格指标）。
*   **对比方法（Baselines）：**
    *   **传统增强：** Jitter, Scale, Mixup, Freq-Mask 等。
    *   **生成模型：** WGAN-GP, DDPM（1000步）, DDIM（50步）, PatchEMG。
*   **评估指标：** 准确率（ACC）、F1分数、FID（保真度）、IS（多样性）、CAS（类别准确率分数）以及 PRDC（精确度、召回率、密度、覆盖率）。

#### 4. 资源与算力
*   **训练配置：** 使用 Adam 优化器，学习率 $5 \times 10^{-4}$，Batch Size 为 128，总训练步数为 20,000 步。使用了指数移动平均（EMA）技术稳定模型权重。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号和数量。但提到模型采用了轻量化的 1D U-Net 架构，且在推理效率对比中给出了 FLOPs 和每秒生成样本数，显示其在 20-40 步 NFE（网络函数评估次数）下即可达到优异性能。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 3 个大型数据集，涉及 70 名受试者的个体化模型训练与验证。
*   **消融实验：** 进行了极其详尽的消融研究，包括：
    *   不同 ODE 求解器（Euler vs Heun vs RK4）的对比。
    *   时间采样策略（Uniform vs Logit-Normal）的影响。
    *   条件注入接口（AdaGN vs Add vs Concat）的优劣。
    *   无分类器指导（CFG）权重对保真度与下游效用之间权衡的深入分析。
*   **客观性：** 采用了统一的骨干网络对比 FM 和扩散模型，确保了公平性；使用了统计显著性检验（Wilcoxon 符号秩检验）来验证结果。

#### 6. 论文的主要结论与发现
*   **性能领先：** EMGFlow 在 TSTR 协议下显著优于 GAN 和加速后的扩散模型（DDIM），在数据增强任务中也达到了顶尖水平。
*   **效率优势：** 相比 DDPM，EMGFlow 在推理速度上提升了约 25-50 倍，且在仅 10-20 步 NFE 下的保真度即可超越 50 步的 DDIM。
*   **保真度-效用张力：** 发现过高的 CFG 权重虽然能提高样本的视觉保真度和类别区分度，但会因压缩分布覆盖范围（Coverage）而损害下游分类任务的性能。
*   **设计偏好：** Logit-Normal 采样和 Heun 求解器被证明是 sEMG 生成任务中的最佳实践组合。

#### 7. 优点：方法或实验设计上的亮点
*   **首创性：** 首次将流匹配引入生理信号处理领域，填补了连续时间生成模型在 sEMG 应用中的空白。
*   **评估全面：** 不仅关注分类准确率，还引入了 PRDC 等几何指标深入剖析生成分布的质量，建立了 sEMG 生成的统一评估范式。
*   **实用性强：** 解决了扩散模型推理慢的痛点，使得生成式增强在实时或资源受限的肌电控制系统中更具可行性。

#### 8. 不足与局限
*   **实验覆盖：** 目前仅验证了“受试者内（Within-subject）”场景，尚未探讨跨受试者或跨 session 的泛化增强效果。
*   **窗口限制：** 生成基于固定长度的滑动窗口，未涉及长时序、全过程的信号合成。
*   **特征提取器依赖：** FID 等指标依赖于预训练的分类器特征空间，可能存在一定的归纳偏置。
*   **应用范围：** 尚未在实际的假肢控制或实时人机交互系统中进行在线验证。

（完）

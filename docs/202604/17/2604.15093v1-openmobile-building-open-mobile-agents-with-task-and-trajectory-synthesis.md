---
title: "OpenMobile: Building Open Mobile Agents with Task and Trajectory Synthesis"
title_zh: OpenMobile：通过任务与轨迹合成构建开放式移动智能体
authors: "Kanzhi Cheng, Zehao Li, Zheng Ma, Nuo Chen, Jialin Cao, Qiushi Sun, Zichen Ding, Fangzhi Xu, Hang Yan, Jiajun Chen, Anh Tuan Luu, Jianbing Zhang, Lewei Lu, Dahua Lin"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15093v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 合成高质量任务指令和智能体轨迹的框架
tldr: 针对移动智能体训练数据封闭且合成方法不透明的问题，本文提出开源框架 OpenMobile。该框架包含可扩展的任务合成流水线，通过环境探索构建全局记忆并生成多样化指令；同时采用策略切换策略进行轨迹生成，通过交替使用学习者和专家模型捕获关键的错误恢复数据。实验表明，基于该数据微调的 Qwen 系列模型在 AndroidWorld 等基准测试中表现优异，显著超越现有开源方案，且证明了性能提升源于功能覆盖而非过拟合。
selection_source: fresh_fetch
motivation: 现有的高性能移动智能体训练数据和合成方法大多不透明，限制了开源社区的研究进展。
method: 提出包含全局环境记忆的任务合成流水线，并结合学习者与专家模型的策略切换机制来生成包含错误恢复能力的轨迹。
result: "微调后的 Qwen3-VL 在 AndroidWorld 上达到 64.7% 的成功率，远超现有开源数据训练的模型。"
conclusion: OpenMobile 通过开源高质量合成数据和方法，有效弥补了移动智能体领域的数据鸿沟，并验证了广泛功能覆盖对提升性能的重要性。
---

## 摘要
由视觉语言模型驱动的移动智能体在自动化移动任务方面展现出卓越的能力，近期领先的模型实现了显著的性能飞跃，例如在 AndroidWorld 上达到了近 70% 的成功率。然而，这些系统的训练数据并不公开，且其任务与轨迹合成的方法也缺乏透明度。我们提出了 OpenMobile，这是一个合成高质量任务指令和智能体轨迹的开源框架，包含两个核心组件：(1) 第一个是可扩展的任务合成流水线，它通过探索构建全局环境记忆，并利用该记忆生成多样化且有据可依的指令；(2) 第二个是用于轨迹展开的策略切换策略。通过在学习者模型和专家模型之间交替，该策略能够捕获标准模仿学习中经常缺失的关键错误恢复数据。在我们的数据上训练的智能体在三个动态移动智能体基准测试中取得了具有竞争力的结果：值得注意的是，我们微调后的 Qwen2.5-VL 和 Qwen3-VL 在 AndroidWorld 上分别达到了 51.7% 和 64.7% 的成功率，远超现有的开源数据方法。此外，我们对合成指令与基准测试集之间的重叠进行了透明化分析，并验证了性能提升源于广泛的功能覆盖，而非对基准测试的过拟合。我们在 https://njucckevin.github.io/openmobile/ 发布了数据和代码，旨在弥补数据差距并促进更广泛的移动智能体研究。

## Abstract
Mobile agents powered by vision-language models have demonstrated impressive capabilities in automating mobile tasks, with recent leading models achieving a marked performance leap, e.g., nearly 70% success on AndroidWorld. However, these systems keep their training data closed and remain opaque about their task and trajectory synthesis recipes. We present OpenMobile, an open-source framework that synthesizes high-quality task instructions and agent trajectories, with two key components: (1) The first is a scalable task synthesis pipeline that constructs a global environment memory from exploration, then leverages it to generate diverse and grounded instructions. and (2) a policy-switching strategy for trajectory rollout. By alternating between learner and expert models, it captures essential error-recovery data often missing in standard imitation learning. Agents trained on our data achieve competitive results across three dynamic mobile agent benchmarks: notably, our fine-tuned Qwen2.5-VL and Qwen3-VL reach 51.7% and 64.7% on AndroidWorld, far surpassing existing open-data approaches. Furthermore, we conduct transparent analyses on the overlap between our synthetic instructions and benchmark test sets, and verify that performance gains stem from broad functionality coverage rather than benchmark overfitting. We release data and code at https://njucckevin.github.io/openmobile/ to bridge the data gap and facilitate broader mobile agent research.

---

## 论文详细总结（自动生成）

### 论文总结：OpenMobile - 构建开放式移动智能体

#### 1. 核心问题与整体含义（研究动机和背景）
当前移动智能体（Mobile Agents）领域存在严重的“性能鸿沟”：闭源的工业界模型（如 Step-GUI, MAI-UI）在 AndroidWorld 等基准测试上已接近 70% 的成功率，而开源社区模型仍停留在 30% 左右。这种差距的主因在于**高质量训练数据的封闭性**以及**数据合成方案的不透明**。现有的开源数据集（如 AndroidControl）往往存在标注噪声大、缺乏复杂推理逻辑等问题。OpenMobile 旨在通过开源一套完整的任务与轨迹合成框架，弥补这一数据缺口，让开源社区能够复现并超越现有的领先水平。

#### 2. 方法论：核心思想与关键技术
OpenMobile 框架由两个核心阶段组成：

*   **可扩展的任务合成（Scalable Task Synthesis）：**
    *   **解耦探索与生成：** 不同于以往从单一轨迹生成指令的耦合方式，该方法先通过探索（如随机游走）构建**全局环境记忆（Global Environment Memory, M）**。
    *   **记忆构建：** 对屏幕进行视觉去重（感知哈希），利用强 VLM 标注 UI 功能，并建立语义索引。
    *   **记忆增强合成：** 在生成指令时，同时引入当前屏幕、短期记忆（邻近屏幕功能）和长期记忆（语义相关的跨屏幕功能），从而合成出复杂、多步且在环境中真实可行的指令。
*   **策略切换轨迹展开（Policy-Switching Rollout）：**
    *   **解决分布偏移：** 传统的专家蒸馏只提供完美轨迹，导致模型在推理出错时无法恢复。
    *   **错误干预切换（Error-Intervention Switch）：** 在轨迹生成过程中，由学习者模型（Learner）先行，监控器实时检测偏差。一旦发现学习者偏离目标，立即切换到专家模型（Expert）进行纠正。这种方式捕获了大量的“错误-诊断-修正”信号，显著增强了智能体的鲁棒性。

#### 3. 实验设计
*   **数据集与场景：** 涵盖了 20 个 Android 应用，合成了 2.8K 条指令和 34K 个动作步骤。
*   **基准测试（Benchmarks）：**
    *   **AndroidWorld：** 行业标准的动态评估环境。
    *   **AndroidLab：** 系统性的移动智能体基准。
    *   **MobileWorld：** 侧重于长程任务和跨应用协作的挑战性基准。
*   **对比方法：**
    *   **商业模型：** GPT-4o, Gemini-3-Pro。
    *   **开源权重模型：** Qwen2.5-VL, Qwen3-VL, UI-Venus, Step-GUI 等。
    *   **开源数据模型：** UI-S1, ScaleCUA。

#### 4. 资源与算力
*   **模型微调：** 使用 LLaMA-Factory 框架，对 Qwen2.5-VL-7B 和 Qwen3-VL-8B 进行全参数微调。
*   **训练参数：** Batch size 为 32，学习率 1e-5，训练 3 个 epoch。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长，但提到在 AndroidWorld 模拟器环境下进行大规模轨迹展开。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了主性能对比、消融实验、数据缩放实验（Data Scaling）以及错误恢复能力分析。
*   **消融实验：** 针对任务合成策略（对比 OS-Genesis 和耦合流水线）和轨迹展开策略（对比专家蒸馏、自我进化、随机切换）做了详细对比。
*   **客观性与公平性：** 实验采用了 Pass@1 和 Pass@3 指标，并对动态环境下的随机性进行了多次运行取均值（Report mean ± half-range），确保了结果的可信度。此外，论文专门进行了**数据污染分析**，通过语义相似度检测证明性能提升并非来自对测试集的过拟合。

#### 6. 主要结论与发现
*   **性能突破：** 基于 OpenMobile 数据训练的 Qwen3-VL-8B 在 AndroidWorld 上达到 64.7%，远超此前开源数据模型的 30% 水平，接近闭源领先系统。
*   **错误恢复是关键：** 策略切换生成的轨迹提供了关键的纠错信号，实验证明这比单纯的专家蒸馏更能提升智能体在真实环境中的成功率。
*   **功能覆盖驱动泛化：** 性能的提升主要源于合成数据对应用功能的广泛覆盖（Functionality Coverage），而非简单的指令重排。

#### 7. 优点
*   **方法论创新：** 提出了“解耦探索与合成”的思路，利用全局记忆生成跨屏幕的复杂任务，提升了指令的多样性和难度。
*   **实用性强：** 策略切换机制有效解决了模仿学习中的分布偏移问题，且易于扩展到其他 GUI 领域。
*   **透明度高：** 论文对数据重叠和污染问题进行了极其详尽的定量分析，树立了开源数据集研究的标杆。

#### 8. 不足与局限
*   **探索策略单一：** 目前主要依赖随机游走进行环境探索，对于深层或隐藏功能的挖掘效率可能有限。
*   **强化学习效果不佳：** 文中提到尝试了轨迹级 RL，但相比 SFT 提升不明显，显示出在动态 GUI 环境下进行 RL 训练仍具挑战。
*   **算力细节缺失：** 缺乏具体的训练资源消耗统计，不便于其他研究者评估复现成本。

（完）

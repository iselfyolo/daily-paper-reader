---
title: "UniToolCall: Unifying Tool-Use Representation, Data, and Evaluation for LLM Agents"
title_zh: UniToolCall：大语言模型智能体工具调用表示、数据与评估的统一框架
authors: "Yijuan Liang, Xinghao Chen, Yifan Ge, Ziyi Wu, Hao Wu, Changyu Zeng, Wei Xing, Xiaoyu Shen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11557v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 标准化LLM智能体的数据集生成
tldr: UniToolCall是一个统一的大模型工具调用框架，旨在解决交互表示不一致、轨迹结构分布失衡及评估基准不兼容的问题。该框架构建了包含2.2万个工具和39万条语料的训练集，涵盖单/多跳、串/并行等复杂模式，并引入锚点链接机制增强多轮推理。通过统一QAOA评估体系，微调后的Qwen3-8B在多项指标上超越了GPT-4o等主流商业模型。
selection_source: fresh_fetch
motivation: 针对大模型工具调用中存在的交互表示不统一、轨迹结构分布失衡以及评估基准不兼容等核心挑战。
method: 提出UniToolCall框架，通过构建大规模工具池、合成结构化轨迹、引入锚点链接机制并统一QAOA评估标准来标准化工具学习全流程。
result: "基于该框架微调的Qwen3-8B模型在Hybrid-20等复杂场景下表现优异，单轮严格准确率达到93.0%，超越了GPT-4o等顶级商业模型。"
conclusion: UniToolCall通过标准化表示、数据和评估，显著提升了开源模型在复杂工具调用场景下的性能，为构建高性能LLM Agent提供了系统性方案。
---

## 摘要
工具调用能力是大语言模型（LLM）智能体的核心组成部分，使其能够通过结构化函数调用与外部系统进行交互。然而，现有研究在交互表示上存在不一致性，在很大程度上忽视了工具调用轨迹的结构分布，且依赖于互不兼容的评估基准。我们提出了 UniToolCall，这是一个统一的工具学习框架，标准化了从工具集构建、数据集生成到评估的整个流程。该框架策划了一个包含 2.2 万多个工具的大型工具池，并通过将 10 个标准化的公开数据集与结构受控的合成轨迹相结合，构建了一个包含 39 万多个实例的混合训练语料库。它显式地建模了多种交互模式，包括单跳与多跳、单轮与多轮，同时捕捉了串行和并行执行结构。为了支持连贯的多轮推理，我们进一步引入了锚点链接（Anchor Linkage）机制，以强化跨轮次依赖关系。此外，我们将 7 个公开基准转换为统一的“查询-动作-观测-回答”（QAOA）表示，并在函数调用、轮次和对话层面进行细粒度评估。实验表明，在我们的数据集上对 Qwen3-8B 进行微调可显著提升工具调用性能。在干扰项密集的 Hybrid-20 设置下，该模型实现了 93.0% 的单轮严格精确度（Strict Precision），优于包括 GPT、Gemini 和 Claude 在内的商业模型。

## Abstract
Tool-use capability is a fundamental component of LLM agents, enabling them to interact with external systems through structured function calls. However, existing research exhibits inconsistent interaction representations, largely overlooks the structural distribution of tool-use trajectories, and relies on incompatible evaluation benchmarks. We present UniToolCall, a unified framework for tool learning that standardizes the entire pipeline from toolset construction and dataset generation to evaluation. The framework curates a large tool pool of 22k+ tools and constructs a hybrid training corpus of 390k+ instances by combining 10 standardized public datasets with structurally controlled synthetic trajectories. It explicitly models diverse interaction patterns, including single-hop vs. multi-hop and single-turn vs. multi-turn, while capturing both serial and parallel execution structures. To support coherent multi-turn reasoning, we further introduce an Anchor Linkage mechanism that enforces cross-turn dependencies. Furthermore, we convert 7 public benchmarks into a unified Query--Action--Observation--Answer (QAOA) representation with fine-grained evaluation at the function-call, turn, and conversation levels. Experiments show that fine-tuning Qwen3-8B on our dataset substantially improves tool-use performance. Under the distractor-heavy Hybrid-20 setting, achieves 93.0% single-turn Strict Precision, outperforming commercial models including GPT, Gemini, and Claude.

---

## 论文详细总结（自动生成）

### 论文总结：UniToolCall - 统一 LLM 智能体工具调用的表示、数据与评估

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出当前大语言模型（LLM）在工具调用（Tool-use）领域存在严重的**碎片化问题**：
*   **表示不一致**：不同数据集采用互不兼容的 Schema 来编码工具调用、参数和观测结果，导致联合训练困难。
*   **结构建模不足**：现有数据生成流程往往忽视了执行结构的复杂性，尤其是串行（Serial）与并行（Parallel）调用模式的区别。
*   **评估基准失配**：各基准测试的协议、工具定义和评估脚本各异，无法进行公平且可复现的跨数据集对比。
**UniToolCall** 旨在通过建立一个统一的框架，标准化从工具集构建、数据合成到细粒度评估的全流程。

#### 2. 方法论：核心思想与关键技术
UniToolCall 提出了一个标准化的工具学习流水线，核心组件包括：
*   **统一表示 (QAOA)**：将所有交互标准化为“查询（Query）– 动作（Action）– 观测（Observation）– 回答（Answer）”格式。
*   **大规模工具池构建**：整合学术基准、MCP（Model Context Protocol）服务器及构建数据集，形成包含 **22,000+** 个工具的资源池，并按 6 个功能类别和 13 个应用领域进行分类。
*   **结构化数据合成引擎**：
    *   **多维度建模**：显式控制单/多跳（Single/Multi-hop）、单/多轮（Single/Multi-turn）以及串行/并行执行结构。
    *   **锚点链接机制 (Anchor Linkage)**：在多轮对话中，强制要求当前轮次的查询必须依赖前一轮工具产生的状态变量（如交易 ID），以解决现有数据中上下文脱节的问题。
*   **混合训练语料**：结合了 10 个经过清洗转换的公开数据集（38.7万条）和高质量合成轨迹（约3000条），总计约 **39万** 个实例。

#### 3. 实验设计
*   **模型与基准**：以 **Qwen3-8B** 为基础模型进行微调。
*   **对比方法**：包括商业模型（GPT-5.2 Instant, Gemini 3 Flash, Claude 4.6 Sonnet）和开源模型（DeepSeek-V3.2, Qwen3-32B, Kimi-K2-Instruct）。
*   **评估场景**：
    *   **Hybrid-20 设置**：在候选工具列表中包含 1 个目标工具和 19 个干扰项（Hard/Easy Negatives），模拟真实检索环境。
    *   **统一基准 Dtest**：整合了 BFCL v3、ACEBench、HammerBench 等 7 个公开基准，统一转换为 QAOA 格式。
*   **评估指标**：采用严格精确度（SP）、灵活精确度（FP）、严格参数准确率（SPA）和灵活参数准确率（FPA）。

#### 4. 资源与算力
*   **硬件环境**：使用了单节点 **4 × NVIDIA A800-SXM4 (40GB)** GPU，配以 Intel Xeon Platinum 8378A CPU。
*   **训练细节**：基于 LLaMA-Factory 框架，使用 **LoRA** 策略（r=8, α=16），训练 1 个 epoch，最大序列长度 8192 轮次，有效 Batch Size 为 8。
*   **合成成本**：合成数据由 Qwen3-32B 生成并进行自我评估。

#### 5. 实验数量与充分性
*   **实验规模**：训练集包含 39 万实例，测试集包含 6,163 条高质量对话。
*   **消融实验**：设计了三组核心消融实验：
    1.  对比公开数据与合成数据的结构分布（串并行比例）。
    2.  验证合成数据中混合不同复杂结构的必要性。
    3.  验证“锚点链接机制”对多轮对话质量的提升。
*   **充分性评价**：实验覆盖了从单跳到长程多轮的多种复杂度，且在 Hybrid-20 这种高难度干扰设置下进行，对比了当前最顶尖的商业模型，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能突破**：微调后的 Qwen3-8B 在单轮严格精确度上达到 **93.0%**，显著超过了 GPT-5.2 和 Claude 4.6 等商业大模型。
*   **结构化训练的价值**：合成数据虽然规模较小，但通过显式注入串行依赖和并行逻辑，显著增强了模型在复杂多跳场景下的推理能力。
*   **抗干扰能力**：UniToolCall 框架显著提升了模型在存在大量无关工具干扰时的定位准确性。

#### 7. 优点
*   **标准化贡献**：首次系统性地统一了工具调用的表示、数据和评估协议，解决了行业碎片化痛点。
*   **高质量合成**：引入锚点链接机制，解决了合成多轮对话中常见的逻辑断层问题。
*   **细粒度评估**：提供了从函数调用级到对话级的多层次评估指标，比单一的成功率更具参考价值。

#### 8. 不足与局限
*   **上下文长度限制**：受限于 8192 标记的长度，无法处理极长程的交互或包含海量输出（如长文档）的工具调用。
*   **模型规模覆盖**：主要在 8B 轻量级模型上验证，尚未系统探讨该框架在 70B+ 超大规模模型上的扩展效应（Scaling Law）。
*   **评估偏差风险**：由于对原始基准进行了严格的过滤和格式转换，可能无法完全保留原始任务的所有特定特征。

（完）

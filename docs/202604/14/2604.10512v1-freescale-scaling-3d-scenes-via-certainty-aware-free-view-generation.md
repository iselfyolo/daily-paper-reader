---
title: "FreeScale: Scaling 3D Scenes via Certainty-Aware Free-View Generation"
title_zh: FreeScale：通过确定性感知的自由视角生成实现 3D 场景扩展
authors: "Chenhan Jiang, Yu Chen, Qingwen Zhang, Jifei Song, Songcen Xu, Dit-Yan Yeung, Jiankang Deng"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10512v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 将真实图像转换为高质量训练数据的框架
tldr: FreeScale 旨在解决通用新视角合成（NVS）模型训练数据匮乏的问题。该框架利用场景重建技术，将有限的真实图像序列转化为大规模、高质量的训练数据。其核心是提出一种确定性感知（certainty-aware）的自由视角采样策略，通过识别语义合理且重建误差小的视角，有效克服了重建伪影对数据质量的影响。实验表明，FreeScale 显著提升了 NVS 模型的泛化能力，并在 3D 高斯泼溅（3DGS）优化中表现出色。
selection_source: fresh_fetch
motivation: 现有的 NVS 训练数据面临真实场景稀疏离散和合成数据存在领域偏差的局限性。
method: 提出一种确定性感知的自由视角采样策略，从不完美的重建场景中筛选出高质量、低误差的新视角图像。
result: 在具有挑战性的分布外基准测试中，将 NVS 模型的 PSNR 提升了 2.7 dB，并增强了 3DGS 的优化效果。
conclusion: FreeScale 为 3D 视觉领域提供了一个强大且实用的数据生成引擎，有效突破了高质量训练数据的瓶颈。
---

## 摘要
可泛化新视角合成（NVS）模型的发展受到具有多样化且精确相机轨迹的大规模训练数据稀缺的严重限制。虽然真实世界的捕捉具有照片级的真实感，但它们通常是稀疏且离散的。相反，合成数据虽然易于扩展，但存在领域差距，且往往缺乏真实的语义。我们提出了 FreeScale，这是一个利用场景重建能力将有限的真实世界图像序列转化为可扩展的高质量训练数据源的新型框架。我们的核心见解是，一个不完美的重建场景可以作为一个丰富的几何代理，但盲目地从中采样会放大伪影。为此，我们提出了一种确定性感知的自由视角采样策略，用于识别既具有语义意义又受重建误差影响最小的新视角。我们通过扩大前馈 NVS 模型的训练规模证明了 FreeScale 的有效性，在具有挑战性的分布外（out-of-distribution）基准测试中实现了 2.7 dB 的显著 PSNR 提升。此外，我们展示了生成的数据可以主动增强单场景 3D 高斯泼溅（3D Gaussian Splatting）优化，从而在多个数据集上实现一致的改进。我们的工作提供了一个实用且强大的数据生成引擎，以克服 3D 视觉中的一个根本性瓶颈。项目主页：https://mvp-ai-lab.github.io/FreeScale。

## Abstract
The development of generalizable Novel View Synthesis (NVS) models is critically limited by the scarcity of large-scale training data featuring diverse and precise camera trajectories. While real-world captures are photorealistic, they are typically sparse and discrete. Conversely, synthetic data scales but suffers from a domain gap and often lacks realistic semantics. We introduce FreeScale, a novel framework that leverages the power of scene reconstruction to transform limited real-world image sequences into a scalable source of high-quality training data. Our key insight is that an imperfect reconstructed scene serves as a rich geometric proxy, but naively sampling from it amplifies artifacts. To this end, we propose a certainty-aware free-view sampling strategy identifying novel viewpoints that are both semantically meaningful and minimally affected by reconstruction errors. We demonstrate FreeScale's effectiveness by scaling up the training of feedforward NVS models, achieving a notable gain of 2.7 dB in PSNR on challenging out-of-distribution benchmarks. Furthermore, we show that the generated data can actively enhance per-scene 3D Gaussian Splatting optimization, leading to consistent improvements across multiple datasets. Our work provides a practical and powerful data generation engine to overcome a fundamental bottleneck in 3D vision. Project page: https://mvp-ai-lab.github.io/FreeScale.

---

## 论文详细总结（自动生成）

这是一份关于论文《FreeScale: Scaling 3D Scenes via Certainty-Aware Free-View Generation》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：通用新视角合成（NVS）模型（如前馈模型）的性能高度依赖于大规模、多样化且具有精确相机轨迹的训练数据。然而，现实世界的捕捉通常是稀疏且离散的，而合成数据（如 Blender 生成）虽然易于扩展，但存在严重的“合成-真实”领域差距（Domain Gap）且缺乏真实语义。
*   **核心问题**：如何利用现有的稀疏真实场景数据，自动生成高质量、高多样性且具有精确位姿标注的“自由视角”图像，以突破 3D 视觉任务的数据瓶颈？
*   **整体含义**：FreeScale 作为一个“数据引擎”，通过场景重建技术将离散的图像序列转化为连续的 3D 表示，并从中智能采样高质量的新视角数据，用于增强前馈模型的泛化能力和单场景的重建精度。

### 2. 方法论：核心思想与技术细节
FreeScale 的核心思想是**“确定性感知采样”**，旨在从不完美的重建中提取最有价值的信息。其流程分为三个阶段：
*   **确定性网格（Certainty Grid）构建**：
    *   首先使用 3D 高斯泼溅（3DGS）重建场景。
    *   将场景空间体素化，定义每个体素的确定性得分 $C(v_i)$。该得分通过累加落入体素内的高斯原型的透明度（Opacity）与其体积（Volume）的比值来计算。高得分区域代表重建质量较好、几何结构明确的区域。
*   **基于视角图（View Graph）的采样与筛选**：
    *   **位姿生成**：预定义 10 种相机轨迹模式（如轨道、螺旋、俯冲等），在场景中放置数千个候选位姿。
    *   **视角图构建**：利用确定性网格计算各视角间的“加权交并比”（WIoU），衡量视角间的几何重叠和信息增量。
    *   **筛选**：通过非极大值抑制（NMS）和视角图关联，剔除冗余视角，保留信息量大且多样性高的候选视角。
*   **图像修复与校正（Rectification）**：
    *   使用单步扩散模型（如 DIFIX3D）对渲染出的“噪声”图像进行增强，提升照片级真实感。
    *   **创新点**：利用视角图而非简单的欧氏距离来选择扩散模型的参考图像，确保参考图与目标图有真实的几何重叠，减少伪影。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV-10K（包含约 1,900 个场景）。
    *   **测试/泛化集**：MipNeRF360、Tanks & Temples (tnt)、Nerfbusters。
*   **Benchmark 与任务**：
    *   **前馈模型增强**：以 LVSM 为基准，对比加入 FreeScale 生成数据前后的性能。
    *   **单场景优化增强**：对比 3DGS 原型、DIFIX3D+、Nerfbusters 等方法。
*   **对比指标**：PSNR、SSIM、LPIPS 以及推理/训练时间。

### 4. 资源与算力
*   **算力配置**：
    *   训练前馈模型（LVSM）使用了 **4 张 NVIDIA A40 GPU**。
    *   训练迭代次数为 20,000 次。
*   **训练细节**：采用了余弦学习率调度，初始学习率为 $4 \times 10^{-4}$，包含 3,000 次迭代的预热期。
*   **效率**：FreeScale 的运行时间与 3DGS 基准相当（约 37 分钟），远快于需要多次扩散推理的 DIFIX3D+（约 81 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：论文在数千个场景上进行了大规模训练，并在多个不同分布的数据集上验证了泛化性。
*   **消融实验**：非常充分。作者分别验证了：
    1.  确定性网格的作用；
    2.  视角图对冗余过滤和参考图选择的贡献；
    3.  扩散模型修复步骤的必要性；
    4.  不同相机轨迹模式的影响；
    5.  对数据稀疏性的鲁棒性（即使初始数据仅 5% 也能提升性能）。
*   **客观性**：通过 OOD（分布外）测试和“大尺度相机运动”协议，客观地评估了模型在极端情况下的表现，证明了性能提升并非来自过拟合。

### 6. 主要结论与发现
*   **性能飞跃**：在前馈模型训练中加入 22% 的 FreeScale 生成数据，在大型相机运动场景下实现了 **2.7 dB 的 PSNR 提升**。
*   **泛化能力**：FreeScale 生成的数据能显著增强模型对未见场景（OOD）的理解，减少模糊感。
*   **优化增强**：在单场景 3DGS 优化中，FreeScale 能有效填补未观察区域的几何空洞，减少“浮点伪影”（floaters）。
*   **课程学习**：发现配合视角图进行“由易到难”的课程学习（先学高重叠视角，再学低重叠视角）能进一步稳定训练。

### 7. 优点与亮点
*   **确定性驱动**：不同于以往的随机采样，FreeScale 能够识别重建中“靠谱”的部分进行采样，从根本上保证了伪数据的质量。
*   **视角图机制**：巧妙地解决了 3D 空间中视角关联的问题，比单纯的位姿距离更符合视觉几何逻辑。
*   **通用性强**：既可以作为离线数据增强工具提升前馈模型，也可以作为在线辅助手段优化单场景表示。

### 8. 不足与局限
*   **扩散模型依赖**：最终图像的质量仍受限于外部扩散模型的能力。如果扩散模型无法处理复杂的视角相关反射（如镜面），生成图可能会出现错误。
*   **极端环境限制**：在极低光照或纹理极度缺失的场景下，确定性网格可能失效，导致无法生成有效的自由视角。
*   **伪影遗传**：如果初始 3DGS 重建存在严重的“浮点伪影”，扩散模型有时会将其误认为真实结构并进行强化（Over-sharpening）。

（完）

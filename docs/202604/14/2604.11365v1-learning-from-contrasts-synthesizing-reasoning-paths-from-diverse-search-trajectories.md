---
title: "Learning from Contrasts: Synthesizing Reasoning Paths from Diverse Search Trajectories"
title_zh: 从对比中学习：从多样化搜索轨迹中合成推理路径
authors: "Peiyang Liu, Zhirui Chen, Xi Wang, Di Liang, Youru Li, Zhi Cai, Wei Ye"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11365v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 合成用于训练数据的推理路径
tldr: 本研究针对蒙特卡洛树搜索（MCTS）在推理数据生成中效率低下的问题，提出了对比推理路径合成（CRPS）框架。CRPS不再简单过滤最优路径，而是通过分析高低质量搜索轨迹之间的差异，提取战略转折点和失败模式，合成更具启发性的推理链。实验表明，CRPS仅需1/20的数据量即可达到传统方法的性能，并显著提升了模型的泛化能力。
selection_source: fresh_fetch
motivation: 现有的推理数据提取方法仅保留最高奖励路径，忽略了搜索过程中大量失败轨迹所包含的对比学习信号。
method: 提出CRPS框架，通过结构化反思过程分析不同质量轨迹的差异，合成融合成功模式并规避潜在陷阱的推理路径。
result: 在仅使用6万条合成数据的情况下，模型性能达到或超过了使用59万条传统拒绝采样数据训练的基准模型。
conclusion: 从成功与失败的对比中学习，比单纯从成功案例中学习能产生更强且更具迁移性的推理能力。
---

## 摘要
蒙特卡洛树搜索 (MCTS) 已广泛用于自动推理数据探索，但目前的监督提取方法仍然效率低下。标准方法仅保留奖励最高的单一轨迹，丢弃了众多探索路径中存在的对比信号。在此，我们引入了对比推理路径合成 (CRPS)，这是一个将监督提取从过滤过程转变为合成过程的框架。CRPS 使用结构化的反思过程来分析高质量和低质量搜索轨迹之间的差异，提取关于战略转折点和局部失败模式的显式信息。这些见解指导推理链的合成，使其在融入成功模式的同时避开已识别的陷阱。实证结果表明，仅在 6 万个 CRPS 合成样本上进行微调的模型，其性能就达到或超过了在 59 万个基于标准拒绝采样生成的样本上训练的基线模型，数据集规模缩减了 20 倍。此外，CRPS 提高了在域外基准测试上的泛化能力，证明了从成功与失败的对比中学习比仅从成功中学习能产生更具迁移性的推理能力。

## Abstract
Monte Carlo Tree Search (MCTS) has been widely used for automated reasoning data exploration, but current supervision extraction methods remain inefficient. Standard approaches retain only the single highest-reward trajectory, discarding the comparative signals present in the many explored paths. Here we introduce \textbf{Contrastive Reasoning Path Synthesis (CRPS)}, a framework that transforms supervision extraction from a filtering process into a synthesis procedure. CRPS uses a structured reflective process to analyze the differences between high- and low-quality search trajectories, extracting explicit information about strategic pivots and local failure modes. These insights guide the synthesis of reasoning chains that incorporate success patterns while avoiding identified pitfalls. We show empirically that models fine-tuned on just 60K CRPS-synthesized examples match or exceed the performance of baselines trained on 590K examples derived from standard rejection sampling, a 20$\times$ reduction in dataset size. Furthermore, CRPS improves generalization on out-of-domain benchmarks, demonstrating that learning from the contrast between success and failure produces more transferable reasoning capabilities than learning from success alone.

---

## 论文详细总结（自动生成）

### 论文总结：Learning from Contrasts: Synthesizing Reasoning Paths from Diverse Search Trajectories (CRPS)

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是如何高效地从自动搜索（如蒙特卡洛树搜索 MCTS）中提取高质量的推理训练数据。
*   **研究动机**：目前主流的方法（如拒绝采样微调 RFT）通常只保留奖励最高（正确）的单一路径，而丢弃了搜索过程中产生的大量失败或次优轨迹。
*   **核心痛点**：这种“胜者通吃”的模式忽略了成功与失败路径之间的对比信号，导致数据利用率极低，且模型难以学习到如何规避特定的逻辑陷阱。
*   **整体含义**：CRPS 框架将数据提取从简单的“过滤”转变为“合成”，通过分析正确与错误路径的差异，生成包含“避坑指南”的高质量推理链，从而大幅提升学习效率。

#### 2. 方法论：核心思想与关键技术
CRPS 采用了**解耦的探索者-分析者（Explorer-Analyst）架构**，核心流程如下：
*   **分布感知轨迹收集**：
    *   **探索者 (Explorer)**：使用 MCTS 在解空间进行搜索。
    *   **采样策略**：不仅选择最优路径（正样本 $\tau^+$），还通过访问计数（Visit Count）采样那些模型“非常有信心但做错”的路径作为“硬负样本”（Hard Negatives），以及正确但冗长的路径作为“软负样本”（Soft Negatives）。
*   **分析者驱动的对比分析**：
    *   **全局战略评论**：分析者模型对比 $\tau^+$ 和 $\tau^-$，识别高层级的战略分歧点。
    *   **局部步骤评论（语义对齐）**：通过语义对齐找到逻辑开始偏差的第一步（语义分歧点），并针对该步骤生成具体的对比评论。
*   **模式启发式路径合成**：
    *   分析者基于原始问题和对比评论，合成一条全新的推理路径。
    *   **关键点**：合成路径会显式地融入“为什么选择此路径”以及“为什么要避开某种错误模式”的解释性信息。
*   **验证过滤**：最后通过客观答案验证合成路径的正确性，确保训练数据的可靠性。

#### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理（主战场）**：使用 GSM8K 和 MATH 的训练集作为种子问题。
    *   **域外（OOD）泛化测试**：CollegeMath、DeepMind Mathematics、OlympiadBench、TheoremQA。
    *   **非数学领域**：代码生成（HumanEval）和常识推理（StrategyQA）。
*   **基准模型 (Backbones)**：DeepSeekMath-7B、LLaMA3-8B、Mistral-7B。
*   **对比方法**：RFT（拒绝采样）、DART-Math（难度感知采样）、SIGMA（局部修复）、MathFusion（多样化聚合）、MMIQC（大规模外部数据）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-3 优化。
*   **效率对比**：
    *   CRPS-30K 数据集的微调仅需 **10 个 GPU 小时**。
    *   相比之下，达到同等性能的基线方法（如 DART-Math 590K）需要 **192 个 GPU 小时**。
    *   **结论**：在训练阶段实现了约 **19 倍的算力加速**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 15K 到 60K 不同规模的合成数据实验，并与高达 2.3M 规模的外部数据集进行了对比。
*   **消融实验**：非常充分，分别验证了合成范式、对比信号的必要性、分析粒度（全局 vs 局部）以及搜索多样性（K值）的影响。
*   **客观性与公平性**：所有基线方法均在相同的原始 MCTS 轨迹池上重新实现，确保了对比的变量仅为“数据合成/提取方法”。此外，在三种不同的模型架构上验证了通用性。

#### 6. 主要结论与发现
*   **极高的数据效率**：CRPS 仅需 **6 万 (60K)** 条数据，即可匹配或超越使用 **59 万 (590K)** 条拒绝采样数据训练的模型性能，实现了 **20 倍的数据缩减**。
*   **更强的泛化能力**：在域外（OOD）基准测试上，CRPS 显著优于局部修复方法（如 SIGMA），证明学习“如何避错”能帮助模型掌握更本质的推理结构。
*   **推理深度与稳定性**：在长程推理（Hard 级别题目）和抗干扰测试（语义扰动）中，CRPS 表现出更强的鲁棒性。

#### 7. 优点：亮点总结
*   **范式转移**：将 MCTS 数据的利用从“筛选”提升到了“深度加工”，充分挖掘了失败尝试的教学价值。
*   **元认知监督**：合成路径中包含的“避坑”逻辑类似于人类教师的讲解，为模型提供了更高密度的监督信号。
*   **解耦架构**：允许使用强大的模型（如 GPT-5-mini）作为“分析者”来提升较小模型（如 7B 级别）的推理上限。

#### 8. 不足与局限
*   **依赖可验证的奖励**：目前主要适用于数学、代码等有明确客观答案的领域，在开放式任务（如创意写作）上的应用受限。
*   **前期推理开销**：虽然训练时间缩短了，但在数据合成阶段需要进行大量的 MCTS 探索和分析者模型调用，这增加了前期的推理成本。
*   **分析者模型依赖**：该框架的效果高度依赖于“分析者”模型的诊断能力，如果分析者产生幻觉（错误诊断失败原因），可能会引入错误的偏见。
*   **潜在的风格偏差**：合成路径可能带有分析者模型的特定语言风格，这在某些应用场景下可能被视为一种偏差。

（完）

---
title: "CircuitSynth: Reliable Synthetic Data Generation"
title_zh: CircuitSynth：可靠的合成数据生成
authors: "Zehua Cheng, Wei Dai, Jiahao Sun, Thomas Lukasiewicz"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10114v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 用于合成数据的神经符号框架
tldr: "针对大语言模型在生成结构化合成数据时存在的幻觉、逻辑不一致及模式崩溃等问题，本文提出 CircuitSynth 神经符号框架。该框架通过将教师模型的推理能力蒸馏至概率命题决策图（PSDD）中，实现了语义推理与表面生成的解耦。CircuitSynth 能够强制执行硬性逻辑约束，并通过凸优化满足分布目标，在保证 100% 模式有效性的同时，显著提升了罕见组合的覆盖率。"
selection_source: fresh_fetch
motivation: 现有大语言模型在生成结构化数据时缺乏逻辑一致性和有效性保障，且难以平衡语言表达力与形式化约束。
method: 提出一种将教师模型推理能力蒸馏为 PSDD 的神经符号框架，结合硬性逻辑约束与凸优化机制来满足分布目标。
result: "在复杂逻辑谜题中实现了 100% 的模式有效性（对比基线的 12.4%），并在罕见组合覆盖率上显著优于现有方法。"
conclusion: CircuitSynth 通过解耦语义与表达，为生成高保真、逻辑严密且分布可控的合成数据提供了一种可靠的解决方案。
---

## 摘要
高保真合成数据的生成是现代机器学习的基石，然而大语言模型（LLMs）在执行结构化生成任务时，经常面临幻觉、逻辑不一致和模式崩溃等问题。现有的方法，如提示词工程或检索增强生成，缺乏在语言表达能力与关于有效性和覆盖范围的正式保证之间取得平衡的机制。为了解决这一问题，我们提出了 CircuitSynth，这是一种新型的神经符号框架，它将语义推理与表面实现（surface realization）相解耦。通过将教师 LLM 的推理能力蒸馏到概率命题决策图（PSDD）中，CircuitSynth 创建了一个可处理的语义先验，从而在结构上强制执行硬逻辑约束。此外，我们引入了一种凸优化机制，以严格满足软分布目标。在多种基准测试上的实证评估表明，即使在无约束基线模型表现不佳（仅 12.4%）的复杂逻辑谜题中，CircuitSynth 也能实现 100% 的模式有效性（Schema Validity），同时在稀有组合覆盖率方面显著优于现有最先进的方法。

## Abstract
The generation of high-fidelity synthetic data is a cornerstone of modern machine learning, yet Large Language Models (LLMs) frequently suffer from hallucinations, logical inconsistencies, and mode collapse when tasked with structured generation. Existing approaches, such as prompting or retrieval-augmented generation, lack the mechanisms to balance linguistic expressivity with formal guarantees regarding validity and coverage. To address this, we propose CircuitSynth, a novel neuro-symbolic framework that decouples semantic reasoning from surface realization. By distilling the reasoning capabilities of a Teacher LLM into a Probabilistic Sentential Decision Diagram (PSDD), CircuitSynth creates a tractable semantic prior that structurally enforces hard logical constraints. Furthermore, we introduce a convex optimization mechanism to rigorously satisfy soft distributional goals. Empirical evaluations across diverse benchmarks demonstrate that CircuitSynth achieves 100% Schema Validity even in complex logic puzzles where unconstrained baselines fail (12.4%) while significantly outperforming state-of-the-art methods in rare-combination coverage.

---

## 论文详细总结（自动生成）

这是一份关于论文《CircuitSynth: Reliable Synthetic Data Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成高保真合成数据是现代机器学习的关键，但大语言模型（LLMs）在处理结构化生成任务时面临三大挑战：
*   **幻觉与逻辑不一致**：LLM 难以严格遵守复杂的模式（Schema）约束（如“男性”不能有“怀孕”诊断）。
*   **模式崩溃（Mode Collapse）**：随机采样倾向于重复常见模式，忽略长尾分布中的稀有组合。
*   **缺乏形式化保证**：现有的提示词工程（Prompting）或检索增强生成（RAG）无法从数学上保证输出的合法性或分布的覆盖率。

**核心含义**：本文旨在通过一种**神经符号（Neuro-symbolic）框架**，将“说什么（语义推理）”与“怎么说（语言表达）”解耦，从而在保持语言流畅性的同时，提供 100% 的逻辑有效性保证和可控的分布覆盖。

### 2. 方法论：核心思想与关键技术
CircuitSynth 采用“计划-生成”的架构，核心流程如下：

*   **语义先验（Semantic Prior）**：使用**概率命题决策图（PSDD）**作为核心。PSDD 是一种可处理概率电路（PC），其结构通过“逻辑编译”直接嵌入了硬性逻辑约束（$\Phi$）。这意味着在电路拓扑层面，不符合逻辑的状态概率被设为 0，从而在数学上杜绝了非法样本的产生。
*   **投影与蒸馏协议（Project-and-Distill）**：
    1.  **教师引导**：利用强大的 Teacher LLM（如 Llama-3.1-70B）生成初始的语义计划。
    2.  **符号过滤**：通过符号验证器剔除不符合约束的计划。
    3.  **参数蒸馏**：将通过验证的计划蒸馏到 PSDD 中，使其学习到教师模型的语义相关性。
*   **凸优化机制（Convex Optimization）**：为了满足“软分布约束”（如确保 10% 的样本包含稀有属性），利用 PSDD 边缘概率是参数线性函数的特性，将其建模为凸优化问题，通过投影梯度下降（PGD）调整电路参数，最小化与教师分布的 KL 散度。
*   **神经实现器（Neural Realizer）**：由 Student LLM 担任。它接收 PSDD 采样的合法计划，并结合**语法约束解码（GCD）**生成最终文本，确保文本内容与计划严格对齐。

### 3. 实验设计
论文在三个具有挑战性的场景下进行了测试：
*   **数据集**：
    1.  **WebNLG (v3.0)**：RDF 到文本生成，测试标准结构化能力。
    2.  **DART**：开放域结构化数据，测试在噪声和稀疏模式下的鲁棒性。
    3.  **ZebraLogic**：复杂的逻辑谜题（如斑马问题），测试极高逻辑密度下的硬约束遵守能力。
*   **对比方法（Baselines）**：包括基础 LLM（Llama-3.1-8B, Qwen 2.5 7B）、Evol-Instruct、GraphRAG、TreeSynth（基于树的子空间划分）以及消融版本。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 GPU (80GB VRAM)**。
*   **耗时**：
    *   PSDD 编译与凸优化：约 **4 小时**。
    *   学生模型（Student LLM）微调：约 **12 小时**。
*   **模型规模**：Teacher 为 Llama-3.1-70B，Student 为 Llama-3.1-8B。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，包括 10 万个“银标”计划的生成、WebNLG 全量测试、DART 抽样 5.5 万例以及 ZebraLogic 1 万个谜题。
*   **充分性与客观性**：
    *   涵盖了从简单分类到极难逻辑推理的梯度。
    *   进行了 **21 个子领域的细粒度审计**（表 2），展示了模型在不同难度下的稳定性。
    *   包含**消融实验**（表 3），分别验证了 PSDD 骨干、蒸馏和优化机制的贡献。
    *   引入了 Bits-Per-Token (BPT) 等泛化指标，证明了合成数据对下游模型训练的真实价值。实验设计严谨且对比公平。

### 6. 主要结论与发现
*   **100% 有效性**：CircuitSynth 在所有数据集上均实现了 **100% 的模式有效性（Schema Validity）**，而最强基线 TreeSynth 在复杂逻辑任务中仅为 81.2%，普通 LLM 仅为 12.4%。
*   **解决模式崩溃**：在稀有组合覆盖率（RCC）上，CircuitSynth 达到 52.1%，远超 TreeSynth 的 35.2%，证明了凸优化在探索长尾分布方面的有效性。
*   **高质量泛化**：在合成数据上训练的学生模型具有更低的困惑度（PPL）和 BPT，说明 CircuitSynth 生成的数据逻辑更严密，减少了噪声拟合。

### 7. 优点（亮点）
*   **数学完备性**：通过电路拓扑强制执行逻辑，而非依赖 LLM 的随机采样或软性惩罚。
*   **解耦架构**：巧妙地结合了符号逻辑的“严谨性”和神经网络的“表达力”。
*   **高效可控**：将复杂的分布控制转化为凸优化问题，保证了全局最优解且推理延迟极低（约 42ms/样本）。
*   **安全性**：通过“设计即安全”的方法，从源头上消除了合成数据中的事实性错误（如性别与生理特征不符）。

### 8. 不足与局限
*   **扩展性瓶颈**：PSDD 的编译复杂度受限于约束图的**树宽（Treewidth）**。对于极度密集、全连接的约束网络（如复杂的蛋白质相互作用），电路规模可能会发生指数级爆炸。
*   **模式依赖**：系统高度依赖预定义的模式（Schema）。如果模式本身定义有误或存在道德偏见，系统会高效地生成符合该错误逻辑的样本。
*   **冷启动成本**：需要强大的 Teacher LLM 来生成初始计划，且 PSDD 的编译需要一定的计算资源和专业知识。

（完）

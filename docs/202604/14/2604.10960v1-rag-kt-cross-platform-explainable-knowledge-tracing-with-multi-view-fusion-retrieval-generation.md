---
title: "RAG-KT: Cross-platform Explainable Knowledge Tracing with Multi-view Fusion Retrieval Generation"
title_zh: RAG-KT：基于多视图融合检索生成的跨平台可解释知识追踪
authors: "Zhiyi Duan, Hongyu Yuan, Rui Liu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10960v1"
tags: ["query:edu-llm"]
score: 10.0
evidence: 利用大模型和检索增强生成的 RAG-KT 可解释知识追踪
tldr: 针对传统深度学习知识追踪模型难以跨平台迁移且缺乏解释性，以及大语言模型在KT任务中存在幻觉或领域依赖的问题，本文提出了RAG-KT。该框架利用检索增强生成技术，通过“问题组”抽象实现跨平台数据对齐，并构建多源结构化上下文，为LLM提供可靠的推理依据。实验证明，RAG-KT在准确性、鲁棒性及跨平台泛化能力上均优于现有方法，并能提供可解释的诊断结果。
selection_source: fresh_fetch
motivation: 传统KT模型受限于平台特定标识符且难以处理跨平台分布偏移，而LLM方法在可靠性和通用性上存在局限。
method: 提出一种检索增强的KT范式，通过问题组抽象对齐多源数据，并融合多视图检索到的结构化上下文辅助LLM进行推理。
result: 在三个公开基准数据集上的实验显示，该方法在预测准确性、鲁棒性以及跨平台迁移场景下均取得了显著提升。
conclusion: RAG-KT通过结合检索增强与LLM，有效解决了知识追踪中的跨平台对齐与可解释性难题，提升了模型的泛化性能。
---

## 摘要
知识追踪（KT）通过学生的历史交互记录推断其知识状态，从而预测其未来的表现。传统的基于深度学习（DL）的知识追踪模型通常受限于特定平台的标识符和隐式表示，导致其难以迁移和解释。基于大语言模型（LLM）的方法在提示词模式下可能缺乏依据，而在微调模式下则可能过度依赖特定领域。此外，大多数现有的知识追踪方法是在同分布假设下开发和评估的。在实际部署中，教育数据往往来自具有显著分布偏移的异构平台，这通常会降低模型的泛化能力。为此，我们提出了 RAG-KT，这是一种检索增强范式，将跨平台知识追踪建模为受可靠上下文约束的 LLM 推理过程。该方法通过“问题组”（Question Group）抽象构建了具有跨源对齐的统一多源结构化上下文，并为每次预测检索互补且丰富可靠的上下文，从而实现有据可依的预测和可解释的诊断。在三个公开知识追踪基准数据集上的实验表明，该方法在准确性和鲁棒性方面均有持续提升，且在跨平台条件下表现优异。

## Abstract
Knowledge Tracing (KT) infers a student's knowledge state from past interactions to predict future performance. Conventional Deep Learning (DL)-based KT models are typically tied to platform-specific identifiers and latent representations, making them hard to transfer and interpret. Large Language Model (LLM)-based methods can be either ungrounded under prompting or overly domain-dependent under fine-tuning. In addition, most existing KT methods are developed and evaluated under a same-distribution assumption. In real deployments, educational data often arise from heterogeneous platforms with substantial distribution shift, which often degrades generalization. To this end, we propose RAG-KT, a retrieval-augmented paradigm that frames cross-platform KT as reliable context constrained inference with LLMs. It builds a unified multi-source structured context with cross-source alignment via Question Group abstractions and retrieves complementary rich and reliable context for each prediction, enabling grounded prediction and interpretable diagnosis. Experiments on three public KT benchmarks demonstrate consistent gains in accuracy and robustness, including strong performance under cross-platform conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAG-KT: Cross-platform Explainable Knowledge Tracing with Multi-view Fusion Retrieval Generation》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：知识追踪（KT）是个性化教育的核心，旨在根据学生历史答题记录预测其未来表现。
*   **核心问题**：
    *   **跨平台泛化难**：传统深度学习（DL）模型依赖特定平台的题目ID，面对新平台、新题目或新知识点时表现极差（分布偏移）。
    *   **可解释性缺失**：DL模型多为黑盒，难以提供教学建议；而直接使用大语言模型（LLM）虽有解释力，但易产生“幻觉”或过度依赖微调数据。
    *   **同分布假设局限**：现有研究多假设训练与测试数据来自同一平台，忽略了现实中教育数据的异构性。
*   **研究动机**：通过检索增强生成（RAG）技术，利用结构化知识库为LLM提供可靠上下文，解决跨平台对齐与可解释性难题。

### 2. 论文提出的方法论：RAG-KT 框架
RAG-KT 将知识追踪建模为“受可靠上下文约束的推理过程”，分为三个阶段：
*   **阶段1：构建多源异构知识库**
    *   **核心思想**：引入**问题组（Question Group, QG）**作为中间抽象层。QG 由“知识点（KC）+ 难度等级（D）”定义，将不同平台但语义等价的题目对齐。
    *   **技术细节**：使用 IRT-2PL 模型估计学生能力和题目难度，并将其离散化为高/中/低等级。通过 LLM 辅助的语义匹配将各平台 KC 映射到统一的教科书知识图谱中。
*   **阶段2：多视图融合检索**
    *   从三个维度检索与目标学生最相似的“同伴”数据：
        1.  **行为视图**：基于 KC 兴趣子图，计算动态加权准确率（DWA）的余弦相似度。
        2.  **结构视图**：基于知识图谱中的结构路径，衡量学生掌握知识点的拓扑相似性。
        3.  **能力视图**：基于 IRT 估计的全局能力值进行匹配。
    *   **融合公式**：$Sim_{final} = \lambda_1 Sim_{bhv} + \lambda_2 Sim_{struc} + \lambda_3 Sim_{abil}$。
*   **阶段3：可解释诊断报告生成**
    *   将检索到的同伴表现、目标学生历史趋势、知识点关联性等封装进**结构化可靠上下文提示词（Prompt）**。
    *   引导 LLM 进行逻辑推理（如冲突解决、风险评估），输出预测概率及人类可读的诊断报告。

### 3. 实验设计
*   **数据集**：
    *   **基准测试**：ASSIST09, ASSIST12, DBE-KT22（涵盖不同平台和学科）。
    *   **冷启动测试**：Eedi 数据集（用于验证在完全未见的平台、学生和题目下的表现）。
*   **对比方法**：
    *   **DL-based**：DKT, DKT+, AKT, GKT, Deep-IRT 等。
    *   **LLM-based (Prompting)**：HISE-KT, EFKT, LOKT。
    *   **LLM-based (Fine-tuning)**：LLM-KT, CIKT, 2T-KT 等。
*   **评估指标**：ACC（准确率）、AUC（曲线下面积）、F1-score，以及针对报告质量的人工+LLM混合评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **效率分析**：
    *   **训练成本**：RAG-KT 属于非微调范式，**训练时长为 0**（对比 CIKT 需 24 小时，LLM-KT 需 10 小时）。
    *   **推理延迟**：平均每个样本推理耗时约 **8 秒**，其中 LLM 推理占 6 秒，图检索和相似度计算占 2 秒。虽然慢于传统 DL 模型，但快于同类可解释模型 HISE-KT（19 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了测试，涵盖了从同分布到完全冷启动的场景。
*   **消融实验**：非常充分。分别验证了“相似学生”、“问题组抽象”、“不同子图视图”以及“检索模块整体”对性能的影响。
*   **参数敏感性**：测试了序列长度（seq-len）、检索数量（top-k）和权重比例（$\lambda$）的影响。
*   **客观性**：采用学生级别的非重叠划分（Student-level disjoint split），并进行 5 次独立随机实验取平均值，确保了评估的公平性。

### 6. 论文的主要结论与发现
*   **性能领先**：RAG-KT 在所有数据集上均显著优于现有 DL 和 LLM 方法，尤其在 DeepSeek-R1 作为底座时表现最强。
*   **冷启动优势**：在完全陌生的 Eedi 数据集上，RAG-KT 的 AUC 达到 74.40%，远超传统模型（约 50% 左右，接近随机猜测），证明了跨平台对齐的有效性。
*   **解释质量**：生成的报告在可解释性、教育有用性和严谨性上得分最高，能够识别学生的能力瓶颈并给出针对性建议。

### 7. 优点（亮点）
*   **创新的对齐机制**：通过“问题组”而非“题目ID”进行检索，从根本上解决了跨平台数据无法互通的痛点。
*   **多视图融合**：结合了教育测量学（IRT）、认知结构（KG）和学习行为（DWA），检索到的上下文更具参考价值。
*   **即插即用**：无需昂贵的 LLM 微调，通过 RAG 架构即可实现高性能和高解释性。

### 8. 不足与局限
*   **推理延迟**：8 秒的响应时间对于实时在线练习系统仍显略长，难以支持极高并发的实时预测。
*   **依赖对齐质量**：如果不同平台间的知识点（KC）映射出现偏差，会直接影响检索质量和预测精度。
*   **API 成本**：依赖高性能 LLM（如 GPT-4o 或 DeepSeek-R1），在大规模部署时会产生较高的 API 调用费用。
*   **应用限制**：在知识点定义极度模糊或数据极度稀疏的学科领域，检索模块的效用可能会打折扣。

（完）

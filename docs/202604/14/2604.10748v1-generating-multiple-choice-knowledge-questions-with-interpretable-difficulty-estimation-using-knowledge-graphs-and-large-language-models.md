---
title: Generating Multiple-Choice Knowledge Questions with Interpretable Difficulty Estimation using Knowledge Graphs and Large Language Models
title_zh: 利用知识图谱与大语言模型生成具有可解释难度估计的多项选择知识题
authors: "Mehmet Can Şakiroğlu, H. Altay Güvenir, Kamer Kaya"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10748v1"
tags: ["query:edu-llm"]
score: 10.0
evidence: 利用大模型为AI辅助教育生成多选题
tldr: 针对自适应教育中多选题（MCQ）自动生成及难度评估的挑战，本文提出一种结合知识图谱（KG）与大语言模型（LLM）的新方法。该方法利用LLM从文档构建KG，基于图结构系统化生成题干和干扰项，并提取九种难度信号进行可解释的难度评估。实验证明，该方法生成的题目质量高，且难度预测与人类感知高度一致，为AI辅助教育提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的自动题目生成系统在生成高质量多选题及提供可解释的难度评估方面仍面临巨大挑战。
method: 利用LLM构建知识图谱，基于图节点和三元组生成题干与干扰项，并结合九种数据驱动的难度信号计算统一难度得分。
result: 实验结果显示，该方法生成的MCQ质量优异，且其难度评估模型具有良好的可解释性并与人类主观感受相符。
conclusion: 通过整合结构化知识表示与大语言模型，本研究显著提升了自动化题目生成的质量及其难度预测的准确性。
---

## 摘要
在用于自适应、人工智能辅助教育的自动多项选择题（MCQ）生成系统中，生成带有难度估计的题目仍是一项挑战。本研究提出了一种利用知识图谱（KG）和大语言模型（LLM）从输入文档中生成带有难度估计的 MCQ 的新方法。该方法首先利用 LLM 从输入文档构建知识图谱，并据此系统地生成题目。每个 MCQ 的生成过程包括：从知识图谱中选择一个节点作为正确答案，采样相关的三元组或五元组（可选择性地增加一个额外三元组），并提示 LLM 根据这些图组件生成相应的题干。干扰项则从知识图谱中选取。针对每个 MCQ，系统会计算九个难度信号，并采用数据驱动的方法将其整合为统一的难度评分。实验结果表明，该方法生成的 MCQ 质量较高，其难度估计具有可解释性且符合人类感知。本研究通过整合结构化知识表示、大语言模型及数据驱动的难度估计模型，提升了自动 MCQ 生成的效果。

## Abstract
Generating multiple-choice questions (MCQs) with difficulty estimation remains challenging in automated MCQ-generation systems used in adaptive, AI-assisted education. This study proposes a novel methodology for generating MCQs with difficulty estimation from the input documents by utilizing knowledge graphs (KGs) and large language models (LLMs). Our approach uses an LLM to construct a KG from input documents, from which MCQs are then systematically generated. Each MCQ is generated by selecting a node from the KG as the key, sampling a related triple or quintuple -- optionally augmented with an extra triple -- and prompting an LLM to generate a corresponding stem from these graph components. Distractors are then selected from the KG. For each MCQ, nine difficulty signals are computed and combined into a unified difficulty score using a data-driven approach. Experimental results demonstrate that our method generates high-quality MCQs whose difficulty estimation is interpretable and aligns with human perceptions. Our approach improves automated MCQ generation by integrating structured knowledge representations with LLMs and a data-driven difficulty estimation model.

---

## 论文详细总结（自动生成）

以下是对论文《Generating Multiple-Choice Knowledge Questions with Interpretable Difficulty Estimation using Knowledge Graphs and Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自适应学习和AI辅助教育中，自动生成高质量的多选题（MCQ）并准确估计其难度至关重要。
*   **研究动机**：现有的自动出题方法存在两极分化：要么依赖手工规则（扩展性差），要么依赖黑盒深度学习模型（缺乏可解释性）。
*   **核心挑战**：如何在保证题目自然语言流畅性的同时，确保事实的准确性，并提供能够反映人类感知的、可解释的难度评分。
*   **整体含义**：本文提出了一种结合**知识图谱（KG）**的结构化能力与**大语言模型（LLM）**的生成能力的新框架，旨在生成事实可靠、难度可控且评估透明的知识型多选题。

### 2. 方法论：核心思想与技术细节
该框架分为三个主要阶段：
*   **知识图谱构建**：
    *   利用 LLM（GPT-4o）和 LangChain 的 `LLMGraphTransformer` 从非结构化文本（如维基百科文章）中提取“主语-谓语-宾语”三元组。
    *   将提取的实体和关系存储在 Neo4j 图数据库中，并计算节点中心性（Degree Centrality）和节点嵌入（FastRP）。
*   **多选题生成**：
    *   **正确答案（Key）选择**：选取图中中心性高的节点作为正确选项。
    *   **题干（Stem）生成**：从图中采样与 Key 相关的子图（单跳三元组或双跳五元组），提示 LLM 根据这些结构化事实编写题干。
    *   **干扰项（Distractors）选择**：通过广度优先搜索（BFS）在图中寻找与 Key 类型相同但语义距离不同的节点。
    *   **验证**：再次调用 LLM 检查干扰项是否可能成为正确答案，确保题目唯一性。
*   **可解释难度估计**：
    *   提取 **9 种难度信号**：推理深度（单/双跳）、额外事实、干扰项图深度、节点嵌入相似度、文本嵌入相似度、度中心性、可读性评分（Flesch Reading Ease）、最大间隙计数（语义分布）、以及 LLM 额外事实标记。
    *   使用回归模型（如 XGBoost）将这些信号映射到人类回答的错误率（即难度得分）。

### 3. 实验设计
*   **数据集**：选取维基百科浏览量前 100 的文章作为原始语料。
*   **基准与对比**：
    *   生成了 156 道 MCQ。
    *   **人类评估**：每道题平均收集了 38 份独立的人类回答，以实际“错误率”作为难度的地面真值（Ground Truth）。
    *   **模型对比**：对比了线性回归、随机森林、梯度提升树（GBDT）和 XGBoost 四种回归模型。
*   **消融实验**：逐一剔除 9 种信号，评估各特征对难度预测准确性的贡献。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M3 芯片**、集成 GPU 和 **16 GB 统一内存**的 MacBook Pro 上完成。
*   **软件/模型**：使用了 OpenAI 的 **GPT-4o** 进行图构建和文本生成，使用 `text-embedding-3-large` 生成嵌入向量。
*   **训练时长**：文中未明确提及具体训练时长，但考虑到使用的是传统机器学习回归模型（XGBoost 等）且样本量较小（156 条），训练过程预计在秒级或分钟级完成。

### 5. 实验数量与充分性
*   **实验规模**：共生成 156 道题目，虽然样本数量在深度学习领域不算大，但每道题都有约 38 名人类参与者的反馈，这在教育测量学中具有较高的统计信度。
*   **充分性**：
    *   进行了多模型对比和详细的消融实验。
    *   使用了 SHAP 值分析特征重要性，增强了模型的可解释性。
    *   **局限性**：实验仅限于维基百科的通用知识领域，尚未在医学、法律等专业垂直领域进行大规模验证。

### 6. 主要结论与发现
*   **模型表现**：**XGBoost** 在预测难度方面表现最好（RMSE=0.12, $R^2$=0.58, Spearman’s $\rho$=66.1%）。
*   **关键特征**：**文本嵌入相似度**（干扰项与题干的语义接近度）和**可读性评分**是影响题目难度最核心的因素。
*   **图特征作用**：节点嵌入相似度和图结构特征（如度中心性）对难度预测有显著贡献，证明了引入知识图谱的必要性。
*   **一致性**：自动生成的难度评分与人类实际感受高度相关，证明了该框架在自适应教育系统中的实用价值。

### 7. 优点
*   **可解释性强**：不同于纯黑盒模型，该方法通过 9 种直观的信号解释“为什么这道题难”。
*   **事实一致性**：通过知识图谱约束 LLM 的生成，有效减少了幻觉问题，确保题目基于真实事实。
*   **闭环验证**：结合了自动验证机制和人类反馈校准，形成了一个可靠的评估闭环。

### 8. 不足与局限
*   **样本量限制**：156 道题目的数据集规模较小，可能限制了更复杂模型（如深度神经网络）的应用。
*   **领域偏差**：主要基于维基百科热门条目，题目难度偏向中等，对于极难或极专业的知识覆盖不足。
*   **异常值敏感**：实验中发现一道因语义歧义导致的高难度题目（离群点），说明自动化验证机制仍有改进空间。
*   **成本考虑**：频繁调用 GPT-4o 等商业 LLM API 可能会产生较高的运营成本。

（完）

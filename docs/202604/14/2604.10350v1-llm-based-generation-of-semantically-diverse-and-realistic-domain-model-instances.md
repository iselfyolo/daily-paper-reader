---
title: LLM-based Generation of Semantically Diverse and Realistic Domain Model Instances
title_zh: 基于大语言模型的语义多样化且真实的领域模型实例生成
authors: "Andrei Coman, Lola Burgueño, Dominik Bork, Manuel Wimmer"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10350v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 基于大语言模型生成用于数据驱动研究的真实领域模型实例
tldr: 本研究针对领域模型实例化中缺乏真实语义和多样性的挑战，提出了一种结合大语言模型（LLM）与模型验证工具的新方法。该方法利用两种提示策略，从UML类图生成既符合结构约束又具备真实、多样化属性值的模型实例。通过在教育及文献领域的多个模型上进行评估，证明了该方法在生成语法正确、语义连贯且高度多样化的实例方面具有显著优势，为教学和数据驱动研究提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的自动化建模工具虽能生成结构正确的实例，但在提供具有现实语义、多样化且人类可理解的属性值方面仍存在不足。
method: 提出一种结合LLM提示策略与模型验证工具的框架，通过特定提示引导LLM生成符合UML类图约束且语义丰富的实例数据。
result: 实验结果显示，生成的实例在语法正确性和模型一致性上表现优异，且属性值在保持语义连贯的同时展现了高度的多样性。
conclusion: 该研究证明了LLM在生成高质量、语义真实的领域模型实例方面的潜力，为模型驱动工程提供了新的自动化手段。
---

## 摘要
最近，大语言模型（LLMs）被提议用于支持领域建模任务，主要涉及通过推荐额外的模型元素来补全部分模型。然而，还存在许多其他的建模任务，其中之一是领域模型的实例化，用以表示具体的领域对象。虽然已有大量工作支持生成结构有效的实例化，但在通过使实例包含真实值来融入现实世界语义，以及确保生成语义多样的模型方面，仍存在公开挑战。只有这样，生成的模型才能变得易于人类理解，并在教育或数据驱动的研究背景中发挥作用。为了应对这些挑战，本文提出了一种方法，该方法结合使用 LLMs、两种提示策略以及现有的模型验证工具，用于实例化以 UML 类图表示的语义真实且多样的领域模型。我们将该方法应用于教育领域及文献中不同领域的模型，并从语法正确性、模型一致性、语义正确性以及生成值的多样性等方面对生成的实例进行了评估。结果表明，生成的实例在语法上大多是正确的，符合领域模型，且仅存在少量语义错误。此外，生成的实例值具有语义多样性，即符合领域的具体真实示例，且单个模型内的值组合在语义上是连贯的。

## Abstract
Large Language Models (LLMs) have been recently proposed for supporting domain modeling tasks mostly related to the completion of partial models by recommending additional model elements. However, there are many more modeling tasks, one of them being the instantiation of domain models to represent concrete domain objects. While there is considerable work supporting the generation of structurally valid instantiations, there are still open challenges to incorporating real-world semantics by having realistic values contained in instances and ensuring the generation of semantically diverse models. Only then will such generated models become human-understandable and helpful in educational or data-driven research contexts.   To tackle these challenges, this paper presents an approach that employs LLMs and two prompting strategies in combination with existing model validation tools for instantiating semantically realistic and diverse domain models expressed as UML class diagrams. We have applied our approach to models used in education and available in the literature from different domains and evaluated the generated instances in terms of syntactic correctness, model conformance, semantic correctness, and diversity of the generated values. The results show that the generated instances are mostly syntactically correct, that they conform to the domain model, and that there are only a few semantic errors. Moreover, the generated instance values are semantically diverse, i.e., concrete realistic examples in line with the domain and the combination of the values within one model are semantically coherent.

---

## 论文详细总结（自动生成）

这篇论文针对软件工程中领域模型实例化（Domain Model Instantiation）的质量问题，提出了一种结合大语言模型（LLM）与形式化验证工具的新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在模型驱动工程（MDE）中，现有的自动化实例化工具（如基于约束求解器或启发式搜索的方法）虽然能生成结构上有效的实例（UML对象图），但往往缺乏**现实语义**（如属性值是随机字符串而非真实姓名）和**语义多样性**。
*   **研究动机**：高质量、真实的模型实例对于教育（帮助学生理解抽象概念）、软件测试（生成测试数据）以及机器学习（构建高质量模型库）至关重要。目前缺乏能够生成既符合结构约束又具备人类可理解语义的工具。

### 2. 方法论
论文提出了两种基于提示工程（Prompt Engineering）的策略，并结合 **USE (UML-based Specification Environment)** 工具进行闭环验证：
*   **核心思想**：利用 LLM 的领域知识生成语义丰富的初始实例，再利用 USE 工具的确定性检查能力（语法和 OCL 约束检查）对 LLM 的输出进行迭代修正。
*   **策略一：指令学习 (Instruction Learning, IL)**
    *   采用单次提示（One-shot Learning），提供领域模型（类图）和 SOIL 语言（USE 的实例化脚本语言）的示例。
    *   通过对话记忆，要求 LLM 连续生成多个结构和语义不同的实例。
*   **策略二：思维链学习 (Chain-of-Thought, CoT)**
    *   **步骤1：模型分析**。让 LLM 先用自然语言描述类图的结构、关系和约束。
    *   **步骤2：场景生成**。引导 LLM 构思五类特定场景：基础场景、复杂场景、边界场景（极值）、边缘场景（罕见但不违规）和过度约束检测（故意违规以测试模型限制）。
    *   **步骤3：代码生成**。根据场景描述生成 SOIL 脚本。
*   **自动化修正算法**：对于 LLM 生成的任何实例，系统会自动调用 USE 进行语法和一致性检查。若报错，则将错误信息反馈给 LLM 进行自我修复（最多尝试 2 次）。

### 3. 实验设计
*   **数据集/场景**：选择了 10 个不同领域的 UML 类图，包括银行系统、酒店管理、状态机（元模型级别）、视频俱乐部、足球联赛等。这些模型涵盖了继承、枚举、复杂 OCL 约束和时间推理。
*   **评估指标**：
    1.  **语法正确性 (RQ1.1)**：能否被 USE 解析。
    2.  **模型一致性 (RQ1.2)**：是否违反类图定义的类型、多重性和 OCL 约束。
    3.  **语义正确性 (RQ2)**：区分“真实感 (Realistic)”（如符合格式的电话、邮箱）和“真实性 (Real)”（如现实中存在的地址、有效的银行校验码）。
    4.  **语义多样性 (RQ3)**：使用编辑距离（Levenshtein distance）和精确匹配率衡量实例间值的差异。
*   **对比基准**：主要对比了 IL 策略和 CoT 策略在上述指标上的表现。

### 4. 资源与算力
*   **模型选择**：使用了 **GPT-4o (gpt-4o-2024-08-06)**。
*   **工具链**：基于 Java 开发，使用了 LangChain4j 库与 OpenAI API 交互，集成 USE 工具进行后端验证。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号或训练时长，因为该研究是基于现成 LLM 的 API 进行推理（Inference）而非微调（Fine-tuning）。实验主要消耗的是 API Token 和常规 CPU 算力。

### 5. 实验数量与充分性
*   **实验规模**：每个领域模型使用两种策略各生成 30 个实例，总计生成了 **600 个模型实例**。
*   **充分性评价**：实验设计较为充分。涵盖了从简单到复杂的 10 个领域，并针对语义正确性进行了自动化（正则、API 校验）与人工抽检相结合的评估。对比了两种不同的提示策略，验证了 CoT 在探索边界条件方面的优越性。

### 6. 主要结论与发现
*   **高正确率**：两种策略生成的实例在语法和一致性上表现极佳，IL 策略几乎无错，CoT 仅在极少数复杂模型（如餐厅、足球）中出现微量错误。
*   **语义优势**：LLM 在生成“具有现实感”的数据（如格式正确的 IBAN、姓名）方面非常出色，但在生成“真实存在”的数据（如真实存在的街道地址）时表现较弱，这可能与隐私保护机制或幻觉有关。
*   **多样性显著**：生成的实例在属性值上具有极高的多样性，几乎没有重复，且 CoT 策略能有效探索模型的边界空间。
*   **时间推理能力**：LLM 能够很好地处理涉及时间的约束（如“退房日期必须晚于入住日期”）。

### 7. 优点
*   **结合了形式化与生成式 AI**：利用 USE 工具弥补了 LLM 逻辑推理的不确定性，确保了结果的严谨性。
*   **语义丰富性**：解决了传统建模工具生成“垃圾数据”的痛点，使生成的模型更具人类可读性。
*   **场景化生成**：CoT 策略通过定义五种测试场景，为模型验证提供了系统化的思路。

### 8. 不足与局限
*   **模型规模限制**：实验中的领域模型规模中等（最大 22 个类），对于超大规模的企业级模型，LLM 的上下文窗口和推理能力可能面临挑战。
*   **真实性瓶颈**：对于需要严格真实物理世界对应的数据（如真实地理坐标），LLM 仍存在幻觉。
*   **模型依赖**：研究仅基于 GPT-4o，未深入探讨其他开源模型（如 Llama 3, DeepSeek）在特定建模语言（SOIL）上的表现差异。
*   **偏差风险**：LLM 生成的数据可能带有训练数据的文化或地域偏差（如生成的姓名多为西方姓名）。

（完）

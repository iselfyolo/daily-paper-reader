---
title: Evaluating LLMs Code Reasoning Under Real-World Context
title_zh: 评估大语言模型在真实世界上下文下的代码推理能力
authors: Changshu Liu
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12881v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 从真实项目中构建新的代码推理问题基准数据集
tldr: 针对现有代码推理基准测试过于简化、缺乏真实项目复杂性的问题，本文推出了 R2Eval 基准测试。该基准包含从 10 个主流 Python 项目中提取的 135 个推理任务，其核心创新在于支持复合及自定义数据类型的序列化，从而保留了真实世界的代码依赖和结构。R2Eval 能够更真实地评估大语言模型在处理复杂工程逻辑时的推理能力，有效弥补了传统评估方法与实际应用场景之间的差距。
selection_source: carryover_cache
motivation: 现有基准测试多采用简单的代码片段和原始数据类型，难以反映真实世界软件项目的复杂结构和依赖关系。
method: 开发了包含 135 个真实 Python 项目问题的 R2Eval 基准，并引入了复合及自定义类型的序列化机制。
result: R2Eval 成功保留了真实世界的数据复杂性，为评估 LLM 在实际工程环境下的推理能力提供了更具挑战性的标准。
conclusion: 该研究通过提供更贴近实际的评估框架，揭示了现有模型在处理复杂代码逻辑时的局限性，推动了代码推理能力的真实性评估。
---

## 摘要
代码推理任务在评估大语言模型（LLMs）方面变得日益重要。然而，大多数现有基准测试依赖于简化的、由 LLM 生成的代码片段或人类编写的编程挑战解决方案，且通常将输入和输出限制为原始类型，未能反映真实世界项目的结构和依赖关系。这些简化限制了它们衡量实际泛化能力的能力。我们提出了 R2Eval1，这是一个包含 135 个代码推理问题的基准测试，这些问题提取自十个广泛使用的 Python 项目。与以往的工作不同，R2Eval 对复合类型和自定义类型进行了序列化，保留了真实世界数据的复杂性，从而能够对 LLM 进行更真实的评估。

## Abstract
Code reasoning tasks are increasingly crucial to evaluating large language models (LLMs). Yet most existing benchmarks rely on simplistic, LLM-generated snippets or human-written solutions to code challenges and often restrict inputs and outputs to primitive types, failing to reflect the structure and dependencies of real-world projects. These simplifications limit their ability to measure practical generalizability. We present R2Eval1, a benchmark of 135 code reasoning problems drawn from ten widely used Python projects. Unlike prior work, R2Eval serializes compound and custom types, preserving real-world data complexity and enabling a more realistic assessment of LLMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **R2Eval** 的新型基准测试，旨在评估大语言模型（LLMs）在真实软件工程环境下的代码推理能力。以下是对该论文的深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的代码推理基准测试（如 CRUX-Eval、HumanEval）大多依赖于简单的、自包含的代码片段，且输入输出多限于原始数据类型（如整数、字符串）。这导致评估结果无法反映 LLM 在处理具有复杂结构、跨函数依赖和自定义对象类型的真实世界项目时的真实水平。
*   **研究背景**：虽然 LLM 在现有基准测试上表现优异，但它们在处理生产级代码时的泛化能力仍存疑。真实项目中的代码往往包含复杂的类继承、跨模块调用以及难以直接打印的自定义对象。

### 2. 论文提出的方法论
*   **核心思想**：构建一个名为 **R2Eval**（Realistic Code Reasoning Evaluation）的基准测试，直接从 10 个流行的开源 Python 项目中提取推理问题，并保留其复杂的上下文和数据结构。
*   **关键技术细节**：
    *   **复杂类型序列化**：针对自定义对象（通常缺乏有效的 `__str__` 实现）的挑战，R2Eval 利用静态和动态程序分析，递归地将复杂数据类型分解为原始类型，并将其序列化为 JSON 格式。这使得模型能够“看到”对象的内部状态。
    *   **自动化检测与验证**：为了避免文本匹配带来的误判（False Negatives），R2Eval 会将 LLM 预测的输入/输出进行反序列化，并在运行时创建测试对象进行动态验证。
    *   **三元组表示**：每个问题表示为 `{P, I, O}`，其中 `P` 是包含依赖和类上下文的代码，`I/O` 是序列化后的输入和输出。

### 3. 实验设计
*   **数据集来源**：从 10 个广泛使用的 Python 项目中提取了 135 个推理问题，包括：`scikit-learn`、`django`、`requests`、`seaborn`、`sphinx`、`pytest`、`astropy`、`xarray`、`matplotlib` 和 `sympy`。
*   **对比基准（Benchmark）**：与广泛使用的 **CRUXEval** 进行对比（从中采样 135 个问题以保持规模一致）。
*   **评估对象**：对比了 6 个主流 LLM，包括推理模型（o4-mini, Gemini-2.5-Pro, DeepSeek-R1）和非推理/标准模型（GPT-4.1, Gemini-1.5-Pro, DeepSeekCoder-33B-Inst）。
*   **任务类型**：输入预测（Input Prediction）和输出预测（Output Prediction）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的算力资源（如 GPU 型号、数量或推理时长）。由于该研究侧重于基准测试的构建和对现有模型的评估（Inference），而非模型训练，因此对算力的需求主要集中在模型 API 调用和本地动态分析环境的搭建上。

### 5. 实验数量与充分性
*   **实验规模**：共 135 个真实世界问题，对比了 6 种模型在两个基准测试上的表现。
*   **充分性评价**：
    *   **优点**：实验涵盖了从基础模型到最新推理模型的广泛梯队，且对比了输入和输出两个维度的推理能力。
    *   **局限**：135 个样本量相对较小（受限于真实项目提取和序列化的复杂性），虽然具有代表性，但在统计严密性上可能不如包含数千个问题的合成数据集。

### 6. 论文的主要结论与发现
*   **性能大幅下滑**：相比 CRUXEval，所有 LLM 在 R2Eval 上的表现均出现剧烈下降。平均而言，输入预测性能下降了 **64.32%**，输出预测下降了 **52.22%**。
*   **推理模型的优势**：推理增强型模型（如 o4-mini, DeepSeek-R1）在真实场景下依然显著优于普通模型，领先幅度约 12%-14%。
*   **逆向推理更难**：在真实场景中，从输出推导输入的“逆向推理”难度远高于正向推理，这归因于复杂的代码构造和丰富的依赖关系。

### 7. 优点
*   **真实性高**：突破了原始数据类型的限制，首次系统性地处理了真实项目中的自定义类和复合对象序列化问题。
*   **评估严谨**：通过动态反序列化和运行时测试来验证模型预测，比单纯的文本比对更准确。
*   **揭示现状**：有力地质疑了 LLM 在简单基准测试上表现出的“强大推理能力”，指出了它们在处理实际工程逻辑时的短板。

### 8. 不足与局限
*   **样本规模有限**：135 个问题对于全面评估模型可能略显不足，且仅限于 Python 语言。
*   **依赖环境复杂**：由于涉及真实项目，复现该基准测试可能需要复杂的环境配置（如特定版本的第三方库）。
*   **静态上下文限制**：虽然提供了类上下文和依赖，但对于极大规模仓库的全局依赖处理仍有提升空间。

（完）

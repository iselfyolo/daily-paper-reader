---
title: "Domain-oriented RAG Assessment (DoRA): Synthetic Benchmarking for RAG-based Question Answering on Defense Documents"
title_zh: 面向领域的 RAG 评估 (DoRA)：针对国防文档上基于 RAG 的问答的合成基准测试
authors: "Bao Gia Doan, Aditya Joshi, Pantelis Elinas, Aarya Bodhankar, Oscar Leslie, Tom Marchant, Flora Salim"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17943v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 基于RAG问答的合成基准测试
tldr: 针对通用RAG基准测试因预训练数据重叠导致性能高估的问题，本文提出DoRA，一个基于国防领域文档的合成基准测试。DoRA包含6.5K个涵盖五种提问类型的实例，并提供可审计的证据段落。实验表明，在DoRA上进行微调能显著提升模型在特定领域的QA成功率并大幅降低幻觉率，为领域迁移下的回归测试提供了有效工具。
selection_source: fresh_fetch
motivation: 通用RAG基准测试因数据污染和缺乏严格归因要求，难以准确评估模型在特定领域（如国防）的实际部署性能。
method: 开发了包含6.5K个实例的DoRA基准，通过合成意图驱动的问答对并关联可审计的证据段落，涵盖查找、解释、总结、生成和提供五类任务。
result: "相比Llama3.1-8B-Instruct基座模型，经过DoRA微调的模型在QA任务成功率上提升了26%，且幻觉率降低了47%。"
conclusion: DoRA为评估和提升RAG系统在特定领域下的忠实度与准确性提供了可靠的基准，有效支持了防范数据污染的回归测试。
---

## 摘要
基于公开语料库的开放域 RAG 基准测试可能会因为预训练重叠和弱归因要求而高估部署性能。我们提出了 DoRA（面向领域的 RAG 评估），这是一个基于国防文档构建的领域基准测试，它将合成的、基于意图的问答 (QA) 与用于归因的可审计证据段落相结合。DoRA 涵盖了五种问题类型（查找、解释、总结、生成、提供），并包含 6500 个精选实例。在使用固定稠密检索器的端到端评估中，通用语言模型 (LM) 的表现相似，而在 DoRA 上训练的模型 (DoRA SFT) 相比基础模型 (Llama3.1-8B-Instruct) 取得了显著提升：问答任务成功率最高提升了 26%，同时在 RAG 忠实度评分中将幻觉率降低了 47%，支持领域偏移下感知污染的回归测试。

## Abstract
Open-domain RAG benchmarks over public corpora can overestimate deployment performance due to pretraining overlap and weak attribution requirements. We present DoRA (Domain-oriented RAG Assessment), a domain-grounded benchmark built from defense documents that pairs synthetic, intent-conditioned QA (question answering) with auditable evidence passages for attribution. DoRA covers five question types (find, explain, summarize, generate, provide) and contains 6.5K curated instances. In end-to-end evaluation with a fixed dense retriever, general-purpose Language Models (LMs) perform similarly, while a model trained on DoRA (DoRA SFT) yields large gains over the base model (Llama3.1-8B-Instruct): up to 26% improvement in QA task success, while reducing the hallucination rate by 47% in RAG faithfulness scores, supporting contamination-aware regression testing under domain shift.

---

## 论文详细总结（自动生成）

### 论文总结：Domain-oriented RAG Assessment (DoRA)

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，现有的检索增强生成（RAG）基准测试主要依赖于维基百科等公开语料库，这在实际部署中存在三大失效模式：
*   **数据污染**：公开语料常出现在大模型的预训练或指令微调数据中，导致评估分数虚高，掩盖了模型在私有数据上的泛化能力。
*   **意图不匹配**：现有的 QA 基准多侧重于事实性问答（Factoid QA），无法代表实际应用中复杂的查询意图。
*   **指标错位**：传统的重叠度指标（如 BLEU/ROUGE）侧重表面相似度，而忽视了 RAG 系统核心的“归因”和“忠实度”。
针对这些问题，论文提出了 **DoRA**，一个专门针对国防领域文档构建的合成基准测试，旨在为高风险、私有数据环境下的 RAG 系统提供可审计、抗污染的评估框架。

#### 2. 论文提出的方法论
DoRA 的核心思想是构建一个“意图驱动”且“证据锚定”的合成数据集生成流水线：
*   **五大意图分类**：根据国防工作流定义了五种问题类型：**FIND**（查找事实）、**EXPLAIN**（解释概念）、**SUMMARIZE**（总结报告）、**GENERATE**（生成清单/选项）、**PROVIDE**（提供定量数据）。
*   **任务感知证据选择**：
    1.  **预过滤**：使用自然语言推理（NLI）模型从文档块中筛选相关候选。
    2.  **LLM 评分**：利用 LLM 对证据束的完整性、互补性、连贯性和任务契合度进行评分，选择最优证据组合 $E$。
*   **合成 QA 生成**：基于选定的证据束 $E$ 和特定意图 $s$，通过提示词工程生成问题 $q$ 和参考答案 $a$。
*   **质量控制与归因**：
    *   **硬性检查**：如 PROVIDE 类型必须包含数字。
    *   **忠实度过滤**：使用文档蕴含得分（DES）、提取式跨度对齐（Span F1）和数值一致性检查，确保答案严格源自证据。
*   **DoRA SFT**：利用生成的合成数据对开源模型（如 Llama 3.1-8B）进行 LoRA 微调，以实现领域适配。

#### 3. 实验设计
*   **数据集**：基于 20 份国防种子文档，生成了约 6.5K 个 QA 实例。其中 1,259 个作为测试集，5,150 个作为训练集。
*   **Benchmark 场景**：
    1.  **检索器评估**：对比 BM25、GTE-multilingual、IBM Granite、BGE-M3 等。
    2.  **端到端 QA 评估**：在固定检索器（GTE）下测试不同生成模型的表现。
    3.  **Oracle 评估**：提供“金标准”证据，排除检索误差，纯粹评估生成能力。
*   **对比方法**：
    *   **通用模型**：GPT-4o, GPT-4o-mini, Claude-3-Haiku, Llama 3.1-8B, Mistral-7B, Qwen 2.5-7B 等。
    *   **基准对比**：Base 模型 vs. DoRA SFT 模型 vs. ICL（1-shot/2-shot 上下文学习）。
    *   **专家校验**：包含一个由领域专家手动标注的 86 个实例的小型数据集作为“天际线”对比。

#### 4. 资源与算力
*   **微调细节**：论文提到使用 **LoRA** 技术对 Llama 3.1-8B-Instruct 进行微调。
*   **算力说明**：**文中未明确说明具体的 GPU 型号、数量或具体的训练时长**。仅提到训练集规模为 5,150 个实例，并使用了 90/10 的训练/验证划分。

#### 5. 实验数量与充分性
*   **实验组数**：实验涵盖了检索器性能曲线（Hit@k, Recall@k）、端到端 QA 指标（Token F1, ROUGE-L, BLEU, BLEURT, BERTScore）以及 RAGEval 忠实度诊断（完整性、幻觉率、无关率）。
*   **充分性与公平性**：
    *   实验对比了当前主流的闭源和开源模型，具有代表性。
    *   通过 Oracle 实验隔离了检索和生成的误差，分析深入。
    *   引入了专家标注数据进行验证，增强了合成数据的可信度。
    *   **评价**：实验设计较为客观、公平，充分考虑了 RAG 系统的各个组件。

#### 6. 论文的主要结论与发现
*   **通用模型表现趋同**：在私有国防领域，如果不进行领域适配，顶级模型（如 GPT-4o）与中型开源模型在固定检索器下的表现差异不大，性能瓶颈主要在于证据获取和领域理解。
*   **SFT 提升显著**：经过 DoRA 数据微调的模型（DoRA SFT）在 QA 成功率上比基座模型提升了 **26%**，且在忠实度评估中将幻觉率降低了 **47%**。
*   **检索器差异**：在国防文档上，BM25 在 Top-1 检索中表现强劲，但在更大范围（k 值增加）时，稠密检索器（如 GTE）展现出更好的覆盖度和稳定性。
*   **合成数据的有效性**：即使是少量的专家数据（52 条）加入 SFT 训练，也能进一步提升模型在专家风格上的对齐能力。

#### 7. 优点
*   **领域针对性**：填补了国防领域 RAG 评估的空白，解决了公开数据集无法模拟私有环境的问题。
*   **意图多样性**：不仅关注事实检索，还涵盖了总结、解释等多种实际部署中常见的用户意图。
*   **可审计性**：每个 QA 对都绑定了明确的证据束，支持细粒度的归因分析和回归测试。
*   **实用性**：不仅提供评估基准，还展示了如何利用该基准进行模型微调以提升性能。

#### 8. 不足与局限
*   **样本规模**：6.5K 的数据量虽然对于特定领域尚可，但相比通用基准仍显单薄。
*   **自动指标局限**：尽管使用了 BERTScore 和 BLEURT，但自动指标在处理复杂语义和长文本总结时仍可能存在偏差。
*   **多跳推理不足**：目前的 DoRA 主要侧重于单文档或简单多块检索，对于需要复杂逻辑链的多跳（Multi-hop）推理覆盖较少（作者将其列为未来工作）。
*   **应用限制**：由于国防文档的敏感性，论文主要使用公开的国防文档进行演示，实际私有部署时的效果可能受限于具体文档的异构性。

（完）

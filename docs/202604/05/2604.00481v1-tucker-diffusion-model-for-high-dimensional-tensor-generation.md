---
title: Tucker Diffusion Model for High-dimensional Tensor Generation
title_zh: 用于高维张量生成的 Tucker 扩散模型
authors: "Jianhua Guo, Xinbing Kong, Zeyu Li, Junfan Mao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00481v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于高维张量生成的扩散模型
tldr: 针对高维张量数据生成难题，本文提出了一种基于低Tucker秩假设的Tucker扩散模型。该模型利用Tucker-Unet架构对得分函数进行结构化分解与高效估计。理论证明其收敛速度仅取决于张量各维度的最大值而非乘积，有效缓解了维度诅咒。实验表明，该方法在保持优异生成质量的同时，显著降低了训练和采样成本。
selection_source: fresh_fetch
motivation: 现有的扩散模型在处理具有多重线性结构的高维张量数据时，缺乏针对性的结构化建模方法，导致计算效率低下且难以捕捉张量内在特性。
method: 提出Tucker扩散模型，结合低Tucker秩假设对得分函数进行结构化分解，并设计了专门的Tucker-Unet架构进行高效估计。
result: 理论上实现了优于传统向量化方法的收敛速率，实验中在合成和真实数据集上均展现出更低的计算成本和卓越的生成性能。
conclusion: Tucker扩散模型为高维结构化张量生成提供了一个兼具理论保障和计算效率的新框架，具有广泛的应用潜力。
---

## 摘要
高维张量数据的统计推断在文献中已得到广泛研究，并广泛应用于经济学、生物学、机器学习等领域，但如何生成具有目标分布的结构化张量仍是一个新问题。作为强大的 AI 生成器，扩散模型在学习复杂分布方面取得了显著成功。然而，将其扩展到生成多线性张量值观测数据仍有待探索。在这项工作中，我们提出了一种新型的 Tucker 扩散模型，用于学习高维张量分布。我们证明了在低 Tucker 秩假设下，得分函数（score function）具有结构化分解，从而可以使用精心设计的名为 Tucker-Unet 的张量形状架构对其进行精确逼近和高效估计。此外，由估计的得分函数诱导的生成张量分布以取决于张量模态维度最大值的速率收敛到真实数据分布，从而相比于具有乘积依赖性的朴素向量化方法，提供了明显的理论优势。在实验方面，与现有方法相比，Tucker 扩散模型在合成和真实世界的张量生成任务中展现出强大的实际潜力，在显著降低训练和采样成本的同时，实现了相当甚至更优的统计性能。

## Abstract
Statistical inference on large-dimensional tensor data has been extensively studied in the literature and widely used in economics, biology, machine learning, and other fields, but how to generate a structured tensor with a target distribution is still a new problem. As profound AI generators, diffusion models have achieved remarkable success in learning complex distributions. However, their extension to generating multi-linear tensor-valued observations remains underexplored. In this work, we propose a novel Tucker diffusion model for learning high-dimensional tensor distributions. We show that the score function admits a structured decomposition under the low Tucker rank assumption, allowing it to be both accurately approximated and efficiently estimated using a carefully tailored tensor-shaped architecture named Tucker-Unet. Furthermore, the distribution of generated tensors, induced by the estimated score function, converges to the true data distribution at a rate depending on the maximum of tensor mode dimensions, thereby offering a clear theoretical advantage over the naive vectorized approach, which has a product dependence. Empirically, compared to existing approaches, the Tucker diffusion model demonstrates strong practical potential in synthetic and real-world tensor generation tasks, achieving comparable and sometimes even superior statistical performance with significantly reduced training and sampling costs.

---

## 论文详细总结（自动生成）

### 论文总结：用于高维张量生成的 Tucker 扩散模型 (Tucker Diffusion Model)

#### 1. 核心问题与整体含义（研究动机和背景）
在经济学、生物学和机器学习等领域，高维张量（多维数组）数据非常普遍。虽然扩散模型（Diffusion Models）在图像和文本生成上取得了巨大成功，但在处理高维张量时面临严峻挑战：
*   **维度诅咒**：若将张量简单向量化，维度将呈乘积级增长（如 $100^3 = 1,000,000$），导致计算负担沉重且需要海量训练样本。
*   **结构丢失**：传统的卷积神经网络（CNN）主要捕捉局部特征，难以有效利用张量固有的多线性低秩结构（如 Tucker 分解）。
*   **研究空白**：如何在保证统计效率的同时，生成符合特定目标分布的结构化高维张量，仍是一个尚未解决的前沿问题。

#### 2. 方法论：核心思想与技术细节
论文提出了 **Tucker 扩散模型**，其核心思想是将高维张量的生成任务投影到低维的 Tucker 流形上。
*   **得分函数分解（Score Decomposition Lemma）**：论文证明了在低 Tucker 秩假设下，高维得分函数可以分解为“子空间得分”（对应低维核心张量）和“补空间得分”（对应正交噪声部分）。
*   **Tucker-Unet 架构**：设计了一种专门的神经网络结构，包含五个关键组件：
    1.  **FiLM 层**：处理噪声的异质性。
    2.  **Tucker 编码器**：利用加载矩阵（Loading Matrices）将高维观测值投影到低维核心空间。
    3.  **Core-Net**：在低维空间内学习核心张量的扩散过程。
    4.  **Tucker 解码器**：将处理后的核心张量重新映射回高维空间。
    5.  **跳跃连接（Skip Connection）**：保持高频细节并进行时间校正。
*   **理论保障**：证明了生成分布的收敛速率取决于张量各维度中的**最大值**（$\max\{p_d\}$），而非所有维度的**乘积**（$\prod p_d$），这在理论上证明了该模型能有效缓解维度诅咒。

#### 3. 实验设计
论文通过合成数据和真实世界数据验证了模型性能：
*   **数据集/场景**：
    1.  **合成数据**：基于矩阵因子模型生成的 $64 \times 64$ 矩阵。
    2.  **分子生成**：PROTEINS 和 PRC FM 数据集（拓扑数据分析编码的张量）。
    3.  **医疗影像**：OrganAMNIST 数据集（$128 \times 128$ 肝脏肿瘤分割图像）。
    4.  **城市交通**：纽约市出租车行程转移张量（$24 \times 128 \times 128$）。
*   **Benchmark 与对比方法**：
    *   **Conv-Unet**：标准的基于卷积的扩散模型。
    *   **Tucker-Unet (Cold/Warm/Fixed)**：分别代表冷启动（随机初始化）、预训练（利用 Tucker PCA 初始化）和固定参数的三种变体。
*   **评估指标**：子空间质量（$D(U, \hat{U})$）、核心弗雷歇距离（CFD）、测试重构误差（RE test）以及训练/采样时间。

#### 4. 资源与算力
*   **硬件设备**：实验明确指出使用了单张 **NVIDIA RTX 4070 Ti SUPER** 显卡进行公平对比。
*   **训练与采样耗时**：
    *   在合成实验中，Tucker-Unet 的单轮训练时间约为 14 秒，而 Conv-Unet 约为 45 秒。
    *   **采样加速显著**：生成 2048 个样本，Tucker-Unet 仅需约 265 秒，而 Conv-Unet 需约 23,000 秒，**采样时间缩短了约 98%**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成模拟到生物、医疗、交通等多个领域的真实数据，实验设计较为全面。
*   **消融与对比**：对比了不同初始化策略（冷启动 vs 预训练），验证了引入先验结构信息的必要性。
*   **客观性**：通过多次随机种子运行并报告均值和标准差，确保了结果的可重复性和客观性。

#### 6. 主要结论与发现
*   **高效性**：Tucker-Unet 在显著降低计算成本（训练快 3 倍，采样快近 100 倍）的同时，保持了与高维模型相当甚至更优的生成质量。
*   **结构捕捉**：该模型能准确捕捉张量的空间转移动态（如纽约出租车流量模式）和复杂的生物结构特征。
*   **理论一致性**：实验结果支持了理论推导，即利用低秩结构可以大幅提升高维数据的学习效率。

#### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了新架构，还给出了严谨的收敛速率证明，填补了张量扩散模型的理论空白。
*   **端到端训练**：不同于两阶段生成方法，Tucker-Unet 支持端到端训练，避免了多级生成带来的误差累积。
*   **极高的实用价值**：对于算力受限的环境，该模型提供了一种生成高质量高维数据的可行方案。

#### 8. 不足与局限
*   **低秩假设依赖**：模型性能高度依赖于数据是否具有显著的低 Tucker 秩结构。对于秩较高的复杂数据，效果可能受限。
*   **秩的选择**：实验中需要预先设定 Tucker 秩（$r_d$），在实际应用中，如何自动确定最优秩仍是一个挑战。
*   **超参数敏感性**：虽然提供了预训练（Warm-start）方案，但模型对初始化和超参数（如早期停止时间 $t_0$）仍有一定的敏感性。

（完）

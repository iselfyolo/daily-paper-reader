---
title: "CRIT: Graph-Based Automatic Data Synthesis to Enhance Cross-Modal Multi-Hop Reasoning"
title_zh: CRIT：基于图的自动数据合成以增强跨模态多跳推理
authors: "Junyoung Sung, Seungwoo Lyu, Minjun Kim, Sumin An, Arsha Nagrani, Paul Hongsuck Seo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01634v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 基于图的自动流水线，用于生成复杂的跨模态推理数据
tldr: 针对现有视觉语言模型在跨模态多步推理中表现不佳且易产生幻觉的问题，本文提出了CRIT数据集和基准。该研究通过一种基于图的自动化数据合成流水线，生成了涵盖自然图像、视频和富文本等领域的复杂推理任务。实验证明，CRIT能显著提升模型在多步推理任务（如SPIQA）上的表现，有效增强了模型对视觉证据的整合与推理能力。
selection_source: fresh_fetch
motivation: 现有基准和训练数据多依赖单一模态，缺乏对跨模态多步推理的挑战，导致模型在处理复杂视觉-文本关联时容易产生幻觉。
method: 开发了一种基于图的自动化流水线，用于合成跨越自然图像、视频和富文本等多种领域的复杂跨模态推理任务。
result: 实验显示顶尖模型在CRIT基准上表现欠佳，而经过CRIT训练的模型在跨模态多步推理及标准多模态基准上均取得了显著提升。
conclusion: CRIT通过高质量的合成数据有效填补了跨模态多步推理的空白，是提升视觉语言模型推理可靠性和减少幻觉的有效方案。
---

## 摘要
现实世界的推理通常需要结合跨模态的信息，在多跳过程中将文本上下文与视觉线索联系起来。然而，大多数多模态基准测试未能捕捉到这种能力：它们通常依赖于单张图像或图像集，其中答案仅凭单一模态即可推断。这种局限性也反映在训练数据中，交错的图文内容很少强制执行互补的多跳推理。因此，视觉语言模型（VLM）经常产生幻觉，并生成缺乏视觉证据支撑的推理轨迹。为了弥补这一差距，我们推出了 CRIT，这是一个通过基于图的自动流水线构建的新数据集和基准测试，用于生成复杂的跨模态推理任务。CRIT 涵盖了从自然图像、视频到富文本源的多个领域，并包含一个经过人工验证的测试集，以进行可靠的评估。在该基准测试上的实验表明，即使是最先进的模型在处理此类推理任务时也面临挑战。在 CRIT 上训练的模型在跨模态多跳推理方面表现出显著提升，包括在 SPIQA 和其他标准多模态基准测试上的大幅改进。

## Abstract
Real-world reasoning often requires combining information across modalities, connecting textual context with visual cues in a multi-hop process. Yet, most multimodal benchmarks fail to capture this ability: they typically rely on single images or set of images, where answers can be inferred from a single modality alone. This limitation is mirrored in the training data, where interleaved image-text content rarely enforces complementary, multi-hop reasoning. As a result, Vision-Language Models (VLMs) frequently hallucinate and produce reasoning traces poorly grounded in visual evidence. To address this gap, we introduce CRIT, a new dataset and benchmark built with a graph-based automatic pipeline for generating complex cross-modal reasoning tasks. CRIT consists of diverse domains ranging from natural images, videos, and text-rich sources, and includes a manually verified test set for reliable evaluation. Experiments on this benchmark reveal that even state-of-the-art models struggle on such reasoning tasks. Models trained on CRIT show significant gains in cross-modal multi-hop reasoning, including strong improvements on SPIQA and other standard multimodal benchmarks.

---

## 论文详细总结（自动生成）

### 论文总结：CRIT —— 基于图的自动数据合成以增强跨模态多跳推理

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）虽然在基础任务上表现出色，但在处理需要结合交错图文信息进行**多步（多跳）推理**的任务时仍面临巨大挑战。
*   **研究动机**：现有的多模态基准测试（如 MMBench, MathVista）往往依赖单张图像，且许多问题仅凭文本或图像单一模态即可推断出答案，未能真正测试“跨模态整合能力”。
*   **核心问题**：训练数据中缺乏高质量、互补性的图文交错推理样本，导致模型在推理过程中容易产生幻觉，且推理轨迹（Chain of Thought）往往脱离视觉证据。

#### 2. 论文提出的方法论：CRIT 流水线
论文提出了一种**基于图的自动化数据生成框架**，旨在不依赖 VLM 的情况下（避免循环偏见），利用 LLM 和结构化知识生成复杂的推理任务。其流程分为三个阶段：
*   **多模态内容图构建**：
    *   从带有场景图（Scene Graph）标注的图像出发，过滤并合并多个图像的实体。
    *   利用 LLM 增加文本实体和关系，作为连接不同图像实体的“桥梁”，形成统一的跨模态图结构。
*   **文本上下文生成**：
    *   根据图中定义的文本节点及其与图像节点的关联，利用 LLM 生成多样化的叙述性文本（如故事、日记、科学论文段落），确保文本与图像内容互补而非冗余。
*   **问答对（QA）生成与过滤**：
    *   在图中采样 1 到 5 跳的子图路径，确保路径终点落在图像属性上。
    *   提示 LLM 生成问题，要求必须通过完整推理链才能回答，且隐藏中间实体。
    *   **严格过滤**：使用三个不同的 LLM 进行测试，剔除仅靠单一模态（纯文本或纯图像）即可回答的问题。

#### 3. 实验设计
*   **数据集构成**：CRIT 包含三个领域：自然图像（基于 GQA）、视频帧（基于 ActivityNet Captions）和科学论文（基于 SPIQA）。
*   **Benchmark 与对比**：
    *   **测试集**：包含 1,446 个样本（1,607 个 QA 对），经过 12 名 STEM 背景的专业人员人工验证。
    *   **对比模型**：包括闭源模型（GPT-4o, Gemini 2.0 Flash）和开源模型（Qwen2.5-VL, LLaVA-Onevision, Intern2.5-VL 等）。
    *   **外部基准**：在 SPIQA, VEGA, MMQA, FCMR 等跨模态推理基准以及 BLINK, MME 等标准多模态基准上验证泛化能力。

#### 4. 资源与算力
*   **硬件**：训练使用了 **4 台 NVIDIA H100 80GB GPU**。
*   **模型生成**：使用 Qwen3-30B-Instruct 作为生成流水线的主要引擎。
*   **训练细节**：采用 LoRA 微调技术，针对 Qwen2.5-VL-7B 和 Idefics2-8B 进行微调。具体训练时长未详细标明，但给出了超参数（如 Epochs=1, Batch size=64/128）。

#### 5. 实验数量与充分性
*   **实验规模**：生成了约 **27.1 万个 QA 对**用于训练，规模庞大。
*   **充分性**：
    *   涵盖了从 2 跳到 5 跳的不同难度实验。
    *   进行了消融实验，验证了“跨图像边缘”和“基于图的生成”对数据质量的贡献。
    *   在 10 多个外部基准测试上进行了验证，证明了 CRIT 训练不仅提升了推理能力，还保持了通用多模态能力。
*   **客观性**：通过人工验证测试集和多模型交叉过滤生成数据，确保了评估的可靠性。

#### 6. 主要结论与发现
*   **现有模型表现不佳**：即使是 GPT-4o 和 Qwen2.5-VL-72B，在 CRIT 基准上的表现也远未达到饱和（准确率较低），证明了任务的挑战性。
*   **微调效果显著**：在 CRIT 上微调后的模型在 SPIQA 等推理任务上性能翻倍（如 METEOR 分数从 3.6 升至 10.5）。
*   **错误分析**：55% 的错误源于**证据定位错误**（模型找错了图像或文本段落），32% 源于视觉感知错误，说明“在复杂上下文中寻找证据”是当前 VLM 的最大瓶颈。

#### 7. 优点
*   **结构化生成**：利用图结构保证了推理链的逻辑严密性，避免了直接提示 LLM 生成时常见的逻辑断裂。
*   **互补性设计**：强制要求跨模态整合，有效解决了现有数据集“单模态可解”的弊端。
*   **领域多样性**：不仅限于自然图像，还扩展到了视频和复杂的科学文献领域。
*   **可解释性**：每个 QA 对都附带了基于子图的推理轨迹（CoT），便于模型训练和错误诊断。

#### 8. 不足与局限
*   **合成数据偏差**：尽管有图约束，生成的文本仍可能带有 LLM 特有的风格偏见或虚构内容。
*   **流水线误差累积**：多阶段生成（图 -> 文本 -> QA）中，前期的微小错误可能会在后期放大。
*   **计算开销**：自动生成大规模图结构和文本上下文需要消耗大量的 LLM 推理算力。
*   **科学领域挑战**：在科学论文领域，即使经过微调，模型的表现提升依然有限，说明长文本+复杂图表的推理仍是极大的难题。

（完）

---
title: Infeasibility Aware Large Language Models for Combinatorial Optimization
title_zh: 具备不可行性感知能力的大语言模型用于组合优化
authors: "Yakun Wang, Min Chen, Zeguan Wu, Junyu Liu, Sitao Zhang, Zhenwen Shao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01455v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 组合优化问题的可验证数据集构建
tldr: 本研究针对LLM在组合优化中忽视不可行性检测的问题，提出一种不可行性感知框架。通过构建带证明的训练集微调8B模型，使其兼具解生成与不可行性识别能力。该方法不仅在准确率上显著超越现有模型，还能作为热启动手段使下游局部搜索效率翻倍。
selection_source: fresh_fetch
motivation: 现有LLM优化方法主要关注可行解生成，缺乏对NP-hard问题不可行性的识别与处理能力。
method: 提出结合可证明数据集构建、监督微调与LLM辅助搜索的框架，并引入数学规划与不可行性筛选机制。
result: "微调模型在准确率上比GPT-5.2提升达30%，且引导的热启动使下游搜索速度提升2倍。"
conclusion: 引入不可行性感知能显著增强LLM解决复杂优化问题的能力，并有效加速传统搜索算法。
---

## 摘要
大语言模型（LLMs）正越来越多地被探索用于解决 NP 难组合优化问题，但现有方法大多强调可行实例的解生成，而未明确解决不可行性检测问题。我们提出了一种不可行性感知框架，该框架结合了可验证数据集构建、监督微调以及 LLM 辅助的下游搜索。针对子图嵌入（minor-embedding）问题，我们引入了一种新的数学规划公式以及可证明的零阶段不可行性筛选，从而能够大规模构建训练实例，这些实例被标记为带有结构化证明的可行实例或可证明的不可行实例。利用通过该精确优化流水线生成的训练数据，我们展示了一个 8B 参数的大语言模型可以经过微调，共同执行解生成和不可行性检测。我们进一步利用 LLM 的输出作为下游局部搜索的热启动，提供了一种即使在 LLM 输出不完美的情况下也能加速优化的实用方法。实验表明，我们的微调模型相比 GPT-5.2 将整体准确率提高了多达 30%；同时，与下游局部搜索中的从零开始相比，LLM 引导的热启动提供了高达 2 倍的加速。

## Abstract
Large language models (LLMs) are increasingly explored for NP-hard combinatorial optimization problems, but most existing methods emphasize feasible-instance solution generation and do not explicitly address infeasibility detection. We propose an infeasibility-aware framework that combines certifiable dataset construction, supervised fine-tuning, and LLM-assisted downstream search. For the minor-embedding problem, we introduce a new mathematical programming formulation together with provable zero-phase infeasibility screening, which enables scalable construction of training instances labeled either as feasible with structured certificates or as certifiably infeasible. Using training data generated through this exact optimization pipeline, we show that an 8B-parameter LLM can be fine-tuned to jointly perform solution generation and infeasibility detection. We further utilize LLM outputs as warm starts for downstream local search, providing a practical way to accelerate optimization even when the LLM outputs are imperfect. Experiments show that our fine-tuned model improves overall accuracy by up to 30\% over GPT-5.2; meanwhile LLM-guided warm starts provide up to $2\times$ speedup compared with starting from scratch in downstream local search.

---

## 论文详细总结（自动生成）

以下是对论文《Infeasibility Aware Large Language Models for Combinatorial Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管大语言模型（LLM）在解决组合优化（CO）问题上展现出潜力，但现有研究主要集中在“生成可行解”上，忽视了**不可行性检测（Infeasibility Detection）**。在实际应用中，识别一个问题无解（UNSAT）与找到解（SAT）同样重要。
*   **核心挑战**：
    1.  **数据稀缺**：对于复杂的 NP-hard 问题（如子图嵌入 Minor-embedding），现有求解器难以在大规模时间内生成带证明的标签。
    2.  **模型可靠性**：LLM 直接生成的解往往存在结构性错误，且缺乏自我修复机制。
    3.  **负向偏差**：通用模型在面对复杂约束时，往往倾向于简单地回答“不可行”，导致在可行实例上的准确率极低。

### 2. 论文提出的方法论
该论文提出了一个**不可行性感知框架**，包含三个核心阶段：
*   **可验证数据集构建**：
    *   针对**子图嵌入问题**，提出了一种新的数学规划（Integer Programming）公式。
    *   引入**零阶段不可行性筛选（Zero-phase screening）**：利用图论属性（如度数约束、顶点/边数量限制）预先排除明显不可行的实例，提高数据生成效率。
    *   结合精确求解器（Gurobi）和启发式算法（minorminer）生成包含可行解证明和不可行性标签的高质量数据集。
*   **监督微调（SFT）**：
    *   使用 **LoRA** 技术对 Llama-3.1-8B 进行微调。
    *   采用**统一且紧凑的输出格式**：模型直接输出“Yes + 结构化解”或“No”，不使用思维链（CoT），以提高推理效率并降低 Token 消耗。
*   **推理与修复（LLM 辅助搜索）**：
    *   **Best-of-N 采样**：通过多次采样并结合多数投票机制来判定不可行性。
    *   **热启动 Feasibility Jump (FJ)**：将 LLM 输出的不完美解作为初始值，输入到局部搜索算法（FJ）中进行快速修复和优化。

### 3. 实验设计
*   **实验场景**：主要针对**子图嵌入（Minor-embedding）**（量子计算中的关键预处理步骤）和 **k-着色问题**。
*   **数据集**：每个问题构建了 10,000 个平衡实例（50% SAT, 50% UNSAT），涵盖多种图拓扑（Erdős–Rényi, Barabási–Albert 等）。
*   **Benchmark 与对比方法**：
    *   **前沿模型**：GPT-5.2, GPT-5.2-Reasoning, DeepSeek-V3.2, Llama-3.3-70B, Grok-4.1-Fast。
    *   **传统求解器**：Gurobi（默认模式及 NoRel 启发式模式）。
    *   **消融/变体**：Best-of-1 vs. Best-of-8。

### 4. 资源与算力
*   **模型规模**：主要基于 Llama-3.1-8B。
*   **训练框架**：使用了 **Unsloth** 和 TRL SFTTrainer 进行高效微调。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了使用 `bf16` 精度和特定的超参数设置（如 Batch Size 为 8，学习率为 2e-4）。

### 5. 实验数量与充分性
*   **实验规模**：每类问题 10,000 组数据，实验覆盖了从 10 到 300 个顶点的不同规模实例。
*   **充分性**：
    *   进行了**分布外（OOD）泛化测试**，验证了模型在未见过的图拓扑（如 Pegasus 图）上的表现。
    *   对比了 LLM 独立求解与“LLM + 局部搜索”混合方案的效果。
    *   **客观性**：通过平衡 SAT/UNSAT 比例，避免了模型通过猜测单一类别来获得高分的可能性。

### 6. 论文的主要结论与发现
*   **性能超越**：微调后的 8B 模型在子图嵌入任务上的总准确率达到 99.9%，远超 GPT-5.2（78.5%）。
*   **消除偏差**：通用前沿模型在复杂优化问题上表现出严重的“负向偏差”（倾向于预测不可行），而微调模型在 SAT 和 UNSAT 实例上表现均衡。
*   **加速搜索**：利用 LLM 输出作为热启动，使下游局部搜索算法（Feasibility Jump）的收敛速度提升了 **2 倍**，且成功率更高。
*   **泛化能力**：该框架在不同类型的组合优化问题（着色 vs. 嵌入）间具有良好的迁移性。

### 7. 优点（亮点）
*   **填补空白**：首次系统性地在 LLM 组合优化研究中引入了“不可行性感知”。
*   **数学严谨性**：通过数学规划和零阶段筛选理论保证了训练数据的质量。
*   **实用性强**：不追求 LLM 100% 正确，而是将其作为传统优化流水线的高效“热启动”组件，具有很强的工业应用潜力。

### 8. 不足与局限
*   **模型规模限制**：主要实验集中在 8B 模型，尚未探讨更大规模模型（如 70B+）在更复杂约束下的表现。
*   **问题覆盖面**：虽然验证了两个问题，但对于具有连续变量或更复杂非线性约束的混合整数规划（MIP）问题的适用性尚待研究。
*   **Token 长度风险**：对于超大规模图，图结构的文本表示可能会超出 LLM 的上下文窗口限制。

（完）

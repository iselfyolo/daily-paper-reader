---
title: "TGIF2: Extended Text-Guided Inpainting Forgery Dataset & Benchmark"
title_zh: TGIF2：扩展的文本引导修复伪造数据集与基准
authors: "Hannes Mareen, Dimitrios Karageorgiou, Paschalis Giakoumoglou, Peter Lambert, Symeon Papadopoulos, Glenn Van Wallendael"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28613v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 构建用于文本引导修复伪造的新研究数据集
tldr: 随着生成式AI的发展，文本引导的图像修复给媒体取证带来巨大挑战。本文推出TGIF2数据集，作为TGIF的扩展版，它集成了FLUX.1等最新模型生成的篡改图像及非语义掩码。研究评估了现有取证方法在定位完全再生图像及应对生成式超分辨率攻击时的表现，揭示了现有技术在泛化性、对象偏见及鲁棒性方面的局限，为现代图像篡改检测提供了新的基准。
selection_source: fresh_fetch
motivation: 现有取证方法难以有效定位完全再生的篡改图像，且随着FLUX.1等新生成模型的出现，亟需更新的数据集和基准来评估取证技术的鲁棒性。
method: 扩展原有的TGIF数据集，引入由FLUX.1模型生成的编辑内容和随机非语义掩码，并针对完全再生图像进行微调及生成式超分辨率攻击测试。
result: 实验表明现有取证方法在FLUX.1生成的图像上表现下降，且生成式超分辨率操作会显著削弱取证痕迹，同时微调过程揭示了模型存在对象偏见。
conclusion: TGIF2数据集通过涵盖最新的生成技术和增强操作，为深入分析和提升现代图像取证算法的泛化能力与鲁棒性提供了重要参考。
---

## 摘要
生成式人工智能使文本引导的图像修复（inpainting）成为一种强大的图像编辑工具，但同时也给媒体取证带来了日益严峻的挑战。现有的基准测试（包括我们的文本引导修复伪造（TGIF）数据集）表明，图像伪造定位（IFL）方法可以定位拼接图像中的篡改，但在完全重新生成（FR）的图像中表现不佳；而合成图像检测（SID）方法可以检测完全重新生成的图像，但无法进行定位。随着新的生成式修复模型的出现，以及 FR 图像定位这一开放性问题依然存在，需要更新的数据集和基准测试。我们推出了 TGIF2，它是 TGIF 的扩展版本，涵盖了文本引导修复的最新进展，并能对取证鲁棒性进行更深入的分析。TGIF2 在原始数据集的基础上，增加了由 FLUX.1 模型生成的编辑内容，以及随机的非语义掩码。利用 TGIF2 数据集，我们进行了涵盖 IFL 和 SID 的取证评估，包括在 FR 图像上微调 IFL 方法以及生成式超分辨率攻击。实验表明，IFL 和 SID 方法在处理 FLUX.1 篡改时性能均有所下降，突显了泛化能力的局限性。此外，虽然微调提高了在 FR 图像上的定位能力，但使用随机非语义掩码进行的评估揭示了对象偏差。此外，生成式超分辨率显著削弱了取证痕迹，表明常见的图像增强操作可能会破坏现有的取证流程。总之，TGIF2 提供了一个更新的数据集和基准，为现代修复技术和基于人工智能的图像增强所带来的挑战提供了新的见解。TGIF2 可在 https://github.com/IDLabMedia/tgif-dataset 获取。

## Abstract
Generative AI has made text-guided inpainting a powerful image editing tool, but at the same time a growing challenge for media forensics. Existing benchmarks, including our text-guided inpainting forgery (TGIF) dataset, show that image forgery localization (IFL) methods can localize manipulations in spliced images but struggle not in fully regenerated (FR) images, while synthetic image detection (SID) methods can detect fully regenerated images but cannot perform localization. With new generative inpainting models emerging and the open problem of localization in FR images remaining, updated datasets and benchmarks are needed. We introduce TGIF2, an extended version of TGIF, that captures recent advances in text-guided inpainting and enables a deeper analysis of forensic robustness. TGIF2 augments the original dataset with edits generated by FLUX.1 models, as well as with random non-semantic masks. Using the TGIF2 dataset, we conduct a forensic evaluation spanning IFL and SID, including fine-tuning IFL methods on FR images and generative super-resolution attacks. Our experiments show that both IFL and SID methods degrade on FLUX.1 manipulations, highlighting limited generalization. Additionally, while fine-tuning improves localization on FR images, evaluation with random non-semantic masks reveals object bias. Furthermore, generative super-resolution significantly weakens forensic traces, demonstrating that common image enhancement operations can undermine current forensic pipelines. In summary, TGIF2 provides an updated dataset and benchmark, which enables new insights into the challenges posed by modern inpainting and AI-based image enhancements. TGIF2 is available at https://github.com/IDLabMedia/tgif-dataset.

---

## 论文详细总结（自动生成）

### TGIF2: 扩展的文本引导修复伪造数据集与基准 —— 论文总结

#### 1. 核心问题与整体含义
*   **研究动机**：随着生成式 AI（如 Stable Diffusion, FLUX.1）的普及，文本引导的图像修复（Inpainting）已成为极易获取的篡改手段。
*   **核心挑战**：
    *   **完全再生（Fully Regenerated, FR）图像**：与传统的“拼接（Splicing）”不同，FR 模式下整张图像都会被重新生成，这会抹除传统的局部取证痕迹，导致现有定位方法失效。
    *   **模型迭代快**：现有数据集缺乏对最新高性能模型（如 FLUX.1）的覆盖。
    *   **语义偏见**：现有取证模型可能只是学会了识别“物体边缘”，而非真正的“取证痕迹”。
*   **研究意义**：本文通过构建 TGIF2 数据集，旨在提供一个更全面、更具挑战性的基准，以评估和提升取证算法在现代 AI 篡改环境下的鲁棒性。

#### 2. 核心方法论
*   **数据集构建流程**：
    1.  **源数据**：采用 MS-COCO 验证集，筛选出高质量、高分辨率的真实图像。
    2.  **生成模型**：集成了 6 种模型，包括 SD2、SDXL、Adobe Firefly 以及最新的 **FLUX.1 ([schnell], [dev], Fill [dev])**。
    3.  **掩码策略**：
        *   **语义掩码**：基于物体的边界框或分割掩码。
        *   **随机非语义掩码**：新增随机位置和大小的矩形掩码，用于排除模型对特定物体的偏见。
    4.  **篡改模式**：同时提供 **拼接（SP）** 和 **完全再生（FR）** 两个版本。
*   **取证增强实验**：
    *   **微调（Fine-tuning）**：针对 FR 图像定位难题，对 TruFor 和 MMFusion 两个 SOTA 模型进行微调。
    *   **攻击模拟**：引入生成式超分辨率（Real-ESRGAN）作为后期处理攻击，测试取证痕迹的留存率。

#### 3. 实验设计
*   **数据集规模**：TGIF2 包含约 27.1 万张伪造图像，分为 19 个子集。
*   **Benchmark 对象**：
    *   **图像伪造定位（IFL）**：对比了 PSCC-Net, SPAN, TruFor, MMFusion, CAT-Net 等 8 种方法。
    *   **合成图像检测（SID）**：对比了 CNNDetect, Dire, B-Free, RINE-ITW 等 17 种方法。
*   **评估指标**：IFL 使用像素级 F1 分数和 IoU；SID 使用 AUC 分数。
*   **对比维度**：跨模型泛化性、语义 vs. 随机掩码、生成质量与检测性能的相关性、超分辨率攻击下的鲁棒性。

#### 4. 资源与算力
*   **文中说明**：论文**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **相关信息**：提到 FLUX.1 [schnell] 仅需 1-4 步推理即可生成高质量图像，暗示了该模型在数据集构建过程中的高效性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 27 万余张图像，进行了大规模的交叉验证和基准测试，实验组数非常多。
*   **充分性**：
    *   **客观性**：通过引入随机掩码，客观地揭示了现有模型对“物体”的依赖。
    *   **公平性**：数据集划分（80/10/10）严格遵循类别不重叠原则，避免了数据泄露。
    *   **全面性**：不仅测试了现有模型，还进行了微调实验和攻击实验，覆盖了取证研究的多个维度。

#### 6. 主要结论与发现
*   **FLUX.1 的威胁**：FLUX.1 生成的图像在视觉和统计特性上更难被现有 IFL 和 SID 方法捕捉，性能显著下降。
*   **FR 定位依然是难题**：即使是 SOTA 模型，在未经针对性训练时，对 FR 图像的定位能力几乎为零。
*   **对象偏见（Object Bias）**：实验证明，许多 IFL 模型在随机掩码上的表现远差于语义掩码，说明模型过度依赖语义特征而非底层取证痕迹。
*   **超分辨率攻击的破坏性**：生成式超分辨率（Real-ESRGAN）能极大地削弱取证痕迹，其破坏力远超传统的双三次插值，对现有取证流程构成严重威胁。

#### 7. 优点
*   **时效性强**：率先将 FLUX.1 这一前沿模型引入取证数据集。
*   **实验设计严谨**：通过“随机掩码”这一变量，精准地识别出了取证模型中的“捷径学习（Shortcut Learning）”问题。
*   **多维度评估**：不仅关注检测率，还深入探讨了生成质量、后期处理攻击等实际应用场景。

#### 8. 不足与局限
*   **泛化瓶颈**：微调后的模型虽然在同分布数据上表现良好，但在跨模型（Out-of-domain）场景下依然脆弱。
*   **质量指标失效**：研究发现现有的美学和文本匹配指标无法有效预测图像的“可检测性”，暗示需要开发专门针对取证的质量评估工具。
*   **算力成本不明**：缺乏训练成本的量化数据，不便于后续研究者评估复现难度。

（完）

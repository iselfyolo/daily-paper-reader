---
title: "ReLeVAnT: Relevance Lexical Vectors for Accurate Legal Text Classification"
title_zh: ReLeVAnT：用于准确法律文本分类的相关性词汇向量
authors: "Ishaan Gakhar, Harsh Nandwani"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22292v1"
tags: ["query:q8"]
score: 9.0
evidence: 用于法院备案和检索的法律文件分类
tldr: "法律文档分类对法律实务至关重要，但现有方法往往依赖结构化元数据或高算力的大模型。本文提出ReLeVAnT框架，通过n-gram处理、对比得分匹配和浅层神经网络实现高效的二分类。该方法利用一次性关键词提取和轻量级分类器，在LexGLUE数据集上达到了99.3%的准确率，为法律文本分类提供了一种低功耗且高精度的解决方案。"
selection_source: fresh_fetch
motivation: 针对现有法律文本分类方法过度依赖元数据和高昂计算成本的问题，寻求一种基于文档判别性特征的高效分类方案。
method: 提出ReLeVAnT框架，结合n-gram处理、对比得分匹配和浅层神经网络进行特征提取与分类。
result: "在LexGLUE数据集上实现了99.3%的准确率和98.7%的F1分数，表现优异。"
conclusion: 该研究证明了利用词汇向量和浅层模型在法律文本分类任务中兼顾高精度与低计算开销的可行性。
---

## 摘要
从非结构化数据语料库中对法律文档进行分类在下游任务中具有多个关键应用。与法院备案相关的文档在起草动议、备忘录和提纲等用例，以及案卷摘要、检索系统和训练数据管理等任务中至关重要。当前的方法主要基于提供的元数据、大语言模型（LLM）提取的元数据或多模态方法进行分类。这些方法依赖于结构化数据、元数据和大量的计算资源。本研究从利用文档类别间判别性特征的角度来处理这一任务。作者提出了 ReLeVAnT，这是一个用于法律文档二分类的框架。ReLeVAnT 利用 n-gram 处理、对比得分匹配和浅层神经网络作为判别性分类的主要驱动力。它通过对每个语料库进行一次性关键词提取，并结合浅层分类器，在 LexGLUE 数据集上实现了 99.3% 的准确率和 98.7% 的 F1 分数，能够快速且可靠地对文档进行分类。

## Abstract
The classification of legal documents from an unstructured data corpus has several crucial applications in downstream tasks. Documents relevant to court filings are key in use cases such as drafting motions, memos, and outlines, as well as in tasks like docket summarisation, retrieval systems, and training data curation. Current methods classify based on provided metadata, LLM-extracted metadata, or multimodal methods. These methods depend on structured data, metadata, and extensive computational power. This task is approached from a perspective of leveraging discriminative features in the documents between classes. The authors propose ReLeVAnT, a framework for legal document binary classification. ReLeVAnT utilises n-gram processing, contrastive score matching, and a shallow neural network as the primary drivers for discriminative classification. It leverages one-time keyword extraction per corpus, followed by a shallow classifier to swiftly and reliably classify documents with 99.3% accuracy and 98.7% F1 score on the LexGLUE dataset.

---

## 论文详细总结（自动生成）

以下是对论文《ReLeVAnT: Relevance Lexical Vectors for Accurate Legal Text Classification》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖元数据（Metadata）、特定文档结构或高昂计算资源（如大语言模型 LLM）的情况下，从海量非结构化法律语料库中准确识别与法院备案相关的“相关文档”。
*   **研究动机**：
    *   **现有方法局限性**：目前的法律文本分类多依赖于文件名、结构化元数据或多模态信息，但在现实法律实务中，元数据往往缺失或充满噪声。
    *   **计算成本高昂**：基于 Transformer 或 LLM 的方法虽然效果好，但处理大规模长文档时算力开销巨大，难以在大型律师事务所大规模推广。
    *   **法律文本特性**：法律文档的“相关性”往往由特定的判别性短语（如“Motion to Dismiss”）决定，而非全文的语义相似度。

### 2. 方法论
ReLeVAnT 提出了一个两阶段的轻量级框架：**关键词提取（KE）**和**神经网络分类（CLS）**。

*   **核心思想**：通过对比分析相关与不相关文档中的词频差异，提取具有强判别力的 n-gram 关键词，并将其转化为稀疏向量输入浅层神经网络。
*   **关键技术流程**：
    1.  **预处理与过滤**：使用 NER（命名实体识别）移除人名（避免特定案件干扰），保留组织和地点；使用 `eyecite` 工具移除法律条款引用。
    2.  **n-gram 构造**：生成 1 到 4-gram 的短语，捕捉比单字更强的法律信号。
    3.  **对比得分匹配 (CSM)**：计算每个 term 在正负样本中的频率对比，公式为：
        $$CSM(t) = \frac{f^+_t}{f^+_t + (f^-_t)^p + \epsilon}$$
        其中 $p$ 是惩罚指数，用于严厉惩罚在不相关文档中频繁出现的词。
    4.  **文档频率 (DF) 评分**：衡量关键词在文档集中的覆盖广度。
    5.  **综合评分与向量化**：结合 CSM 和 DF 得到最终权重 $S(t)$，将文档转化为基于关键词频率的稀疏向量。
    6.  **浅层分类器**：使用一个简单的多层感知机（MLP），结构为 [512-256-128-64-1]，配合 Sigmoid 激活函数进行二分类。

### 3. 实验设计
*   **数据集**：使用 **LexGLUE** 基准测试集。
    *   **正样本（相关）**：ECtHR A（欧洲人权法院案例）和 SCOTUS（美国最高法院意见）。
    *   **负样本（不相关）**：EUR-LEX（欧洲立法）和 UNFAIR-ToS（不公平服务条款）。
*   **Benchmark（基准方法）**：
    *   多数类选择（Majority selection）。
    *   全正向选择（Always positive）。
    *   人工选择关键词（Manually chosen keywords）。
*   **场景模拟**：对比了“原始比例”和“现实比例”（模拟法律实务中更严重的类别不平衡）。

### 4. 资源与算力
*   **硬件环境**：实验在 **Intel Core Ultra 9 H-class CPU** 上运行。
*   **算力特点**：论文特别强调了该方法的**低功耗**和**非 GPU 依赖性**。它不使用昂贵的显卡进行大规模嵌入计算，而是通过 CPU 高效处理稀疏向量。
*   **训练细节**：神经网络最大迭代 500 次，采用自适应学习率和早停机制（Early Stopping）。

### 5. 实验数量与充分性
*   **实验规模**：使用了近 5 万份文档（超过 36 万页文本）进行训练、验证和测试。
*   **消融实验与参数敏感性分析**：
    *   **预处理影响**：证明了在法律领域，词干提取（Stemming）和词形还原（Lemmatization）反而会降低精度（下降约 2.6%-4.3%），因为法律术语的微小形式变化具有特定含义。
    *   **n-gram 范围**：测试了 2-gram 到 15-gram，确定 4-gram 为最优平衡点。
    *   **超参数调节**：对最小词频（MTF）、惩罚指数（PenEx）、文档频率权重（DocFreq）进行了详尽的网格搜索。
    *   **鲁棒性测试**：通过移除前 3 个最强信号词（如 "remanded"）来验证模型是否过度依赖个别词汇，结果显示模型依然能保持高精度。
*   **评价**：实验设计非常充分，涵盖了从特征工程到模型架构的各个维度，对比实验客观且符合实际应用场景。

### 6. 主要结论与发现
*   **高性能**：在 LexGLUE 上达到了 **99.3% 的准确率**和 **98.7% 的 F1 分数**，远超人工关键词筛选。
*   **局部频率胜过全局**：实验发现，文档内词频（Local Frequency）比跨文档的全局频率对分类贡献更大。
*   **法律语言的精确性**：法律文本分类不需要复杂的语义嵌入，判别性的词汇信号足以提供极强的分类边界。
*   **抗失衡性**：该方法对类别不平衡具有很强的鲁棒性，在正负样本比例大幅波动时性能稳定。

### 7. 优点
*   **高效轻量**：可以在普通 CPU 上快速运行，适合处理大规模法律存量数据。
*   **独立性强**：不依赖元数据、文件名或文档结构，具有极强的通用性。
*   **可解释性**：通过提取的关键词列表，律师可以直观地理解模型分类的依据（如哪些词导致文档被判定为“相关”）。
*   **精度极高**：在特定二分类任务上达到了接近完美的表现。

### 8. 不足与局限
*   **任务局限性**：目前仅限于**二分类**（相关 vs 不相关），尚未扩展到多分类或多标签任务（如具体法律领域的细分）。
*   **语义理解缺失**：由于基于词汇统计，可能无法处理某些极度依赖上下文语境、但不含明显关键词的复杂法律逻辑判断。
*   **领域依赖**：虽然在法律领域表现优异，但其关键词提取机制需要针对不同语料库重新运行（虽然是一次性的）。

（完）

---
title: Dual-View Training for Instruction-Following Information Retrieval
title_zh: 遵循指令的信息检索的双视图训练
authors: "Qingcheng Zeng, Puxuan Yu, Aman Mehta, Fuheng Zhao, Rajhans Samdani"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18845v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 用于训练检索器的双视图数据合成策略
tldr: "针对指令遵循信息检索（IF-IR）中检索器难以区分语义相关性与指令约束的问题，本文提出一种基于极性反转的双视图数据合成策略。通过LLM生成互补指令，使同一文档对在不同指令下互换相关性标签，强制模型关注指令而非仅依赖主题线索。实验显示该方法在FollowIR基准上提升了45%，显著增强了检索器的指令敏感性。"
selection_source: fresh_fetch
motivation: 现有的检索器主要针对语义相关性训练，往往无法区分符合主题但违反特定指令约束的文档。
method: 提出双视图数据合成策略，利用LLM为同一组文档生成互补指令，通过反转相关性标签来强化指令监督信号。
result: "在305M参数的编码器上，该方法使FollowIR基准性能提升了45%，超越了同等甚至更大规模的通用嵌入模型。"
conclusion: 针对性的数据合成对于构建兼具通用检索能力和指令感知能力的检索系统至关重要。
---

## 摘要
遵循指令的信息检索 (IF-IR) 研究的是检索系统不仅要找到与查询相关的文档，还必须遵守明确的用户约束，如要求的属性、排除项或输出偏好。然而，大多数检索器主要针对语义相关性进行训练，往往无法区分符合主题的文档与满足指令要求的文档。我们提出了一种基于极性反转的双视图数据合成策略：给定一个查询、一个在指令下相关的文档以及一个匹配查询但违反指令的难负样本，我们提示大语言模型 (LLM) 生成一个互补指令，使得这两个文档的相关性标签发生互换。通过在反转相关性标签的互补指令下呈现同一文档对，训练信号迫使检索器通过指令重新审视相同的候选集，而不是依赖固定的主题线索。在一个 305M 参数的编码器上，我们的方法在 FollowIR 基准测试中将性能提升了 45%，超过了同等或更大规模的通用嵌入模型。通过在相同数据预算下的直接对比，我们进一步表明数据多样性和指令监督起到了互补作用：前者保持了通用的检索质量，而后者提高了指令敏感性。这些结果突显了针对性数据合成对于构建兼具广泛能力和指令感知能力的检索系统的价值。

## Abstract
Instruction-following information retrieval (IF-IR) studies retrieval systems that must not only find documents relevant to a query, but also obey explicit user constraints such as required attributes, exclusions, or output preferences. However, most retrievers are trained primarily for semantic relevance and often fail to distinguish documents that match the topic from those that satisfy the instruction. We propose a dual-view data synthesis strategy based on polarity reversal: given a query, a document that is relevant under the instruction, and a hard negative that matches the query but violates the instruction, we prompt an LLM to generate a complementary instruction under which the two documents swap relevance labels. By presenting the same document pair under complementary instructions that invert their relevance labels, the training signal forces the retriever to reconsider the same candidate set through the instruction, rather than relying on fixed topical cues. On a 305M-parameter encoder, our method improves performance on the FollowIR benchmark by 45%, surpassing general-purpose embedding models of comparable or larger scale. Through head-to-head comparisons at matched data budgets, we further show that data diversity and instruction supervision play complementary roles: the former preserves general retrieval quality, while the latter improves instruction sensitivity. These results highlight the value of targeted data synthesis for building retrieval systems that are both broadly capable and instruction-aware.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“双视图训练（Dual-View Training）”的策略，旨在提升信息检索（IR）系统遵循复杂用户指令的能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的密集检索器（Dense Retrievers）主要关注查询与文档之间的**语义相关性**，但在面对包含特定约束（如“排除某类信息”、“限定特定风格”或“字数要求”）的**指令遵循型检索（IF-IR）**时表现较差。现有模型往往忽略指令细节，仅依赖查询与文档的表面相似度。论文的核心动机是：如何让检索器真正理解指令约束，而不仅仅是匹配主题？

### 2. 方法论：核心思想与关键技术
论文提出了一种基于**极性反转（Polarity Reversal）**的数据合成策略：
*   **核心思想**：对于同一个查询和一对文档（一个是符合指令的正样本 $D^+$，一个是语义相关但违反指令的负样本 $D^-$），利用大语言模型（LLM）生成一个新的“互补指令” $I_{new}$。
*   **极性反转**：在 $I_{new}$ 下，$D^-$ 变为正样本，而原本的正样本 $D^+$ 变为负样本。
*   **双视图训练**：在对比学习（Contrastive Learning）中，模型必须在同一批次内处理这两个视图。这迫使编码器不能仅依赖查询编码，而必须根据指令的细微差别来调整文档的排序。
*   **算法流程**：
    1.  从现有指令数据中提取包含“指令负样本”的案例。
    2.  提示 LLM（如 Qwen3-80B）分析 $D^+$ 和 $D^-$ 的属性差异。
    3.  生成新指令，通过添加硬性约束（如地域、时间、格式等）使两者的相关性标签互换。
    4.  使用 InfoNCE 损失函数进行微调。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **FollowIR**：评估指令敏感性（p-MRR）和综合检索质量（Score）。
    *   **InfoSearch**：测试长度约束和关键词约束。
    *   **MAIR (IFEval & InstructIR)**：评估通用指令遵循能力。
*   **对比方法**：
    *   **Ins-orig / All-orig**：使用原始 Promptriever 数据集的指令数据或混合数据。
    *   **通用模型**：如 EmbeddingGemma-300M、Qwen3-Embedding-0.6B 等。
*   **骨干模型**：采用了 gte-multilingual-mlm-base (305M) 和 bge-m3-retromae 两种不同的编码器架构以验证通用性。

### 4. 资源与算力
*   **合成模型**：使用 Qwen3-Next-80B-A3B-Instruct 进行数据合成。
*   **训练框架**：使用 Snowflake 的 Arctic-Embed 框架。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到了预训练阶段使用了 14.1 亿个查询-文档对，微调阶段涉及约 48k 到 88k 个样本。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多组对比实验，包括“仅指令数据”对比和“混合数据”对比，且所有对比均在**相同数据预算（Data Budget）**下进行，确保了公平性。
*   **充分性**：通过在两个不同的骨干模型（GTE 和 BGE）上重复实验，证明了该方法的有效性是跨架构的。此外，还进行了人工评估（100 个样本），确认 99% 的合成指令是可用的。实验设计客观且逻辑严密。

### 6. 主要结论与发现
*   **性能飞跃**：在 305M 参数模型上，双视图训练使 FollowIR 的 p-MRR 提升了 **45%**，显著超过了更大规模的通用模型。
*   **多样性与监督的平衡**：实验发现，增加非指令数据（多样性）有助于维持通用检索质量，但会稀释指令信号；而双视图合成数据能在不增加数据量的情况下，同时提升指令敏感性和通用检索质量。
*   **指令敏感性是脆弱的**：如果训练中混合了过多的非指令数据，模型会倾向于忽略指令，甚至在某些基准上出现负值（即做出与指令相反的预测）。

### 7. 优点
*   **数据效率极高**：无需额外的人工标注，通过对现有数据的“废物利用”和逻辑反转，创造了极具挑战性的训练对。
*   **即插即用**：该方法不需要修改检索器的模型架构，仅通过数据增强即可实现显著提升。
*   **深入的机理分析**：论文清晰地解释了为什么简单的混合数据会导致指令遵循能力下降，并提供了解决方案。

### 8. 不足与局限
*   **语言局限性**：实验主要集中在英语基准测试上，尚未验证在多语言环境下的表现。
*   **合成假设**：该方法假设每个数据点都能找到合理的“反转指令”，对于某些定义极其狭窄的查询，生成的反转指令可能不够自然。
*   **架构限制**：目前仅在双编码器（Bi-encoder）架构上进行了验证，尚未探讨在重排序器（Cross-encoder）或基于解码器的模型上的适用性。

（完）

---
title: Controllable and Verifiable Tool-Use Data Synthesis for Agentic Reinforcement Learning
title_zh: 面向智能体强化学习的可控且可验证的工具使用数据合成
authors: "Siyuan Xu, Shiyang Li, Xin Liu, Tianyi Liu, Yixiao Li, Zhan Shi, Zixuan Zhang, Zilong Wang, Qingyu Yin, Jianshu Chen, Tuo Zhao, Bing Yin"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09813v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于工具使用数据自演化合成的两阶段流水线
tldr: 针对强化学习在工具使用场景中缺乏可执行环境和自动奖励机制的问题，本文提出 COVERT 框架。该框架通过两阶段流水线生成高质量基础轨迹，并利用保持 Oracle 的增强技术引入环境复杂性（如干扰工具、模糊查询等），同时确保奖励可自动验证。实验表明，COVERT-RL 显著提升了模型在 BFCL 和 ACEBench 上的表现，证明了其作为 SFT 补充阶段在提升工具调用鲁棒性方面的有效性。
selection_source: fresh_fetch
motivation: 现有的工具使用数据集多用于离线微调，缺乏支持强化学习在线训练所需的可执行环境和自动奖励校验机制。
method: 提出 COVERT 框架，通过多级验证生成基础轨迹，并采用保持 Oracle 的增强手段增加环境复杂度，实现自动化的奖励计算。
result: 在 Qwen2.5-Instruct-14B 上，COVERT-RL 显著提升了 BFCL v3 和 ACEBench 的准确率，且与 SFT 结合后效果更佳。
conclusion: 保持 Oracle 的合成环境为提升工具调用在模糊和不可靠反馈下的鲁棒性提供了一种实用且有效的强化学习优化方案。
---

## 摘要
现有的合成工具使用语料库主要针对离线监督微调设计，然而强化学习（RL）需要支持奖励可检查在线展开的可执行环境。我们提出了 COVERT，这是一个两阶段流水线：首先通过带有多级验证的自进化合成生成可靠的基础工具使用轨迹，然后应用保持真值（oracle-preserving）的增强手段，系统性地增加环境复杂度。这些增强引入了干扰工具、间接或模糊的用户查询，以及噪声、多格式或错误的工具输出，同时严格保留真值工具调用和最终答案作为地面真值。这种设计使得标准案例可以通过参考匹配进行自动奖励计算，而对于错误检测等特殊行为，则可以通过轻量级的评判员辅助验证，从而支持工具调用策略的强化学习优化。在 Qwen2.5-Instruct-14B 上，COVERT-RL 将 BFCL v3 的整体准确率从 56.5 提升至 59.9，将 ACEBench 的准确率从 53.0 提升至 59.3，且在通用能力基准测试上的退化极小；当在 SFT 之上叠加时，准确率进一步达到 62.1 和 61.8，证实了叠加增益。这些结果表明，保持真值的合成环境提供了一个实用的强化学习精调阶段，与监督微调互补，能够提高在模糊性和不可靠工具反馈下的工具使用鲁棒性。

## Abstract
Existing synthetic tool-use corpora are primarily designed for offline supervised fine-tuning, yet reinforcement learning (RL) requires executable environments that support reward-checkable online rollouts. We propose COVERT, a two-stage pipeline that first generates reliable base tool-use trajectories through self-evolving synthesis with multi-level validation, and then applies oracle-preserving augmentations that systematically increase environmental complexity. These augmentations introduce distractor tools, indirect or ambiguous user queries, and noisy, multi-format, or erroneous tool outputs, while strictly preserving oracle tool calls and final answers as ground truth. This design enables automatic reward computation via reference matching for standard cases and lightweight judge-assisted verification for special behaviors such as error detection, supporting RL optimization of tool-calling policies. On Qwen2.5-Instruct-14B, COVERT-RL improves overall accuracy on BFCL v3 from 56.5 to 59.9 and on ACEBench from 53.0 to 59.3, with minimal regressions on general-ability benchmarks; when stacked on SFT, it further reaches 62.1 and 61.8, confirming additive gains. These results suggest that oracle-preserving synthetic environments offer a practical RL refinement stage, complementary to SFT, for improving tool-use robustness under ambiguity and unreliable tool feedback.

---

## 论文详细总结（自动生成）

以下是对论文《Controllable and Verifiable Tool-Use Data Synthesis for Agentic Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
大型语言模型（LLM）在工具使用（Tool-use）方面虽有进步，但在实际部署中面临**鲁棒性不足**的挑战，如难以应对模糊查询、干扰工具、噪声输出或工具调用失败等复杂情况。
*   **现有瓶颈**：目前的合成数据集主要为监督微调（SFT）设计，是静态的轨迹，缺乏可执行的环境。
*   **RL 的困境**：强化学习（RL）需要在线展开（Rollout）和可验证的奖励信号，但构建覆盖各种边缘情况且具备自动奖励机制的工具使用环境非常困难。
*   **研究目标**：构建一个可控、可验证的合成流水线，为工具使用的强化学习提供模拟环境，提升模型在复杂现实场景下的鲁棒性。

### 2. 方法论：COVERT 框架
COVERT 采用两阶段流水线来构建 RL 就绪的合成环境：

*   **阶段 I：可靠基础轨迹生成**
    *   **自进化合成**：从少量种子数据开始，利用 LLM 迭代生成新轨迹并扩充种子池。
    *   **多级验证**：通过**格式检查器**（JSON 结构）、**规则检查器**（参数约束、枚举值）和**语义检查器**（LLM 评判工具选择与逻辑一致性）确保基础数据的绝对可靠。
*   **阶段 II：保持真值（Oracle-preserving）的增强**
    *   **核心思想**：在修改环境组件（系统提示词、查询、工具集、输出）的同时，严格保留原始的“真值工具调用（c*）”和“最终答案（a*）”。
    *   **增强类别**：
        1.  **精确可验证增强**：引入干扰工具、重写模糊查询、改变输出格式或注入噪声。奖励通过与保留的真值进行确定性匹配计算。
        2.  **评判员辅助增强**：模拟工具报错或无效查询。此时最终答案会改变，需借助轻量级 LLM 评判员根据预设的“能力标签”进行验证。
*   **强化学习优化**：采用 **GRPO** 算法，将奖励分解为格式奖励和能力条件下的正确性奖励，实现策略优化。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **工具使用能力**：Berkeley Function Calling Leaderboard (BFCL) v3 和 ACEBench。
    *   **通用能力**：LiveCodeBench（代码）、AIME/Math500（数学）、MMLU-Pro（知识）、IFEval（指令遵循）等，用于检测是否存在能力退化。
*   **对比方法**：
    *   基座模型：Qwen2.5-Instruct (7B/14B)。
    *   SFT 基准：TOUCAN-SFT（在 150 万条真实协议数据上微调）。
    *   本文方法：COVERT-RL（直接在基座上 RL）以及 TOUCAN-SFT + COVERT-RL（SFT 后接 RL）。

### 4. 资源与算力
*   **硬件**：使用 8 × NVIDIA H200 GPU。
*   **时长**：7B 模型单次 RL 训练约 **28 小时**；14B 模型训练时长约为 7B 的 **1.6 倍**（约 45 小时）。
*   **评判员模型**：使用轻量级的 Qwen2.5-Instruct-7B 作为奖励函数中的 LLM Judge。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7B 和 14B 两个主流参数量级。
*   **维度覆盖**：不仅测试了整体准确率，还深入分析了“干扰项检测”、“错误检测”、“多轮对话”等细分维度。
*   **客观性**：所有模型（包括基准模型）均在统一的“最小化函数调用提示词”下评估，排除了提示词工程带来的偏差。
*   **充分性**：通过对比 SFT 和 RL 的独立效果及叠加效果，验证了 RL 作为补充阶段的必要性。

### 6. 主要结论与发现
*   **性能显著提升**：在 Qwen2.5-14B 上，COVERT-RL 将 BFCL v3 准确率从 56.5 提升至 59.9，ACEBench 从 53.0 提升至 59.3。
*   **SFT 与 RL 互补**：在 SFT 模型基础上叠加 RL，BFCL 进一步升至 62.1。SFT 提供结构化先验，而 RL 显著增强了语义正确性和对异常情况的鲁棒性。
*   **通用能力保持**：与 SFT 相比，RL 训练对模型通用推理和数学能力的损害更小，几乎没有发生灾难性遗忘。
*   **针对性增强有效**：提升最明显的是“无关查询检测”和“特殊错误处理”类别，证明了增强策略的有效性。

### 7. 优点
*   **创新性地解决了奖励获取问题**：通过“保持真值”的增强设计，绕过了工具使用中难以自动评分的难题。
*   **细粒度可控**：能够针对特定的弱点（如噪声干扰）进行定向强化训练。
*   **实用性强**：不依赖昂贵的真实 API 调用，完全在合成的模拟环境中完成策略精调。

### 8. 不足与局限
*   **长程规划限制**：目前的模拟环境主要针对单步或短程交互，对于需要复杂状态维护的长程智能体规划（Long-horizon planning）提升有限。
*   **评判员风险**：部分增强场景仍依赖 LLM 评判员，虽然有真值辅助，但仍存在潜在的偏见或不一致性，且文中未做大规模人类一致性研究。
*   **环境真实度**：模拟环境返回的错误信息较为通用，可能无法完全覆盖真实 API 极其复杂的报错逻辑。

（完）

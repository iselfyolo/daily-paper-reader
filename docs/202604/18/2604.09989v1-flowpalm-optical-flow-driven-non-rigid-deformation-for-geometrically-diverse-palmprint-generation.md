---
title: "FlowPalm: Optical Flow Driven Non-Rigid Deformation for Geometrically Diverse Palmprint Generation"
title_zh: FlowPalm：用于几何多样化掌纹生成的光流驱动非刚性形变
authors: "Yuchen Zou, Huikai Shao, Lihuang Fang, Zhipeng Xiong, Dexing Zhong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09989v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于训练识别模型的合成数据生成
tldr: 针对合成掌纹在几何多样性方面的不足，本文提出FlowPalm框架。该方法利用光流估计捕捉真实掌纹间的非刚性变形模式，并将其作为先验引入扩散模型的渐进式采样过程中。在保持身份一致性的同时，FlowPalm能生成具有复杂几何变化的掌纹图像，有效解决了现有方法仅关注风格转换而忽视几何形变的问题。实验证明，该方法在六个基准数据集上的识别性能显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有掌纹生成方法主要关注风格转换，缺乏对真实掌纹中复杂非刚性几何变形的有效模拟。
method: 利用真实掌纹对的光流估计捕捉变形先验，并结合扩散模型通过渐进式采样在生成过程中引入几何形变。
result: 在六个主流掌纹识别基准数据集上的实验表明，FlowPalm生成的样本在下游识别任务中显著优于现有最先进方法。
conclusion: FlowPalm通过光流驱动的变形机制显著增强了合成掌纹的几何多样性，为提升掌纹识别模型的泛化能力提供了高质量数据支持。
---

## 摘要
近年来，合成掌纹越来越多地被用作真实数据的替代品来训练识别模型。为了确保有效性，此类合成数据必须反映真实掌纹的多样性，包括风格变化和几何变化。然而，现有的掌纹生成方法主要侧重于风格迁移，而几何变化要么被忽略，要么仅通过简单的手工增强来近似。在这项工作中，我们提出了 FlowPalm，这是一个光流驱动的掌纹生成框架，能够模拟在真实手掌中观察到的复杂非刚性形变。具体而言，FlowPalm 通过估计真实掌纹对之间的光流来捕捉几何形变的统计模式。基于这些先验，我们设计了一个渐进式采样过程，在扩散过程中逐渐引入几何形变，同时保持身份一致性。在六个基准数据集上的广泛实验表明，FlowPalm 在下游识别任务中显著优于现有的最先进掌纹生成方法。项目页面：https://yuchenzou.github.io/FlowPalm/

## Abstract
Recently, synthetic palmprints have been increasingly used as substitutes for real data to train recognition models. To be effective, such synthetic data must reflect the diversity of real palmprints, including both style variation and geometric variation. However, existing palmprint generation methods mainly focus on style translation, while geometric variation is either ignored or approximated by simple handcrafted augmentations. In this work, we propose FlowPalm, an optical-flow-driven palmprint generation framework capable of simulating the complex non-rigid deformations observed in real palms. Specifically, FlowPalm estimates optical flows between real palmprint pairs to capture the statistical patterns of geometric deformations. Building on these priors, we design a progressive sampling process that gradually introduces the geometric deformations during diffusion while maintaining identity consistency. Extensive experiments on six benchmark datasets demonstrate that FlowPalm significantly outperforms state-of-the-art palmprint generation approaches in downstream recognition tasks. Project page: https://yuchenzou.github.io/FlowPalm/

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FlowPalm** 的新型掌纹生成框架，旨在通过模拟真实的非刚性几何形变来提升合成掌纹的多样性和识别模型的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的合成掌纹生成方法（如基于 GAN 或扩散模型的方法）主要关注**风格和纹理**的迁移，而忽视了真实掌纹中由于手部弯曲、手指运动和相机视角变化引起的**复杂非刚性几何形变**。
*   **研究背景**：高质量掌纹识别模型的训练依赖大规模多样化数据，但隐私保护限制了真实数据的获取。现有的合成方法通常使用简单的手工增强（如仿射变换或线条振荡）来模拟几何变化，这无法还原真实的物理形变，导致合成数据与真实数据之间存在分布鸿沟。

### 2. 方法论
FlowPalm 的核心思想是**利用真实世界的光流先验来驱动扩散模型的生成过程**。
*   **变形先验构建 (Deformation Prior Construction)**：
    *   使用预训练的光流模型 (RAFT) 估计同一身份的真实掌纹对之间的稠密对应关系，捕捉非刚性变形场。
    *   **质量过滤**：通过“平滑度校验”（计算流场梯度）和“身份一致性校验”（利用识别模型对比变形前后的余弦相似度）筛选出高质量的变形场，构建**变形库 (Deformation Library)**。
*   **三阶段渐进式生成 (Three-stage Generation)**：
    *   **阶段 I：变形主线生成**。从变形库采样变形场，对初始褶皱图（Crease Map）进行双线性插值变形，引导扩散模型生成具有几何形变的清晰主线结构。
    *   **阶段 II：变形纹理生成**。利用扩散模型的“噪声-内容等变性”，将变形场应用于高斯噪声（R-Noise 策略），注入变形后的噪声以生成与主线几何一致的细粒度纹理。
    *   **阶段 III：无条件纹理细化**。在采样的最后阶段移除褶皱图约束，进行无条件去噪，以修正手工褶皱带来的不自然痕迹，增强图像真实感。

### 3. 实验设计
*   **数据集**：在 6 个主流掌纹基准数据集上进行了验证：XJTU-UP, MPD, TCD, CASIA, IITD, 和 PolyU。
*   **Benchmark 与对比方法**：
    *   对比了多种最先进 (SOTA) 的生成方法：RPG-Palm (ICCV'23), PCE-Palm (AAAI'24), Diff-Palm (CVPR'25), PFIG-Palm (TIP'25) 以及数据增强方法 UAA (ICCV'25)。
    *   **评估指标**：主要使用下游识别任务的 TAR@FAR=10⁻⁶（真阳性率），以及衡量生成质量的 FID、类间/类内距离。

### 4. 资源与算力
*   **硬件环境**：训练和采样过程使用了 **8 台 NVIDIA A100 GPU (40GB)**。
*   **训练细节**：扩散模型在 256x256 分辨率下训练了 100k 个 step，使用 Adam 优化器，采样步数 T=250。下游识别模型（MobileFaceNet）的训练则在单台 A100 上完成。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个不同的数据集上进行了跨库和内库测试，涵盖了从“仅合成数据训练”到“合成+真实混合训练”等多种范式。
*   **消融实验**：详细分析了变形场筛选、褶皱变形、噪声变形以及无条件细化阶段起始时间 ($\tau_u$) 对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过对比有无数据增强（RandAugment）的情况，证明了 FlowPalm 提供的几何多样性是手工增强无法替代的。

### 6. 主要结论与发现
*   **超越真实数据**：仅使用 FlowPalm 生成的合成数据训练的模型，在多个数据集上的识别表现**超过了仅使用真实数据训练的模型**（例如在 XJTU 数据集上，合成训练达到 86.33%，而真实训练为 78.05%）。
*   **几何多样性的重要性**：实验证明，同时对主线条件和注入噪声进行变形，对于保持身份一致性和生成逼真纹理至关重要。
*   **SOTA 性能**：FlowPalm 在所有测试场景下均达到了当前最先进的验证性能。

### 7. 优点
*   **物理意义明确**：相比于随机的数学变换，利用光流捕捉真实形变使合成图像更符合手部的解剖学特征。
*   **渐进式解耦**：将结构变形、纹理对齐和真实感细化分阶段处理，有效解决了身份保持与几何多样性之间的矛盾。
*   **即插即用**：生成的变形场库可以作为通用先验，扩展到其他基于扩散模型的生物特征生成任务中。

### 8. 不足与局限
*   **依赖初始真实数据**：构建变形库仍需要一定规模的含有同一身份多张照片的真实数据集，对于完全没有原始数据的领域可能存在冷启动问题。
*   **计算开销**：虽然采样时噪声变形只需执行一次，但扩散模型本身的多步迭代性质使得大规模生成样本的时间成本仍然高于基于 GAN 的方法。
*   **极端形变风险**：尽管有过滤机制，但如果变形场过于剧烈，仍可能导致主线交叉等不符合生理结构的伪影。

（完）

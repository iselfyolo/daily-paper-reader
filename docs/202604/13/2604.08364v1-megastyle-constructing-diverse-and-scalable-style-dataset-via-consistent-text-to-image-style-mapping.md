---
title: "MegaStyle: Constructing Diverse and Scalable Style Dataset via Consistent Text-to-Image Style Mapping"
title_zh: MegaStyle：通过一致的文本到图像风格映射构建多样化且可扩展的风格数据集
authors: "Junyao Gao, Sibo Liu, Jiaxing Li, Yanan Sun, Yuanpeng Tu, Fei Shen, Weidong Zhang, Cairong Zhao, Jun Zhang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08364v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 利用大型生成模型构建可扩展的风格数据集合成流水线
tldr: 本研究提出MegaStyle，一种利用大规模生成模型构建高质量风格数据集的流水线。通过170K风格提示词与400K内容提示词的组合，生成了包含140万张图像的MegaStyle-1.4M数据集，解决了现有数据在风格一致性与多样性上的不足。基于该数据集训练的MegaStyle-Encoder和MegaStyle-FLUX模型，在风格表示提取和通用风格迁移任务中表现优异，为该领域提供了重要的数据与模型支持。
selection_source: fresh_fetch
motivation: 针对现有风格数据集在风格一致性、多样性和图像质量方面的局限性，旨在构建一个大规模且高质量的风格基准。
method: 利用预训练生成模型的文本到图像映射能力，通过海量风格与内容提示词的交叉组合自动化生成大规模风格一致的数据集。
result: 成功构建了MegaStyle-1.4M数据集，并训练出具备高可靠风格度量能力的编码器和强泛化性的风格迁移模型。
conclusion: 研究证明了维护风格内一致性与风格间多样性的重要性，MegaStyle显著提升了风格迁移任务的性能与评估标准。
---

## 摘要
在本文中，我们介绍了 MegaStyle，这是一种新颖且可扩展的数据策规划流程，用于构建具有类内一致性、类间多样性且高质量的风格数据集。我们通过利用当前大型生成模型的一致性文本到图像风格映射能力来实现这一目标，这些模型可以根据给定的风格描述生成具有相同风格的图像。在此基础上，我们策划了一个包含 17 万个风格提示词和 40 万个内容提示词的多样化且均衡的提示词库，并通过内容-风格提示词组合生成了大规模风格数据集 MegaStyle-1.4M。利用 MegaStyle-1.4M，我们提出了风格监督对比学习来微调风格编码器 MegaStyle-Encoder，以提取具有表现力的风格特定表示，并训练了一个基于 FLUX 的风格迁移模型 MegaStyle-FLUX。广泛的实验证明了在风格数据集中保持类内一致性、类间多样性和高质量的重要性，以及所提出的 MegaStyle-1.4M 的有效性。此外，在 MegaStyle-1.4M 上训练后，MegaStyle-Encoder 和 MegaStyle-FLUX 提供了可靠的风格相似度度量和泛化性强的风格迁移，为风格迁移社区做出了重大贡献。更多结果可在我们的项目网站 https://jeoyal.github.io/MegaStyle/ 查看。

## Abstract
In this paper, we introduce MegaStyle, a novel and scalable data curation pipeline that constructs an intra-style consistent, inter-style diverse and high-quality style dataset. We achieve this by leveraging the consistent text-to-image style mapping capability of current large generative models, which can generate images in the same style from a given style description. Building on this foundation, we curate a diverse and balanced prompt gallery with 170K style prompts and 400K content prompts, and generate a large-scale style dataset MegaStyle-1.4M via content-style prompt combinations. With MegaStyle-1.4M, we propose style-supervised contrastive learning to fine-tune a style encoder MegaStyle-Encoder for extracting expressive, style-specific representations, and we also train a FLUX-based style transfer model MegaStyle-FLUX. Extensive experiments demonstrate the importance of maintaining intra-style consistency, inter-style diversity and high-quality for style dataset, as well as the effectiveness of the proposed MegaStyle-1.4M. Moreover, when trained on MegaStyle-1.4M, MegaStyle-Encoder and MegaStyle-FLUX provide reliable style similarity measurement and generalizable style transfer, making a significant contribution to the style transfer community. More results are available at our project website https://jeoyal.github.io/MegaStyle/.

---

## 论文详细总结（自动生成）

这是一份关于论文《MegaStyle: Constructing Diverse and Scalable Style Dataset via Consistent Text-to-Image Style Mapping》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：图像风格迁移（Style Transfer）长期受限于**高质量配对数据**的匮乏。现有数据集（如 WikiArt 或通过旧式风格迁移生成的合成数据）存在三大痛点：
    1.  **类内不一致**：同一艺术家的作品或同一风格标签下的图像在色彩、笔触上差异巨大。
    2.  **类间多样性不足**：风格覆盖面窄，难以涵盖现代数字艺术或特定细分风格。
    3.  **质量缺陷**：合成数据常伴有色彩溢出、光晕或轮廓破碎等伪影。
*   **研究动机**：利用当前大型生成模型（如 Qwen-Image）强大的“文本到图像（T2I）一致性映射能力”，即相同的风格描述词在不同内容下能保持高度稳定的视觉特征，从而自动化构建大规模、高一致性的风格数据集。

### 2. 方法论：核心思想与关键技术
*   **MegaStyle 数据集策规划流水线**：
    1.  **图像池收集**：从 JourneyDB、WikiArt 和 LAION 收集 2M 风格图和 2M 内容图。
    2.  **提示词策划与平衡**：使用 **Qwen3-VL** 对图像进行解耦标注。风格提示词涵盖艺术媒介、光影、纹理、笔触等维度；内容提示词仅描述物体关系。通过**分层 K-Means 聚类**进行去重和平衡采样，最终获得 170K 风格提示词和 400K 内容提示词。
    3.  **风格图像生成**：将风格与内容提示词交叉组合，利用 Qwen-Image 生成 1.4M 张具有高度类内一致性的图像。
*   **MegaStyle-Encoder（风格编码器）**：
    *   采用**风格监督对比学习（SSCL）**。
    *   **公式思想**：结合了监督对比损失（$L_{scl}$，拉近同风格图像，推开异风格图像）和图文对比损失（$L_{itc}$，增强风格表示的判别力）。
*   **MegaStyle-FLUX（风格迁移模型）**：
    *   基于 **FLUX.1-dev** 架构，使用 MegaStyle-1.4M 进行配对监督训练。
    *   引入 **Shifted RoPE（旋转位置编码）**：防止参考图与目标图之间的位置冲突，缓解内容泄露。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：MegaStyle-1.4M。
    *   **评估基准**：自建的 **StyleRetrieval**（2400 种细分风格）、**StyleBench**（50 张真实艺术品）、**FLUX-Retrieval** 及 **OmniStyle-150K**。
*   **对比方法**：
    *   **编码器对比**：CLIP、SigLIP、CSD。
    *   **风格迁移对比**：DEADiff、StyleShot、StyleAligned、InstantStyle、CSGO、StyleCrafter、Attention-Distillation。
*   **评估指标**：mAP@k、Recall@k（检索）；CLIP Text Score（内容一致性）；MegaStyle-Encoder Score（风格相似度）；用户偏好研究（Human Preference）。

### 4. 资源与算力
*   **训练细节**：
    *   **MegaStyle-Encoder**：基于 SigLIP-so400m，训练 30 个 epoch，**Batch Size 高达 8,192**（暗示使用了大规模 GPU 集群），学习率 5e-4。
    *   **MegaStyle-FLUX**：基于 FLUX.1-dev，训练 30,000 步，Batch Size 为 8，LoRA Rank 为 128，分辨率 512x512。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号（如 A100 或 H100）及总耗时，但从 Batch Size 和模型规模推断，其数据生成和编码器训练需要极高的算力支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的风格检索实验（跨 4 个不同来源的 Benchmark）。
    *   针对风格迁移进行了定性、定量及用户主观评价对比。
    *   **消融实验**：验证了不同训练数据集（JourneyDB vs OmniStyle vs MegaStyle）对模型性能的影响，以及不同编码器架构的效果。
*   **充分性与公平性**：实验设计较为客观。作者不仅在自建数据集上测试，还在第三方真实艺术品数据集（StyleBench）上验证了泛化性。通过微调基准模型（如 StyleShot-FLUX）来确保对比的公平性。

### 6. 主要结论与发现
1.  **数据质量决定上限**：在 MegaStyle-1.4M 上训练的模型在风格捕捉的细腻度（笔触、纹理）上远超在不一致数据（如 JourneyDB）上训练的模型。
2.  **编码器解耦成功**：MegaStyle-Encoder 能够有效摆脱语义内容的干扰，专注于提取纯粹的风格特征，检索准确率大幅领先 CSD 和 CLIP。
3.  **FLUX 架构优势**：结合配对监督训练，MegaStyle-FLUX 在保持文本内容一致性的同时，实现了极高的风格还原度，解决了传统方法常见的“内容泄露”问题。

### 7. 优点（亮点）
*   **可扩展性**：提出了一套自动化的流水线，理论上可以无限扩展风格空间。
*   **高质量度量工具**：MegaStyle-Encoder 为风格迁移领域提供了一个比 CLIP 更可靠的评估指标。
*   **平衡性**：通过分层聚类解决了长尾风格分布问题，使模型对各种艺术风格都有均衡的生成能力。

### 8. 不足与局限
*   **描述词局限**：VLM 在描述极少数罕见风格或极其细腻的纹理时，仍可能使用模糊词汇。
*   **关联偏差（Association Bias）**：生成模型（Qwen-Image）存在固有偏见，例如输入“日本画”风格时，模型倾向于自动生成穿着和服的人物或江户时代背景，即使提示词中未要求。
*   **算力门槛**：复现该数据集的生成过程及超大 Batch Size 的对比学习训练对普通研究者而言成本极高。

（完）

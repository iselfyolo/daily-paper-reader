---
title: Reinforcement-Guided Synthetic Data Generation for Privacy-Sensitive Identity Recognition
title_zh: 针对隐私敏感身份识别的强化引导合成数据生成
authors: "Xuemei Jia, Jiawei Du, Hui Wei, Jun Chen, Joey Tianyi Zhou, Zheng Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07884v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 强化学习引导的合成数据生成框架
tldr: 针对隐私敏感场景下数据获取受限导致的生成模型与识别任务性能瓶颈，本文提出一种强化学习引导的合成数据生成框架。该框架通过冷启动适配对齐预训练生成器，并利用多目标奖励函数优化样本的语义一致性、多样性与丰富度。结合动态样本选择机制，该方法在提升生成质量的同时显著增强了下游识别任务的准确率与泛化能力。
selection_source: fresh_fetch
motivation: 隐私监管和版权限制导致敏感身份识别领域数据匮乏，形成了“数据少导致生成差、生成差无法补足数据”的恶性循环。
method: 提出一种结合冷启动适配、多目标奖励优化及动态样本选择的强化学习引导生成框架，将通用生成先验适配至特定领域。
result: 实验证明该框架在基准数据集上显著提升了生成保真度和分类准确率，且在小样本及新类别场景下表现出极强的泛化性。
conclusion: 通过强化学习引导通用生成模型进行领域适配，是解决隐私敏感任务中数据稀缺并提升识别性能的有效途径。
---

## 摘要
高保真生成模型在隐私敏感场景中的需求日益增长，在这些场景中，由于监管和版权约束，数据访问受到严格限制。这种稀缺性阻碍了模型的开发——讽刺的是，在这些最需要生成模型来弥补数据匮乏的设置中，情况尤为严重。这产生了一个自我强化的挑战：有限的数据导致生成模型质量低下，进而无法缓解数据稀缺问题。为了打破这一循环，我们提出了一种强化引导的合成数据生成框架，将通用领域的生成先验适配到隐私敏感的身份识别任务中。我们首先进行冷启动适配，使预训练生成器与目标领域对齐，建立语义相关性和初始保真度。在此基础上，我们引入了一种多目标奖励，共同优化语义一致性、覆盖多样性和表达丰富度，引导生成器产生既真实又对任务有效的样本。在下游训练期间，动态样本选择机制进一步优先考虑高实用性的合成样本，从而实现自适应数据缩放并改善领域对齐。在基准数据集上的广泛实验表明，我们的框架显著提高了生成保真度和分类准确率，同时在小数据量情形下对新类别表现出强大的泛化能力。

## Abstract
High-fidelity generative models are increasingly needed in privacy-sensitive scenarios, where access to data is severely restricted due to regulatory and copyright constraints. This scarcity hampers model development--ironically, in settings where generative models are most needed to compensate for the lack of data. This creates a self-reinforcing challenge: limited data leads to poor generative models, which in turn fail to mitigate data scarcity. To break this cycle, we propose a reinforcement-guided synthetic data generation framework that adapts general-domain generative priors to privacy-sensitive identity recognition tasks. We first perform a cold-start adaptation to align a pretrained generator with the target domain, establishing semantic relevance and initial fidelity. Building on this foundation, we introduce a multi-objective reward that jointly optimizes semantic consistency, coverage diversity, and expression richness, guiding the generator to produce both realistic and task-effective samples. During downstream training, a dynamic sample selection mechanism further prioritizes high-utility synthetic samples, enabling adaptive data scaling and improved domain alignment. Extensive experiments on benchmark datasets demonstrate that our framework significantly improves both generation fidelity and classification accuracy, while also exhibiting strong generalization to novel categories in small-data regimes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reinforcement-Guided Synthetic Data Generation for Privacy-Sensitive Identity Recognition》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在隐私敏感的身份识别任务（如人脸识别、行人重识别）中，由于法律监管和版权限制，获取大规模真实标注数据极其困难。
*   **研究动机**：现有的合成数据方法陷入了“恶性循环”：真实数据稀缺导致生成的合成数据质量差、多样性不足，进而无法有效提升下游识别任务的性能。
*   **整体含义**：本文旨在打破这一循环，提出通过强化学习（RL）引导通用领域的生成先验（Pre-trained Priors）向特定隐私敏感领域进行适配，生成既保真又具备高任务实用性的合成数据。

### 2. 方法论
该框架包含三个连续阶段：
*   **冷启动初始化（Cold-Start Initialization）**：
    *   使用在 ImageNet 上预训练的 **Diffusion Transformer (DiT-XL/2)** 作为基础。
    *   通过在少量目标领域数据上进行轻量级微调，将生成器的类别嵌入（Class Embedding）与目标标签空间对齐，建立初步的语义相关性。
*   **奖励驱动的强化学习优化（Reward-driven Optimization）**：
    *   将扩散模型的去噪过程建模为多步马尔可夫决策过程，利用 **DPOK 算法**进行策略梯度更新。
    *   **多目标奖励函数**包含三个组件：
        1.  **语义一致性 ($R_{sem}$)**：通过余弦相似度确保生成图像与目标身份的特征原型一致。
        2.  **分布覆盖度 ($R_{cov}$)**：利用 RBF 核函数衡量生成特征与参考特征的分布对齐，并惩罚样本冗余。
        3.  **表达丰富度 ($R_{exp}$)**：通过协方差矩阵的迹（Trace）鼓励生成特征在受控范围内扩张，增加类内多样性。
*   **动态样本选择策略（Dynamic Sample Selection, DSS）**：
    *   在下游训练时，通过“前瞻性”虚拟梯度更新，估计每个合成样本对降低身份损失的贡献，动态筛选高实用性的样本参与模型更新。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **行人重识别 (Re-ID)**：使用 Market-1501 和 CUHK03-NP 数据集。
    *   **人脸识别 (Face Recognition)**：使用 CASIA-WebFace 的子集进行训练；在 LFW、AgeDB、CFP-FP 等 5 个基准上进行验证。
    *   **人口统计偏差评估**：使用 RFW 数据集测试模型在不同种族上的公平性。
*   **对比方法**：
    *   **真实数据增强**：Random Erasing, CIDAM, CaAug。
    *   **模拟/虚拟环境**：FineGPR, InfinitePerson, VIPerson。
    *   **合成增强**：DG-Net, GIF-SD, IDiff, NegFaceDiff 等 SOTA 方法。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA H200 GPU** 进行训练。
*   **训练细节**：文中提到了具体的超参数设置（如学习率 1e-5，DiT-XL/2 模型），但未明确给出总训练时长或使用的 GPU 具体数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个不同的身份识别任务（Re-ID 和人脸识别），验证了方法的通用性。
    *   进行了详尽的**消融实验**，逐一验证了 DSS、语义奖励、覆盖奖励和丰富度奖励的增量贡献。
    *   针对不同的下游骨干网络（ResNet-50, ViT-16）进行了兼容性测试。
*   **客观性与公平性**：通过在相同的小规模数据集（如 CASIA 子集）上重新实现对比方法，确保了评估的基准一致。引入种族偏差评估（RFW）增加了实验的深度。

### 6. 主要结论与发现
*   **性能提升**：在 Market-1501 上达到了 88.6% mAP，显著优于现有的合成增强方法。在人脸识别任务中，平均准确率（79.07%）超过了真实数据基准和所有对比的生成模型。
*   **打破循环**：证明了利用通用先验+强化学习引导，可以在极少量真实数据引导下生成高质量、高多样性的样本。
*   **偏差缓解**：合成数据的多样性有助于减少模型在不同种族间的识别偏差。

### 7. 优点
*   **范式转变**：从单纯的“分布匹配”转向“任务实用性驱动”的生成，解决了小样本下生成模型过拟合的问题。
*   **奖励设计精巧**：三位一体的奖励函数平衡了身份保持（保真度）与类内差异（多样性）。
*   **即插即用**：动态样本选择机制可以轻松集成到现有的下游训练流程中。

### 8. 不足与局限
*   **先验依赖**：方法的有效性在很大程度上取决于预训练模型（如 DiT）的质量，如果目标领域与预训练领域差异过大，适配难度会增加。
*   **调参成本**：多目标奖励函数中的超参数（$\lambda_{sem}, \lambda_{cov}, \lambda_{exp}$）可能需要针对不同任务进行精细调整。
*   **模态限制**：目前仅验证了图像任务，尚未扩展到视频或跨模态（如文本转图像）的隐私敏感任务。

（完）

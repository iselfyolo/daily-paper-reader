---
title: "WRAP++: Web discoveRy Amplified Pretraining"
title_zh: WRAP++：基于网络发现增强的预训练
authors: "Jiang Zhou, Yunhao Wang, Xing Wu, Tinghao Yu, Feng Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06829v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 为预训练合成跨文档的问答对数据
tldr: 针对大语言模型预训练中合成数据重写仅局限于单文档、缺乏跨文档关联的问题，本文提出 WRAP++。该方法通过挖掘网页超链接发现跨文档关系（如双向链接、共同提及），并合成需要跨文档推理的问答对。在维基百科上的实验显示，WRAP++ 将 8.4B 原始文本扩充至 80B 跨文档问答数据，显著提升了 OLMo 模型在 SimpleQA 等任务上的知识获取能力和扩展性。
selection_source: fresh_fetch
motivation: 现有的合成数据重写方法仅针对单文档，无法捕捉跨文档的关联知识，限制了模型对事实知识的关联理解。
method: 通过挖掘网页超链接发现双向链接和共同提及等关系模式，并据此合成跨文档的联合问答数据。
result: 在维基百科上将数据规模从 8.4B 扩展至 80B，使 7B 和 32B 规模的 OLMo 模型在 SimpleQA 任务上显著优于单文档重写方法。
conclusion: 跨文档知识的发现与合成能有效增强模型的事实关联能力，并提供比单文档重写更强的预训练扩展潜力。
---

## 摘要
合成数据改写已成为增强大语言模型（LLM）预训练期间知识获取的一种强大技术。然而，现有方法主要在单文档层面运行，孤立地改写单个网页。这使得合成样本局限于文档内部知识，缺失了跨文档关系，导致事实的关联上下文有限。我们提出了 WRAP++（Web discoveRy Amplified Pretraining），通过从网页超链接中发现跨文档关系，并针对发现的每个文档对合成联合问答（QA），从而增强事实知识的关联上下文。具体而言，WRAP++ 发现了包括双向链接（dual-links）和共同提及（co-mentions）在内的高置信度关系模式（relational motifs），并合成了需要跨两个文档进行推理的问答。这产生了任何单一源文档都不具备的关系知识，为相同的事实创建了多样化的切入点。由于有效实体对的数量呈组合式增长，这种发现驱动的合成还将数据规模扩展到了远超单文档改写的程度。在维基百科上实例化 WRAP++，我们将约 84 亿 token 的原始文本扩充为 800 亿 token 的跨文档问答数据。在 SimpleQA 评测中，使用 WRAP++ 训练的 7B 和 32B 规模的 OLMo 模型显著优于单文档方法，并表现出持续的扩展增益，凸显了跨文档知识发现与增强的优势。

## Abstract
Synthetic data rephrasing has emerged as a powerful technique for enhancing knowledge acquisition during large language model (LLM) pretraining. However, existing approaches operate at the single-document level, rewriting individual web pages in isolation. This confines synthesized examples to intra-document knowledge, missing cross-document relationships and leaving facts with limited associative context. We propose WRAP++ (Web discoveRy Amplified Pretraining), which amplifies the associative context of factual knowledge by discovering cross-document relationships from web hyperlinks and synthesizing joint QA over each discovered document pair. Concretely, WRAP++ discovers high-confidence relational motifs including dual-links and co-mentions, and synthesizes QA that requires reasoning across both documents. This produces relational knowledge absent from either source document alone, creating diverse entry points to the same facts. Because the number of valid entity pairs grows combinatorially, this discovery-driven synthesis also amplifies data scale far beyond single-document rewriting. Instantiating WRAP++ on Wikipedia, we amplify ~8.4B tokens of raw text into 80B tokens of cross-document QA data. On SimpleQA, OLMo-based models at both 7B and 32B scales trained with WRAP++ substantially outperform single-document approaches and exhibit sustained scaling gains, underscoring the advantage of cross-document knowledge discovery and amplification.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **WRAP++**，一种旨在通过挖掘网页拓扑结构来增强大语言模型（LLM）预训练中事实知识关联性的合成数据生成框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的合成数据预训练方法（如 WRAP、Nemotron-CC）主要集中在**单文档重写**上，即孤立地改写单个网页。
*   **研究动机**：
    *   **知识孤岛**：单文档合成仅限于文档内部知识，忽略了跨文档的关联（如两个人物之间的合作关系），导致模型获取的事实缺乏关联上下文。
    *   **规模瓶颈**：单文档重写受限于原始语料的文档总数，数据规模难以进一步扩展。
    *   **目标**：通过发现跨文档关系并合成联合问答（Joint QA），利用实体间的组合关系实现数据的指数级扩充，并增强模型的关系推理能力。

### 2. 方法论（WRAP++）
WRAP++ 包含两个核心阶段：
*   **拓扑关系发现（Topological Relation Discovery）**：
    *   利用网页超链接构建有向图，识别高置信度的关系模式：
        *   **双向链接（Dual-link）**：文档 A 和 B 互相引用（如：导演与其代表作）。
        *   **共同提及（Co-mention）**：A 和 B 同时引用一个核心节点 E，且 A 引用 B（如：两个在同一综述中被对比的竞争理论）。
*   **跨文档联合问答合成（Joint QA Synthesis）**：
    *   使用指令微调模型（如 Qwen3-30B）根据发现的文档对生成 QA。
    *   **三大约束**：
        1.  **严格跨文档依赖**：问题必须同时依赖 A 和 B 的信息才能回答。
        2.  **显式事实链（Factual Chaining）**：要求模型在给出答案前先写出推理步骤，连接两篇文档的事实。
        3.  **全知内化（Omniscient Internalization）**：禁止使用“根据文档 A”等表述，要求模型以“全知视角”直接陈述事实，以便模型将其内化为参数化知识。

### 3. 实验设计
*   **数据集**：以维基百科（FineWiki 英文子集，约 8.4B token）为原始语料。
*   **模型规模**：基于 **OLMo-3** 架构，分别在 7B 和 32B 规模上进行持续预训练。
*   **Benchmark**：主要使用 **SimpleQA**（衡量事实准确性），并辅以 12 个通用任务（如 MMLU Redux、ARC、HellaSwag 等）。
*   **对比方法**：
    *   **Pretrained Base**：原始 OLMo-3 检查点。
    *   **WRAP**：标准的单文档 QA 合成（约 5.4B token）。
    *   **Extended WRAP**：通过穷举提取尝试突破单文档上限的基准（约 17.4B token）。
    *   **WRAP++**：本文方法（约 80B token）。

### 4. 资源与算力
论文详细记录了算力消耗：
*   **合成阶段**：使用 Qwen3-30B-A3B-Instruct (FP8) 作为生成器。
*   **训练阶段**：
    *   **OLMo-3-7B (80B token)**：使用 **256 张 H20 GPU**，训练时长未直接给出 80B 的总时长，但提到 8B 规模需 4.5 小时。
    *   **OLMo-3-32B (80B token)**：使用 **1024 张 H20 GPU**，训练时长约为 **1 天 20 小时**。
    *   **精度**：使用 bfloat16 精度和 HSDP（分级共享数据并行）技术。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 的模型扩展，以及从 8B 到 80B 的数据扩展。
*   **消融实验**：非常充分，包括：
    *   **配对策略**：对比了随机配对与拓扑发现配对。
    *   **拓扑类型**：对比了双向链接与共同提及的贡献。
    *   **合成格式**：对比了原始拼接、带原文的 QA 和纯联合 QA。
    *   **生成器规模**：对比了 30B 与 235B 生成器的效果。
*   **客观性**：通过 5-shot 评测和 unbiased pass@k 指标确保了评估的公平性，并进行了 100B token 的中期训练（Mid-training）集成实验。

### 6. 主要结论与发现
*   **性能飞跃**：在 SimpleQA 上，WRAP++ 显著优于所有单文档基准（在 32B 模型上比 Extended WRAP 高出约 6 个百分点）。
*   **突破扩展瓶颈**：单文档方法在 17B token 左右即出现信息饱和与性能平台，而 WRAP++ 凭借组合爆炸带来的数据量，在 80B token 规模下仍保持增长趋势。
*   **知识内化**：通过 pass@k 曲线分析，WRAP++ 不仅提高了 Top-1 准确率，还显著增强了模型提取事实的路径多样性。
*   **通用性保持**：集成到 100B 的中期训练混合数据中时，WRAP++ 在提升事实性的同时，未损害模型的通用推理和语言理解能力。

### 7. 优点
*   **数据扩增能力强**：将 8.4B 的原始文本“放大”为 80B 的高质量 QA，解决了预训练语料枯竭的问题。
*   **拓扑引导的有效性**：利用超链接这一人类先验知识来确保跨文档合成的逻辑合理性，避免了随机配对产生的幻觉。
*   **推理链设计**：强制要求显式事实链，有助于模型学习多跳逻辑。

### 8. 不足与局限
*   **领域局限性**：目前仅在维基百科（结构化程度高、实体中心化）上验证。在噪声巨大的通用网页（如 Common Crawl）上，超链接可能包含广告或导航，需更复杂的过滤机制。
*   **合成成本**：虽然训练高效，但使用 30B 甚至更大的模型生成 80B token 的合成数据，其推理算力成本不容忽视。
*   **潜在偏差**：合成数据可能带有生成器模型（Qwen3）的特定风格或系统性偏差。

（完）

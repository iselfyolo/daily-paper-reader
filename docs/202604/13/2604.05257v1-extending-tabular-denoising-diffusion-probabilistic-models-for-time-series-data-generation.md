---
title: Extending Tabular Denoising Diffusion Probabilistic Models for Time-Series Data Generation
title_zh: 将表格去噪扩散概率模型扩展至时间序列数据生成
authors: "Umang Dobhal, Christina Garcia, Sozo Inoue"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05257v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 用于合成表格和时间序列数据生成的扩散模型
tldr: 针对扩散模型在生成时间序列数据时难以捕捉时间依赖性的局限，本文提出了TabDDPM的扩展版本。通过引入轻量级时间适配器和上下文感知嵌入模块，将表格扩散模型转化为序列感知模型。该方法利用窗口化序列建模和显式时间上下文（如时间步嵌入和掩码），在WISDM数据集上生成了具有高度时间一致性和统计对齐性的合成数据，有效提升了少数类别的表示能力。
selection_source: fresh_fetch
motivation: 现有的TabDDPM模型假设样本相互独立，无法有效处理具有关键时间依赖性的传感器时间序列数据。
method: 通过引入轻量级时间适配器和上下文感知嵌入模块，并结合窗口化序列重构与显式时间上下文建模，增强了模型的序列推理能力。
result: 在WISDM数据集上，该方法生成的合成序列在时间真实性、多样性和分类性能上表现优异，达到了0.64的F1分数和0.71的准确率。
conclusion: 研究证明，配备时间推理机制的扩散模型是生成高质量、具有时间相干性的序列数据的有效且灵活的解决方案。
---

## 摘要
扩散模型正越来越多地被用于创建合成表格和时间序列数据，以进行隐私保护增强。表格去噪扩散概率模型（TabDDPM）能从异构表格数据集中生成高质量的合成数据，但其假设样本之间相互独立，这限制了其在时间依赖性至关重要的时间序列领域的应用。为解决这一问题，我们提出了 TabDDPM 的时间扩展版本，通过使用轻量级时间适配器（temporal adapters）和上下文感知嵌入模块引入了序列感知能力。通过将传感器数据重新表述为窗口化序列，并利用时间步嵌入、条件活动标签以及观测/缺失掩码显式建模时间上下文，我们的方法能够生成具有时间连贯性的合成序列。与基准方法和插值技术相比，使用二元语法转移矩阵（bigram transition matrices）和自相关分析进行的验证表明，该方法在时间真实性、多样性和连贯性方面均有所提升。在 WISDM 加速度计数据集上，所提系统生成的合成时间序列与真实世界的传感器模式高度相似，并实现了相当的分类性能（宏 F1 分数为 0.64，准确率为 0.71）。这对于少数类别的表示以及保持与真实分布的统计对齐尤为有利。这些进展表明，当扩散模型具备时间推理能力时，能为序列数据合成提供有效且灵活的解决方案。未来的工作将探索扩展至更长序列以及集成更强大的时间架构。

## Abstract
Diffusion models are increasingly being utilised to create synthetic tabular and time series data for privacy-preserving augmentation. Tabular Denoising Diffusion Probabilistic Models (TabDDPM) generate high-quality synthetic data from heterogeneous tabular datasets but assume independence between samples, limiting their applicability to time-series domains where temporal dependencies are critical. To address this, we propose a temporal extension of TabDDPM, introducing sequence awareness through the use of lightweight temporal adapters and context-aware embedding modules. By reformulating sensor data into windowed sequences and explicitly modeling temporal context via timestep embeddings, conditional activity labels, and observed/missing masks, our approach enables the generation of temporally coherent synthetic sequences. Compared to baseline and interpolation techniques, validation using bigram transition matrices and autocorrelation analysis shows enhanced temporal realism, diversity, and coherence. On the WISDM accelerometer dataset, the suggested system produces synthetic time-series that closely resemble real world sensor patterns and achieves comparable classification performance (macro F1-score 0.64, accuracy 0.71). This is especially advantageous for minority class representation and preserving statistical alignment with real distributions. These developments demonstrate that diffusion based models provide effective and adaptable solutions for sequential data synthesis when they are equipped for temporal reasoning. Future work will explore scaling to longer sequences and integrating stronger temporal architectures.

---

## 论文详细总结（自动生成）

这篇论文题为《将表格去噪扩散概率模型扩展至时间序列数据生成》，由 Umang Dobhal 等人撰写。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：人体活动识别（HAR）依赖大量标注的传感器数据，但实际采集过程成本高、耗时长且涉及隐私。
*   **核心问题**：现有的扩散模型（如 TabDDPM）虽然在处理异构表格数据方面表现出色，但其核心假设是样本间相互独立，忽略了传感器数据中至关重要的高维时间依赖性。
*   **整体含义**：本文旨在通过对 TabDDPM 进行时间维度的扩展，使其能够生成既符合统计分布又具有时间连贯性的多变量传感器序列，从而解决 HAR 领域的数据稀缺和类别不平衡问题。

### 2. 论文提出的方法论
该方法在保留 TabDDPM 基础架构的同时，引入了序列感知组件：
*   **核心思想**：将时间序列视为窗口化的表格数据，通过轻量级适配器和上下文嵌入增强模型对序列动态的建模能力。
*   **关键技术细节**：
    *   **上下文感知嵌入块**：包含三个部分：1) **时间步嵌入**（区分去噪阶段）；2) **条件嵌入**（注入活动标签以实现受控生成）；3) **观测掩码**（处理缺失值，增强鲁棒性）。
    *   **时间适配器（Temporal Adapters）**：在去噪网络中插入轻量级模块。首先添加正弦位置编码，随后通过两层 **1D 卷积**（Conv1D，卷积核大小为3）捕捉局部时间模式，最后进行特征投影。
    *   **算法流程**：输入特征经分位数转换和独热编码后，与上下文嵌入合并进入扩散过程。在反向去噪过程中，时间适配器辅助 MLP 预测噪声，最终重建出长度为 $T$ 的连贯序列。

### 3. 实验设计
*   **数据集**：使用 **WISDM** 智能手机加速度计数据集（包含行走、慢跑、坐、立、上楼、下楼 6 类活动）。
*   **场景**：针对少数类活动（如坐、立、上下楼）进行数据增强，构建平衡数据集。
*   **对比方法（Benchmark）**：
    *   **Baseline**：原始不平衡数据集。
    *   **SMOTE**：传统的过采样插值方法。
    *   **Original TabDDPM**：未经过时间扩展的原始扩散模型。
*   **评估指标**：分类性能（准确率、宏 F1 分数）、二元语法转移矩阵（评估短程转换）、自相关函数（ACF，评估长程依赖）。

### 4. 资源与算力
*   **硬件平台**：实验在 **Apple M2 GPU** 上运行，使用 PyTorch 的 MPS 后端。
*   **训练参数**：Batch size 为 64，学习率为 $1 \times 10^{-3}$，使用 AdamW 优化器，训练 50 个 Epoch。
*   **算力说明**：论文强调了该架构的“轻量级”特性，Conv1D 适配器的计算复杂度为 $O(T)$，远低于 Transformer 的 $O(T^2)$，适合在资源受限环境下运行。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 名用户的窗口化数据进行了训练与生成，生成了 2055 条合成序列。
*   **充分性评价**：实验设计较为全面，不仅对比了分类性能，还通过统计学手段（ACF 和转移矩阵）深入分析了序列的内部结构。然而，实验仅限于单一的 WISDM 数据集，且仅选取了部分用户数据，在跨数据集的泛化性验证上略显不足。

### 6. 论文的主要结论与发现
*   **性能对等**：在分类任务中，该方法达到了与 SMOTE 相当的水平（F1 0.64，准确率 0.71），但在少数类（如“坐”和“立”）的精确度上表现更好。
*   **时间真实性**：通过 ACF 和转移矩阵证实，该方法生成的序列比原始 TabDDPM 和 SMOTE 更接近真实传感器的物理特性（如微小的姿态漂移和周期性）。
*   **稳定性**：引入的时间适配器有效消除了原始 TabDDPM 在生成序列时产生的异常尖峰和噪声。

### 7. 优点
*   **架构高效**：通过轻量级适配器而非重型 Transformer 实现了序列感知，兼顾了性能与计算成本。
*   **端到端处理**：能够同时处理数值型（加速度）和类别型（标签）特征，并内置了缺失值处理机制。
*   **物理一致性**：生成的合成数据在时域特征上具有更高的保真度，不仅是数值上的对齐，更是运动逻辑上的对齐。

### 8. 不足与局限
*   **分类增益有限**：虽然序列质量提升明显，但在下游分类任务中的准确率提升并不显著，这可能受限于所选的随机森林分类器无法充分利用高阶时间特征。
*   **实验覆盖面窄**：仅在一个数据集上进行了验证，未测试更长序列或更复杂的多模态传感器数据。
*   **动态类别挑战**：对于“上楼”和“下楼”等高变异性活动，所有方法（包括本文方法）的召回率仍然较低，说明模型对复杂动态活动的建模仍有提升空间。

（完）

---
title: "Revise: A Framework for Revising OCRed text in Practical Information Systems with Data Contamination Strategy"
title_zh: Revise：一种基于数据污染策略的实际信息系统 OCR 文本修正框架
authors: "Gyuho Shim, Seongtae Hong, Heuiseok Lim"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08115v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于训练的模拟OCR错误的合成数据生成策略
tldr: 针对现有Document AI框架在文档结构化管理方面的不足，本文提出了Revise框架。该框架通过建立多层级的OCR错误分类体系，并利用数据污染策略生成模拟真实错误的合成数据，训练模型在字符、词汇和结构层面纠正OCR错误。实验证明，Revise能显著提升文档内容的结构化表示质量，进而增强下游检索和问答任务的性能。
selection_source: fresh_fetch
motivation: 现有Document AI方法侧重于解决特定任务，缺乏对OCR输出进行系统性结构化组织和错误修正的能力。
method: 提出Revise框架，利用分层OCR错误分类法和模拟真实错误的合成数据生成策略来训练纠错模型。
result: 实验结果显示Revise能有效修正OCR输出，显著提升了文档检索和问答等下游任务的表现。
conclusion: Revise框架克服了现有Document AI在结构化管理上的局限，为实用信息系统中的文档处理提供了有效方案。
---

## 摘要
近期大语言模型（LLMs）的进展显著提升了文档人工智能（Document AI）领域，在问答等文档理解任务上展现了卓越的性能。然而，现有方法主要侧重于解决特定任务，缺乏结构化组织和管理文档信息的能力。为解决这一局限性，我们提出了 Revise，这是一个在字符、单词和结构层面系统性纠正 OCR 引入错误的框架。具体而言，Revise 采用了常见 OCR 错误的全面分层分类法，以及一种能够真实模拟此类错误以训练有效纠正模型的合成数据生成策略。实验结果表明，Revise 能有效纠正 OCR 输出，从而实现文档内容的更结构化表示和系统化管理。因此，我们的方法显著增强了文档检索和问答任务的下游性能，凸显了克服现有 Document AI 框架结构化管理局限性的潜力。

## Abstract
Recent advances in Large Language Models (LLMs) have significantly improved the field of Document AI, demonstrating remarkable performance on document understanding tasks such as question answering. However, existing approaches primarily focus on solving specific tasks, lacking the capability to structurally organize and manage document information. To address this limitation, we propose Revise, a framework that systematically corrects errors introduced by OCR at the character, word, and structural levels. Specifically, Revise employs a comprehensive hierarchical taxonomy of common OCR errors and a synthetic data generation strategy that realistically simulates such errors to train an effective correction model. Experimental results demonstrate that Revise effectively corrects OCR outputs, enabling more structured representation and systematic management of document contents. Consequently, our method significantly enhances downstream performance in document retrieval and question answering tasks, highlighting the potential to overcome the structural management limitations of existing Document AI frameworks.

---

## 论文详细总结（自动生成）

以下是对论文《Revise: A Framework for Revising OCRed text in Practical Information Systems with Data Contamination Strategy》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在文档人工智能（Document AI）领域，虽然大语言模型（LLM）在问答和推理任务上表现出色，但实际应用中仍面临**OCR（光学字符识别）错误**的严峻挑战。OCR 在处理复杂布局、多样字体或低质量扫描件时，不可避免地会产生字符误认、词汇分割错误及阅读顺序混乱。这些错误会产生“级联效应”，严重损害下游的文档检索（Retrieval）和知识库构建。
本文的核心动机是：**如何系统性地纠正 OCR 错误，将非结构化的图像文本转化为高质量、结构化的数字资产**，从而提升信息系统的整体性能。

### 2. 方法论：核心思想与关键技术
论文提出了 **REVISE** 框架，其核心思想是通过模拟真实的 OCR 错误模式来生成合成数据，并训练轻量级 LLM 进行端到端的文本修正。

*   **分层错误分类法（Hierarchical Taxonomy）**：
    *   **字符级**：插入（多字）、删除（漏字）、替换（形近字误认，如 O→0）、转置（位置颠倒）。
    *   **词级**：过度分割（单词被空格切断）、欠分割（多个单词粘连）。
    *   **列级（结构级）**：分栏阅读顺序错误（OCR 错误地横向跨栏读取文本）。
*   **数据污染策略（Data Contamination Strategy）**：
    *   利用维基百科等干净语料，根据预设比例（如字符替换率 0.05 等）自动注入上述三类错误。
    *   特别设计了“分栏模拟算法”，将单栏文本重新排列并模拟 OCR 跨栏读取的逻辑混乱。
*   **模型训练**：
    *   以 **Llama-3.1-1B-Instruct** 为基座模型。
    *   训练了一个综合模型（**REVISE-meta**）和六个针对特定错误类型的专项辅助模型。
    *   采用指令微调（Instruction Tuning），引导模型识别并修复特定模式的 OCR 噪声。

### 3. 实验设计
*   **数据集**：
    *   **检索任务**：VisualMRC、DUDE（包含复杂布局的文档）。
    *   **相似度与问答任务**：DocVQA、CORD（收据）、FUNSD（表单）。
*   **Benchmark 与对比方法**：
    *   **Baseline**：原始 OCR 输出（使用 EasyOCR 或数据集自带 OCR 结果）。
    *   **对比维度**：对比了 REVISE-meta 与各个专项模型（如 only-Segmentation, only-Column）的效果。
*   **评估指标**：Recall@k（检索）、BERTScore（语义相似度）、CIDEr 和 F1-score（问答质量）、GPT-4o-mini 胜率（定性分析）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A6000 GPU**（每台 48GB 显存），CPU 为 AMD EPYC 7513（32 核）。
*   **训练细节**：
    *   训练 1 个 Epoch，Batch Size 为 32。
    *   使用 Adam 优化器，学习率 1e-4，采用 bfloat16 精度。
    *   训练数据量为 30,000 条合成样本。
*   **推理**：推理阶段仅需单张加速卡。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流 Document AI 数据集上进行了测试，涵盖了检索、语义匹配和生成式问答三大场景。
*   **消融实验**：通过训练 6 个专项模型，量化了不同错误类型（如分栏错误 vs. 字符错误）对下游任务的影响权重。
*   **客观性**：引入了第三方 LLM（GPT-4o-mini）作为裁判进行定性评估（Win Rate），并使用了多种嵌入模型（bge, e5, jina, gte）验证检索的通用性。实验设计较为全面，能够客观反映模型在不同布局文档上的鲁棒性。

### 6. 主要结论与发现
*   **性能提升显著**：REVISE-meta 在 DUDE 数据集上的检索 Recall 平均提升了 **17.3%**。
*   **结构修正至关重要**：在复杂布局（如 DUDE）中，**列级阅读顺序（Column Order）**的修正对性能贡献最大；而在自由文本中，**词级分割（Segmentation）**的修正对语义理解最关键。
*   **任务无关性**：REVISE 作为一个预处理步骤，无需针对特定下游任务微调，即可普适性地增强 Gemma-2 和 Llama-3 等模型的问答表现。
*   **合成数据的有效性**：证明了无需昂贵的人工标注，仅靠科学的错误模拟即可训练出高效的纠错模型。

### 7. 优点
*   **系统化的错误建模**：不同于以往只关注拼写检查的研究，本文深入探讨了文档布局导致的结构性错误。
*   **轻量化与实用性**：选用 1B 参数模型，既保证了纠错速度，又降低了在实际信息系统中的部署门槛。
*   **数据驱动**：提出的污染策略可以根据特定行业的 OCR 错误分布进行灵活调整，具有很强的工程迁移价值。

### 8. 不足与局限
*   **内容类型受限**：目前仅针对纯文本，无法处理文档中的**表格、图表、数学公式**等复杂非文本元素。
*   **领域覆盖不足**：实验主要基于通用数据集，未在特定工业领域（如医疗病历、法律卷宗）的极端噪声环境下进行深度验证。
*   **模型偏见风险**：虽然使用了 LLM 评估，但 LLM 裁判本身可能存在偏见，且纠错模型在极端情况下可能产生“过度纠错”或幻觉。
*   **统计基础薄弱**：错误注入的比例（Ratio）主要基于经验观察，缺乏更大规模真实 OCR 错误分布的统计学支撑。

（完）

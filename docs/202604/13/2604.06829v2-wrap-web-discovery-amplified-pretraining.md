---
title: "WRAP++: Web discoveRy Amplified Pretraining"
title_zh: WRAP++：基于网页发现的增强预训练
authors: "Jiang Zhou, Yunhao Wang, Xing Wu, Tinghao Yu, Feng Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06829v2"
tags: ["query:ds-gen"]
score: 10.0
evidence: 为LLM预训练合成跨文档的联合问答数据
tldr: 针对大语言模型预训练中合成数据重写仅局限于单文档、缺乏跨文档关联的问题，本文提出 WRAP++。该方法通过挖掘网页超链接发现跨文档关系（如双向链接、共同提及），并合成需要跨文档推理的问答对。在维基百科上的实验显示，WRAP++ 将 8.4B 原始文本扩充至 80B 跨文档问答数据，显著提升了 OLMo 模型在 SimpleQA 等任务上的知识获取能力和扩展性。
selection_source: fresh_fetch
motivation: 现有的合成数据重写方法仅针对单文档，无法捕捉跨文档的关联知识，限制了模型对事实知识的关联理解。
method: 通过挖掘网页超链接识别高置信度的关系模式（如双向链接和共同提及），并据此合成跨文档的联合问答数据。
result: 在维基百科上将数据规模从 8.4B 扩充至 80B，使 7B 和 32B 规模的 OLMo 模型在 SimpleQA 任务上显著优于单文档重写方法。
conclusion: 跨文档知识发现与合成能有效增强模型的事实关联能力，并提供比单文档重写更强的预训练扩展潜力。
---

## 摘要
合成数据改写已成为增强大语言模型（LLM）预训练过程中知识获取的一种强大技术。然而，现有方法主要在单文档层面运行，孤立地改写单个网页。这使得合成示例局限于文档内部知识，缺失了跨文档关系，导致事实的关联上下文有限。我们提出了 WRAP++（Web discoveRy Amplified Pretraining），通过从网页超链接中发现跨文档关系，并针对发现的每个文档对合成联合问答（QA），从而增强事实知识的关联上下文。具体而言，WRAP++ 发现包括双向链接（dual-links）和共同提及（co-mentions）在内的高置信度关系基元（relational motifs），并合成需要跨两个文档进行推理的问答。这产生了任何单一源文档中都不具备的关系知识，为相同的事实创建了多样化的切入点。由于有效实体对的数量呈组合式增长，这种发现驱动的合成还将数据规模扩展到了远超单文档改写的程度。在维基百科上实例化 WRAP++，我们将约 84 亿 token 的原始文本扩充为 800 亿 token 的跨文档问答数据。在 SimpleQA 评测中，使用 WRAP++ 训练的 7B 和 32B 规模的 OLMo 模型显著优于单文档方法，并表现出持续的扩展增益，凸显了跨文档知识发现与增强的优势。

## Abstract
Synthetic data rephrasing has emerged as a powerful technique for enhancing knowledge acquisition during large language model (LLM) pretraining. However, existing approaches operate at the single-document level, rewriting individual web pages in isolation. This confines synthesized examples to intra-document knowledge, missing cross-document relationships and leaving facts with limited associative context. We propose WRAP++ (Web discoveRy Amplified Pretraining), which amplifies the associative context of factual knowledge by discovering cross-document relationships from web hyperlinks and synthesizing joint QA over each discovered document pair. Concretely, WRAP++ discovers high-confidence relational motifs including dual-links and co-mentions, and synthesizes QA that requires reasoning across both documents. This produces relational knowledge absent from either source document alone, creating diverse entry points to the same facts. Because the number of valid entity pairs grows combinatorially, this discovery-driven synthesis also amplifies data scale far beyond single-document rewriting. Instantiating WRAP++ on Wikipedia, we amplify ~8.4B tokens of raw text into 80B tokens of cross-document QA data. On SimpleQA, OLMo-based models at both 7B and 32B scales trained with WRAP++ substantially outperform single-document approaches and exhibit sustained scaling gains, underscoring the advantage of cross-document knowledge discovery and amplification.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **WRAP++**，一种旨在通过挖掘网页拓扑结构来增强大语言模型（LLM）预训练中事实知识关联性的合成数据生成框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的合成数据预训练方法（如 WRAP）主要采用“单文档改写”模式，即孤立地重写单个网页。这种方式导致合成数据仅包含文档内部知识，缺乏跨文档的关联（如不同实体间的复杂关系），限制了模型对事实知识的关联检索能力。
*   **核心目标**：通过发现网页间的超链接关系，将多个相关文档组合在一起，合成需要跨文档推理的联合问答（QA）数据，从而打破知识孤岛，实现数据规模的组合式扩充。

### 2. 方法论
WRAP++ 的核心流程分为两个阶段：
*   **拓扑关系发现（Topological Relation Discovery）**：
    *   利用网页超链接构建有向图，识别两种高置信度的关系基元（Motifs）：
        1.  **双向链接（Dual-link）**：文档 A 和 B 互相引用（如导演与其代表作）。
        2.  **共同提及（Co-mention）**：文档 A 和 B 同时引用文档 E，且 A 引用 B（如两个在同一综述中被提及且有直接关联的竞争理论）。
*   **跨文档联合问答合成（Joint QA Synthesis）**：
    *   使用指令微调模型（如 Qwen3-30B）根据发现的文档对生成 QA。
    *   **三大约束**：
        1.  **严格跨文档依赖**：问题必须要求同时结合两个文档的信息才能回答。
        2.  **显式事实链条**：在给出答案前，模型必须先输出推理路径（Factual Chaining）。
        3.  **全知视角内化**：禁止使用“根据文档 A”等表述，要求模型以“全知全能”的口吻陈述事实，便于模型将其内化为参数化知识。
*   **数据扩充**：由于实体对的组合特性，WRAP++ 将 8.4B 的原始维基百科文本扩充到了 80B token 的合成数据。

### 3. 实验设计
*   **数据集**：以 FineWiki 中的英文维基百科（约 6.7M 文章，8.4B token）作为源语料。
*   **评测基准**：主要使用 **SimpleQA**（衡量事实准确性），并辅助 12 个通用任务（如 MMLU Redux、ARC、HellaSwag 等）评估通用能力。
*   **对比方法**：
    *   **Pretrained Base**：原始 OLMo-3 检查点。
    *   **WRAP**：标准的单文档 QA 合成（约 5.4B token）。
    *   **Extended WRAP**：通过穷举提取等策略强化的单文档合成（约 17.4B token）。
*   **训练模型**：基于 OLMo-3 架构的 7B 和 32B 模型。

### 4. 资源与算力
论文在附录 B 中详细列出了算力消耗：
*   **GPU 型号与数量**：
    *   **7B 模型**：使用 256 张 **H20 GPU**。
    *   **32B 模型**：使用 1024 张 **H20 GPU**。
*   **训练时长**：
    *   7B 模型在 8B 数据上训练约 4.5 小时；在 80B 数据上训练时间按比例增加。
    *   32B 模型在 80B 数据上训练约 **1 天 20 小时**。
*   **合成模型**：使用 Qwen3-30B-Instruct 进行数据生成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 的模型参数量，以及从 8B 到 80B 的数据扩展曲线。
*   **消融实验**：非常充分，分别验证了：
    *   拓扑发现的必要性（对比随机配对）。
    *   不同拓扑基元（双向链接 vs 共同提及）的贡献。
    *   合成格式（QA vs 原始拼接）的影响。
    *   合成模型规模（30B vs 235B）对质量的影响。
*   **公平性**：在相同的源语料（FineWiki）和相同的训练框架（olmo-core）下进行对比，确保了实验的客观性。

### 6. 主要结论与发现
*   **性能领先**：WRAP++ 在 SimpleQA 上的表现显著优于所有单文档基线（在 32B 规模下比 Extended WRAP 高出 6.1 个百分点）。
*   **扩展性强**：单文档方法在 17B token 左右即出现收益递减，而 WRAP++ 在 80B token 范围内仍保持持续的性能增长，未见明显饱和。
*   **知识恢复力**：通过 Pass@k 曲线分析，WRAP++ 不仅提高了 Top-1 准确率，还显著增强了模型提取事实的鲁棒性和多样化路径。
*   **通用性保持**：将 WRAP++ 数据混入 100B 的中段训练（Mid-training）中，能在不损害通用能力的前提下显著提升事实性任务表现。

### 7. 优点
*   **突破瓶颈**：巧妙利用网页拓扑结构解决了合成数据在单文档层面的规模和关联性瓶颈。
*   **组合扩容**：实现了约 10 倍的数据放大率，为预训练提供了海量高质量的关系型数据。
*   **设计严谨**：提出的“全知视角”和“事实链条”约束有效解决了合成数据中常见的归因偏差问题。

### 8. 不足与局限
*   **语料局限性**：目前仅在维基百科这种结构化程度高、实体清晰的语料上验证。在噪声巨大的通用网页（如 Common Crawl）上，如何过滤无效链接和广告干扰仍是挑战。
*   **合成成本**：虽然训练高效，但使用大模型（Qwen3-30B）合成 80B token 的数据需要巨大的推理算力开销。
*   **潜在幻觉**：尽管有拓扑引导，但合成模型在强行连接两个文档时仍可能产生虚假的关系描述。

（完）

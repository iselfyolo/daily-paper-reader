---
title: BlendFusion -- Scalable Synthetic Data Generation for Diffusion Model Training
title_zh: BlendFusion —— 用于扩散模型训练的可扩展合成数据生成
authors: "Thejas Venkatesh, Suguna Varshini Velury"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09022v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 用于扩散模型训练的可扩展合成数据生成
tldr: 针对扩散模型训练中纯合成数据导致的视觉不一致和模型崩溃（MAD）问题，本文提出了 BlendFusion 框架。该框架利用 3D 场景和路径追踪技术，结合以物体为中心的相机放置策略、鲁棒过滤机制和自动标注，生成高质量的图像-文本对。基于此构建了 FineBLEND 数据集，实验证明其在质量和多样性上优于传统数据集，为可扩展的合成数据生成提供了新方案。
selection_source: fresh_fetch
motivation: 解决纯扩散模型生成的合成数据存在的视觉不一致性及训练中的模型自噬（MAD）问题。
method: 提出一种基于 3D 场景路径追踪的生成框架，包含以物体为中心的相机采样、自动过滤和描述生成。
result: 构建了高质量数据集 FineBLEND，并验证了其在图像质量和物体中心采样策略上的优越性。
conclusion: BlendFusion 为生成大规模、高质量且可配置的 3D 合成数据集提供了一个有效的开源解决方案。
---

## 摘要
随着扩散模型的快速普及，合成数据生成已成为应对大规模图像数据集日益增长需求的一种极具前景的方法。然而，纯由扩散模型生成的图像往往表现出视觉不一致性，且在这些数据上训练模型可能会产生一种自噬反馈循环，导致模型崩溃，这通常被称为模型自噬障碍（MAD）。为了应对这些挑战，我们提出了 BlendFusion，这是一个利用路径追踪从 3D 场景生成合成数据的可扩展框架。我们的流水线结合了以物体为中心的相机放置策略、鲁棒的过滤机制以及自动标注功能，以生成高质量的图像-文本对。利用该流水线，我们构建了 FineBLEND，这是一个由多样化 3D 场景构建的图像-文本数据集。我们对 FineBLEND 的质量进行了实证分析，并将其与几个广泛使用的图像-文本数据集进行了比较。我们还证明了以物体为中心的相机放置策略相对于物体无关采样方法的有效性。我们的开源框架具有高度的可配置性，使社区能够从 3D 场景中创建自己的数据集。

## Abstract
With the rapid adoption of diffusion models, synthetic data generation has emerged as a promising approach for addressing the growing demand for large-scale image datasets. However, images generated purely by diffusion models often exhibit visual inconsistencies, and training models on such data can create an autophagous feedback loop that leads to model collapse, commonly referred to as Model Autophagy Disorder (MAD). To address these challenges, we propose BlendFusion, a scalable framework for synthetic data generation from 3D scenes using path tracing. Our pipeline incorporates an object-centric camera placement strategy, robust filtering mechanisms, and automatic captioning to produce high-quality image-caption pairs. Using this pipeline, we curate FineBLEND, an image-caption dataset constructed from a diverse set of 3D scenes. We empirically analyze the quality of FineBLEND and compare it to several widely used image-caption datasets. We also demonstrate the effectiveness of our object-centric camera placement strategy relative to object-agnostic sampling approaches. Our open-source framework is designed for high configurability, enabling the community to create their own datasets from 3D scenes.

---

## 论文详细总结（自动生成）

以下是对论文《BlendFusion -- Scalable Synthetic Data Generation for Diffusion Model Training》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：扩散模型（如 Stable Diffusion）的成功依赖于大规模图像-文本对，但获取真实数据成本高且存在隐私/偏见问题。
*   **核心挑战**：使用扩散模型生成的合成数据进行训练会引发“模型自噬障碍”（Model Autophagy Disorder, MAD），导致模型质量退化和崩溃。此外，扩散生成的图像常伴有几何畸变、物体幻觉和语义伪影。
*   **研究目标**：提出 **BlendFusion** 框架，利用 3D 场景和物理路径追踪（Path Tracing）生成几何一致、光影真实且无自噬风险的合成数据集。

### 2. 方法论
BlendFusion 是一个自动化的 3D 渲染与标注流水线，核心步骤包括：
*   **以物体为中心的相机放置（Object-centric Camera Placement）**：
    *   针对 3D 场景中的每个网格物体，计算其轴向包围盒（AABB）中心。
    *   在物体周围 45° 间隔设置 8 个方位角，固定仰角为 0°（模拟人眼视角）。
    *   **自适应距离计算**：根据物体的投影高度和预设的填充比例（Fill Fraction, 设为 2/3），动态调整相机距离，确保物体在画面中比例一致。
*   **多级过滤机制**：
    *   **启发式过滤**：剔除无物体像素（遮挡）、亮度过低（过暗）或像素方差过小（无信息量）的图像。
    *   **VLM 过滤**：使用 Qwen3-VL-8B 模型判断图像是否“可描述”，剔除极端特写、严重截断或无法识别类别的渲染图。
*   **自动标注与质量筛选**：
    *   利用 VLM 生成事实性、中立且简洁的描述。
    *   应用 CLIPScore（语义对齐）和 LAION 审美评分（视觉质量）进行二次筛选。
*   **多样性感知采样**：使用 DinoV2 嵌入向量进行最远点采样（FPS），在减少冗余的同时最大化视觉覆盖范围。

### 3. 实验设计
*   **数据集/场景**：使用了 NVIDIA ORCA 项目（Bistro, Emerald Square, Sun Temple）、Blender 官方演示场景（Classroom, Barbershop）等多样化 3D 环境。
*   **基准对比（Benchmark）**：
    *   **质量对比**：将生成的 **FineBLEND** 数据集与 MS-COCO、Conceptual Captions (CC) 和 ReLAION 进行对比。
    *   **消融实验**：对比了“以物体为中心”策略与“随机视角采样”、“基于锚点的相机扫描”在不同场景下的有效样本产出率。
    *   **定性对比**：对比了 BlendFusion 渲染图与 Stable Diffusion XL (SDXL) 根据相同描述生成的图像。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/渲染时长。
*   **实现细节**：提到使用了 Blender 引擎和 BlenderProc 库进行程序化渲染，标注和过滤使用了 Qwen3-VL-8B-Instruct 模型。作者指出由于路径追踪和 VLM 过滤的计算开销，目前数据集规模限制在 7,500 对。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 7,500 个图像-文本对的 FineBLEND 数据集。
*   **充分性评价**：
    *   **消融实验充分**：在高中低三种不同难度的场景下验证了采样策略，证明了物体中心采样的优越性。
    *   **对比客观**：使用了标准的 CLIPScore 和审美评分指标，并提供了直观的视觉对比来揭示扩散模型的几何缺陷。
    *   **局限性**：实验未包含使用该数据集实际训练扩散模型并观察其对 MAD 缓解效果的闭环验证。

### 6. 主要结论与发现
*   **高质量对齐**：FineBLEND 的 CLIPScore 与 COCO 相当，且标准差更低，说明 VLM 标注的合成数据具有极高的语义一致性。
*   **几何真实性**：相比 SDXL 生成的图像，BlendFusion 渲染图在几何结构、文字渲染和空间逻辑上完全符合物理规律，避免了幻觉。
*   **采样效率**：以物体为中心的策略在复杂场景（如理发店）中产生的可用样本比例远高于随机采样。

### 7. 优点
*   **物理一致性**：通过 3D 渲染从根本上解决了扩散模型合成数据中的几何畸变问题。
*   **高度可控**：框架开源且高度可配置，允许用户根据需求定制相机视角、分辨率和过滤阈值。
*   **自动化程度高**：集成了从相机放置到 VLM 标注的全流程，降低了人工干预成本。

### 8. 不足与局限
*   **规模限制**：目前数据集（7.5k）相对于主流预训练数据集规模较小，扩展到百万级需要巨大的渲染算力。
*   **领域差异（Domain Gap）**：渲染图的审美评分低于真实照片，存在一定的“塑料感”或非真实感，可能需要扩散模型后处理（如 ControlNet）来增强写实度。
*   **缺乏下游验证**：论文未展示在 FineBLEND 上训练后的模型在真实任务中的性能表现。

（完）

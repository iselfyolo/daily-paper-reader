---
title: Task-Aware LLM Routing with Multi-Level Task-Profile-Guided Data Synthesis for Cold-Start Scenarios
title_zh: 面向冷启动场景的基于多层级任务画像引导数据合成的任务感知 LLM 路由
authors: "Hui Liu, Bin Zou, Kecheng Chen, Jie Liu, Wenya Wang, Haoliang Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09377v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 多层级任务画像引导的数据合成框架
tldr: 针对大语言模型路由系统在冷启动场景下因缺乏领域内数据而泛化性差的问题，本文提出了一种多层级任务画像引导的数据合成框架。该框架通过构建分层任务分类体系并生成多样化问答对来模拟真实分布，并据此开发了TRouter路由模型。TRouter利用潜在任务类型变量和先验正则化来建模查询的成本与性能，在冷启动和领域内设置下均表现优异，有效实现了模型选择的成本-性能平衡。
selection_source: fresh_fetch
motivation: 现有的LLM路由系统在缺乏领域内训练数据的冷启动场景下难以有效泛化。
method: 提出多层级任务画像引导的数据合成框架，并结合潜在任务类型变量和先验正则化构建了TRouter路由模型。
result: 实验证明该合成框架能显著缓解冷启动问题，且TRouter在多个基准测试中均实现了高效的模型路由。
conclusion: 通过任务感知的合成数据增强和概率建模，可以显著提升LLM路由系统在未知领域和复杂任务中的实用性。
---

## 摘要
大语言模型 (LLMs) 在不同任务和查询中的性能与计算成本表现出显著差异，这促使了路由系统的出现，用于选择模型以满足用户特定的成本-性能权衡。然而，现有的路由器在缺乏领域内训练数据的冷启动场景下泛化能力较差。我们通过一种多层级任务画像引导的数据合成框架来解决这一局限性，该框架构建了一个层级化的任务分类体系，并生成多样化的问答对以逼近测试时的查询分布。在此基础上，我们提出了 TRouter，一种任务类型感知的路由方法，它通过隐式任务类型变量对基于查询条件的成本和性能进行建模，并利用从合成任务分类体系中导出的先验正则化。这种设计增强了 TRouter 在冷启动和领域内设置下的路由效用。在多个基准测试中，我们证明了我们的合成框架缓解了冷启动问题，并且 TRouter 实现了有效的 LLM 路由。

## Abstract
Large language models (LLMs) exhibit substantial variability in performance and computational cost across tasks and queries, motivating routing systems that select models to meet user-specific cost-performance trade-offs. However, existing routers generalize poorly in cold-start scenarios where in-domain training data is unavailable. We address this limitation with a multi-level task-profile-guided data synthesis framework that constructs a hierarchical task taxonomy and produces diverse question-answer pairs to approximate the test-time query distribution. Building on this, we introduce TRouter, a task-type-aware router approach that models query-conditioned cost and performance via latent task-type variables, with prior regularization derived from the synthesized task taxonomy. This design enhances TRouter's routing utility under both cold-start and in-domain settings. Across multiple benchmarks, we show that our synthesis framework alleviates cold-start issues and that TRouter delivers effective LLM routing.

---

## 论文详细总结（自动生成）

以下是对论文《Task-Aware LLM Routing with Multi-Level Task-Profile-Guided Data Synthesis for Cold-Start Scenarios》的结构化深入总结：

### 1. 核心问题与整体含义
论文聚焦于**大语言模型（LLM）路由系统**在**冷启动场景**下的失效问题。
*   **研究动机**：不同LLM在性能和成本上差异巨大，路由系统旨在根据用户偏好（如成本优先或性能优先）为每个查询选择最合适的模型。
*   **核心挑战**：现有的路由器（如基于分类或回归的模型）高度依赖领域内（In-domain）的标注数据。在冷启动场景（无标注数据）下，现有路由器由于领域偏移（Domain Shift）表现极差，甚至不如简单的规则策略。
*   **整体含义**：本文旨在通过“合成数据”替代“人工标注”，使路由器在没有任何目标领域数据的情况下，也能实现高效的模型分发。

### 2. 方法论
论文提出了一个双层框架：**数据合成框架** + **TRouter 路由模型**。

#### A. 多层级任务画像引导的数据合成
为了模拟真实世界的查询分布，框架通过以下步骤生成数据：
1.  **任务类型生成器**：构建“领域（Domain）- 子类（Subcategory） - 难度（Difficulty）”三级层级体系。从种子领域出发，递归生成非重叠的子任务。
2.  **质量评估器**：利用LLM进行自我批判（Self-critique），确保分类体系的去重、具体性和完整性。
3.  **问答对生成器**：基于任务画像（Task Profile）批量生成QA对，并使用语义相似度过滤（阈值0.9）以保证多样性。
4.  **自动标注**：利用候选LLM池运行生成的QA，并使用“LLM-as-a-Judge”自动评估性能，记录Token消耗作为成本。

#### B. TRouter：任务感知路由模型
TRouter 改变了直接从查询特征预测性能/成本的传统做法，引入了**隐式任务类型变量**：
*   **概率建模**：将条件分布分解为 $p(h|q, m) = \sum_{t \in T} p(h|t, m)p(t|q)$，其中 $h$ 是指标（性能或成本），$q$ 是查询，$m$ 是模型，$t$ 是任务类型。
*   **关键组件**：
    *   **任务识别模块**：预测查询属于各任务类型的概率分布。
    *   **指标预测模块**：基于任务类型预测特定模型在该任务下的表现。
*   **正则化**：引入变分推理，利用合成的任务分类体系作为先验分布进行正则化，增强模型的泛化能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **主实验**：Alpaca (混合问答), GSM8K (数学推理), SQuAD (阅读理解), Multi-News (摘要)。
    *   **扩展实验**：WMT (翻译), MBPP (编程), LegalBench (法律), MedMcQA (医疗)。
*   **LLM 候选池**：
    *   开源池：Qwen 3 系列（0.6B 到 235B）。
    *   商业池：Gemini 系列、豆包（Doubao）等。
*   **对比方法（Baselines）**：
    *   **冷启动基准**：最小模型、最大模型、自适应模型（规则）、Prompt LLM（让GPT-4选模型）。
    *   **领域内基准**：RouterDC, GraphRouter, FrugalGPT, C2MAB-V, MetricRouter。
*   **评估协议**：传统指标（F1, Accuracy等）与 LLM-as-a-Judge。

### 4. 资源与算力
*   **合成成本**：论文明确指出使用 GPT-4.1 生成 17,880 个 QA 对的成本约为 **14.34 美元**；使用 Gemini-2.5-Flash 的成本约为 **10.03 美元**。
*   **训练细节**：TRouter 使用轻量级 MLP 和 Sentence Transformer（all-MiniLM-L6-v2）作为编码器。
*   **算力说明**：文中未详细列出具体的 GPU 型号和训练时长，但由于路由器模型非常轻量（参数量级小），且实验涉及 30-shot 的小样本微调，推测其对算力需求极低，普通单张消费级 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个不同领域的任务，测试了开源和闭源两个不同的模型池。
*   **消融实验**：非常充分。包括：
    *   不同层级分类体系（领域 vs 子类 vs 难度）对采样效率的影响。
    *   样本数量（Shot number）从 1 到 30 的敏感度分析。
    *   任务正则化项的有效性验证。
    *   不同合成引擎（GPT vs Gemini）的鲁棒性测试。
*   **客观性**：通过数据泄露分析（检查合成数据与测试集的重合度）证明了实验的严谨性。

### 6. 主要结论与发现
1.  **合成数据有效性**：仅依靠领域描述生成的合成数据，训练出的路由器在冷启动场景下显著优于跨领域迁移的路由器。
2.  **TRouter 优越性**：在冷启动和领域内设置下，TRouter 均优于现有的回归和分类路由器，证明了引入“任务类型”作为中间变量的有效性。
3.  **样本效率**：TRouter 表现出极高的样本效率，通常每个任务类型只需 5-10 个样本即可达到近乎最优的路由效果。
4.  **难度层级的重要性**：在合成数据时，按“难度”划分画像比按“领域”划分能产生更高质量、更多样化的样本。

### 7. 优点
*   **首创性**：首次识别并系统性解决了 LLM 路由中的冷启动问题。
*   **可解释性**：TRouter 的层级分类体系为路由决策提供了透明的语义解释。
*   **实用性强**：合成数据成本极低（约 20 美元），远低于人工标注（估算需 6000 美元以上），极具商业落地价值。

### 8. 不足与局限
*   **初始依赖**：仍需要人工提供初始的领域描述（虽然这比标注数据容易得多）。
*   **评估偏置**：使用 LLM-as-a-Judge 可能引入模型偏置（Self-preference bias），尽管文中通过跨家族模型评估进行了缓解。
*   **合成引擎依赖**：合成数据的质量高度依赖于底层 LLM（如 GPT-4）的能力，若底层模型对某些冷门领域认知不足，合成效果可能受限。

（完）

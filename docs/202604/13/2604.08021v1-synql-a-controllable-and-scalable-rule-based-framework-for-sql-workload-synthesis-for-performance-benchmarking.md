---
title: "SynQL: A Controllable and Scalable Rule-Based Framework for SQL Workload Synthesis for Performance Benchmarking"
title_zh: SynQL：一种用于性能基准测试的可控且可扩展的基于规则的 SQL 工作负载合成框架
authors: "Kahan Mehta, Amit Mankodi"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08021v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 基于规则的SQL工作负载合成框架用于训练数据
tldr: 针对数据库研究中真实SQL负载获取难、现有合成工具（如LLM）存在幻觉及结构单一等问题，本文提出SynQL框架。它通过遍历数据库外键图构建抽象语法树（AST），确保生成的SQL在语法和模式上完全正确。SynQL支持通过参数控制连接拓扑、分析强度和谓词选择性。实验证明，SynQL能生成高度多样化的负载，并能有效训练代价模型，为缺乏生产日志时的性能基准测试提供了可靠的数据源。
selection_source: fresh_fetch
motivation: 隐私法规限制了真实SQL负载的获取，而现有合成方法存在模式幻觉或结构过于简单的问题，难以满足查询优化器的训练需求。
method: 采用基于规则的确定性生成方法，通过遍历实时数据库的外键图来填充抽象语法树，并利用配置向量实现对查询拓扑和复杂度的精确控制。
result: 在TPC-H和IMDb上的实验显示，SynQL生成的负载具有极高的拓扑熵，且基于该语料训练的代价模型在测试集上达到了R² ≥ 0.79的预测精度。
conclusion: SynQL是一个可控且可扩展的SQL合成框架，能够生成结构多样且执行有效的负载，是解决训练数据缺失问题的有效基础工具。
---

## 摘要
数据库研究和学习型查询优化器的开发严重依赖于真实的 SQL 工作负载。然而，由于严格的隐私法规，获取真实世界的查询变得越来越困难，且公开发布的匿名追踪数据通常会剔除可执行的查询文本以保护机密性。现有的合成工具未能弥补这一训练数据差距：传统基准测试提供的固定模板太少，难以进行统计泛化；而大语言模型（LLM）方法则面临模式幻觉（schema hallucination）问题（伪造不存在的列），以及拓扑崩溃（topological collapse）问题（系统性地默认使用简单的连接模式，无法对查询优化器进行压力测试）。我们提出了 SynQL，这是一个确定性的工作负载合成框架，能够生成结构多样且可直接执行的 SQL 工作负载。作为弥补训练数据差距的基础性步骤，SynQL 针对核心 SQL 片段——包含投影、聚合和范围谓词的多表连接——这在分析型工作负载中占据主导地位。SynQL 放弃了概率性文本生成，转而通过遍历实时数据库的外键图来填充抽象语法树（AST），从而在构造上保证了模式和语法的有效性。配置向量 $Θ$ 提供了对连接拓扑（星型、链型、叉型）、分析强度和谓词选择性的显式参数化控制。在 TPC-H 和 IMDb 上的实验表明，SynQL 能够生成接近最大多样性的工作负载（拓扑熵 $H = 1.53$ bits），并且在合成语料库上训练的基于树的代价模型在留出合成测试集上达到了 $R^2 \ge 0.79$，且推理延迟低于毫秒级。这确立了 SynQL 在无法获取生产日志时作为生成训练数据的有效基础。

## Abstract
Database research and the development of learned query optimisers rely heavily on realistic SQL workloads. Acquiring real-world queries is increasingly difficult, however, due to strict privacy regulations, and publicly released anonymised traces typically strip out executable query text to preserve confidentiality. Existing synthesis tools fail to bridge this training data gap: traditional benchmarks offer too few fixed templates for statistical generalisation, while Large Language Model (LLM) approaches suffer from schema hallucination fabricating non-existent columns and topological collapse systematically defaulting to simplistic join patterns that fail to stress-test query optimisers. We propose SynQL, a deterministic workload synthesis framework that generates structurally diverse, execution-ready SQL workloads. As a foundational step toward bridging the training-data gap, SynQL targets the core SQL fragment -- multi-table joins with projections, aggregations, and range predicates -- which dominates analytical workloads. SynQL abandons probabilistic text generation in favour of traversing the live database's foreign-key graph to populate an Abstract Syntax Tree (AST), guaranteeing schema and syntactic validity by construction. A configuration vector $Θ$ provides explicit, parametric control over join topology (Star, Chain, Fork), analytical intensity, and predicate selectivity. Experiments on TPC-H and IMDb show that SynQL produces near-maximally diverse workloads (Topological Entropy $H = 1.53$ bits) and that tree-based cost models trained on the synthetic corpus achieve $R^2 \ge 0.79$ on held-out synthetic test sets with sub-millisecond inference latency, establishing SynQL as an effective foundation for generating training data when production logs are inaccessible.

---

## 论文详细总结（自动生成）

这是一份关于论文《SynQL: A Controllable and Scalable Rule-Based Framework for SQL Workload Synthesis for Performance Benchmarking》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代数据库研究（尤其是学习型查询优化器）依赖大量真实的 SQL 工作负载，但获取这些数据面临三大障碍：
    *   **隐私限制**：法规禁止访问包含敏感信息的生产查询。
    *   **现有基准测试局限**：如 TPC-H 等传统基准模板固定且数量极少，容易导致模型过拟合。
    *   **LLM 生成缺陷**：大语言模型在生成 SQL 时常出现“模式幻觉”（虚构列名）和“拓扑崩溃”（倾向于生成简单的星型连接，无法覆盖复杂结构）。
*   **整体含义**：SynQL 旨在提供一个确定性的、基于规则的框架，通过遍历数据库外键图来合成结构多样、100% 语法正确且可执行的 SQL 工作负载，为缺乏生产日志的研究者提供高质量训练数据。

### 2. 方法论：核心思想与技术细节
SynQL 放弃了概率性的文本生成，采用两阶段的确定性构建流程：
*   **核心思想**：利用数据库实时元数据（Catalog）中的外键关系构建模式图，通过参数化控制的图遍历生成查询蓝图，再填充语义并编译为抽象语法树（AST）。
*   **关键技术细节**：
    *   **配置向量 $\Theta$**：包含五个关键参数：$\alpha_{shape}$（拓扑偏好）、$K_{join}$（最大连接深度）、$P_{agg}$（聚合概率）、$P_{where}$（谓词概率）和 $K_{pred}$（谓词数量限制）。
    *   **阶段 I：拓扑遍历（算法 1）**：从随机根表开始，根据 $\alpha_{shape}$ 权重选择外键边。$\alpha_{shape} \to 1$ 倾向于星型（Star），$\alpha_{shape} \to 0$ 倾向于链型（Chain），中间值产生叉型（Fork）。
    *   **阶段 II：语义注入与 AST 组装（算法 2）**：随机采样列，注入聚合函数（SUM/AVG），并根据数据库统计信息生成有效的范围谓词。最后通过 AST 强制执行 SQL 语法规则（如自动补全 GROUP BY 列表），确保生成的 SQL 绝对可执行。

### 3. 实验设计
*   **数据集/场景**：
    *   **TPC-H**：标准 8 表 OLAP 模式，用于验证拓扑多样性。
    *   **IMDb (JOB)**：包含 21 张表，具有真实世界的数据倾斜和复杂外键关系。
*   **Benchmark 与对比**：
    *   生成了 20,000 条查询（每个模式 10,000 条）。
    *   **对比目标**：评估合成负载在训练查询执行时间（QET）预测模型上的有效性。
    *   **评估指标**：拓扑熵（Shannon Entropy）、$R^2$、RMSE、MAE。

### 4. 资源与算力
*   **硬件环境**：Apple M1 工作站（8 核 CPU，16 GB 统一内存）。
*   **软件环境**：PostgreSQL 14（默认配置），Python 3.9。
*   **训练时长**：文中未明确给出 ML 模型的具体训练分钟数，但提到生成 20,000 条查询的语料库耗时**不到 10 分钟**。推理延迟在毫秒以下。

### 5. 实验数量与充分性
*   **实验规模**：
    *   展示了 4 个典型案例研究（Case Studies），涵盖不同拓扑和分析强度。
    *   使用了 20,000 条合成查询进行大规模验证。
    *   采用了 **5 折交叉验证（5-fold CV）** 来确保结果的统计稳健性。
*   **充分性与公平性**：实验设计较为充分，不仅验证了预测精度，还专门进行了**跨拓扑转移实验**（例如：用星型查询训练的模型去预测链型查询），证明了拓扑多样性对模型泛化能力的必要性。对比过程客观，直接指出了 LLM 在该领域的机械性缺陷。

### 6. 主要结论与发现
*   **高多样性**：SynQL 生成的工作负载拓扑熵达到 1.53 bits（接近理论最大值 1.58），远超固定模板基准。
*   **高预测精度**：在合成测试集上，基于树的代价模型（XGBoost, Random Forest）达到了 $R^2 \ge 0.79$（TPC-H 上甚至达到 0.99）。
*   **拓扑多样性的必要性**：实验发现，仅在单一拓扑（如星型）上训练的模型在面对其他拓扑（如链型）时表现极差（$R^2$ 甚至为负），而 SynQL 的混合拓扑负载能显著提升模型的鲁棒性。
*   **100% 有效性**：生成的查询完全符合模式和语法，消除了 LLM 常见的幻觉问题。

### 7. 优点
*   **确定性与可控性**：通过 $\alpha_{shape}$ 等参数精确控制查询形状，便于进行消融研究和压力测试。
*   **构造即正确**：基于 AST 的方法从根本上解决了语法错误和无效列引用的问题。
*   **可扩展性**：框架不依赖特定数据库引擎，生成的 SQL 具有良好的移植性。
*   **解决冷启动问题**：为没有生产日志的新系统提供了生成高质量训练数据的手段。

### 8. 不足与局限
*   **SQL 覆盖范围有限**：目前仅支持核心 SQL 片段（连接、投影、聚合、谓词），尚未支持关联子查询、CTE（公用表表达式）、UNION 或窗口函数。
*   **特征提取依赖性**：虽然 SQL 生成是通用的，但用于 QET 预测的特征提取层目前与 PostgreSQL 的 `EXPLAIN` 格式绑定。
*   **生产环境迁移验证**：实验主要在合成数据上进行闭环验证，尚未在真实的匿名生产追踪数据（如 Redset）上大规模验证其迁移学习效果。
*   **模式规模**：实验覆盖了中等规模模式（21 表），对于拥有数百张表的超大规模企业级模式，其遍历效率和覆盖率仍需进一步观察。

（完）

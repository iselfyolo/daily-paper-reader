---
title: Synthetic Data for any Differentiable Target
title_zh: 面向任意可微目标的合成数据
authors: "Tristan Thrush, Sung Min Park, Herman Brunborg, Luke Bailey, Marcel Roed, Neil Band, Christopher Potts, Tatsunori Hashimoto"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08423v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 优化合成数据生成器以产生目标样本数据集
tldr: 本研究探讨了利用合成数据控制语言模型的极限，提出了“数据集策略梯度（DPG）”方法。该方法通过高阶梯度计算数据归因分数，并将其作为强化学习的奖励，从而优化数据生成器。生成的合成数据在对目标模型进行监督微调后，能使其在特定的可微分指标上表现优异。实验证明，DPG不仅能改变模型权重的物理形态（如嵌入QR码），还能在不改变提示词的情况下引导模型生成特定内容，展示了极高的灵活性。
selection_source: fresh_fetch
motivation: 旨在探索如何通过优化合成训练数据来精确控制和塑造语言模型的特定属性或性能指标。
method: 提出数据集策略梯度（DPG），利用高阶梯度进行精确的数据归因，并将其作为奖励信号来训练合成数据生成器。
result: 实验成功实现了通过合成数据微调使模型权重嵌入QR码、降低权重范数，以及在无显式提示下完成特定语言改写和UUID生成。
conclusion: DPG证明了仅通过合成数据即可灵活且精准地操控模型属性，为模型对齐和定制化提供了强有力的新工具。
---

## 摘要
通过合成训练数据控制语言模型的极限在哪里？我们开发了一种强化学习（RL）原语，即数据集策略梯度（Dataset Policy Gradient, DPG），它可以精确地优化合成数据生成器，以产生包含特定目标的示例数据集。当这些示例用于目标模型的监督微调（SFT）时，会使目标模型在我们选择的可微指标上表现良好。我们的方法通过高阶梯度进行精确的数据归因，并将这些得分作为策略梯度的奖励来实现这一目标。我们证明了该过程能够紧密逼近合成数据生成器真实的、难以计算的梯度。为了展示 DPG 的潜力，我们证明了仅通过对生成的示例进行 SFT，就可以使目标模型的语言模型（LM）头权重（1）嵌入二维码，（2）嵌入模式 $\texttt{67}$，以及（3）具有更低的 $\ell^2$ 范数。我们还展示了可以使生成器（4）用一种新语言改写输入，以及（5）生成特定的 UUID，尽管这些目标都没有在生成器的输入提示词中传达。这些发现表明，DPG 是一种强大且灵活的技术，仅使用合成训练示例即可塑造模型属性。

## Abstract
What are the limits of controlling language models via synthetic training data? We develop a reinforcement learning (RL) primitive, the Dataset Policy Gradient (DPG), which can precisely optimize synthetic data generators to produce a dataset of targeted examples. When used for supervised fine-tuning (SFT) of a target model, these examples cause the target model to do well on a differentiable metric of our choice. Our approach achieves this by taking exact data attribution via higher-order gradients and using those scores as policy gradient rewards. We prove that this procedure closely approximates the true, intractable gradient for the synthetic data generator. To illustrate the potential of DPG, we show that, using only SFT on generated examples, we can cause the target model's LM head weights to (1) embed a QR code, (2) embed the pattern $\texttt{67}$, and (3) have lower $\ell^2$ norm. We additionally show that we can cause the generator to (4) rephrase inputs in a new language and (5) produce a specific UUID, even though neither of these objectives is conveyed in the generator's input prompts. These findings suggest that DPG is a powerful and flexible technique for shaping model properties using only synthetic training examples.

---

## 论文详细总结（自动生成）

以下是对论文《Synthetic Data for any Differentiable Target》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管合成数据在提升语言模型性能方面表现出色，但如何“精确控制”合成数据对下游模型的影响仍是一个难题。现有的合成数据生成往往依赖于启发式方法或简单的提示词工程。
*   **核心问题**：能否训练一个合成数据生成器，使其产生的训练数据能够诱导目标模型表现出“任意”指定的、可微分的属性（例如特定的权重模式、特定的基准测试表现或特定的输出行为）？
*   **整体含义**：本文提出了一种名为**数据集策略梯度（Dataset Policy Gradient, DPG）**的强化学习原语，证明了通过高阶梯度（元梯度）进行数据归因，可以实现对合成数据生成过程的极高精度控制。

### 2. 方法论
*   **核心思想**：将“合成数据对目标模型性能的影响”转化为强化学习的奖励信号。
*   **关键技术细节**：
    *   **双层优化结构**：外层循环优化生成器策略 $\pi_\theta$，内层循环模拟目标模型在合成数据上的训练过程。
    *   **元梯度归因**：利用 Engstrom 等人（2025）的方法，计算下游指标 $\Phi$ 对训练样本权重 $w$ 的梯度 $\nabla_w \Phi$。这个梯度反映了每个特定样本对最终目标的贡献度。
    *   **奖励机制**：将上述元梯度直接作为强化学习（GRPO 算法）中每个合成样本的奖励 $r(x)$。
    *   **理论证明**：作者证明了这种基于样本级元梯度的策略梯度，在一定平滑性假设下，能够紧密逼近直接优化整个数据集性能的、计算上不可行的真实梯度。
*   **算法流程**：生成器产生一批数据 $\rightarrow$ 目标模型在这些数据上进行 $T$ 步训练 $\rightarrow$ 计算目标模型在验证集或特定指标上的损失 $\rightarrow$ 通过反向传播穿过训练步骤，计算每个样本的奖励 $\rightarrow$ 使用 GRPO 更新生成器。

### 3. 实验设计
*   **场景与任务**：
    1.  **权重模式嵌入**：让目标模型（GPT-2）在微调后，其 LM Head 的权重矩阵在视觉上呈现出 **QR 码**或数字 **“67”** 的图案。
    2.  **权重范数控制**：优化数据以降低目标模型权重的 $\ell^2$ 范数。
    3.  **隐蔽翻译**：在不提示翻译的情况下，引导生成器将英文维基百科改写为德、西、法、意语，以降低目标模型在这些语言上的损失。
    4.  **特定字符串生成**：诱导生成器产生一个特定的 32 位 UUID。
*   **对比方法（Baselines）**：
    *   **Naive RL**：直接将下游指标作为奖励，不使用元梯度。
    *   **优化器对比**：在元梯度计算中使用 SGD vs. Adam。
    *   **启发式奖励**：如 Embedding 相似度、fasttext 语言分类得分、Levenshtein 编辑距离等。

### 4. 资源与算力
*   **模型使用**：生成器统一使用 **Llama 3.2 Instruct (1B)**；目标模型根据任务选择 **GPT-2** 或 **Llama 3.2 Instruct (1B)**。
*   **算力说明**：论文**未明确给出总的 GPU 小时数或硬件型号**。但提到了一些关键参数：如 QR 码实验运行了 200 个 GRPO 步数，每步包含 96 步目标模型训练，batch size 为 1024。这表明该方法虽然比朴素 RL 高效，但由于涉及高阶梯度计算，计算开销依然显著。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从底层的权重操纵到高层的语义理解共 5 类截然不同的任务。
*   **消融实验**：详细对比了内层训练步数（1 步、8 步、96 步）对结果的影响，以及不同优化器在元梯度计算中的表现。
*   **充分性评价**：实验设计非常大胆且具有创意（尤其是 QR 码实验），结果显示 DPG 在所有任务中均显著优于 Naive RL 和启发式基准。实验在不同语言和不同目标模型上进行了验证，具有较好的客观性和说服力。

### 6. 主要结论与发现
*   **DPG 的有效性**：DPG 能够以极高的精度引导生成器，甚至能实现像“在权重中画图”这样极端的目标。
*   **Adam 的重要性**：**关键发现**是在计算元梯度时，内层循环必须使用 **Adam** 优化器。使用 SGD 计算的元梯度在复杂任务中往往失效，这挑战了传统影响函数（Influence Functions）通常忽略优化器状态的假设。
*   **隐蔽性**：生成的合成数据在表面上看起来是正常的维基百科改写，但却能潜移默化地改变模型的下游行为。

### 7. 优点
*   **高精度控制**：提供了目前为止对合成数据生成最细粒度的控制手段。
*   **理论支撑**：不仅有实验，还提供了严谨的数学证明，确保了方法的收敛性和合理性。
*   **通用性**：只要目标函数是可微的，该框架理论上可以优化任何属性。

### 8. 不足与局限
*   **计算成本**：计算穿过多个训练步的高阶梯度对显存和计算资源要求较高，限制了其在大规模模型（如 70B+）上的直接应用。
*   **安全风险**：该技术可能被恶意利用，用于制造极其隐蔽的数据投毒攻击（例如在看似无害的文档中埋入改变模型特定偏见的“暗号”）。
*   **任务范围**：目前主要集中在监督微调（SFT）场景，对于更复杂的预训练阶段或非可微目标的扩展性仍有待探索。

（完）

---
title: Synthesizing real-world distributions from high-dimensional Gaussian Noise with Fully Connected Neural Network
title_zh: 利用全连接神经网络从高维高斯噪声中合成真实世界分布
authors: Joanna Komorniczak
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09091v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 基于神经网络的表格数据集合成数据生成方法
tldr: 本研究针对机器学习中合成数据生成效率低的问题，提出了一种基于全连接神经网络和随机损失函数的新方法。该方法通过将高维高斯噪声转换为目标真实数据集的分布，在25个多样化的表格数据集上进行了验证。结果表明，该方案在生成质量上超越了现有最先进方法，且在计算速度上提升了数个数量级，同时在数据增强、隐私保护和分类性能提升方面表现出色。
selection_source: fresh_fetch
motivation: 旨在解决现有深度学习生成模型在处理表格数据时计算成本高、训练时间长且难以兼顾数据质量与隐私的问题。
method: 采用全连接神经网络结合随机化损失函数，将随机高斯分布映射并逼近目标真实世界数据集的分布。
result: 在25个数据集上的实验证明，该方法在MMD指标和分类质量上优于SOTA模型，且运行速度快了几个数量级。
conclusion: 该方法为高效合成数据生成提供了新途径，通过结合PCA等技术进一步增强了数据隐私并降低了计算复杂度。
---

## 摘要
合成数据在机器学习应用和研究中具有诸多益处，包括通过数据增强提升性能、保护原始样本隐私，以及利用全合成数据进行可靠的方法评估。本研究提出了一种基于全连接神经网络和随机损失函数的高效合成数据生成方法，该方法通过变换随机高斯分布来逼近目标真实世界数据集。在 25 个不同的表格型真实世界数据集上进行的实验证实，所提方案优于现有的最先进生成方法，并且在达到参考 MMD 分数方面比现代深度学习方案快几个数量级。实验涉及分析分布相似性、评估对分类质量的影响，并使用 PCA 进行降维，这进一步增强了数据隐私，并能在降低时间和内存复杂度的同时提升分类质量。

## Abstract
The use of synthetic data in machine learning applications and research offers many benefits, including performance improvements through data augmentation, privacy preservation of original samples, and reliable method assessment with fully synthetic data. This work proposes a time-efficient synthetic data generation method based on a fully connected neural network and a randomized loss function that transforms a random Gaussian distribution to approximate a target real-world dataset. The experiments conducted on 25 diverse tabular real-world datasets confirm that the proposed solution surpasses the state-of-the-art generative methods and achieves reference MMD scores orders of magnitude faster than modern deep learning solutions. The experiments involved analyzing distributional similarity, assessing the impact on classification quality, and using PCA for dimensionality reduction, which further enhances data privacy and can boost classification quality while reducing time and memory complexity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Synthesizing real-world distributions from high-dimensional Gaussian Noise with Fully Connected Neural Network》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在机器学习中，合成数据对于**隐私保护**（防止原始敏感数据泄露）、**数据增强**（解决类别不平衡和过拟合）以及**可靠的模型评估**至关重要。然而，现有的深度学习生成模型（如 GANs 和 VAEs）在处理表格数据时存在以下痛点：
*   **计算成本高**：训练过程复杂且耗时。
*   **数据需求量大**：通常需要大规模训练集才能收敛。
*   **架构复杂**：如 GAN 的对抗训练不稳定，VAE 需要复杂的编码-解码结构。
本研究旨在提出一种**简单、高效且快速**的方法，利用基础的全连接神经网络（FCNN）将高维高斯噪声直接映射为目标真实分布。

### 2. 论文提出的方法论：DiMSO
论文提出了 **DiMSO (Distribution Mapping with Shuffled Optimization)**，其核心思想是学习一个映射函数，将随机高斯分布 $N(0, 1)$ 转换为目标数据集的分布。

*   **核心架构**：采用简单的全连接神经网络，包含 3 个隐藏层（每层 100 个神经元），使用 ReLU 激活函数。
*   **关键技术细节**：
    *   **高维输入**：输入噪声的维度通常高于目标数据的维度，以简化映射参数的优化。
    *   **按类建模**：为每个类别独立训练一个神经网络模型，最终合并生成平衡的合成数据集。
    *   **随机化损失函数（Randomized Loss Function）**：这是该方法的创新点。作者提出了 **RAE (Randomized Absolute Error)**，在每个优化步骤中随机选择合成样本与目标样本配对并计算绝对误差。这种随机配对机制不仅降低了计算复杂度，还通过“非理想重叠”增强了隐私保护。
    *   **其他损失函数支持**：除了 RAE，还支持基于 Wasserstein 距离（W）和特征协方差（WC）的损失函数。
*   **PCA 结合方案**：提出在 PCA 降维后的特征空间中进行合成，再通过逆变换还原，以进一步增强隐私并降低复杂度。

### 3. 实验设计
*   **数据集**：使用了来自 PMLB 库的 **25 个真实世界表格数据集**，涵盖二分类和多分类任务，特征数从 4 到 44 不等，样本量从 163 到 3772 不等。
*   **对比方法（Benchmark）**：
    *   传统过采样方法：SMOTE, SVM-SMOTE。
    *   统计生成方法：Gaussian Copula (GC)。
    *   深度学习 SOTA 方法：TVAE (Tabular VAE), CTGAN (Conditional Tabular GAN)。
*   **评估指标**：
    *   **分布相似性**：Wasserstein 距离 (WD)、最大均值差异 (MMD)、最近邻平均欧氏距离 (MeanNN)。
    *   **分类质量**：使用 5 种分类器（GNB, DT, RFC, SVC, MLP）在合成数据训练、真实数据测试下的平衡准确率 (BAC)。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **7 核 ARM 架构 CPU**（最高主频 3.2 GHz）的设备上运行。
*   **算力说明**：论文**未提及使用 GPU**，这强调了该方法对计算资源的需求极低。
*   **训练时长**：DiMSO 在达到与 TVAE 或 CTGAN 相同的 MMD 分数时，速度快了**数个数量级**。例如，在某些任务中，DiMSO 仅需 0.014 秒，而 CTGAN 需要约 20.9 秒。

### 5. 实验数量与充分性
*   **实验规模**：进行了 3 大类核心实验。通过 5 折分层交叉验证确保结果的稳定性。
*   **充分性**：涵盖了 25 个不同领域的数据集，并针对 5 种主流分类算法进行了 40 种“生成器-分类器”组合测试。
*   **客观性**：使用了非参数检验（Friedman test）和 Nemenyi 后置检验进行统计显著性分析，确保对比的公平性。实验不仅关注生成质量，还深入探讨了 PCA 预处理对隐私和性能的影响。

### 6. 主要结论与发现
*   **性能卓越**：DiMSO（尤其是 RAE 变体）在分布模拟指标（如 MMD）上优于或持平于 TVAE 和 CTGAN。
*   **分类提升**：在处理不平衡数据集时，使用 DiMSO 生成的平衡数据训练分类器，能显著提升 SVC 和 MLP 等模型的分类性能。
*   **极高效率**：DiMSO 的执行效率远超深度学习基准模型，且架构极其简单。
*   **隐私与降维**：在 PCA 提取的特征空间中进行生成，不仅能保持分类质量，还能通过消除特征语义进一步保护隐私。

### 7. 优点（亮点）
*   **简洁性**：摒弃了 GAN 的对抗架构和 VAE 的编码器，仅靠 FCNN 和随机损失函数实现高质量生成。
*   **时间效率**：计算开销极小，适合资源受限的环境或需要快速迭代的场景。
*   **灵活性**：通过更换损失函数（RAE, W, WC），可以根据具体需求（如侧重样本匹配或侧重特征相关性）调整生成策略。

### 8. 不足与局限
*   **特征类型限制**：目前主要针对数值型数据。虽然实验中包含了分类变量，但需先进行标准化处理，未专门针对分类变量的语义保持进行深度优化。
*   **超参数敏感性**：RAE 损失函数对训练轮数（Epochs）较为敏感，过多的迭代可能导致模型过度拟合原始样本，从而降低隐私保护效果。
*   **应用范围**：目前仅在表格数据上验证，尚未扩展到图像或文本等非结构化数据领域。

（完）

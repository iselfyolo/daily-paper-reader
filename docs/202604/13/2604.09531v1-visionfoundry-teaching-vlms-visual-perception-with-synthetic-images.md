---
title: "VisionFoundry: Teaching VLMs Visual Perception with Synthetic Images"
title_zh: VisionFoundry：利用合成图像提升视觉语言模型（VLM）的视觉感知能力
authors: "Guanyu Zhou, Yida Yin, Wenhao Chai, Shengbang Tong, Xingyu Fu, Zhuang Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09531v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 使用大语言模型和文生图模型构建任务感知合成数据生成流水线
tldr: 针对视觉语言模型（VLM）在空间理解和视角识别等底层视觉感知任务上的不足，本文提出 VisionFoundry 框架。该框架仅需任务关键词，利用大语言模型生成问答对和绘图提示词，通过文本生成图像模型合成数据，并由闭源 VLM 进行一致性验证。实验表明，在 VisionFoundry-10K 数据集上微调的模型在 MMVP 和 CV-Bench-3D 等基准测试中表现显著提升，证明了合成数据在增强 VLM 视觉感知能力方面的巨大潜力。
selection_source: fresh_fetch
motivation: 现有自然图像数据集缺乏对底层视觉技能的针对性监督，导致 VLM 在空间和视角感知任务上表现不佳。
method: 提出一种仅需任务名称即可自动生成合成图像、问答对并进行一致性验证的端到端流水线。
result: "在 VisionFoundry-10K 上训练的模型在 MMVP 和 CV-Bench-3D 基准上分别提升了 7% 和 10%，且具备良好的扩展性。"
conclusion: 针对性合成监督是解决 VLM 视觉感知瓶颈的有效途径，为系统化训练更强大的视觉模型提供了新方向。
---

## 摘要
视觉语言模型（VLM）在空间理解和视角识别等视觉感知任务上仍面临挑战。一个可能的原因是，自然图像数据集对底层视觉技能提供的监督信息有限。这引发了一个实际的问题：仅根据“深度顺序”等任务关键词生成的针对性合成监督，能否解决这些弱点？为了研究这一问题，我们推出了 VisionFoundry，这是一个任务感知的合成数据生成流水线。它仅以任务名称作为输入，利用大语言模型（LLM）生成问题、答案和文本生成图像（T2I）提示词，随后通过 T2I 模型合成图像，并使用闭源 VLM 验证一致性，无需参考图像或人工标注。利用 VisionFoundry，我们构建了 VisionFoundry-10K，这是一个包含 10,000 个图像-问题-答案三元组的合成视觉问答（VQA）数据集，涵盖 10 个任务。在 VisionFoundry-10K 上训练的模型在视觉感知基准测试中取得了显著提升：在 MMVP 上提升了 7%，在 CV-Bench-3D 上提升了 10%，同时保留了更广泛的能力，并随着数据规模的增加表现出良好的扩展特性。我们的结果表明，有限的任务针对性监督是导致这一瓶颈的重要因素，而合成监督是实现 VLM 更系统化训练的一条极具前景的路径。

## Abstract
Vision-language models (VLMs) still struggle with visual perception tasks such as spatial understanding and viewpoint recognition. One plausible contributing factor is that natural image datasets provide limited supervision for low-level visual skills. This motivates a practical question: can targeted synthetic supervision, generated from only a task keyword such as Depth Order, address these weaknesses? To investigate this question, we introduce VisionFoundry, a task-aware synthetic data generation pipeline that takes only the task name as input and uses large language models (LLMs) to generate questions, answers, and text-to-image (T2I) prompts, then synthesizes images with T2I models and verifies consistency with a proprietary VLM, requiring no reference images or human annotation. Using VisionFoundry, we construct VisionFoundry-10K, a synthetic visual question answering (VQA) dataset containing 10k image-question-answer triples spanning 10 tasks. Models trained on VisionFoundry-10K achieve substantial improvements on visual perception benchmarks: +7% on MMVP and +10% on CV-Bench-3D, while preserving broader capabilities and showing favorable scaling behavior as data size increases. Our results suggest that limited task-targeted supervision is an important contributor to this bottleneck and that synthetic supervision is a promising path toward more systematic training for VLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《VisionFoundry: Teaching VLMs Visual Perception with Synthetic Images》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管视觉语言模型（VLM）在通用推理上取得了巨大进步，但在底层视觉感知（如空间关系、深度排序、视角识别）方面仍存在明显的“感知瓶颈”。
*   **核心问题**：作者认为这一瓶颈源于自然图像数据集（如互联网抓取的数据）缺乏对这些细粒度视觉技能的系统性监督。论文探讨了是否可以仅通过“任务关键词”（如“深度顺序”）生成针对性的合成数据，来有效修补 VLM 的这些感知短板。

### 2. 方法论：VisionFoundry 流水线
VisionFoundry 是一个端到端的合成数据生成框架，其核心思想是**“任务驱动的闭环合成与验证”**，无需任何参考图像或人工标注。
*   **VQA 三元组生成**：利用 LLM（如 GPT-5.2）根据给定的任务配置（Task Config），生成包含物体、属性、场景和风格的实体池，进而产生问题、确定性答案以及极其详尽的文本生成图像（T2I）提示词。
*   **图像合成**：使用现代 T2I 模型（如 Gemini-2.5-Flash-Image）根据提示词合成高保真图像。
*   **对齐验证与过滤**：使用强力闭源 VLM（如 Gemini-3-Pro）作为裁判。它将问题和答案转化为陈述句，验证合成图像是否真实反映了该视觉事实。未通过验证的样本会被剔除或进行局部编辑修正。
*   **数据集产出**：通过该流水线构建了 **VisionFoundry-10K**，包含 10 个感知任务（如视角、空间关系、相对距离等），每个任务 1000 个样本。

### 3. 实验设计
*   **测试模型**：选取了三个代表性开源模型：Qwen2.5-VL-3B-Instruct、Llama-3.2-11B-Vision-Instruct 和 MiMo-VL-7B-SFT。
*   **基准测试 (Benchmarks)**：
    *   **视觉感知**：MMVP, CV-Bench (2D/3D), RealWorldQA。
    *   **通用推理**：MMMU, MMBench, MathVista, BLINK, MMStar。
    *   **特定应用**：OCRBench, ScreenSpot-Pro (GUI), 3DSRBench 等。
*   **对比设置**：
    *   **Baseline**：原始预训练/指令微调模型。
    *   **数据规模实验**：测试从 0.5k 到 10k 不同数据量的影响。
    *   **混合实验**：对比“纯自然数据”与“合成+自然混合数据”的效果。
    *   **消融实验**：验证“验证环节”的必要性，以及“合成图像”相对于“自然图像+合成问答”的优越性。

### 4. 资源与算力
*   **算力平台**：论文提到使用了普林斯顿大学计算机系的 Neuronic GPU 计算集群。
*   **训练细节**：所有模型均采用非 LoRA 的全参数或部分参数微调（Qwen 和 MiMo 更新全量，Llama 冻结 LLM 仅更新 ViT 和 Adapter）。
*   **超参数**：全局 Batch Size 为 128，仅训练 1 个 Epoch。
*   **未明确信息**：文中未具体列出使用的 GPU 型号（如 A100 或 H100）的具体数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨 3 个模型、13 个基准测试的全面评估，并针对数据规模、训练轮数、任务特异性等进行了多维度消融。
*   **充分性**：实验设计非常详尽。作者不仅展示了性能提升，还通过人工审计（Manual Audit）分析了验证器的准确率（Precision 达 99%），并对比了合成数据与自然数据的互补性。
*   **客观性**：使用了统一的开源评估框架 VLMEvalKit，并对所有实验结果进行了 4 次独立运行取平均值，确保了结果的可靠性和公平性。

### 6. 主要结论与发现
*   **显著提升感知力**：在 VisionFoundry-10K 上训练后，模型在 MMVP 上提升了约 7%，在 CV-Bench-3D 上提升了约 10%。
*   **良好的扩展性**：随着合成数据规模从 0.5k 增加到 10k，感知性能呈现清晰的上升趋势。
*   **合成数据的独特价值**：在同等数据量下，“合成图像+合成问答”的效果优于“自然图像+合成问答”，说明 T2I 模型生成的图像能更精准地对齐复杂的感知指令。
*   **通用能力保持**：在提升感知能力的同时，模型在通用推理和数学任务上的表现基本保持稳定，未出现明显的灾难性遗忘。

### 7. 优点
*   **低成本与高效率**：仅需任务名称即可自动生成高质量数据，完全摆脱了对人工标注和真实图像采集的依赖。
*   **精准打击弱点**：通过任务感知配置，可以针对性地生成自然界中稀缺的组合场景（如特定的空间排列）。
*   **质量控制严密**：引入强力 VLM 验证机制，极大地降低了合成数据中常见的“图文不符”噪声。

### 8. 不足与局限
*   **任务覆盖面**：目前主要集中在底层感知任务，尚未验证该方法在需要长链推理或复杂常识的视觉任务上的表现。
*   **验证器依赖**：流水线的质量高度依赖于作为裁判的闭源 VLM（如 Gemini-3-Pro），若裁判模型本身存在偏见或错误，会影响训练集质量。
*   **OCR 性能波动**：由于合成任务中未包含文字识别，模型在 OCRBench 等基准上出现了轻微下滑。
*   **幻觉风险**：尽管有验证机制，但 T2I 模型仍可能生成违反物理规律的图像，这可能给模型带来潜在的错误偏置。

（完）

---
title: "ActivityEditor: Learning to Synthesize Physically Valid Human Mobility"
title_zh: ActivityEditor：学习合成符合物理规律的人类移动性
authors: "Chenjie Yang, Yutian Jiang, Anqi Liang, Wei Qi, Chenyu Wu, Junbo Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05529v2"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于零样本轨迹生成的双LLM智能体框架
tldr: 针对城市应用中人类移动建模面临的数据稀缺问题，本文提出ActivityEditor框架。该框架采用双大语言模型（LLM）代理协作模式，通过意图代理生成社会语义一致的活动链，并由编辑代理基于强化学习和物理约束进行迭代修正，实现了无需历史数据的零样本跨区域轨迹合成，显著提升了生成轨迹的真实性与泛化能力。
selection_source: fresh_fetch
motivation: 现有的数据驱动方法高度依赖历史轨迹数据，限制了其在数据匮乏或受限地区的跨区域应用。
method: 提出一种双LLM代理框架，通过意图代理生成活动链，并利用强化学习训练的编辑代理根据物理约束进行迭代优化。
result: 实验表明ActivityEditor在零样本跨区域任务中表现优异，生成的轨迹具有极高的统计保真度和物理合理性。
conclusion: 该研究为解决数据稀缺场景下的移动模拟问题提供了一种具有高度泛化能力的鲁棒性解决方案。
---

## 摘要
人类移动性建模对于各种城市应用至关重要。然而，现有的数据驱动方法通常面临数据稀缺问题，限制了它们在历史轨迹不可用或受限地区的适用性。为了填补这一空白，我们提出了 ActivityEditor，这是一个专为零样本跨区域轨迹生成设计的新型双大语言模型（LLM）智能体框架。我们的框架将复杂的合成任务分解为两个协作阶段。具体而言，基于意图的智能体利用人口统计驱动的先验知识生成结构化的人类意图和粗粒度的活动链，以确保高层级的社会语义一致性。随后，编辑智能体通过强制执行人类移动规律的迭代修订来细化这些输出，从而获得移动轨迹。这种能力是通过基于真实世界物理约束的多重奖励强化学习获得的，使智能体能够内化移动规律并确保高保真轨迹的生成。大量实验表明，ActivityEditor 在跨不同城市环境迁移时实现了卓越的零样本性能。它保持了高统计保真度和物理有效性，为数据稀缺场景下的移动性模拟提供了一种鲁棒且具有高度泛化能力的解决方案。我们的代码可在以下网址获取：https://anonymous.4open.science/r/ActivityEditor-066B。

## Abstract
Human mobility modeling is indispensable for diverse urban applications. However, existing data-driven methods often suffer from data scarcity, limiting their applicability in regions where historical trajectories are unavailable or restricted. To bridge this gap, we propose \textbf{ActivityEditor}, a novel dual-LLM-agent framework designed for zero-shot cross-regional trajectory generation. Our framework decomposes the complex synthesis task into two collaborative stages. Specifically, an intention-based agent, which leverages demographic-driven priors to generate structured human intentions and coarse activity chains to ensure high-level socio-semantic coherence. These outputs are then refined by editor agent to obtain mobility trajectories through iteratively revisions that enforces human mobility law. This capability is acquired through reinforcement learning with multiple rewards grounded in real-world physical constraints, allowing the agent to internalize mobility regularities and ensure high-fidelity trajectory generation. Extensive experiments demonstrate that \textbf{ActivityEditor} achieves superior zero-shot performance when transferred across diverse urban contexts. It maintains high statistical fidelity and physical validity, providing a robust and highly generalizable solution for mobility simulation in data-scarce scenarios. Our code is available at: https://anonymous.4open.science/r/ActivityEditor-066B.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ActivityEditor** 的双 LLM 智能体框架，旨在解决在缺乏历史轨迹数据的情况下，如何生成既符合社会语义逻辑又遵循物理规律的人类移动轨迹。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的人类移动性建模高度依赖大规模、私有的历史轨迹数据。这导致了三个主要瓶颈：
    1.  **数据稀缺与隐私**：在隐私监管严格或新规划区域（冷启动场景），无法获取历史数据。
    2.  **泛化能力差**：现有模型难以在未见过的城市或区域进行零样本（Zero-shot）迁移。
    3.  **物理一致性缺失**：现有的 LLM 生成方法多为单次生成，容易产生时空冲突（如活动重叠、不符合常理的持续时间）。
*   **研究背景**：作者认为，虽然不同城市的空间布局不同，但“人口统计特征”与“行为意图”之间的映射关系是具有普适性的。

### 2. 方法论
ActivityEditor 采用“生成-再编辑”的协作模式，包含两个核心阶段：
*   **意图驱动智能体 (Intention-driven Agent)**：
    *   **核心思想**：利用 LLM 作为世界模型，将人口统计属性（年龄、职业、收入等）映射为人类意图链。
    *   **流程**：输入用户画像，通过推理生成一个粗略的活动草稿（Skeletal Schedule），确保高层级的社会语义一致性（例如：退休人员不需要通勤）。
*   **编辑智能体 (Editor Agent)**：
    *   **核心思想**：作为一个规则审计员，通过迭代修正解决草稿中的时空矛盾。
    *   **关键技术**：
        *   **动作空间**：定义了“增加、删除、偏移、替换、拆分”五种编辑操作。
        *   **GRPO 强化学习**：使用群组相对策略优化（Group Relative Policy Optimization）进行对齐训练。
        *   **奖励函数设计**：
            1.  **格式奖励 ($R_{fmt}$)**：确保输出符合指定的 JSON 和思考链格式。
            2.  **约束奖励 ($R_{con}$)**：检查物理可行性（24小时覆盖、时间连续性、时长合理性）。
            3.  **保真度奖励 ($R_{sim}$)**：通过 Jensen-Shannon 散度（JSD）衡量生成序列与真实统计分布的相似度。

### 3. 实验设计
*   **数据集**：使用 2017 年美国国家家庭旅行调查 (NHTS) 数据，涵盖加利福尼亚州、亚利桑那州、佐治亚州和俄克拉荷马州。
*   **Benchmark（基准方法）**：
    *   **传统深度学习**：DeepMove, LSTPM。
    *   **通用 LLM**：ChatGPT-5.2, Qwen3-8B, LLaMA3.1-8B。
    *   **特定移动性 LLM 框架**：CoPB, RAGHome。
*   **评估指标**：涵盖序列相似性（Acc, F1, BLEU）、时间对齐（JSD 散度）和分布一致性（活动类型分布）等 12 项指标。

### 4. 资源与算力
*   **模型配置**：基础模型为 Qwen3-8B，使用 LoRA 技术进行微调（$r=32, \alpha=64$）。
*   **训练细节**：
    *   **SFT 阶段**：学习率 $5 \times 10^{-5}$，训练 3 个 epoch。
    *   **GRPO 阶段**：学习率 $1 \times 10^{-5}$，训练 1000 步，每个 Prompt 进行 8 次采样（Roll-out）。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长，但根据 8B 参数模型及 GRPO 的采样需求，推测需要多块 A100 或同级别显卡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同规模和特征的州进行了独立测试。
    *   进行了**跨州迁移实验**（如加州训练，在其他三州测试），验证零样本泛化能力。
    *   **消融实验**：对比了单智能体 vs 双智能体、SFT vs GRPO 的性能差异。
*   **充分性评价**：实验设计较为充分，通过跨区域迁移验证了模型的核心主张（即学习的是通用移动逻辑而非局部地理模式）。对比方法涵盖了从传统模型到最新 LLM 框架，评估维度全面。

### 6. 主要结论与发现
*   **零样本能力强**：ActivityEditor 在完全未见过的目标区域表现显著优于所有基准模型，证明了“意图-编辑”框架的有效性。
*   **物理规律内化**：通过 GRPO 训练，编辑智能体能够自主识别并修正时空冲突，显著降低了 LLM 的幻觉。
*   **架构优势**：多智能体协作模式优于单次生成模式，因为移动性建模本质上是一个受限优化问题，迭代修正更符合此类任务特征。

### 7. 优点
*   **创新架构**：将复杂的轨迹合成分解为“语义生成”与“物理修正”，模拟了人类“计划-调整”的认知过程。
*   **无需目标数据**：真正实现了零样本跨区域合成，解决了冷启动和隐私数据获取难的问题。
*   **强化学习对齐**：巧妙地将物理定律转化为奖励函数，使模型具备了自我审计和纠错能力。

### 8. 不足与局限
*   **空间粒度受限**：目前仅能生成区域级（Zone-level）的活动链，无法精确到具体的经纬度坐标或 POI（兴趣点）。
*   **计算开销**：双智能体迭代推理和 GRPO 采样过程计算成本较高，可能限制大规模人口模拟的效率。
*   **幻觉风险**：尽管有纠错机制，但作为基于 LLM 的方法，仍存在生成不存在的地址或极端异常行为的潜在风险。
*   **应用限制**：目前主要针对 24 小时内的活动链，对于更长周期的移动性建模（如周或月）尚未验证。

（完）

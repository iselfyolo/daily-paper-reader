---
title: "ActivityEditor: Learning to Synthesize Physically Valid Human Mobility"
title_zh: ActivityEditor：学习合成符合物理规律的人类移动性
authors: "Chenjie Yang, Yutian Jiang, Anqi Liang, Wei Qi, Chenyu Wu, Junbo Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05529v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于零样本轨迹生成的双LLM智能体框架
tldr: 本研究提出ActivityEditor，一个用于零样本跨区域轨迹生成的双大语言模型（LLM）代理框架，旨在解决城市应用中的数据稀缺问题。该框架通过意图代理生成具有社会语义一致性的活动链，并利用编辑代理通过强化学习结合物理约束对轨迹进行迭代优化。实验证明，该方法在缺乏历史数据的地区能生成高保真且符合物理规律的人类移动轨迹，具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的数据驱动方法受限于数据稀缺，难以在缺乏历史轨迹的区域实现跨区域的人类移动建模。
method: 采用双代理架构，由意图代理生成粗略活动链，再由编辑代理通过基于物理约束的强化学习进行迭代修正。
result: 在多种城市背景下的实验表明，该框架在零样本迁移中表现优异，生成的轨迹具有极高的统计保真度和物理有效性。
conclusion: ActivityEditor为数据匮乏场景下的移动性模拟提供了一种鲁棒且泛化性强的解决方案。
---

## 摘要
人类移动性建模对于各种城市应用至关重要。然而，现有的数据驱动方法往往面临数据稀缺的问题，限制了它们在历史轨迹不可用或受限地区的适用性。为了填补这一空白，我们提出了 ActivityEditor，这是一个专为零样本跨区域轨迹生成设计的新型双大语言模型（LLM）智能体框架。我们的框架将复杂的合成任务分解为两个协作阶段。具体而言，基于意图的智能体利用人口统计驱动的先验知识生成结构化的人类意图和粗粒度的活动链，以确保高层级的社会语义一致性。随后，编辑智能体通过强制执行人类移动规律的迭代修订来细化这些输出，从而获得移动轨迹。这种能力是通过基于现实世界物理约束的多重奖励强化学习获得的，使智能体能够内化移动规律并确保高保真轨迹的生成。大量实验表明，ActivityEditor 在跨不同城市环境迁移时实现了卓越的零样本性能。它保持了高统计保真度和物理有效性，为数据稀缺场景下的移动性模拟提供了一种鲁棒且具有高度泛化能力的解决方案。我们的代码可在以下网址获取：https://anonymous.4open.science/r/ActivityEditor-066B。

## Abstract
Human mobility modeling is indispensable for diverse urban applications. However, existing data-driven methods often suffer from data scarcity, limiting their applicability in regions where historical trajectories are unavailable or restricted. To bridge this gap, we propose \textbf{ActivityEditor}, a novel dual-LLM-agent framework designed for zero-shot cross-regional trajectory generation. Our framework decomposes the complex synthesis task into two collaborative stages. Specifically, an intention-based agent, which leverages demographic-driven priors to generate structured human intentions and coarse activity chains to ensure high-level socio-semantic coherence. These outputs are then refined by editor agent to obtain mobility trajectories through iteratively revisions that enforces human mobility law. This capability is acquired through reinforcement learning with multiple rewards grounded in real-world physical constraints, allowing the agent to internalize mobility regularities and ensure high-fidelity trajectory generation. Extensive experiments demonstrate that \textbf{ActivityEditor} achieves superior zero-shot performance when transferred across diverse urban contexts. It maintains high statistical fidelity and physical validity, providing a robust and highly generalizable solution for mobility simulation in data-scarce scenarios. Our code is available at: https://anonymous.4open.science/r/ActivityEditor-066B.

---

## 论文详细总结（自动生成）

以下是对论文《ActivityEditor: Learning to Synthesize Physically Valid Human Mobility》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对城市应用中**人类移动性建模的数据稀缺与隐私限制**问题展开研究。
*   **研究动机**：现有的深度学习模型（如RNN、GAN）高度依赖大规模私有轨迹数据，在缺乏历史记录的新规划区域（“冷启动”场景）或跨区域迁移时表现较差。
*   **核心挑战**：大语言模型（LLM）虽具备常识推理能力，但在生成轨迹时常出现时空不一致、违反物理规律（如时间重叠、活动不连续）以及对局部数据分布敏感等问题。
*   **整体含义**：提出一种名为 **ActivityEditor** 的双智能体框架，旨在实现**零样本（Zero-shot）**跨区域轨迹合成，即仅凭人口统计学特征（如年龄、职业）即可生成符合物理规律且具有统计保真度的人类移动轨迹。

### 2. 方法论：核心思想与技术细节
ActivityEditor 采用“**先生成、后编辑**”的迭代优化思路，将任务分解为两个阶段：
*   **意图驱动智能体（Intention-driven Agent）**：
    *   利用 LLM 的世界模型知识，将人口统计属性（Socio-demographic Attributes）映射为结构化的人类意图。
    *   生成一个粗略的活动草案（Activity Draft），确保高层级的社会语义一致性（例如：退休人员不应有通勤意图）。
*   **编辑智能体（Editor Agent）**：
    *   **核心思想**：作为规则审计员，通过“增加、删除、偏移、替换、拆分”五种操作修正草案中的时空冲突。
    *   **关键技术 - GRPO 强化学习**：使用群体相对策略优化（Group Relative Policy Optimization）对编辑智能体进行对齐训练。
    *   **奖励模型（Reward Model）**：
        1.  **格式奖励 ($R_{fmt}$)**：确保输出符合 [THOUGHT] 和 [JSON] 结构。
        2.  **约束奖励 ($R_{con}$)**：强制执行硬约束（24小时覆盖、时间连续性）和软约束（生理时长合理性）。
        3.  **保真度奖励 ($R_{sim}$)**：通过 Jensen-Shannon 散度（JSD）衡量生成轨迹与真实统计分布的相似性。

### 3. 实验设计
*   **数据集**：使用 2017 年美国全国住户旅行调查（NHTS），选取加利福尼亚州（CA）、亚利桑那州（AZ）、佐治亚州（GA）和俄克拉荷马州（OK）四个数据量差异巨大的州。
*   **Benchmark（基准方法）**：
    *   **传统深度学习**：DeepMove, LSTPM。
    *   **特定 LLM 框架**：CoPB（基于计划行为理论）, RAGHome（检索增强生成）。
    *   **通用 LLM**：ChatGPT-5.2, Qwen3-8B, LLaMA3.1-8B（含单智能体与多智能体模式）。
*   **评估指标**：涵盖序列相似性（Acc, F1, Edit Dist）、时间对齐（JSD 分布）和分布一致性等 12 项指标。

### 4. 资源与算力
*   **基础模型**：使用 **Qwen3-8B** 作为学生模型，**GPT-5.2** 作为教师模型生成推理轨迹数据。
*   **微调技术**：采用 **LoRA**（Rank=32, Alpha=64）进行高效参数微调。
*   **训练细节**：
    *   SFT 阶段：3 个 Epoch，学习率 5e-5。
    *   GRPO 阶段：1000 个 Step，学习率 1e-5，每个 Prompt 采样 8 个 Roll-out 样本。
*   **算力说明**：文中未明确指出具体的 GPU 型号和数量，但根据 8B 模型规模和 GRPO 采样需求，推测使用了多块 A100 或同级别 GPU。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同特征的州进行了广泛测试，并执行了跨州迁移实验（如 CA 训练，AZ/GA/OK 测试）。
*   **充分性**：
    *   **消融实验**：对比了单智能体 vs 多智能体、SFT vs GRPO 的效果，证明了迭代编辑和强化学习的必要性。
    *   **客观性**：通过跨区域零样本测试验证了模型的泛化能力，而非仅仅死记硬背局部地理模式。
    *   **案例研究**：提供了具体的推理轨迹编辑案例，直观展示了模型修正逻辑错误的过程。

### 6. 主要结论与发现
*   **性能领先**：ActivityEditor 在所有测试区域的性能均显著优于现有 SOTA 方法，尤其在时间对齐和物理约束方面提升巨大。
*   **架构优势**：多智能体协作模式比单次生成模式更稳健，能有效减少 LLM 的幻觉。
*   **规律内化**：通过 GRPO 训练，模型能够自主内化人类移动的物理规律（如活动不可重叠），无需在推理时依赖外部硬编码规则。
*   **零样本能力**：模型成功学习到了通用的移动逻辑，在完全未见过的城市也能生成高质量轨迹。

### 7. 优点
*   **创新性**：将移动性建模视为“生成-编辑”任务，引入 GRPO 强化学习解决物理一致性问题。
*   **实用性**：解决了冷启动问题，无需目标区域的私有轨迹数据即可进行高质量模拟。
*   **可解释性**：通过 Chain-of-Thought（CoT）展示了智能体对轨迹冲突的识别与修正过程。

### 8. 不足与局限
*   **空间粒度**：目前仅支持区域级（Zone-level）生成，缺乏精确的 POI 坐标级合成能力。
*   **计算开销**：多轮迭代推理和强化学习采样导致计算成本较高，难以支持超大规模人口的实时模拟。
*   **鲁棒性风险**：尽管有约束，LLM 仍可能产生“幻觉地址”或解析失败，在开放世界应用中需进一步增强鲁棒性。

（完）

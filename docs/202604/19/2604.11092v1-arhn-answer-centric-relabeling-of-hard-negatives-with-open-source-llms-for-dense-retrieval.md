---
title: "ARHN: Answer-Centric Relabeling of Hard Negatives with Open-Source LLMs for Dense Retrieval"
title_zh: ARHN：利用开源大语言模型进行以答案为中心的稠密检索难负样本重标注
authors: "Hyewon Choi, Jooyoung Choi, Hansol Jang, Hyun Kim, Chulmin Yun, ChangWook Jun, Stanley Jungkyu Choi"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11092v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 使用大模型对检索器训练数据进行重新标注和优化的框架
tldr: 针对神经检索模型训练中硬负采样引入的假负例和歧义负例问题，本文提出ARHN框架。该框架利用开源大语言模型，通过生成答案片段和列表排序对负样本进行重标注与过滤。在BEIR基准上的实验证明，ARHN能有效净化监督信号并提升检索性能，为大规模检索模型训练提供了一种低成本且高效的样本精炼方案。
selection_source: fresh_fetch
motivation: 传统的硬负采样容易引入包含正确答案或相关信息的假负例，导致训练监督信号不一致并损害检索效果。
method: 采用两阶段框架，利用开源LLM提取段落答案片段并进行列表排序，从而重标注假负例并过滤歧义负例。
result: 在BEIR基准测试中，ARHN的重标注与过滤组合策略在多个数据集上均显著优于单一策略和原始基线。
conclusion: ARHN证明了利用开源LLM进行以答案为中心的样本精炼是提升神经检索模型性能的一种低成本且可扩展的有效途径。
---

## 摘要
神经检索器通常在由查询、正样本段落和一组难负样本组成的大规模三元组数据上进行训练。在实践中，难负样本挖掘可能会引入假负样本和其他模糊负样本，包括与查询相关或包含部分答案的段落。这种标签噪声会导致监督信号不一致，并可能降低检索效果。我们提出了 ARHN（以答案为中心的难负样本重标注），这是一个利用开源大语言模型（LLMs）并根据以答案为中心的相关性信号来优化难负样本的两阶段框架。在第一阶段，针对每个查询-段落对，ARHN 提示 LLM 生成基于段落的答案片段，或者指出该段落不支持回答。在第二阶段，ARHN 对候选集应用基于 LLM 的列表排序（listwise ranking），根据对查询的直接可回答性对段落进行排序。排名高于原始正样本的段落被重新标注为额外的正样本。在排名低于正样本的段落中，ARHN 从负样本集中剔除任何包含答案片段的段落，以避免模糊的监督信号。我们在 BEIR 基准测试上评估了 ARHN 的三种配置：仅重标注、仅过滤以及两者的结合。在各个数据集上，结合策略的效果始终优于任何单一步骤，这表明同时重标注假负样本和过滤模糊负样本能为训练神经检索模型提供更清晰的监督信号。通过严格依赖开源模型，ARHN 建立了一个具有成本效益且可扩展的优化流水线，适用于大规模训练。

## Abstract
Neural retrievers are often trained on large-scale triplet data comprising a query, a positive passage, and a set of hard negatives. In practice, hard-negative mining can introduce false negatives and other ambiguous negatives, including passages that are relevant or contain partial answers to the query. Such label noise yields inconsistent supervision and can degrade retrieval effectiveness.   We propose ARHN (Answer-centric Relabeling of Hard Negatives), a two-stage framework that leverages open-source LLMs to refine hard negative samples using answer-centric relevance signals. In the first stage, for each query-passage pair, ARHN prompts the LLM to generate a passage-grounded answer snippet or to indicate that the passage does not support an answer. In the second stage, ARHN applies an LLM-based listwise ranking over the candidate set to order passages by direct answerability to the query. Passages ranked above the original positive are relabeled to additional positives. Among passages ranked below the positive, ARHN excludes any that contain an answer snippet from the negative set to avoid ambiguous supervision.   We evaluated ARHN on the BEIR benchmark under three configurations: relabeling only, filtering only, and their combination. Across datasets, the combined strategy consistently improves over either step in isolation, indicating that jointly relabeling false negatives and filtering ambiguous negatives yields cleaner supervision for training neural retrieval models. By relying strictly on open-source models, ARHN establishes a cost-effective and scalable refinement pipeline suitable for large-scale training.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ARHN**（Answer-Centric Relabeling of Hard Negatives），旨在通过开源大语言模型（LLM）优化稠密检索模型的训练数据质量。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
稠密检索模型（Dense Retriever）通常采用对比学习训练，依赖“查询-正样本-难负样本”三元组。然而，现有的难负样本挖掘技术（如基于 BM25 或模型得分采样）经常会引入**假负样本（False Negatives）**——即那些实际上包含答案或与查询高度相关，却被错误标注为负样本的文档。
这种标签噪声会导致监督信号冲突，扭曲向量空间的几何结构，从而降低检索性能。论文的核心动机是利用 LLM 的语义理解能力，通过“以答案为中心”的逻辑，自动纠正和精炼这些训练数据。

### 2. 方法论：核心思想与关键流程
ARHN 采用两阶段框架，利用开源 LLM 对挖掘出的难负样本进行重新评估：

*   **第一阶段：答案片段生成（Answer Snippet Generation）**
    *   对于每个查询-文档对，提示 LLM 从文档中**原样提取**（Verbatim）一段能回答查询的片段。
    *   如果文档不包含答案，LLM 必须输出 `NO_ANSWER`。
    *   *关键点*：要求提取而非生成，是为了减少幻觉，并强制模型寻找证据。
*   **第二阶段：以答案为中心的列表排序与重标注（Reranking & Relabeling）**
    *   将第一阶段提取的所有片段（包括原始正样本的片段）汇总，让 LLM 进行**列表排序（Listwise Ranking）**，依据是片段回答查询的直接程度和完整性。
    *   **重标注逻辑**：
        1.  **正向重标注（Relabeling）**：如果某个负样本的片段排名高于原始正样本，则将其提升为正样本。
        2.  **过滤（Filtering）**：如果负样本片段排名低于正样本但并非 `NO_ANSWER`，则视为“歧义样本”并从负样本集中剔除。
        3.  **保留（Retention）**：只有被判定为 `NO_ANSWER` 的文档才作为真正的难负样本保留。

### 3. 实验设计
*   **数据集**：使用 BGE 训练集中的 7 个核心数据集（如 MS MARCO, NQ, HotpotQA 等）进行精炼，并在 **BEIR 评估基准**（包含 16 个测试集）上验证零样本（Zero-shot）检索性能。
*   **检索模型（Backbones）**：
    *   **E5-base**（基于 BERT，110M 参数）。
    *   **LG-ANNA-Embedding**（基于 Mistral-7B 的大型嵌入模型）。
*   **对比方法**：
    *   **No Refinement**：原始数据训练。
    *   **TopK-PercPos**：NV-Retriever 使用的传统采样过滤方法。
    *   **RLHN**：使用 GPT-4o-mini/GPT-4o 进行重标注的现有方法。
*   **消融实验**：分别测试了“仅过滤”、“仅重标注”以及“两者结合（R+F）”的效果。

### 4. 资源与算力
*   **算力投入**：论文明确提到使用 **16 张 NVIDIA A100 GPU** 进行 LLM 推理（用于第一、二阶段的标注）。
*   **模型微调**：E5-base 和 Mistral-7B 模型的微调同样在 A100 GPU 上完成。
*   **LLM 规模**：实验对比了 Qwen3-8B、14B 和 32B 三种规模的模型。

### 5. 实验数量与充分性
*   **实验规模**：在 16 个 BEIR 数据集上进行了广泛测试，涵盖了领域内和领域外（OOD）场景。
*   **充分性**：
    *   对比了不同规模的开源 LLM 对最终检索效果的影响。
    *   进行了**人工评估**，计算了 LLM 标注与人类判断的一致性（Cohen’s Kappa）。
    *   对比了“以段落为中心（PRHN）”与“以答案为中心（ARHN）”的差异。
*   **客观性**：实验涵盖了不同架构的检索器（BERT 类和 LLM 类），证明了方法的普适性。

### 6. 主要结论与发现
*   **组合策略最优**：同时进行重标注和过滤（R+F）的效果显著优于单一策略，在 E5-base 上比原始基线提升了 1.3 个 nDCG@10 点。
*   **开源模型可替代闭源**：使用 Qwen3-32B 的 ARHN 效果优于使用 GPT-4 接口的 RLHN，证明了开源流水线的成本效益和高性能。
*   **规模效应**：LLM 的规模至关重要。32B 模型表现最好，而 8B 模型由于标注错误较多，甚至可能损害检索性能。
*   **OOD 泛化能力**：ARHN 在未见过的领域（OOD）表现出更强的提升，说明净化标签噪声对提高模型鲁棒性至关重要。

### 7. 优点
*   **粒度更细**：通过提取“答案片段”而非直接处理整段文字，减少了背景噪声对 LLM 评分的干扰。
*   **成本低且可扩展**：完全依赖开源模型，避免了昂贵的商业 API 调用，适合大规模工业级应用。
*   **逻辑严密**：将负样本细分为“假负例”、“歧义例”和“真负例”，提供了更精细的监督信号。

### 8. 不足与局限
*   **推理开销**：虽然使用了开源模型，但对大规模数据集进行两阶段 LLM 推理仍需要显著的计算资源和时间。
*   **模型依赖性**：实验显示较小的 LLM（如 8B）会引入新噪声，这意味着该方法对所选 LLM 的基础能力有较高门槛。
*   **最大长度限制**：由于检索器有最大序列长度限制，如果答案片段出现在文档末尾（被截断处），该方法的效果可能会打折扣。

（完）

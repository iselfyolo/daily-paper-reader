---
title: "REGREACT: Self-Correcting Multi-Agent Pipelines for Structured Regulatory Information Extraction"
title_zh: REGREACT：用于结构化监管信息提取的自纠错多智能体流水线
authors: "Mohammed Ali, Abdelrahman Abdallah, Adam Jatowt"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12054v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于结构化监管信息提取的自动推理
tldr: 针对监管文档提取中存在的幻觉、层级丢失及跨文档依赖难题，本文提出 RegReAct 框架。该框架采用七阶段多智能体流水线，通过“观察-诊断-修复”循环验证输出，并构建类型化准则图以确保结构准确。通过解析外部引用并整合法律内容，RegReAct 在欧盟分类法数据集上表现优异，显著提升了复杂合规标准的自动化提取质量。
selection_source: fresh_fetch
motivation: 现有的单次生成模型在处理具有复杂层级和外部引用的监管文档时，难以保证提取信息的结构完整性和准确性。
method: 引入包含七个专业阶段的多智能体框架，利用自校正循环和外部依赖解析技术构建自包含的结构化准则图。
result: 实验表明，RegReAct 在处理欧盟分类法相关的 242 项活动和 4800 多个准则时，其结构和语义指标均超越了 GPT-4o 基准。
conclusion: 多智能体协作与自校正机制能有效解决复杂法律文本提取中的幻觉和引用解析问题，为自动化合规审查提供了可靠方案。
---

## 摘要
从监管文件中提取结构化、机器可读的合规标准仍然是一个开放性挑战。单次推理语言模型会产生结构元素幻觉，丢失层级关系，且无法解决文档间的依赖关系。我们引入了 REGREACT，这是一个自纠错多智能体框架，它将监管信息提取分解为七个专业阶段，每个阶段都包含一个“观察-诊断-修复”（ODR）循环，根据源文件验证输出，不仅纠正模型幻觉，还纠正监管文件本身的交叉引用错误。为了确保结构准确性，REGREACT 构建了一个类型化标准图；为了确保完整性，它通过检索、总结并内联嵌入引用的法律内容来解决外部依赖，从而生成自包含的输出。通过将 REGREACT 应用于三项欧盟分类法授权法案，我们构建了一个包含 242 项活动、超过 4,800 条层级标准、阈值和丰富源摘要的数据集。与 GPT-4o 单次推理基准相比，评估结果证实 REGREACT 在所有结构和语义指标上均优于前者。代码和数据将公开：https://github.com/RECOR-Benchmark/RECOR

## Abstract
Extracting structured, machine-readable compliance criteria from regulatory documents remains an open challenge. Single-pass language models hallucinate structural elements, lose hierarchical relationships, and fail to resolve inter-document dependencies. We introduce \textsc{RegReAct}, a self-correcting multi-agent framework that decomposes regulatory information extraction into seven specialized stages, each with an \textit{Observe--Diagnose--Repair} (ODR) loop that validates outputs against the source, correcting not only model hallucinations but also cross-reference errors in the regulations themselves. To ensure structural accuracy, \textsc{RegReAct} constructs a typed criterion graph; to ensure completeness, it resolves external dependencies by retrieving, summarizing, and embedding referenced legal content inline, producing self-contained outputs. Applying \textsc{RegReAct} to three EU Taxonomy Delegated Acts, we construct a dataset comprising 242 activities with over 4,800 hierarchical criteria, thresholds, and enriched source summaries. Evaluation against a GPT-4o single-pass baseline confirms that \textsc{RegReAct} outperforms it across all structural and semantic metrics. Code and data will be made publicly available: https://github.com/RECOR-Benchmark/RECOR

---

## 论文详细总结（自动生成）

### REGREACT：用于结构化监管信息提取的自纠错多智能体流水线

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对从复杂监管文档（如《欧盟分类法》）中提取机器可读、结构化合规标准的难题展开研究。传统的单次大语言模型（LLM）提取方法面临四大局限：
*   **结构性幻觉**：难以在数十个准则中维持深层嵌套的层级关系。
*   **跨阶段不一致**：早期错误会传播并放大。
*   **结构违规**：无法保证全局属性（如无环继承）。
*   **引用不完整**：监管文本频繁引用外部法律、内部章节和脚注，导致输出结果不完整，仍需人工查阅其他文档。

#### 2. 方法论：核心思想与技术细节
REGREACT 提出了一个包含七个专业阶段的多智能体框架，核心思想是通过“分解-验证-整合”来确保提取的精确性：
*   **七阶段流水线**：
    1.  **结构解析**：解析 HTML 并恢复逻辑层级，处理无编号段落。
    2.  **阈值提取**：提取并归一化定量限值和时间期限。
    3.  **分类与逻辑推理**：推断准则类型（定量/定性）及逻辑关系（AND/OR/N_OF_K）。
    4.  **引用提取**：识别外部法律和标准，并将其归一化为 CELEX 标识符。
    5.  **依赖解析**：识别准则间的条件触发关系，并纠正法规原文中的交叉引用错误。
    6.  **脚注处理**：分解并链接隐藏在脚注中的关键合规细节。
    7.  **模式组装**：将所有组件组装成经过验证的 JSON 模式。
*   **ODR 自纠错循环（Observe-Diagnose-Repair）**：每个阶段都包含一个循环，通过将输出与源文档证据进行对比，观察差异、诊断根因并执行修复。
*   **类型化准则图（Typed Criterion Graph）**：编码 6 种关系类型（层级、组成、阈值继承等），通过图算法强制执行结构约束。
*   **准则条件 RAG**：针对每个准则独立检索外部法律内容，生成针对性的摘要并内联嵌入，使输出实现“自包含”。

#### 3. 实验设计
*   **数据集**：构建了 **EU-TAXO STRUCT** 数据集，涵盖 242 项经济活动，包含 4,800 多个层级准则。
*   **金标准（Gold Standard）**：从 242 项活动中分层抽样 100 项，由两名博士生进行手动标注，Cohen’s κ 一致性系数达 0.84–0.91。
*   **基准测试（Benchmark）**：对比了 **GPT-4o 单次生成（Single-pass）** 基准。
*   **评估指标**：
    *   **结构与分类**：结构 F1 分数、分类准确率、逻辑推理准确率。
    *   **语义等效性**：使用 GPT-4o 作为裁判（LLM Judge），在 1-5 分制下评估阈值、引用、脚注和依赖关系的准确性。
    *   **RAG 质量**：评估忠实度、相关性、完整性和覆盖范围。

#### 4. 资源与算力
*   **模型使用**：框架核心使用了 **DeepSeek-R1-Distill-Qwen-32B**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练/推理时长。但考虑到其多阶段、多循环的 Agent 架构，其推理成本显著高于单次调用模型。

#### 5. 实验数量与充分性
*   **实验规模**：对 100 个复杂活动进行了详尽的人工评估，并对 709 个“准则-源”对进行了 RAG 质量评估。
*   **消融实验**：进行了消融研究，分别验证了 ODR 循环（-ODR）和图验证机制（-Graph）的贡献。
*   **客观性与公平性**：实验设计较为客观。通过分层抽样覆盖了不同复杂度的活动；使用了双人标注及一致性校验；对比基准 GPT-4o 获得了与 REGREACT 相同的 Prompt 和 Schema 约束。

#### 6. 主要结论与发现
*   **性能领先**：REGREACT 在所有指标上均显著优于 GPT-4o 基准。结构 F1 提升了 15.52%，依赖关系语义等效性提升了 1.67 分（5分制）。
*   **分解优于规模**：实验证明，通过多智能体分解、自纠错和图验证，使用较小的模型（32B）可以超越参数量更大的单次生成模型。
*   **RAG 有效性**：RAG 模块的忠实度得分高达 4.61，证明其能有效减少幻觉并提供可靠的外部法律背景。
*   **自纠错能力**：ODR 循环能有效识别并修复 75% 以上的初始提取错误。

#### 7. 优点
*   **自包含性**：首个能将外部引用内容直接内联到提取结果中的系统，用户无需跳转查阅其他法律文件。
*   **鲁棒的纠错机制**：不仅纠正模型错误，还能通过语义分析纠正法规原文中的印刷或逻辑错误。
*   **结构严谨**：引入图论约束，解决了 LLM 难以处理的长程层级依赖和逻辑一致性问题。

#### 8. 不足与局限
*   **付费墙限制**：对于 ISO/EN 等受版权保护的标准，RAG 无法直接抓取内容，限制了这部分信息的自包含性。
*   **语言局限**：目前仅在英文监管文档上进行了验证，多语言支持尚待测试。
*   **时效性**：提取结果是静态的，若法规发生修订，需要重新运行整个流水线。
*   **成本与延迟**：由于存在多阶段和多次迭代循环，处理速度和 API 成本会高于传统方法。

（完）

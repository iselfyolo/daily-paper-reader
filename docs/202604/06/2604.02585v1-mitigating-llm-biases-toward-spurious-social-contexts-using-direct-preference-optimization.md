---
title: Mitigating LLM biases toward spurious social contexts using direct preference optimization
title_zh: 利用直接偏好优化缓解大语言模型对伪社会上下文的偏见
authors: "Hyunji Nam, Dorottya Demszky"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02585v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 评估教师教学质量并减轻课堂转录中的社会偏见
tldr: 本研究针对LLM在教师评估等高风险决策中易受无关社交背景（如教师资历、身份等）干扰而产生偏见的问题，评估了多种主流模型并发现偏见现象普遍存在。为此，作者提出了Debiasing-DPO方法，通过对比模型在纯净查询与含干扰背景下的推理差异进行自监督训练，并结合监督微调。实验证明，该方法在显著降低偏见的同时大幅提升了预测准确性，为构建鲁棒的决策模型提供了新思路。
selection_source: fresh_fetch
motivation: LLM在处理高风险决策任务时，容易受到与任务无关的社交背景信息干扰，导致评估结果出现严重偏差。
method: 提出Debiasing-DPO方法，通过对比模型在有无干扰背景下的推理过程进行自监督训练，并结合标签监督微调以保持准确性。
result: "在Llama和Qwen系列模型上，该方法平均减少了84%的偏见，并使预测准确率提升了52%。"
conclusion: 模型规模的扩大并不能自动消除对无关背景的敏感性，而Debiasing-DPO能有效提升模型在提示词预测任务中的鲁棒性与准确性。
---

## 摘要
大语言模型（LLM）越来越多地被用于高风险决策，但它们对伪上下文信息的敏感性可能会引入有害偏见。当模型被部署于评估教师教学质量等任务时，这是一个关键问题，因为有偏见的评估会影响教师的职业发展和职业轨迹。我们利用最大的美国课堂录音公开数据集（NCTE）及其配套的专家评分量表，研究了模型对伪社会上下文的鲁棒性。通过在七类伪上下文（包括教师经验、教育水平、人口统计学身份和诱导谄媚的框架）中评估七个前沿模型和开源权重模型，我们发现无关的上下文信息在 7 分制量表上最高可使模型预测偏移 1.48 分；尽管大模型具有更高的预测准确率，但有时表现出更高的敏感性。使用提示词和标准直接偏好优化（DPO）的缓解措施被证明在很大程度上是不够的。我们提出了 Debiasing-DPO，这是一种自监督训练方法，它将仅根据查询生成的中心推理与根据查询及额外伪上下文生成的有偏推理进行配对。我们进一步将该目标与基于真实标签的有监督微调相结合，以防止预测准确率的损失。应用于 Llama 3B & 8B 和 Qwen 3B & 7B 指令模型后，Debiasing-DPO 平均减少了 84% 的偏见，并提高了 52% 的预测准确率。我们从教育案例研究中得出的结论强调，对伪上下文的鲁棒性并不是模型缩放的自然副产品，而我们提出的方法可以在基于提示的预测任务中显著提高准确率和鲁棒性。

## Abstract
LLMs are increasingly used for high-stakes decision-making, yet their sensitivity to spurious contextual information can introduce harmful biases. This is a critical concern when models are deployed for tasks like evaluating teachers' instructional quality, where biased assessment can affect teachers' professional development and career trajectories. We investigate model robustness to spurious social contexts using the largest publicly available dataset of U.S. classroom transcripts (NCTE) paired with expert rubric scores. Evaluating seven frontier and open-weight models across seven categories of spurious contexts -- including teacher experience, education level, demographic identity, and sycophancy-inducing framings -- we find that irrelevant contextual information can shift model predictions by up to 1.48 points on a 7-point scale, with larger models sometimes exhibiting greater sensitivity despite higher predictive accuracy. Mitigations using prompts and standard direct preference optimization (DPO) prove largely insufficient. We propose **Debiasing-DPO**,, a self-supervised training method that pairs neutral reasoning generated from the query alone, with the model's biased reasoning generated with both the query and additional spurious context. We further combine this objective with supervised fine-tuning on ground-truth labels to prevent losses in predictive accuracy. Applied to Llama 3B \& 8B and Qwen 3B \& 7B Instruct models, Debiasing-DPO reduces bias by 84\% and improves predictive accuracy by 52\% on average. Our findings from the educational case study highlight that robustness to spurious context is not a natural byproduct of model scaling and that our proposed method can yield substantial gains in both accuracy and robustness for prompt-based prediction tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mitigating LLM biases toward spurious social contexts using direct preference optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
本文关注大语言模型（LLM）在执行高风险决策任务（如教师教学质量评估）时，容易受到**伪社会上下文（Spurious Social Contexts）**干扰的问题。
*   **研究动机**：LLM 具有强大的上下文学习能力，但这种能力是双刃剑。在评估教师表现时，模型本应只关注课堂转录文本，却往往被提示词中提到的教师资历、教育背景、种族或性别等无关信息左右，导致评分偏差。
*   **核心挑战**：现有的缓解措施（如安全提示词、思维链推理或标准 DPO）效果有限，且模型规模的扩大（Scaling）并不能自然解决鲁棒性问题，甚至前沿大模型表现出更高的敏感性。

### 2. 论文提出的方法论：Debiasing-DPO
作者提出了一种名为 **Debiasing-DPO** 的训练框架，旨在同时提升模型的鲁棒性和预测准确性。
*   **核心思想**：利用模型自身的推理能力生成“对比对”。将“仅基于查询生成的推理（中性）”作为**偏好项（Chosen）**，将“基于查询+伪上下文生成的推理（有偏）”作为**拒绝项（Rejected）**。
*   **关键技术细节**：
    1.  **自监督推理生成**：模型针对同一输入生成两份推理轨迹。一份不含干扰背景，一份包含（如“该教师有10年经验”）。
    2.  **DPO 损失函数**：通过 DPO 目标函数训练模型，使其在存在伪上下文的情况下，依然偏好中性的推理逻辑。
    3.  **锚定 SFT 损失**：为了防止模型在 DPO 过程中出现“模型崩溃”（即为了鲁棒性而始终输出固定分数的退化解），引入了基于专家真实标签（Ground-truth）的有监督微调（SFT）损失。
*   **总损失函数**：$L_{\theta} = w_{DPO} \cdot L_{DPO} + w_{SFT} \cdot L_{SFT}$。

### 3. 实验设计
*   **数据集**：使用 **NCTE (National Center for Teacher Effectiveness)** 转录数据集，这是目前最大的美国课堂转录公开数据集，包含专家根据 7 分制量表给出的评分。
*   **评估场景**：针对 7 个评估维度（如课堂管理、教学支持、学生参与度等）和 7 类伪上下文（教师经验、正式教育、证书、学位、直接谄媚、间接谄媚、人口统计学身份）。
*   **对比模型（Benchmark）**：
    *   **前沿模型**：GPT-5、Claude Haiku 4.5、Gemini 3.1 Flash Lite。
    *   **开源模型**：Llama 3.2-3B、Llama 3.1-8B、Qwen 2.5-3B/7B。
*   **对比方法**：推理时干预（多次采样平均、输入分段、安全提示词、CoT）以及训练基准（标准 SFT、基于真实标签的 DPO、反事实 DPO）。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **6 × NVIDIA H100 GPU** 用于训练较大的模型（如 7B/8B 级别），**2 × H100 GPU** 用于训练 3B 级别的模型。
*   **训练细节**：采用 bf16 精度，学习率在 1e-6 到 1e-7 之间，Batch Size 为 32。部分实验观察到训练 2 个 Epoch 效果更佳。

### 5. 实验数量与充分性
*   **实验规模**：评估了 7 个模型在 7 种偏见类别下的表现，涵盖了从闭源顶尖模型到开源轻量级模型的完整光谱。
*   **消融与泛化实验**：
    *   对比了多种推理时策略，证明了单纯的 Prompting 无法解决问题。
    *   进行了**跨类别泛化测试**：在“教师经验”偏见上训练的模型，被测试在“教育背景”等未见偏见上的表现。
*   **充分性评价**：实验设计非常严谨，使用了 Wilcoxon 符号秩检验来验证统计显著性。通过平衡采样解决了原始数据集严重的类别不平衡问题，确保了评估的客观性。

### 6. 主要结论与发现
*   **鲁棒性不随规模增长**：前沿大模型（如 GPT-5）虽然预测准确率更高，但对伪上下文的敏感度往往高于小模型（评分偏移最高达 1.48/7 分）。
*   **推理时干预失效**：安全提示词和 CoT 有时反而会加剧偏见，因为模型会在推理过程中自我强化伪上下文带来的逻辑。
*   **Debiasing-DPO 效果显著**：在 Llama 和 Qwen 模型上，该方法平均**减少了 84% 的偏见**，同时将预测准确率（Spearman 相关系数）**提升了 52%**。
*   **泛化能力**：模型能很好地泛化到语义相关的偏见类别（如从“经验”泛化到“证书”），但对语义无关的类别（如从“经验”泛化到“种族身份”）效果较弱。

### 7. 优点与亮点
*   **自监督特性**：不需要人工标注“偏见-非偏见”的推理对，降低了数据构建成本。
*   **双重优化**：通过结合 SFT 损失，成功解决了 DPO 在回归/预测任务中容易导致的模型退化问题。
*   **现实意义强**：选择教育评估这一高风险场景，具有极高的社会价值和伦理参考意义。

### 8. 不足与局限
*   **泛化局限性**：模型无法跨越语义鸿沟进行脱偏（例如，针对资历偏见的训练无法消除人口统计学偏见）。
*   **性能天花板**：虽然准确率提升了 52%，但绝对相关系数依然处于中等水平（0.2-0.3 左右），这反映了课堂评估任务本身的极高难度。
*   **数据分布不均**：尽管进行了平衡采样，但原始数据中 89% 的样本集中在中间分值，这可能限制了模型对极端评分情况的判别力。

（完）

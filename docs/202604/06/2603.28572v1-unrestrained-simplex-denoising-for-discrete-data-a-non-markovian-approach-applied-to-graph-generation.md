---
title: Unrestrained Simplex Denoising for Discrete Data. A Non-Markovian Approach Applied to Graph Generation
title_zh: 离散数据的无约束单纯形去噪：一种应用于图生成的非马尔可夫方法
authors: "Yoann Boget, Alexandros Kalousis"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28572v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 利用扩散模型进行离散数据和图生成的生成框架
tldr: 针对离散数据生成中状态变化剧烈的问题，本文提出了一种非马尔可夫单纯形去噪框架。该方法在概率单纯形上操作，通过条件独立的加噪方案简化了生成过程并移除了冗余约束。在合成及真实图生成基准测试中，该方法超越了现有的离散扩散和流匹配模型，证明了概率单纯形在离散生成建模中的有效性。
selection_source: fresh_fetch
motivation: 传统的离散状态空间生成模型往往存在状态变化过于突兀且受限于复杂约束的问题。
method: 提出一种基于概率单纯形的非马尔可夫去噪方案，使给定原始数据下的不同时刻噪声表示保持条件独立。
result: 在多个图生成基准测试中，该方法性能优于强力的离散扩散和流匹配基线模型。
conclusion: 概率单纯形是一个高效且简洁的离散生成建模框架，能有效提升图生成等任务的性能。
---

## 摘要
扩散模型或流匹配等去噪模型近期推动了离散结构生成建模的发展，但大多数方法直接在离散状态空间中运行，导致状态发生突变。我们引入了单纯形去噪（simplex denoising），这是一个在概率单纯形上运行的简单且有效的生成框架。其核心思想是一种非马尔可夫加噪方案，在该方案中，对于给定的干净数据点，不同时刻的噪声表示是条件独立的。在保留基于去噪的生成模型理论保证的同时，我们的方法移除了不必要的约束，从而提升了性能并简化了公式表述。实验结果表明，无约束单纯形去噪在合成和真实世界图基准测试中均优于强力的离散扩散和流匹配基准模型。这些结果凸显了概率单纯形作为离散生成建模有效框架的地位。

## Abstract
Denoising models such as Diffusion or Flow Matching have recently advanced generative modeling for discrete structures, yet most approaches either operate directly in the discrete state space, causing abrupt state changes. We introduce simplex denoising, a simple yet effective generative framework that operates on the probability simplex. The key idea is a non-Markovian noising scheme in which, for a given clean data point, noisy representations at different times are conditionally independent. While preserving the theoretical guarantees of denoising-based generative models, our method removes unnecessary constraints, thereby improving performance and simplifying the formulation. Empirically, \emph{unrestrained simplex denoising} surpasses strong discrete diffusion and flow-matching baselines across synthetic and real-world graph benchmarks. These results highlight the probability simplex as an effective framework for discrete generative modeling.

---

## 论文详细总结（自动生成）

### 离散数据的无约束单纯形去噪：一种应用于图生成的非马尔可夫方法

#### 1. 核心问题与研究动机
*   **核心问题**：传统的离散生成模型（如离散扩散模型或流匹配）通常直接在离散状态空间操作，导致状态转换时出现“跳跃式”突变（例如删除一条边可能瞬间导致图断裂）。此外，标准模型受限于马尔可夫假设，容易产生去噪误差累积（Compounding Denoising Errors）。
*   **研究动机**：
    *   现有的单纯形（Simplex）去噪方法公式复杂，且在加噪路径上存在支持度不连续等病态属性。
    *   旨在通过在概率单纯形上构建一种更平滑、约束更少且非马尔可夫的生成框架，以提高离散结构（如分子图）生成的质量和效率。

#### 2. 方法论：UNSIDE 框架
*   **核心思想**：引入“无约束单纯形去噪”（UNSIDE），将离散类别表示为概率单纯形上的顶点，通过连续的概率路径进行加噪和去噪。
*   **关键技术细节**：
    *   **非马尔可夫加噪方案**：假设在给定干净数据 $x_1$ 的条件下，不同时间步的噪声状态 $x_t$ 和 $x_s$ 是条件独立的。这消除了对前一时刻噪声状态的直接依赖。
    *   **Dirichlet 概率路径**：不使用线性插值，而是采用 Dirichlet 分布 $Dir(x_t; 1 + \alpha_t x_1)$ 来定义加噪过程。这种方法保证了在整个单纯形上的全支持度，避免了支持度坍缩。
    *   **去噪转换**：去噪步骤被简化为：首先根据当前状态预测干净数据的后验分布 $P_{1|t}$，然后基于该预测重新采样一个噪声更小（或更接近目标）的状态。
    *   **Voronoi 概率**：提出了一种新的理论工具，通过计算点落在其原始顶点所属 Voronoi 区域的概率，来分析和校准加噪动力学。
*   **算法流程**：训练时最小化加权负对数似然损失；推理时从先验分布（如均匀或经验边分布）开始，迭代执行 T 次去噪采样。

#### 3. 实验设计
*   **数据集**：
    *   **分子图**：QM9H（含显式氢原子）、ZINC250K。
    *   **合成图**：Planar（平面图）、SBM（随机块模型）。
    *   **真实世界图**：Enzymes（酶蛋白质结构）。
*   **Benchmark 与对比方法**：
    *   **离散扩散类**：DiGress、SID。
    *   **连续/流匹配类**：GruM、GraphBFN、DeFog、Dirichlet Flow Matching (Dirichlet FM)。
    *   **评估指标**：有效性（Validity）、FCD（化学相似度）、NSPDK（结构相似度）、MMD（度分布、聚类系数等）。

#### 4. 资源与算力
*   **硬件设备**：论文明确提到实验在一台配备 **1 张 NVIDIA GeForce RTX 3090 GPU** 和 128 核 CPU 的服务器上运行。
*   **效率表现**：UNSIDE 在采样速度上表现优异，是所有对比方法中最快的。例如在 Planar 数据集上，100 次迭代采样仅需约 6.3 秒，显著优于 DiGress（16.9 秒）和 GruM（29.3 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从合成图到复杂分子图的 5 个主流数据集。
    *   所有采样指标均基于 **5 次独立运行** 的平均值和标准差，保证了结果的统计可靠性。
*   **消融实验**：
    *   对比了不同函数评估次数（NFE）的影响（从 16 到 1024 步）。
    *   研究了噪声调度参数 $a$ 的鲁棒性。
    *   对比了不同先验初始化（均匀先验 vs 经验边分布先验）的效果。
*   **客观性**：作者重新运行了部分基准模型（如 GruM 和 DiGress）以确保在相同测试环境下的公平对比，并指出了部分基准模型在原始论文中可能存在的过拟合问题。

#### 6. 主要结论与发现
*   **性能领先**：UNSIDE 在几乎所有图生成基准测试中均达到或超越了最先进的（SOTA）离散扩散和流匹配模型。
*   **高有效性**：在 ZINC250K 等挑战性任务中，UNSIDE 仅需 64 步即可达到极高的分子有效性。
*   **误差缓解**：非马尔可夫采样方案有效缓解了离散生成中常见的误差累积问题。
*   **无需手工特征**：由于连续噪声天然提供了节点唯一标识，UNSIDE 无需像离散模型那样依赖复杂的拉普拉斯特征值或循环计数等手工结构特征。

#### 7. 优点
*   **简洁高效**：相比流匹配（需要计算向量场导数），UNSIDE 的公式极其简单，计算开销低。
*   **理论完备**：提供了关于收敛到平稳分布的理论证明，并给出了去噪转换误差的上限。
*   **平滑过渡**：单纯形上的操作允许类别间的概率叠加，实现了比离散跳跃更平滑的状态演变。

#### 8. 不足与局限
*   **密集计算限制**：尽管采样速度快，但模型底层仍基于全连接图的 GNN 处理，对于超大规模图（如数万个节点）可能存在显存瓶颈。
*   **类别依赖**：该方法主要针对分类数据（Categorical data），对于混合了连续属性和离散结构的复杂场景，其扩展性尚待进一步验证。
*   **超参数敏感性**：虽然对调度参数 $a$ 具有一定鲁棒性，但在不同性质的数据集上仍需通过 Voronoi 概率进行一定的校准。

（完）

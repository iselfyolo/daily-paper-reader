---
title: Real-Time Surrogate Modeling for Personalized Blood Flow Prediction and Hemodynamic Analysis
title_zh: 用于个性化血流预测和血流动力学分析的实时代理建模
authors: "Sokratis J. Anagnostopoulos, George Rovas, Vasiliki Bikia, Theodore G. Papaioannou, Athanase D. Protogerou, Nikolaos Stergiopulos"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03197v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 为机器学习训练生成大规模计算机模拟队列
tldr: 针对心血管建模中1D模型在大规模人群应用时参数估计难、易产生非生理性数据的问题，本研究提出了一种基于深度学习的实时代理模型框架。该框架利用临床数据集生成符合生理规律的虚拟队列，通过训练神经网络实现血流动力学参数的瞬时预测和参数筛选，显著降低了合成数据集的生成成本，并能有效应用于临床数据的中心主动脉压和心输出量估算。
selection_source: fresh_fetch
motivation: 解决1D动脉模型在处理大规模人群时，因难以准确估计末端阻力等参数而导致产生大量非生理性无效数据的问题。
method: 结合Asklepios临床数据集构建生理相关的虚拟队列，并训练深度神经代理模型进行血流动力学预测、参数先验筛选及逆向参数估计。
result: 该模型实现了对非生理参数组合的即时剔除，大幅降低了特定病理数据集的生成成本，并成功在临床数据上估算了心输出量和中心收缩压。
conclusion: 深度学习代理模型为个性化血流预测和临床血流动力学分析提供了一种高效、准确且具备生理一致性的实时计算方案。
---

## 摘要
随着健康追踪和心血管疾病早期检测需求的日益增长，心血管建模在过去几十年中取得了飞速发展。虽然一维（1-D）动脉模型在计算效率和求解保真度之间提供了极具吸引力的平衡，但其在大规模人群应用或生成大规模计算机模拟（in silico）队列方面仍面临挑战。某些血流动力学参数（如末端阻力/顺应性）难以在临床上进行估算，且在简单采样时往往会产生非生理性的血流动力学结果，导致大部分模拟数据集被废弃。在这项工作中，我们提出了一个用于训练机器学习（ML）模型的系统框架，能够实现瞬时血流动力学预测和参数估计。我们首先基于大型 Asklepios 临床数据集中观察到的多变量相关性，生成了一个参数化的虚拟患者队列，确保遵循生理参数分布。随后，我们训练了一个深度神经代理模型，能够预测患者特定的动脉血压和心输出量（CO），从而实现对输入参数的快速先验筛选。这允许立即剔除非生理性的参数组合，并大幅降低了生成针对性合成数据集（例如高血压群体）的成本。该模型还提供了一种有原则的末端阻力采样方法，以最大限度地减少不可测量参数的不确定性。此外，通过评估模型的预测性能，我们确定了足以解决估计心输出量（CO）逆问题的理论信息。最后，我们将该代理模型应用于临床数据集，用于估计中心主动脉血流动力学，即心输出量（CO）和主动脉收缩压（cSBP）。

## Abstract
Cardiovascular modeling has rapidly advanced over the past few decades due to the rising needs for health tracking and early detection of cardiovascular diseases. While 1-D arterial models offer an attractive compromise between computational efficiency and solution fidelity, their application on large populations or for generating large \emph{in silico} cohorts remains challenging. Certain hemodynamic parameters like the terminal resistance/compliance, are difficult to clinically estimate and often yield non-physiological hemodynamics when sampled naively, resulting in large portions of simulated datasets to be discarded. In this work, we present a systematic framework for training machine learning (ML) models, capable of instantaneous hemodynamic prediction and parameter estimation. We initially start with generating a parametric virtual cohort of patients which is based on the multivariate correlations observed in the large Asklepios clinical dataset, ensuring that physiological parameter distributions are respected. We then train a deep neural surrogate model, able to predict patient-specific arterial pressure and cardiac output (CO), enabling rapid a~priori screening of input parameters. This allows for immediate rejection of non-physiological combinations and drastically reduces the cost of targeted synthetic dataset generation (e.g. hypertensive groups). The model also provides a principled means of sampling the terminal resistance to minimize the uncertainties of unmeasurable parameters. Moreover, by assessing the model's predictive performance we determine the theoretical information which suffices for solving the inverse problem of estimating the CO. Finally, we apply the surrogate on a clinical dataset for the estimation of central aortic hemodynamics i.e. the CO and aortic systolic blood pressure (cSBP).

---

## 论文详细总结（自动生成）

### 论文总结：用于个性化血流预测和血流动力学分析的实时代理建模

#### 1. 核心问题与研究动机
*   **核心问题**：一维（1-D）动脉脉搏波模型虽然在计算效率和保真度之间取得了平衡，但在处理大规模人群或生成大规模计算机模拟（in silico）队列时，计算成本依然较高。此外，末端阻力（$R_T$）和顺应性（$C_T$）等关键血流动力学参数在临床上难以测量，随机采样往往会产生非生理性的无效数据，导致计算资源浪费。
*   **研究动机**：开发一种基于机器学习的实时代理模型（Surrogate Model），旨在实现瞬时的血流动力学预测、参数先验筛选（剔除非生理组合）以及从非侵入性测量值中逆向估计心输出量（CO）等关键指标。

#### 2. 方法论
*   **核心思想**：利用临床数据集的统计相关性指导虚拟队列生成，并训练深度神经网络（DNN）来替代耗时的物理求解器。
*   **关键技术细节**：
    *   **虚拟队列生成**：基于 **Asklepios 临床数据集**（n=2524）中参数（如身高、体重、年龄、血压、PWV等）的多变量相关性，利用分位数拉丁超立方采样（LHS）生成了 2000 名虚拟患者。
    *   **物理模拟**：使用经过验证的 1-D 动脉网络模型（包含 103 个血管段）为虚拟队列生成地面真值（Ground Truth）。
    *   **代理模型架构**：采用全连接神经网络，包含对称的扩张-压缩结构（隐藏层神经元数为 128-256-128），使用 Tanh 激活函数、Batch Normalization 和 Dropout（0.1）。
    *   **损失函数**：使用 **Huber Loss（平滑 L1 损失）**，以增强对测量噪声和离群值的鲁棒性。
    *   **运行模式**：
        *   **前向模式**：输入几何和生理参数，预测肱动脉的收缩压（SBP）和舒张压（DBP）。
        *   **逆向模式**：输入临床可测参数（如 HR、DBP、SBP 等），估计心输出量（CO）和中心主动脉收缩压（cSBP）。

#### 3. 实验设计
*   **数据集**：
    *   **Asklepios 数据集**：用于提取生理参数的相关性。
    *   **In silico 数据集**：由 1-D 模型生成的 2000 组模拟数据，用于训练和验证代理模型。
    *   **临床验证集**：包含 20 名健康受试者的侵入性/非侵入性测量数据，用于评估模型的临床泛化能力。
*   **Benchmark 与对比**：
    *   以 1-D 物理求解器的数值解作为基准。
    *   对比了不同输入特征组合（如是否包含径向动脉压力、是否包含末端参数）对逆向估计 CO 准确性的影响。

#### 4. 资源与算力
*   **算力对比**：文中提到，若使用原始物理模型生成 30x30x30 网格规模的参数空间探索，单核 CPU 需要约 **50 天**；而训练好的 DNN 代理模型可以实现**实时（毫秒级）**预测。
*   **训练细节**：使用了 AdamW 优化器，学习率为 $3 \times 10^{-4}$，训练 2000 个 epoch。文中未明确提及具体的 GPU 型号和具体训练时长，但此类规模的 DNN 通常在单张消费级 GPU 或高性能 CPU 上即可快速完成训练。

#### 5. 实验数量与充分性
*   **实验规模**：生成了 2000 组虚拟样本，并进行了 80/20 的训练/测试集划分。
*   **实验维度**：
    *   进行了**成对参数敏感性分析**，涵盖了几何缩放、末端阻力、顺应性等 7 个维度。
    *   进行了**逆问题唯一性研究**，探讨了不同传感器位置（肱动脉 vs 径向动脉）的信息增益。
    *   进行了**人群匹配实验**，验证了模型筛选出的生理样本与真实临床分布的一致性。
*   **充分性评价**：实验设计较为全面，不仅验证了数值准确性，还深入探讨了逆问题的数学病态性（非唯一性），实验逻辑客观、公平。

#### 6. 主要结论与发现
*   **参数敏感性**：末端阻力（$R_T$）、心输出量（CO）、动脉顺应性（C）和心率（HR）是影响血压的主导因素；而几何缩放和末端顺应性（$C_T$）影响较小。
*   **逆问题求解**：仅靠肱动脉血压估计 CO 存在非唯一性（误差约 11%）；引入**径向动脉压力测量**可显著降低不确定性（误差降至 2.7%）。
*   **临床应用**：模型在临床数据集上表现良好，cSBP 的估计精度（r=0.959）优于 CO（r=0.615）。

#### 7. 优点
*   **生理一致性**：通过临床数据相关性指导采样，避免了传统机器学习模型训练中常见的“非生理性数据”问题。
*   **高效筛选**：代理模型可作为物理模拟的先验过滤器，大幅提升了生成特定病理（如高血压）合成数据集的效率。
*   **可解释性**：通过敏感性矩阵和响应面分析，为复杂的血流动力学交互提供了直观的解释。

#### 8. 不足与局限
*   **人群覆盖有限**：目前主要基于健康人群数据（Asklepios 样本年龄 35-55 岁），未充分涵盖严重心血管疾病（如动脉瘤、狭窄）或极端高龄人群。
*   **模型简化**：1-D 模型本身基于理想化假设（如长锥形血管段），无法捕捉 3-D 复杂的旋涡流或特定病变处的局部流场。
*   **临床验证样本量小**：临床测试仅使用了 20 名受试者，对于证明其在多样化临床环境中的鲁棒性尚显不足。
*   **数据噪声敏感性**：虽然使用了 Huber Loss，但临床实际测量中的传感器误差和操作变异性仍可能对逆向估计产生较大干扰。

（完）

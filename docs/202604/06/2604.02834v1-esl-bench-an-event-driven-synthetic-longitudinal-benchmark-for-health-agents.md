---
title: "ESL-Bench: An Event-Driven Synthetic Longitudinal Benchmark for Health Agents"
title_zh: ESL-Bench：面向健康智能体的事件驱动合成纵向基准测试
authors: "Chao Li, Cailiang Liu, Ang Gao, Kexin Deng, Shu Zhang, Langping Xu, Xiaotong Shi, Xionghao Ding, Jian Pei, Xun Jiang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02834v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于合成纵向健康数据的事件驱动合成框架
tldr: 针对健康智能体在处理多源长程健康数据时面临的隐私限制和缺乏标准答案的问题，本文提出了ESL-Bench。这是一个事件驱动的合成长程基准测试框架，包含100名虚拟用户1-5年的健康轨迹。该框架结合LLM语义规划与算法模拟生成生理指标，并提供1万个可编程计算标准答案的查询，涵盖查找、趋势、对比、异常和解释五个维度，旨在评估智能体的多跳推理和证据归因能力。
selection_source: fresh_fetch
motivation: 真实健康数据难以大规模发布且缺乏结构化的因果标注，导致长程健康智能体的评估面临巨大挑战。
method: 采用混合流水线，利用LLM生成语义化的生活事件计划，并结合基于生理约束的算法模拟生成受事件驱动的连续指标动态。
result: "对13种方法的评估显示，数据库原生智能体（48-58%）在对比和解释类复杂查询中显著优于基于内存的RAG基准（30-38%）。"
conclusion: ESL-Bench为健康智能体提供了高保真且具备确定性答案的评估平台，揭示了当前模型在多跳推理和长程数据归因方面的局限性。
---

## 摘要
纵向健康智能体必须在结合了连续设备流、稀疏临床检查和偶发生活事件的多源轨迹中进行推理，但对其进行评估具有挑战性：现实世界的数据无法大规模发布，且在缺乏结构化基准真相（ground truth）的情况下，具有时间背景的归因问题很难获得确定的答案。我们提出了 ESL-Bench，这是一个事件驱动的合成框架和基准，提供了 100 名合成用户，每名用户都拥有 1-5 年的轨迹，包括健康档案、多阶段叙事计划、每日设备测量、定期检查记录以及带有明确逐项指标影响参数的事件日志。每个指标遵循一个由离散事件驱动的基准随机过程，这些事件在饱和与投影约束下具有 Sigmoid 起始和指数衰减核；混合流水线将稀疏的语义伪影委托给基于大语言模型（LLM）的规划，而将密集的指标动态委托给具有硬性生理边界的算法模拟。每位用户配有跨越五个维度（查询、趋势、比较、异常、解释）的 100 个评估查询，并分为简单、中等和困难三个层级，所有基准真相答案均可根据记录的事件-指标关系通过程序计算得出。通过评估涵盖带工具的 LLM、数据库原生智能体和记忆增强 RAG 的 13 种方法，我们发现数据库智能体（48-58%）显著优于记忆 RAG 基准（30-38%），差距主要集中在需要多跳推理和证据归因的“比较”和“解释”查询上。

## Abstract
Longitudinal health agents must reason across multi-source trajectories that combine continuous device streams, sparse clinical exams, and episodic life events - yet evaluating them is hard: real-world data cannot be released at scale, and temporally grounded attribution questions seldom admit definitive answers without structured ground truth. We present ESL-Bench, an event-driven synthesis framework and benchmark providing 100 synthetic users, each with a 1-5 year trajectory comprising a health profile, a multi-phase narrative plan, daily device measurements, periodic exam records, and an event log with explicit per-indicator impact parameters. Each indicator follows a baseline stochastic process driven by discrete events with sigmoid-onset, exponential-decay kernels under saturation and projection constraints; a hybrid pipeline delegates sparse semantic artifacts to LLM-based planning and dense indicator dynamics to algorithmic simulation with hard physiological bounds. Users are each paired with 100 evaluation queries across five dimensions - Lookup, Trend, Comparison, Anomaly, Explanation - stratified into Easy, Medium, and Hard tiers, with all ground-truth answers programmatically computable from the recorded event-indicator relationships. Evaluating 13 methods spanning LLMs with tools, DB-native agents, and memory-augmented RAG, we find that DB agents (48-58%) substantially outperform memory RAG baselines (30-38%), with the gap concentrated on Comparison and Explanation queries where multi-hop reasoning and evidence attribution are required.

---

## 论文详细总结（自动生成）

以下是对论文《ESL-Bench: An Event-Driven Synthetic Longitudinal Benchmark for Health Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着可穿戴设备和电子健康记录（EHR）的普及，健康数据呈现出长程（Longitudinal）、异构和高频的特点。理解这些跨越数月或数年的交织数据流是“纵向健康智能体”的核心任务。
*   **核心挑战**：
    1.  **隐私限制**：真实世界的健康数据因隐私保护难以大规模公开发布。
    2.  **缺乏基准真相（Ground Truth）**：在真实数据中，由于存在混杂因素和缺失上下文，很难确定某个指标的变化究竟是由哪个事件引起的（归因难题）。
    3.  **推理复杂性**：长程健康分析需要跨源对齐、时间窗口聚合、多跳关联及证据归因，而不仅仅是长文本处理。
*   **研究目的**：开发一个可扩展、可复现且具备确定性答案的合成基准，用于评估智能体在复杂时间维度上的推理能力。

### 2. 方法论：事件驱动的合成框架
*   **核心思想**：将患者轨迹建模为“基准健康状态”与“离散生活/临床事件”的叠加。每个事件对生理指标的影响通过明确的数学核函数定义，从而使答案可程序化计算。
*   **关键技术细节**：
    *   **混合流水线（Hybrid Pipeline）**：
        *   **LLM 模块**：负责生成稀疏的语义内容，如用户画像、多阶段叙事计划（Trajectory Plan）、事件描述及临床检查报告。
        *   **算法模拟器**：负责生成密集的每日设备指标（如心率、步数）。
    *   **动力学模型**：指标动态遵循公式：`观测值 = 基准 + 自回归残差 + 事件贡献 + 噪声`。
    *   **时间核函数（Temporal Kernels）**：事件影响采用 **Sigmoid 起始**（模拟逐渐发作）和 **指数衰减**（模拟恢复/消退）的核函数，并设有饱和约束以防止指标数值超出生理极限。
    *   **机制恢复（Mechanism Recovery）**：由于每个事件对指标的影响参数（方向、强度、持续时间）在生成时已确定，系统可以精确计算出任何时间点各事件的贡献度。

### 3. 实验设计
*   **数据集规模**：包含 100 名合成用户，每人拥有 1-5 年的纵向数据。
*   **评估维度（5x3 矩阵）**：
    *   **五个维度**：查找（Lookup）、趋势（Trend）、比较（Comparison）、异常（Anomaly）、解释（Explanation）。
    *   **三个难度**：简单、中等、困难（基于推理步数和约束条件划分）。
*   **对比方法（共 13 种）**：
    *   **带工具的 LLM**：GPT-5.4、GPT-5.2、Gemini 3 Flash、Claude 3.5 Sonnet、MiniMax M2.5、GLM-5。
    *   **数据库原生智能体（DB Agents）**：Theta 系列（General, Expert, Smart Expert），通过 SQL/API 操作 DuckDB。
    *   **记忆增强 RAG**：HippoRAG（不同检索预算 k=10, 20, 50）。
*   **评分机制**：两阶段评分，先进行程序化 JSON 匹配（数值允许 1% 误差），再由 LLM 依据准则（Rubric）进行质量评分。

### 4. 资源与算力
*   **合成成本**：论文明确指出使用 **Gemini 3.1 Pro Preview** 作为合成引擎。
*   **算力消耗**：生成单个用户平均需要 614 次 API 调用，消耗约 20.3M 输入 Token 和 2.8M 输出 Token。按当时费率计算，**每位用户的生成成本约为 74 美元**。
*   **训练说明**：文中未提及对模型进行微调或训练，主要侧重于推理评估和数据合成。

### 5. 实验数量与充分性
*   **实验规模**：总计生成了 **10,000 个查询**（每位用户 100 个），涵盖了 13 种主流模型和架构。
*   **充分性**：实验设计非常充分。通过 5 个维度和 3 个难度的交叉验证，清晰地展示了不同架构在特定任务上的优劣。
*   **客观性**：采用了确定性的程序化评分作为第一道门槛，减少了 LLM 评分的主观性；同时通过临床专家审核合成模板，确保了生理逻辑的合理性。

### 6. 主要结论与发现
*   **架构差异显著**：**数据库智能体（48-58%）显著优于记忆 RAG（30-38%）**。RAG 在处理需要跨时间窗口聚合和多表连接的“比较”与“解释”类问题时表现极差。
*   **推理瓶颈**：所有方法在“解释”维度（归因分析）上的得分普遍偏低（15-31%），说明当前智能体在建立复杂的证据链方面仍有巨大提升空间。
*   **难度敏感度**：随着难度从简单提升到困难，带工具的 LLM 表现大幅下滑，而数据库智能体表现相对稳健，证明了结构化查询在复杂推理中的优势。

### 7. 优点与亮点
*   **可验证的归因**：首次解决了长程健康数据中“因果归因”难以评估的问题，通过构建“事件-指标-时间”图谱实现了答案的确定性。
*   **生理一致性**：通过硬性生理约束和软性饱和机制，避免了合成数据中常见的数值漂移和不合理跳变。
*   **细粒度诊断**：5x3 的评估矩阵为开发者提供了精准的“错题本”，能明确指出智能体是弱在检索、算术还是逻辑推理。

### 8. 不足与局限
*   **外部有效性**：虽然合成数据逻辑严密，但其整齐的事件边界和清晰的因果关系可能简化了现实临床环境的复杂性（如未记录的干预、极度嘈杂的传感器数据）。
*   **生成成本高昂**：每用户 74 美元的生成成本限制了其在大规模群体模拟中的应用。
*   **应用限制**：该基准主要评估推理能力，而非临床诊断的准确性，不能直接用于替代医疗决策。
*   **偏差风险**：合成逻辑依赖于 LLM 的先验知识和预设模板，可能存在模型固有的医学偏见。

（完）

---
title: Querying Structured Data Through Natural Language Using Language Models
title_zh: 使用语言模型通过自然语言查询结构化数据
authors: "Hontan Valentin-Micu, Bunea Andrei-Alexandru, Tantaroudas Nikolaos Dimitrios, Popovici Dan-Matei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03057v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 使用大语言模型生成合成训练数据的原则性流水线
tldr: 本研究针对检索增强生成（RAG）在处理结构化和数值数据时的局限性，提出了一种将自然语言转化为可执行查询的开源方法。通过构建合成数据生成流水线，并利用QLoRA技术对DeepSeek R1 Distill 8B模型进行4比特量化微调，实现了在消费级硬件上的高效部署。在西班牙Durangaldea服务可及性数据集上的实验证明，该方法在多语言和未知地理场景下均具有极高的查询准确率和泛化能力。
selection_source: fresh_fetch
motivation: 传统的RAG技术难以有效处理高度结构化和数值型信息，因此需要一种能将自然语言精准转化为可执行查询的专用模型。
method: 开发了一套合成数据生成流水线来捕捉用户意图与数据语义，并采用QLoRA量化微调技术训练轻量级开源模型。
result: 微调后的8B模型在单语言、多语言及未见过的地理位置测试中均表现出高精度的查询生成能力。
conclusion: 小型领域特定模型在结构化数据查询任务中可替代大型商业模型，为资源受限环境提供了高精度且易于扩展的解决方案。
---

## 摘要
本文提出了一种开源方法，允许用户通过自然语言查询结构化的非文本数据集。与在处理数值和高度结构化信息时表现欠佳的检索增强生成（RAG）不同，我们的方法训练大语言模型（LLM）生成可执行查询。为了支持这一能力，我们引入了一套规范的合成训练数据生成流水线，产生多样化的问答对，以同时捕捉用户意图和底层数据集的语义。我们使用带有 4 位量化的 QLoRA 技术微调了一个紧凑型模型 DeepSeek R1 Distill 8B，使该系统适合部署在通用硬件上。我们在描述西班牙杜兰加尔德亚（Durangaldea）地区基本服务可及性的数据集上评估了我们的方法。微调后的模型在单语、多语以及未见地点场景下均实现了高准确率，展示了鲁棒的泛化能力和可靠的查询生成能力。我们的结果强调，小型领域特定模型可以在不依赖大型专有 LLM 的情况下，在此任务中实现高精度，使该方法适用于资源受限的环境，并可适配更广泛的多数据集系统。

## Abstract
This paper presents an open source methodology for allowing users to query structured non textual datasets through natural language Unlike Retrieval Augmented Generation RAG which struggles with numerical and highly structured information our approach trains an LLM to generate executable queries To support this capability we introduce a principled pipeline for synthetic training data generation producing diverse question answer pairs that capture both user intent and the semantics of the underlying dataset We fine tune a compact model DeepSeek R1 Distill 8B using QLoRA with 4 bit quantization making the system suitable for deployment on commodity hardware We evaluate our approach on a dataset describing accessibility to essential services across Durangaldea Spain The fine tuned model achieves high accuracy across monolingual multilingual and unseen location scenarios demonstrating both robust generalization and reliable query generation Our results highlight that small domain specific models can achieve high precision for this task without relying on large proprietary LLMs making this methodology suitable for resource constrained environments and adaptable to broader multi dataset systems We evaluate our approach on a dataset describing accessibility to essential services across Durangaldea Spain The fine tuned model achieves high accuracy across monolingual multilingual and unseen location scenarios demonstrating both robust generalization and reliable query generation Our results highlight that small domain specific models can achieve high precision for this task without relying on large proprietary LLMs making this methodology suitable for resource constrained environments and adaptable to broader multi dataset systems.

---

## 论文详细总结（自动生成）

### 论文总结：使用语言模型通过自然语言查询结构化数据

#### 1. 核心问题与整体含义（研究动机和背景）
传统的检索增强生成（RAG）技术在处理非结构化文本时表现出色，但在面对**高度结构化数据（如数值、时间序列、地理坐标）**时存在局限性，因为向量嵌入（Embeddings）难以准确捕捉数值间的逻辑关系。虽然超大规模模型（如 GPT-4）可以通过长上下文和复杂提示词实现结构化查询，但其面临高昂成本、隐私泄露及对闭源 API 的依赖。本文旨在开发一种**完全开源、可部署在消费级硬件上**的方案，通过微调小型语言模型（8B 规模），使其能够将自然语言精准转化为可执行的 API 查询，从而实现对专业结构化数据集的交互式访问。

#### 2. 核心方法论
论文提出了一套从数据生成到模型部署的完整流水线：
*   **合成数据生成流水线**：
    *   **基于投影的模板化**：针对数据库中的表，提取具有语义意义的属性投影（如“位置”与“距离”），并利用 SOTA 模型（Gemini 2.5 Pro 和 DeepSeek R1）生成多样化的提问模板。
    *   **实例化与查询映射**：使用 Overpass API 提取真实地理坐标填充模板，并自动生成对应的 API 查询语句及执行结果。
    *   **语义改写**：对生成的 44,849 个问答对进行语法改写，以增强模型对不同表达方式的鲁棒性。
*   **模型微调技术**：
    *   **基础模型**：选用推理能力较强的 **DeepSeek R1-Distill-8B**。
    *   **微调策略**：采用 **QLoRA（4-bit 量化）** 技术，仅训练约 1000 万个参数（低秩矩阵），保持基础模型权重冻结。
*   **推理流程**：采用“思考-调用-回答”模式。模型识别出需要查询时生成 `<API>` 标签，系统挂起生成并执行查询，将结果回填至上下文后，模型再生成最终自然语言答案。

#### 3. 实验设计
*   **数据集**：使用西班牙杜兰加尔德亚（Durangaldea）地区的辅助功能数据集（包含 10 万条记录，涉及医院、超市、药店的坐标及步行/骑行/驾车时间）。
*   **测试场景**：
    *   **未见地点（Unseen Locations）**：测试模型对训练集外地理位置的泛化能力。
    *   **语义变体（Semantic Variants）**：测试对同义提问的稳定性。
    *   **多语言查询**：测试模型在西班牙语、加泰罗尼亚语、巴斯克语、加利西亚语和法语下的表现。
*   **Benchmark 与对比方法**：将微调后的 8B 模型与 **GPT-4、Gemini 2.5 Pro、Grok、DeepSeek R1（原始版）** 在零样本（Zero-shot）设置下进行对比。
*   **评估指标**：准确匹配率（Exact Match, EM）、BLEU-4、ROUGE-L。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 3090 (24GB)** GPU。
*   **训练细节**：
    *   使用 4-bit 量化以降低显存占用。
    *   训练 4 个 Epoch，每个 Epoch 耗时数小时。
    *   Batch Size 为 32，采用余弦学习率调度器。
*   **部署**：推理延迟约 3 秒/请求，主要消耗在模型生成阶段。

#### 5. 实验数量与充分性
论文构建了包含 **44,849 个样本** 的大规模合成数据集，并划分了多个专项测试集（如 500 组未见地点、500 组语义变体、500 组多语言样本）。实验覆盖了泛化性、鲁棒性和多语言能力，并与当前最顶尖的商业模型进行了横向对比。整体实验设计较为客观、公平，充分验证了小型模型在特定领域通过微调可以达到甚至超越通用大模型的精度。

#### 6. 主要结论与发现
*   **高精度**：微调后的 8B 模型在语义变体测试中达到 **94.2% 的准确匹配率**，接近 GPT-4 的 97.2%。
*   **强泛化性**：在完全未见过的地理位置上，模型仍能保持 **89% 的 EM 准确率**。
*   **多语言差异**：模型在西班牙语（81%）和加利西亚语（93%）表现优异，但在巴斯克语（24%）上表现较差，这反映了底层预训练模型在小众语言上的语料短板。
*   **成本效益**：证明了在消费级硬件上运行的小型模型足以处理复杂的结构化数据查询任务，无需依赖昂贵的云端 API。

#### 7. 优点与亮点
*   **开源与轻量化**：全流程使用开源工具和模型，且支持在单张 3090 显卡上完成训练与推理。
*   **原则性数据生成**：提出了一种系统化的方法，解决了特定领域缺乏“自然语言-查询语句”对齐数据的难题。
*   **模块化扩展**：通过 LoRA 适配器，系统可以轻松切换或增加对不同数据集的支持，而无需重新训练整个模型。

#### 8. 不足与局限
*   **人工干预**：虽然数据生成高度自动化，但初始的模板定义和投影选择仍需人工干预，难以完全自动化地迁移到任意新数据库。
*   **错误恢复机制缺失**：目前系统缺乏自动纠错功能，如果模型生成的 API 调用语法错误，系统无法通过迭代反馈进行自我修正。
*   **语言偏差**：对巴斯克语等低资源语言的支持显著弱于主流语言。
*   **并发限制**：目前的架构主要针对单用户请求优化，在高并发场景下的扩展性（如批处理推理）尚未充分测试。

（完）

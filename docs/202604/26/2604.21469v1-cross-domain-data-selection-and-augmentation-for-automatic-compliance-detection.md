---
title: Cross-Domain Data Selection and Augmentation for Automatic Compliance Detection
title_zh: 自动合规检测中的跨领域数据选择与增强
authors: "Fariz Ikhwantri, Dusica Marijan"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21469v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 自动合规检测与法律文本变异性
tldr: 本研究针对自动化合规检测中模型跨法规泛化能力差的问题，提出了一种基于数据选择的增强策略。研究者将合规检测视为自然语言推理（NLI）任务，系统评估了随机采样、Moore-Lewis 交叉熵差、重要性权重及基于嵌入的检索四种数据选择方法。通过调整增强数据比例，研究证明了精准的数据选择能显著抑制负迁移，为跨异构法规的可靠合规自动化提供了实用且可扩展的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决法律文本复杂多变导致合规检测模型在不同法规领域间泛化困难及负迁移的问题。
method: 将合规检测建模为自然语言推理任务，并对比分析了随机采样、交叉熵差、重要性权重和嵌入检索四种数据选择策略。
result: 实验证明，通过有针对性的数据选择进行领域增强，可以大幅减少负迁移并提升跨领域适配性能。
conclusion: 精准的数据选择是实现跨异构法规、可扩展且可靠的自动化合规检测的关键路径。
---

## 摘要
由于法律文本的复杂性和多变性，自动化监管合规检测仍然是一项具有挑战性的任务。在一种法规上训练的模型往往难以泛化到其他法规。这一局限性凸显了开发有原则的方法以改进跨领域迁移的必要性。我们将合规检测构建为自然语言推理（NLI）任务，并研究将数据选择作为减轻负迁移的策略。具体而言，我们评估了从较大的源域中选择增强数据的四种方法：随机采样、Moore-Lewis 交叉熵差、重要性权重以及基于嵌入的检索。我们系统地改变所选数据的比例，以分析其对跨领域适应的影响。研究结果表明，有针对性的数据选择显著减少了负迁移，为跨异构法规实现可扩展且可靠的合规自动化提供了一条切实可行的路径。

## Abstract
Automating the detection of regulatory compliance remains a challenging task due to the complexity and variability of legal texts. Models trained on one regulation often fail to generalise to others. This limitation underscores the need for principled methods to improve cross-domain transfer. We study data selection as a strategy to mitigate negative transfer in compliance detection framed as a natural language inference (NLI) task. Specifically, we evaluate four approaches for selecting augmentation data from a larger source domain: random sampling, Moore-Lewis's cross-entropy difference, importance weighting, and embedding-based retrieval. We systematically vary the proportion of selected data to analyse its effect on cross-domain adaptation. Our findings demonstrate that targeted data selection substantially reduces negative transfer, offering a practical path toward scalable and reliable compliance automation across heterogeneous regulations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cross-Domain Data Selection and Augmentation for Automatic Compliance Detection》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**自动化合规检测（Compliance Detection）在跨法律领域时的泛化难题**。
*   **背景**：确保软件系统符合法律法规（如 GDPR、HIPAA）是资源密集型任务。虽然已有基于机器学习的自动化方法，但法律文本在术语、结构和推理逻辑上存在巨大的领域差异。
*   **动机**：在某一法规（源域）上训练的模型直接应用于另一法规（目标域）时，往往会因为数据分布不一致导致“负迁移”（Negative Transfer），即引入额外数据反而降低模型性能。
*   **核心目标**：探索如何通过“有原则的数据选择策略”从大规模源域中筛选出对目标域最有益的增强数据，以实现高效、可靠的跨领域合规自动化。

### 2. 方法论：核心思想与关键技术
研究者将合规检测建模为**自然语言推理（NLI）任务**：将法规要求作为“前提”（Premise），将合同条款或证据作为“假设”（Hypothesis），判断两者是否存在“蕴含”（Entailment）关系。

为了从源域（GDPR）中选择数据增强目标域（HIPAA），论文对比了四种核心策略：
*   **Moore-Lewis 交叉熵差**：利用分别在源域和目标域训练的语言模型，计算源域样本在两个模型下的交叉熵之差，差值越小表示样本与目标域越接近。
*   **基于密度比的重要性权重（Importance Weighting）**：训练一个逻辑回归分类器来区分源域和目标域样本，通过预测概率估计密度比 $p_{target}(x) / p_{source}(x)$，以此对源域样本进行排序。
*   **嵌入相似度（Embedding Similarity）**：使用预训练模型（如 RoBERTa）将文本转化为向量，计算源域样本与目标域样本之间的余弦相似度，检索语义最接近的候选数据。
*   **随机采样（Random Sampling）**：作为基准对照组。

### 3. 实验设计
*   **数据集**：
    *   **源域**：GDPR-DPA 数据集（包含 362,317 条训练实例）。
    *   **目标域**：HIPAA 数据集（规模较小，训练集仅 394 条实例，测试集 1,426 条）。
*   **对比模型**：
    *   **判别式模型（微调）**：BERT-large、RoBERTa-large、Legal-BERT。
    *   **生成式模型（Prompting）**：GPT-2-XL、Llama-3.2 (1B/3B)，采用 Zero-shot 和 One-shot 设置。
*   **实验场景**：
    *   **域内（In-domain）**：在同一法规数据集上训练和评估。
    *   **跨域（Cross-domain）**：从 GDPR 迁移至 HIPAA，系统性改变数据选择比例（1% 到 90%）。

### 4. 资源与算力
*   **算力说明**：论文提到受益于挪威研究理事会支持的 eX3（Experimental Infrastructure for Exploration of Exascale Computing）基础设施。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了训练参数：Batch Size 为 64（32 配合 2 步梯度累积），微调 6 个 Epoch，学习率为 1e-05。

### 5. 实验数量与充分性
*   **实验规模**：研究者针对 3 种主要选择方法 + 1 种随机基准，在 8 种不同的数据选择比例（1% - 75%/90%）下进行了多组实验，并对比了 5 种不同的基础模型。
*   **充分性与客观性**：
    *   **充分性**：实验涵盖了从编码器（BERT 类）到解码器（Llama 类）的主流架构，且通过细粒度的比例划分捕捉到了性能波动的曲线。
    *   **客观性**：通过设置“全量增强”和“随机采样”作为 Baseline，清晰地展示了数据选择的必要性。同时，论文不仅关注 F1 分数，还深入分析了负迁移发生的区间（如 10%-20% 比例处）。

### 6. 主要结论与发现
*   **数据选择的有效性**：有针对性的跨领域数据选择显著优于仅使用目标域数据或全量盲目增强。
*   **最佳策略**：**重要性权重（Importance Weighting）**在低比例（5%）时表现最强；**Moore-Lewis** 在高比例选择时最为稳定。
*   **负迁移现象**：实验观察到明显的“性能 U 型曲线”，即在加入 10%-20% 的中等相关数据时，性能往往会下降（负迁移），而极少量的高质量数据（1%-5%）或极大量的数据反而可能回升。
*   **微调 vs. 提示词**：在法律合规这种高度专业化的任务中，**微调判别式模型**（尤其是 Legal-BERT）的效果远好于大语言模型的 Zero-shot/One-shot 表现。

### 7. 优点与亮点
*   **系统性分析**：首次系统地研究了数据选择比例对法律合规检测中负迁移的影响，而非简单地堆砌数据。
*   **任务建模新颖**：将合规检测转化为 NLI 任务，相比传统的分类任务，这种方法能更好地处理法律文本的语义蕴含关系，且具有更好的可解释性潜力。
*   **实用性强**：为法律科技领域在标注数据匮乏的情况下，如何利用现有其他法规数据提供了明确的工程指导。

### 8. 不足与局限
*   **迁移方向单一**：实验仅验证了从 GDPR 到 HIPAA 的单向迁移，未测试反向迁移或其他法规（如金融、网络安全标准）之间的迁移效果。
*   **语义深度不足**：数据选择主要依赖统计指标（交叉熵、密度比）或通用嵌入，可能仍无法完全捕捉法律条款中深层的逻辑冲突或细微的法律解释差异。
*   **LLM 探索有限**：对大语言模型的评估仅限于较小参数量（1B-3B）的版本，未测试更大规模模型（如 Llama-3-70B 或 GPT-4）在复杂法律推理上的潜力。

（完）

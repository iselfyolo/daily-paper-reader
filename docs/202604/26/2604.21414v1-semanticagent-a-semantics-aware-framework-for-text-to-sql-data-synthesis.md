---
title: "SemanticAgent: A Semantics-Aware Framework for Text-to-SQL Data Synthesis"
title_zh: SemanticAgent：一种语义感知的 Text-to-SQL 数据合成框架
authors: "Qiang Gao, Zhenping Li, Anqi Zhuo, Yingxiao Zhao, Weibo Geng, Xiaosong Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21414v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 语义感知的 Text-to-SQL 数据合成框架
tldr: 针对现有Text-to-SQL合成方法中可执行性不等于语义正确性的问题，本文提出SemanticAgent框架。该框架通过分析器、合成器和验证器三个模块，实现了语义分析、逐步合成及诊断优化的三阶段流程。实验表明，SemanticAgent生成的合成数据在语义质量上显著优于前人方法，并能有效提升下游模型的微调性能，特别是在复杂语义场景下表现优异。
selection_source: fresh_fetch
motivation: 现有的Text-to-SQL合成流程往往混淆了可执行性与语义有效性，导致生成的查询虽然能运行但违反数据库语义。
method: 提出SemanticAgent框架，通过分析器、合成器和验证器三个专业模块，执行语义分析、逐步合成和诊断细化的三阶段协议。
result: 该框架生成的合成数据在语义质量评估中持续优于现有方法，并显著增强了下游模型在语义要求较高的基准测试中的微调表现。
conclusion: SemanticAgent将单纯的基于执行的验证转化为可追溯的推理过程，为高质量Text-to-SQL数据合成提供了有效的语义感知方案。
---

## 摘要
现有的 Text-to-SQL 合成流水线仍将可执行性与语义有效性混为一谈：语法检查和基于执行的验证可能会保留那些虽然能成功执行但违反数据库语义的查询。为了解决这些局限性，我们提出了 SemanticAgent，一个语义感知的合成框架。SemanticAgent 围绕三个专门的模块组织合成过程：分析器、合成器和验证器。通过语义分析、逐步合成和诊断细化的三阶段协议，SemanticAgent 将单纯的基于执行的验证转变为一个可追溯的推理过程。我们的框架生成的合成数据在语义质量评估下始终优于先前的合成方法，从而带来了更强的下游微调性能，特别是在对语义要求较高的基准测试中。

## Abstract
Existing text-to-SQL synthesis pipelines still conflate executability with semantic validity: syntactic checks and execution-based validation can retain queries that execute successfully while violating database semantics. To address these limitations, we propose SemanticAgent, a semantic-aware synthesis framework. SemanticAgent organizes synthesis around three specialized modules: an analyzer, a synthesizer, and a verifier. Through a three-stage protocol of semantic analysis, stepwise synthesis, and diagnostic refinement, SemanticAgent transforms execution-based validation alone into a traceable reasoning process. Our framework generates synthetic data that consistently outperforms prior synthesis methods under semantic-quality evaluation, leading to stronger downstream fine-tuning performance, especially on semantically demanding benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《SemanticAgent: A Semantics-Aware Framework for Text-to-SQL Data Synthesis》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 Text-to-SQL 数据合成方法（如基于模板或 LLM 生成）往往将“可执行性”等同于“语义正确性”。
*   **研究背景**：许多合成的 SQL 语句虽然能通过语法检查并在数据库中成功运行，但其逻辑可能违反业务语义（例如：对学校 ID 进行求平均值操作）。这种“语义无效”的数据会作为噪声干扰模型训练，尤其是在医疗、科学等对领域知识要求极高的场景下，获取高质量标注数据的成本极高。

### 2. 方法论：SemanticAgent 框架
该框架通过引入**显式语义监督**，将合成过程分为三个阶段，由三个专门的工具（Agent）协作完成：

*   **阶段一：多层级语义知识推断（分析器 DA）**
    *   从数据库 Schema 和实例中提取 6 个层级的知识：元数据、领域约束、字段类型、列语义、表约束及跨表关系。
    *   构建一个结构化的**业务知识库 (K)**，作为后续生成和验证的基准。
*   **阶段二：基于追踪的受控合成（合成器 DS）**
    *   根据领域上下文、分析任务类型和 SQL 复杂度（L1-L4 四个等级）进行受控生成。
    *   不仅生成“问题-SQL”对，还生成**推理链（Rationale/Trace）**，记录表选择、列操作及策略制定的逻辑。
*   **阶段三：诊断与细化（验证器 DT）**
    *   利用知识库 (K) 对生成的样本进行迭代检查。
    *   **诊断**：识别列误用、类型不匹配或连接错误。
    *   **修复**：根据检索到的正确语义证据对 SQL 和问题进行针对性修正，直至通过验证。

### 3. 实验设计
*   **数据集/场景**：
    *   **跨领域基准**：Spider 1.0, BIRD（知识密集型）, Spider 2.0-SQLite（企业级复杂度）。
    *   **领域特定**：EHRSQL（医疗）, ScienceBenchmark（科学）。
    *   **鲁棒性测试**：Spider-Syn, Spider-Realistic, Spider-DK。
*   **对比方法**：CodeS（基于模板）、SynQL（基于 AST 重组）、OmniSQL（基于 LLM 验证）。
*   **评估指标**：执行准确率 (EX)、测试套件准确率 (TS)、语义对齐度 (SA)、执行成功率 (SER) 及语义多样性。

### 4. 资源与算力
*   **合成模型**：使用 Qwen3-235B-A22B-Instruct 作为教师模型进行数据合成。
*   **下游模型**：使用 Qwen2.5-Coder (7B/14B) 和 Qwen3-14B 进行全参数微调。
*   **算力投入**：
    *   硬件：8 台 NVIDIA A800-SXM4-80GB GPU。
    *   时长：构建完整训练语料库约需 **2880 GPU 小时**，其中验证和细化阶段占总成本的 74.3%。
*   **数据规模**：最终生成约 10 万条高质量合成数据。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个不同的基准测试集上进行了广泛评估，涵盖了从简单到极高难度的 SQL 任务。
*   **公平性控制**：所有对比方法均在相同的训练预算、下游骨干模型和微调配置下进行，确保了结果的客观性。
*   **去重处理**：采用了 n-gram 过滤机制，剔除了与评估集重叠的合成样本（约占 0.76%），防止数据泄露。

### 6. 主要结论与发现
*   **性能提升**：SemanticAgent 生成的数据在所有基准测试中均优于基线。在 BIRD 上比 OmniSQL 提升了 2.6 个百分点，在 Spider 2.0 上提升了 3.4 个百分点。
*   **语义质量**：合成数据在语义多样性和结构复杂度上表现更好，生成的复杂 SQL 比例更高。
*   **规模效应**：随着合成数据量的增加，模型性能表现出良好的扩展性（Scaling Behavior），且在低资源阶段就展现出更高的效率。

### 7. 优点与亮点
*   **显式语义建模**：突破了以往仅依赖“执行反馈”的局限，通过构建业务知识库确保了逻辑的严密性。
*   **可追溯性**：引入推理链（Rationale），使合成过程不再是黑盒，方便诊断和修正错误。
*   **高难度场景表现优异**：在 BIRD 和 Spider 2.0 等极具挑战性的任务中进步显著，证明了其在处理复杂 Schema 时的优势。

### 8. 不足与局限
*   **计算成本高昂**：验证和迭代修复过程消耗了大量的 GPU 算力，在大规模应用时存在成本压力。
*   **教师模型依赖**：合成质量高度依赖于教师模型（如 Qwen3-235B），可能存在模型偏见或预训练阶段的知识污染。
*   **现实环境差异**：实验主要在学术基准集上完成，对于真实企业环境中存在的 Schema 噪声、方言差异及部署限制的覆盖尚不充分。
*   **评估局限**：主要依赖执行准确率，但在极少数情况下，错误的 SQL 仍可能碰巧产生正确的输出。

（完）

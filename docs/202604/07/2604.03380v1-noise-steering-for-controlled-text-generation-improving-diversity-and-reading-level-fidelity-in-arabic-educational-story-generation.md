---
title: "Noise Steering for Controlled Text Generation: Improving Diversity and Reading-Level Fidelity in Arabic Educational Story Generation"
title_zh: 噪声引导用于可控文本生成：提升阿拉伯语教育故事生成的多样性与阅读水平保真度
authors: "Haziq Mohammad Khalid, Salsabeel Shapsough, Imran Zualkernan"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03380v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 为阿拉伯语阅读评估生成多样化故事
tldr: 针对阿拉伯语低年级阅读评估中故事生成多样性与约束性难以平衡的问题，本文研究了“噪声引导”技术。通过在推理时向 Transformer 内部表示注入高斯扰动，在 5 个 7-9B 参数模型上验证了该方法。结果显示，相比高温度采样，内部噪声注入能显著提升叙事多样性，同时更好地保持阅读难度水平和生成质量，为受限教育内容的生成提供了更优方案。
selection_source: fresh_fetch
motivation: 在阿拉伯语教育故事生成中，需要在满足严格词汇和阅读难度约束的同时，避免因情节重复而影响评估的有效性。
method: 研究了在推理阶段向 Transformer 内部表示（如残差流）注入校准高斯扰动的“噪声引导”方法，并与高温度采样进行对比。
result: 残差流噪声注入在提升叙事多样性的同时，能有效维持低年级阅读水平和生成质量，而高温度采样常导致难度失控或模型崩溃。
conclusion: 内部表示层面的扰动比传统的输出层随机化更适合受限的教育内容生成，能更好地平衡多样性与任务约束。
---

## 摘要
为阿拉伯语低年级阅读评估生成多样化且符合教学要求的故事，需要在词汇、阅读水平和叙事结构的严格约束与避免削弱评估有效性的重复情节之间取得平衡。我们研究了噪声引导（noise steering），即在推理阶段向 Transformer 模型的内部表示中注入经过校准的高斯扰动，作为一种无需训练的多样化方法，并在五个以阿拉伯语为中心的小型语言模型（70-90亿参数）上进行了评估。我们将四种注入策略与高温采样基准进行了对比，衡量了多样性、质量、约束遵循度和阅读年级水平。结果表明，残差流噪声在所有模型中都能持续提高叙事多样性，且质量或约束成本极低，并能保持低年级阅读水平。注意力熵噪声注入（AENI）稳定了原本不可靠的注意力-对数（attention-logit）噪声，同时恢复了质量。高温采样会虚增阅读年级水平，并在多个模型上导致灾难性崩溃。我们发现，对于受限的教育内容生成，内部表示层级的扰动是比输出层级随机性更合适的多样化策略。

## Abstract
Generating diverse, pedagogically valid stories for Arabic early-grade reading assessments requires balancing tight constraints on vocabulary, reading level, and narrative structure against the need to avoid repetitive plots that undermine assessment validity. We investigate noise steering, injecting calibrated Gaussian perturbations into the internal representations of transformer models at inference time, as a training-free diversity method evaluated across five small Arabic-centric language models (7-9B parameters). We compare four injection strategies against high-temperature sampling baselines, measuring diversity, quality, constraint adherence, and reading grade level. Residual stream noise consistently improves narrative diversity with minimal quality or constraint cost and preserves early-grade reading level across all models. Attention entropy noise injection (AENI) stabilizes the otherwise unreliable attention-logit noise while recovering quality. High-temperature sampling inflates reading grade level and causes catastrophic collapse on several models. We find internal representation-level perturbation to be a more suitable diversity strategy than output-level stochasticity for constrained educational content generation.

---

## 论文详细总结（自动生成）

### 论文总结：噪声引导用于可控文本生成

#### 1. 核心问题与整体含义
本文针对**阿拉伯语低年级阅读评估（EGRA）**中的故事自动生成问题展开研究。核心挑战在于平衡**多样性**与**约束遵循度**：
*   **背景**：EGRA 故事需要满足极其严格的教学约束（如词汇量限制、特定阅读难度、叙事结构及文化中立性）。
*   **动机**：传统的大型语言模型在生成此类内容时，若追求多样性（如通过提高采样温度），往往会导致阅读难度失控或模型输出崩溃；若严格遵循约束，则会导致情节高度重复，削弱评估的有效性。
*   **核心问题**：如何在不进行模型微调的前提下，通过推理时的干预，提升小型语言模型（SLM）生成故事的多样性，同时保持其教学有效性和阅读水平。

#### 2. 方法论
论文提出了一种名为**“噪声引导”（Noise Steering）**的推理时技术，即在 Transformer 模型的内部表示中注入经过校准的高斯扰动。

*   **核心思想**：在模型内部表示层级（而非输出概率层级）引入随机性，以激发模型潜在的叙事创意，同时利用衰减机制确保生成的收敛性和约束遵循。
*   **关键技术细节**：
    1.  **注入位置**：
        *   **嵌入层噪声 (Embedding Noise)**：在 Token 嵌入查找后立即注入。
        *   **注意力对数噪声 (Attention-logit Noise)**：在自注意力子层输出后、MLP 之前注入。
        *   **残差流噪声 (L-RES)**：在整个 Transformer 块计算完成后注入（效果最稳健）。
        *   **注意力熵噪声注入 (AENI)**：一种自适应方法，根据注意力分布的“尖锐度”（Peakedness）动态调整噪声强度。当模型表现出过度自信（可能导致重复）时增加噪声。
    2.  **噪声校准 (RMS Calibration)**：由于不同模型的激活值尺度不同，论文通过计算模型残差流的均方根（RMS）来统一缩放噪声标准差，确保跨模型的可比性。
    3.  **噪声衰减 (Noise Decay)**：采用余弦衰减调度，在故事开头注入强噪声以引导创意，随着生成进行逐渐减弱噪声，以确保故事逻辑完备并遵循约束。

#### 3. 实验设计
*   **场景**：阿拉伯语 EGRA 故事生成（字数限制约 60 词，特定语法和结构要求）。
*   **模型 (Benchmarks)**：选取了 5 个 7B-9B 参数规模的阿拉伯语中心化模型：**Jais 8B、ALLaM 7B、Fanar 9B、AceGPT 8B** 以及多语言模型 **Phi-4-mini**。
*   **对比方法**：
    *   **基准 (Baseline)**：温度 T=1.0 的标准采样。
    *   **高温采样 (High-Temperature)**：T=1.8，结合 Top-k 或 Top-p 采样。
    *   **四种噪声策略**：EMBED、ATTN、AENI、L-RES。
*   **评估指标**：
    *   **多样性**：Vendi Score（基于嵌入的分布均匀度）、Self-BLEU。
    *   **质量**：LLM-as-a-judge（可读性、逻辑性、语法）。
    *   **约束遵循**：程序化检查（字数、词性）+ LLM 评估（叙事结构、性别平衡等）。
    *   **阅读水平**：Osman 可读性公式 + LLM 判定的年级水平。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   由于该方法属于**推理时干预（Inference-time intervention）**，不需要重新训练或微调模型，因此算力需求主要集中在批量推理生成（每种配置生成 50 个故事）和 LLM 评估上。

#### 5. 实验数量与充分性
*   **实验规模**：针对 5 个模型，对比了 7 种不同的解码/噪声策略。
*   **样本量**：每种配置独立生成 50 个故事，并使用固定随机种子以消除采样随机性的干扰。
*   **充分性**：实验涵盖了主流的阿拉伯语小型模型，并进行了**一元方差分析 (ANOVA)** 和 **Tukey HSD 事后检验**，以验证结果的统计显著性。
*   **客观性**：通过排除崩溃率过高（>40%）的配置，确保了对比的公平性，避免了因崩溃样本导致的指标虚高。

#### 6 主要结论与发现
*   **残差流噪声 (L-RES) 表现最优**：在所有模型中均能显著提升叙事多样性，且几乎不增加约束违反率，同时能完美保持低年级阅读水平。
*   **AENI 的稳定性**：相比原始的注意力噪声，AENI 通过自适应机制消除了模型崩溃现象，在多样性与稳定性之间取得了良好平衡。
*   **高温采样的缺陷**：高温采样虽然能增加多样性，但会导致阅读难度显著上升（例如从 3 年级水平漂移到 6 年级），并引发严重的模型崩溃（如 AceGPT 崩溃率达 84%）。
*   **内部扰动优于输出随机化**：在受限生成任务中，改变模型内部的“思考路径”比单纯改变最后的词频分布更能产生高质量且符合约束的变体。

#### 7. 优点
*   **无需训练**：作为一种插件式方法，极易部署到现有的预训练模型中。
*   **跨模型校准**：提出的 RMS 校准方法解决了不同架构模型对噪声敏感度不一的问题。
*   **教学保真度**：首次深入探讨了多样性干预对教育文本“阅读年级水平”的影响，具有很强的实际应用价值。

#### 8. 不足与局限
*   **LLM 评估偏差**：部分质量评估依赖于 GPT-5.3（模拟），可能存在偏好长文本或特定风格的偏见，缺乏真实人类教育专家的验证。
*   **模型规模限制**：实验仅限于 7B-9B 的小型模型，尚未验证该方法在超大规模模型（如 GPT-4 级别）上的表现。
*   **语言局限性**：虽然针对阿拉伯语设计，但其在其他具有复杂形态学特征的语言上的通用性仍需进一步验证。
*   **应用场景单一**：目前仅验证了 EGRA 故事生成，对于更长篇幅或非叙事类受限文本的效果未知。

（完）

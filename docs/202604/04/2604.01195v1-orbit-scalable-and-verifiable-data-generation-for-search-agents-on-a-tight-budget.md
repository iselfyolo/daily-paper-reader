---
title: "ORBIT: Scalable and Verifiable Data Generation for Search Agents on a Tight Budget"
title_zh: ORBIT：预算有限下搜索智能体的可扩展且可验证的数据生成
authors: "Nandan Thakur, Zijian Chen, Xueguang Ma, Jimmy Lin"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01195v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 用于生成2万个高推理强度训练查询的节俭框架
tldr: 本研究针对搜索智能体在处理复杂多步推理任务时面临的高质量训练数据匮乏且标注昂贵的问题，提出了ORBIT框架。该框架无需付费API，通过种子创建、问答生成、自我验证及外部搜索验证四个模块化阶段，生成了包含2万个具有可验证答案的推理密集型查询。实验表明，基于此数据集训练的Qwen3-4B模型在多项任务中表现强劲，证明了低成本合成数据在提升搜索智能体性能方面的巨大潜力。
selection_source: fresh_fetch
motivation: 解决搜索智能体在多步检索与推理任务中，高质量训练数据获取成本高昂且难以大规模验证的挑战。
method: 采用包含种子生成、问答对构建、自我验证及外部搜索验证的四阶段模块化框架，在低预算下生成2万条高质量数据。
result: 经过ORBIT数据集训练的Qwen3-4B模型在维基百科问答评估中，展现出超越同规模其他搜索智能体的强劲性能。
conclusion: 验证了通过可扩展且可验证的合成数据生成流程，能够有效且经济地提升轻量级语言模型在复杂搜索任务中的表现。
---

## 摘要
搜索智能体将语言模型（LM）与网络搜索相结合，在回答复杂用户查询方面正变得至关重要。由于昂贵的人工标注或繁琐的前提条件，为涉及多步检索和推理的深度研究任务构建训练数据集仍然具有挑战性。在这项工作中，我们介绍了 ORBIT，这是一个包含 2 万个推理密集型查询及其简短可验证答案的训练数据集，该数据集是使用一种不依赖付费 API 服务的节俭框架生成的。该模块化框架包含四个阶段：种子创建、问答对生成，以及两个验证阶段（自我验证和外部验证）。ORBIT 涵盖 15 个领域，每个训练对需要 4-5 个推理步骤，并需要通过完整网络进行外部搜索验证。我们使用 GRPO 在 ORBIT 上训练 Qwen3-4B 作为基础模型，并在维基百科问答任务上对其进行评估。广泛的实验结果表明，ORBIT-4B 在 4B 参数以下的搜索智能体大语言模型（LLM）中表现强劲，证明了合成数据集的效用。我们的框架、代码和数据集已开源并公开发布。

## Abstract
Search agents, which integrate language models (LMs) with web search, are becoming crucial for answering complex user queries. Constructing training datasets for deep research tasks, involving multi-step retrieval and reasoning, remains challenging due to expensive human annotation, or cumbersome prerequisites. In this work, we introduce ORBIT, a training dataset with 20K reasoning-intensive queries with short verifiable answers, generated using a frugal framework without relying on paid API services. The modular framework relies on four stages: seed creation, question--answer pair generation, and two stages of verification: self and external. ORBIT spans 15 domains and each training pair requires 4--5 reasoning steps, with external search verification required from the complete web. We train Qwen3-4B as the base model on ORBIT using GRPO and evaluate it on Wikipedia question answering tasks. Extensive experiment results demonstrate that ORBIT-4B achieves strong performance among sub-4B LLMs as search agents, proving the utility of synthetic datasets. Our framework, code and datasets are open-sourced and available publicly.

---

## 论文详细总结（自动生成）

以下是对论文《ORBIT: Scalable and Verifiable Data Generation for Search Agents on a Tight Budget》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的搜索智能体（Search Agents）在处理“深度搜索”（Deep Search）任务时，面临高质量训练数据匮乏的问题。深度搜索需要多步检索、复杂推理和信息聚合。
*   **研究背景**：
    *   **标注成本高**：人工标注复杂的多跳（multi-hop）查询及其可验证答案极其昂贵且耗时。
    *   **现有数据局限**：传统数据集（如 NQ, HotpotQA）推理深度不足；而现有的合成数据集往往依赖昂贵的付费 API（如 GPT-4）或复杂的前提条件（如构建知识图谱）。
*   **整体含义**：本文旨在提出一种**低成本（Frugal）**、可扩展且可验证的框架，用于生成高质量的复杂搜索查询数据，从而提升轻量级语言模型作为搜索智能体的推理能力。

### 2. 方法论：ORBIT 框架
该框架包含四个模块化阶段，核心思想是利用具有搜索能力的 LLM 生成“反向问题”（即给定答案和线索生成问题，使其难以直接回答但易于验证）：
1.  **种子创建（Seed Creation）**：
    *   确定 15 个领域（如科技、医学、法律、谜题等）。
    *   利用工具生成各领域的维基百科类别，并通过 API 获取相关页面标题作为初始种子。
2.  **问答对生成（QA Pair Generation）**：
    *   使用具有联网搜索功能的 **DeepSeek-V3.1**（通过自动化脚本调用其免费网页版）。
    *   基于种子生成推理密集型的问题和简短答案，要求问题必须包含多个事实线索，且需要 5 次以上搜索才能回答。
3.  **自我验证（Self-Verification）**：
    *   再次调用 DeepSeek-V3.1 对生成的问答对进行核查，要求其提供支持每个事实线索的 URL 引用。
    *   使用 Qwen3-4B 过滤掉证据不足或逻辑不符的样本。
4.  **外部验证（External Verification）**：
    *   **去偏处理**：为避免模型自洽性偏差，抓取引用网页的原始内容。
    *   **级联评审**：由 Qwen3-4B 和 GPT-OSS-120B 作为法官，根据抓取的网页内容独立回答问题，并与原始答案比对，仅保留一致的样本。

### 3. 实验设计
*   **训练设置**：使用 **GRPO**（群体相对策略优化）算法，以 Qwen3-4B 为基础模型进行强化学习训练。
*   **数据集/场景**：
    *   **训练集**：ORBIT（2万条）与 NQ、HotpotQA 按 1:1:1 比例混合。
    *   **评估基准**：
        *   单跳 QA：NQ, TriviaQA, PopQA。
        *   多跳 QA：HotpotQA, 2WikiMultihopQA, MuSiQue, Bamboogle。
        *   挑战性基准：FRAMES（长程推理）。
*   **对比方法**：Search-R1, InfoSeeker, ZeroSearch, AutoRefine 等（主要针对参数量小于 4B 的模型）。

### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA H100 GPU**。
*   **训练时长**：训练 160 个步骤，耗时约 **1-2 天**。
*   **数据生成耗时**：由于采用自动化脚本模拟网页交互以节省 API 费用，数据生成阶段持续了约 **2-3 个月**。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个主流维基百科问答基准和 1 个复杂推理基准（FRAMES）上进行了测试。
*   **消融实验**：
    *   测试了不同的数据集混合比例（如 1:1:1 vs 1:2:4），发现加入简单数据对小模型学习搜索逻辑至关重要。
    *   测试了搜索结果数量（Top-5 vs Top-10）对性能的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从简单到复杂的多种任务，并对比了同规模下的最新基线模型，客观展示了 ORBIT 数据集的增益。

### 6. 主要结论与发现
*   **性能卓越**：ORBIT-4B 在多跳 QA 任务上显著优于现有 4B 以下模型，平均 EM（精确匹配）得分提升了约 4.3 到 9.0 个百分点。
*   **合成数据的有效性**：证明了在不依赖昂贵 API 的情况下，通过多阶段验证生成的合成数据可以有效训练出具备深度搜索能力的轻量级模型。
*   **混合训练策略**：对于小参数模型，完全使用复杂数据并非最优，混合简单数据（NQ/HotpotQA）能帮助模型更好地收敛。

### 7. 优点
*   **低成本且开源**：框架不依赖付费 API，适合预算有限的学术研究，且数据集和代码全开源。
*   **高复杂度**：生成的查询平均需要 4-6 个推理步骤，远高于传统数据集。
*   **验证严谨**：通过自我验证和基于网页抓取的外部验证双重把关，确保了合成数据的真实性和可靠性。

### 8. 不足与局限
*   **生成效率低**：依赖网页自动化脚本（Selenium）导致数据采集周期长，且易受网页结构变化影响。
*   **搜索工具限制**：由于预算限制，训练和推理使用的是免费的 DDGS 搜索聚合器，其稳定性及结果质量可能不如付费的 Google/SerpAPI。
*   **模型规模限制**：目前仅在 4B 以下模型上验证，尚未探讨该数据集对超大规模模型（如 70B+）的进一步提升效果。
*   **环境依赖**：搜索结果具有时效性，网页内容的变动可能导致部分旧数据的验证失效。

（完）

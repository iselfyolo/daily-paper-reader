---
title: "Optimsyn: Influence-Guided Rubrics Optimization for Synthetic Data Generation"
title_zh: Optimsyn：用于合成数据生成的影响引导评分标准优化
authors: "Zhiting Fan, Ruizhe Chen, Tianxiang Hu, Ru Peng, Zenan Huang, Haokai Xu, Yixin Chen, Jian Wu, Junbo Zhao, Zuozhu Liu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00536v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 使用大语言模型优化合成数据生成
tldr: 针对高质量SFT数据在专业领域稀缺且合成数据评分准则难以人工优化的问题，本文提出了Optimsyn框架。该框架利用梯度信息评估合成数据对目标模型的训练效用（影响力分数），并以此作为奖励信号，通过强化学习自动优化评分准则生成器。实验证明，该方法在多个领域和模型上均能显著提升合成数据质量，具有较强的泛化能力。
selection_source: fresh_fetch
motivation: 传统的合成数据评分准则依赖专家手工设计且缺乏量化反馈，导致优化过程低效且难以跨领域迁移。
method: 提出一种基于影响力估计的优化框架，利用梯度信息量化合成样本对目标模型的贡献，并将其作为奖励通过强化学习优化评分准则。
result: 在人文、医学、法律等多个知识密集型领域的实验表明，该方法能持续提升下游任务性能，且无需针对特定任务进行微调。
conclusion: 通过将模型训练效用引入评分准则优化闭环，Optimsyn为高质量合成数据的自动化生成提供了一种有效且通用的解决方案。
---

## 摘要
大语言模型（LLMs）之所以能取得强大的下游性能，很大程度上归功于丰富的监督微调（SFT）数据。然而，在人文、社会科学、医学、法律和金融等知识密集型领域，高质量的 SFT 数据非常稀缺，因为专家策划成本高昂、隐私限制严格，且标签一致性难以保证。近期的研究工作通常使用合成数据，即通过提示生成器处理领域文档，并使用手工编写的评分标准（rubrics）过滤输出。然而，评分标准的设计依赖于专家，跨领域迁移性差，且通常通过一种脆弱的启发式循环进行优化：编写评分标准、合成数据、训练、检查结果以及手动猜测修订方案。这一过程缺乏关于评分标准如何影响下游性能的可靠定量反馈。我们提出根据合成数据在目标模型上的训练效用（training utility）对其进行评估，并利用该信号指导数据生成。受影响估计（influence estimation）的启发，我们采用了一种优化器感知（optimizer-aware）的估计器，利用梯度信息来量化每个合成样本对目标模型在特定任务目标上的贡献。我们的分析表明，即使合成样本和真实样本在嵌入空间中非常接近，它们对学习的影响也可能存在显著差异。基于这一见解，我们提出了一个基于优化的框架，利用目标模型的反馈来调整评分标准。我们提供轻量级的引导文本，并使用专门的评分标准模型来生成基于任务条件的评分标准。影响分数被用作奖励，通过强化学习来优化评分标准生成器。在不同领域、目标模型和数据生成器上的实验表明，该方法在无需特定任务微调的情况下，实现了持续的性能提升和强大的泛化能力。

## Abstract
Large language models (LLMs) achieve strong downstream performance largely due to abundant supervised fine-tuning (SFT) data. However, high-quality SFT data in knowledge-intensive domains such as humanities, social sciences, medicine, law, and finance is scarce because expert curation is expensive, privacy constraints are strict, and label consistency is hard to ensure. Recent work uses synthetic data, typically by prompting a generator over domain documents and filtering outputs with handcrafted rubrics. Yet rubric design is expert-dependent, transfers poorly across domains, and is often optimized through a brittle heuristic loop of writing rubrics, synthesizing data, training, inspecting results, and manually guessing revisions. This process lacks reliable quantitative feedback about how a rubric affects downstream performance. We propose evaluating synthetic data by its training utility on the target model and using this signal to guide data generation. Inspired by influence estimation, we adopt an optimizer-aware estimator that uses gradient information to quantify each synthetic sample's contribution to a target model's objective on specific tasks. Our analysis shows that even when synthetic and real samples are close in embedding space, their influence on learning can differ substantially. Based on this insight, we propose an optimization-based framework that adapts rubrics using target-model feedback. We provide lightweight guiding text and use a rubric-specialized model to generate task-conditioned rubrics. Influence score is used as the reward to optimize the rubric generator with reinforcement learning. Experiments across domains, target models, and data generators show consistent improvements and strong generalization without task-specific tuning.

---

## 论文详细总结（自动生成）

### Optimsyn: 影响引导的合成数据评分标准优化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在医学、法律、人文社科等知识密集型领域，高质量的监督微调（SFT）数据极度匮乏。现有的合成数据方法主要依赖专家手工设计的评分标准（Rubrics）来指导生成，但这存在两个致命缺陷：
    1.  **迁移性差**：针对某一领域设计的评分标准难以直接应用于其他领域。
    2.  **优化盲目**：传统的“设计-合成-训练-观察-修改”循环依赖人工直觉，缺乏关于数据对模型性能贡献的定量反馈。
*   **研究背景**：论文指出，即使合成数据在嵌入空间（Embedding Space）中与真实数据接近，它们对模型训练的实际影响（Influence）也可能大相径庭。因此，需要一种能够直接量化合成数据“训练效用”的方法来闭环指导数据生成。

#### 2. 论文提出的方法论
*   **核心思想**：将评分标准的构建视为一个可学习的策略，利用目标模型（Target Model）的梯度信息计算“影响力分数”（Influence Score）作为奖励信号，通过强化学习（RL）优化评分标准生成器（Prompter）。
*   **关键技术细节**：
    *   **优化器感知的影响力估计**：采用兼容 Adam 优化器的 TracIn 变体。通过计算合成样本的梯度更新方向与验证集梯度方向的余弦相似度，量化该样本对目标模型性能提升的贡献。
    *   **RL 优化框架**：
        1.  **Prompter（策略模型）**：根据种子文档生成特定领域的评分标准。
        2.  **Generator（生成器）**：根据评分标准和文档生成 QA 对。
        3.  **Reward（奖励）**：结合轻量级格式校验和影响力分数。
        4.  **算法**：使用 **GRPO**（Group Relative Policy Optimization）算法更新 Prompter，通过组内相对优势减少方差。
*   **流程说明**：种子文档 $\rightarrow$ Prompter 生成多组 Rubrics $\rightarrow$ Generator 生成 QA 对 $\rightarrow$ Target Model 计算影响力分数 $\rightarrow$ 奖励反馈 $\rightarrow$ 更新 Prompter。

#### 3. 实验设计
*   **实验场景与领域**：专注于**人文社科（HSS）**和**医学健康（Medical）**两个知识密集型领域。
*   **数据集/种子源**：
    *   **HSS**：来自 223 本高评分书籍的章节。
    *   **医学**：来自 Meditron 预训练语料和 PubMed 摘要。
*   **Benchmark（基准测试）**：
    *   **HSS**：MMLU-pro (History), SuperGPQA (History), HLE, BBH, HellaSwag, DROP。
    *   **医学**：MMLU-pro (Health), SuperGPQA (Health), HLE, HealthBench, PubMedQA, MedQA。
*   **对比方法**：
    *   **通用/合成数据**：WildChat, OpenHermes, Bonito, Condor, WebR Pro, Cosmopedia 等。
    *   **医学专用**：ChatDoctor, Medical-o1, Medical-R1-Distill, ReasonMed。

#### 4. 资源与算力
*   **硬件配置**：使用了 **8 × NVIDIA H200 GPUs**。
*   **训练时长**：处理约 20,000 个样本的 RL 优化过程大约需要 **10 小时**。
*   **模型规格**：
    *   **Generator**：Qwen3-235B-Instruct（默认），也测试了 GPT-4 和 Gemini。
    *   **Target Model**：Qwen3-8B-Base（默认），也测试了 4B、14B 及 Llama3-8B。
    *   **Prompter**：基于 Qwen3-8B-Instruct 初始化。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的广泛实验，包括：
    *   **主实验**：在两个大领域、12 个基准测试上对比了十余种 SFT 数据集。
    *   **泛化性实验**：验证了方法在不同模型规模（4B/8B/14B）和不同家族（Qwen/Llama）之间的有效性。
    *   **鲁棒性实验**：更换不同的 Generator（GPT-4, Gemini）均取得提升。
    *   **消融实验**：分析了验证集大小、采样策略、RL 组大小（G）对结果的影响。
*   **评价**：实验设计非常充分且严谨，通过跨模型、跨领域的验证，证明了该框架的普适性。同时，通过去重（Decontamination）检查确保了结果的客观性。

#### 6. 论文的主要结论与发现
*   **影响力分数的有效性**：实验证实影响力分数与下游任务的准确率存在强正相关，是比嵌入空间距离更可靠的数据质量指标。
*   **性能超越**：Optimsyn 生成的数据微调后的 8B 模型，在多个指标上不仅超过了现有的开源 SFT 数据集，甚至超越了官方的 Instruct 版本和部分经过 o1/R1 蒸馏的专用模型。
*   **评分标准的演变**：经过 RL 优化后，Prompter 生成的评分标准从通用的指令（如“简洁”）转向了领域相关的深度属性（如“逻辑严密性”、“信息密度”）。

#### 7. 优点（亮点）
*   **闭环自动化**：将原本依赖专家的 Rubric 设计转变为模型驱动的自动化优化过程。
*   **模型感知（Model-aware）**：生成的合成数据是针对特定目标模型的优化需求“量身定制”的。
*   **高效性**：相比于盲目增加合成数据量，该方法通过提升单样本质量（影响力），在较小的数据规模下实现了更高的性能。

#### 8. 不足与局限
*   **信用分配（Credit Assignment）难题**：奖励信号是从 Target Model 反馈给 Prompter 的，中间隔了一个 Generator 的生成步骤，梯度无法直接回传，这可能导致 RL 训练存在较高的方差和不稳定性。
*   **计算开销**：虽然比人工标注便宜，但计算影响力分数需要频繁获取模型梯度，且 RL 阶段需要多次采样生成，对算力仍有一定要求。
*   **验证集依赖**：影响力分数的计算依赖于一个高质量的验证集，如果验证集本身存在偏差，可能会引导 Prompter 走向错误的方向。

（完）

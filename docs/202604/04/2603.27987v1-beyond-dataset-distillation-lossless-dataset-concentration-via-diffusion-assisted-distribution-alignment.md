---
title: "Beyond Dataset Distillation: Lossless Dataset Concentration via Diffusion-Assisted Distribution Alignment"
title_zh: 超越数据集蒸馏：通过扩散辅助分布对齐实现无损数据集浓缩
authors: "Tongfei Liu, Yufan Liu, Bing Li, Weiming Hu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27987v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 通过扩散辅助分布对齐合成紧凑的代理数据集
tldr: 针对大规模视觉识别中数据集成本高的问题，本文提出了数据集浓缩（DsCo）框架。研究首先从理论上证明了数据集蒸馏等同于分布匹配，并揭示了其效率极限。随后，通过基于扩散模型的噪声优化（NOpt）合成代表性样本，并结合“掺杂”策略引入原始样本以突破效率瓶颈。该方法在低数据量下达到SOTA，在高数据量下能将数据集缩小一半且无性能损失，且支持无数据场景。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的数据集蒸馏方法缺乏理论依据，且在大规模数据扩展和无数据场景下表现不佳。
method: 建立分布匹配理论框架，提出基于扩散模型的噪声优化合成方法，并结合“掺杂”策略混合原始样本以提升效率。
result: 在低数据量下取得SOTA性能，并在高数据量场景下实现了近乎无损的二倍数据集压缩。
conclusion: DsCo框架通过理论创新与混合合成策略，有效解决了数据集蒸馏的效率与适用性难题，实现了高效的数据集浓缩。
---

## 摘要
大型数据集带来的高昂成本和可访问性问题阻碍了大规模视觉识别系统的发展。数据集蒸馏（Dataset Distillation）通过合成紧凑的代理数据集来解决这些问题，从而实现高效的训练、存储、传输和隐私保护。现有的基于扩散模型的最先进数据集蒸馏方法面临三个问题：缺乏理论依据、在大数据量下的扩展效率低下，以及在无数据场景下失效。为了解决这些问题，我们建立了一个理论框架，通过证明数据集蒸馏与分布匹配之间的等价性，为扩散模型的使用提供了理论依据，并揭示了数据集蒸馏范式中固有的效率限制。随后，我们提出了一个数据集浓缩（Dataset Concentration, DsCo）框架，该框架利用基于扩散模型的噪声优化（Noise-Optimization, NOpt）方法来合成一小组具有代表性的样本，并可选择性地通过“掺杂”（Doping）策略来增强合成数据，即通过将原始数据集中的精选样本与合成样本混合，以克服数据集蒸馏的效率限制。DsCo 适用于数据可访问和无数据两种场景，在低数据量下达到了最先进（SOTA）的性能，并且能够很好地扩展到高数据量场景，在不降低性能的情况下将数据集规模缩减了近一半。

## Abstract
The high cost and accessibility problem associated with large datasets hinder the development of large-scale visual recognition systems. Dataset Distillation addresses these problems by synthesizing compact surrogate datasets for efficient training, storage, transfer, and privacy preservation. The existing state-of-the-art diffusion-based dataset distillation methods face three issues: lack of theoretical justification, poor efficiency in scaling to high data volumes, and failure in data-free scenarios. To address these issues, we establish a theoretical framework that justifies the use of diffusion models by proving the equivalence between dataset distillation and distribution matching, and reveals an inherent efficiency limit in the dataset distillation paradigm. We then propose a Dataset Concentration (DsCo) framework that uses a diffusion-based Noise-Optimization (NOpt) method to synthesize a small yet representative set of samples, and optionally augments the synthetic data via "Doping", which mixes selected samples from the original dataset with the synthetic samples to overcome the efficiency limit of dataset distillation. DsCo is applicable in both data-accessible and data-free scenarios, achieving SOTA performances for low data volumes, and it extends well to high data volumes, where it nearly reduces the dataset size by half with no performance degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Dataset Distillation: Lossless Dataset Concentration via Diffusion-Assisted Distribution Alignment》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视觉识别系统规模的扩大，大型数据集带来了高昂的存储、传输成本及隐私风险。**数据集蒸馏（Dataset Distillation, DD）**旨在合成极小的代理数据集来保留原始数据信息。
然而，现有基于扩散模型的方法存在三大痛点：
*   **缺乏理论支撑**：使用扩散模型合成代理数据的动机多基于启发式经验，缺乏严谨的数学证明。
*   **高 IPC 扩展性差**：当每个类别合成样本数（IPC）增加时，合成成本激增且边际收益递减，难以实现“无损”压缩。
*   **无数据场景失效**：大多数方法依赖对原始数据的直接访问，无法处理隐私受限的无数据（Data-free）场景。

### 2. 论文提出的方法论：DsCo 框架
论文提出了**数据集浓缩（Dataset Concentration, DsCo）**框架，核心思想是通过理论指导的分布对齐和混合采样策略。

*   **理论基础**：建立“记忆-泛化”模型，证明了在特定条件下，数据集蒸馏等价于**分布匹配问题**。这为使用扩散模型（本质是分布对齐工具）提供了理论合法性。
*   **噪声优化（Noise-Optimization, NOpt）**：
    *   识别出扩散模型在去噪过程中存在“随机采样偏差”。
    *   通过优化去噪每一步的噪声张量（$\epsilon$），而非仅仅微调模型或引导函数，来强制实现步骤级的分布对齐。
    *   引入**现实约束（Reality Constraint）**确保优化后的噪声仍符合高斯分布，避免生成伪影。
    *   使用**随机特征投影**（随机初始化的 CNN）来提取平移不变特征，减少空间冗余干扰。
*   **掺杂策略（Doping）与触发器**：
    *   理论发现：数据集中存在“远距离样本（Far-apart samples）”，合成这些样本的效率极低。
    *   **Dope Trigger**：当增加合成样本的边际收益低于直接采样时，触发“掺杂”。
    *   **Doping**：利用模型对原始数据的“困惑度分数（Confusion Score）”挑选最具代表性的真实样本，与合成样本混合，从而突破合成数据的效率瓶颈。

### 3. 实验设计
*   **数据集**：ImageNet-1K（大规模）、ImageNette 和 ImageWoof（ImageNet 的子集，分别代表易分类和难分类场景）。
*   **Benchmark 与对比方法**：
    *   **低 IPC 场景**：对比了 DM, IDC, SRe²L, Minimax, IGD, OT 等主流蒸馏方法。
    *   **高 IPC 场景**：由于前人很少做高 IPC，主要与随机采样（Random Sampling）对比。
    *   **无数据场景**：与 SRe²L 等数据无关方法对比。
*   **评估指标**：在 ConvNet-6, ResNet-18/34/101, MobileNet-V2, EfficientNet-B0 等多种架构上的 Top-1 分类准确率。

### 4. 资源与算力
*   **硬件设备**：论文明确指出使用了 **NVIDIA GeForce RTX 2080 Ti** GPU。
*   **算力优势**：
    *   DsCo 具有极高的显存效率，峰值显存仅需 **4.3G**（相比之下，Minimax 需要 14.8G，IGD 需要 13.2G），这意味着它可以在消费级显卡上运行。
    *   **耗时**：在 ImageNet-1k (IPC 50) 任务中，总耗时（准备+生成）约为 76 小时，显著优于 IGD 的 471 小时和 Minimax 的 288 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从极低 IPC（10, 50）到极高 IPC（700, 1000, 1300）的完整区间。
*   **消融实验**：非常充分。分别针对数据访问/无数据场景下的噪声优化组件（对齐损失、现实约束、统计正则化等）进行了独立验证。
*   **公平性**：使用了统一的预训练扩散模型（DiT）和 VAE 架构，并对对比方法进行了复现或引用了相同设置下的报告值，实验设计客观。

### 6. 主要结论与发现
*   **无损压缩**：DsCo 在 ImageNet-1k 上实现了**近乎无损的 50% 压缩**（在 700 IPC 时，ResNet-18 达到 69.0% 准确率，与全量数据持平）。
*   **SOTA 性能**：在低 IPC 场景下，DsCo 在多个数据集和架构上均刷新了最先进记录。
*   **无数据场景突破**：在无法访问原始数据时，其性能显著超过了现有的无数据蒸馏方法（如在 ImageNet-1k IPC 50 下比 SRe²L 高出 11.4%）。
*   **跨架构泛化**：合成的数据集在未参与合成过程的架构（如 DenseNet）上依然表现稳健。

### 7. 优点（亮点）
*   **理论深度**：不仅提出了算法，还通过数学推导解释了为什么扩散模型有效，以及为什么纯合成数据在高 IPC 下会遇到瓶颈。
*   **实用性强**：低显存需求使其更易于普及；“掺杂”策略巧妙地结合了生成式和判别式数据的优势。
*   **灵活性**：一套框架同时兼容“有数据”和“无数据”两种极端工业需求。

### 8. 不足与局限
*   **依赖预训练模型**：DsCo 的效果高度依赖于预训练扩散模型（如 DiT）的质量。如果预训练模型本身存在类别偏差，合成数据也会继承这种偏差。
*   **困惑度计算成本**：在高 IPC 的 Doping 阶段，需要先用合成数据训练一个模型来计算原始数据的困惑度，这增加了一定的计算流水线复杂度。
*   **超参数敏感度**：虽然论文称其鲁棒，但 $\lambda_{align}$ 等超参数在不同分布的数据集上可能仍需微调以达到最优效果。

（完）

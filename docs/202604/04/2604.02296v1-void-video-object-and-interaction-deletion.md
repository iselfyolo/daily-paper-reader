---
title: "VOID: Video Object and Interaction Deletion"
title_zh: VOID：视频物体与交互删除
authors: "Saman Motamed, William Harvey, Benjamin Klein, Luc Van Gool, Zhuoning Yuan, Ta-Ying Cheng"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02296v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 生成新的反事实物体移除配对数据集
tldr: 针对现有视频物体移除方法在处理复杂物理交互（如碰撞）时表现不佳的问题，本文提出了VOID框架。该框架通过构建包含因果关系的合成数据集，并结合视觉语言模型识别受影响区域，引导视频扩散模型生成符合物理规律的反事实补全结果。实验证明，VOID在保持场景动力学一致性方面优于现有方法，为提升视频编辑模型的物理模拟能力提供了新思路。
selection_source: fresh_fetch
motivation: 现有方法仅能修复外观层面的伪影，无法修正物体移除后本应改变的物理交互（如碰撞导致的运动轨迹变化）。
method: 通过构建反事实交互数据集，并利用视觉语言模型识别受影响区域来引导视频扩散模型生成物理一致的补全内容。
result: 在合成和真实场景下的实验均表明，该方法在移除物体后能更有效地维持场景动力学的逻辑连贯性。
conclusion: VOID框架证明了结合高层因果推理可以使视频编辑模型更好地模拟现实世界的物理规律。
---

## 摘要
现有的视频物体移除方法擅长修复物体“背后”的内容，并修正阴影和反射等外观层面的伪影。然而，当被移除的物体具有更显著的交互作用（例如与其他物体发生碰撞）时，现有模型无法对其进行修正，从而产生不合理的结果。我们提出了 VOID，这是一个旨在这些复杂场景下执行符合物理规律的修复的视频物体移除框架。为了训练该模型，我们利用 Kubric 和 HUMOTO 生成了一个新的反事实物体移除配对数据集，其中移除一个物体需要改变后续的物理交互。在推理过程中，视觉语言模型会识别受被移除物体影响的场景区域。随后，这些区域被用于引导视频扩散模型，生成物理一致的反事实结果。在合成数据和真实数据上的实验表明，与先前的视频物体移除方法相比，我们的方法在移除物体后能更好地保持一致的场景动力学。我们希望这一框架能为如何通过高层因果推理使视频编辑模型成为更好的世界模拟器提供启示。

## Abstract
Existing video object removal methods excel at inpainting content "behind" the object and correcting appearance-level artifacts such as shadows and reflections. However, when the removed object has more significant interactions, such as collisions with other objects, current models fail to correct them and produce implausible results. We present VOID, a video object removal framework designed to perform physically-plausible inpainting in these complex scenarios. To train the model, we generate a new paired dataset of counterfactual object removals using Kubric and HUMOTO, where removing an object requires altering downstream physical interactions. During inference, a vision-language model identifies regions of the scene affected by the removed object. These regions are then used to guide a video diffusion model that generates physically consistent counterfactual outcomes. Experiments on both synthetic and real data show that our approach better preserves consistent scene dynamics after object removal compared to prior video object removal methods. We hope this framework sheds light on how to make video editing models better simulators of the world through high-level causal reasoning.

---

## 论文详细总结（自动生成）

### VOID: 视频物体与交互删除 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的视频物体移除方法（如视频补全/Inpainting）主要关注于填充物体背后的背景，或修正阴影、反射等外观层面的伪影。然而，现实世界中的物体存在复杂的**因果物理交互**。例如，移除正在倒下的多米诺骨牌中间的一块，后续的骨牌理应停止倒下；移除支撑物，上方的物体理应下落。
现有模型由于缺乏物理常识和高层因果推理能力，往往只能机械地填充像素，导致生成物理上不可能的场景。本论文提出了 **VOID (Video Object and Interaction Deletion)** 框架，旨在实现“反事实”的视频编辑：不仅移除物体，还要模拟该物体消失后对整个场景动力学产生的连锁反应。

#### 2. 方法论
VOID 框架结合了数据驱动的模拟、视觉语言模型（VLM）的推理以及扩散模型的生成能力：
*   **反事实数据集构建**：利用 **Kubric**（刚体动力学）和 **HUMOTO**（人体-物体交互）仿真引擎，生成了约 6400 对“有物体”与“无物体”的配对视频。通过重新模拟物理过程，获取物体移除后真实的物理演变作为监督信号。
*   **交互感知四色掩码 (Quadmask)**：在传统三色掩码（物体、受影响区、背景）基础上增加第四种颜色，专门标注“被移除物体”与“受影响区域”的重叠部分，为模型提供更精确的空间引导。
*   **VLM 引导的推理流程**：在推理阶段，利用 VLM（如 Gemini 3 Pro）分析视频，识别受影响的物体及其潜在的反事实运动轨迹，自动生成 Quadmask。
*   **两阶段生成策略**：
    *   **第一阶段**：生成初步的反事实轨迹。
    *   **第二阶段（可选）**：针对大幅度运动导致的物体变形，利用**光流扭曲噪声 (Flow-Warped Noise)** 进行细化，增强物体的结构一致性和刚性。
*   **模型架构**：基于 **CogVideoX (5B)** 扩散变压器（DiT）架构进行微调。

#### 3. 实验设计
*   **数据集**：
    *   **真实场景**：收集了 75 个涉及碰撞、支撑移除、复杂交互的真实视频。
    *   **合成场景**：40 个来自 Kubric、HUMOTO 及前人研究的合成视频。
*   **对比方法**：
    *   **视频补全类**：ProPainter, DiffuEraser, ROSE, MiniMax-Remover。
    *   **层分解类**：Generative Omnimatte。
    *   **通用视频编辑类**：Runway (Gen-3)。
*   **评估指标**：
    *   **主观评估**：25 人参与的人类偏好研究。
    *   **VLM 评测**：使用 Gemini 3 Pro, GPT-5.2, Qwen 3.5 作为裁判，从物理交互、物体移除、背景质量等 6 个维度打分。
    *   **客观指标**：PSNR, LPIPS, DreamSim, DINOv2, FVD。

#### 4. 资源与算力
*   **模型规模**：使用了 50 亿参数（5B）的 CogVideoX 作为骨干网络。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到其初始化权重来自 Generative Omnimatte 的公开权重，并在合成数据集上进行了进一步微调。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 115 个测试案例（75 真实 + 40 合成），在视频编辑领域属于标准规模。
*   **消融实验**：针对数据集组成（仅 Kubric、仅 HUMOTO、混合数据）、掩码策略（三色 vs 四色）以及第二阶段细化进行了详细对比。
*   **公平性**：对比实验中，为不同模型提供了其最擅长的输入格式（如为 Runway 提供详细的文本描述，为补全模型提供掩码），确保了对比的客观性。

#### 6. 主要结论与发现
*   **物理一致性**：VOID 在处理涉及重力、碰撞和支撑关系的场景时，表现显著优于现有所有模型。
*   **泛化能力**：尽管只在合成数据上训练，VOID 能泛化到未见过的真实效果，如气球在人松手后飘走、搅拌机在人离开后停止转动。
*   **VLM 的关键作用**：VLM 提供的语义理解和空间预测是实现复杂反事实编辑的核心，弥补了扩散模型在逻辑推理上的不足。

#### 7. 优点
*   **因果推理**：率先将视频物体移除从“像素填充”提升到了“物理模拟”的高度。
*   **数据质量**：通过物理引擎生成配对数据，解决了真实世界中缺乏反事实 Ground Truth 的难题。
*   **鲁棒性**：两阶段生成策略有效缓解了视频生成中常见的物体变形问题。

#### 8. 不足与局限
*   **生成长度与分辨率**：受限于底座模型，生成的视频长度仅为几秒，且分辨率有待进一步提高。
*   **视角敏感性**：在极端相机角度或物体距离过近时，模型表现会有所下降。
*   **推理成本**：依赖 VLM 进行掩码生成和两阶段扩散推理，计算开销较大，难以实现实时编辑。
*   **领域偏差**：虽然有泛化，但在完全脱离训练分布的极端物理现象上仍可能失效。

（完）

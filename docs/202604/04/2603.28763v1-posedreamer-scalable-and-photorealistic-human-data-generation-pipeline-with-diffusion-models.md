---
title: "PoseDreamer: Scalable and Photorealistic Human Data Generation Pipeline with Diffusion Models"
title_zh: PoseDreamer：基于扩散模型的可扩展且具有照片级真实感的人体数据生成管线
authors: "Lorenza Prospero, Orest Kupyn, Ostap Viniavskyi, João F. Henriques, Christian Rupprecht"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28763v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 利用扩散模型生成大规模合成数据集
tldr: 针对3D人体网格估计中真实数据标注难、传统合成数据真实感差的问题，本文提出PoseDreamer框架。该方法利用扩散模型生成大规模带3D标注的合成数据集，通过可控生成、DPO对齐、难样本挖掘及质量过滤，生成了50万张高质量样本。实验证明其在图像质量和模型训练效果上均优于传统合成数据，且能与现有数据集形成良好互补。
selection_source: fresh_fetch
motivation: 现有的3D人体数据集面临真实数据标注困难、规模有限，以及传统渲染合成数据缺乏真实感和多样性的双重挑战。
method: 提出一种结合可控扩散模型、DPO控制对齐、课程学习难样本挖掘及多阶段质量过滤的自动化数据生成流水线。
result: "生成了超过50万个高保真样本，图像质量较渲染数据提升76%，训练出的模型性能达到或超过了使用真实或传统合成数据的水平。"
conclusion: PoseDreamer证明了利用生成式模型构建大规模、高真实感且带精确3D标注的人体数据集的可行性与优越性。
---

## 摘要
由于深度歧义以及从单目图像标注 3D 几何结构的固有难度，获取用于 3D 人体网格估计的有标注数据集具有挑战性。现有的数据集要么是真实的，具有人工标注的 3D 几何结构且规模有限；要么是合成的，由 3D 引擎渲染而成，虽然提供了精确的标签，但存在照片级真实感不足、多样性低以及制作成本高的问题。在这项工作中，我们探索了第三条路径：生成数据。我们介绍了 PoseDreamer，这是一个利用扩散模型生成带有 3D 网格标注的大规模合成数据集的新型管线。我们的方法将可控图像生成与用于控制对齐的直接偏好优化（Direct Preference Optimization）、基于课程学习的难样本挖掘以及多阶段质量过滤相结合。这些组件共同作用，自然地保持了 3D 标签与生成图像之间的对应关系，同时优先处理具有挑战性的样本，以最大化数据集的效用。利用 PoseDreamer，我们生成了超过 500,000 个高质量合成样本，与基于渲染的数据集相比，在图像质量指标上实现了 76% 的提升。在 PoseDreamer 上训练的模型达到了与在真实世界和传统合成数据集上训练的模型相当或更优的性能。此外，将 PoseDreamer 与合成数据集结合使用，其性能优于将真实世界数据集与合成数据集结合，证明了我们数据集的互补性。我们将发布完整的数据集和生成代码。

## Abstract
Acquiring labeled datasets for 3D human mesh estimation is challenging due to depth ambiguities and the inherent difficulty of annotating 3D geometry from monocular images. Existing datasets are either real, with manually annotated 3D geometry and limited scale, or synthetic, rendered from 3D engines that provide precise labels but suffer from limited photorealism, low diversity, and high production costs. In this work, we explore a third path: generated data. We introduce PoseDreamer, a novel pipeline that leverages diffusion models to generate large-scale synthetic datasets with 3D mesh annotations. Our approach combines controllable image generation with Direct Preference Optimization for control alignment, curriculum-based hard sample mining, and multi-stage quality filtering. Together, these components naturally maintain correspondence between 3D labels and generated images, while prioritizing challenging samples to maximize dataset utility. Using PoseDreamer, we generate more than 500,000 high-quality synthetic samples, achieving a 76% improvement in image-quality metrics compared to rendering-based datasets. Models trained on PoseDreamer achieve performance comparable to or superior to those trained on real-world and traditional synthetic datasets. In addition, combining PoseDreamer with synthetic datasets results in better performance than combining real-world and synthetic datasets, demonstrating the complementary nature of our dataset. We will release the full dataset and generation code.

---

## 论文详细总结（自动生成）

以下是对论文《PoseDreamer: Scalable and Photorealistic Human Data Generation Pipeline with Diffusion Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：获取高质量的 3D 人体网格估计（HMR）标注数据极具挑战。
    *   **真实数据**：受限于深度歧义和单目图像标注难度，规模小且标注精度有限。
    *   **渲染合成数据**：虽然 3D 标签精确，但缺乏照片级的真实感（Domain Gap），且构建多样化场景的 3D 资产成本极高。
*   **研究动机**：探索“生成数据”作为第三条路径。利用扩散模型（Diffusion Models）强大的生成能力，构建一个既具有精确 3D 标注，又具备高度真实感和场景多样性的自动化数据生成管线。

### 2. 方法论
PoseDreamer 采用“逆向生成”策略：先确定 3D 参数，再生成对应图像。
*   **标签生成**：从两个来源获取 SMPL-X 姿态参数：
    *   **LAION**：提供真实世界的场景多样性，通过视觉语言模型（Gemma）生成描述，并用现有模型提取伪标签。
    *   **AMASS**：提供复杂的运动捕捉数据，弥补真实图像中缺乏的动态/极端姿态。
*   **可控图像生成**：
    *   **空间编码**：改进了 PNCC（位置归一化颜色代码）方案，将 3D 网格的 X,Y,Z 坐标映射为 RGB 颜色，为扩散模型提供更丰富的空间线索。
    *   **DPO 控制对齐**：引入直接偏好优化（Direct Preference Optimization），利用 2D 关键点相似度（OKS）作为奖励信号，微调控制模型，确保生成的图像与 3D 标签严格对齐。
*   **难样本挖掘（Hard Sample Mining）**：采用两阶段课程学习。首先训练一个回归器预测样本难度（OKS 分数），然后优先生成预测得分较低（即模型难以处理）的挑战性姿态。
*   **多阶段过滤**：通过 YOLO 检测（过滤拥挤场景）、OKS 评估（过滤姿态不匹配）和 VGGHeads（过滤头部姿态不一致）确保数据质量。

### 3. 实验设计
*   **数据集与场景**：生成了 50 万张高质量合成图像（PoseDreamer 数据集）。
*   **Benchmark**：在 AGORA（合成）、UBody（真实）、EgoBody（第一视角）、3DPW（户外）和 EHF（实验室）等多个主流基准上进行评估。
*   **对比方法**：对比了 BEDLAM、SynBody、GTA-Human II 等基于渲染的合成数据集，以及 MSCOCO、InstaVariety 等真实数据集。
*   **评估指标**：PVE（顶点误差）、MPJPE（关节误差）、IS（起始分数）和 FID（图像真实感指标）。

### 4. 资源与算力
*   **控制模型对齐（DPO）**：在 4 张 NVIDIA A6000 GPU 上训练了 2 天。
*   **数据生成**：总计消耗约 **6,700 GPU 小时**（用于生成 80 万张初始图像并过滤至 50 万张）。
*   **模型训练**：使用了 SMPLer-X-S（小模型）和 SMPLer-X-L（大模型/ViT-L）作为骨干网络进行验证。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的跨数据集泛化实验，涵盖了从单数据集训练到多数据集混合训练的对比。
*   **消融实验**：针对数据过滤、AMASS 数据引入、难样本挖掘等关键组件进行了详细的消融分析。
*   **扩展性实验**：验证了模型规模（从 ViT-S 到 ViT-L）和特定领域（如瑜伽姿态生成）的适应性。
*   **充分性评价**：实验设计非常全面，不仅关注下游任务性能，还通过 FID/IS 指标客观评估了图像生成的质量，实验对比具有较高的公平性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：仅用 50 万张 PoseDreamer 数据训练的模型，性能即可媲美或超越使用 95 万张昂贵渲染数据（BEDLAM）训练的模型。
*   **真实感极强**：在图像质量指标上，PoseDreamer 比传统渲染数据集提升了 **76%**，显著缩小了模拟与现实的域差异。
*   **互补性**：PoseDreamer 与渲染数据集结合使用时，效果优于“真实+渲染”的组合，证明了生成数据在多样性和真实感上的独特价值。
*   **领域定制**：证明了该管线可以轻松生成特定领域（如瑜伽）的数据，显著提升模型在特定场景下的表现。

### 7. 优点（亮点）
*   **高效率与可扩展性**：无需昂贵的 3D 资产建模，仅需扩散模型即可无限扩展数据规模。
*   **创新的对齐机制**：首次将 DPO 应用于人体姿态的可控生成对齐，有效解决了扩散模型生成人体时常见的“幻觉”和姿态偏移问题。
*   **数据质量高**：结合了难样本挖掘和严格过滤，确保了每一条生成数据都具有高学习价值。

### 8. 不足与局限
*   **标签精度微差**：尽管经过对齐，生成数据的标签精度在理论上仍略逊于物理引擎渲染的绝对精确值。
*   **生成伪影**：在处理极端复杂交互（如多人缠绕）或特定视角时，仍可能出现肢体畸变或“漂浮”等物理不合理现象。
*   **基础模型偏差**：生成质量受限于底层扩散模型（如 FLUX），可能继承预训练数据中的人口统计学偏差。

（完）

---
title: Multi-Agent Dialectical Refinement for Enhanced Argument Classification
title_zh: 用于增强型论辩分类的多智能体辩证优化
authors: "Jakub Bąba, Jarosław A. Chudziak"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27451v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 用于自动写作评估的多智能体辩论
tldr: "针对论辩挖掘中传统监督学习微调成本高以及大模型单智能体易产生自我强化错误的问题，本文提出MAD-ACC框架。该框架采用多智能体辩论机制（支持者-反对者-裁判），通过辩证推理解决论点与前提等组件的分类歧义。实验表明，该方法在UKP学生论文数据集上达到85.7%的Macro F1值，显著优于单智能体基准，且无需领域特定训练，同时提供了高可解释性的辩论记录。"
selection_source: fresh_fetch
motivation: 传统监督学习微调成本高，且大模型在处理结构模糊的论辩组件分类时容易出现单智能体自我强化错误的现象。
method: 提出MAD-ACC框架，利用“支持者-反对者-裁判”的多智能体辩论模式，通过对冲突解释的辩证博弈来消除分类不确定性。
result: "在UKP Student Essays语料库上实现了85.7%的Macro F1分数，在无需领域特定训练的情况下显著超越了单智能体推理基准。"
conclusion: 多智能体辩论机制不仅提升了论辩组件分类的准确性，还通过生成可读的辩论记录提供了透明且可解释的决策过程。
---

## 摘要
论辩挖掘 (Argument Mining, AM) 是自动写作评估的一项基础技术，但传统的监督方法严重依赖昂贵的、特定领域的微调。虽然大语言模型 (LLMs) 提供了一种无需训练的替代方案，但它们往往难以应对结构歧义，无法有效区分主张 (Claims) 和前提 (Premises) 等相似组件。此外，单智能体自我修正机制通常存在“阿谀奉承” (sycophancy) 问题，即模型倾向于强化其初始错误，而非对其进行批判性评估。我们提出了 MAD-ACC (Multi-Agent Debate for Argument Component Classification)，这是一个利用辩证优化来解决分类不确定性的框架。MAD-ACC 采用“支持者-反对者-裁判”模型，通过智能体对歧义文本的冲突解释进行辩护，揭示单智能体模型容易忽略的逻辑细微差别。在 UKP Student Essays 语料库上的评估表明，MAD-ACC 实现了 85.7% 的 Macro F1 分数，在无需特定领域训练的情况下，显著优于单智能体推理基准。此外，与“黑盒”分类器不同，MAD-ACC 的辩证方法通过生成人类可读的辩论记录来解释决策背后的推理过程，提供了一种透明且可解释的替代方案。

## Abstract
Argument Mining (AM) is a foundational technology for automated writing evaluation, yet traditional supervised approaches rely heavily on expensive, domain-specific fine-tuning. While Large Language Models (LLMs) offer a training-free alternative, they often struggle with structural ambiguity, failing to distinguish between similar components like Claims and Premises. Furthermore, single-agent self-correction mechanisms often suffer from sycophancy, where the model reinforces its own initial errors rather than critically evaluating them. We introduce MAD-ACC (Multi-Agent Debate for Argument Component Classification), a framework that leverages dialectical refinement to resolve classification uncertainty. MAD-ACC utilizes a Proponent-Opponent-Judge model where agents defend conflicting interpretations of ambiguous text, exposing logical nuances that single-agent models miss. Evaluation on the UKP Student Essays corpus demonstrates that MAD-ACC achieves a Macro F1 score of 85.7%, significantly outperforming single-agent reasoning baselines, without requiring domain-specific training. Additionally, unlike "black-box" classifiers, MAD-ACC's dialectical approach offers a transparent and explainable alternative by generating human-readable debate transcripts that explain the reasoning behind decisions.

---

## 论文详细总结（自动生成）

### 论文总结：用于增强型论辩分类的多智能体辩证优化 (MAD-ACC)

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论辩挖掘（Argument Mining）中的组件分类任务（如区分“主张”与“前提”）极具挑战性。
*   **研究背景**：
    *   **传统监督学习**：依赖昂贵的领域特定微调，泛化能力差，且模型往往是“黑盒”。
    *   **大语言模型（LLM）**：虽然无需训练即可使用，但在处理结构模糊的文本时表现不佳，容易混淆语义相似的组件。
    *   **单智能体缺陷**：现有的单智能体自我修正机制常陷入“阿谀奉承”（Sycophancy）陷阱，即模型倾向于确认并强化其初始错误，而非进行真正的批判性反思。
*   **研究目的**：探索是否可以通过多智能体辩论框架，在无需微调的情况下，通过辩证推理提升论辩组件分类的准确性与透明度。

#### 2. 论文提出的方法论：MAD-ACC 框架
MAD-ACC（Multi-Agent Debate for Argument Component Classification）将分类任务转化为结构化辩论，包含四个角色：**管理者（Manager）**、**支持者（Proponent）**、**反对者（Opponent）**和**裁判（Judge）**。其流程分为三个阶段：
*   **阶段一：概率初始化（Probabilistic Initialization）**
    *   管理者（Manager）对输入文本进行初步评估，估算各标签（MajorClaim, Claim, Premise）的概率分布。
    *   选取概率最高的两个标签，随机分配给支持者和反对者，确保辩论的公平性并消除位置偏见。
*   **阶段二：辩证交互（Dialectical Interaction）**
    *   支持者与反对者针对各自代表的标签进行多轮辩论（实验设定为2轮共4次发言）。
    *   智能体被要求优先考虑结构和逻辑依赖关系（如支持方向），而非单纯的语义断言。
*   **阶段三：裁判裁决（Judge Classification）**
    *   裁判（Judge）阅读完整的辩论记录，根据预定义的标签定义评估双方证据。
    *   裁判通过验证哪一方提供了更准确的逻辑支撑方向来做出最终预测，并生成可读的推理说明。

#### 3. 实验设计
*   **数据集**：使用 **UKP Student Essays v2** 语料库。实验严格基于前人研究的测试集划分（80篇论文，1266个论辩陈述）。
*   **基准方法（Benchmarks）**：
    1.  **Vanilla**：标准提示词直接分类（使用 Gemini 2.5 Flash）。
    2.  **Chain-of-Thought (CoT)**：思维链推理提示。
    3.  **Smart Reasoning**：使用更强大的单智能体模型（Gemini 2.5 Pro）进行直接推理，模拟没有辩论过程的裁判决策。
    4.  **SOTA Reference**：对比了 2025 年最新的微调 LLM 监督学习结果。
*   **评估指标**：Macro F1、Weighted F1 以及各类别（MajorClaim, Claim, Premise）的 F1 分数。

#### 4. 资源与算力
*   **模型使用**：采用了 Google 的 **Gemini 2.5** 系列模型。
    *   管理者和辩论者：Gemini 2.5 Flash（侧重速度与多样性）。
    *   裁判：Gemini 2.5 Pro（侧重高容量逻辑处理）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或推理总时长。由于该方法属于“无需训练”（Training-free）的推理框架，其主要开销在于 API 调用产生的 Token 消耗，而非模型训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：在 1266 个测试样本上进行了完整测试。
*   **实验设置**：
    *   对比了不同能力的单智能体基准（Flash vs Pro）。
    *   进行了定性案例分析（Case Studies），展示了辩论如何纠正单智能体的逻辑错误。
*   **客观性与公平性**：通过随机分配辩论立场来消除位置偏见；未使用训练集进行提示词微调，确保了零样本（Zero-shot）测试的严谨性。实验设计较为客观，但目前仅在一个主流数据集上进行了验证。

#### 6. 主要结论与发现
*   **性能提升**：MAD-ACC 达到了 **85.7% 的 Macro F1 分数**，显著优于所有单智能体推理基准（最高提升约 7.2%）。
*   **解决歧义**：该框架在区分“主张（Claim）”和“前提（Premise）”方面表现尤为突出，Claim F1 分数比最强单智能体基准高出 2%。
*   **缩小差距**：虽然仍略低于经过全量数据微调的 SOTA 模型（89.5%），但在无需任何标注数据训练的前提下，MAD-ACC 极大地缩小了推理模型与监督模型之间的差距。
*   **透明度**：辩论记录提供了人类可读的决策依据，解决了传统分类器的“黑盒”问题。

#### 7. 优点
*   **无需训练**：在低资源领域或缺乏标注数据的场景下具有极高的实用价值。
*   **辩证纠错**：通过强制性的对立观点辩论，有效克服了 LLM 常见的自我强化错误（Sycophancy）。
*   **高可解释性**：生成的辩论记录不仅给出了结果，还解释了逻辑上的“为什么”，这对教育技术（如辅助写作教学）至关重要。
*   **模型无关性**：框架逻辑可适配不同的底层 LLM。

#### 8. 不足与局限
*   **成本较高**：相比单次推理，多智能体辩论涉及多轮对话，Token 消耗量显著增加，推理延迟也更高。
*   **数据集单一**：目前仅在学生论文语料库上验证，其在法律、医疗等更复杂领域的泛化能力尚待进一步研究。
*   **性能上限**：尽管表现优异，但仍未完全超越重度微调的监督学习模型。
*   **辩论轮数限制**：实验发现轮数过多会导致重复，如何动态确定最佳辩论深度仍是挑战。

（完）

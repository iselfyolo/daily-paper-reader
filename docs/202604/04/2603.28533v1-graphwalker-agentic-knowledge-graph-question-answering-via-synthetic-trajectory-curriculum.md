---
title: "GraphWalker: Agentic Knowledge Graph Question Answering via Synthetic Trajectory Curriculum"
title_zh: GraphWalker：通过合成轨迹课程实现的智能体化知识图谱问答
authors: "Shuwen Xu, Yao Xu, Jiaxiang Liu, Chenhao Yuan, Wenshuo Peng, Jun Zhao, Kang Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28533v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于训练数据的自动化轨迹合成
tldr: 针对知识图谱问答（KGQA）中训练数据稀缺和推理泛化性差的问题，本文提出GraphWalker框架。该框架通过自动化轨迹合成和阶段式微调（SFT）来增强智能体的探索与纠错能力。首先利用随机游走合成多样化轨迹建立探索先验，再通过专家轨迹微调提升反思能力。实验表明，GraphWalker在CWQ和WebQSP上达到SOTA，并显著提升了在分布外推理路径上的泛化性能。
selection_source: fresh_fetch
motivation: 现有的KGQA方法受限于训练数据匮乏及预定义推理路径，导致智能体缺乏自主导航和应对复杂推理的能力。
method: 提出一种两阶段微调范式，先利用合成的随机游走轨迹建立探索先验，再通过少量专家轨迹培养智能体的反思与错误恢复能力。
result: 在CWQ和WebQSP基准测试中取得SOTA性能，并在GrailQA及新构建的GraphWalkerBench上展现出卓越的泛化性。
conclusion: GraphWalker通过合成轨迹课程学习有效解决了KGQA中的数据瓶颈，为轻量级强化学习阶段奠定了高性能基础。
---

## 摘要
智能体化知识图谱问答（KGQA）要求智能体与知识图谱（KGs）进行迭代交互，这在训练数据稀缺性和推理泛化性方面都带来了挑战。具体而言，现有方法往往限制了智能体的探索：基于提示的方法缺乏自主导航训练，而目前的训练流程通常将推理局限于预定义的轨迹。为此，本文提出了 GraphWalker，这是一个新颖的智能体化 KGQA 框架，通过“自动化轨迹合成”和“分阶段微调”来应对这些挑战。GraphWalker 采用两阶段 SFT 训练范式：首先，在从受限随机游走路径合成的结构多样化轨迹上训练智能体，从而在知识图谱上建立广泛的探索先验；其次，在少量专家轨迹上进一步微调智能体，以培养其反思和错误恢复能力。大量实验表明，我们的分阶段 SFT 范式为轻量级强化学习（RL）阶段解锁了更高的性能上限，使 GraphWalker 在 CWQ 和 WebQSP 数据集上达到了最先进的性能。在 GrailQA 以及我们构建的 GraphWalkerBench 上的额外结果证实，GraphWalker 增强了对分布外推理路径的泛化能力。代码已在 https://github.com/XuShuwenn/GraphWalker 公开。

## Abstract
Agentic knowledge graph question answering (KGQA) requires an agent to iteratively interact with knowledge graphs (KGs), posing challenges in both training data scarcity and reasoning generalization. Specifically, existing approaches often restrict agent exploration: prompting-based methods lack autonomous navigation training, while current training pipelines usually confine reasoning to predefined trajectories. To this end, this paper proposes \textit{GraphWalker}, a novel agentic KGQA framework that addresses these challenges through \textit{Automated Trajectory Synthesis} and \textit{Stage-wise Fine-tuning}. GraphWalker adopts a two-stage SFT training paradigm: First, the agent is trained on structurally diverse trajectories synthesized from constrained random-walk paths, establishing a broad exploration prior over the KG; Second, the agent is further fine-tuned on a small set of expert trajectories to develop reflection and error recovery capabilities. Extensive experiments demonstrate that our stage-wise SFT paradigm unlocks a higher performance ceiling for a lightweight reinforcement learning (RL) stage, enabling GraphWalker to achieve state-of-the-art performance on CWQ and WebQSP. Additional results on GrailQA and our constructed GraphWalkerBench confirm that GraphWalker enhances generalization to out-of-distribution reasoning paths. The code is publicly available at https://github.com/XuShuwenn/GraphWalker

---

## 论文详细总结（自动生成）

以下是对论文《GraphWalker: Agentic Knowledge Graph Question Answering via Synthetic Trajectory Curriculum》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
智能体化知识图谱问答（Agentic KGQA）旨在让大语言模型（LLM）作为智能体，通过与全球知识图谱（KG）的迭代交互（如调用工具查询关系和三元组）来回答复杂问题。
*   **核心痛点**：
    1.  **训练数据稀缺**：高质量的智能体交互轨迹（Trajectory）难以获取，现有方法多依赖预定义的静态路径。
    2.  **推理泛化性差**：智能体在面对未见过的图结构或噪声数据时，缺乏自主导航和纠错能力。
    3.  **RL 启动困难**：强化学习（RL）在 KGQA 中往往因为搜索空间巨大且奖励稀疏，导致难以从零开始训练。
*   **研究意义**：本文提出 GraphWalker 框架，通过自动化合成多样化轨迹并采用“分阶段微调”策略，为智能体建立强大的探索先验，从而突破推理能力的上限。

### 2. 方法论：核心思想与技术细节
GraphWalker 采用**两阶段监督微调（SFT）+ 轻量级强化学习（RL）**的课程学习范式：

*   **数据构建阶段**：
    1.  **GraphSynth-15k（探索先验）**：利用**受限随机游走（Constrained Random Walk, CRW）**在 KG 上自动生成 15,000 条结构多样（链式、汇聚式等）的推理路径，并利用 LLM 合成对应的自然语言问题和思考过程。
    2.  **GraphRoll-6k（反思与纠错）**：通过专家模型（如 GPT-4o-mini）在真实数据集上生成 6,000 条高质量轨迹，特别包含“遇到死胡同后回溯”和“反思错误观察”的样本。
*   **训练流程**：
    1.  **SFT 阶段 1**：在 GraphSynth 上训练，使模型掌握基本的 KG 导航和工具调用规范。
    2.  **SFT 阶段 2**：在 GraphRoll 上微调，赋予模型自我修正和处理复杂逻辑的能力。
    3.  **RL 阶段**：采用 **GRPO（组相对策略优化）** 算法，以最终答案的准确性（Exact Match, EM）作为稀疏奖励，在 SFT 模型的基础上进一步优化决策策略。

### 3. 实验设计
*   **数据集**：
    *   **主流基准**：CWQ（复杂多跳问答）和 WebQSP（简单事实问答）。
    *   **泛化性评估**：GrailQA（Zero-shot 评估）以及自建的 **GraphWalkerBench**（包含训练集中未出现的复杂图结构，如 IP、PI 型路径）。
*   **对比方法**：
    *   **提示词方法**：ToG, GoG, ToG-2.0。
    *   **训练方法**：RoG, KG-Agent, KBQA-o1, 以及最新的 RL 方法 KG-R1。
    *   **闭源模型**：GPT-4o-mini, DeepSeek-V3.2（在相同框架下对比）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：
    *   **SFT 阶段**：学习率 1e-5，Batch Size 32，训练 2 个 Epoch。
    *   **RL 阶段**：基于 GRPO 算法，学习率 1e-6，Global Batch Size 512，训练 100 个 Step。
    *   **推理配置**：最大交互轮数 $T=10$。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验，具有很高的客观性和说服力：
*   **主实验**：在两个标准数据集上对比了 7 种以上的方法，涵盖了不同参数规模（3B, 7B, 8B）的基座模型（Qwen2.5, Llama-3.1）。
*   **消融实验**：针对“SFT 阶段 1”、“SFT 阶段 2”、“RL 阶段”以及“数据质量过滤”分别做了剔除实验，验证了每一阶段的必要性。
*   **泛化性实验**：在 GrailQA 和 GraphWalkerBench 上验证了 OOD（分布外）泛化能力。
*   **数据规模分析**：分析了合成数据量（5k 到 15k）对检索率和恢复率的影响。
*   **Pass@k 分析**：通过采样 32 次运行结果，证明了 GraphSynth 确实扩大了智能体的搜索空间。

### 6. 主要结论与发现
1.  **性能领先**：GraphWalker 在 CWQ 和 WebQSP 上均取得 SOTA 结果，甚至超越了使用 GPT-4 的方法。
2.  **课程学习的价值**：先学习“如何探索”（Stage 1），再学习“如何纠错”（Stage 2），最后通过 RL 对齐，这种顺序比混合训练效果显著更好。
3.  **RL 的催化作用**：两阶段 SFT 建立的强大先验是 RL 成功的关键；没有 Stage 1 的探索能力，RL 无法在巨大的图空间中找到奖励。
4.  **跨任务迁移**：仅在 CWQ 上训练的模型，在 WebQSP 上表现出极强的 Zero-shot 迁移能力（91.5% EM）。

### 7. 优点（亮点）
*   **创新的数据合成方案**：通过受限随机游走解决了 KGQA 训练数据匮乏的顽疾，且不依赖昂贵的专家标注。
*   **分阶段训练范式**：清晰地解耦了“导航能力”与“反思能力”，为轻量级模型（如 3B, 7B）赋予了超越巨型模型的推理潜力。
*   **全球 KG 交互**：不同于许多方法在预先提取的小子图上运行，GraphWalker 直接在完整的全球知识图谱上操作，更符合真实应用场景。

### 8. 不足与局限
*   **检索依赖性**：虽然模型具备纠错能力，但如果初始的 BM25 检索或关系重排序（Reranking）完全失效，智能体仍难以触达正确答案。
*   **计算开销**：虽然 RL 阶段是轻量级的，但两阶段 SFT 加上数据合成过程仍需要一定的计算资源和时间成本。
*   **知识图谱完整性**：实验假设 KG 中存在答案，对于 KG 缺失（Incomplete KG）情况下的推理能力讨论较少。

（完）

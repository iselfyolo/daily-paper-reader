---
title: Spectral-Aware Text-to-Time Series Generation with Billion-Scale Multimodal Meteorological Data
title_zh: 基于十亿级多模态气象数据的频谱感知文本到时间序列生成
authors: Shijie Zhang
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27135v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于构建十亿级数据集的多智能体协同标注流水线
tldr: 本研究针对气象领域文本生成时间序列的需求，解决了大规模多模态数据集缺失及模型忽略频谱结构的问题。首先通过多智能体协作标注构建了包含十亿级规模、具有专家级描述的MeteoCap-3B数据集。随后提出基于扩散模型的MTransformer，利用频谱提示生成器将文本映射为多频段先验，实现精确的语义控制。实验证明该方法在生成质量、跨模态对齐及下游预测任务上均达到SOTA水平，且具备良好的泛化性。
selection_source: fresh_fetch
motivation: 现有气象时间序列生成缺乏大规模物理一致的多模态数据，且模型架构难以捕捉复杂的频谱-时间结构。
method: 构建了十亿级规模的MeteoCap-3B数据集，并提出基于频谱感知注意力机制和扩散模型的MTransformer框架。
result: 该方法在生成质量和语义可控性上达到SOTA，并显著增强了数据稀疏及零样本场景下的下游气象预测性能。
conclusion: 该框架通过结合大规模多模态数据与频谱先验，为气象及通用时间序列的文本引导生成提供了高效且可扩展的解决方案。
---

## 摘要
文本到时间序列生成在气象学中尤为重要，因为自然语言为复杂、多尺度的大气动力学提供了直观的控制手段。现有方法受限于缺乏大规模、具有物理基础的多模态数据集，以及忽略了气象信号频谱-时间结构的架构。我们通过一个统一的文本引导气象时间序列生成框架来应对这些挑战。首先，我们推出了 MeteoCap-3B，这是一个十亿级的气象数据集，配有通过多智能体协作字幕（MACC）流水线构建的专家级描述，从而产生了信息密集且物理一致的标注。基于该数据集，我们提出了 MTransformer，这是一种基于扩散的模型，通过频谱提示生成器将文本描述映射为多频段频谱先验，并通过频率感知注意力机制引导生成，从而实现精确的语义控制。在真实世界基准测试上的广泛实验表明，该模型具有最先进的生成质量、准确的跨模态对齐、强大的语义可控性，并在数据稀疏和零样本设置下的下游预测中取得了显著提升。在通用时间序列基准测试上的额外结果表明，所提出的框架具有超越气象领域的泛化能力。

## Abstract
Text-to-time-series generation is particularly important in meteorology, where natural language offers intuitive control over complex, multi-scale atmospheric dynamics. Existing approaches are constrained by the lack of large-scale, physically grounded multimodal datasets and by architectures that overlook the spectral-temporal structure of weather signals. We address these challenges with a unified framework for text-guided meteorological time-series generation. First, we introduce MeteoCap-3B, a billion-scale weather dataset paired with expert-level captions constructed via a Multi-agent Collaborative Captioning (MACC) pipeline, yielding information-dense and physically consistent annotations. Building on this dataset, we propose MTransformer, a diffusion-based model that enables precise semantic control by mapping textual descriptions into multi-band spectral priors through a Spectral Prompt Generator, which guides generation via frequency-aware attention. Extensive experiments on real-world benchmarks demonstrate state-of-the-art generation quality, accurate cross-modal alignment, strong semantic controllability, and substantial gains in downstream forecasting under data-sparse and zero-shot settings. Additional results on general time-series benchmarks indicate that the proposed framework generalizes beyond meteorology.

---

## 论文详细总结（自动生成）

以下是对论文《Spectral-Aware Text-to-Time Series Generation with Billion-Scale Multimodal Meteorological Data》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**气象领域文本引导的时间序列生成**。
*   **研究动机**：气象数据具有复杂的多尺度动力学特征，自然语言是控制这些特征的直观接口。然而，现有研究面临两大瓶颈：
    1.  **数据匮乏**：缺乏大规模、高质量且符合物理规律的“文本-时间序列”配对数据集。现有数据集多基于简单模板，语义密度低，无法捕捉深层的物理机制（如锋面过境、逆温等）。
    2.  **架构缺陷**：现有的生成模型（如扩散模型）直接应用于时间序列时，往往忽略了信号的频谱结构（周期性、波动性），导致生成的序列虽然语义看似合理，但在物理频谱上存在失真。

### 2. 方法论：核心思想与关键技术
论文提出了一个统一的框架，包含数据构建和模型生成两个核心部分：

#### A. MeteoCap-3B 数据集构建（MACC 流水线）
采用**多智能体协作标注（MACC）**流水线，利用 LLM 代理替代传统模板：
1.  **多工具推理**：Agent 调用趋势检测、异常搜索、FFT 分析等工具提取统计和频率特征。
2.  **语义对齐**：将提取的特征映射到物理气象概念（如“对流不稳定”）。
3.  **多 LLM 集成生成**：协同 GPT-4o、Claude 3.5、Gemini 等生成不同风格和侧重点的描述。
4.  **全局反思与修正**：利用 DeepSeek-V3 作为“反思者”进行事实核查，消除幻觉（如数据无雨却描述有雨）。

#### B. MTransformer 生成模型
基于**扩散 Transformer (DiT)** 架构，引入频谱感知机制：
*   **MSPG（多频段频谱提示生成器）**：将文本嵌入投影到多个可学习的频率子空间，生成频谱提示（Spectral Prompts）。
*   **自适应频谱门控**：根据文本语义动态调节不同频段（如高频波动或低频趋势）的权重。
*   **SPCA（频谱提示交叉注意力）**：在扩散去噪过程中，利用频谱提示引导时间序列的更新，确保生成结果在频域上的物理一致性。
*   **频谱调度器**：采用从弱到强的课程学习策略，逐步加强频谱约束。

### 3. 实验设计
*   **数据集**：
    *   **MeteoCap-3B**：自建的十亿级气象数据集，分为稳态（Steady）、过渡态（Transitional）和挥发态（Volatile）三个子集。
    *   **TSFragment-600K**：通用领域时间序列基准。
*   **Benchmark 与对比方法**：
    *   **生成基准**：T2S (IJCAI 2025)、DiffusionTS、TimeVAE。
    *   **LLM 基准**：GPT-4o-mini、Llama 3.1-8b、Gemini-3-Flash。
*   **评估指标**：MSE（均方误差）、WAPE（加权平均百分比误差）、MRR@10（平均倒数排名）。

### 4. 资源与算力
*   **硬件**：使用了 **2 × NVIDIA A100 80GB** GPU。
*   **训练细节**：
    *   VQ-VAE 预训练 5k 步。
    *   扩散模型训练 80k 步，Batch Size 为 256。
    *   文本嵌入维度为 1024，原型维度为 128。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验**：在 2 个大类数据集、多个子集、不同序列长度（24 到 336）上进行了对比。
*   **Scaling 实验**：验证了模型性能随参数量（10M 到 1B）、数据规模和序列长度增加的扩展性。
*   **下游任务**：测试了生成数据对气象预测（TimesNet, Transformer）的增强效果，包括少样本和零样本场景。
*   **消融实验**：验证了 MSPG、SPCA 和频谱调度器等核心组件的有效性。
*   **人类评估**：邀请 8 位气象学博士对标注质量进行盲测。
*   **客观性**：实验涵盖了从统计指标到物理频谱分析，再到下游应用的全方位评估，对比方法包含最新的 SOTA 模型，实验设计较为客观公平。

### 6. 主要结论与发现
1.  **生成质量领先**：MTransformer 在气象和通用领域均显著优于现有方法，尤其在处理极端天气（挥发态）时优势明显。
2.  **频谱一致性**：通过显式的频谱引导，模型能准确还原气象信号的周期性和功率谱密度，避免了基准模型常见的“过度平滑”问题。
3.  **下游增益显著**：使用 MeteoCap-3B 进行多模态预训练，可使下游预测任务的 MSE 降低 22.8%，证明了合成数据在解决数据稀疏问题上的价值。
4.  **语义可控性强**：模型能精准响应“趋势反转”、“周期偏移”等细粒度文本指令。

### 7. 优点（亮点）
*   **数据质量高**：利用多智能体流水线构建了目前规模最大、物理一致性最强的气象多模态数据集。
*   **物理启发式架构**：将信号处理中的频谱分析与现代扩散模型结合，解决了时间序列生成中的频域失真难题。
*   **闭环验证**：通过人类专家评估和下游预测任务双重验证了生成数据的实用性。

### 8. 不足与局限
*   **计算开销**：作为扩散模型，其采样速度（1k 步迭代）可能限制实时交互应用。
*   **依赖外部 LLM**：数据标注流水线高度依赖 GPT-4o、Claude 等商业模型，构建成本较高且难以完全开源标注全过程。
*   **变量限制**：目前主要涵盖 5 个核心气象变量（温、压、湿、风、降水），对于更复杂的三维大气变量（如垂直气流、云量分布）覆盖不足。

（完）

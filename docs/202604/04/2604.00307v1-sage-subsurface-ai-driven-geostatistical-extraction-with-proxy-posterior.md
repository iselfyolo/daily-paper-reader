---
title: "SAGE: Subsurface AI-driven Geostatistical Extraction with proxy posterior"
title_zh: SAGE：基于代理后验的地下人工智能驱动地统计提取
authors: "Huseyin Tuna Erdinc, Ipsita Bhar, Rafael Orozco, Thales Souza, Felix J. Herrmann"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00307v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于地下速度模型合成的生成网络
tldr: 针对地下速度模型合成中高质量地质数据集匮乏的问题，本文提出SAGE框架。该方法利用稀疏测井数据和迁移地震图像，通过学习代理后验分布来生成统计一致的速度模型。在推理阶段，仅需地震图像即可生成包含测井信息的全分辨率速度场。SAGE在合成和实际数据集上均表现出捕捉复杂地质变化的能力，为地震成像和反演提供了一种高效的数据驱动方案。
selection_source: fresh_fetch
motivation: 传统生成方法依赖大规模高质量地质数据集，而实际应用中这类数据难以获取且观测信息往往不完整。
method: 提出一种学习代理后验分布的框架，训练时结合测井和地震数据，推理时仅利用地震图像生成隐含测井特征的速度场。
result: 在合成及实测数据集上的实验证明，该方法能有效捕捉复杂的地下变化，并能为下游反演任务提供高质量样本。
conclusion: SAGE为受限观测条件下的地质代理后验学习提供了一种可扩展且数据高效的路径，增强了地震成像与反演的可靠性。
---

## 摘要
生成网络的最新进展为地下速度模型合成开辟了新途径，为全波形反演（Full Waveform Inversion）等传统方法提供了一个极具吸引力的替代方案。然而，这些方法主要依赖于大规模、高质量且符合地质实际的地下速度模型数据集，而这在实践中往往难以获得。我们提出了 SAGE，这是一个旨在从不完整观测数据（特别是稀疏测井记录和偏移地震图像）中生成统计一致代理速度模型的创新框架。在训练阶段，SAGE 学习了一个以测井和地震两种模态为条件的

## Abstract
Recent advances in generative networks have enabled new approaches to subsurface velocity model synthesis, offering a compelling alternative to traditional methods such as Full Waveform Inversion. However, these approaches predominantly rely on the availability of large-scale datasets of high-quality, geologically realistic subsurface velocity models, which are often difficult to obtain in practice. We introduce SAGE, a novel framework for statistically consistent proxy velocity generation from incomplete observations, specifically sparse well logs and migrated seismic images. During training, SAGE learns a proxy posterior over velocity models conditioned on both modalities (wells and seismic); at inference, it produces full-resolution velocity fields conditioned solely on migrated images, with well information implicitly encoded in the learned distribution. This enables the generation of geologically plausible and statistically accurate velocity realizations. We validate SAGE on both synthetic and field datasets, demonstrating its ability to capture complex subsurface variability under limited observational constraints. Furthermore, samples drawn from the learned proxy distribution can be leveraged to train downstream networks, supporting inversion workflows. Overall, SAGE provides a scalable and data-efficient pathway toward learning geological proxy posterior for seismic imaging and inversion. Repo link: https://github.com/slimgroup/SAGE.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SAGE**（Subsurface AI-driven Geostatistical Extraction）的框架，旨在解决地下速度模型构建中高质量训练数据匮乏的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的地下速度模型构建（如全波形反演 FWI）具有高度非线性且计算昂贵。虽然生成式 AI 提供了新途径，但现有方法极度依赖大规模、高分辨率且地质真实的完整速度模型数据集。在现实中，这种“地面真值（Ground Truth）”数据极难获取。
*   **研究动机**：利用实际生产中易于获得的**不完整观测数据**（如稀疏的测井数据和偏移后的地震图像）来训练生成模型，从而绕过对完整速度模型数据集的依赖。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：SAGE 学习一个“代理后验（Proxy Posterior）”分布。在训练时，它同时利用测井（Well logs）和地震偏移图像（RTM）；在推理时，仅需地震图像即可生成全分辨率的速度场，而测井信息已隐式编码在模型分布中。
*   **关键技术细节**：
    *   **条件评分生成网络（Score-based Diffusion）**：采用基于方差爆炸（Variance-exploding）公式的扩散模型，通过去噪评分匹配（Denoising Score Matching）来估计后验分布。
    *   **双重掩码策略（Double Masking）**：为了防止网络只学会简单的插值（恒等映射），引入了观测掩码 $A$ 和随机子采样掩码 $\tilde{A}$。模型被训练为从更稀疏的 $\tilde{x}_{obs}$ 恢复出较完整的 $x_{obs}$，从而迫使网络学习全局地质特征和地震图像的关联。
    *   **训练目标**：优化目标是最小化重建速度场与实际观测测井数据之间的 $L_2$ 损失，同时以 RTM 图像作为条件输入。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：基于北海 Compass 3D 模型的 2D 切片（1000 个样本）。模拟了 16 个震源、256 个检波器，并加入 10dB 噪声。
    *   **实际数据**：来自英国国家数据存储库（UK NDR）的真实地震图像和 40 口井的测井数据。
*   **Benchmark 与对比**：
    *   **评估指标**：使用结构相似性指数（SSIM）和后验标准差（Uncertainty Quantification）。
    *   **下游任务验证**：将 SAGE 生成的样本用于训练另一个反演网络 **WISE**，对比使用“SAGE 样本”与“地面真值样本”训练出的 WISE 性能差异。

### 4. 资源与算力
*   **模型架构**：采用了 U-Net 作为去噪器（Denoiser）。
*   **算力消耗**：论文明确指出，总训练时间约为 **20 GPU 小时**。但未具体说明 GPU 的型号（如 A100 或 V100）和数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在合成数据集上进行了 1000 个样本的训练和验证。
    *   展示了 16 个样本的后验均值和标准差分析。
    *   进行了从合成模型到真实场数据的**微调（Fine-tuning）**实验。
*   **充分性评价**：实验设计较为全面，不仅验证了生成质量，还通过下游任务（WISE 训练）证明了生成样本的实用价值。然而，由于真实井位数据较少（仅 40 口），真实数据的实验仍处于初步阶段。

### 6. 主要结论与发现
*   **高效性**：SAGE 在仅保留约 1% 速度信息（极稀疏测井）的情况下，仍能重建出结构一致的速度模型（SSIM 达 0.82）。
*   **下游支持**：使用 SAGE 生成的合成数据训练 WISE 网络，性能仅从 SSIM 0.88 轻微下降至 0.84，证明了 SAGE 作为数据增强工具的潜力。
*   **不确定性量化**：模型生成的标准差图能有效识别地质复杂区域（如断层、高速层边缘），为风险评估提供参考。

### 7. 优点与亮点
*   **数据效率**：突破了对完整速度模型数据集的依赖，实现了从“不完整数据”中学习。
*   **实用性强**：推理阶段不需要测井数据，这非常符合实际地震勘探中“先有地震成像，再预测未知区域速度”的流程。
*   **可扩展性**：通过微调策略，能够将合成数据学到的先验知识迁移到真实工区。

### 8. 不足与局限
*   **细节平滑**：由于观测数据极度稀疏，生成的模型在细微地质结构上存在一定的平滑效应，无法完全还原极高频的地质特征。
*   **维度限制**：目前主要展示的是 2D 结果，虽然论文提到未来会扩展到 3D，但 3D 环境下的计算开销和内存压力将是巨大挑战。
*   **真实数据偏差**：真实数据的表现高度依赖于预处理（如时深转换、井震校准），这些步骤的误差可能会传播至生成模型。

（完）

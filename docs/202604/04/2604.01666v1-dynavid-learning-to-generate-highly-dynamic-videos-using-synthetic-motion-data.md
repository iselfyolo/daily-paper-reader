---
title: "DynaVid: Learning to Generate Highly Dynamic Videos using Synthetic Motion Data"
title_zh: DynaVid：利用合成运动数据学习生成高动态视频
authors: "Wonjoon Jin, Jiyun Won, Janghyeok Han, Qi Dai, Chong Luo, Seung-Hwan Baek, Sunghyun Cho"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01666v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 在训练中使用合成运动数据
tldr: 针对视频扩散模型在生成高动态运动和精细控制方面的局限性，本文提出DynaVid框架。该框架利用计算机图形学生成的合成光流数据来学习复杂的运动模式，通过将运动与外观解耦，有效避免了合成视频的虚假感。DynaVid采用“运动生成+运动引导视频生成”的两阶段流程，在剧烈人体动作和极端相机控制场景下显著提升了视频生成的真实感与可控性。
selection_source: fresh_fetch
motivation: 现有视频训练数据集缺乏高动态运动样本，导致模型难以合成真实且可控的复杂动作。
method: 提出一种两阶段框架，先利用合成光流数据生成运动轨迹，再由运动引导的视频生成器根据该轨迹合成最终画面。
result: 在剧烈人体运动和极端相机控制任务中，DynaVid在生成质量和运动可控性上均优于现有模型。
conclusion: 通过利用合成运动数据并实现运动与外观的解耦，DynaVid为生成高动态、高可控的真实视频提供了有效方案。
---

## 摘要
尽管最近取得了进展，视频扩散模型在合成涉及高动态运动或需要细粒度运动可控性的逼真视频方面仍然面临挑战。一个核心限制在于常用训练数据集中此类示例的稀缺。为了解决这一问题，我们推出了 DynaVid，这是一个在训练中利用合成运动数据的视频合成框架，这些数据以光流形式表示，并使用计算机图形学流水线渲染。该方法具有两个关键优势：首先，合成运动提供了从真实数据中难以获取的多样化运动模式和精确控制信号；其次，与具有人工外观的渲染视频不同，渲染的光流仅编码运动并与外观解耦，从而防止模型复现合成视频的不自然外观。基于这一思路，DynaVid 采用了两阶段生成框架：运动生成器首先合成运动，随后运动引导的视频生成器以该运动为条件生成视频帧。这种解耦的公式使模型能够从合成数据中学习动态运动模式，同时保留真实世界视频的视觉逼真度。我们在两个具有挑战性的场景中验证了我们的框架：剧烈的人体运动生成和极端的摄像机运动控制，在这些场景中，现有数据集尤其有限。广泛的实验表明，DynaVid 提高了动态运动生成和摄像机运动控制中的逼真度和可控性。

## Abstract
Despite recent progress, video diffusion models still struggle to synthesize realistic videos involving highly dynamic motions or requiring fine-grained motion controllability. A central limitation lies in the scarcity of such examples in commonly used training datasets. To address this, we introduce DynaVid, a video synthesis framework that leverages synthetic motion data in training, which is represented as optical flow and rendered using computer graphics pipelines. This approach offers two key advantages. First, synthetic motion offers diverse motion patterns and precise control signals that are difficult to obtain from real data. Second, unlike rendered videos with artificial appearances, rendered optical flow encodes only motion and is decoupled from appearance, thereby preventing models from reproducing the unnatural look of synthetic videos. Building on this idea, DynaVid adopts a two-stage generation framework: a motion generator first synthesizes motion, and then a motion-guided video generator produces video frames conditioned on that motion. This decoupled formulation enables the model to learn dynamic motion patterns from synthetic data while preserving visual realism from real-world videos. We validate our framework on two challenging scenarios, vigorous human motion generation and extreme camera motion control, where existing datasets are particularly limited. Extensive experiments demonstrate that DynaVid improves the realism and controllability in dynamic motion generation and camera motion control.

---

## 论文详细总结（自动生成）

以下是对论文《DynaVid: Learning to Generate Highly Dynamic Videos using Synthetic Motion Data》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视频扩散模型（如 Sora, Wan2.1 等）在生成**高动态运动**（如霹雳舞、剧烈动作）或实现**精细运动控制**（如极端的相机视角切换）时表现不佳。
*   **核心瓶颈**：
    1.  **数据稀缺**：真实世界的大规模数据集中，高动态运动的样本占比极低。
    2.  **标注困难**：对于极端相机运动，很难获得准确的 3D 相机姿态标注。
    3.  **合成数据陷阱**：直接使用渲染的合成视频训练会导致模型产生“塑料感”或不真实的视觉风格（领域鸿沟）。
*   **核心意义**：提出 DynaVid 框架，通过利用**合成光流（Optical Flow）**而非合成视频来学习运动模式，实现了运动与外观的解耦，从而在保持真实视觉效果的同时生成极具张力的动态视频。

### 2. 论文提出的方法论
*   **核心思想**：采用**两阶段生成框架**。第一阶段生成运动轨迹（光流），第二阶段根据运动轨迹合成视频帧。
*   **关键技术细节**：
    *   **合成运动数据生成**：利用 Blender 渲染引擎构建 `DynaVid-Human`（人体剧烈动作）和 `DynaVid-Camera`（极端相机轨迹）数据集。仅导出光流图和控制信号，避开合成视频的外观干扰。
    *   **运动表示**：为了复用预训练的视频 VAE，将 2D 光流向量通过 HSV 映射转换为 RGB 域图像，从而在潜空间（Latent Space）中进行扩散建模。
    *   **两阶段架构**：
        1.  **运动生成器（Motion Generator）**：以文本和相机参数（Plücker embedding）为条件，生成连续的光流序列。
        2.  **运动引导视频生成器（Motion-Guided Video Generator）**：以生成的光流为显式约束，合成最终的 RGB 视频。
    *   **训练策略**：采用“混合批次（Batch Mixture）”微调，同时使用真实视频提取的光流和合成光流进行训练，并引入基于“光流循环一致性（Cycle Consistency）”的数据过滤机制，剔除低质量的真实光流样本。

### 3. 实验设计
*   **场景与数据集**：
    *   **物体运动场景**：使用 Pexels（常规）和 DynaVid-Human（高动态）测试集。
    *   **相机控制场景**：使用 RealEstate10K（常规）和 DynaVid-Camera（极端相机运动）测试集。
*   **Benchmark 与对比方法**：
    *   **通用生成**：对比 CogVideoX-5B, Wan2.2-5B。
    *   **人体动画**：对比 HyperMotion。
    *   **相机控制**：对比 AC3D, GEN3C。
*   **评估指标**：FVD（视觉质量）、VBench（审美、成像、平滑度等）、mRotErr（相机旋转误差）、M-Err（运动误差）。

### 4. 资源与算力
*   **模型底座**：使用了 **Wan2.2-5B** 作为 Backbone。
*   **算力说明**：论文正文**未明确列出**具体的 GPU 型号、数量及总训练时长。文中提到相关细节位于补充材料（Supplementary Document）中。

### 5. 实验数量与充分性
*   **实验规模**：针对每个场景随机采样了 100 个视频剪辑进行定量评估。
*   **消融实验**：设计了四组关键消融实验，分别验证了“合成运动数据”、“混合批次训练”、“数据过滤机制”以及“使用合成视频 vs 合成光流”的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从定量指标到定性视觉对比的多个维度。通过引入噪声鲁棒性测试（5dB 到 25dB）验证了第二阶段生成器的稳定性，实验逻辑闭环，对比实验选取了当前最先进的开源模型，具有较高的客观性。

### 6. 论文的主要结论与发现
*   **解耦的优越性**：光流作为中间表示能有效隔离合成数据的“人工感”，使模型在学习复杂物理运动的同时不损失画质。
*   **合成数据的价值**：在真实数据难以覆盖的极端场景（如 180 度急速转头、复杂空翻），合成运动数据是提升模型能力的必要补充。
*   **训练技巧的关键**：混合真实与合成数据进行训练是防止模型过拟合到特定运动模式的关键。

### 7. 优点
*   **创新性地解决了领域鸿沟问题**：通过“光流”这一桥梁，巧妙利用了 CG 引擎的精确性又避开了其渲染的不真实性。
*   **极强的可控性**：相比于隐式的运动学习，两阶段框架提供了更明确的运动控制手段。
*   **泛化能力**：实验展示了模型在处理非人类物体（如熊猫）时也能产生合理的动态效果，表现出一定的跨领域泛化性。

### 8. 不足与局限
*   **多人场景限制**：由于合成数据集目前主要集中在单人场景，模型在处理多人复杂互动时效果受限。
*   **两阶段开销**：相比于端到端的生成，两阶段流程增加了推理时的计算步骤和时间。
*   **依赖光流质量**：第二阶段生成器的上限受限于第一阶段生成的（或真实提取的）光流质量，光流估计错误可能导致肢体扭曲。

（完）

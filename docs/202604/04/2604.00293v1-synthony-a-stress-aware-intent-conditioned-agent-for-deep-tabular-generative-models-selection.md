---
title: "SYNTHONY: A Stress-Aware, Intent-Conditioned Agent for Deep Tabular Generative Models Selection"
title_zh: SYNTHONY：一种用于深度表格生成模型选择的压力感知、意图条件智能体
authors: "Hochan Son, Xiaofeng Lin, Jason Ni, Guang Cheng"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00293v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 为数据合成选择深度表格生成模型
tldr: 针对表格数据生成模型在不同数据集上表现不稳定的问题，本文提出了SYNTHONY框架。该框架引入了“压力分析”技术，通过量化数据集在长尾分布、高基数类别等维度的难度，结合用户对保真度、隐私和效用的特定意图，自动选择最优生成器。实验表明，基于压力特征的选择器在多种意图下均优于零样本LLM和随机基准，显著提升了合成数据的质量。
selection_source: fresh_fetch
motivation: 表格生成模型在处理复杂分布时表现脆弱，且难以平衡用户在保真度、隐私和效用方面的不同偏好。
method: 提出SYNTHONY框架，通过提取数据集的四维压力元特征，并将其与合成器能力注册表匹配来进行意图感知的模型选择。
result: 实验证明压力特征能高度预测模型性能，基于此的kNN选择器在Top-1准确率上显著优于零样本LLM和随机基准。
conclusion: 压力分析是解决表格生成模型选择问题的有效途径，未来可进一步探索自动学习的合成器能力表示。
---

## 摘要
表格数据的深度生成模型（如 GAN、扩散模型和基于大语言模型的生成器）在不同数据集上表现出高度不一致的行为；性能最佳的合成器系列很大程度上取决于分布压力因素（distributional stressors），例如长尾边际分布、高基数类别变量、齐普夫（Zipfian）不平衡和小样本情况。这种脆弱性使得实际部署面临挑战，特别是当用户必须平衡保真度、隐私和效用等相互竞争的目标时。我们研究了“意图条件下的表格合成选择”：给定一个数据集和以评估指标偏好表示的用户意图，目标是选择一个合成器，使其相对于特定意图的理想参考（oracle）的遗憾（regret）最小化。我们提出了“压力剖析”（stress profiling），这是一种专门针对合成任务的元特征表示，它从四个可解释的压力维度量化数据集难度，并将其集成到 SYNTHONY 框架中。该框架将压力剖析与经过校准的合成器系列能力注册表进行匹配。在包含 7 个数据集、10 个合成器和 3 种意图的基准测试中，我们证明了基于压力的元特征对合成器性能具有高度预测性：使用这些特征的 $k$NN 选择器实现了强大的 Top-1 选择准确率，显著优于零样本大语言模型（LLM）选择器和随机基准。我们分析了基于元特征的选择与基于能力的选择之间的差距，指出手工构建的能力注册表是主要瓶颈，并提出了学习能力表示作为未来的研究方向。

## Abstract
Deep generative models for tabular data (GANs, diffusion models, and LLM-based generators) exhibit highly non-uniform behavior across datasets; the best-performing synthesizer family depends strongly on distributional stressors such as long-tailed marginals, high-cardinality categorical, Zipfian imbalance, and small-sample regimes. This brittleness makes practical deployment challenging, especially when users must balance competing objectives of fidelity, privacy, and utility. We study {intent-conditioned tabular synthesis selection}: given a dataset and a user intent expressed as a preference over evaluation metrics, the goal is to select a synthesizer that minimizes regret relative to an intent-specific oracle. We propose {stress profiling}, a synthesis-specific meta-feature representation that quantifies dataset difficulty along four interpretable stress dimensions, and integrate it into {SYNTHONY}, a selection framework that matches stress profiles against a calibrated capability registry of synthesizer families. Across a benchmark of 7 datasets, 10 synthesizers, and 3 intents, we demonstrate that stress-based meta-features are highly predictive of synthesizer performance: a $k$NN selector using these features achieves strong Top-1 selection accuracy, substantially outperforming zero-shot LLM selectors and random baselines. We analyze the gap between meta-feature-based and capability-based selection, identifying the hand-crafted capability registry as the primary bottleneck and motivating learned capability representations as a direction for future work.

---

## 论文详细总结（自动生成）

### 论文详细总结：SYNTHONY

#### 1. 核心问题与研究动机
*   **核心问题**：在表格数据合成领域，不存在“一劳永逸”的生成模型。GAN、扩散模型和大语言模型（LLM）在不同数据集上的表现极不均衡。
*   **研究动机**：
    *   **模型脆弱性**：模型性能受数据集“压力因素”（如长尾分布、高基数类别、样本量极小等）影响巨大。
    *   **意图冲突**：用户在实际应用中往往需要在数据保真度（Fidelity）、隐私保护（Privacy）和下游任务效用（Utility）之间进行权衡。
    *   **缺乏自动化**：目前缺乏类似AutoML的“自动合成器选择”框架，导致从业者只能依靠试错或直觉选择模型。

#### 2. 方法论
SYNTHONY 框架的核心思想是将数据集的物理特征转化为“压力剖析”，并与预定义的模型“能力注册表”进行匹配。
*   **压力剖析（Stress Profiling）**：从四个维度量化数据集难度：
    1.  **长尾偏度**：数值列的最大偏度。
    2.  **基数复杂度**：类别列的最大唯一值数量。
    3.  **齐普夫集中度（Zipfian）**：前20%类别出现的频率比例。
    4.  **小样本状态**：总行数。
*   **需求向量化**：将上述压力指标映射为 0-4 级的需求等级 $r$。
*   **能力注册表（Capability Registry）**：记录 10 种主流合成器在处理上述压力维度时的预期鲁棒性得分 $c$。
*   **意图感知评分公式**：
    $$Score(g, i) = \sum_{j=1}^{J} \alpha_{i,j} \cdot c_{g,j} \cdot r_j(\phi(D))$$
    其中 $g$ 是生成器，$i$ 是用户意图，$\alpha$ 是通过贝叶斯优化校准的意图缩放因子。
*   **校准机制**：使用 Optuna（TPE 采样器）对 78 个参数（能力得分和意图权重）进行联合优化，以最大化预测排名与真实排名之间的 Spearman 相关系数。

#### 3. 实验设计
*   **数据集**：选取了 7 个具有代表性的 OpenML/UCI 表格数据集（如 Abalone, Bean, Obesity, Insurance 等），涵盖分类和回归任务。
*   **候选生成器**：共 10 种，分为四类：
    *   **差异隐私类**：AIM, DPCART。
    *   **深度生成类**：AutoDiff, TabDDPM, TVAE, NFlow。
    *   **树模型类**：ARF, CART。
    *   **统计类**：BayesianNetwork, SMOTE。
*   **对比方法（Baselines）**：
    *   **静态启发式**：基于行数和意图的简单逻辑规则。
    *   **原生 LLM 选择器**：使用 GPT-4o-mini 进行零样本推理。
    *   **随机搜索**：随机推荐。
    *   **元特征 kNN**：基于数据集相似度的元学习方法（作为参考上限）。
*   **评估指标**：Top-1/Top-3 准确率、Spearman 秩相关系数、NDCG。

#### 4. 资源与算力
*   **贝叶斯优化**：文中提到在单核 CPU 上运行 500 次试验约需 **35 秒**。
*   **模型训练与评估**：虽然未详细列出所有生成器的总训练时长，但提到部分模型（如 TabDDPM, TVAE）需要 GPU 支持。实验在 7 个数据集上穷举了所有模型以获取 Ground Truth，这涉及大量的计算开销，但 SYNTHONY 框架本身在推理阶段是极轻量级的。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖 7 个数据集、10 个模型、3 种意图，共产生 21 个“数据集-意图”对。
*   **充分性评价**：
    *   **优点**：进行了消融实验（验证压力剖析和意图缩放的有效性）和跨家族对比，实验设计逻辑严密。
    *   **局限**：7 个数据集的样本量相对较小，统计效力有限。由于 CART 在某些指标上表现过于强势，导致部分实验结果存在类别不平衡现象。

#### 6. 主要结论与发现
*   **压力特征极具预测性**：基于压力的元特征能有效预测深度生成模型的成败。
*   **SYNTHONY 表现优异**：其 SF-only 变体在 Spearman 相关系数上达到了 0.573，接近拥有先验知识的 kNN（0.578），远超 LLM 和启发式方法。
*   **LLM 并非万能**：GPT-4o-mini 在零样本场景下表现糟糕（Top-1 准确率为 0），证明了特定领域校准的必要性。
*   **隐私意图最难预测**：隐私保护表现与模型架构的关系比保真度更复杂，没有单一模型能统治所有数据集。

#### 7. 优点
*   **可解释性强**：通过四个直观的压力维度解释模型选择的原因，而非黑盒推荐。
*   **推理零开销**：无需实际运行合成器即可给出建议，适合集成到自动化流水线中。
*   **意图驱动**：首次系统性地将用户的主观偏好（隐私 vs 效用）量化到模型选择过程中。

#### 8. 不足与局限
*   **基准规模较小**：仅在 7 个数据集上测试，可能无法完全覆盖超大规模或极高维度的极端情况。
*   **手工注册表瓶颈**：目前的能力得分部分依赖人工经验，未来需要通过更大规模的自动化基准测试来自动学习这些得分。
*   **模型偏差**：实验中树模型（CART）在中小规模数据集上表现过于突出，可能掩盖了深度学习模型在超大数据集上的潜在优势。

（完）

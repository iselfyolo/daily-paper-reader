---
title: "From Legal Text to Executable Decision Models: Evaluating Structured Representations for Legal Decision Model Generation"
title_zh: 从法律文本到可执行决策模型：评估法律决策模型生成的结构化表示
authors: David Graus
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17153v1"
tags: ["query:q2"]
score: 9.0
evidence: 利用大语言模型和结构化表示将法律文本转化为可执行的决策逻辑
tldr: 本研究探讨了利用大语言模型（LLM）将法律文本转化为可执行决策模型的方法。研究基于荷兰《环境与规划法》的真实数据集，对比了原始文本、语义角色标注（SRL）及输入输出（I/O）约束等不同中间结构化表示对生成效果的影响。通过结构相似度和功能等效性评估，发现I/O约束能显著提升模型质量，且LLM能有效简化冗余逻辑。研究发布了包含95个生产级决策模型的开源数据集，为法律信息学提供了重要资源。
selection_source: fresh_fetch
motivation: 旨在解决将复杂法律文本自动转化为可执行决策逻辑的挑战，并评估中间结构化表示在提升LLM生成质量中的作用。
method: 采用LLM基于四种不同输入条件（原始文本、SRL、I/O约束及其组合）生成决策模型，并从结构相似度和功能执行结果两个维度进行评估。
result: "实验表明I/O约束对提升结构相似度贡献最大，生成的模型在约52%的场景中实现功能等效，并能自动消除大量冗余逻辑。"
conclusion: 结构相似性与功能等效性是互补的评估维度，引入特定的结构化约束能显著优化法律决策模型的自动化生成。
---

## 摘要
将法律文本转化为可执行的决策逻辑是法律信息学中一个长期存在的挑战。随着大语言模型（LLMs）的兴起，这一任务重新引起了人们的关注，但由于需要大量的手动编码和评估，仍然具有挑战性。我们使用了一个独特的真实世界数据集，将生产级决策模型与荷兰《环境与规划法》中的法律文本进行配对。这些模型为 Omgevingsloket 政府平台提供支持，公民可以在该平台上查询环境活动的许可要求。我们研究了中间结构化表示是否能改进基于 LLM 的法律文本可执行决策模型生成。我们比较了四种输入条件：原始法律文本、富含语义角色标注的文本、富含输入输出（I/O）约束的文本，以及同时包含两者的文本。我们从两个维度进行评估：结构评估，通过图核和图的描述性统计量衡量与金标准决策模型的相似度；结果评估，通过在预配置的测试场景中执行模型来衡量功能等效性。研究结果表明，I/O 约束提供了主要的改进（比基准线提高 37-54% 的相似度），而语义角色标注的改进较小。结果评估显示，尽管生成的模型通常更小、更简单，但在 51-53% 的测试场景中与金标准相匹配。我们发现 LLM 消除了占节点总数 45-55% 的冗余直通逻辑。重要的是，结构相似性和结果等效性是互补的：结构相似性并不能保证结果等效，反之亦然。为了促进可重复性，我们公开了包含 95 个生产决策模型及其相关法律文本的数据集以及所有实验代码。

## Abstract
Transforming legal text into executable decision logic is a longstanding challenge in legal informatics. With the rise of LLMs, this task has gained renewed interest, but remains challenging due to requiring extensive manual coding and evaluation. We use a unique real-world dataset that pairs production-grade decision models with legal text from the Dutch Environment and Planning Act. These models power the Omgevingsloket government platform, where citizens check permit requirements for environmental activities. We study whether intermediate structured representations can improve LLM-based generation of executable decision models from legal text. We compare four input conditions: raw legal text, text enriched with semantic role labels, text enriched with input and output constraints, and text enriched with both. We evaluate along two dimensions: structural evaluation, through similarity to gold decision models with graph kernels and graphs' descriptive statistics, and outcome evaluation, through functional equivalence by executing models on pre-configured test scenarios. Our findings show that I/O constraints provide the dominant improvement (+37-54% similarity over baseline), while semantic role labels show modest improvements. Outcome evaluation shows that generated models match the gold standard on 51-53% of test scenarios, even though generated models are typically smaller and simpler. We find LLMs eliminate redundant pass-through logic that comprises up to 45-55% of nodes. Importantly, structural similarity and outcome equivalence are complementary: structural similarity does not guarantee outcome equivalence, and vice versa. To facilitate reproducibility, we publicly release our dataset of 95 production decision models with associated legal text and all experimental code.

---

## 论文详细总结（自动生成）

### 论文总结：从法律文本到可执行决策模型

#### 1. 核心问题与整体含义
*   **研究动机**：将复杂的法律文本转化为机器可读、可执行的决策逻辑（如 DMN 标准）是法律信息学中的核心难题。传统方法依赖专家手动编码，效率低下且难以维护。
*   **核心问题**：本研究探讨在大语言模型（LLM）时代，如何通过增强输入表示（如语义角色标注 SRL 和输入输出 I/O 约束）来提升 LLM 生成法律决策模型的准确性与功能等效性。
*   **背景**：研究基于荷兰《环境与规划法》的真实生产数据，旨在探索自动化“法律转代码”（Law-to-Code）的可行路径。

#### 2. 方法论
*   **核心思想**：对比“源端增强”（理解法律文本语义）与“目标端约束”（明确接口规范）两种策略对 LLM 生成质量的影响。
*   **关键技术细节**：
    *   **数据表示简化**：将冗长的 DMN XML 格式转换为轻量级 JSON 格式，在减少 Token 消耗的同时保留完整的图结构和决策表逻辑。
    *   **语义角色标注 (SRL)**：利用预训练的法律专用 BERT 模型 `FlintFiller-srl` 提取法律条文中的执行者、动作、对象和接收者，作为 Prompt 的补充信息。
    *   **I/O 规范约束**：在 Prompt 中直接提供目标模型所需的输入变量名和输出类型，固定决策模型的“接口”。
    *   **生成流程**：采用 1-shot 提示词工程，配合系统消息定义 DMN 语义规范，使用 GPT-5.1 进行模型生成。

#### 3. 实验设计
*   **数据集**：使用荷兰政府 Omgevingsloket 平台的 95 个真实生产级决策模型（包含 50 个 Outcome 模型和 45 个 Requirements 模型）及其对应的法律条文。
*   **对比方法（四种输入条件）**：
    1.  **Text**：仅提供原始法律文本（基准线）。
    2.  **Text+srl**：法律文本 + 语义角色标注。
    3.  **Text+io**：法律文本 + 输入输出规范。
    4.  **Text+srl+io**：结合上述所有增强信息。
*   **评估指标**：
    *   **结构评估**：使用最短路径（Shortest-Path）图核相似度衡量生成模型与金标准在拓扑结构上的接近程度。
    *   **结果评估**：通过执行引擎在 13,080 个测试输入组合上运行模型，计算功能等效性（Outcome Equivalence）。

#### 4. 资源与算力
*   **算力资源**：论文未明确提及具体的 GPU 硬件配置。实验主要通过调用 GPT-5.1 的 API 完成。
*   **实验设置**：温度（Temperature）设为 0.1 以保证结果的相对确定性，开启 JSON 模式确保输出格式合法。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 1,900 次模型生成（95 个目标模型 × 4 种条件 × 5 次独立运行）。
*   **充分性分析**：
    *   **统计显著性**：通过 Wilcoxon 符号秩检验验证了改进的显著性。
    *   **多维度验证**：不仅对比了图结构，还进行了大规模的执行测试，实验设计全面。
    *   **客观性**：使用了真实世界的生产数据而非合成数据，避免了实验室环境下的理想化偏差。

#### 6. 主要结论与发现
*   **I/O 约束占主导地位**：提供输入输出规范对生成质量的提升最为显著（结构相似度提升 37-54%），而 SRL 的作用相对有限且具有类型依赖性。
*   **LLM 具备逻辑简化能力**：生成的模型通常比人工设计的金标准更精简（节点数减少约 2/3），因为 LLM 能够识别并消除人工建模中常见的冗余“直通逻辑”（Pass-through logic）。
*   **结构与功能的互补性**：高结构相似度并不一定带来高功能等效性，反之亦然。这表明法律建模评估必须结合静态结构和动态执行。
*   **性能表现**：在最佳运行条件下，约 33% 的生成模型实现了与金标准的完全功能等效，50% 的模型等效性超过 90%。

#### 7. 优点
*   **数据真实可靠**：采用政府实际运行的法律决策系统数据，具有极高的应用参考价值。
*   **评估视角独特**：深入分析了人工建模规范（如 UR/CR 链）与 LLM 生成逻辑之间的差异，揭示了 LLM 在优化法律逻辑结构方面的潜力。
*   **开源贡献**：公开发布了包含 95 个生产级模型的数据集和实验代码，填补了该领域高质量开源数据的空白。

#### 8. 不足与局限
*   **法域与语言局限**：研究仅限于荷兰法律和荷兰语环境，其结论在其他法系或语言下的普适性有待验证。
*   **模型单一性**：实验仅使用了 GPT-5.1，未探讨其他模型（如 Claude 或开源的 Llama 系列）在相同任务下的表现。
*   **人工依赖**：虽然实现了自动化生成，但 I/O 规范的提取目前仍高度依赖人工定义，尚未实现全流程的“零人工”转化。
*   **复杂逻辑挑战**：对于包含深层嵌套逻辑或大量外部交叉引用的法律条文，生成的准确率仍有待提高。

（完）

---
title: "Faithfulness-QA: A Counterfactual Entity Substitution Dataset for Training Context-Faithful RAG Models"
title_zh: Faithfulness-QA：一个用于训练上下文忠实型 RAG 模型的反事实实体替换数据集
authors: "Li Ju, Junzhe Wang, Qi Zhang"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25313v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 通过反事实替换构建新研究数据集的方法论
tldr: 针对检索增强生成（RAG）模型倾向于依赖内部参数记忆而非检索上下文的问题，本文提出了Faithfulness-QA数据集。该数据集包含约9.9万个样本，通过对SQuAD和TriviaQA中的命名实体进行反事实替换，制造上下文与内部知识的冲突。该资源旨在训练模型优先考虑上下文信息，提高RAG系统的忠实度，并提供完整的构建流程和实体库。
selection_source: fresh_fetch
motivation: RAG模型常因过度依赖内部参数记忆而忽视检索到的上下文，且目前缺乏能训练模型优先选择上下文的专用数据集。
method: 通过将SQuAD和TriviaQA中的答案实体替换为类型一致的替代实体，自动构建具有知识冲突的反事实样本。
result: "成功构建了包含99,094个样本的大规模数据集，并在多项自动化质量检查中达到了100%的合格率。"
conclusion: Faithfulness-QA为训练和评估RAG模型的上下文忠实度提供了关键的资源支持和基准测试。
---

## 摘要
检索增强生成（RAG）模型经常生成基于参数化记忆而非检索上下文的答案，这损害了检索增强的核心承诺。解决这种不忠实性的一个根本障碍是缺乏明确要求模型优先选择上下文而非内部知识的训练数据。我们介绍了 Faithfulness-QA，这是一个包含 99,094 个样本的大规模数据集，通过反事实实体替换构建而成。从两个成熟的抽取式问答基准（SQuAD 和 TriviaQA）出发，我们自动识别每个上下文中包含答案的命名实体，并使用从包含 76,953 个实体的精选库中提取的类型一致的替代实体进行替换，从而在上下文和参数化记忆之间制造受控的知识冲突。严格的质量过滤确保了在对随机 200 个样本进行的四项自动化检查审计中达到 100% 的通过率。我们发布了完整的数据集、构建流水线以及涵盖八个命名实体类别的分类实体库。Faithfulness-QA 被设计为基于注意力的忠实性目标的训练资源，以及衡量 RAG 系统中上下文落地行为的评估基准。数据和代码可在 https://github.com/qzhangFDU/faithfulness-qa-dataset 获取。

## Abstract
Retrieval-Augmented Generation (RAG) models frequently produce answers grounded in parametric memory rather than the retrieved context, undermining the core promise of retrieval augmentation. A fundamental obstacle to fixing this unfaithfulness is the lack of training data that explicitly requires models to prefer context over internal knowledge. We introduce Faithfulness-QA, a large-scale dataset of 99,094 samples constructed through counterfactual entity substitution. Starting from two established extractive QA benchmarks--SQuAD and TriviaQA--we automatically identify answer-bearing named entities in each context, replace them with type-consistent alternatives drawn from a curated bank of 76,953 entities, and thereby manufacture controlled knowledge conflicts between context and parametric memory. Rigorous quality filtering ensures 100% pass rates across four automated checks on random 200-sample audits. We release the full dataset, the construction pipeline, and a typed entity bank covering eight named entity categories. Faithfulness-QA is designed as a training resource for attention-based faithfulness objectives and as an evaluation benchmark for measuring context-grounding behavior in RAG systems. Data and code are available at https://github.com/qzhangFDU/faithfulness-qa-dataset.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Faithfulness-QA** 的大规模数据集，旨在解决检索增强生成（RAG）模型在面对知识冲突时，过度依赖内部参数记忆而忽视检索上下文的问题。

### 1. 核心问题与整体含义
*   **研究动机：** RAG 模型本应根据检索到的文档生成答案，但在实际应用中，当检索内容与模型预训练时记忆的知识冲突时，模型往往会选择“相信”记忆而非上下文（即不忠实性）。
*   **核心障碍：** 现有数据集大多是上下文与记忆一致的，难以区分模型是在“跟随上下文”还是在“背诵记忆”。目前缺乏大规模、专门用于训练模型在冲突下保持忠实性的数据集。
*   **研究目标：** 通过自动化的反事实实体替换，制造受控的知识冲突，迫使模型必须通过阅读上下文才能得出正确答案。

### 2. 方法论
论文提出了一个四阶段的自动化构建流水线：
*   **实体库构建：** 从 Wikipedia 提取并分类了 76,953 个唯一实体，涵盖 PERSON（人名）、GPE（地理政治实体）、ORG（机构）、DATE（日期）等 8 个类别。
*   **答案-实体匹配：** 使用 NER（命名实体识别）工具识别 SQuAD 和 TriviaQA 上下文中的实体，并通过级联策略（精确匹配、子串匹配、位置重叠）将标准答案关联到具体的 NER 实体。
*   **反事实替换（核心）：** 
    1.  从实体库中随机抽取一个与原实体**类型相同**但内容不同的替代实体。
    2.  执行全局替换，将上下文中所有出现的原实体替换为新实体。
    3.  将新实体设为该样本的“忠实答案”（Faithful Answer）。
*   **质量过滤：** 应用 6 项规则检查（如：替换后实体必须存在、上下文长度需大于 50 字符、替换实体不能原本就在文中等），确保生成的样本逻辑自洽且非退化。

### 3. 实验设计
*   **数据集来源：** 基于 **SQuAD**（短篇 Wikipedia 段落）和 **TriviaQA**（长篇阅读理解文档）。
*   **数据规模：** 最终生成 **99,094** 个样本（SQuAD 49,094 个，TriviaQA 50,000 个）。
*   **分析维度：** 
    *   **实体分布：** 分析了 8 种实体类型在两个子集中的分布（TriviaQA 偏向人名，SQuAD 分布较均匀）。
    *   **失败原因分析：** 详细记录了 43.3% 的原始样本被过滤的原因（主要是 NER 未能匹配答案）。
    *   **质量审计：** 随机抽取 200 个样本进行人工/自动混合审计，检查实体清除率、上下文修改有效性等。

### 4. 资源与算力
*   **算力需求：** 该论文强调其构建流水线**不需要 GPU**。
*   **运行效率：** 在**单台 CPU 机器**上，处理约 17.5 万个原始 QA 对并生成最终数据集仅需约 **5.5 小时**。
*   **存储：** 数据集以 JSONL 格式发布，总大小约为 367 MB。

### 5. 实验数量与充分性
*   **实验规模：** 论文对近 10 万个样本进行了全量统计分析，实验规模在同类数据论文中属于较大级别。
*   **充分性：** 论文详细对比了 SQuAD 和 TriviaQA 的互补性，并对过滤机制进行了消解式分析。
*   **局限性：** 实验部分主要集中在**数据集本身的质量验证**，**未包含**使用该数据集训练具体模型（如 Llama 或 Qwen）并对比性能的下游实验。作者将其列为未来工作。

### 6. 主要结论与发现
*   **自动化可行性：** 证明了通过简单的实体替换规则可以大规模生成高质量的反事实数据，成功率达 56.7%。
*   **数据互补性：** 结合不同来源的 QA 数据集可以覆盖更多样化的实体类型和上下文长度，防止模型产生类型偏见。
*   **质量保证：** 经过严格过滤，自动化生成的反事实样本在结构完整性上可以达到 100% 的合格率。

### 7. 优点
*   **规模优势：** 9.9 万的规模远超现有的忠实性评估集（如 FaithEval 的 4.9k），足以支持深度学习模型的微调。
*   **低成本：** 无需昂贵的 LLM 调用或人工标注，利用 CPU 和开源 NER 工具即可复现。
*   **设计精巧：** 通过“类型一致”替换，最大限度保持了句子的语法正确性。

### 8. 不足与局限
*   **指代一致性问题：** 仅执行了字符串替换，未处理代词（如 he/she/his）的共指消解，可能导致微小的语义不连贯。
*   **语义合理性：** 可能会生成“出生在[上海], 纽约州”这种地理逻辑错误的句子（虽然这正是测试模型是否盲从上下文的极端案例）。
*   **语言单一：** 目前仅支持英文。
*   **缺乏模型验证：** 论文更像是一篇“数据资源”论文，缺乏模型在接受该数据训练后在真实 RAG 场景下表现提升的实证数据。

（完）

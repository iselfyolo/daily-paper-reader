---
title: "TingIS: Real-time Risk Event Discovery from Noisy Customer Incidents at Enterprise Scale"
title_zh: TingIS：企业级规模下从噪声客户事件中实时发现风险事件
authors: "Jun Wang, Ziyin Zhang, Rui Wang, Hang Yu, Peng Di, Rui Wang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21889v1"
tags: ["query:kgee"]
score: 8.0
evidence: 实时风险事件发现与多阶段事件链接引擎
tldr: 本文提出 TingIS 系统，旨在从大规模、高噪声的云服务客户投诉中实时发现技术风险。针对传统监控易遗漏风险且客户数据语义复杂、吞吐量大的挑战，TingIS 创新性地结合了高效索引技术与大语言模型（LLM）构建多阶段事件链接引擎，并集成级联路由与多维降噪机制。该系统在实际生产中表现优异，能从少量多样化描述中稳定提取关键事件，显著提升了风险发现的效率与准确性。
selection_source: fresh_fetch
motivation: 尽管客户投诉是发现监控遗漏风险的关键信号，但其极高的噪声、海量数据和复杂的业务语义使得提取有效信息变得异常困难。
method: 系统采用结合 LLM 与索引技术的多阶段事件链接引擎，配合级联路由进行业务归因，并通过多维流水线过滤噪声。
result: "在日均 30 万条消息的生产环境下，TingIS 实现了 3.5 分钟的 P90 告警延迟和 95% 的高优先级事件发现率。"
conclusion: TingIS 成功验证了通过 LLM 增强的事件处理架构在大规模企业级实时风险监控中的实用性和卓越性能。
---

## 摘要
实时检测和缓解技术异常对于大规模云原生服务至关重要，即使是几分钟的停机也可能导致巨大的经济损失和用户信任度下降。虽然客户事件是发现监控遗漏风险的重要信号，但由于极端噪声、高吞吐量以及不同业务线的语义复杂性，从这些数据中提取可操作的情报仍然具有挑战性。在本文中，我们提出了 TingIS，这是一个专为企业级事件发现设计的端到端系统。TingIS 的核心是一个多阶段事件链接引擎，它协同高效的索引技术与大语言模型（LLMs），在事件合并上做出明智决策，从而能够仅从少量多样化的用户描述中稳定提取可操作的事件。该引擎辅以用于精确业务归因的级联路由机制，以及集成领域知识、统计模式和行为过滤的多维降噪流水线。TingIS 部署在处理峰值吞吐量超过每分钟 2,000 条消息、每天 300,000 条消息的生产环境中，实现了 3.5 分钟的 P90 告警延迟和 95% 的高优先级事件发现率。基于真实数据构建的基准测试表明，TingIS 在路由准确性、聚类质量和信噪比方面显著优于基准方法。

## Abstract
Real-time detection and mitigation of technical anomalies are critical for large-scale cloud-native services, where even minutes of downtime can result in massive financial losses and diminished user trust. While customer incidents serve as a vital signal for discovering risks missed by monitoring, extracting actionable intelligence from this data remains challenging due to extreme noise, high throughput, and semantic complexity of diverse business lines. In this paper, we present TingIS, an end-to-end system designed for enterprise-grade incident discovery. At the core of TingIS is a multi-stage event linking engine that synergizes efficient indexing techniques with Large Language Models (LLMs) to make informed decisions on event merging, enabling the stable extraction of actionable incidents from just a handful of diverse user descriptions. This engine is complemented by a cascaded routing mechanism for precise business attribution and a multi-dimensional noise reduction pipeline that integrates domain knowledge, statistical patterns, and behavioral filtering. Deployed in a production environment handling a peak throughput of over 2,000 messages per minute and 300,000 messages per day, TingIS achieves a P90 alert latency of 3.5 minutes and a 95\% discovery rate for high-priority incidents. Benchmarks constructed from real-world data demonstrate that TingIS significantly outperforms baseline methods in routing accuracy, clustering quality, and Signal-to-Noise Ratio.

---

## 论文详细总结（自动生成）

### 论文总结：TingIS - 企业级规模下从噪声客户事件中实时发现风险事件

#### 1. 核心问题与整体含义
*   **研究背景**：在现代云原生架构中，即使是几分钟的系统故障也会导致巨大的经济损失和用户信任危机。
*   **核心痛点**：虽然内部监控（指标、日志等）是第一道防线，但往往存在盲区。客户投诉（在线反馈、热线）能直接反映用户感知的异常，但这类数据具有**极高噪声、口语化严重、语义复杂且吞吐量巨大**（峰值每分钟超过 2000 条）的特点。
*   **研究目标**：开发一个名为 TingIS 的端到端系统，旨在从海量、高噪声的客户投诉流中，仅凭少量（如 3 条）相关描述即可实时、准确地识别出潜在的“风险事件”。

#### 2. 方法论
TingIS 采用三层架构（数据观察层、语义智能引擎、长期知识存储），包含五个核心模块：
*   **M1 语义提炼 (Semantic Distillation)**：利用大语言模型（Qwen3-8B）将非结构化的口语投诉转化为“主体 + 问题”的结构化摘要，并使用 BGE-M3 模型生成向量表示。
*   **M2 级联路由 (Cascaded Routing)**：采用“关键词匹配（高精度）+ 向量检索重排序（高召回）”的瀑布式策略，将投诉精准分发至对应的业务域。
*   **M3 事件链接引擎 (Event Linking Engine - 核心)**：
    *   **批次内聚合**：利用局部敏感哈希（LSH）进行初步聚类，再由 LLM（Kimi-K2）审核聚类纯度并生成事件标题。
    *   **跨批次关联**：计算当前聚类与历史事件的相似度，并引入**时间衰减权重公式** $s^* = s \cdot e^{-k\Delta t}$（防止旧事件错误吸收新事件），最后由 LLM 裁定是合并至已有事件还是新建事件。
*   **M4 状态管理**：通过状态层、审计层和快照层解耦事件的实时波动与历史统计。
*   **M5 多维降噪**：集成负向知识库过滤、基于动态基准（$\mu + 2\sigma$）的统计过滤，以及告警静默与“斜率穿透”（检测到爆发性增长时强制告警）机制。

#### 3. 实验设计
*   **数据集与场景**：基于蚂蚁集团（Alipay）真实生产环境数据构建。
    *   **告警回放集**：约 50,000 条投诉记录，用于模拟系统端到端行为。
    *   **基准事件集**：由 SRE 专家标注的 12 个真实高优先级风险事件。
    *   **事件身份集/路由集**：分别包含 1,400 和 3,200 条标注数据，用于评估聚类和路由模块。
*   **对比方法**：
    *   **系统级**：仅关键词、仅语义、单阶段向量匹配、无降噪版 TingIS。
    *   **算法级**：DBSCAN 聚类算法（经过参数调优）。
*   **评估指标**：风险发现率、P90 告警延迟、B3-F1 分数（评估聚类质量）、误合并率、信噪比（SNR）。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 硬件型号（如 A100 数量），但详细量化了**推理成本**。
*   **Token 消耗**：在处理日均 25 万条投诉的规模下，系统日均消耗约 **800 万个 Token**（其中 Qwen3 占 5M，Kimi-K2 占 3M）。
*   **处理效率**：端到端平均处理延迟为 12.4 秒/批次，其中 LLM 推理环节占总耗时的 69.7%。

#### 5. 实验数量与充分性
*   **实验规模**：进行了为期一个月的在线生产环境部署验证，并配合多组离线基准测试。
*   **消融实验**：针对 M1（摘要）、M2（路由策略）、M3（业务分区与 LLM 裁定）分别进行了消融实验，验证了各组件对提升 B3-F1 分数和降低误合并率的贡献。
*   **充分性评价**：实验设计较为全面，既有宏观的业务指标（发现率、延迟），也有微观的算法指标（聚类纯度），且基于真实工业场景，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **高效性**：TingIS 实现了 **95% 的高优先级事件发现率**，且 P90 告警延迟仅为 **3.5 分钟**。
*   **降噪能力**：多维降噪机制在保持召回率的同时，将告警噪声降低了 **94.3%**，有效缓解了 SRE 团队的告警疲劳。
*   **混合智能的必要性**：实验证明，单纯依靠语义向量聚类在复杂业务场景下极易失效（误合并率高），引入 LLM 的逻辑推理和业务元数据的硬隔离是实现企业级准确度的关键。

#### 7. 优点
*   **架构设计精巧**：通过 LSH 预聚类和阈值门控机制，巧妙地在 LLM 的深度推理能力与大规模实时处理的成本/延迟之间取得了平衡。
*   **工业实战性强**：系统设计充分考虑了业务异构性、流量波动和审计需求，提供了如“斜率穿透”等符合实际运维逻辑的功能。
*   **高信噪比**：通过多层过滤体系，将海量噪声转化为极少数高质量的告警，具有极高的实用价值。

#### 8. 不足与局限
*   **成本门槛**：尽管做了优化，但日均 800 万 Token 的消耗对于资源有限的中小企业仍是一笔不小的开支。
*   **知识库依赖**：路由和降噪效果在很大程度上依赖于预设关键词库和负向知识库的质量，在全新业务上线（冷启动）时可能需要人工干预。
*   **延迟瓶颈**：LLM 推理仍占总耗时的近 70%，在极端流量激增情况下，API 的并发限制和响应延迟可能成为系统瓶颈。

（完）

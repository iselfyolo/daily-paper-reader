---
title: "VG-CoT: Towards Trustworthy Visual Reasoning via Grounded Chain-of-Thought"
title_zh: VG-CoT：通过视觉定位思维链实现可信视觉推理
authors: "Byeonggeuk Lim, Kyeonghyun Kim, JungMin Yun, YoungBin Kim"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21396v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 自动生成视觉推理数据集的流水线
tldr: 该研究针对大型视觉语言模型（LVLM）在局部区域推理中缺乏视觉证据对齐的问题，提出了VG-CoT数据集。通过全自动的三阶段流水线（视觉证据提取、GPT-4o生成推理、逻辑驱动细化），将推理步骤与图像区域显式关联。同时引入了涵盖推理质量、准确率和对齐度的评估基准。实验证明，该方法显著提升了模型的推理可信度与准确性。
selection_source: fresh_fetch
motivation: 现有LVLM数据集在多步推理与图像区域的显式对齐上存在局限，且人工标注成本高，难以扩展。
method: 提出一种全自动三阶段流水线，利用检测/OCR模型提取证据，结合GPT-4o生成推理链，并通过逻辑驱动的检测过程细化视觉定位。
result: 在LLaVA-1.5和Qwen2-VL等模型上的实验表明，VG-CoT在推理质量、答案准确性和逻辑对齐度等多个维度上均有显著提升。
conclusion: VG-CoT通过自动化构建高质量的视觉定位推理链，有效增强了LVLM的可信度与循证推理能力。
---

## 摘要
大视觉语言模型（LVLMs）的发展需要精确的基于局部区域的推理，从而将模型的逻辑忠实地建立在实际视觉证据之上。然而，现有数据集由于大量的标注工作以及多步推理与相应图像区域之间缺乏显式对齐，在可扩展性方面面临限制，这制约了对模型可信度的评估。为了应对这些挑战，我们提出了视觉定位思维链（VG-CoT）数据集，该数据集通过一个全自动的三阶段流水线，将每个推理步骤显式地链接到图像中的真实视觉证据。该流水线首先利用最先进的检测和 OCR 模型提取物体级和文本级的视觉证据，然后使用 GPT-4o 生成逐步的定位推理，最后通过基于原理驱动的开集检测过程来优化定位。此外，我们引入了一个新的基准测试，从三个互补维度全面评估 LVLMs 的推理能力：原理质量（Rationale Quality）、答案准确性（Answer Accuracy）以及推理-答案一致性（Reasoning-Answer Alignment）。对包括 LLaVA-1.5 和 Qwen2-VL 在内的代表性 LVLMs 进行的实验表明，在大多数评估指标上都有持续的提升，证实了 VG-CoT 在保持数据集构建的可扩展性和成本效益的同时，有效地增强了可信的、基于证据的推理。数据集和代码将在论文被接收后公开发布，以促进进一步的研究。

## Abstract
The advancement of Large Vision-Language Models (LVLMs) requires precise local region-based reasoning that faithfully grounds the model's logic in actual visual evidence. However, existing datasets face limitations in scalability due to extensive manual annotation and lack of explicit alignment between multi-step reasoning and corresponding image regions, which constrains the evaluation of model trustworthiness. To address these challenges, we propose the Visual Grounding Chain-of-Thought (VG-CoT) dataset, which explicitly links each reasoning step to real visual evidence within the image through a fully automated three-stage pipeline. The pipeline first extracts object- and text-level visual evidence using state-of-the-art detection and OCR models, then generates step-by-step grounded reasoning with GPT-4o, and finally refines the grounding through a rationale-driven open-set detection process. In addition, we introduce a new benchmark that comprehensively evaluates LVLMs reasoning across three complementary dimensions: Rationale Quality, Answer Accuracy, and Reasoning-Answer Alignment. Experiments with representative LVLMs, including LLaVA-1.5 and Qwen2-VL, demonstrate consistent improvements on most evaluation metrics, confirming that VG-CoT effectively enhances trustworthy, evidence-based reasoning while maintaining scalable and cost-efficient dataset construction. The dataset and code will be released publicly upon acceptance to facilitate further research.

---

## 论文详细总结（自动生成）

这是一份关于论文《VG-CoT: Towards Trustworthy Visual Reasoning via Grounded Chain-of-Thought》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大型视觉语言模型（LVLMs）虽然在全局图像理解上表现出色，但在涉及局部区域的精确推理（如自动驾驶、医疗分析）时仍面临挑战。
*   **核心痛点**：
    1.  **可扩展性受限**：现有包含局部信息的数据集高度依赖人工标注，成本高且难以大规模扩展。
    2.  **逻辑与视觉脱节**：现有的多模态思维链（CoT）数据集往往缺乏推理步骤与图像具体空间坐标（Bounding Box）之间的显式关联，导致无法验证模型是否基于正确的视觉证据得出结论。
*   **研究动机**：开发一种全自动化的流水线，构建一个将推理逻辑与视觉证据（坐标）深度对齐的数据集，以提升模型的可信度和循证推理能力。

### 2. 方法论
论文提出了一种**全自动三阶段流水线**来构建 VG-CoT 数据集：
*   **阶段 1：多粒度视觉证据提取**：利用 YOLO 模型提取物体级证据，利用 PaddleOCR 提取文本级证据。对于 GQA 数据集，还结合了其原有的场景图（Scene Graph）信息。
*   **阶段 2：视觉定位 CoT 生成**：将图像、问题、答案及阶段 1 提取的证据输入 GPT-4o。通过精心设计的 Prompt，强制要求 GPT-4o 在生成每一步推理逻辑时，必须引用对应的空间坐标。
*   **阶段 3：原理驱动的定位细化**：使用 NLTK 提取推理链中的关键名词，将其作为查询词输入开集检测器 Grounding DINO，从而对推理中提到的细微或上下文相关的物体进行更精确的坐标修正和对齐。

### 3. 实验设计
*   **数据集来源**：整合了三个领域的原始数据：TextVQA（文本密集型）、Visual7W（物体识别）、GQA（复杂关系推理），共计 13,826 个样本。
*   **评估基准（Benchmark）**：提出了一个三维评估体系：
    1.  **原理质量 (RQ)**：利用 GPT-4o 评估推理的视觉证据利用、连贯性和完整性。
    2.  **答案准确性 (AA)**：最终预测结果的正确率。
    3.  **推理-答案一致性 (RAA)**：包括一致性得分（逻辑好且答案对，或逻辑差且答案错）和忠实度得分（逻辑好的样本中答案正确的比例）。
*   **对比方法**：对 LLaVA-1.5 (7B/13B)、Qwen2-VL-7B-Instruct 和 Qwen2.5-VL-7B-Instruct 进行了微调前后的对比实验。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H100 (80GB)** GPU。
*   **训练细节**：采用 LoRA（低秩自适应）进行参数高效微调（PEFT）。
*   **超参数**：训练 3 个 epoch，学习率为 2e-4，batch size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：总样本量约 1.4 万，涵盖了从通用 VQA 到关系推理及场景文本的三大任务。
*   **充分性**：实验覆盖了当前主流的开源 LVLM 架构（LLaVA 和 Qwen 系列），并针对不同参数规模（7B, 13B）进行了测试。
*   **客观性**：除了使用 GPT-4o 作为主观评估器外，还引入了 mAP@0.5 和 mAP@0.75 等客观的物体检测指标来衡量模型预测坐标的精准度，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能提升**：微调后的模型在所有评估维度（RQ, AA, RAA）上均有显著提升。例如，LLaVA-1.5-7B 的答案准确率从 48.7% 提升至 62.5%。
*   **定位瓶颈**：实验发现，精确的空间定位（Visual Evidence 分数）普遍低于逻辑文本生成的质量，说明空间对齐仍是 LVLM 的核心挑战。
*   **任务差异**：模型在处理大型物体（关系推理）时定位较准，但在处理细小且密集的文本（Scene-text）时精度显著下降（mAP 仅约 30%）。
*   **模型规模效应**：13B 模型在定位精度（mAP）上明显优于 7B 模型。

### 7. 优点
*   **全自动化构建**：无需人工干预，解决了多模态 CoT 数据稀缺和标注昂贵的问题。
*   **深度对齐**：首次在推理的每一个逻辑步骤中强制嵌入空间坐标，极大地增强了模型推理的可解释性和可信度。
*   **综合评估体系**：新基准不仅关注“结果对不对”，更关注“过程对不对”以及“过程与结果是否匹配”。

### 8. 不足与局限
*   **基础模型依赖**：流水线的质量高度依赖于上游模型（如 YOLO, PaddleOCR, GPT-4o）的初始表现，存在误差累积风险。
*   **领域覆盖**：目前主要集中在通用视觉任务，尚未在医疗影像、工程制图等高度专业化的领域进行验证。
*   **定位精度限制**：虽然模型学会了“大概在哪里”，但在高 IoU 阈值（如 mAP@0.75）下的表现依然较弱，难以实现极高精度的视觉引导。

（完）

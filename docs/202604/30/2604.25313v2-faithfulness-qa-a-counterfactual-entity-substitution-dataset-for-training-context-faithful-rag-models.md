---
title: "Faithfulness-QA: A Counterfactual Entity Substitution Dataset for Training Context-Faithful RAG Models"
title_zh: Faithfulness-QA：用于训练上下文忠实 RAG 模型的反事实实体替换数据集
authors: "Li Ju, Junzhe Wang, Qi Zhang"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25313v2"
tags: ["query:ds-gen"]
score: 9.0
evidence: 通过实体替换构建新研究数据集的方法论
tldr: 针对检索增强生成（RAG）模型倾向于依赖内部参数记忆而非检索上下文的问题，本文提出了Faithfulness-QA数据集。该数据集包含约9.9万个样本，通过对SQuAD和TriviaQA中的命名实体进行反事实替换，制造上下文与内部知识的冲突。该资源旨在训练模型优先考虑上下文信息，提高RAG系统的忠实度，并提供完整的构建流程和实体库。
selection_source: fresh_fetch
motivation: RAG模型常因过度依赖内部参数记忆而产生不忠实回答，且缺乏能促使模型优先选择上下文的训练数据。
method: 利用反事实实体替换技术，将原始QA数据集中的实体替换为类型一致的替代项，从而制造受控的知识冲突。
result: "构建了包含99,094个样本的大规模数据集，并在多项自动化质量检查中达到了100%的合格率。"
conclusion: Faithfulness-QA为训练和评估RAG系统的上下文忠实度提供了关键的资源支持。
---

## 摘要
检索增强生成（RAG）模型经常生成基于参数化记忆而非检索上下文的答案，这削弱了检索增强的核心承诺。解决这种不忠实性的一个根本障碍是缺乏明确要求模型优先选择上下文而非内部知识的训练数据。我们介绍了 Faithfulness-QA，这是一个包含 99,094 个样本的大规模数据集，通过反事实实体替换构建而成。从两个成熟的抽取式问答基准（SQuAD 和 TriviaQA）出发，我们自动识别每个上下文中包含答案的命名实体，并使用从包含 76,953 个实体的精选库中提取的类型一致的替代实体进行替换，从而在上下文和参数化记忆之间制造受控的知识冲突。严格的质量过滤确保了在对随机 200 个样本进行的四项自动化检查审计中达到 100% 的通过率。我们发布了完整的数据集、构建流水线以及涵盖八个命名实体类别的分类实体库。Faithfulness-QA 被设计为基于注意力的忠实性目标的训练资源，以及衡量 RAG 系统中上下文落地行为的评估基准。数据和代码可在 https://github.com/qzhangFDU/faithfulness-qa-dataset 获取。

## Abstract
Retrieval-Augmented Generation (RAG) models frequently produce answers grounded in parametric memory rather than the retrieved context, undermining the core promise of retrieval augmentation. A fundamental obstacle to fixing this unfaithfulness is the lack of training data that explicitly requires models to prefer context over internal knowledge. We introduce Faithfulness-QA, a large-scale dataset of 99,094 samples constructed through counterfactual entity substitution. Starting from two established extractive QA benchmarks--SQuAD and TriviaQA--we automatically identify answer-bearing named entities in each context, replace them with type-consistent alternatives drawn from a curated bank of 76,953 entities, and thereby manufacture controlled knowledge conflicts between context and parametric memory. Rigorous quality filtering ensures 100% pass rates across four automated checks on random 200-sample audits. We release the full dataset, the construction pipeline, and a typed entity bank covering eight named entity categories. Faithfulness-QA is designed as a training resource for attention-based faithfulness objectives and as an evaluation benchmark for measuring context-grounding behavior in RAG systems. Data and code are available at https://github.com/qzhangFDU/faithfulness-qa-dataset.

---

## 论文详细总结（自动生成）

### 论文总结：Faithfulness-QA 数据集构建与分析

#### 1. 核心问题与整体含义（研究动机和背景）
检索增强生成（RAG）模型的核心承诺是基于检索到的上下文生成答案。然而，在实际应用中，模型往往会忽略上下文，转而依赖预训练阶段获得的“参数化记忆”，这种现象被称为“不忠实性”（Unfaithfulness）。
**核心障碍**在于：现有的训练目标（如标准交叉熵损失）无法区分信息来源，且缺乏大规模的训练数据来强制模型在上下文与内部知识发生冲突时优先选择上下文。本文旨在通过构建大规模的反事实实体替换数据集，为训练“上下文忠实”的 RAG 模型提供资源。

#### 2. 方法论：核心思想与算法流程
论文提出了一个四阶段的自动化流水线，通过**反事实实体替换（Counterfactual Entity Substitution）**制造受控的知识冲突：
*   **阶段 1：实体库构建**：从 Wikipedia 中提取并分类了 76,953 个唯一实体，涵盖 PERSON、GPE、ORG、DATE 等 8 类。
*   **阶段 2：答案-实体匹配**：利用 NER（命名实体识别）和三级级联策略（精确匹配、子串匹配、位置重叠）将 QA 对中的答案关联到上下文中的特定实体。
*   **阶段 3：反事实替换**：从实体库中随机抽取一个**类型一致**且**长度兼容**（长度比在 0.3 到 3.0 之间）的替代实体，替换上下文中所有出现的原始实体。
*   **阶段 4：质量过滤与划分**：应用 6 项基于规则的检查（如确保替换后上下文长度合理、替换实体不在原文中等），最后按 80/10/10 比例划分训练、开发和测试集。

#### 3. 实验设计
由于本文侧重于**数据集的构建与验证**，其实验设计主要集中在数据质量分析上：
*   **源数据集**：使用了 SQuAD（短上下文，实体分布均衡）和 TriviaQA（长上下文，侧重人物实体）。
*   **基准对比**：对比了现有的 FaithEval（仅 4.9K 样本，主要用于评估），强调了 Faithfulness-QA 在规模（99K）和训练用途上的优势。
*   **质量审计**：从 SQuAD 子集中随机抽取 200 个样本，针对“实体存在性”、“原始实体清除”、“上下文修改有效性”和“长度比例”四个维度进行 100% 覆盖的自动化审计。

#### 4. 资源与算力
*   **算力需求**：该流水线设计极其高效，**不需要 GPU**。
*   **运行时间**：在单台 CPU 机器上，处理 17.4 万个原始样本并生成最终数据集仅需约 **5.5 小时**。
*   **存储**：最终数据集大小约为 367 MB（JSONL 格式）。

#### 5. 实验数量与充分性
*   **数据规模**：最终生成了 99,094 个高质量样本，规模足以支持深度学习模型的微调。
*   **分析维度**：论文对失败原因（如 NER 未匹配占 64%）、实体类型分布、上下文长度统计等进行了详尽的定量分析。
*   **充分性评价**：作为数据集论文，其对构建过程的验证非常充分。虽然论文未包含下游模型的训练实验（列为未来工作），但其提供的质量审计结果（100% 通过率）证明了数据生成的可靠性。

#### 6. 主要结论与发现
*   **自动化可行性**：证明了通过简单的实体替换规则可以大规模制造高质量的知识冲突数据。
*   **数据互补性**：SQuAD 和 TriviaQA 在实体类型和上下文长度上具有很强的互补性，结合使用能提高模型的泛化能力。
*   **冲突的重要性**：只有当上下文与模型内部记忆矛盾时，才能真正衡量和训练模型“遵循上下文”的能力。

#### 7. 优点
*   **规模巨大**：9.9 万个样本，比同类评估基准大一个数量级。
*   **低成本高效率**：无需昂贵的 LLM 标注或 GPU 算力，纯 CPU 即可快速复现。
*   **控制严密**：通过类型一致性约束和多重质量过滤，确保了反事实样本的结构正确性。
*   **开源贡献**：同时发布了数据集、代码和包含 7.6 万实体的分类库。

#### 8. 不足与局限
*   **缺乏指代消解**：替换实体后，文中的代词（如 he/she）未同步更新，可能导致细微的语义不一致。
*   **语义合理性风险**：部分替换可能产生逻辑荒谬的句子（如“出生在伊利诺伊州的阿姆利则市”），虽然语法正确但常识违和。
*   **语言限制**：目前仅支持英语。
*   **缺乏下游验证**：论文未展示使用该数据集训练后的模型在实际 RAG 任务中的性能提升指标。

（完）

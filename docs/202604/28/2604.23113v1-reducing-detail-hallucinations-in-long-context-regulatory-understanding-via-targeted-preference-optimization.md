---
title: Reducing Detail Hallucinations in Long-Context Regulatory Understanding via Targeted Preference Optimization
title_zh: 通过针对性偏好优化减少长上下文规制理解中的细节幻觉
authors: "Yang Liu, Bin Chong, Yuhan Lin, Chongyang Zhang, Hao Zheng, Ziyi Zhang, Jiayu Liang, Ran Ran, Qian Li, Kefu Xu"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23113v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 理解长篇监管文件和义务级别
tldr: "针对大语言模型在处理长篇监管文档时容易产生细节幻觉（如数值、单位、义务等级错误）的问题，本文提出了包含五种错误类型的细节错误分类法，并构建了包含1.3万个偏好对的基准测试集DetailBench。研究进一步提出了DetailDPO框架，通过构建仅在单一细节维度上存在差异的对比对，将优化信号集中在关键细节Token上。实验表明，该方法在多种模型和长文本场景下将细节错误率降低了42-61%，并具有良好的跨领域迁移能力。"
selection_source: fresh_fetch
motivation: 大语言模型在理解长篇监管文档时常出现看似合理但关键参数错误的细节幻觉，严重影响安全敏感场景的应用。
method: 提出了细节错误分类法，构建了DetailBench基准，并开发了利用最小细节扰动对比对进行针对性偏好优化的DetailDPO框架。
result: 在Qwen2.5和Llama-3.1等模型上的实验显示，DetailDPO显著降低了细节错误率，且在金融和医疗领域表现出良好的泛化性。
conclusion: 通过针对性的细节偏好优化，可以有效减少长文本理解中的细粒度幻觉，提升模型在专业监管领域的可靠性。
---

## 摘要
大型语言模型（LLMs）在处理长篇规制文档时经常产生“细节幻觉”，包括在阈值、单位、范围、义务级别和条件方面出现的细微错误。这些错误在保持表面合理性的同时，破坏了安全关键参数。我们通过包含五种错误类型的细粒度“细节错误分类法”将这一现象形式化，并推出了 DetailBench。这是一个基于来自三个司法管辖区的 172 份真实规制文档和 150 份合成文档构建的基准，包含 13,000 个由人工标注的细节级真实偏好对。我们提出了 DetailDPO，这是一种针对性的偏好优化框架，通过构建仅在单个细节维度上存在差异的对比对，将 DPO 梯度信号集中在承载细节的标记（tokens）上。我们提供了理论分析，证明了在温和假设下，为什么“最小细节扰动”对会产生梯度集中。在 Qwen2.5 系列（7B、14B、72B）和 Llama-3.1-8B 上跨三个上下文长度层级（8K-64K tokens）的实验表明，DetailDPO 相对于基准模型将细节错误率降低了 42-61%，在所有五种错误类型上均取得了一致的提升，并展现出向金融和医疗文档的跨领域迁移能力。

## Abstract
Large language models (LLMs) frequently produce \emph{detail hallucinations} when processing long regulatory documents, including subtle errors in threshold values, units, scopes, obligation levels, and conditions that preserve surface plausibility while corrupting safety-critical parameters. We formalize this phenomenon through a fine-grained \emph{Detail Error Taxonomy} of five error types and introduce \textbf{DetailBench}, a benchmark built from 172 real regulatory documents and 150 synthetic documents spanning three jurisdictions, with human-annotated detail-level ground truth comprising 13,000 preference pairs. We propose \textbf{DetailDPO}, a targeted preference optimization framework that constructs contrastive pairs differing in exactly one detail dimension, concentrating DPO gradient signal on detail-bearing~tokens. We provide theoretical analysis showing why \emph{minimal detail perturbation} pairs yield gradient concentration under mild assumptions. Experiments on the Qwen2.5 family (7B, 14B, 72B) and Llama-3.1-8B across three context-length tiers (8K--64K tokens) show that DetailDPO reduces the Detail Error Rate by 42--61\% relative to baselines, with consistent gains across all five error types and cross-domain transfer to financial and medical documents.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在处理长篇专业文档时极易出现的“细节幻觉”问题，提出了一套从分类法、基准测试到优化框架的完整解决方案。以下是对该论文的深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在能源、医药、金融等安全敏感领域，LLM 处理长篇监管文档时常出现“细节幻觉”（Detail Hallucination）。这种幻觉不同于完全的胡编乱造，它生成的文本在结构和叙述上高度合理，但会悄悄篡改关键参数（如压力阈值、计量单位、法律义务等级等）。
*   **核心挑战**：
    *   缺乏对细节错误的细粒度刻画。
    *   现有长文本基准测试主要关注整体理解，而非孤立的细节忠实度。
    *   标准偏好优化（DPO）将所有错误同等对待，无法将学习信号聚焦在关键的细节 Token 上。

### 2. 论文提出的方法论
*   **细节错误分类法（Detail Error Taxonomy）**：将错误分为五类：阈值（数值错误）、单位（单位混淆）、范围（适用对象扩大或缩小）、等级（如“必须”变“应该”）、条件（前提条款缺失或修改）。
*   **DetailDPO 框架**：
    *   **最小细节扰动（Minimal Detail Perturbation）**：这是核心思想。在构造 DPO 偏好对时，确保“选中（Chosen）”和“拒绝（Rejected）”的回复仅在**一个**细节维度上存在差异，其余 Token 完全一致。
    *   **梯度集中理论**：论文通过数学证明，在这种最小扰动下，DPO 的梯度会高度集中在发生变化的细节 Token 上，避免了学习信号被长文本中的无关信息稀释。
    *   **证据引用机制**：在训练目标中加入证据监督项（Evidence Supervision），强制模型在给出结论时引用原文，增强可追溯性。

### 3. 实验设计
*   **数据集与 Benchmark (DetailBench)**：
    *   **数据来源**：包含 172 份真实监管文档（中国 GB 标准、美国 CFR、欧盟 EUR-Lex）和 150 份合成文档。
    *   **规模**：13,000 个偏好对（10K 训练，1K 验证，2K 测试）。
    *   **测试层级**：按上下文长度分为 Short (8K-16K)、Medium (16K-32K)、Long (32K-64K) 三个梯度。
*   **对比方法**：
    *   **基础模型**：Qwen2.5 (7B/14B/72B) 和 Llama-3.1-8B。
    *   **对齐基准**：SFT、RLHF (PPO)、通用 DPO、KTO、SimPO。
    *   **外部基准**：GPT-4o、Claude-3.5-Sonnet、RAG + Zero-shot。

### 4. 资源与算力
论文详细列出了不同规模模型的训练开销（均使用 LoRA 训练）：
*   **Qwen2.5-7B / Llama-3.1-8B**：使用 4 × A100-80GB GPU，训练时长约 18-20 小时。
*   **Qwen2.5-14B**：使用 4 × A100-80GB GPU，训练时长约 30 小时。
*   **Qwen2.5-72B**：使用 8 × A100-80GB GPU，配合 DeepSpeed ZeRO-3，训练时长约 72 小时。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨模型家族（Qwen, Llama）、跨参数规模（7B 到 72B）、跨上下文长度（最高 64K）的多维度实验。
*   **消融实验**：验证了最小扰动策略、证据监督、合成数据对最终效果的贡献。
*   **跨领域迁移**：在未见过的金融（SEC 文件）和医疗（临床指南）领域进行了测试，验证了方法的通用性。
*   **客观性**：通过梯度分析（Empirical Gradient Analysis）从底层证实了梯度集中的假设，实验设计严谨，对比基准涵盖了当前主流的对齐算法。

### 6. 主要结论与发现
*   **显著提升**：DetailDPO 将细节错误率（DER）降低了 42-61%，在 72B 模型上 DER 降至 0.06（即每 16 个细节仅 1 个错误）。
*   **长文本优势**：随着上下文长度增加，Zero-shot 的错误率剧增，而 DetailDPO 的领先优势在长文本下更加扩大。
*   **错误类型差异**：数值（阈值）和单位错误的修复效果最好，因为其 Token 表现最局部化；范围和条件错误由于涉及更长的 Token 跨度，提升幅度略小。
*   **超越闭源模型**：经过 DetailDPO 优化的 7B 模型在细节忠实度上甚至超过了 Zero-shot 的 GPT-4o。

### 7. 优点
*   **针对性极强**：不同于通用的对齐方法，它精准打击了专业领域最痛的“细粒度幻觉”问题。
*   **理论与实践结合**：不仅提出了方法，还给出了梯度集中的理论证明，并得到了实验数据的支撑。
*   **高质量基准**：DetailBench 覆盖了多国司法管辖区，具有很强的行业参考价值。
*   **可解释性**：通过证据引用机制，让模型的输出不再是黑盒，方便人工复核。

### 8. 不足与局限
*   **长文本瓶颈**：尽管有显著提升，但在 64K 长度下 DER 仍有 15%，说明超长上下文的细节保持依然是一个未完全解决的难题。
*   **标注依赖**：构建 DetailBench 需要极高水平的专家标注，这限制了该方法向更多细分领域的快速扩张。
*   **语义复杂性**：对于涉及复杂逻辑判断的“条件错误（$\tau_5$）”，提升效果弱于简单的数值替换，说明模型对深层法律逻辑的理解仍有提升空间。

（完）

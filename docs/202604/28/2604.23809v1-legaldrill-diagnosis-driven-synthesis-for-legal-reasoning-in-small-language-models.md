---
title: "LegalDrill: Diagnosis-Driven Synthesis for Legal Reasoning in Small Language Models"
authors: "Tianchun Li, Haochen Liu, Vishwa Pardeshi, Xingchen Wang, Tianci Liu, Huijun Zhao, Wei Fan, Jing Gao"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23809v1"
tags: ["query:profile-3"]
score: 10.0
evidence: 小语言模型中的法律推理与法规解释合成
tldr: 针对小语言模型（SLM）在复杂法律推理任务中表现不佳且高质量推理数据匮乏的问题，本文提出 LegalDrill 框架。该框架通过诊断驱动的合成方法，利用教师模型提取并迭代优化细粒度的推理轨迹，并结合自我反思验证机制筛选高质量数据，用于 SLM 的监督微调和偏好优化。实验证明，该方法在无需专家标注的情况下显著提升了 SLM 的法律推理能力，为构建实用法律系统提供了可扩展路径。
selection_source: fresh_fetch
motivation: 小语言模型在处理高风险法律推理时缺乏逻辑一致性，且获取高质量的人工标注推理数据成本极高。
method: 提出 LegalDrill 框架，通过细粒度提示从教师模型提取推理轨迹，并利用自我反思验证进行自适应数据筛选与优化。
result: 在多个法律基准测试上的实验表明，LegalDrill 显著增强了代表性小语言模型的法律推理性能。
conclusion: 该研究证明了通过诊断驱动的合成数据训练，可以在不依赖专家标注的情况下有效提升小模型的法律推理能力。
---

## Abstract
Small language models (SLMs) are promising for real-world deployment due to their efficiency and low operational cost. However, their limited capacity struggles with high-stakes legal reasoning tasks that require coherent statute interpretation and logically consistent deduction. Furthermore, training SLMs for such tasks demands high-quality, concise reasoning trajectories, which are prohibitively expensive to manually collect and difficult to curate via standard rejection sampling, lacking granularity beyond final verdicts. To address these challenges, we propose {LegalDrill}, a diagnosis-driven synthesis framework that extracts and iteratively refines reasoning trajectories from a capable teacher via fine-grained prompting, then a self-reflective verification is employed to adaptively select the most effective data for the SLM student. The resulting data empower SLM training through supervised fine-tuning and direct preference optimization. Extensive experiments on several legal benchmarks demonstrate that {LegalDrill} significantly bolsters the legal reasoning capabilities of representative SLMs while bypassing the need for scarce expert annotations, paving a scalable path toward practical legal reasoning systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LegalDrill** 的框架，旨在通过诊断驱动的合成数据提升小语言模型（SLMs）在法律推理任务中的表现。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：法律推理（如法规解释、逻辑演绎）对模型能力要求极高，且涉及大量敏感隐私数据，因此在本地部署高效、低成本的小语言模型（SLM，通常指参数量 < 3B）具有重要意义。
*   **核心挑战**：
    1.  **能力受限**：SLM 难以处理多步骤、依赖性强的法律逻辑。
    2.  **数据匮乏**：高质量法律推理轨迹的人工标注成本极高。
    3.  **学习失配**：强模型（如 GPT-4）生成的推理链往往过于冗长，SLM 难以有效吸收和模仿这种复杂的思维过程。
*   **核心目标**：通过一种自动化的、诊断驱动的合成框架，为 SLM 量身定制简洁且针对性强的法律推理训练数据。

### 2. 方法论
LegalDrill 是一个迭代框架，包含以下关键技术环节：
*   **诊断驱动的合成（Diagnosis-Driven Synthesis）**：
    *   **探索（Exploration）**：让学生模型（SLM）对法律问题生成初步回答。
    *   **诊断（Diagnosis）**：使用一个“审计智能体”（Audit Agent，由强模型担任）分析 SLM 的错误根源（如误读条款、逻辑跳跃），并将其抽象为“上下文无关”的错误指令。
    *   **定向生成（Targeted Generation）**：利用错误指令库，让教师模型生成偏好对——一个模仿特定错误逻辑的“拒绝响应”和一个简洁纠错的“选中响应”。
*   **自我反思验证（Self-Reflective Verification）**：
    *   引入**难度评分（Difficulty Score, DS）**：利用 SLM 自身的概率分布，计算其对“正确”与“错误”推理的辨别难度。
    *   **数据筛选**：仅保留 SLM 感到困惑（即 DS 高）的样本，确保训练集集中在模型的盲点上。
*   **优化目标**：
    *   **冷启动**：先进行监督微调（SFT）。
    *   **偏好对齐**：使用直接偏好优化（DPO）在筛选后的偏好对上进行迭代训练。

### 3. 实验设计
*   **数据集**：
    *   **公开基准（LegalBench）**：包含 Consumer QA（消费者问答）、Contracts QA（合同问答）、Sara Entailment（税务法定推理）、Privacy Policy Entailment（隐私政策蕴含）。
    *   **真实场景（Real-World）**：来自金融行业的专有数据集，包括 POA（授权委托书）和 Trust（信托文档）的合规性审查。
*   **基准模型（Backbones）**：Qwen3-0.6B 和 Qwen3-1.7B。
*   **对比方法**：
    *   通用 SLM 和 LLM（GPT-4o, Qwen3-30B）。
    *   法律专用大模型：Law-LLM-13B、DeepSeek ESFT-16B、DiscLaw-13B。
*   **评估指标**：准确率（Acc）、F1 分数，以及使用 LLM 作为裁判的“推理质量评分”（Judge Accuracy）。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A6000 GPU**。
*   **训练细节**：
    *   对 0.6B 和 1.7B 模型进行了**全参数微调**（而非 PEFT）。
    *   DPO 训练通常进行 1-3 个 epoch。
    *   学习率设定在 $1 \times 10^{-6}$ 到 $1 \times 10^{-4}$ 之间。
*   **时长**：文中未明确给出总训练时长，但提到推理能力在 1-2 次迭代后即可达到显著提升。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同的法律数据集，对比了从 0.6B 到 30B 不等规模的多个模型。
*   **消融实验**：
    *   验证了 **DPO 相比仅 SFT** 的优越性。
    *   分析了**迭代次数**的影响（发现 2 次迭代效果最佳）。
    *   探讨了**合成样本数量 K** 对性能的影响。
*   **客观性**：通过引入第三方 LLM 裁判和真实工业界数据集，增强了实验的现实意义和说服力。实验设计考虑了类别不平衡（报告 F1）和推理逻辑的正确性，较为全面。

### 6. 主要结论与发现
*   **性能飞跃**：LegalDrill 显著提升了 SLM 的法律推理能力，1.7B 的模型在多个任务上接近或达到了 30B 教师模型的水平。
*   **简洁性至关重要**：针对 SLM 优化的简洁推理轨迹比冗长的思维链更有效。
*   **盲点修复**：通过自我反思验证筛选出的“高难度”样本能更高效地利用有限的参数容量。
*   **工业价值**：在处理长文档和复杂金融合规任务时，该方法表现出极强的实用性。

### 7. 优点
*   **诊断精准**：不只是简单的拒绝采样，而是通过 Audit Agent 定位逻辑缺陷，实现了“对症下药”。
*   **数据高效**：通过上下文无关的错误指令扩展了训练数据，且通过难度评分过滤了冗余样本。
*   **部署友好**：成功将强大的法律推理能力压缩进极小规模的模型中，有利于隐私保护和本地化部署。

### 8. 不足与局限
*   **教师依赖**：框架仍高度依赖于一个强大的教师模型（如 GPT-4o）来进行诊断和生成，若教师模型本身存在法律偏见，可能会传递给学生。
*   **超参数敏感**：DPO 过程中的学习率、权重衰减等超参数需要仔细调优，增加了训练复杂性。
*   **任务局限**：目前主要集中在二元分类（Yes/No）形式的法律问答，对于更复杂的法律文书生成或多项选择任务的泛化性有待进一步验证。

（完）

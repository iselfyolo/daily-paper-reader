---
title: Continuous Adversarial Flow Models
title_zh: 连续对抗流模型
authors: "Shanchuan Lin, Ceyuan Yang, Zhijie Lin, Hao Chen, Haoqi Fan"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11521v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于高质量图像生成的连续对抗流模型
tldr: 本研究提出连续对抗流模型，将对抗性训练引入连续时间流模型中。不同于传统流匹配使用的均方误差准则，该方法利用可学习的判别器引导训练，使生成的样本更贴合目标分布。该方法主要用于现有流匹配模型的后训练，在ImageNet 256px及文本生成图像任务上显著提升了生成质量，大幅降低了FID指标，证明了对抗目标在优化流模型性能方面的有效性。
selection_source: fresh_fetch
motivation: 传统的流匹配模型依赖固定的均方误差准则，难以使生成的样本分布与目标数据分布达到最优对齐。
method: 引入可学习的判别器构建对抗性目标，通过后训练方式优化现有的连续时间流模型（如SiT和JiT）。
result: 在ImageNet 256px任务中，该方法将SiT模型的无引导FID从8.26显著降低至3.63，并在文本生成图像基准测试中取得领先结果。
conclusion: 对抗性目标能有效诱导更优的广义分布，是提升预训练流模型生成性能和对齐精度的有效途径。
---

## 摘要
我们提出了连续对抗流模型，这是一种采用对抗性目标训练的连续时间流模型。与使用固定均方误差准则的流匹配（flow matching）不同，我们的方法引入了一个可学习的判别器来引导训练。这种目标的改变诱导出了不同的广义分布，实验表明，该分布生成的样本与目标数据分布的对齐效果更好。我们的方法主要针对现有流匹配模型的后训练（post-training）而提出，尽管它也可以用于从头训练模型。在 ImageNet 256px 生成任务上，我们的后训练将潜空间 SiT 的无引导 FID 从 8.26 显著降低至 3.63，将像素空间 JiT 的无引导 FID 从 7.17 降低至 3.57。它还改进了有引导生成，将 SiT 的 FID 从 2.06 降低至 1.53，将 JiT 的 FID 从 1.86 降低至 1.80。我们进一步在文本生成图像任务上评估了我们的方法，在 GenEval 和 DPG 基准测试中均取得了更好的结果。

## Abstract
We propose continuous adversarial flow models, a type of continuous-time flow model trained with an adversarial objective. Unlike flow matching, which uses a fixed mean-squared-error criterion, our approach introduces a learned discriminator to guide training. This change in objective induces a different generalized distribution, which empirically produces samples that are better aligned with the target data distribution. Our method is primarily proposed for post-training existing flow-matching models, although it can also train models from scratch. On the ImageNet 256px generation task, our post-training substantially improves the guidance-free FID of latent-space SiT from 8.26 to 3.63 and of pixel-space JiT from 7.17 to 3.57. It also improves guided generation, reducing FID from 2.06 to 1.53 for SiT and from 1.86 to 1.80 for JiT. We further evaluate our approach on text-to-image generation, where it achieves improved results on both the GenEval and DPG benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Continuous Adversarial Flow Models》（连续对抗流模型）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的流匹配（Flow Matching, FM）模型在生成图像和视频时，若不使用分类器自由引导（CFG），往往会产生偏离训练数据分布（Out-of-distribution）的样本。
*   **研究动机**：
    *   **欧氏距离的局限性**：FM 训练通常采用均方误差（MSE）损失，这是一种各向同性的欧氏距离度量，无法感知复杂的数据流形（Manifold）。
    *   **泛化偏差**：在有限模型容量下，MSE 准则诱导的泛化分布与真实数据分布不一致。
    *   **对抗训练的优势**：判别器网络能学习感知细节（如纹理、边缘），比点对点的 MSE 更能捕捉流形结构。
    *   **技术空白**：此前的对抗流模型（AFM）仅限于离散时间，如何在连续时间流模型中引入对抗训练仍是开放问题。

### 2. 方法论
*   **核心思想**：将对抗性目标引入连续时间流模型（CNF），通过学习一个判别器来引导生成器预测的“速度场”（Velocity Field），使其在导数空间（Derivative Space）内与真实分布对齐。
*   **关键技术细节**：
    *   **JVP（雅可比向量积）判别器**：这是本文的核心创新。判别器 $D(x_t, t)$ 不直接对样本打分，而是通过计算其相对于位置 $x_t$ 和时间 $t$ 的方向导数（即 JVP）来区分真实速度场 $\bar{v}_t$ 和生成速度场 $G(x_t, t)$。
    *   **对抗目标函数**：采用类似 LSGAN 的最小二乘损失。判别器旨在使真实流的方向导数为 +1，生成流的方向导数为 -1；生成器则试图将自己的方向导数推向 +1。
    *   **正则化机制**：
        *   **中心化惩罚（Centering Penalty）**：防止判别器绝对值漂移。
        *   **连续时间最优传输（OT）正则化**：鼓励生成器选择最小范数解，提高训练稳定性。
    *   **后训练策略**：由于从头训练成本高，该方法主要设计为对已有的 FM 模型进行微调（Post-training），仅需少量 Epoch 即可显著提升质量。

### 3. 实验设计
*   **数据集与场景**：
    *   **ImageNet 256px**：类别条件图像生成，涵盖潜空间（Latent-space, SiT 模型）和像素空间（Pixel-space, JiT 模型）。
    *   **文本生成图像（T2I）**：使用 6B 参数的 Z-Image 模型，在 512px 分辨率下评估。
*   **Benchmark 与指标**：
    *   图像质量：FID（Fréchet Inception Distance）、IS（Inception Score）。
    *   文本对齐：GenEval、DPG-Bench。
*   **对比方法**：对比了原始的 SiT、JiT 模型，以及加入 FM 持续训练的基准，并与 DiT、ADM、SiD 等 SOTA 模型进行了横向对比。

### 4. 资源与算力
*   **算力说明**：文中**未明确给出**具体的 GPU 型号和总数量。
*   **训练效率**：
    *   **后训练耗时**：在 ImageNet 上仅需 10 个 Epoch 的微调。
    *   **计算开销**：由于引入了判别器网络和 JVP 计算，CAFM 的单 Epoch 墙钟时间约为标准 FM 的 **4.8 倍**。
    *   **实现细节**：利用 PyTorch 的 `torch.func.jvp` 和 `vmap` 实现高效的前向模式自动微分。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了潜空间和像素空间两种主流架构。
    *   进行了从头训练（Pre-training）与后训练（Post-training）的对比。
*   **消融实验**：非常充分。作者针对判别器更新步数 $N$、最优传输权重 $\lambda_{ot}$、学习率、训练时长以及不同的对抗损失函数（Hinge, Non-saturating, Least Squares）进行了详细的消融研究。
*   **客观性**：通过设置“FM 持续训练”作为对照组，证明了性能提升确实源于对抗目标而非仅仅是更多的训练步数，实验设计较为公平。

### 6. 主要结论与发现
*   **性能飞跃**：在无引导（Guidance-free）设置下，SiT 的 FID 从 8.26 降至 3.63，JiT 从 7.17 降至 3.57。
*   **引导增强**：即使在使用 CFG 的情况下，CAFM 依然能进一步优化 FID（如 SiT 从 2.06 降至 1.53）。
*   **泛化改善**：CAFM 诱导的分布比 MSE 诱导的分布更接近真实数据流形，生成的图像在纹理和结构上更自然。
*   **连续性优势**：通过 JVP 形式，成功解决了离散对抗流模型在步长趋于零时训练不稳定的问题。

### 7. 优点
*   **理论创新**：首次将对抗训练成功扩展到连续时间流模型，并巧妙利用 JVP 解决了速度场判别的问题。
*   **实用性强**：作为一种后训练手段，它能以极低的额外训练成本（10 Epoch）大幅提升现有预训练模型的性能。
*   **通用性**：在潜空间和像素空间、类别条件和文本条件任务中均表现出一致的有效性。

### 8. 不足与局限
*   **计算成本**：虽然训练步数少，但单步计算开销（4.8x）显著高于普通流匹配。
*   **低密度区风险**：尽管改善了泛化，但在数据分布极稀疏的区域（离群值），无引导生成仍可能产生错误结果（如物体数量错误）。
*   **架构依赖**：判别器需要使用 RMSNorm 才能保持稳定，对某些特定架构的适配可能需要额外调整。

（完）

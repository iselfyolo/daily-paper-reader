---
title: "CompliBench: Benchmarking LLM Judges for Compliance Violation Detection in Dialogue Systems"
title_zh: CompliBench：对话系统中合规违规检测的大语言模型裁判基准测试
authors: "Jingbo Yang, Guanyu Yao, Bairu Hou, Xinghan Yang, Nikolai Glushnev, Iwona Bialynicka-Birula, Duo Ding, Shiyu Chang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12312v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 针对现实智能体违规行为的系统化数据生成方法
tldr: 随着LLM在企业对话系统中的广泛应用，确保其遵循合规准则至关重要。本文提出了CompliBench，一个评估LLM裁判检测和定位多轮对话中违规行为能力的基准。通过自动化的数据生成流水线，利用可控缺陷注入和对抗搜索生成高质量标注数据。研究发现现有SOTA模型在该任务上表现欠佳，而基于合成数据微调的小模型展现出更强的性能和泛化能力。
selection_source: fresh_fetch
motivation: 由于缺乏系统性的数据生成方法和高昂的人工标注成本，评估LLM裁判在检测特定业务准则违规方面的可靠性仍面临巨大挑战。
method: 开发了一套自动化的数据生成流水线，通过可控缺陷注入和对抗搜索模拟真实且具有挑战性的多轮对话违规场景并自动生成精确标签。
result: 实验表明当前的顶级商业LLM在检测和定位违规行为方面表现不佳，而经过微调的小规模模型在性能和跨领域泛化上均优于领先模型。
conclusion: CompliBench为训练和评估鲁棒的生成式奖励模型提供了有效基础，证明了合成数据在提升合规性检测能力方面的巨大潜力。
---

## 摘要
随着大语言模型（LLM）越来越多地作为任务导向型智能体部署在企业环境中，确保其严格遵守复杂的、特定领域的业务准则变得至关重要。虽然利用“LLM作为裁判”（LLM-as-a-Judge）是实现可扩展评估的一种极具前景的方案，但这些裁判在检测特定政策违规方面的可靠性在很大程度上仍未得到探索。这一空白主要是由于缺乏系统的数据生成方法，而细粒度人工标注的高昂成本以及合成真实的智能体违规行为的难度阻碍了该方法的发展。在本文中，我们提出了 CompliBench，这是一个旨在评估 LLM 裁判在多轮对话中检测和定位准则违规能力的新型基准。为了克服数据稀缺问题，我们开发了一个可扩展的自动化数据生成流水线来模拟用户与智能体之间的交互。我们的可控缺陷注入过程能够自动生成违规准则和确切对话轮次的精确真实标签（ground-truth），而对抗性搜索方法则确保了这些引入的扰动具有极高的挑战性。我们的全面评估表明，目前最先进的闭源 LLM 在这项任务上表现欠佳。此外，我们证明了在合成数据上进行微调的小规模裁判模型优于领先的 LLM，并且在未见过的业务领域中具有良好的泛化能力，这突显了我们的流水线是训练鲁棒生成式奖励模型的有效基础。

## Abstract
As Large Language Models (LLMs) are increasingly deployed as task-oriented agents in enterprise environments, ensuring their strict adherence to complex, domain-specific operational guidelines is critical. While utilizing an LLM-as-a-Judge is a promising solution for scalable evaluation, the reliability of these judges in detecting specific policy violations remains largely unexplored. This gap is primarily due to the lack of a systematic data generation method, which has been hindered by the extensive cost of fine-grained human annotation and the difficulty of synthesizing realistic agent violations. In this paper, we introduce CompliBench, a novel benchmark designed to evaluate the ability of LLM judges to detect and localize guideline violations in multi-turn dialogues. To overcome data scarcity, we develop a scalable, automated data generation pipeline that simulates user-agent interactions. Our controllable flaw injection process automatically yields precise ground-truth labels for the violated guideline and the exact conversation turn, while an adversarial search method ensures these introduced perturbations are highly challenging. Our comprehensive evaluation reveals that current state-of-the-art proprietary LLMs struggle significantly with this task. In addition, we demonstrate that a small-scale judge model fine-tuned on our synthesized data outperforms leading LLMs and generalizes well to unseen business domains, highlighting our pipeline as an effective foundation for training robust generative reward models.

---

## 论文详细总结（自动生成）

以下是对论文《CompliBench: Benchmarking LLM Judges for Compliance Violation Detection in Dialogue Systems》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着 LLM 广泛应用于企业级任务导向型对话系统（如航空、医疗、保险的虚拟助手），智能体必须严格遵守特定领域的业务准则（如退款协议、专业语气）。
*   **核心问题**：目前业界倾向于使用“LLM 作为裁判”（LLM-as-a-Judge）来评估智能体的合规性，但这些裁判模型在检测细粒度违规行为方面的可靠性尚未得到系统验证。
*   **研究动机**：获取高质量的合规性标注数据极难，因为这需要领域专家对每一轮对话进行准则匹配和违规判定，且真实的违规案例稀缺。

### 2. 方法论：CompliBench 数据生成流水线
论文提出了一套自动化的数据生成框架，核心思想是“通过可控的缺陷注入来自动生成带标签的数据”：
*   **准则扩展（Guideline Scaling）**：从真实种子准则出发，利用 LLM 扩展出涵盖不同意图（Intent）和工作流变体（Workflow Variants）的准则池，并使用“裁判-修正”循环确保准则间无冲突、无重叠。
*   **违规变体生成与对抗优化**：
    *   为每条准则生成“违规版本”（如删除关键步骤、添加冲突指令）。
    *   **对抗搜索算法**：使用两个独立裁判（内容一致性裁判和对抗合规裁判）进行迭代优化，确保生成的违规行为既符合逻辑又具有隐蔽性，使通用 LLM 难以察觉。
*   **多智能体对话模拟**：
    *   **用户模拟器**：根据预设画像引导对话触发违规场景。
    *   **选择器智能体**：实时跟踪当前应遵循的准则。
    *   **助手智能体**：根据被注入了违规变体的准则文件生成回复。
*   **自动标注**：由于违规是主动注入的，系统能自动记录每一轮对话违反了哪条准则及其确切位置。

### 3. 实验设计
*   **数据集/场景**：涵盖三个垂直领域：**航空（Airline）**、**医疗（Healthcare）**、**保险（Insurance）**。共包含 318 个多轮对话，平均每场对话 16.6 轮，包含 3.7 个违规点。
*   **评估指标**：
    1.  **SGA（严格准则准确率）**：评估合规轮次的准则识别和标签判定。
    2.  **VDA（违规检测准确率）**：评估违规轮次的检出率。
    3.  **CLA（对话级准确率）**：整场对话所有轮次全部判定正确才算得分。
*   **对比方法**：
    *   **顶级闭源模型**：GPT-5、GPT-4o、Gemini-3-pro、Claude-sonnet-4.6 等。
    *   **开源模型**：Qwen3 系列（4B 到 Max）。
    *   **奖励模型（RM）**：包括 Skywork-Reward、InternLM2-RM、DeepSeek-GRM 等 9 种主流 RM。
    *   **微调模型**：基于本流水线合成数据微调的 Qwen3-8B。

### 4. 资源与算力
*   **微调配置**：论文微调了 Qwen3-8B 模型，使用了 1,400 条航空领域的合成数据。
*   **训练细节**：采用 GPT-5 蒸馏的推理轨迹作为 SFT 目标，训练 3 个 epoch，学习率 2e-5，warm-up 比例 0.05。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）及具体训练时长，但从 8B 模型和 1.4k 数据量推断，算力需求处于中等水平。

### 5. 实验数量与充分性
*   **实验规模**：对十余种主流 LLM 和 9 种奖励模型进行了跨三个领域的全面测试，每组实验运行 4 次取平均值以降低随机性。
*   **消融与分析**：
    *   进行了深入的**错误分析**，将 GPT-5 的错误归纳为 8 类（如范围归因错误、过度严格解释、忽略关键证据等）。
    *   验证了模型在未见领域（从航空迁移到医疗/保险）的泛化能力。
*   **客观性**：通过多模型投票和人工验证（验证准确率达 84%-97%）来确保合成数据的质量，实验设计较为客观公平。

### 6. 主要结论与发现
*   **现有模型不足**：即使是 GPT-5 或 Gemini-3 等顶级模型，在对话级准确率（CLA）上也仅能达到 30%-50% 左右，难以胜任严苛的商业合规审计。
*   **非对称失败**：LLM 裁判表现出一种矛盾——在面对合规回复时“过于严苛”（脑补不存在的规则），而在面对违规回复时“过于宽松”（接受功能相似但违规的行为）。
*   **小模型逆袭**：在合成数据上微调的 8B 小模型，其表现优于所有通用大模型，证明了**特定任务监督数据**比模型规模更重要。
*   **RM 表现极差**：现有的奖励模型（RM）在多轮对话合规检测上几乎全部失效，因为它们大多针对单轮偏好对齐训练。

### 7. 优点（亮点）
*   **创新的数据生成**：解决了合规性评估中“真实违规数据难获得”和“细粒度标签成本高”的痛点。
*   **对抗性优化**：通过对抗搜索提升了基准测试的难度，避免了合成数据过于简单的问题。
*   **细粒度评估**：不仅看最终结果，还评估每一轮的准则归因和违规定位，更符合实际业务需求。

### 8. 不足与局限
*   **领域覆盖**：虽然涵盖了三个领域，但对于法律、金融等更复杂的合规场景尚未涉及。
*   **模型偏差风险**：数据生成和初步标注仍依赖于顶级 LLM（如 GPT-5），可能存在“模型自我认同”偏差，即生成的违规模式可能带有特定模型的逻辑特征。
*   **实时性考量**：论文主要关注离线评估，未讨论该裁判模型在实时对话监控中的推理延迟问题。

（完）

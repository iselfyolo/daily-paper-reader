---
title: "Legal2LogicICL: Improving Generalization in Transforming Legal Cases to Logical Formulas via Diverse Few-Shot Learning"
title_zh: Legal2LogicICL：通过多样化少样本学习提升法律案例到逻辑公式转换的泛化能力
authors: "Jieying Xue, Phuong Minh Nguyen, Ha Thanh Nguyen, May Myo Zin, Ken Satoh"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11699v1"
tags: ["query:profile-3"]
score: 10.0
evidence: 利用大语言模型将法律案例转化为逻辑公式进行推理
tldr: 该研究针对法律逻辑推理中高质量标注数据稀缺的问题，提出了Legal2LogicICL框架。该框架利用大语言模型的上下文学习能力，通过检索增强生成技术，在语义和法律结构层面平衡示例的多样性与相似性，并有效减少了实体引起的检索偏差。此外，研究者还构建了Legal2Proleg数据集。实验证明，该方法在将法律案例转化为逻辑公式方面具有显著的准确性、稳定性和泛化能力，为实现可解释且可靠的法律推理提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的法律逻辑推理系统依赖微调模型进行语义解析，受限于高质量标注数据的匮乏，导致其泛化能力不足。
method: 提出Legal2LogicICL框架，通过在语义和结构层面平衡示例多样性与相似性，并消除实体干扰，实现高效的少样本检索增强生成。
result: 在开源和闭源大模型上的实验表明，该方法显著提升了法律案例转化为逻辑公式的准确性、稳定性和泛化性。
conclusion: 该研究通过创新的少样本学习策略和新数据集，有效解决了法律语义解析中的数据瓶颈，增强了法律推理系统的可靠性与可解释性。
---

## 摘要
本研究旨在通过将自然语言处理（NLP）的最新进展与利用大语言模型（LLM）的法律领域自适应少样本学习技术相结合，提升基于逻辑的法律推理系统的泛化能力。现有的基于逻辑的法律推理流水线通常依赖微调模型将自然语言法律案例映射为逻辑公式，然后再将其传递给符号推理器。然而，此类方法受到高质量标注训练数据稀缺的严重制约。为了解决这一局限性，我们提出了一种新型的基于 LLM 的法律推理框架，该框架通过检索增强生成实现有效的上下文学习（ICL）。具体而言，我们引入了 Legal2LogicICL，这是一个少样本检索框架，它在潜在语义表示层面和法律文本结构层面平衡了示例的多样性与相似性。此外，我们的方法通过减轻法律文本中由实体引起的检索偏差，明确地考虑了法律结构；在法律文本中，冗长且高度具体的实体提及往往主导了语义表示，从而掩盖了具有法律意义的推理模式。我们的 Legal2LogicICL 构建了具有信息量且鲁棒的少样本演示，从而在无需额外训练的情况下实现了准确且稳定的逻辑规则生成。此外，我们构建了一个名为 Legal2Proleg 的新数据集，该数据集标注了法律案例与 PROLEG 逻辑公式之间的对齐关系，以支持法律语义解析的评估。在开源和闭源 LLM 上的实验结果表明，我们的方法在将自然语言法律案例描述转换为逻辑表示方面显著提高了准确性、稳定性和泛化能力，突显了其在可解释且可靠的法律推理中的有效性。我们的代码可在 https://github.com/yingjie7/Legal2LogicICL 获取。

## Abstract
This work aims to improve the generalization of logic-based legal reasoning systems by integrating recent advances in NLP with legal-domain adaptive few-shot learning techniques using LLMs. Existing logic-based legal reasoning pipelines typically rely on fine-tuned models to map natural-language legal cases into logical formulas before forwarding them to a symbolic reasoner. However, such approaches are heavily constrained by the scarcity of high-quality annotated training data. To address this limitation, we propose a novel LLM-based legal reasoning framework that enables effective in-context learning through retrieval-augmented generation. Specifically, we introduce Legal2LogicICL, a few-shot retrieval framework that balances diversity and similarity of exemplars at both the latent semantic representation level and the legal text structure level. In addition, our method explicitly accounts for legal structure by mitigating entity-induced retrieval bias in legal texts, where lengthy and highly specific entity mentions often dominate semantic representations and obscure legally meaningful reasoning patterns. Our Legal2LogicICL constructs informative and robust few-shot demonstrations, leading to accurate and stable logical rule generation without requiring additional training. In addition, we construct a new dataset, named Legal2Proleg, which is annotated with alignments between legal cases and PROLEG logical formulas to support the evaluation of legal semantic parsing. Experimental results on both open-source and proprietary LLMs demonstrate that our approach significantly improves accuracy, stability, and generalization in transforming natural-language legal case descriptions into logical representations, highlighting its effectiveness for interpretable and reliable legal reasoning. Our code is available at https://github.com/yingjie7/Legal2LogicICL.

---

## 论文详细总结（自动生成）

### Legal2LogicICL：通过多样化少样本学习提升法律语义解析泛化能力

#### 1. 核心问题与整体含义（研究动机和背景）
法律推理系统（如 PROLEG）通常需要将自然语言（NL）描述的法律案例转化为结构化的逻辑公式。现有的方法主要依赖于在特定标注数据上微调（SFT）的模型，但面临以下挑战：
*   **数据稀缺性**：高质量的法律案例与逻辑公式对齐数据极难获得。
*   **泛化能力差**：微调模型容易过拟合于训练集中的特定模板或实体，面对未见过的法律结构或复杂的实体描述时表现脆弱。
*   **实体偏差**：法律文本中冗长的实体名（如合同编号、当事人姓名）往往主导了语义向量表示，导致检索到的参考示例仅是表面相似，而非法律逻辑结构相似。

本论文提出了 **Legal2LogicICL** 框架，旨在利用大语言模型（LLM）的上下文学习（ICL）能力，通过改进的检索策略实现高效、泛化的法律语义解析。

#### 2. 方法论：核心思想与关键技术
该框架的核心是**多样化混合少样本检索策略**，主要包含以下技术细节：
*   **DiverseSim 算法**：这是一种平衡“相似度”与“多样性”的检索算法。它通过超参数 $\lambda$ 调节：在选择少样本示例时，不仅考虑示例与查询案例的语义相似度，还通过惩罚已选示例间的相似度来增加多样性，从而覆盖更广的推理模式。
*   **实体无关的模板检索（Entity-agnostic Retrieval）**：为了消除实体偏差，该方法将法律案例抽象为“模板”（用占位符替换具体实体）。检索时同时在原始案例层面和抽象模板层面进行，确保选出的示例在法律逻辑结构上与查询案例高度一致。
*   **混合少样本构建**：最终的 Prompt 结合了语义相似的案例示例（Case-based）和结构相似的模板示例（Template-based），为 LLM 提供丰富的上下文参考。
*   **PROLEG 逻辑转换**：引导 LLM 将自然语言事实直接映射为符合 PROLEG 语法规范的逻辑事实公式。

#### 3. 实验设计
*   **数据集**：
    *   **Legal2Proleg（新构建）**：包含贷款、租赁、购买和版权等多种合同类型，具有高模板多样性和复杂的逻辑规则。
    *   **LegalCaseNER**：作为基准数据集，用于对比在简单模式下的表现。
*   **对比方法（Baselines）**：
    *   **SFT 方法**：基于 RoBERTa 的 LegalCaseNER 模型（传统的命名实体识别+规则组合方法）。
    *   **LLM 零样本/少样本**：包括 Qwen3 (8B/14B)、Llama-3.1 (8B)、Phi-4 (14B) 以及 GPT-5.2。
*   **评估指标**：采用**精确匹配（Exact Match）**作为核心指标，并引入**语义感知软匹配（Soft-Match）**来评估结构正确性。

#### 4. 资源与算力
*   论文中提到使用了 `Qwen/Qwen3-Embedding-8B` 模型进行向量编码和相似度计算。
*   **未明确说明**具体的 GPU 型号、数量及推理总时长。由于该方法基于 ICL（上下文学习），无需对 LLM 进行参数微调，因此主要的算力消耗集中在向量检索和 LLM 的推理生成上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同训练数据比例（20% 到 80% 的可见数据）进行了对比实验。
    *   对多样性参数 $\lambda$ 进行了灵敏度分析（0.2 到 0.8）。
    *   进行了消融实验，对比了纯语义检索（3c/5c）与混合检索（3c+3t）的效果。
    *   所有实验均在 **5 个随机种子**下运行并取平均值。
*   **充分性与公平性**：实验覆盖了开源与闭源模型，并严格控制了“未见模板”的测试环境，确保了评估的客观性。通过与 SFT 方法在低资源下的对比，充分证明了 ICL 方案的优越性。

#### 6. 主要结论与发现
*   **泛化能力显著**：在仅有 20% 数据可见的情况下，Legal2LogicICL 的准确率（约 83%）远超 SFT 方法（约 16%）。
*   **多样性至关重要**：DiverseSim 算法（$\lambda=0.6$）的效果优于纯余弦相似度检索，证明了多样化的示例能帮助模型更好地理解逻辑构造。
*   **混合策略有效**：结合模板检索能有效抵消实体偏差，显著提升逻辑公式生成的稳定性。
*   **LLM 规模效应**：较大参数量的模型（如 Phi-4, Qwen3-14B）在处理复杂法律逻辑时表现出更高的精确度。

#### 7. 优点
*   **无需微调**：通过 RAG 和 ICL 实现了即插即用的法律语义解析，降低了标注成本。
*   **结构感知**：创新的模板化检索解决了法律领域特有的“长实体干扰”问题。
*   **高质量资源贡献**：发布的 Legal2Proleg 数据集为法律 AI 社区提供了更具挑战性的基准。

#### 8. 不足与局限
*   **表面形式敏感**：错误分析显示，模型常因冠词（a/the）或标点符号的微小差异导致精确匹配失败，尽管逻辑语义是正确的。
*   **推理成本**：混合少样本策略增加了 Prompt 的长度，对于长文本法律案例，可能会面临 Token 消耗增加和上下文窗口限制的问题。
*   **实体对齐依赖**：模板化过程仍依赖于初步的实体识别或类型标注，若预处理阶段出错，可能影响后续检索质量。

（完）

---
title: "Temporal Flattening in LLM-Generated Text: Comparing Human and LLM Writing Trajectories"
title_zh: LLM 生成文本中的时间平坦化：人类与 LLM 写作轨迹的比较
authors: "Zhanwei Cao, YeoJin Go, Yifan Hu, Shanu Sushmita"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12097v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 构建人类与大模型写作的纵向数据集
tldr: "本研究探讨了LLM生成文本与人类写作在长跨度时间维度上的差异。通过构建2012-2024年间涵盖学术、博客和新闻的纵向数据集，研究发现LLM生成的文本存在“时间扁平化”现象：相比于人类随时间演变的写作风格，LLM在语义和认知情感上的漂移显著较小。这种时间变异性差异可作为识别AI生成内容的强力特征，分类准确率高达94%，揭示了当前LLM生成范式在模拟人类长期动态演变方面的局限性。"
selection_source: fresh_fetch
motivation: 旨在探究无状态的LLM是否能模拟人类写作中随时间推移而产生的风格演变和认知状态变化。
method: 构建包含412名作者、6086篇文档的十年跨度数据集，通过漂移和方差指标对比人类与LLM在语义、词汇及认知情感维度的写作轨迹。
result: "LLM生成的文本呈现“时间扁平化”，即语义和认知情感的演变幅度远低于人类，利用该特征区分人机文本的准确率达94%。"
conclusion: LLM无法重现人类写作的自然时间结构，这一缺陷对合成数据质量及需要真实时间演化特性的应用场景具有重要影响。
---

## 摘要
大语言模型（LLMs）在从内容生成到代码编写的日常应用中日益普及，其中每次交互都将模型视为无状态的，即独立生成响应而无记忆。然而，人类写作本质上是纵向的：作者的风格和认知状态会随年月演变。这提出了一个核心问题：LLMs 能否在较长的时间跨度内重现这种时间结构？我们构建并公开发布了一个纵向数据集，包含 412 位人类作者在 2012 年至 2024 年间跨越三个领域（学术摘要、博客、新闻）的 6,086 篇文档，并将其与三种代表性 LLMs 在标准和历史条件生成设置下生成的轨迹进行比较。通过对语义、词汇和认知情感表征使用基于漂移和方差的指标，我们发现 LLM 生成的文本中存在时间平坦化现象。与人类相比，LLMs 产生了更高的词汇多样性，但在语义和认知情感漂移方面显著减少。这些差异具有高度的预测性：仅凭时间变异性模式，在区分人类与 LLM 轨迹时即可达到 94% 的准确率和 98% 的 ROC-AUC。我们的结果表明，无论 LLMs 是独立生成还是能够访问增量历史，时间平坦化现象都持续存在，这揭示了当前部署范式的一个基本属性。这一差距对于需要真实时间结构的应用（如合成训练数据和纵向文本建模）具有直接影响。

## Abstract
Large language models (LLMs) are increasingly used in daily applications, from content generation to code writing, where each interaction treats the model as stateless, generating responses independently without memory. Yet human writing is inherently longitudinal: authors' styles and cognitive states evolve across months and years. This raises a central question: can LLMs reproduce such temporal structure across extended time periods? We construct and publicly release a longitudinal dataset of 412 human authors and 6,086 documents spanning 2012--2024 across three domains (academic abstracts, blogs, news) and compare them to trajectories generated by three representative LLMs under standard and history-conditioned generation settings. Using drift and variance-based metrics over semantic, lexical, and cognitive-emotional representations, we find temporal flattening in LLM-generated text. LLMs produce greater lexical diversity but exhibit substantially reduced semantic and cognitive-emotional drift relative to humans. These differences are highly predictive: temporal variability patterns alone achieve 94% accuracy and 98% ROC-AUC in distinguishing human from LLM trajectories. Our results demonstrate that temporal flattening persists regardless of whether LLMs generate independently or with access to incremental history, revealing a fundamental property of current deployment paradigms. This gap has direct implications for applications requiring authentic temporal structure, such as synthetic training data and longitudinal text modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Temporal Flattening in LLM-Generated Text: Comparing Human and LLM Writing Trajectories》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的部署通常将其视为“无状态”的独立生成器，而人类写作本质上是“纵向”的，其风格、情感和认知状态会随时间（数月或数年）发生演变。
*   **核心问题**：LLM 能否重现人类写作中随时间推移而产生的自然演化结构？如果不能，哪些维度的动态特征丢失了？
*   **整体含义**：研究揭示了 LLM 生成文本中普遍存在的“时间平坦化（Temporal Flattening）”现象，即 LLM 无法模拟人类在语义和心理层面的长期演进，这对合成数据质量和个性化 AI 系统的开发具有重要警示意义。

### 2. 方法论
*   **核心思想**：通过构建人类与 LLM 的“影子轨迹（Shadow Trajectories）”，在严格控制领域、主题和长度的前提下，对比两者在多年跨度内的演化差异。
*   **关键技术细节**：
    *   **特征表征空间**：
        1.  **词汇空间**：使用 TF-IDF 向量（降维至 10D）。
        2.  **语义空间**：使用 SBERT 嵌入（384D）。
        3.  **认知情感（Cog-Emo）空间**：提取 20 个可解释特征，包括大五人格代理指标、情感极性（VADER）、风格复杂度（Flesch 阅读易读性、句子长度等）。
    *   **演化算子**：
        1.  **漂移（Drift）**：衡量全局几何位移，计算年度特征向量之间的累积 L2 距离。
        2.  **方差（Variance）**：衡量局部波动的不规则性，使用变异系数（CV）来量化特征随年份波动的剧烈程度。
    *   **生成设置**：
        *   **独立生成（Instance-wise）**：每次生成不参考历史。
        *   **历史增强生成（History-augmented）**：在提示词中加入模型前几年输出的累积摘要，模拟“记忆”。

### 3. 实验设计
*   **数据集**：构建了一个包含 412 位人类作者、6,086 篇文档的纵向数据集（2012–2024），涵盖三个领域：
    *   **学术摘要**（STEM 领域）。
    *   **博客**（Reddit 长期活跃用户）。
    *   **新闻**（HuffPost 记者）。
*   **对比方法（Benchmark）**：
    *   **人类轨迹**作为基准。
    *   **LLM 轨迹**：使用 DeepSeek V1、GPT-4o mini、Claude 3.5 Haiku 生成。
    *   **提示词变体**：从仅包含关键词的极简提示到包含“人格+示例”的复杂提示。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **生成规模**：研究通过 OpenRouter API 调用了多种模型，总计生成了 **103,459 份** LLM 文档。由于研究侧重于推理生成而非模型训练，算力消耗主要集中在 API 调用和特征提取计算上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 12 年的时间跨度。
    *   对比了 3 种主流商用模型（主实验）及另外 3 种模型（附录稳健性测试）。
    *   进行了 3 种不同层级的提示词工程实验。
*   **充分性与公平性**：实验设计非常严谨。通过“配对二项式检验”确保了人类与 LLM 在文档数量、主题分布和长度上的严格对齐。此外，还进行了消融实验（如移除长度特征后分类器的表现）和多种变异性指标（RMSSD, MASD）的交叉验证，结果具有高度的一致性和客观性。

### 6. 主要结论与发现
*   **时间平坦化现象**：LLM 在词汇层面表现出较高的多样性（甚至超过人类），但在**语义漂移**和**认知情感波动**方面显著低于人类。
*   **高可区分性**：仅利用时间变异性模式（Cog-Emo CV），随机森林分类器区分人类与 LLM 轨迹的准确率达到 **94%**，ROC-AUC 达到 **98%**。
*   **历史增强的局限性**：即使为 LLM 提供过去的写作摘要，其生成的文本依然呈现平坦化特征，说明这种缺陷是当前生成范式的内在属性，而非简单的上下文缺失。
*   **关键特征**：平均句子长度、宜人性（Agreeableness）和神经质（Neuroticism）的波动是不规则性差异最明显的维度。

### 7. 优点
*   **视角新颖**：首次系统性地从“纵向时间维度”探讨人机文本差异，跳出了静态的质量评估框架。
*   **数据集价值**：公开发布了一个高质量、跨度十年的多领域纵向数据集，填补了该领域的空白。
*   **特征全面**：结合了分布式嵌入（SBERT）与可解释的心理学特征（大五人格），使结论既有统计深度又有心理学解释力。

### 8. 不足与局限
*   **语言限制**：研究仅限于英语文本，未探讨其他语言或文化背景下的时间动态。
*   **历史压缩**：历史增强实验使用的是“年度摘要”而非“全文本上下文”，可能限制了模型捕捉微观演化的能力。
*   **内生性问题**：人类的主题选择本身随兴趣演化，而实验中 LLM 的主题是被动给定的，这可能掩盖了部分人类特有的主动演化特征。
*   **应用范围**：主要关注多年度的长期演化，对于短时间尺度（如数天或数周）的动态捕捉尚不明确。

（完）

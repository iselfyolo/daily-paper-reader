---
title: "DPDSyn: Improving Differentially Private Dataset Synthesis for Model Training by Downstream Task Guidance"
title_zh: DPDSyn：通过下游任务引导改进用于模型训练的差分隐私数据集合成
authors: "Mingxuan Jia, Wen Huang, Weixin Zhao, Xingyi Wang, Jian Peng, Zhishuo Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15660v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 在实现差分隐私的同时合成用于AI模型训练的数据集
tldr: 针对差分隐私数据集合成中低维分布选择困难导致数据效用低的问题，本文提出DPDSyn。该方法通过在原始私有数据上训练一个满足差分隐私的下游任务模型，并利用该模型指导数据集合成。这种方式既保证了隐私性，又保留了下游任务的关键信息。实验表明，DPDSyn在准确率和合成效率上显著优于现有SOTA方法，且具有良好的可扩展性。
selection_source: fresh_fetch
motivation: 现有差分隐私合成方法依赖于难以准确选择的低维分布，导致合成数据的下游任务效用受损。
method: 提出利用在原始数据上训练的差分隐私下游任务模型来直接指导数据集合成，从而捕捉关键特征。
result: 在四个基准数据集上，DPDSyn的准确率最高提升2.40倍，合成效率最高提升333.73倍。
conclusion: 结合下游任务引导的差分隐私模型训练是提高合成数据集效用和生成效率的有效途径。
---

## 摘要
如何在实现 AI 模型训练的差分隐私的同时合成数据集，是一个具有重要意义但充满挑战的问题。为了解决这一问题，现有最先进的方法首先从原始私有数据集中选择多个具有高精度逼近原始分布潜力的低维分布，然后合成一个遵循所有选定低维分布的数据集作为合成数据集。然而，选择合适的低维分布非常困难，这反过来又降低了所得合成数据集的数据效用。为了改进差分隐私数据集合成，我们提出在原始私有数据集上针对下游任务训练一个差分隐私 AI 模型，并利用训练好的模型来合成数据集。具体而言，一方面，该 AI 模型满足差分隐私，因此无论如何使用该模型都不会泄露原始私有数据集的隐私信息；另一方面，该 AI 模型是为完成下游任务而训练的，因此它保留了完成下游任务的关键信息。我们利用该 AI 模型合成数据集，以实现在保护隐私的同时提高数据效用的目标。在四个基准数据集上的实证评估表明，我们提出的 DPDSyn 一贯优于八种最先进的基准方法，在准确率上最高提升了 2.40 倍，在合成效率上最高提升了 333.73 倍。进一步的实验还验证了 DPDSyn 在不同数据规模下具有强大的可扩展性。

## Abstract
How to synthesize a dataset while achieving differential privacy for AI model training is a meaningful but challenging problem. To address this problem, state-of-the-art methods first select original private dataset's multiple low-dimensional distributions that have the potential to approximate the distribution of original private dataset with high precision, and then synthesize a dataset obeying all selected low-dimensional distributions as the synthetic dataset. However, it is difficult to select suitable low-dimensional distributions, which in turn degrades the data utility of resulting synthetic dataset. To improve differentially private dataset synthesis, we propose to train a differentially private AI model for downstream tasks on the original private dataset and utilize the trained model to synthesize datasets. In particular, on the one hand, the AI model satisfies differential privacy so no matter how to use the model does not disclose private information of original private dataset. On the other hand, the AI model is trained to complete the downstream task so the AI model preserves critical information for completing downstream tasks. We utilize the AI model to synthesize datasets to achieve the goal of improving data utility while preserving privacy. Empirical evaluations on four benchmark datasets demonstrate that our proposed DPDSyn consistently outperforms eight state-of-the-art baselines with a maximum improvement of 2.40x in accuracy and 333.73x in synthesis efficiency. Further experiments also validate that DPDSyn has strong scalability across varying data scales.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DPDSyn** 的新型差分隐私（Differential Privacy, DP）数据集合成方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保护隐私的前提下，合成高质量的数据集用于 AI 模型训练。
*   **研究背景**：现有的 DP 数据合成方法主要分为两类：
    1.  **选择-测量-生成类（Select-Measure-Generate）**：试图通过选择低维分布来逼近高维分布，但难以准确选择合适的分布，且在大规模数据集上扩展性差。
    2.  **基于 GAN 的方法**：在差分隐私噪声干扰下训练不稳定，导致下游任务的数据效用（Utility）较低。
*   **核心动机**：现有方法大多试图还原整个数据集的统计分布，而忽略了下游任务的特定需求。论文提出通过“下游任务引导”来保留对模型训练最关键的信息。

### 2. 方法论：核心思想与技术细节
DPDSyn 的核心思想是**训练一个任务感知的 DP 模型作为合成数据的“标签生成器”**。具体流程分为四个阶段：
1.  **阶段 1：训练 DP AI 模型**：在原始私有数据集上，使用 **DP-SGD** 算法针对特定的下游任务训练一个 AI 模型。该模型通过梯度裁剪和加噪满足 $(\epsilon, \delta)$-DP，从而在不泄露隐私的情况下捕捉特征到标签的映射关系。
2.  **阶段 2：合成特征（Attributions）**：为了保持特征的边际分布并打破记录间的关联，对原始数据集的每一列特征进行**随机打乱（Column-wise Shuffling）**。
3.  **阶段 3：合成标签**：将打乱后的特征输入到阶段 1 训练好的 DP AI 模型中，预测并生成对应的标签。
4.  **阶段 4：构建数据集**：将合成的特征与预测的标签融合，形成最终的合成数据集。

### 3. 实验设计
*   **数据集**：使用了 4 个基准数据集：Adult（收入预测）、Br2000（高收入预测）、LPD（肝病预测）、Smoking（吸烟状态预测）。
*   **下游模型（Benchmark）**：为了验证通用性，使用了三种模型：MLP（多层感知机）、SVM（支持向量机）和 FT-Transformer（深度学习模型）。
*   **对比方法（Baselines）**：对比了 8 种最先进的方法，包括 ABSyn, PrivSyn, PrivPetal, PrivMRF, AIM, MST, MWEM+PGM 和 DP-GAN。
*   **评估指标**：准确率（Accuracy）、F1 分数、运行时间（Efficiency）以及数据规模扩展性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 `tensorflow-privacy` 库来实现 DP-SGD 训练，并设置了 6 小时的实验超时限制（部分基准方法在 Smoking 和 LPD 数据集上因超时被排除）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上针对 3 种下游模型进行了对比，每组实验重复运行 5 次取平均值。
*   **充分性**：
    *   **效用实验**：覆盖了准确率和 F1 分数，验证了在不同隐私预算下的表现。
    *   **效率实验**：对比了合成数据集的时间消耗。
    *   **扩展性实验**：将数据规模扩大至 2 倍和 3 倍，验证了方法在大规模数据下的稳定性。
*   **客观性**：对比了多种类型的基准方法（统计类和生成对抗类），实验设计较为公平，且指出了部分基准方法在复杂任务上的局限性（如超时或崩溃）。

### 6. 主要结论与发现
*   **效用显著提升**：DPDSyn 在准确率上最高提升了 **2.40 倍**，F1 分数最高提升了 **15.28 倍**。
*   **合成效率极高**：由于采用了推理生成而非复杂的迭代优化，合成效率最高提升了 **333.73 倍**。
*   **扩展性强**：当数据规模增加时，传统方法（如 PrivMRF, PrivPetal）往往会崩溃或超时，而 DPDSyn 依然能保持稳定的性能和线性增长的时间开销。

### 7. 优点（亮点）
*   **任务导向**：通过引入下游任务引导，解决了传统方法“眉毛胡子一把抓”导致关键特征丢失的问题。
*   **简单高效**：利用特征打乱和模型推理代替了复杂的概率图模型（PGM）或不稳定的 GAN 训练。
*   **隐私保证**：基于 DP-SGD 和序列组合定理（Sequential Composition），提供了严格的差分隐私数学证明。

### 8. 不足与局限
*   **任务依赖性**：合成的数据集高度依赖于预设的下游任务。如果合成数据集后需要用于完全不同的任务，其效用可能会大幅下降。
*   **特征关联丢失**：由于采用了列打乱（Shuffling），特征之间的相关性（Correlation）被破坏，这可能影响某些对特征间关系敏感的模型。
*   **模型选择风险**：阶段 1 需要预先确定 AI 模型架构，若架构选择不当，会直接限制合成数据的质量上限。

（完）

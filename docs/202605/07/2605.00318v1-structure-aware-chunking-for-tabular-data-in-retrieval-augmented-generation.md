---
title: Structure-Aware Chunking for Tabular Data in Retrieval-Augmented Generation
title_zh: 检索增强生成中针对表格数据的结构感知分块
authors: "Pooja Guttal, Varun Magotra, Vasudeva Mahavishnu, Natasha Chanto, Sidharth Sivaprasad, Manas Gaur"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00318v1"
tags: ["query:kgee"]
score: 8.0
evidence: 用于语义关系的层次化行树表示
tldr: "针对RAG中表格数据处理的局限性，本文提出结构感知表格分块（STC）框架。该框架通过构建分层行树将行编码为键值块，并采用对齐结构边界的拆分与贪婪合并策略。实验表明，STC在减少分块数量的同时显著提升了检索性能（如MRR和Recall@1），为表格数据的RAG应用提供了更高效的结构化处理方案。"
selection_source: fresh_fetch
motivation: 现有的RAG分块策略主要针对非结构化文本，忽视了表格数据的结构特性，导致检索效率和语义完整性受损。
method: 提出STC框架，通过行树表示将行转化为键值块，并结合结构对齐的拆分与无重叠贪婪合并生成紧凑分块。
result: "在MAUD数据集上，STC将分块数量减少了多达56%，并将检索指标MRR从0.3576提升至0.5945。"
conclusion: 保持表格结构的分块方法能显著优化Token利用率并提升检索性能，是处理表格RAG任务的关键。
---

## 摘要
CSV 和 Excel 文件等表格文档在企业数据流水线中被广泛使用，然而现有的检索增强生成 (RAG) 分块策略主要针对非结构化文本设计，未考虑表格结构。我们提出了一种结构感知表格分块 (STC) 框架，该框架通过构建分层行树 (Row Tree) 表示，以行级单元为操作对象，其中每一行被编码为一个键值块。STC 执行与结构边界对齐的令牌受限拆分，并应用无重叠的贪婪合并，以生成密集的、非重叠的分块。这种设计在提高令牌利用率并减少碎片化的同时，保留了行内字段间的语义关系。在 MAUD 数据集的评估中，与标准的递归分块和基于键值的分块基准相比，STC 分别减少了高达 40% 和 56% 的分块数量，同时提高了令牌利用率和处理效率。在检索基准测试中，STC 在混合设置下将 MRR 从 0.3576 提升至 0.5945，并在仅使用 BM25 的检索中将 Recall@1 从 0.366 提升至 0.754。这些结果表明，在分块过程中保留结构可以提高检索性能，凸显了结构感知分块对于表格数据 RAG 的重要性。

## Abstract
Tabular documents such as CSV and Excel files are widely used in enterprise data pipelines, yet existing chunking strategies for retrieval-augmented generation (RAG) are primarily designed for unstructured text and do not account for tabular structure. We propose a structure-aware tabular chunking (STC) framework that operates on row-level units by constructing a hierarchical Row Tree representation, where each row is encoded as a key-value block. STC performs token-constrained splitting aligned with structural boundaries and applies overlap-free greedy merging to produce dense, non-overlapping chunks. This design preserves semantic relationships between fields within a row while improving token utilization and reducing fragmentation. Across evaluations on the MAUD dataset, STC reduces chunk count by up to 40% and 56% compared to standard recursive and key-value based baselines, respectively, while improving token utilization and processing efficiency. In retrieval benchmarks, STC improves MRR from 0.3576 to 0.5945 in a hybrid setting and increases Recall@1 from 0.366 to 0.754 in BM25-only retrieval. These results demonstrate that preserving structure during chunking improves retrieval performance, highlighting the importance of structure-aware chunking for RAG over tabular data.

---

## 论文详细总结（自动生成）

### 论文总结：检索增强生成中针对表格数据的结构感知分块 (STC)

#### 1. 核心问题与研究动机
在检索增强生成（RAG）系统中，文档分块（Chunking）是决定检索质量的关键环节。现有的主流分块策略（如固定大小分块、滑动窗口或递归字符拆分）主要针对**非结构化文本**设计，假设文本是线性且语义连续的。
然而，表格数据（如 CSV、Excel）具有高度的结构化特征（行、列、分层关系）。直接将表格线性化为文本并进行传统分块会导致：
*   **语义断裂**：一行数据可能被切分到不同的块中。
*   **上下文丢失**：缺乏列名与单元格值的关联。
*   **效率低下**：为了保持连贯性通常需要设置重叠（Overlap），导致 Token 冗余。
本研究旨在提出一种能够感知表格结构、保留行级完整性并优化 Token 利用率的分块框架。

#### 2. 核心方法论：STC 框架
论文提出了**结构感知表格分块（Structure-Aware Tabular Chunking, STC）**，其核心流程包含三个阶段：

*   **行树表示（Row Tree Representation）**：
    将表格转换为层次化树状结构。根节点下是表或工作表（Sheet），叶子节点是行。每一行被编码为**键值对（Key-Value, KV）块**，格式为 `列名: 单元格值`。这确保了每个字段与其属性标签在语义上紧密耦合。
*   **递归与紧急拆分（Recursive & Emergency Splitting）**：
    *   **递归拆分**：自顶向下遍历行树。如果一个节点（如整个表）超过 Token 预算，则拆分为子节点（行）。
    *   **紧急拆分**：如果单行数据本身超过了 Token 限制，则在 KV 边界处进行拆分，确保拆分后的片段仍包含完整的字段信息，而非在单词中间断开。
*   **无重叠贪婪合并（Greedy Merging）**：
    在满足 Token 预算的前提下，将相邻的行级单元顺序合并。该策略不设置重叠区域，旨在生成尽可能密集且非重叠的分块，从而最大化每个分块的信息密度。

#### 3. 实验设计
*   **数据集**：使用 **MAUD (Merger Agreement Understanding Dataset)**。这是一个法律合同数据集，包含大量复杂的半结构化记录（如交易条款、法律问题及答案），非常适合测试处理复杂表格/结构化数据的能力。
*   **基准方法（Benchmarks）**：
    1.  **Recursive**：标准的递归字符拆分（如 LangChain 的工具），带有 100 Token 的滑动窗口重叠。
    2.  **KV + Recursive**：消融实验，先将数据转为 KV 格式，再使用标准递归拆分。
*   **评估指标**：
    *   **分块质量**：分块总数、平均/最小/最大 Token 数、Token 利用率、处理速度。
    *   **检索性能**：Recall@1/3/5 和 MRR（平均倒数排名）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练/推理时长。由于该方法主要涉及预处理阶段的逻辑算法和现成的嵌入模型（如 `multi-qa-MiniLM-L6-cos-v1`），其算力需求主要集中在向量化索引阶段，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 MAUD 的训练集（25,827 行）、验证集（6,753 行）和测试集（6,651 行）上均进行了完整测试。
*   **检索设置**：对比了“混合检索（双编码器 + BM25 + 交叉编码器重排序）”和“仅 BM25 检索”两种场景。
*   **充分性评价**：实验设计较为充分，通过消融实验（KV + Recursive）证明了性能提升不仅源于 KV 格式化，更源于其结构感知的拆分与合并策略。实验对比了检索精度与处理效率，评估维度全面。

#### 6. 主要结论与发现
*   **效率提升**：STC 显著减少了分块数量。相比标准递归分块减少了约 **40%**，相比 KV+递归基准减少了 **56%**。
*   **Token 利用率**：STC 生成的分块更紧凑，平均 Token 利用率更高（约 400/512），且处理速度更快。
*   **检索精度大幅飞跃**：
    *   在混合检索中，MRR 从 0.3576 提升至 **0.5945**。
    *   在 BM25 检索中，Recall@1 从 0.366 提升至 **0.754**，证明了结构化分块对词法匹配的巨大帮助。

#### 7. 优点与亮点
*   **结构保真度**：通过行树和 KV 边界拆分，最大限度地保留了表格数据的原始逻辑关系。
*   **消除冗余**：采用无重叠合并策略，在不损失上下文的前提下，消除了传统方法中因滑动窗口导致的 Token 浪费。
*   **通用性**：虽然在法律数据集上测试，但其逻辑适用于 CSV、Excel、日志文件等任何行级结构化数据。
*   **线性时间复杂度**：算法处理速度随行数线性增长，适合大规模工业流水线。

#### 8. 不足与局限
*   **数据集单一**：主要集中在法律领域的 MAUD 数据集，尚未在更多样化的通用表格数据集（如 WikiTableQuestions）上验证。
*   **固定预算限制**：实验主要基于 512 Token 的固定预算，未深入探讨不同分块大小对性能的敏感度。
*   **检索判定简化**：检索相关性判定采用了启发式的字符串匹配（AND 条件），可能无法完全模拟真实场景中复杂的语义相关性。
*   **多表关联处理**：目前主要针对单表或多工作表，对于跨表关联（Join）的复杂结构处理提及较少。

（完）

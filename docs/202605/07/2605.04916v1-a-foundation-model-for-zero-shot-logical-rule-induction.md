---
title: A Foundation Model for Zero-Shot Logical Rule Induction
title_zh: 一种用于零样本逻辑规则归纳的基础模型
authors: Yin Jun Phua
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04916v1"
tags: ["query:profile-3"]
score: 8.0
evidence: 使用基础模型进行零样本逻辑规则归纳
tldr: 本研究提出了神经规则归纳器（NRI），这是首个用于零样本逻辑规则归纳的预训练基础模型。针对传统归纳逻辑程序设计（ILP）方法需针对新任务重新训练的局限，NRI通过编码领域无关的统计特征（如类条件率、熵等）实现跨任务泛化。该模型采用统计编码器与并行槽位解码器架构，支持端到端微分训练，在规则恢复、鲁棒性及真实世界基准测试中展现了卓越的零样本迁移能力。
selection_source: fresh_fetch
motivation: 传统的归纳逻辑程序设计方法受限于特定谓词，无法在不重新训练的情况下直接处理新任务。
method: 提出NRI模型，利用领域无关的统计属性表示文字，并结合并行槽位解码器和乘积T-范数松弛实现可微的规则学习。
result: NRI在规则恢复、抗噪声干扰以及真实世界数据集的零样本迁移任务中均表现出色，无需针对新任务重新训练。
conclusion: 该工作证明了构建符号推理基础模型的可行性，为实现通用的零样本逻辑规则归纳开辟了新方向。
---

## 摘要
归纳逻辑程序设计（ILP）从数据中学习可解释的逻辑规则。现有方法通常是转导式的：其学习到的参数与特定谓词绑定，且每个新任务都需要重新训练。我们提出了神经规则归纳器（Neural Rule Inducer, NRI），这是一种用于零样本规则归纳的预训练模型。NRI 并非编码文字标识（literal identities），而是利用与领域无关的统计属性（如类条件率、熵和共现性）来表示文字，这些属性可以在无需重新训练的情况下，跨变量标识和数量进行泛化。该模型由一个统计编码器和一个基于槽位的并行解码器组成。并行解码保留了逻辑析取的置换不变性；相比之下，自回归解码器会强加任意的子句顺序。乘积 T-范数（Product T-norm）松弛使得规则执行过程可微，从而允许仅根据预测准确率进行端到端训练。我们在规则恢复、对标签噪声和伪相关的鲁棒性，以及在真实世界基准测试上的零样本迁移方面对 NRI 进行了评估。我们认为这项工作开启了符号推理基础模型的可能性。代码和参考检查点可在 https://github.com/phuayj/neural-rule-inducer 获取。

## Abstract
Inductive Logic Programming (ILP) learns interpretable logical rules from data. Existing methods are transductive: their learned parameters are bound to specific predicates and require retraining for each new task. We introduce Neural Rule Inducer (NRI), a pretrained model for zero-shot rule induction. Rather than encoding literal identities, NRI represents literals using domain-agnostic statistical properties such as class-conditional rates, entropy, and co-occurrence, which generalize across variable identities and counts without retraining. The model consists of a statistical encoder and a parallel slot-based decoder. Parallel decoding preserves the permutation invariance of logical disjunction; an autoregressive decoder would instead impose an arbitrary clause order. Product T-norm relaxation makes rule execution differentiable, allowing end-to-end training on prediction accuracy alone. We evaluate NRI on rule recovery, robustness to label noise and spurious correlations, and zero-shot transfer to real-world benchmarks, and we believe this work opens up the possibility of foundation models for symbolic reasoning. Code and the reference checkpoint are available at https://github.com/phuayj/neural-rule-inducer.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**神经规则归纳器（Neural Rule Inducer, NRI）**的基础模型，旨在实现逻辑规则的零样本（Zero-Shot）归纳。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究动机**：传统的归纳逻辑程序设计（ILP）系统通常是“转导式”的，即模型参数与特定的谓词（如“父亲”、“蛋白质”）绑定。这意味着每当面对一个新领域或新数据集时，模型都必须从头开始重新训练。
*   **核心问题**：如何构建一个能够像大语言模型（LLM）那样，在未见过的任务上直接进行逻辑推理和规则提取的“基础模型”？
*   **整体含义**：NRI 试图通过学习“如何进行归纳”这一抽象过程，而非学习特定领域的知识，来实现跨领域的零样本规则生成。

### 2. 方法论
NRI 的核心思想是**放弃编码变量的字面身份，转而编码变量的统计特征**。
*   **文字统计编码器（Literal Statistics Encoder）**：将每个文字（变量及其否定）表示为一个 18 维的统计向量，包含类条件概率 $P(l|y)$、二元熵 $H(l)$、文字极性、共现强度等。这种表示方式与变量的具体含义无关，具有置换不变性。
*   **并行槽位解码器（Parallel Slot-Based Decoder）**：
    *   采用 Transformer 解码器架构，通过多个并行的“槽位（Slots）”同时生成多个子句。
    *   **并行性**：保留了逻辑析取（OR）的置换不变性（即 $A \lor B \equiv B \lor A$），避免了自回归模型产生的随机顺序偏差。
*   **可微逻辑执行（T-Norm）**：使用**乘积 T-范数（Product T-norm）**将逻辑运算松弛到 $[0, 1]$ 区间。
    *   合取（AND）：$x \cdot y$
    *   析取（OR）：$1 - (1-x)(1-y)$
    *   这使得整个规则生成和评估过程端到端可微，可以仅根据预测准确率进行训练。
*   **训练策略**：完全在**合成的随机布尔公式**上进行预训练。通过引入“伪相关环境（Spurious Environments）”，强制模型识别真正的因果特征而非偶然的相关性。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：用于评估规则恢复率、复杂度缩放、噪声鲁棒性和伪相关鲁棒性。
    *   **真实世界数据**：14 个 UCI 经典分类任务（如 Adult, Breast Cancer, Diabetes 等），涵盖了医疗、金融、生物等多个领域。
*   **Benchmark 与对比方法**：
    *   **非解释性基准**：XGBoost, LightGBM。
    *   **可解释性/规则基准**：EBM (Explainable Boosting Machine), RIPPER, RuleFit, FIGS, 决策树 (DT), 神经 DNF (N-DNF)。
*   **评估方式**：NRI 采用**零样本**模式（直接在测试集上推理），而所有对比基准均在每个数据集上进行了专门的训练。

### 4. 资源与算力
*   **算力设备**：论文提到研究使用了东京科学大学（Institute of Science Tokyo）的 **TSUBAME 4.0 超级计算机**。
*   **训练细节**：使用了 AdamW 优化器，学习率为 $6 \times 10^{-4}$，Batch Size 为 8192，训练步数仅为 500 步。
*   **具体型号**：文中未明确说明具体使用的 GPU 型号或数量（注：TSUBAME 4.0 通常配备 NVIDIA H100 GPU，但作者未在文中详述分配情况）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 14 个真实数据集上进行了 5 折交叉验证。
    *   进行了针对规则复杂度（子句数量 $K$ 和文字数量 $L$）的缩放实验。
    *   进行了消融实验（针对 6 种不同的损失函数组件）。
*   **充分性与客观性**：实验设计较为全面，不仅对比了准确率，还通过热图分析了模型在极端噪声（高达 30%）和大量干扰变量（多达 32 个伪特征）下的表现。对比基准涵盖了从传统符号方法到现代深度学习方法的多个维度，评估较为客观。

### 6. 主要结论与发现
*   **零样本能力**：仅在合成数据上训练的 NRI 能够直接迁移到真实 UCI 任务，平均准确率达到 69.7%。虽然低于专门训练的 EBM（82.7%），但证明了零样本逻辑归纳的可行性。
*   **极强的鲁棒性**：在标签噪声超过 15% 时，NRI 的表现开始超过 RIPPER 和决策树等传统方法。在存在强伪相关特征时，NRI 依然能保持 92% 以上的准确率。
*   **可解释性**：NRI 生成的是标准的析取范式（DNF）规则，人类可直接阅读并理解其逻辑。

### 7. 优点（亮点）
*   **范式创新**：首次提出了逻辑归纳的“基础模型”概念，打破了 ILP 任务必须重新训练的限制。
*   **架构设计**：并行槽位解码器巧妙地解决了逻辑运算的对称性问题。
*   **训练方案**：通过复杂的损失函数（如反事实必要性损失 $L_{cf}$）解决了神经符号系统中的“捷径学习”问题。

### 8. 不足与局限
*   **性能差距**：作为零样本模型，其绝对准确率在多数任务上仍显著低于针对特定任务微调的 SOTA 模型。
*   **输入限制**：目前主要针对布尔变量。虽然文中提到可以通过离散化处理连续变量，但这种预处理可能会损失信息。
*   **复杂度瓶颈**：实验显示，当规则包含超过 4 个子句或每个子句超过 3 个文字时，逻辑恢复的准确率下降较快。
*   **Schema 扩展性**：虽然模型在变量数量上具有泛化性，但对于极大规模特征空间的适应能力仍有待验证。

（完）

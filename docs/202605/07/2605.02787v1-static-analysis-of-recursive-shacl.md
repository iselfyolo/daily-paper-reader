---
title: Static Analysis of Recursive SHACL
title_zh: 递归 SHACL 的静态分析
authors: "Anouk Oudshoorn, Magdalena Ortiz, Mantas Simkus"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02787v1"
tags: ["query:q9"]
score: 8.0
evidence: 使用SHACL和描述逻辑进行知识表示的形式化方法
tldr: 本研究探讨了递归SHACL文档的静态分析问题，重点研究文档间的蕴含（包含）判定。针对RDF数据的约束验证，作者分析了支持模型、稳定模型和良基语义下的判定性。研究发现，前两者在蕴含判定上不可判定，而良基语义下则是可判定的。通过将良基语义下的SHACL转换为混合μ-演算，并利用基于自动机的决策程序，实现了单指数级复杂度的判定，填补了递归SHACL静态分析的空白。
selection_source: fresh_fetch
motivation: 旨在解决SHACL文档之间缺乏静态比较（蕴含判定）服务的问题，特别是针对包含递归定义和目标的情况。
method: 将良基语义下的SHACL约束转换为混合μ-演算，并结合基于自动机的决策程序进行判定。
result: 证明了蕴含判定在支持模型和稳定模型下不可判定，但在良基语义下可在单指数时间内判定。
conclusion: 揭示了良基模型与不动点模态逻辑之间的新联系，为递归SHACL的静态分析提供了理论基础和最优决策算法。
---

## 摘要
SHACL（形状约束语言）通过所谓的“形状”来表达对 RDF 数据的约束。其核心服务是验证：即验证数据图是否符合 SHACL 文档。但到目前为止，还没有用于比较文档的静态分析服务。在本文中，我们研究了以下问题：判定所有验证一个 SHACL 文档的图是否也验证另一个文档。与以往仅考虑形状表达式蕴涵的研究不同，我们考虑了包含（递归）形状定义和目标的文档。我们证明了在支持模型和稳定模型语义下，蕴涵（又称包含性）是不可判定的，即使对于使用描述逻辑 ALCIO 进行形状表达式定义的片段也是如此。与此形成鲜明对比的是，在良基语义下，该问题在单指数时间内是可判定的。我们的核心技术贡献是将良基语义下的 SHACL 转换为全混合 mu-演算，揭示了良基模型与不动点模态逻辑之间的一种新颖联系，并提出了一种基于自动机的最坏情况最优判定过程。

## Abstract
SHACL (Shapes Constraint Language) expresses constraints on RDF data by means of so-called shapes. Its central service is validation: verifying whether a data graph complies with a SHACL document. But so far, there are no static analysis services to compare documents. In this paper, we study the following problem: decide whether all graphs that validate one SHACL document also validate another. Unlike previous works that have considered the implication of shape expressions only, we consider documents comprising (recursive) shape definitions and targets. We show that implication (a.k.a. containment) is undecidable under the supported and the stable model semantics, even for the fragment that uses the description logic ALCIO for shape expressions. Under the well-founded semantics, in surprising contrast, it is decidable in single exponential time. Our key technical contribution is a translation of SHACL under the well-founded semantics into the full hybrid mu-calculus, revealing a novel link between well-founded models and a fixed point modal logic, and a worst-case optimal automata-based decision procedure.

---

## 论文详细总结（自动生成）

这篇论文《Static Analysis of Recursive SHACL》（递归 SHACL 的静态分析）深入探讨了 RDF 形状约束语言（SHACL）在包含递归定义时的文档比较问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：研究递归 SHACL 文档的**静态分析**，特别是**蕴含（Implication/Containment）问题**。即：给定两个 SHACL 文档 $S_1$ 和 $S_2$，判定是否所有验证 $S_1$ 的数据图也必然验证 $S_2$。
*   **研究动机**：SHACL 是 W3C 推荐的 RDF 数据约束标准。虽然其“验证”服务（检查特定图是否符合约束）已有深入研究，但“静态分析”（不依赖具体数据的文档比较）仍处于空白。这对于文档优化、自动转换和一致性检测至关重要。
*   **背景挑战**：递归（循环定义）在 SHACL 中很常见，但其语义复杂。目前存在多种语义（支持模型、稳定模型、良基语义），论文旨在厘清这些语义在静态分析下的计算性质。

### 2. 论文提出的方法论
*   **核心思想**：通过将 SHACL 语义映射到逻辑形式化系统，利用描述逻辑（DL）和不动点逻辑的理论成果来解决判定性问题。
*   **关键技术细节**：
    *   **不可判定性证明**：通过将“平铺问题”（Tiling Problem）归约到 SHACL 蕴含问题，证明了在**支持模型语义**和**稳定模型语义**下，即使是受限的 $ALCIO$ 片段也是不可判定的。
    *   **逻辑翻译**：针对**良基语义（Well-founded Semantics）**，论文提出了一套翻译函数 $tr^+$ 和 $tr^-$，将 SHACL 文档转换为**全混合 $\mu$-演算（Full Hybrid $\mu$-calculus）**公式。
    *   **算法流程**：
        1.  将 SHACL 约束转化为具有最小和最大不动点算子的 $\mu$-演算公式。
        2.  利用**双向交替奇偶树自动机（2ATA）**来判定公式的可满足性。
        3.  通过构造特定的“猜测（Guesses）”列表来处理名义（Nominals/Individuals），从而避免翻译过程中的指数级状态爆炸。

### 3. 实验设计
*   **研究性质**：本论文属于**理论计算机科学与形式化方法**领域，不包含传统意义上的软件测试或数据集实验。
*   **论证场景**：作者通过数学证明覆盖了不同的 SHACL 片段（如 $ALC, ALCI, ALCO, ALCIO$）以及三种主流递归语义。
*   **对比基准**：对比了描述逻辑中的概念包含问题（Concept Subsumption）和逻辑程序中的良基模型理论。

### 4. 资源与算力
*   **算力说明**：文中未提及任何 GPU 或大规模集群的使用。所有的结论均基于计算复杂性分析（如 EXPTIME 边界的推导）。
*   **实现状态**：论文侧重于算法的可行性证明和复杂度上界推导，未提及具体的原型系统实现。

### 5. 实验数量与充分性
*   **理论完备性**：论文对所提出的问题进行了详尽的分类讨论，包括有限模型属性（Finite Model Property）的分析。
*   **客观性**：通过严谨的归约证明（Reduction）确立了复杂度的下界，并通过自动机构造确立了上界，论证过程符合形式化验证的标准，具有高度的客观性。

### 6. 论文的主要结论与发现
*   **语义差异显著**：在支持模型和稳定模型语义下，递归 SHACL 的蕴含判定是**不可判定**的。
*   **良基语义的优越性**：在良基语义下，该问题是**可判定**的，且复杂度为 **EXPTIME-complete**。这意味着在良基语义下进行静态分析并不比验证单个形状表达式更难。
*   **逻辑关联**：首次揭示了 SHACL 良基语义与混合 $\mu$-演算之间的深刻联系，特别是发现捕获良基语义只需要有限的不动点交替（Alternation）。
*   **有限模型属性**：发现 $ALCO$ 片段具有有限模型属性，而 $ALCI$ 片段则不具备。

### 7. 优点
*   **开创性**：填补了递归 SHACL 文档级静态分析的理论空白。
*   **技术深度**：成功将逻辑编程中的良基语义转化为模态不动点逻辑，提供了最优的决策程序。
*   **实用指导**：为 SHACL 工具开发者提供了理论依据，暗示了良基语义在需要静态保证的应用场景中更具优势。

### 8. 不足与局限
*   **语法覆盖有限**：目前的研究尚未涵盖 SHACL 中的数值约束（如基数限制 `minCount/maxCount`），这在实际应用中非常常见。
*   **有限模型判定的上界较松**：对于有限模型下的蕴含判定，虽然证明了可判定性，但给出的复杂度上界（如 3-EXPTIME）可能不是紧确的（Tight）。
*   **缺乏实证**：虽然理论上是单指数时间，但在实际大规模 SHACL 文档上的性能表现（常数项影响、平均情况复杂度）尚不清楚。

（完）

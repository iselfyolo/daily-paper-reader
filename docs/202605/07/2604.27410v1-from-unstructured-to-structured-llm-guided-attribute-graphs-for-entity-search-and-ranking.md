---
title: "From Unstructured to Structured: LLM-Guided Attribute Graphs for Entity Search and Ranking"
title_zh: 从非结构化到结构化：用于实体搜索与排序的大语言模型引导属性图
authors: "Yilun Zhu, Nikhita Vedula, Shervin Malmasi"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27410v1"
tags: ["query:kgee"]
score: 8.0
evidence: 用于结构化信息提取和排序的大模型引导属性图
tldr: "针对电商实体搜索中传统嵌入方法难以捕捉细粒度上下文属性相关性的问题，本文提出一种结合大语言模型（LLM）与属性图的两阶段方法。离线阶段利用LLM从非结构化文本中提取属性并构建类别感知的属性图；在线阶段通过对结构化表示进行推理来排序候选实体。该方法在零样本场景下将排序精度提升了5%以上，同时减少了57%的Token消耗，展现了极高的实用价值。"
selection_source: fresh_fetch
motivation: 传统嵌入方法在电商搜索中难以处理不同类别和上下文下复杂的属性相关性。
method: 提出两阶段框架，离线构建类别感知的属性图，在线基于结构化图表示进行LLM推理排序。
result: "在零样本实验中，该方法比基线模型平均精度提升超5%，且单产品Token使用量降低了57%。"
conclusion: 该方法通过结构化属性图有效提升了实体搜索的精度与效率，具有强大的跨类别泛化能力和实际部署潜力。
---

## 摘要
实体搜索，即寻找与查询实体最相似的实体，在电子商务领域面临独特挑战，因为产品相似度会随类别和语境而变化。传统的基于嵌入的方法通常难以捕捉细微的、特定语境下的属性相关性。在本文中，我们提出了一种结合了大语言模型（LLM）驱动的属性图构建与图感知 LLM 排序的两阶段方法。在离线阶段，我们从非结构化文本中提取结构化产品属性，并构建具有类别感知模式的可重用属性图。在在线阶段，我们通过对这种结构化表示而非原始文本进行推理来对检索到的候选对象进行排序，在提高排序精度的同时，将单个产品的 token 使用量减少了 57%。实验表明，我们的方法在零样本场景下优于多个基准模型，在不需要训练数据的情况下，平均精度提升了 5% 以上，在不同产品类别中表现出鲁棒的泛化能力，并展现出巨大的实际部署潜力。

## Abstract
Entity search, i.e., finding the most similar entities to a query entity, faces unique challenges in e-commerce, where product similarity varies across categories and contexts. Traditional embedding-based approaches often struggle to capture nuanced context-specific attribute relevance. In this paper, we present a two-stage approach combining Large Language Model (LLM)-driven attribute graph construction with graph-aware LLM ranking. In the offline stage, we extract structured product attributes from unstructured text, and construct a reusable attribute graph with category-aware schemas. In the online stage, we rank retrieved candidates by reasoning over this structured representation rather than raw text, reducing per-product token usage by 57% while improving ranking precision. Experiments show that our approach outperforms multiple baselines under zero-shot scenarios, achieving a over 5% improvement in average precision without requiring training data, generalizes robustly across diverse product categories, and shows immense potential for real-world deployment.

---

## 论文详细总结（自动生成）

这篇论文由亚马逊（Amazon）的研究团队发表于 SIGIR '26，提出了一种结合大语言模型（LLM）与属性图的实体搜索与排序框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在电商场景中，实体搜索（寻找相似或可替代产品）至关重要。然而，传统的基于嵌入（Embedding）的方法难以捕捉特定类别下的细微属性差异（如电视的屏幕尺寸 vs. 衣服的材质）。
*   **核心挑战**：
    1.  产品信息多为非结构化文本（标题、描述），难以直接进行系统化比较。
    2.  直接将长篇原始文本输入 LLM 进行排序虽然效果好，但推理成本高、延迟大，且缺乏可解释性。
*   **整体含义**：本文旨在通过将非结构化文本转化为结构化的“属性图”，利用 LLM 对精炼后的属性进行推理，从而在降低计算成本的同时提升搜索排序的精度。

### 2. 方法论
该方法分为两个阶段：

#### 第一阶段：离线属性图构建（Offline Stage）
*   **属性定义（Attribute Definition）**：利用 LLM（Claude 3.5 Sonnet）为 61 个大类和 4940 个子类定义属性模式（Schema）。采用层级结构，子类继承大类属性并增加特定属性。
*   **属性值提取（Attribute Value Extraction）**：将产品文本输入 LLM，根据定义的 Schema 提取 JSON 格式的属性-值对。
*   **图构建**：构建一个二部图 $G = (V_P, V_A, E)$，其中 $V_P$ 是产品节点，$V_A$ 是属性节点，边 $E$ 上的标签代表具体的属性值。

#### 第二阶段：在线图感知排序（Online Stage）
*   **候选生成**：使用 BGE-M3 模型进行向量检索（Dense Retrieval），从海量库中召回前 $K$ 个候选实体。
*   **图感知 LLM 排序**：
    *   **核心思想**：不再向 LLM 输入冗长的产品描述，而是输入查询实体与候选实体的结构化属性对比。
    *   **推理逻辑**：LLM 充当零样本（Zero-shot）列表排序器，通过比较属性值的对齐程度，给出 0-100 的相似度评分。
    *   **公式化表达**：相似度 $S = F_{LLM}(\{(a, val(q, a), val(c, a)) | a \in V_A\})$，即基于共同属性 $a$ 的取值差异进行推理。

### 3. 实验设计
*   **数据集**：使用 Amazon Shopping Queries (ESCI) 数据集，包含约 47 万个产品。
*   **实验场景**：从 8.7k 个查询实体中采样，覆盖多种产品大类（剔除了成人用品、书籍媒体等属性不明显的类别）。
*   **对比方法（Baselines）**：
    1.  **Sparse Retrieval (sr)**：基于 BM25 的稀疏检索。
    2.  **Dense Retrieval (dr)**：基于 BGE-M3 的向量检索。
    3.  **dr + raw-ranker**：向量检索后，使用 LLM 对原始非结构化文本进行排序。
    4.  **dr + graph-ranker**（本文方法）：向量检索后，使用 LLM 对结构化属性图进行排序。
*   **评估指标**：P@k、MRR、mAP、nDCG。

### 4. 资源与算力
*   **模型使用**：主要使用 **Claude 3.5 Sonnet v2** 进行属性提取和在线排序；使用 **Amazon Nova Pro** 作为自动评估器（LLM-as-a-judge）。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或训练时长。由于该方法主要基于现有的预训练 LLM 进行零样本推理和离线提取，其重点在于 API 调用和推理效率的优化。
*   **效率提升**：明确提到相比原始文本排序，单产品 Token 使用量减少了 **57%**，推理延迟缩短了约 **250ms**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **人工评估**：由 3 名专家对 40 个查询、200 个候选对进行标注（Cohen’s $\kappa = 0.71$，一致性高）。
    *   **自动评估**：在 8.7k 个查询实体上进行大规模测试。
    *   **提取质量评估**：对 2 万个样本进行属性提取准确率测试（F1 值达 83.47%）。
*   **充分性评价**：实验设计较为充分，涵盖了人工校验与自动化评估，并设置了不同相似度阈值（$\ge 80$ 和 $\ge 50$）来验证鲁棒性。对比了稀疏、稠密及原始文本 LLM 排序，证明了结构化信息的增益。

### 6. 主要结论与发现
*   **精度提升**：在零样本场景下，该方法比强基线（dr + raw-ranker）在平均精度上提升了 **5% 以上**，P@1 提升显著。
*   **效率优化**：通过将非结构化文本压缩为结构化属性，大幅减少了 LLM 的上下文窗口负担，Token 消耗降低 57%。
*   **泛化能力**：该方法在不同产品类别间表现稳健，能够处理冷启动问题（通过实时提取新实体的属性）。
*   **可解释性**：结构化的属性对比使排序结果更具透明度，便于分析为何两个产品被判定为相似。

### 7. 优点
*   **解耦设计**：将知识构建（离线）与排序推理（在线）解耦，兼顾了深度推理与工业级延迟要求。
*   **Token 经济性**：在不损失（甚至提升）精度的情况下显著降低了 LLM 的推理成本。
*   **Schema 灵活性**：支持动态生成新类别的属性模式，适应电商领域商品更迭快的特点。

### 8. 不足与局限
*   **领域局限性**：对于书籍、电影等缺乏明确物理属性的类别，该方法效果有限（文中已主动剔除）。
*   **依赖 Schema 质量**：如果初始的属性模式定义不全或提取环节出现幻觉，会直接影响后续排序。
*   **专业领域挑战**：在医疗器械等高度专业化的领域，可能需要专家干预来修正 LLM 生成的属性模式。
*   **成本考量**：虽然比原始文本排序便宜，但相比纯向量检索，引入 LLM 排序环节仍会增加额外的 API 开销和系统复杂度。

（完）

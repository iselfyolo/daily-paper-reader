---
title: "Falkor-IRAC: Graph-Constrained Generation for Verified Legal Reasoning in Indian Judicial AI"
title_zh: Falkor-IRAC：用于印度司法人工智能中经过验证的法律推理的图约束生成
authors: Joy Bose
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14665v2"
tags: ["query:profile-3"]
score: 10.0
evidence: 图约束法律推理与IRAC知识图谱
tldr: 针对印度司法AI中大模型易产生虚假先例和推理链断裂的问题，本文提出Falkor-IRAC框架。该框架将法律判决转化为IRAC结构化知识图谱，通过图约束生成技术确保推理过程有据可查。系统引入验证代理对生成路径进行证伪检查，并能识别法律冲突。实验证明该方法在引用准确性和幻觉抑制方面表现优异，并发布了包含500多份标注判决的InIRAC数据集。
selection_source: fresh_fetch
motivation: 传统的向量检索增强生成（RAG）无法准确表达法律推理中的符号逻辑和程序状态转换，导致法律AI频繁出现虚假先例和推理错误。
method: 构建基于IRAC结构的知识图谱并利用FalkorDB存储，通过验证代理对LLM生成的推理路径进行图约束校验和证伪。
result: 在最高法院判决语料库上的测试显示，该系统能准确验证合法引用并拒绝伪造引用，有效解决了法律AI的幻觉问题。
conclusion: Falkor-IRAC通过将结构化法律推理与图约束生成相结合，为实现可验证、无幻觉的司法人工智能提供了有效路径。
---

## 摘要
法律推理并非语义相似度搜索。法院判决编码了受约束的符号推理：先例传播、程序状态转换以及受法规约束的推断。这些属性是基于向量的检索增强生成（RAG）无法忠实表示的。幻觉先例、过时的法规引用以及缺乏支持的推理链仍然是基于大语言模型（LLM）的法律人工智能中持续存在的失败模式，这对印度等高案件量司法管辖区的司法公正产生了实际影响。本文提出了 Falkor-IRAC，这是一个针对印度法律人工智能的图约束生成框架，它将生成过程建立在基于 IRAC（争议点、规则、分析、结论）知识图谱的结构化推理之上。印度最高法院和高等法院的判决被摄取为 IRAC 节点结构，并丰富了程序状态转换、先例关系和法律条文引用，存储在 FalkorDB 中以便进行低延迟的智能体遍历。在推理阶段，只有当能够通过图谱追踪到有效的支持路径时，LLM 生成的答案才会被接受，这一检查由名为“验证者智能体”（Verifier Agent）的可证伪性预言机执行。该系统还将法理冲突检测作为一等输出，而不是默默地解决它们。Falkor-IRAC 使用图原生指标进行评估：引用落地准确率、路径有效率、幻觉先例率和冲突检测率。论证认为，这些指标比 BLEU 和 ROUGE 更适合法律推理评估。在包含 51 份最高法院判决的概念验证语料库上，验证者智能体正确验证了已完成查询中的引用，并正确拒绝了伪造的引用。与仅使用向量的 RAG 基准的对比评估留待未来工作。随本文一同发布的还有配套的 InIRAC 数据集，包含 500 多份带有 IRAC 注释的结构化印度法院判决。

## Abstract
Legal reasoning is not semantic similarity search. A court judgment encodes constrained symbolic reasoning: precedent propagation, procedural state transitions, and statute-bound inference. These are properties that vector-based retrieval-augmented generation (RAG) cannot faithfully represent. Hallucinated precedents, outdated statute citations, and unsupported reasoning chains remain persistent failure modes in LLM-based legal AI, with real consequences for access to justice in high-caseload jurisdictions such as India. This paper presents Falkor-IRAC, a graph-constrained generation framework for Indian legal AI that grounds generation in structured reasoning over an IRAC (Issue, Rule, Analysis, Conclusion) knowledge graph. Judgments from the Supreme Court and High Courts of India are ingested as IRAC node structures enriched with procedural state transitions, precedent relationships, and statutory references, stored in FalkorDB for low-latency agentic traversal. At inference time, LLM-generated answers are accepted only if a valid supporting path can be traced through the graph, a check performed by a falsifiability oracle called the Verifier Agent. The system also detects doctrinal conflicts as a first-class output rather than silently resolving them. Falkor-IRAC is evaluated using graph-native metrics: citation grounding accuracy, path validity rate, hallucinated precedent rate, and conflict detection rate. These metrics are argued to be more appropriate for legal reasoning evaluation than BLEU and ROUGE. On a proof-of-concept corpus of 51 Supreme Court judgments, the Verifier Agent correctly validated citations on completed queries and correctly rejected fabricated citations. Evaluation against vector-only RAG baselines is left for future work. The companion InIRAC dataset, 500+ structured Indian court judgments with IRAC annotations, is released alongside this paper.

---

## 论文详细总结（自动生成）

### 论文总结：Falkor-IRAC —— 用于印度司法人工智能的图约束生成框架

#### 1. 论文的核心问题与整体含义
*   **研究动机**：法律推理本质上是受约束的符号推理（如先例传播、程序状态转换），而非简单的语义相似度搜索。
*   **核心问题**：现有的基于向量检索的 RAG（检索增强生成）系统在法律领域存在三大缺陷：
    1.  **语义相似性不等于法律相关性**：关键先例可能在词汇上与当前案件完全不同。
    2.  **幻觉先例的职业风险**：LLM 虚构的引用可能导致严重的司法误导。
    3.  **缺乏程序状态模型**：法律推理具有状态性（如诉讼所处的具体阶段），RAG 无法捕捉这种动态过程。
*   **背景**：印度司法系统面临 5000 万件积压案件，亟需一种能够验证推理链、不产生幻觉且能处理复杂程序逻辑的 AI 工具。

#### 2. 论文提出的方法论
*   **核心思想**：**图约束生成（Graph-Constrained Generation）**。将生成与验证分离，LLM 提出的答案必须通过“验证者智能体”（Verifier Agent）在知识图谱中的路径校验，否则将被拒绝或要求修正。
*   **关键技术细节**：
    *   **IRAC 知识图谱**：构建包含争议点（Issue）、规则（Rule）、分析（Analysis）、结论（Conclusion）的结构化图谱，并扩展了程序事件链（Procedural Event Chains）和冲突类型先例关系。
    *   **三层架构**：
        1.  **检索智能体**：在 FalkorDB 中执行多策略图遍历（如事项类型、法律条文、引用链）。
        2.  **LLM 生成器**：在图路径引导下合成答案。
        3.  **验证者智能体（证伪预言机）**：对生成的引用进行硬约束检查。若路径不存在，则行使“否决权”，要求 LLM 修改或选择弃权（Abstain）。
    *   **冲突检测**：将法理冲突（如不同法官的矛盾判决）作为一等输出，明确告知用户存在未解决的法律分歧。

#### 3. 实验设计
*   **数据集**：
    *   **InIRAC 数据集**：作者发布的包含 500 多份印度法院判决的结构化数据集（含 IRAC 标注和程序链）。
    *   **评估子集**：初步实验使用了 51 份最高法院判决构成的概念验证语料库。
*   **Benchmark 与对比**：
    *   **模型**：对比了 TinyLlama (1B) 和 Mistral 7B-Instruct。
    *   **指标**：提出了一套**图原生指标**（Graph-native metrics），包括引用落地准确率、路径有效率、幻觉先例率、冲突检测率等，认为传统的 BLEU/ROUGE 不适用于法律推理。
*   **场景**：涵盖保释程序、宪法权利、服务法和藐视法庭等 10 个典型查询。

#### 4. 资源与算力
*   **算力说明**：实验在**消费级 CPU 硬件**上运行（通过 Ollama 部署本地模型）。
*   **局限性**：由于缺乏 GPU 加速，推理延迟较高，10 个查询中有 7 个因超过 300 秒而超时。
*   **数据库**：使用 FalkorDB 实现亚毫秒级的图查询响应。

#### 5. 实验数量与充分性
*   **实验规模**：目前仅处于概念验证（PoC）阶段，针对 51 份判决进行了 10 组查询测试。
*   **充分性评价**：
    *   **验证有效性**：实验证明了验证者智能体能 100% 识别库内真实引用并拒绝虚假引用，达到了架构验证的目的。
    *   **对比不足**：尚未与传统的向量 RAG 或 GraphRAG 进行大规模基准对比，且样本量较小，结论的普适性有待在更大规模（如 3.4 万份判决的 ILDC 全集）上进一步验证。

#### 6. 论文的主要结论与发现
*   **硬约束的必要性**：在法律等高风险领域，基于图路径的“硬否决”机制比软性提示词约束更有效，能显著消除幻觉。
*   **程序推理的价值**：通过图谱追踪诉讼状态（如保释被拒后的下一步选择），比单纯的文本检索更能解决当事人的实际问题。
*   **诚实的 AI 设计**：系统在无法验证时选择“弃权”或“报告冲突”，比给出看似合理但错误的答案更具职业道德。

#### 7. 优点
*   **架构创新**：首次将 IRAC 结构、程序状态机和冲突检测整合进印度法律知识图谱。
*   **高可靠性**：通过图约束确保了推理链的可追溯性和真实性。
*   **开源贡献**：发布了 InIRAC 数据集，填补了印度结构化法律推理数据的空白。

#### 8. 不足与局限
*   **硬件瓶颈**：CPU 推理速度极慢，限制了大规模测试。
*   **覆盖范围**：验证能力受限于已摄取的图谱规模，无法验证库外判决。
*   **提取噪声**：依赖 LLM 进行 IRAC 结构提取，可能引入初始数据噪声。
*   **语言限制**：虽然设计了多语言支持，但目前的评估主要集中在英语。

（完）

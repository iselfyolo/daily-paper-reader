---
title: Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs
title_zh: 评估大语言模型中用于确定性计算的提示与基于执行的方法
authors: Hongkun Yu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03227v2"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于评估大模型推理和计算的合成数据集
tldr: 本研究系统评估了大语言模型在确定性计算任务（如二进制计数、算术运算）中的表现。通过引入合成数据集并对比CoT、Least-to-Most、PoT和SC等提示策略，发现传统提示法准确率有限且易出错，而基于代码执行的PoT表现最佳。此外，微调小型专用模型也能实现完美准确率。研究表明，将LLM与外部工具结合是处理确定性任务的更可靠方案。
selection_source: fresh_fetch
motivation: 旨在系统评估大语言模型在处理需要精确、无误的确定性符号计算任务时的能力及局限性。
method: 引入包含多样化指令的合成数据集，对比评估了多种提示策略，并尝试微调小型专用模型CodeT5-small。
result: 传统提示策略准确率中等且易受错误累积影响，而PoT通过生成可执行代码并调用外部解释器实现了完美准确率。
conclusion: LLM更倾向于模拟推理模式而非执行精确符号计算，在确定性任务中应优先结合外部工具或使用专用模型。
---

## 摘要
大语言模型（LLMs）在自然语言理解和推理方面展现出强大的能力。然而，它们执行精确、确定性计算的能力仍不明确。在这项工作中，我们系统地评估了多种提示策略，包括思维链（CoT）、由简入繁分解（Least-to-Most decomposition）、程序思维（PoT）和自洽性（SC），在需要精确且无误输出的任务上的表现，这些任务包括二进制计数、最长子串检测和算术求值。为了支持这项研究，我们引入了一个包含多样化自然语言指令的合成数据集，从而能够对跨多种任务类型的精确计算进行受控评估。我们的结果表明，标准的提示方法在基于序列的任务上仅达到中等准确率。CoT 提供的改进有限，而 Least-to-Most 则受到错误累积的影响。相比之下，PoT 通过生成可执行代码并将计算委托给外部解释器，实现了完美的准确率。自洽性通过多数投票提高了鲁棒性，但带来了巨大的计算开销。我们进一步训练了一个小型领域特定模型（CodeT5-small）来生成可执行程序，该模型在所有任务的留出合成测试数据上均以极低的训练成本实现了完美的准确率。总的来说，我们的研究结果表明，LLMs 可能是在模拟推理模式，而非可靠地执行精确的符号计算。对于确定性任务，将 LLMs 与外部工具结合或使用专门的模型提供了一种更可靠且高效的解决方案。

## Abstract
Large Language Models (LLMs) have demonstrated strong capabilities in natural language understanding and reasoning. However, their ability to perform exact, deterministic computation remains unclear. In this work, we systematically evaluate multiple prompting strategies, including Chain-of-Thought (CoT), Least-to-Most decomposition, Program-of-Thought (PoT), and Self-Consistency (SC), on tasks requiring precise and error-free outputs, including binary counting, longest substring detection, and arithmetic evaluation. To support this study, we introduce a synthetic dataset with diverse natural language instructions, enabling controlled evaluation of exact computation across multiple task types. Our results show that standard prompting methods achieve only moderate accuracy on sequence-based tasks. CoT provides limited improvement, while Least-to-Most suffers from error accumulation. In contrast, PoT achieves perfect accuracy by generating executable code and delegating computation to an external interpreter. Self-Consistency improves robustness through majority voting, but incurs substantial computational overhead. We further train a small domain-specific model (CodeT5-small) to generate executable programs, which achieves perfect accuracy on held-out synthetic test data across all tasks with minimal training cost. Overall, our findings suggest that LLMs may simulate reasoning patterns rather than reliably perform exact symbolic computation. For deterministic tasks, combining LLMs with external tools or using specialized models provides a more reliable and efficient solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLMs）在处理概率性的自然语言任务时表现优异，但在处理需要**精确、无误的确定性计算**（如二进制计数、符号操作、长算术运算）时，其可靠性究竟如何？
*   **研究动机**：现有的推理增强技术（如 CoT）多在模糊推理任务上评估，而确定性任务要求零容忍的准确性。作者旨在探究 LLM 是在“执行计算”还是仅仅在“模拟推理模式”，并寻找实现精确计算的最优路径。

### 2. 论文提出的方法论
论文对比了五种提示策略以及一种微调模型方法：
*   **提示策略（Prompting Strategies）**：
    *   **Plain Prompt**：直接给出指令和输入，要求输出结果。
    *   **Chain-of-Thought (CoT)**：引导模型生成中间推理步骤。
    *   **Least-to-Most**：将复杂问题分解为子问题（如分段计数）并顺序求解。
    *   **Program-of-Thought (PoT)**：核心思想是**解耦推理与计算**。模型生成 Python 代码，由外部解释器执行并返回结果。
    *   **Self-Consistency (SC)**：通过多次采样（T=0.7）并进行多数投票来提高鲁棒性。
*   **微调模型（Specialized Model）**：
    *   使用 **CodeT5-small** 在合成数据集上进行监督微调，使其学习将自然语言指令映射为可执行的 Python 代码。

### 3. 实验设计
*   **合成数据集**：构建了包含三种任务的测试集：
    1.  **二进制计数**：统计 80-120 位随机序列中 0 和 1 的个数。
    2.  **最长子串检测**：在杂乱序列中寻找特定字符的最长连续重复长度。
    3.  **算术求值**：包含 20-40 个操作数及四则运算的复杂表达式。
*   **Benchmark 与对比**：
    *   使用 `gpt-oss-120b` 作为基础 LLM 进行零样本（Zero-shot）评估。
    *   对比上述五种提示策略在单任务和混合任务下的 **Exact-match Accuracy（精确匹配准确率）**。

### 4. 资源与算力
*   **硬件**：使用了单个 **Google Colab T4 GPU**。
*   **训练时长**：CodeT5-small 的微调过程非常高效，总训练时间约为 **3 分钟**。
*   **模型规模**：对比了 120B 参数的大模型（提示法）与小型 CodeT5 模型（微调法）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   数据集包含 3000 条训练样本、1500 条验证样本和 600 条测试样本。
    *   提示法评估抽取了 100-150 个样本；由于 API 成本，自洽性（SC）评估使用了 50-90 个样本（每样本 5 次查询）。
*   **充分性与客观性**：实验涵盖了从简单算术到复杂序列追踪的不同难度梯度，并引入了多样化的自然语言指令以避免模板过拟合。实验设计较为客观，但在模型多样性（仅测试了一个大模型）方面略显局限。

### 6. 主要结论与发现
*   **任务难度分级**：算术运算（100% 准确） $\ll$ 二进制计数 $<$ 最长子串（最难）。
*   **提示法的局限**：CoT 对精确计算提升微乎其微；Least-to-Most 甚至因**错误累积**导致表现下降。
*   **PoT 的优越性**：基于执行的方法（PoT）在所有任务中均达到 **100% 准确率**，证明了将计算委托给外部工具的必要性。
*   **小模型的潜力**：经过微调的 CodeT5-small 同样达到了 100% 准确率，说明专用小模型在确定性任务上比通用大模型更高效、更可靠。
*   **本质发现**：LLM 倾向于**模拟推理的语言模式**，而非真正执行符号逻辑。

### 7. 优点
*   **解耦思想**：明确提出了计算与推理分离的重要性。
*   **数据集贡献**：发布了针对确定性计算的合成数据集和评估代码，具有复现价值。
*   **实用性强**：证明了低成本的小模型微调可以解决大模型难以处理的精确计算问题。

### 8. 不足与局限
*   **数据集单一**：仅使用合成数据，未测试包含噪声或非结构化描述的真实场景数据。
*   **模型覆盖面**：主要评估了 `gpt-oss-120b`，未对比 GPT-4 或 Claude 等其他主流闭源模型在 PoT 上的表现。
*   **任务复杂度**：虽然序列较长，但逻辑逻辑相对单一，未涉及更复杂的算法逻辑（如动态规划或图论计算）。

（完）

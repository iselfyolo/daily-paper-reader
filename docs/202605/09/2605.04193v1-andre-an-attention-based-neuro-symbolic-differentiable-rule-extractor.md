---
title: "ANDRE: An Attention-based Neuro-symbolic Differentiable Rule Extractor"
title_zh: ANDRE：一种基于注意力的神经符号可微规则提取器
authors: "Iman Sharifi, Peng Wei, Saber Fallah"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04193v1"
tags: ["query:q6"]
score: 8.0
evidence: 学习可解释的一阶规则与逻辑结构
tldr: ANDRE是一个基于注意力机制的神经符号微分规则提取框架，旨在解决归纳逻辑程序设计（ILP）在处理噪声和概率数据时的扩展性与稳定性问题。它通过可微分的注意力算子替代预定义模板，在连续空间内优化一阶逻辑规则，实现了在不确定性环境下的高效、可解释推理，显著提升了规则提取的质量与鲁棒性。
selection_source: fresh_fetch
motivation: 传统ILP方法难以应对噪声数据，而现有微分ILP方法受限于预定义模板或不准确的模糊逻辑算子，导致梯度消失或推理不稳。
method: 引入基于注意力机制的可微分逻辑算子，通过在连续空间内软性选择和组合谓词，模拟逻辑的交并语义并支持谓词否定。
result: 实验表明ANDRE在多个基准测试中表现优异，不仅能准确恢复符号规则，且在处理标签噪声和概率谓词时具有极高的稳定性。
conclusion: 该研究通过注意力机制成功融合了深度学习的优化优势与符号逻辑的可解释性，为复杂环境下的逻辑规则归纳提供了新途径。
---

## 摘要
归纳逻辑程序设计 (ILP) 旨在从数据中学习可解释的一阶规则，但现有的符号和神经符号方法难以扩展到噪声和概率环境。经典的 ILP 依赖于离散的组合规则搜索，在不确定性下表现脆弱；而可微 ILP 方法通常依赖于预定义的规则模板或不准确的模糊算子，在对概率谓词估值进行推理时，这些算子容易出现梯度消失或逻辑结构近似效果差的问题。本文提出了一种基于注意力的神经符号可微规则提取器 (ANDRE)，这是一种新型的 ILP 框架，通过使用基于注意力的逻辑算子在连续规则空间上进行优化来学习一阶逻辑程序。ANDRE 用完全可微的、注意力驱动的合取和析取算子取代了规则模板和逻辑算子，这些算子近似于逻辑的极大极小 (min-max) 语义，从而实现了对概率数据准确、稳定且可解释的推理。通过在每个规则中软性地选择、否定或排除谓词，ANDRE 在保留符号结构的同时支持灵活的规则归纳。在经典 ILP 基准测试、大规模知识库以及具有概率谓词和噪声监督的合成数据集上进行的广泛实验表明，ANDRE 实现了具有竞争力的或更优的预测性能，同时在不确定性下能够可靠地恢复正确的符号规则。特别是，ANDRE 对中度标签噪声保持鲁棒性，在规则提取质量和稳定性方面均显著优于现有的可微 ILP 方法。

## Abstract
Inductive Logic Programming (ILP) aims to learn interpretable first-order rules from data, but existing symbolic and neuro-symbolic approaches struggle to scale to noisy and probabilistic settings. Classical ILP relies on discrete combinatorial rule search and is brittle under uncertainty, while differentiable ILP methods typically depend on predefined rule templates or inaccurate fuzzy operators that suffer from vanishing gradients or poor approximation of logical structure when reasoning over probabilistic predicate valuations. This paper proposes an Attention-based Neuro-symbolic Differentiable Rule Extractor (ANDRE), a novel ILP framework that learns first-order logic programs by optimizing over a continuous rule space with attention-based logical operators. ANDRE replaces both rule templates and logical operators with fully differentiable, attention-driven conjunction and disjunction operators that approximate logical min-max semantics, enabling accurate, stable, and interpretable reasoning over probabilistic data. By softly selecting, negating, or excluding predicates within each rule, ANDRE supports flexible rule induction while preserving symbolic structure. Extensive experiments on classical ILP benchmarks, large-scale knowledge bases, and synthetic datasets with probabilistic predicates and noisy supervision demonstrate that ANDRE achieves competitive or superior predictive performance while reliably recovering correct symbolic rules under uncertainty. In particular, ANDRE remains robust to moderate label noise, substantially outperforming existing differentiable ILP methods in both rule extraction quality and stability.

---

## 论文详细总结（自动生成）

### 论文总结：ANDRE — 基于注意力的神经符号可微规则提取器

#### 1. 核心问题与整体含义（研究动机和背景）
归纳逻辑程序设计（ILP）的核心目标是从结构化数据中自动学习人类可读的一阶逻辑规则。
*   **研究动机**：传统的符号 ILP 方法（如 Metagol）虽然解释性强，但对数据噪声极其敏感，且需要大量人工预定义的“语言偏置”（如模式声明）。现有的神经符号 ILP 方法（如 $\partial$ILP）虽然引入了可微性，但通常依赖于固定的规则模板，或者使用基于乘积的模糊逻辑算子（Product t-norms）。
*   **核心痛点**：现有的模糊算子在处理概率数据时会导致梯度消失，且对逻辑结构的近似不够准确。此外，缺乏一种既能摆脱模板限制，又能保持符号结构完整性的全可微框架。

#### 2. 方法论：核心思想与技术细节
ANDRE 提出了一种在连续规则空间内通过梯度下降优化一阶逻辑程序的方法。
*   **连续规则空间构建**：将谓词的选择、否定或排除（Identity）建模为概率分布。每个谓词 $B_{ij}$ 有三种状态：原谓词 $b_j$、否定 $\neg b_j$ 或排除（逻辑恒等值 1）。通过 **Prob-Sum 激活函数**（基于概率和的平滑近似）将这三种状态融合。
*   **注意力逻辑算子**：
    *   **合取层（AND）**：使用 **Softmin 注意力** 机制。它通过给较小值分配更高权重来模拟逻辑“与”（min 语义），避免了乘积算子在概率数据下的快速衰减。
    *   **析取层（OR）**：使用 **Softmax 注意力** 机制。它通过强调较大值来模拟逻辑“或”（max 语义）。
*   **损失函数体系**：
    *   **语义损失**：包括二元交叉熵（BCE）、鼓励确定性的熵损失（Entropy）和鼓励子规则多样性的相似度损失。
    *   **语法损失**：引入 **值域受限损失（Range-Restricted Loss）** 确保头变量出现在体谓词中，以及 **连接性损失（Connected Loss）** 确保辅助变量在规则中起到桥梁作用。
*   **课程学习与调度**：采用损失权重调度技术，在训练初期侧重语义对齐，后期逐渐加强语法约束和数字化约束。

#### 3. 实验设计
论文在三个维度上进行了广泛评估：
*   **经典 ILP 基准测试**：包括 Predecessor, Odd/Even, Grandparent, Son, Connectedness 等 10 个任务。
*   **大规模知识库（KB）**：使用 Countries (S1-S3), Nations, UMLS, UW-CSE 和 Alzheimers-amine 数据集。
*   **合成概率与噪声数据**：设计了具有不同复杂度和标签噪声（最高达 45%）的合成数据集，专门测试模型在不确定性下的规则恢复能力。
*   **对比基准**：对比了 $\partial$ILP, NeuralLP, DFORL, NTP$\lambda$, CILP++, D-LFIT 等多种最先进的方法。

#### 4. 资源与算力
*   **硬件设施**：实验在配备 **NVIDIA GeForce RTX 5090** GPU 的服务器上运行。
*   **训练时长**：ANDRE 表现出较高的计算效率。例如，在 Countries-S1 数据集上仅需 3.6 分钟，在 Nations 数据集上约 1.2 分钟。相比最强竞争对手 DFORL，ANDRE 在多个任务上的运行速度显著提升（如 Countries-S3 任务快了约 16 倍）。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单逻辑关系到复杂生物医学知识库的 20 多个数据集。
*   **消融实验**：专门对比了“注意力算子”与“乘积算子”的效果，结果显示注意力机制使准确率提升了约 21%。
*   **鲁棒性测试**：在不同噪声水平（5% 到 45%）下进行了多组重复实验。
*   **评价指标**：使用了准确率（ACC）、平均倒数排名（MRR）、HITS@K 以及符号规则提取成功率。实验设计客观、全面，充分验证了模型在不同场景下的泛化能力。

#### 6. 主要结论与发现
*   **规则提取能力**：ANDRE 在所有经典 ILP 任务中均实现了 100% 的规则提取准确率。
*   **噪声鲁棒性**：在存在 25% 标签噪声的情况下，ANDRE 仍能稳定恢复正确的符号规则，而 DFORL 等方法在噪声下容易崩溃。
*   **概率处理**：注意力算子在处理非布尔（概率）谓词估值时，比传统的模糊逻辑算子更准确、更稳定，有效缓解了梯度消失问题。
*   **可扩展性**：能够处理包含数千个事实的大规模知识库，并提取出简洁、符合逻辑语法的规则。

#### 7. 优点（亮点）
*   **无需模板**：摆脱了对预定义规则模板的依赖，实现了真正的端到端规则发现。
*   **创新的算子设计**：首次在 ILP 中明确使用 Softmin/Softmax 注意力作为逻辑算子的可微近似，理论证明其比乘积算子更接近真实的 min-max 语义。
*   **语法约束集成**：通过专门的语法损失函数，确保了神经网络学到的“权重”能够转化为合法的逻辑程序。

#### 8. 不足与局限
*   **规则深度限制**：目前的架构主要针对较浅的规则结构，对于极深或高度嵌套的逻辑组合可能存在搜索效率问题。
*   **命题化开销**：模型依赖于对关系数据的命题化处理（Propositionalization），这在处理超大规模动态图数据时可能会产生预处理负担。
*   **阈值敏感性**：符号规则的最终提取依赖于置信度阈值（$\eta'$），在某些边界情况下可能需要手动微调。

（完）

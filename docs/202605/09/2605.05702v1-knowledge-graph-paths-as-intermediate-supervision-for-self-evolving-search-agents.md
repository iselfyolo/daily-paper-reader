---
title: Knowledge-Graph Paths as Intermediate Supervision for Self-Evolving Search Agents
title_zh: 知识图谱路径作为自进化搜索智能体的中间监督
authors: "Huyu Wu, Jun Liu, Xiaochi Wei, Yan Gao, Yi Wu, Yao Hu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05702v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 通过自我博弈生成搜索任务和训练问题
tldr: 本研究针对自我进化搜索智能体（SSP）中提问者缺乏上下文和解决者仅有二元奖励的问题，提出利用知识图谱（KG）路径作为中间监督。通过LLM引导的KG子图增强提问的关联性，并引入路标覆盖奖励（WCR）为搜索轨迹提供过程反馈。实验表明，该方法在多个问答基准上显著提升了性能，证明了KG路径在无需额外人工标注的情况下能有效引导智能体进化。
selection_source: fresh_fetch
motivation: 现有的自我进化搜索智能体在提问阶段缺乏关系上下文，且在解决阶段仅依赖二元奖励，导致训练效率低下。
method: 利用知识图谱路径为提问提供关联背景，并根据搜索轨迹对路径实体的覆盖程度提供梯度化的路标覆盖奖励（WCR）。
result: 在7个问答基准和9种模型配置下，该方法均优于标准SSP，尤其在多跳问答任务中提升显著。
conclusion: 知识图谱路径可作为轻量级的中间监督信号，在无需人工标注的情况下同时优化问题的构建质量和搜索过程的反馈。
---

## 摘要
自进化搜索智能体通过生成并解决自身的搜索任务，减少了对人工编写训练问题的依赖。我们基于搜索自博弈（Search Self-Play, SSP）开展研究，这是一种代表性的“提议者-求解者”（Proposer and Solver）框架，其中问题通过多步搜索和推理生成并解答。然而在实践中，SSP 面临两个瓶颈：提议者在缺乏关系上下文的情况下从孤立的答案实体构建问题，导致在自博弈训练早期产生许多无效或无法验证的问题；而求解者仅接收二元结果奖励，丢弃了来自部分正确的搜索轨迹中的有用信号。我们通过重用知识图谱路径作为源自构建过程的中间监督，同时用于问题构建和奖励塑造，从而解决了这两个瓶颈。首先，我们将问题构建建立在 LLM 引导的知识图谱子图之上，为提议者提供关系上下文。其次，我们观察到构建和解决多跳问题可能涉及重叠的中间实体：用于构思问题的“事实桥梁”可以为回答问题提供近似的航点（waypoints）。利用这种重叠，我们引入了航点覆盖奖励（Waypoint Coverage Reward, WCR），该奖励根据错误求解轨迹对构建路径上实体的覆盖程度给予分级的部分信用，同时保留对正确答案的全额奖励。在七个问答基准测试和九种模型配置中，我们的方法在所有配置下都比标准 SSP 提高了平均分数，包括在多跳问答任务上的显著提升。这些结果表明，知识图谱路径可以作为轻量级的中间监督被重用，在无需额外特定任务的人工标注或手动标记过程步骤的情况下，提供关系引导和过程反馈。

## Abstract
Self-evolving search agents reduce reliance on human-written training questions by generating and solving their own search tasks. We build on Search Self-Play (SSP), a representative Proposer and Solver framework in which questions are generated and answered via multi-step search and reasoning. In practice, however, SSP faces two bottlenecks: the Proposer constructs questions from isolated answer entities without relational context, yielding many invalid or unverifiable questions in early self-play training, while the Solver receives only a binary outcome reward that discards useful signal from partially on-track search trajectories. We address both bottlenecks by reusing knowledge-graph paths as construction-derived intermediate supervision for both question construction and reward shaping. First, we ground question construction in LLM-guided knowledge-graph subgraphs, providing relational context for the Proposer. Second, we observe that constructing and solving a multi-hop question can involve overlapping intermediate entities: the factual bridges used to formulate the question may provide approximate waypoints for answering it. Exploiting this overlap, we introduce Waypoint Coverage Reward (WCR), which grants graded partial credit to incorrect Solver trajectories according to their coverage of entities on the construction path, while preserving full reward for correct answers. Across seven QA benchmarks and nine model configurations, our approach improves the average score over standard SSP in all configurations, including notable gains on multi-hop QA tasks. These results suggest that knowledge-graph paths can be reused as lightweight intermediate supervision, providing both relational guidance and process feedback without additional task-specific human annotations or manually labeled process steps.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowledge-Graph Paths as Intermediate Supervision for Self-Evolving Search Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
自进化搜索智能体（如 Search Self-Play, SSP）通过自我博弈生成并解决任务来减少对人工标注的依赖。然而，现有的 SSP 框架存在两个关键瓶颈：
*   **提议者（Proposer）侧：** 仅根据孤立的答案实体生成问题，缺乏关系上下文，导致训练早期生成的大量问题无效或不可验证（实验显示早期合格率仅 8.3%）。
*   **求解者（Solver）侧：** 仅接收二元的“对/错”奖励，忽略了搜索轨迹中部分正确的中间步骤，导致样本效率低下。

本研究的核心动机是：**能否在不增加人工标注的前提下，利用构建任务时的中间产物来提供监督？** 论文提出将知识图谱（KG）路径作为“构建衍生监督”，同时解决上述两个瓶颈。

### 2. 方法论：核心思想与关键技术
论文提出了一种利用 KG 路径作为中间监督的框架，主要包含两个核心组件：

*   **LLM 引导的子图提取（提议者侧）：**
    *   不再只给提议者一个答案实体，而是从 Wikidata 中提取一个局部子图。
    *   **流程：** 从种子节点开始，利用 LLM 引导迭代扩展路径（选择最相关的边），形成一条“目标路径”，并采样若干“干扰分支”以增加难度。
    *   提议者基于此子图生成问题，使其具备明确的多跳逻辑和事实依据。

*   **航点覆盖奖励（WCR, 求解者侧）：**
    *   **核心思想：** 构建问题时的 KG 路径节点可以作为求解时的“航点”（Waypoints）。如果求解者的推理轨迹覆盖了这些实体，说明其方向大致正确。
    *   **公式：** 奖励 $R_i = c_i + \alpha (1 - c_i) z_{val,i} \tilde{g}_i(q)$。
        *   $c_i$：最终答案是否正确的二元奖励（0 或 1）。
        *   $\tilde{g}_i(q)$：归一化的航点覆盖率（Solver 推理文本中出现的路径实体比例）。
        *   $\alpha$：调节系数（实验设定为 0.3）。
    *   **特点：** 仅对错误轨迹给予部分信用（Partial Credit），正确答案始终获得满分，从而保持奖励的中立性。

### 3. 实验设计
*   **数据集：** 涵盖 7 个 QA 基准测试。
    *   **单跳/通用：** NQ, TriviaQA, PopQA。
    *   **多跳推理：** HotpotQA, 2WikiMultiHopQA, MuSiQue, Bamboogle。
*   **模型配置：** 测试了 9 种模型配置，包括 Qwen2.5 (3B, 7B, 14B, 32B)、LLaMA-3.1-8B 以及专门的搜索模型 Search-R1-7B、ZeroSearch-7B。
*   **对比方法：**
    *   **Base SSP：** 标准的搜索自我博弈框架。
    *   **Dr.Zero：** 另一种无需训练数据的自进化搜索智能体。
    *   **消融实验：** 验证 KG 构造和 WCR 奖励各自的贡献。

### 4. 资源与算力
*   **硬件：** 使用 1 个节点，配备 **8 张 H20 GPU** (141 GB)。
*   **训练时长：** 完整的自我博弈训练循环（1 个 Epoch，约 195 步）耗时约 **69 小时**，总计约 **552 GPU 小时**。
*   **预处理：** 知识图谱索引和子图提取（CPU 密集型）约耗时 15 小时。
*   **WCR 开销：** 引入 WCR 带来的计算开销极低，每步推理仅增加约 15% 的耗时（约 31ms），因为仅涉及字符串匹配。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 9 种不同的模型初始化（从 Base 到 Instruct，从 3B 到 32B）上进行了验证，实验覆盖面非常广。
*   **充分性：** 除了主实验，还进行了：
    *   **答案分布控制实验：** 证明提升并非来自答案实体的分布改变，而是来自结构化监督。
    *   **生成能力评估：** 证明提议者生成的训练数据质量更高。
    *   **敏感度分析：** 测试了奖励系数 $\alpha$ 和子图排序策略的影响。
*   **客观性：** 采用了 LLM-as-a-Judge 与 EM/F1 多种指标，并与最新的 SOTA 方法（如 Dr.Zero）进行了直接对比，实验设计严谨、公平。

### 6. 主要结论与发现
*   **全线提升：** 在所有 9 种模型配置下，该方法均优于标准 SSP。
*   **多跳任务优势：** 在多跳推理任务上提升尤为显著（如 Qwen2.5-7B-Base 在多跳任务上平均提升 7.3 分）。
*   **解决早期瓶颈：** KG 子图显著提高了提议者在训练早期的有效问题生成率。
*   **过程奖励有效性：** 航点覆盖率与答案正确性呈正相关，WCR 成功为 GRPO 优化提供了更细粒度的信号，使模型学习到更深层的搜索行为（搜索轮数平均增加 1.6 轮）。

### 7. 优点（亮点）
*   **无需额外标注：** 巧妙地实现了“一物两用”，将构建任务时的路径直接转化为奖励信号，无需人工编写过程标签（PRM）。
*   **轻量级：** WCR 奖励计算简单，不涉及额外的模型推理开销。
*   **通用性强：** 实验证明该方法在不同规模、不同系列的 LLM 上均表现稳健。

### 8. 不足与局限
*   **知识源限制：** 目前仅在 Wikidata 上验证，对于缺乏结构化知识图谱的领域（如纯非结构化文档环境）的适用性有待研究。
*   **路径质量依赖：** 子图提取依赖于外部 LLM 的选择能力，如果路径本身逻辑不通，会影响训练效果。
*   **任务类型：** 主要集中在事实性问答，对于开放式研究、主观评价或代码生成等非事实性搜索任务的迁移性尚未测试。

（完）

---
title: Logic-Regularized Verifier Elicits Reasoning from LLMs
title_zh: 逻辑正则化验证器激发大语言模型的推理能力
authors: "Xinyu Wang, Changzhi Sun, Lian Cheng, Yuanbin Wu, Dell Zhang, Xiaoling Wang, Xuelong Li"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05893v1"
tags: ["query:profile-3"]
score: 8.0
evidence: 受逻辑规则约束的无监督推理验证器
tldr: "针对大语言模型推理验证器依赖昂贵标注数据的问题，本文提出LOVER，一种受逻辑规则约束的无监督验证器。它将验证器视为二元隐变量，利用内部激活并引入否定、组内及组间一致性三种逻辑约束。LOVER无需标注即可应用于现有模型，在10个数据集上显著优于无监督基准，性能达到有监督验证器的95%。"
selection_source: fresh_fetch
motivation: 传统的推理验证器构建过程高度依赖昂贵的有监督数据，限制了其数据多样性和应用范围。
method: 提出LOVER框架，利用逻辑规则（否定、组内和组间一致性）作为先验，对大模型的推理路径进行无监督正则化。
result: "在10个推理数据集上，LOVER显著超越了无监督基准，并达到了有监督验证器平均95%的性能。"
conclusion: 通过逻辑规则约束，可以在无需标注数据的情况下有效提升大语言模型的推理验证能力。
---

## 摘要
验证器是增强现代大语言模型（LLM）推理能力的关键组件。典型的验证器需要资源密集型的监督数据集构建，这不仅成本高昂，且在数据多样性方面面临局限。在本文中，我们提出了 LOVER，一种由逻辑规则正则化的无监督验证器。LOVER 将验证器视为一个二元隐变量，利用内部激活并在多个推理路径上施加三个逻辑约束：否定一致性、组内一致性和组间一致性（按最终答案分组）。通过将逻辑规则作为先验引入，LOVER 可以利用未标记的样本，并直接与任何现成的大语言模型兼容。在 10 个数据集上的实验表明，LOVER 显著优于无监督基准，达到了与有监督验证器相当的性能（平均达到其 95% 的水平）。源代码可在 https://github.com/wangxinyufighting/llm-lover 公开获取。

## Abstract
Verifiers are crucial components for enhancing modern LLMs' reasoning capability. Typicalverifiers require resource-intensive superviseddataset construction, which is costly and faceslimitations in data diversity. In this paper, wepropose LOVER, an unsupervised verifier regularized by logical rules. LOVER treats theverifier as a binary latent variable, utilizinginternal activations and enforcing three logical constraints on multiple reasoning paths:negation consistency, intra-group consistency,and inter-group consistency (grouped by thefinal answer). By incorporating logical rulesas priors, LOVER can leverage unlabeled examples and is directly compatible with any offthe-shelf LLMs. Experiments on 10 datasetsdemonstrate that LOVER significantly outperforms unsupervised baselines, achieving performance comparable to the supervised verifier(reaching its 95% level on average). The sourcecode is publicly available at https://github.com/wangxinyufighting/llm-lover.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LOVER**（Logic-Regularized Verifier）的无监督验证器，旨在通过逻辑规则约束来挖掘大语言模型（LLM）内部的推理验证能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在大规模推理任务中，摆脱对昂贵且多样性受限的人类标注数据的依赖，构建高效的推理验证器？
*   **研究动机**：
    *   **标注成本高**：传统的有监督验证器（如 ORM 或 PRM）需要大量专家标注，尤其是过程监控（Process-based）标注极其耗时。
    *   **数据多样性不足**：人类标注者往往倾向于熟悉的解题路径，可能忽略模型生成的其他有效路径。
    *   **现有无监督方法局限**：如 CoT-Decoding 依赖启发式规则且对模型敏感；CCS 仅限于“是/否”问题，难以扩展到复杂的通用推理任务。

### 2. 方法论：LOVER 框架
LOVER 的核心思想是将验证过程建模为受逻辑规则约束的二元隐变量推理。
*   **对比断言生成**：对于模型生成的每个推理路径 $s_i$，通过模板拼接生成正负断言（如“这是一个正确的/错误的答案”），并提取 LLM 对应位置的内部隐藏层激活（Hidden States）作为输入。
*   **隐变量验证模型**：使用一个轻量级的 MLP（多层感知机）作为验证器，输出断言为真的概率 $p_\theta(z|x)$。
*   **三大逻辑约束（核心技术细节）**：
    1.  **否定一致性（Negation Consistency）**：同一路径的正负断言概率之和应趋于 1，且两者应有显著差异（排中律）。
    2.  **组内一致性（Intra-group Consistency）**：得出相同最终答案的不同推理路径，其正确性概率应当趋于一致。
    3.  **组间一致性（Inter-group Consistency）**：在所有不同的答案组中，应当有且仅有一个组是正确的（通过概率求和约束和熵正则化实现）。
*   **软概率目标**：为了使离散的逻辑规则可微，论文设计了相应的连续损失函数，支持端到端的无监督训练。

### 3. 实验设计
*   **数据集**：
    *   数学推理：GSM8K、iGSM（不同难度等级）。
    *   开放域知识推理：HotpotQA（仅保留 Yes/No 类）、MMLU-Pro。
    *   OOD（分布外）泛化：BIG-Bench Hard 中的 6 个子集（如布尔表达式、谎言之网等）。
*   **基准模型（Backbones）**：Mistral-7B、Llama-3.1-8B、Qwen-2.5 全系列（0.5B 到 32B）。
*   **对比方法**：
    *   Greedy（贪婪解码）
    *   Majority Voting（多数投票）
    *   CoT-Decoding（基于 Token 概率的启发式方法）
    *   Supervised（有监督训练的 LOVER，作为性能上限）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型细节**：提到验证器是一个具有 2 个隐藏层的 MLP，使用 ReLU 激活函数，采用 AdamW 优化器，学习率为 $1 \times 10^{-5}$。由于仅训练微型 MLP 且不微调 LLM 参数，预计算力需求极低。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同类型的数据集上进行了测试，涵盖了从 0.5B 到 32B 不同规模的模型。
*   **消融实验**：详细分析了三种逻辑约束各自的贡献、不同解码策略（自然解码、温度采样、束搜索）的影响、不同隐藏层位置的影响，以及解题路径数量 $N$ 的影响。
*   **充分性评价**：实验设计非常全面，通过跨模型、跨任务、跨难度的对比，客观地证明了方法的鲁棒性和有效性。

### 6. 主要结论与发现
*   **性能卓越**：LOVER 在所有测试场景下均显著优于无监督基准，平均达到了有监督验证器 **95%** 的性能水平。
*   **超越投票法**：相比单纯依赖答案频率的多数投票法，LOVER 平均提升了 **3.3%** 的绝对准确率，证明了利用 LLM 内部知识的价值。
*   **模型扩展性**：LOVER 能让 7B 规模的模型在推理表现上媲美 32B 的模型。
*   **强泛化性**：在未见过的 OOD 任务上，LOVER 依然保持了领先优势，证明其学习的是通用的验证逻辑而非简单的领域特征。

### 7. 优点与亮点
*   **完全无监督**：无需任何人工标注，直接利用逻辑先验和模型内生知识。
*   **通用性强**：不限于“是/否”问题，可处理任何有确定答案的推理任务。
*   **即插即用**：兼容任何开源（白盒）LLM，且推理开销极小。
*   **逻辑驱动**：将离散逻辑与连续神经网络结合，提供了一种原则性的无监督学习框架。

### 8. 不足与局限
*   **白盒依赖**：LOVER 必须访问 LLM 的隐藏层激活，因此**无法直接应用于黑盒 API 模型**（如 GPT-4）。
*   **答案提取依赖**：虽然理论上可以处理非结构化输出，但目前的实验仍依赖于从响应中提取最终答案进行分组。
*   **性能差距**：尽管接近，但仍未完全达到有监督验证器的水平，在极高难度的推理任务中可能仍存在瓶颈。

（完）

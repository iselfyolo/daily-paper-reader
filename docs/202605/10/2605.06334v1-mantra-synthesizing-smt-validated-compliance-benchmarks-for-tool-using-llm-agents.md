---
title: "MANTRA: Synthesizing SMT-Validated Compliance Benchmarks for Tool-Using LLM Agents"
title_zh: MANTRA：为使用工具的大语言模型智能体合成经 SMT 验证的合规性基准
authors: "Ashwani Anand, Ivi Chatzi, Ritam Raha, Anne-Kathrin Schmuck"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06334v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: 为LLM智能体合成经SMT验证的合规性基准数据集
tldr: 针对工具调用型LLM智能体在遵循复杂规程手册时的合规性评估难题，本文提出MANTRA框架。该框架能从自然语言手册和工具模式中自动合成可机器检查的基准测试。通过结合符号世界模型与SMT求解器进行一致性验证，MANTRA实现了跨领域、长篇幅手册的自动化基准生成。实验构建了包含6个领域、285个任务的基准集，证明了其在约束执行强度和故障诊断方面的优越性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体合规性评估依赖人工构建或LLM评判，难以扩展且在处理复杂长流程手册时缺乏可靠性。
method: 提出MANTRA框架，通过生成符号世界模型和轨迹级合规性检查，并利用SMT求解进行一致性验证与自动修复。
result: 成功构建了涵盖6个领域、285个任务的基准测试，支持长达50页的手册，且合规性检查比现有基准更严谨。
conclusion: 结合自动化生成与形式化验证方法，为工具调用型智能体提供了可扩展且可靠的合规性评估方案。
---

## 摘要
使用工具的大语言模型（LLM）智能体正越来越多地部署在由严格程序手册规范其可靠行为的场景中。确保此类智能体遵守这些手册中的规则具有挑战性，因为手册通常是以自然语言为人类编写的，而智能体的行为则表现为工具调用的执行轨迹。现有的 LLM 智能体评估依赖于人工构建的基准或基于 LLM 的裁判，这些方法要么难以扩展，要么在处理复杂、长程的手册时缺乏可靠性。为了克服这些局限性，我们提出了 MANTRA，这是一个从自然语言手册和工具模式（tool schemas）中自动合成机器可检查合规性基准的框架。MANTRA 独立生成 (i) 捕获程序依赖关系的符号世界模型，以及 (ii) 针对给定任务的一组轨迹级合规性检查，并使用 SMT 求解验证它们的一致性。结构化的修复循环用于解决不一致性，仅在兜底情况下需要人工干预。重要的是，MANTRA 支持任意领域和长篇程序手册，并提供了一种可调节的任务复杂度概念，用于自动推导伴随合规性检查的挑战性任务。利用 MANTRA，我们以极少的人力投入构建了一个包含 6 个领域、285 个任务的新基准套件，手册长度可扩展至 50 多页。实验表明，与现有基准相比，合规性检查更加丰富，且具有更强的约束执行力。此外，检查的粒度可用于调试智能体的失败模式。这些结果表明，将自动基准生成与具有形式化基础的验证方法相结合，能够实现对使用工具的智能体进行可扩展且可靠的基准测试。

## Abstract
Tool-using large language model (LLM) agents are increasingly deployed in settings where their reliable behavior is governed by strict procedural manuals. Ensuring that such agents comply with the rules from these manuals is challenging, as they are typically written for humans in natural language while agent behavior manifests as an execution trace of tool calls. Existing evaluations of LLM agents rely on manually constructed benchmarks or LLM-based judges, which either do not scale or lack reliability for complex, long-horizon manuals. To overcome these limitations, we present MANTRA, a framework for automatically synthesizing machine-checkable compliance benchmarks from natural-language manuals and tool schemas. MANTRA independently generates (i) a symbolic world model capturing procedural dependencies, and (ii) a set of trace-level compliance checks for a given task, and validates their consistency using SMT solving. A structured repair loop resolves inconsistencies, requiring human intervention only as a fallback. %This yields benchmarks that are formally validated. Importantly, MANTRA supports arbitrary domains and long procedural manuals, and provides a tunable notion of task complexity which is utilized to automatically derive challenging tasks accompanying compliance checks. Using MANTRA, we build a new benchmark suite with 285 tasks across 6 domains scaling to 50+ page manuals with minimal human effort. Empirically, we show that the compliance checks are richer with stronger constraint enforcement compared to existing benchmarks. Additionally, the granularity of the checks can be used for debugging the agents' failure modes. These results demonstrate that combining automated benchmark generation with formally grounded validation methods enables scalable and reliable benchmarking of tool-using agents.

---

## 论文详细总结（自动生成）

### 论文总结：MANTRA — 为使用工具的 LLM 智能体合成经 SMT 验证的合规性基准

#### 1. 核心问题与研究动机
随着 LLM 智能体在企业运营、医疗和科学发现等领域的广泛应用，其行为必须遵循严格的程序手册（如采购政策、安全规程）。然而，评估智能体是否“合规”面临两大挑战：
*   **语义鸿沟**：手册是用自然语言为人类编写的，而智能体行为表现为工具调用的执行轨迹（Trace），两者难以直接匹配。
*   **现有评估局限**：人工构建基准成本极高且难以扩展；基于 LLM 的裁判（LLM-as-a-judge）在处理长轨迹、复杂时间约束和前置条件时缺乏可靠性和确定性。
**研究动机**：开发一种能够自动从长篇自然语言手册中提取规则，并生成可机器自动检查、具有形式化验证保障的合规性基准框架。

#### 2. 方法论：MANTRA 框架
MANTRA（MANual-to-Test-tRAnslation）的核心思想是通过“双路径生成+形式化交叉验证”来确保基准的质量。
*   **文档摄取与场景生成**：
    *   将手册构建为**依赖图（Dependence Graph）**，通过采样图中的子图来控制任务复杂度。
    *   利用 LLM 根据采样内容生成具体的**场景（Scenario）**，包括用户提示词和初始数据库状态。
*   **双路径生成**：
    *   **路径 A（合规性检查 C）**：LLM 生成一组轨迹级布尔检查（如 `CALL`、`NO-CALL`、`PRECEDES` 等）。
    *   **路径 B（世界模型 WS）**：LLM 独立生成一个符号化的世界模型（使用特定的 DSL 语言），描述状态转移和前置条件。
*   **SMT 交叉验证与修复循环**：
    *   将 C 和 WS 编译为 **SMT（可满足性模理论）** 公式。
    *   使用 **Z3 求解器** 寻找冲突：例如，寻找一个满足所有检查 C 但违反世界模型 WS 的轨迹（反例）。
    *   **自动修复**：若发现冲突，LLM 根据反例反馈修复检查项或世界模型，直到两者达成逻辑一致。

#### 3. 实验设计
*   **实验领域**：涵盖 6 个领域，包括企业运营（Operations）、游戏（Sneaky-Sasquatch）、航空安全（Cabin-Safety）以及从 $\tau$-bench 适配的零售、航空和电信领域。
*   **基准规模**：共生成 285 个经过验证的测试任务。手册长度从 1,000 字到 16,000 字不等，工具数量从 13 个到 148 个不等。
*   **对比对象**：评估了 6 种主流 LLM 模型（如 Qwen3.6:36B、GPT-5.4-mini、Claude Haiku 4.5 等，注：文中使用了 2026 年背景下的模型代号）。
*   **评估指标**：Pass@1、Pass@5 以及更严格的可靠性指标 Pass^5。

#### 4. 资源与算力
*   **硬件环境**：使用了 2 台 3.2 GHz Intel Xeon Gold 6134M CPU，2 块 **Nvidia Tesla V100 (32GB)** GPU，以及 768GB RAM。
*   **软件工具**：使用 OpenAI GPT-5.4（代号）作为生成引擎，Z3 作为 SMT 求解器。
*   **训练/生成时长**：文中未明确给出总耗时，但提到 SMT 求解时间随轨迹长度 $h$ 显著增加，实验中设定 $h=16$ 以平衡效率。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个领域进行了 285 个任务的生成，每个任务对 6 个模型分别进行了 5 次独立运行，总计产生了数万条执行轨迹。
*   **充分性**：实验覆盖了从简单到极复杂的长篇手册（50+页），并进行了详细的失败模式分析（如 Missing-Required-Call 等）。
*   **客观性**：通过 SMT 求解器进行确定性评分，排除了 LLM 裁判的主观偏差，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **智能体普遍合规性不足**：即使是顶尖模型，在复杂规程下的合规率也并不理想。
*   **典型失败模式**：76% 的失败源于“缺失必要调用”或“缺失锚点调用”。具体表现为智能体倾向于在未执行“读取/检查”类工具的情况下直接执行“写入/提交”类工具（即违反了前置条件）。
*   **模型差异**：Qwen3.6:36B 在多个领域表现最优，其核心优势在于养成了良好的“先读后写”习惯。
*   **框架有效性**：MANTRA 能够自动发现并修复 LLM 生成基准时的逻辑漏洞，显著降低了人工参与度。

#### 7. 优点
*   **高自动化与可扩展性**：能够处理长达万字的非结构化手册，且扩展到新领域仅需手册和工具模式。
*   **确定性评估**：一旦基准生成并验证通过，评估过程不再需要 LLM 参与，结果完全可复现且无方差。
*   **形式化保障**：引入 SMT 验证，确保了合规性检查项与世界模型之间的一致性，提高了基准的严谨性。

#### 8. 不足与局限
*   **轨迹长度限制**：受限于 SMT 求解效率，验证的轨迹步数有限（$h=16$），可能遗漏极长程的违规行为。
*   **语言限制**：目前仅在英文手册上进行了验证。
*   **依赖工具模式**：需要预先定义好的工具 Schema，对于完全开放域的任务支持有限。
*   **残留人工环节**：虽然大幅减少了人力，但在处理极度模糊的文档语义时，仍可能需要人工介入作为最后防线。

（完）

---
title: The Scaling Properties of Implicit Deductive Reasoning in Transformers
title_zh: Transformer 中隐式演绎推理的缩放特性
authors: "Enrico Vompa, Tanel Tammet"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04330v1"
tags: ["query:profile-3"]
score: 7.0
evidence: Horn子句上的隐式演绎推理
tldr: 本研究探讨了深度受限的 Transformer 在 Horn 子句上的隐式演绎推理扩展属性。通过消除虚假特征并增强算法对齐，研究发现具有双向前缀掩码的深度模型在多种图拓扑和问题宽度上，其隐式推理能力可接近显式的思维链（CoT）性能。然而，在面对深度外推任务时，CoT 依然展现出不可替代的优势。该研究为理解大模型的内在推理机制提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在探究 Transformer 模型在处理 Horn 子句演绎推理时的隐式推理能力及其随规模变化的属性。
method: 通过系统性地消除虚假特征关联并强化算法对齐，在具有双向前缀掩码的深度 Transformer 模型中进行实验对比。
result: 在足够深的模型中，隐式推理在多种图拓扑和问题宽度下的表现接近显式思维链（CoT）的水平。
conclusion: 虽然隐式推理能力随深度提升显著，但在处理超出训练深度的外推任务时，显式思维链仍然是必要的。
---

## 摘要
我们研究了深度受限 Transformer 中基于 Horn 子句的隐式演绎推理的缩放特性。通过系统地将可证明性与伪特征去相关并强化算法对齐，我们发现，在具有双向前缀掩码且足够深的模型中，隐式推理在各种图拓扑和问题宽度上的表现均接近显式思维链（CoT），尽管 CoT 对于深度外推仍然是必要的。

## Abstract
We investigate the scaling properties of implicit deductive reasoning over Horn clauses in depth-bounded Transformers. By systematically decorrelating provability from spurious features and enforcing algorithmic alignment, we find that in sufficiently deep models with a bidirectional prefix mask, implicit reasoning approaches explicit CoT performance across graph topologies and problem widths, though CoT remains necessary for depth extrapolation.
---
title: "GLiNER2-PII: A Multilingual Model for Personally Identifiable Information Extraction"
title_zh: GLiNER2-PII：一种用于个人身份信息提取的多语言模型
authors: "Urchade Zaratiana, Ash Lewis, George Hurn-Maloney"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09973v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 使用约束驱动生成构建多语言合成语料库
tldr: 针对现代数据处理中个人身份信息（PII）识别的挑战，本文提出了GLiNER2-PII，一个仅有0.3B参数的多语言轻量级模型。该模型能识别42类PII实体，通过构建包含4910条文本的多语言合成语料库解决了真实数据稀缺和隐私风险问题。在SPY基准测试中，其性能优于OpenAI隐私过滤器等系统，并已开源。
selection_source: fresh_fetch
motivation: 现有的PII识别面临实体类型多样、语境敏感及真实标注数据稀缺且存在隐私风险等难题。
method: 基于GLiNER2架构开发了0.3B参数的模型，并利用约束驱动生成管线构建了涵盖多语言、多领域的合成标注数据集。
result: 在SPY基准测试中，GLiNER2-PII在跨度级F1分数上超越了OpenAI Privacy Filter及其他GLiNER变体，取得最佳表现。
conclusion: 该研究证明了小规模模型结合高质量合成数据在多语言PII检测任务中的有效性，并为开源隐私保护提供了有力工具。
---

## 摘要
个人身份信息（PII）的可靠检测在现代数据处理系统中变得日益重要，但该任务仍然具有挑战性：PII 跨度具有异构性、地域依赖性、上下文敏感性，且通常嵌入在噪声较多或半结构化的文档中。我们提出了 GLiNER2-PII，这是一个基于 GLiNER2 改进的小型 0.3B 参数模型，旨在以字符跨度分辨率识别包含 42 种 PII 实体类型的广泛分类。然而，训练此类系统受到可共享标注数据稀缺以及大规模收集真实 PII 相关隐私风险的限制。为了应对这一挑战，我们利用约束驱动的生成流水线构建了一个包含 4,910 篇标注文本的多语言合成语料库，该流水线能够生成跨语言、领域、格式和实体分布的多样化且真实的示例。在具有挑战性的 SPY 基准测试中，GLiNER2-PII 在包括 OpenAI Privacy Filter 和三个基于 GLiNER 的检测器在内的五个对比系统中，实现了最高的跨度级 F1 分数。我们在 Hugging Face 上公开释放了该模型，以支持开放式 PII 检测系统的进一步研究和实际部署。

## Abstract
Reliable detection of personally identifiable information (PII) is increasingly important across modern data-processing systems, yet the task remains difficult: PII spans are heterogeneous, locale-dependent, context-sensitive, and often embedded in noisy or semi-structured documents. We present GLiNER2-PII, a small 0.3B-parameter model adapted from GLiNER2 and designed to recognize a broad taxonomy of 42 PII entity types at character-span resolution. Training such systems, however, is constrained by the scarcity of shareable annotated data and the privacy risks associated with collecting real PII at scale. To address this challenge, we construct a multilingual synthetic corpus of 4,910 annotated texts using a constraint-driven generation pipeline that produces diverse, realistic examples across languages, domains, formats, and entity distributions. On the challenging SPY benchmark, GLiNER2-PII achieves the highest span-level F1 among five compared systems, including OpenAI Privacy Filter and three GLiNER-based detectors. We publicly release the model on Hugging Face to support further research and practical deployment of open PII detection systems.

---

## 论文详细总结（自动生成）

以下是对论文《GLiNER2-PII: A Multilingual Model for Personally Identifiable Information Extraction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保护隐私的前提下，构建一个能够高效、精准识别多种语言及复杂场景中个人身份信息（PII）的检测系统。
*   **研究背景**：
    *   **合规需求**：GDPR和CCPA等法规要求在数据处理中必须识别并脱敏敏感信息。
    *   **技术难点**：PII格式随地域变化（如电话、税号），且常嵌入在噪声多、半结构化的文档（如日志、邮件）中。
    *   **数据困境**：真实PII数据极度稀缺且难以共享，导致训练高性能模型面临严重的“隐私悖论”。

### 2. 论文提出的方法论
*   **核心思想**：基于 **GLiNER2** 架构（0.3B参数的小型模型），采用**模式驱动（Schema-driven）**的实体提取方法，通过大规模高质量的**合成数据**进行微调。
*   **关键技术细节**：
    *   **模型架构**：继承了GLiNER2的标签条件化（Label-conditioned）特性，推理时将目标标签作为输入，允许同一模型支持不同的PII分类体系，无需重新训练。
    *   **细粒度分类**：定义了包含42种PII实体类型的分类法，涵盖个人身份、联系方式、政府证件、金融信息、数字身份、凭据和敏感日期七大类。
    *   **约束驱动的数据生成**：利用“Pioneer Agent”框架，通过大型前沿模型（Frontier Decoder）生成合成语料。
        *   **编程约束**：控制标签组合、实体数量和排除逻辑。
        *   **多样性约束**：控制文档类型（聊天记录、医疗记录、发票等）、地域、语调和语言（支持英、法、西、德、意、葡、荷等）。
    *   **嵌套实体支持**：模型支持识别重叠实体（如`full_name`内包含`first_name`），便于下游系统根据策略选择脱敏粒度。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：4,910条由LLM生成的、带约束的多语言合成标注文本。
    *   **测试集（Benchmark）**：采用 **SPY (Synthetic PII Yesterday)** 基准，包含法律咨询（Legal Questions）和医疗咨询（Medical Consultations）两个领域的真实感文本。
*   **对比方法**：
    1.  **OpenAI Privacy Filter**：基于Token分类的隐私过滤器。
    2.  **NVIDIA GLiNER PII**：NVIDIA优化的PII提取模型。
    3.  **urchade/gliner_multi_pii-v1**：早期的多语言GLiNER微调版。
    4.  **knowledgator/gliner-pii-base-v1.0**：另一种基于GLiNER的PII检测器。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如GPU型号、数量）及训练时长。
*   **模型规模**：强调了模型的轻量化，参数量仅为 **0.3B**，适合实际部署。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域（法律、医疗）的测试集上进行了跨度级（Span-level）的精确匹配评估。
*   **客观性与公平性**：
    *   为了公平对比，研究者对所有基准模型进行了**统一的标签映射**，确保预测结果与SPY基准的7类评估标签对齐。
    *   **避开数据泄露**：特意排除了常用的`ai4privacy`数据集，因为许多基准模型已在该数据集上训练过，使用SPY基准能更好地衡量模型的**分布外（OOD）泛化能力**。

### 6. 论文的主要结论与发现
*   **性能领先**：GLiNER2-PII 在 SPY 基准上取得了最高的平均 F1 分数（0.471），显著优于 OpenAI 和 NVIDIA 的同类系统。
*   **高召回率优势**：在脱敏场景中，漏检（False Negative）的代价极高。GLiNER2-PII 在法律和医疗领域的召回率分别达到 0.722 和 0.681，远超其他 GLiNER 变体。
*   **合成数据的有效性**：证明了通过精心设计的约束驱动合成数据，可以训练出在真实世界、未见领域（如法律、医疗）表现优异的模型。

### 7. 优点
*   **灵活性高**：支持 42 种细粒度标签，且由于其模式驱动的特性，用户可根据需求动态调整提取目标。
*   **轻量且开源**：0.3B 的参数量兼顾了性能与推理效率，并已在 Hugging Face 开源。
*   **多语言与嵌套支持**：能够处理复杂的跨语言文本和嵌套实体结构，适应实际业务中复杂的文档格式。

### 8. 不足与局限
*   **精确率（Precision）仍有提升空间**：模型存在过度预测（Over-prediction）倾向，有时会将普通名词或产品名误认为人名。
*   **实验覆盖面有限**：评估仅限于法律和医疗两个领域，且训练数据完全由合成数据组成，缺乏人工校验。
*   **地域偏差风险**：虽然支持多语言，但合成数据可能无法完全捕捉特定文化背景下极具地域特色的PII变体。

（完）

---
title: Predicting Disagreement with Human Raters in LLM-as-a-Judge Difficulty Assessment without Using Generation-Time Probability Signals
title_zh: 在不使用生成时概率信号的情况下预测 LLM-as-a-Judge 难度评估中与人类评分者的不一致
authors: Yo Ehara
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12422v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 自动生成教育材料和难度评估
tldr: 针对LLM在教育材料难度评估中与人类评分不一致的问题，本文提出一种无需生成时概率信号的预测方法。该方法利用难度等级的序数特性，通过ModernBERT等嵌入空间分析评分集的几何一致性，从而识别潜在的评分分歧。实验证明，该方法在CEFR等级评估任务中优于传统的基于概率的基准，能有效筛选出需人工复核的案例。
selection_source: fresh_fetch
motivation: 现有的LLM评分分歧预测方法依赖难以跨模型比较的生成概率信号，且LLM在难度评估上与人类存在显著差异。
method: 利用难度等级的序数属性，通过外部嵌入模型构建嵌入空间，并基于评分集的几何一致性来识别与人类评分不一致的候选案例。
result: 在GPT-OSS-120B和Qwen3-235B-A22B模型上的CEFR难度评估实验显示，该方法的预测AUC显著高于基于概率的基准方法。
conclusion: 该研究提供了一种不依赖模型内部概率的通用框架，能更准确地识别LLM评分中的潜在错误，优化了人机协作的难度评估流程。
---

## 摘要
使用大语言模型（LLMs）自动生成教育材料正变得越来越普遍，但为这些材料分配难度等级仍需要大量的人力。因此，LLM-as-a-Judge 引起了关注，但与人类评分者的不一致仍然是一个主要挑战。我们提出了一种方法，用于预测哪些 LLM 生成的难度评分可能与人类评分者不一致，以便将这些案例发送进行重新评分。与之前的方法不同，我们的方法不依赖于生成时的概率信号，这些信号必须在评分生成过程中收集，且通常难以在不同的 LLM 之间进行比较。相反，利用难度是一个序数尺度这一事实，我们使用一个独立的嵌入空间（如 ModernBERT），并根据评分集的几何一致性来识别不一致的候选案例。在使用 GPT-OSS-120B 和 Qwen3-235B-A22B 进行的基于 CEFR 的英语句子难度评估实验表明，所提出的方法在预测与人类评分者不一致方面的 AUC 高于基于概率的基准方法。

## Abstract
Automatic generation of educational materials using large language models (LLMs) is becoming increasingly common, but assigning difficulty levels to such materials still requires substantial human effort. LLM-as-a-Judge has therefore attracted attention, yet disagreement with human raters remains a major challenge. We propose a method for predicting which LLM-generated difficulty ratings are likely to disagree with human raters, so that such cases can be sent for re-rating. Unlike prior approaches, our method does not rely on generation-time probability signals, which must be collected during rating generation and are often difficult to compare across LLMs. Instead, exploiting the fact that difficulty is an ordinal scale, we use a separate embedding space, such as ModernBERT, and identify disagreement candidates based on the geometric consistency of the rating set. Experiments on English CEFR-based sentence difficulty assessment with GPT-OSS-120B and Qwen3-235B-A22B showed that the proposed method achieved higher AUC for predicting disagreement with human raters than probability-based baselines.

---

## 论文详细总结（自动生成）

这篇论文针对教育领域中“LLM作为裁判（LLM-as-a-Judge）”在难度评估任务中与人类评分不一致的问题，提出了一种创新的预测方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何准确预测 LLM 对教育材料（如英语句子）标注的难度等级何时会与人类专家产生分歧？
*   **研究背景**：
    *   LLM 自动生成教育内容已普及，但人工评估难度耗时耗力。
    *   虽然 LLM 可以代行评估职责，但其结果与人类的一致性（Alignment）仍存挑战。
    *   **现有缺陷**：传统方法依赖 LLM 生成时的“概率信号”（如 Log-probabilities），但这存在三大痛点：1) 必须在生成瞬间采集，事后无法补救；2) 不同模型间的概率尺度不可比；3) 闭源 API 往往不提供这些信号。

### 2. 方法论：几何一致性预测
*   **核心思想**：利用难度等级的**序数特性（Ordinal Scale）**。如果一个句子的难度标签在独立的嵌入空间（Embedding Space）中与其周围样本的难度分布不符合几何逻辑，则该标签极可能存在分歧。
*   **关键技术细节**：
    *   **独立嵌入空间**：使用与评分 LLM 完全无关的小型编码器模型（如 ModernBERT，参数量 < 1B）。
    *   **方向向量 $w$ 的计算**：
        *   **exactw**：计算所有难度级别对之间的平均差异向量，代表难度增加的总体几何方向。
        *   **most2**：仅计算 LLM 标注最频繁的两个难度等级（如 A2 和 B1）质心之间的差异向量。
    *   **一致性得分**：将每个句子的嵌入向量投影到方向向量 $w$ 上。如果投影位置与 LLM 给出的标签顺序严重违背（即几何不一致），则判定为“潜在分歧案例”。
*   **算法优势**：无需 LLM 内部信号，仅需文本和标签即可事后计算。

### 3. 实验设计
*   **数据集**：使用 **CEFR-SP 数据集**，包含 10,004 条英语句子，由两位专家按照 CEFR（A1-C2 六个等级）进行标注。
*   **评分 LLM（裁判）**：选用当时最先进的开源大模型 **GPT-OSS-120B** 和 **Qwen3-235B-A22B**。
*   **对比基准（Benchmark）**：
    *   **logprob label**：基于 LLM 生成标签时的 Token 对数概率（传统置信度方法）。
*   **评估指标**：ROC-AUC、PR-AUC 和 Precision@20（衡量识别分歧案例的准确性）。
*   **嵌入模型对比**：测试了包括 ModernBERT、E5、BGE、DeBERTa-v3 等 11 种不同的嵌入模型。

### 4. 资源与算力
*   **硬件环境**：论文提到这些 120B 和 235B 级别的模型运行在“2026年2月时单台 GPU 可承载”的环境下（注：论文日期标注为2026年，可能为预印本或特定会议设定）。
*   **模型规模**：评分模型巨大（120B-235B），但用于预测分歧的嵌入模型非常轻量（约 300M-700M 参数）。
*   **训练时长**：未明确说明具体训练时长，但由于该方法主要涉及推理和向量运算，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：针对两个顶级 LLM 裁判、两位不同的人类评分者（Rater A/B）分别进行了交叉实验。
*   **充分性**：
    *   对比了 11 种不同的嵌入模型，验证了方法的普适性。
    *   涵盖了多种评估指标（AUC 和 Top-N 精度）。
    *   **客观性**：实验结果显示，在所有组合中，所提方法（尤其是 `modernbert + most2` 组合）的 PR-AUC 均优于基于概率的基准方法，证明了其稳健性。

### 6. 主要结论与发现
*   **超越概率信号**：在不使用任何生成时内部信号的情况下，几何一致性方法在预测分歧方面表现更优。
*   **ModernBERT 表现突出**：在所有嵌入模型中，ModernBERT 配合 `most2` 策略达到了最高的预测精度。
*   **分布敏感性**：发现 `most2`（关注最频繁等级）比 `exactw`（关注全局等级）更有效，因为 LLM 的标注往往集中在少数几个等级上，局部几何特征更能反映分歧。

### 7. 优点
*   **实用性极强**：可作为“插件”应用于任何已有的 LLM 评分结果，无需重新运行昂贵的 LLM 生成过程。
*   **跨模型可比性**：由于使用统一的外部嵌入空间，不同 LLM 的评分质量可以在同一尺度下进行比较。
*   **解释性**：通过向量空间的投影，可以直观地看到哪些样本偏离了难度轴。

### 8. 不足与局限
*   **领域局限性**：目前仅在英语语言难度（CEFR）上验证。在 STEM（理科）领域，难度往往取决于逻辑步骤而非词汇语义，通用嵌入模型可能失效。
*   **依赖序数尺度**：该方法要求评估任务必须具有明确的先后顺序（如难度、质量等级），不适用于分类任务（如情感分析）。
*   **数据偏差风险**：如果 LLM 标注的等级分布极度不均（例如 99% 都是同一等级），则无法构建有效的几何方向向量。

（完）

Title: Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property

URL Source: https://arxiv.org/pdf/2605.05840v1

Published Time: Fri, 08 May 2026 01:07:43 GMT

Number of Pages: 28

Markdown Content:
# Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

# Neta Elad #

Tel Aviv University, Israel 

# Sharon Shoham #

Tel Aviv University, Israel 

Abstract 

Recently, symbolic structures were proposed as finite representations of potentially infinite first-order structures, where Linear Integer Arithmetic terms and formulas define the domain and interpretations of a structure. We generalize symbolic structures to use any base theory that admits a standard model. Symbolic structures induce a symbolic model property, which holds for a fragment of first-order logic if every satisfiable formula in the fragment has a symbolic model. The symbolic model property implies decidability, since the model-checking problem for symbolic structures is decidable. We use the symbolic model property to prove decidability for several fragments that extend the fragment of stratified formulas, relaxing the quantifier-alternation constraints by allowing one sort to have self-looping functions, under certain restrictions. To establish the symbolic model property for these fragments we construct a symbolic model for a formula from an arbitrary model. The construction and its correctness are proved in a generic fashion, which may be instantiated to other similarly restricted fragments. 

2012 ACM Subject Classification Theory of computation → Logic and verification 

Keywords and phrases first-order logic, decidability, symbolic structures 

Digital Object Identifier 10.4230/LIPIcs.LICS.2026.35 

Acknowledgements The research leading to these results has received funding from the European Re-search Council under the European Union’s Horizon 2020 research and innovation programme (grant agreement No [759102-SVIS]). This research was partially supported by Israel Science Foundation (ISF) grant No. 2117/23. 

# 1 Introduction 

In this paper we study a formalism for representing infinite models of formulas in first-order logic (FOL) and use this formalism to prove decidability results for new fragments of FOL. Our investigation is motivated by the use of FOL for verification. First-order logic offers an expressive language for specifying verification conditions, logical formulas that capture the correctness of a computer program or system, as well as means for automation, due to its complete proof system. In particular, we focus on the use of quantified FOL formulas for modeling systems with unbounded domains, where axiomatically-defined order relations are used to abstract important primitives that are not directly definable in FOL, e.g., reachability in linked lists or tree-shaped data-structures. FOL offers a natural way to express verification conditions of such systems, allowing the user to write specifications that match their mental model of the system, and verify the correctness of the system (see, e.g., [19, 26, 29, 36]). However, FOL is undecidable in general, and thus the verification process may fail. Completeness of FOL does guarantee that valid formulas admit finite proofs, but there are no such guarantees on counter-models of invalid formulas. An FOL-based verification tool may get stuck when trying to verify a system due to counter-models it is unable to find, leaving the user with no actionable feedback. This is particularly common during intermediate steps  

> ©Neta Elad and Sharon Shoham; licensed under Creative Commons License CC-BY 4.0 41st Annual Symposium on Logic in Computer Science (LICS 2026). Editors: Claudia Faggian and Joost-Pieter Katoen; Article No. 35; pp. 35:1–35:28 Leibniz International Proceedings in Informatics
> Schloss Dagstuhl – Leibniz-Zentrum für Informatik, Dagstuhl Publishing, Germany
> arXiv:2605.05840v1 [cs.LO] 7 May 2026

35:2 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

in the verification process, when the user still refines the system’s specifications and fixes bugs in the system. One approach to handle these issues is by limiting verification conditions to a fragment of FOL that enjoys a finite model property, which means that every satisfiable formula admits a finite model. The finite-model property implies decidability, and, importantly, ensures that whenever a counter-model exists, it may be found and presented to the user, providing much-desired feedback. One very successful fragment in this regard is the fragment of stratified formulas (SF) [ 30 ], the many-sorted variant of the Effectively PRopositional fragment (EPR) [ 33 , 23 ]. The finite model property of SF is guaranteed by requiring acyclicity of the quantifier-alternation graph of each formula, where vertices correspond to sorts and edges correspond to ∀∃ quantifier alternations (either explicit or implicit via function symbols). Though useful [ 30 , 27 , 25 ], the fragment of stratified formulas is rather limiting in its syntactic constraints, and may require nontrivial amount of work from the user to fit specifications of complex systems into the fragment [35]. In this paper, we pursue an alternative approach that goes beyond a finite model property. We explore effective representations of potentially infinite models, and use them to establish decidability results for fragments of FOL that do not admit a finite model property. We build on the notion of symbolic structures , introduced in [ 15 ] as finite representations of potentially infinite models. Symbolic structures use the standard model of the integers and the theory of Linear Integer Arithmetic (LIA) to represent first-order structures. In symbolic structures, the domain is given symbolically by finitely many nodes, each of which represents a potentially infinite set of integers, and interpretations of signature symbols are defined with terms and formulas in the theory of LIA. This is akin to first-order interpretations (over the standard model of the integers) with similarly-behaved elements explicitly grouped together into symbolic nodes. We start by generalizing the formalism of symbolic structures to other theories, producing a definition that is parametric by the base theory underlying the symbolic structure. Our generalization allows any base theory that admits a standard model (i.e., any theory of a structure). Given such a base theory, the nodes in our generalized symbolic structures represent sets of elements of the standard model of the theory, and interpretations are defined using terms and formulas in the language of the theory. This generalization matches how first-order interpretations may be defined over any base structure. A key property of symbolic structures is that they allow reducing model-checking, which determines whether a given symbolic structure satisfies a given first-order formula, to validity of some formula in the base theory. Thus, model-checking is decidable for symbolic structures over decidable base theories (which is unsurprising due to the connection between symbolic structures and first-order interpretations). Decidability of model-checking induces a procedure for providing counter-models to verification conditions by enumerating and checking symbolic structures. This procedure may not always halt, since undecidability of validity in FOL implies that some invalid formulas do not have symbolic counter-models. Our main contribution is proving decidability for new fragments of FOL via a symbolic 

model property, which states that every satisfiable formula has a symbolic model. Due to decidability of model-checking for symbolic structures, the symbolic model property trivially implies decidability. However, establishing the symbolic model property may be difficult and is the major problem this paper tackles. Interestingly, and in contrast to the fragment of stratified formulas, the fragments we consider do not enjoy a finite model property, and some formulas in them may only admit infinite models. Still, the symbolic model property ensures Neta Elad and Sharon Shoham 35:3 

that when verification fails, a symbolic counter-model can be produced. To define the decidable fragments, we introduce the Ordered Self-Cycle (OSC) family of fragments, where each fragment, OSC[ α], is parameterized by an axiomatization α of an order relation ≺ over some sort s∞. OSC[ α] extends the fragment of stratified formulas by relaxing the acyclicity requirement of the quantifier-alternation graph, allowing function symbols that introduce self-cycles over sort s∞ in the graph, under certain restrictions. The 

OSC family is a generalization of the decidable fragment presented in [ 15 ], which corresponds to OSC[ tot ], where tot axiomatizes a total order. Decidability of OSC[ tot ] is proved in [15] via a symbolic model property, using LIA as the base theory. In this paper, we establish a symbolic model property for additional fragments in the OSC family, using both LIA and a decidable theory of strings as base theories. Most notably, we use the base theory of strings to show the symbolic model property of the Prefix-Ordered Self-Cycle fragment, OSC[ pref ], where pref axiomatizes a prefix order, a common construct in verification that arises naturally when modeling reachability in acyclic tree-shaped data-structures or in acyclic linked lists with sharing. We further prove symbolic model properties for several variants of OSC[ tot ] and OSC[ pref ].Our proofs establish a symbolic model property through a generic proof recipe, which extends and simplifies the proof given in [ 15 ] for OSC[ tot ]. The proofs work by constructing a symbolic model for a formula from an arbitrary model of the formula. The construction is defined in several steps, some of which are general, while others depend on the specific order axiom α and base theory used. Finally, we note that we have implemented a proof-of-concept tool for exploring generic symbolic structures by extending the open-source FEST [ 15 ] Python library. Our implemen-tation generalizes FEST to support different base theories in a modular way, in particular enabling model-checking of symbolic structures over the base theory of strings. In summary, this paper offers the following contributions: (1) a general definition of symbolic structures, finite representations of potentially infinite models that can be viewed and acted upon (Section 4); (2) the introduction of the family of Ordered Self-Cycle fragments, which are parameterized by an order axiomatization (Section 5); and (3) decidability proofs for several OSC fragments, in particular the Prefix-Ordered Self-Cycle fragment OSC[ pref ],as well as variants of OSC[ pref ] and OSC[ tot ] (Section 6). The rest of the paper is organized as follows: related work is discussed in Section 2, preliminaries are given in Section 3, Section 7 gives a brief overview of the implementation of generic symbolic structures, and Section 8 concludes. We omit proofs of lesser salience and defer them to Appendix A. 

# 2 Related Work 

Finite model property. A long line of work has identified syntactic fragments of FOL for which satisfiability is shown to be decidable by establishing a finite model property. Classical results include the Ackermann, Gödel, Löb and Bernays-Schönfinkel-Ramsey fragments, as well as later refinements and extensions studied extensively in logic and finite model theory [ 2 , 16 , 24 , 18 , 28 , 9, 1 , 20 , 37 ]. Though some of these fragments admit specialized decision procedures, they all enjoy a finite model property. For a detailed survey, see [ 37 ]. Related to this line of work are decidability results for satisfiability over finite structures (e.g., [ 34 , 13 , 5]). All of these works are similar in spirit to our symbolic-model-property approach, but distinct as the fragments we consider do not admit a finite model property. 

Decidability and infinite models. The seminal works on monadic second-order logic (MSO) [ 10 , 11 , 32 ] prove decidability for fragments that do not admit a finite model property. 

L I C S 2 0 2 6 35:4 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

There, the specific structures of lines (S1S) and trees (S2S) are considered, and finite-state automata are used to represent infinite models. A complementary line of work studies the construction of infinite models via proof-theoretic methods [ 3, 31 ], using saturated sets of clauses to witness the existence of models. Using the symbolic model property to prove decidability of FOL fragments was introduced in [ 15 ], which this work generalizes to other fragments of FOL and symbolic structures over different base theories. 

Representing infinite structures. Symbolic structures can also be expressed as first-order interpretations (see, e.g., [ 17 ]) over the standard structure of the base theory, by indirectly encoding the nodes in the symbolic domain using the dimensionality of the FO interpretation. The different nodes in a symbolic structure provide a natural way to group and define similarly-behaved elements. An alternative approach to representing infinite structures is by using automatic structures [ 7, 21 , 4, 8], where automata and regular languages are used. However, these works do not provide a method to automatically find a satisfying structure for a given formula, nor identify a decidable fragment where representable satisfying structures are guaranteed to exist. 

# 3 Preliminaries 

First-order logic. We consider many-sorted first-order logic with equality. We use the usual definitions of a signature Σ, terms over signature Terms Σ, formulas LΣ, first-order structures and the satisfaction relation. For a set of variables X, we denote by Terms Σ(X)

and LΣ(X) the sets of terms and, respectively, formulas over Σ with free variables in X.In particular, we write Terms Σ(∅) for the set of ground terms and LΣ(∅) for the set of sentences (formulas without free variables) over Σ.

Theories of structures. In this paper we consider first-order theories of structures. Given a first-order structure M for signature Σ, the theory of M, denoted Th( M), is the set of all sentences satisfied by M, i.e., Th( M) = { φ ∈ L Σ(∅) | M | = φ }. For T = Th( M), we say that a sentence φ ∈ L Σ(∅) is valid in T (or T -valid), denoted |=T φ, if φ ∈ T , i.e., M | = φ.We say that a formula is satisfiable in T (or T -satisfiable) if its existential closure is T -valid. We say that T is decidable when there exists an algorithm that decides whether a sentence is 

T -valid. 

The theory of Linear Integer Arithmetic (LIA). The theory of LIA is the theory of the structure Mlia for the signature 

Σlia = { cz | z ∈ Z } ∪ { +( ·, ·)} ∪ { <(·, ·)}

over the single sort int . The domain of Mlia consists of the integers Z, and the interpretations of the constant symbols cz , function symbol + and relation symbol < are defined in the usual way. With abuse of notation, we sometimes write z for cz . Recall that the theory of LIA is decidable (see, e.g., [12]). 

A decidable theory of strings. Given a finite alphabet ∆, the language of regular expressions over ∆, denoted R∆, is defined by the following grammar: 

r ∈ R ∆ ::= ϵ ∣∣ δ ∈ ∆ ∣∣ (r1 · r2) ∣∣ (r1 | r2) ∣∣ (r∗).

We further denote by (r?) the regular expression (r | ϵ) and by (r+) the regular expression 

(r · (r∗)) . We consider the decidable theory of strings over ∆, denoted STR ∆, which is the theory of the structure Mstr  

> ∆

(also known as Sreg [6]) for the signature 

Σstr  

> ∆

= {cϵ} ∪ { ℓδ (·) | δ ∈ ∆ } ∪ { ⊏(·, ·)} ∪ { /r/( ·) | r ∈ R ∆ }Neta Elad and Sharon Shoham 35:5 

over the single sort string . The domain of Mstr  

> ∆

is the set of finite strings, also known as 

words , over ∆, the constant symbol cϵ is interpreted as the empty string ϵ, the unary function symbols ℓδ (“last”) are interpreted as appending the letter δ to a string, the binary relation symbol ⊏ is interpreted as the strict prefix relation over strings, and the unary relation symbols /r/ are interpreted as the languages (i.e., sets of words) of the regular expressions r.With abuse of notation we sometimes use ϵ as a constant symbol and t · δ (for δ ∈ ∆) as shorthand for ℓδ (t). As shown in [6], the theory STR ∆ is decidable for any finite ∆.

# 4 Symbolic Structures 

In this section we introduce a generalization of the symbolic structures formalism [ 15 ]. Throughout the section we fix a theory T = Th (MT ) of a structure MT = (DT , IT ) for a single-sorted signature ΣT , and define symbolic structures over T . We use the shorthand 

LT and Terms T for LΣT and Terms ΣT respectively. Given a signature Σ (not to be confused with ΣT ), symbolic structures for Σ are finite representations of (explicit) first-order structures for Σ, where the domain of each sort is stitched up piecewise from subsets of the domain of the standard model MT of the underlying base theory T ; each such subset is summarized by a node in the symbolic structure. Interpretations of constant, function and relation symbols from Σ are given symbolically in symbolic structures, using terms and formulas in the language of ΣT .The symbolic representation allows to check if the structure represented by a symbolic structure satisfies a formula φ over Σ by essentially substituting symbols in Σ appearing in 

φ by their symbolic definitions, transforming φ into a formula over ΣT that is T -valid iff the explicit structure satisfies φ. Formally, symbolic structures and their explications are defined as follows. 

▶ Definition 4.1 (Symbolic structure) . A symbolic structure over base theory T for a first-order signature Σ with sorts S is a triple S = (DS , BS , IS ), defined as follows. 

# • DS is a function from S to finite, non-empty sets of nodes , where DS (s1) ∩ D S (s2) = ∅

for every s1̸ = s2 ∈ S . We refer to ⋃ 

> s∈S

DS (s) as the symbolic domain of S.

# • BS is a function from the symbolic domain to T -satisfiable formulas in LT ({x}) (i.e., formulas over ΣT with at most one free variable x). For each node n in the symbolic domain, BS (n) is called the bound formula of n.

# • IS is a function that maps each symbol s ∈ Σ to its symbolic interpretation IS (s), also denoted as sS .The symbolic interpretation of a constant symbol c : s is a pair cS = ⟨n, t ⟩ ∈ D S (s) ×

Terms T (∅) such that 

|=T B(n)[ x 7 → t]. (1) 

The symbolic interpretation of a function symbol f : s1 × · · · × sm → s is a function 

f S : DS (s1) × · · · × D S (sm) → D S (s) × Terms T ({x1, . . . , x m}) such that whenever 

f S (n1, . . . , n m) = ⟨n, t ⟩,

|=T ∀x1, . . . , x m.



> m

∧

> j=1

B(nj )[ x 7 → xj ]

 → B (n)[ x 7 → t]. (2) 

The symbolic interpretation of a relation symbol R : s1×· · ·× sm is a function RS : DS (s1)×· · · × D S (sm) → L T ({x1, . . . , x m}).

L I C S 2 0 2 6 35:6 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

We call conditions (1) and (2) the well-definedness conditions of S.

A symbolic structure S is a finite representation of an explicit structure E(S). Next we define the explicit structure represented by a symbolic structure. 

▶ Definition 4.2 (Explication) . Let S = (DS , BS , IS ) be a symbolic structure over base theory 

T for a first-order signature Σ with sorts S. The explication of S, denoted E(S) is the first-order structure E(S) = (DE(S), IE(S)) for Σ, defined as follows. 

# • The explicit domains DE(S) of sorts are given by explicating each node in the domain of a sort into a set of explicit elements from the domain of MT and taking the disjoint union of these sets. For a symbolic node n we denote its explicit elements by 

E(n) ≜ { ⟨n, d ⟩ ∣∣ d ∈ D T and MT , [x 7 → d] |= B(n) },

and we define the explicit domain as 

DE(S)(s) ≜ ⋃ 

> n∈D S(s)

E(n)

# • The explicit interpretations IE(S) of symbols are given by explicating the symbolic inter-pretations according to MT .The interpretation of a constant symbol c : s ∈ Σ where cS = ⟨n, t ⟩ is the explicit element 

cE(S) ≜

〈

n, t MT 〉

,

where tMT

is defined recursively in the usual way. The interpretation of a function symbol f : s1 × · · · × sm → s ∈ Σ is the function 

f E(S) : DE(S)(s1) × · · · × D E(S)(sm) → D E(S)(s), defined such that if f S (n1, . . . , n m) = 

⟨n, t ⟩ then 

f E(S)(⟨n1, d 1⟩, . . . , ⟨nm, d m⟩) ≜

〈

n, t MT  

> [x17→d1,...,x m7→dm]

〉

.

The interpretation of a relation symbol R : s1 × · · · × sm ∈ Σ is the relation 

RE(S) ≜



(⟨n1, d 1⟩, . . . , ⟨nm, d m⟩)

∣∣∣∣∣∣∣∣∣∣

⟨n1, d 1⟩ ∈ E (n1),. . . , 

⟨nm, d m⟩ ∈ E (nm), and 

MT , [x1 7 → d1, . . . , x m 7 → dm]

|= RS (n1, . . . , n m)



▶ Example 4.3. Consider the infinite first-order structure depicted in Figure 1a over the single-sorted signature Σ = {R(·, ·)} with sort s and a binary relation R, where we have infinitely many elements ordered in a prefix order, forming a full binary tree that has an infinite tail “after” one of its branches. The figure depicts the transitive reduction of the binary relation R by arrows. Figure 1b depicts a symbolic structure S over the base theory of STR {1,2} for Σ, whose explication is the structure from Figure 1a, where the a-elements are grouped together into one node and the b-elements are grouped into a second node. Accordingly, the symbolic structure has two nodes in its symbolic domain, DS (s) = {a, b }, where a represents an Neta Elad and Sharon Shoham 35:7           

> ⟨a, ϵ ⟩
> ⟨a, 1⟩
> ⟨a, 2⟩
> ⟨a, 11 ⟩
> ⟨b, ϵ ⟩⟨b, 1⟩
> ⟨a, 12 ⟩
> ⟨a, 21 ⟩
> ⟨a, 22 ⟩
> (a) The explicit first-order structure, where circles represent elements, and arrows depict the transitive reduction of the
> Rorder relation.
> /(1|2) ∗/(x)

a /1∗/(x)

b               

> x1⊏x2x1⊏x2
> /1∗/(x1)
> ⊥
> (b) The symbolic structure Swhere nodes are shown as boxes, bound formulas are written inside the boxes, and the interpretation of
> Rbetween two nodes n, m is shown with an arrow n99K m, labeled by the formula
> RS(n, m ).

Figure 1 An example of an infinite first-order structure for the signature {R(·, ·)}, presented explicitly and symbolically. The structure contains infinitely many elements, ordered in a prefix order encoded by R. The elements are of two “kinds”: a-elements and b-elements. The a-elements are ordered in a full, infinite binary tree, represented by binary strings over alphabet 1, 2, whereas the b-elements form an infinite tail for the “top” branch of 1-strings of the a-binary tree. 

infinite set of binary words, BS (a) = /(1|2) ∗/, and b represents an infinite set of unary words, BS (b) = /1∗/. The relation symbol R is defined symbolically as: 

RS (a, a ) ≜ x1 ⊏ x2, RS (b, a ) ≜ ⊥, RS (a, b ) ≜ /1∗/(x1), RS (b, b ) ≜ x1 ⊏ x2,

expressing that for any two a-elements or b-elements, their order (according to R) matches the natural prefix order on their respective words, that no b-element comes before an a-element, and that all b-elements come after a-elements with words matching the /1∗/ language. 

▶ Theorem 4.4 (Model-checking for symbolic structures) . Given a symbolic structure S

over base theory T for signature Σ and sentence φ ∈ L Σ(∅), there exists a computable sentence φS ∈ L T (∅) (given by the model-checking transformation described in [ 15 ]) such that E(S) |= φ ⇐⇒ | =T φS .

▶ Remark 4.5. For symbolic structures that only use quantifier-free formulas in bound formulas and interpretations, the model-checking transformation preserves the quantification structure of the original formula. Thus, for a base theory T that is only decidable for the quantifier-free fragment of ΣT model-checking would still be decidable, but only for the quantifier-free fragment of FOL. In the sequel, we focus on symbolic structures over decidable base theories, which ensures that model-checking is decidable. 

# 5 The Ordered Self-Cycle (OSC) Family of Fragments 

In this section we define the OSC family of fragments and state our decidability results. We start with the definition of the OSC[ α] fragments, which are parameterized by a sentence α

that axiomatizes an order relation. We then present the various kinds of orders considered in this paper. Finally we define the simplified OSC ⋆[α] fragments, which are used in our decidability proofs. 

L I C S 2 0 2 6 35:8 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

# 5.1 Fragments Definition 

The Ordered Self-Cycle (OSC) family of first-order logical fragments is a set of extensions of the fragment of stratified formulas (SF) [ 30 ]. We start by recalling the properties of SF. The SF fragment is the many-sorted variant of the Effectively PRopositional fragment (EPR) [ 33 ]. SF establishes a finite model property by requiring the quantifier-alternation graph of each formula in SF to be acyclic. The vertices of the quantifier-alternation graph correspond to sorts in a formula, and edges correspond to ∀∃ quantifier alternations, either explicit in the formula or implicitly given by function symbols. In SF, the finite model property stems from the fact that after Skolemization, there are only finitely many ground terms, which allows complete instantiation of the universal Skolemized formula, as in EPR. The OSC family of fragments relaxes the quantifier-alternation restrictions of SF for a single sort, s∞, equipped with some axiomatically defined strict order relation, ≺ : s∞ × s∞.We denote by strict the axiomatization of ≺ as a strict order relation, given by the conjunction of the following formulas: 

∀x. ¬(x ≺ x) (irreflexivity) 

∀x, y, z. (x ≺ y ∧ y ≺ z) → x ≺ z (transitivity) Formally, the OSC family is defined as follows. 

▶ Definition 5.1 (OSC) . The OSC family of fragments considers first-order signatures that include a distinguished sort s∞ and a binary relation symbol ≺ : s∞ × s∞. Given such a signature Σ and a sentence α such that α |= strict and α is a single-sorted formula over the s∞ sort, the OSC[ α] fragment consists of sentences of the form α ∧ φ, where: 

1. The formula φ uses a single variable of sort s∞.

2. Function and relation symbols other than ≺ used in φ have at most one argument of sort 

s∞.

3. In the quantifier-alternation graph of φ, the only cycles are self-loops at s∞, and the only outgoing edges from s∞ are to s∞, resulting from function symbols whose range is s∞.

4. All nested function-application terms of sort s∞ appearing in φ are ground. 

# 5.2 Decidability Results for Fragments of OSC 

In this section we give an overview of the OSC fragments for which we prove decidability. First, we consider a strict total order, denoted tot , axiomatized as the conjunction of the following formulas: 

∀x. ¬(x ≺ x) (irreflexivity) 

∀x, y, z. (x ≺ y ∧ y ≺ z) → x ≺ z (transitivity) 

∀x, y.x = y ∨ x ≺ y ∨ y ≺ x (totality) Next, we consider a strict prefix order, denoted pref , which is axiomatized as the conjunction of the following: 

∀x. ¬(x ≺ x) (irreflexivity) 

∀x, y, z. (x ≺ y ∧ y ≺ z) → x ≺ z (transitivity) 

∀x, y, z. (x ≺ z ∧ y ≺ z) → (x = y ∨ x ≺ y ∨ y ≺ x) (downards totality) Neta Elad and Sharon Shoham 35:9                          

> action send ( n : node ,v : value ):
> assume n== root or msg (n , v ); sent (n , v ) := true ;msg ( left ( n ) , v ) := true ;msg ( right ( n ) , v ) := true ;
> Figure 2 Simple message broadcast protocol for a network of nodes with a binary-tree topology.

Finally, we consider two variants of each of the above orders. The “progressive-successor” variant, denoted prosucc , where all functions are progressive and there exists a successor to every element, and the “regressive-predecessor” variant, denoted regpred , where all functions are regressive and there exists a predecessor to every element. We consider every combination of each of the tot and pref axioms, together with each of the prosucc and 

regpred variants. Formally, we define prosucc to be the conjunction of 

∧ 

> f∈Σ

∀x.x ≺ f (x) (progressivity) 

∀x. ∃y.x ≺ y ∧ ∀ z.x ≺ z → (z = y ∨ y ≺ z), (successor existence) and regpred to be the conjunction of 

∧ 

> f∈Σ

∀x.f (x) ≺ x (regressivity) 

∀x. ∃y.y ≺ x ∧ ∀ z.z ≺ x → (z = y ∨ z ≺ y). (predecessor existence) 

▶ Example 5.2. For an example of the applicability of OSC [pref ] let us consider a distributed message broadcast protocol, in a network with a topology of a binary tree. The network has a root node and each node has two children. Each node maintains a set of values it received by messages. The root node may send any value to its children, while any other node may only send values it already received, as expressed in Figure 2. We encode the topology of the network by a prefix order ≺, axiomatized by pref , having root be minimal according to pref ,and the functions left and right , encoding the children of a node respect the order. We further encode the send action by a formula over two copies of the signature that specifies how the  

> sent

and msg relations are modified by the action. We wish to prove the safety property that for any node n that received value v, there exists an ancestor n′ (according to the order in tree topology) that sent that value: ∀n : node , v : value .∃n′ : node .msg (n, v ) → n′ ≺ n ∧ sent (n′, v ).This property, along with the encoding of the the protocol is in OSC [pref ], but not in the more restrictive fragment of SF. 

▶ Theorem 5.3. The OSC[ α] fragment is decidable for α ∈ { tot , tot ∧ prosucc , tot ∧

regpred , pref , pref ∧ prosucc , pref ∧ regpred }.

The result for OSC[ tot ] is a restatement of [15], while the other 5 results are new. 

# 5.3 The Simplified OSC ⋆ Family of Fragments 

In this section we define a simplified family of fragments, denoted OSC ⋆, such that satisfiability in OSC is reducible to satisfiability in OSC ⋆, a property that our decidability proofs for fragments of OSC build on. Roughly, formulas in OSC ⋆ are single-sorted and only contain simple c, x, f (x), g (x) terms. 

L I C S 2 0 2 6 35:10 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

▶ Definition 5.4 (OSC ⋆). The OSC ⋆ family of fragments considers single-sorted first-order signatures with a binary relation symbol ≺(·, ·), where all other function and relation symbols are unary. The single sort is denoted s∞. Given such a signature Σ and a sentence α over Σ

such that α |= strict , the OSC ⋆[α] fragment consists of sentences of the form α ∧ φ, where: 

1. φ is a positive Boolean combination of formulas of the form ∀x.ϕ (x), where ϕ is quantifier-free; and 

2. all terms appearing in ϕ are of the form c (for some constant symbol c ∈ Σ), x or f (x)

(for some unary f ∈ Σ). 

Note that Σ may include multiple function symbols, all of which are unary and cause self-loops. 

▶ Theorem 5.5. There exists an algorithm that translates every formula α ∧ φ ∈ OSC[ α] to some formula α ∧ φ⋆ ∈ OSC ⋆[α], such that there exists a model M |= α ∧ φ iff there exists a model M ⋆ |= α ∧ φ⋆. Moreover, M is computable from M ⋆.

The model-preserving translation leverages key properties of OSC. First, the restrictions of OSC guarantee that after Skolemization, the signature of OSC formulas can generate only finitely many ground terms for sorts other than s∞, similarly to SF. This fact allows us to Skolemize and then “instantiate” formulas in OSC with all ground terms of non-s∞ sorts, and arrive at an equi-satisfiable universal formula where the only variables are of sort s∞.Next, we replace applications of function and relation symbols by fresh unary symbols for each tuple of non-s∞ ground terms appearing in them, considering all possible equalities between ground terms. Finally, we perform a sort of flattening of nested ground terms of 

s∞, iteratively replacing each f (t) term with some fresh constant c∗ and adding the axiom 

∀x.x = t → f (x) = c∗ to the formula. 

▶ Corollary 5.6. If OSC ⋆[α] is decidable, then so is OSC[ α].

▶ Note 5.7. Since signatures in OSC ⋆ are single-sorted, we refer to the domains DM of explicit structures and the domains DS of symbolic structures as sets of elements, respectively nodes, i.e., with abuse of notation, DM = DM (s∞) and DS = DS (s∞).

# 6 Decidability via a Symbolic Model Property 

In this section we prove the various decidability results of this paper (Theorem 5.3), following and generalizing the proof method of [ 15 ]. We use symbolic structures over decidable base theories as means to prove decidability for several fragments of the OSC ⋆ family: we show that every satisfiable formula in these fragments admits a satisfying symbolic structure over a decidable base theory. Since model-checking of such symbolic structures is decidable, this symbolic model property ensures that satisfiability is recursively enumerable, and since unsatisfiability of all FOL formulas is recursively enumerable, decidability follows. Formally: 

▶ Definition 6.1. Let F be a fragment of FOL. We say that F admits a symbolic model property when there exists a decidable theory T such that for every satisfiable sentence ψ ∈ F 

there exists a symbolic structure S over base theory T such that E(S) |= ψ.

▶ Theorem 6.2. Every fragment of FOL that admits the symbolic model property is decidable. 

Next we provide a general recipe for establishing a symbolic model property for OSC ⋆[α]

fragments (Section 6.1). In Section 6.2, we restate the symbolic-model-property proof of 

OSC ⋆[tot ] using the proof recipe, and Sections 6.3–6.5 give the new decidability results for 5 additional fragments of OSC ⋆ by instantiating the general proof recipe to prove symbolic model properties for them. Neta Elad and Sharon Shoham 35:11 

# 6.1 Abstract Proof Recipe for OSC ⋆ Decidability 

The proof of the symbolic model property for fragments of the OSC ⋆ family works by showing that given a formula α ∧ φ ∈ OSC ⋆[α] and a satisfying model M |= α ∧ φ, a symbolic structure S over a certain decidable base theory T can be constructed out of M such that 

E(S) |= α ∧ φ.Fix a model M |= α ∧ φ, the construction of S and its correctness are derived from the following insight: since OSC ⋆[α] is made up of positive Boolean combinations of universal formulas with a single variable x, we can prove that M and E(S) are OSC ⋆-equivalent, i.e., satisfy the same OSC ⋆[α] formulas, by proving the following two properties: 

▶ Desired Property 6.3. If M |= α then E(S) |= α.

▶ Desired Property 6.4. There exists a surjective function τ : DM → D S such that for every atomic OSC ⋆ formula γ(x), element d ∈ D M , and explicit element ⟨τ (d), e ⟩ ∈ E (τ (d)) , we have M, [x 7 → d] |= γ iff E(S), [x 7 → ⟨ τ (d), e ⟩] |= γ.Desired Property 6.4 ensures that every node n in the symbolic domain of S “mimics” some explicit element d in the domain of M , in the sense that all the elements in the explication of n satisfy the same atomic formulas as d. In particular, since M |= α ∧ φ,Desired Properties 6.3 and 6.4 imply that E(S) |= α ∧ φ (by induction on the structure of φ). The construction of S is divided into two parts: a generic part, that is shared among the various fragments of OSC ⋆ we consider, and a specific part that depends on α and informs the choice of the base theory. Similarly, the proof recipe breaks down the proofs of Desired Properties 6.3 and 6.4 into different lemmas, some of which are proved correct based on the generic part of the construction alone, while the rest are proved for each specific setting. This section lays out the overall proof recipe and the generic part of the construction, while the fragment- and base-theory-specific details are deferred to Sections 6.2–6.5. 

# 6.1.1 Defining the Symbolic Domain 

We define the symbolic domain DS and the surjective function τ by partitioning the domain of M into finitely many equivalence classes, and set the symbolic domain to be the finite set of equivalence classes. To obtain Desired Property 6.4, we do this partitioning according to the atomic formulas. We observe that it suffices to consider atomic formulas over Σ where x

appears, to which we refer as atoms :

▶ Definition 6.5 (Atoms and non-ground terms) . An atom is an atomic formula γ where x

appears as a free variable. We denote the set of all atoms by A, and the set of non-ground terms appearing in atoms by T ≜ {x} ∪ { f (x) | f ∈ Σ }.

We say that an element d ∈ D M satisfies an atom γ if M, [x 7 → d] |= γ, and denote the set of all atoms d satisfies by Γ( d):

Γ( d) ≜ { γ ∈ A | M [x 7 → d] |= γ }.

We partition DM into classes with identical sets of atoms, defining the equivalence class of an element d ∈ D M as 

[d] ≜ { d′ ∈ D M ∣∣ Γ( d′) = Γ( d) }.

For every class [d] and elements d1, d 2 ∈ [d], Γ (d1) = Γ (d2), and with abuse of notation we denote those shared atoms by Γ([ d]) .

L I C S 2 0 2 6 35:12 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

▶ Lemma 6.6. The above partitioning induces an equivalence relation over DM , and, in particular, for every element d ∈ D M and atom γ ∈ A, M, [x 7 → d] |= γ ⇐⇒ γ ∈ Γ(d) ⇐⇒ 

γ ∈ Γ([ d]) .

The domain of the symbolic structure DS is defined to be the set of all equivalence classes, and we use the names ‘node’ and ‘class’ for the elements of DS interchangeably. We define the surjective mapping function τ : DM → D S by mapping each element to its equivalence class, τ (d) ≜ [d].We use the name regular node for any node n where there exists some constant symbol 

c such that x = c ∈ Γ(n), and otherwise use the name summary node . Intuitively, regular nodes will be explicated into a single element, while summary nodes will be explicated into (infinitely) many elements. Note that DS is a finite set, and we define an arbitrary order over its elements, denoted by ≪, which will be used in the sequel. Given the above definitions, we say that an atom γ ∈ A is observed by a node n ∈ D S

if for every explicit element ⟨n, e ⟩ ∈ E (n), E(S), [x 7 → ⟨ n, e ⟩] |= γ ⇐⇒ γ ∈ Γ (n). Note that observance of all atoms implies the necessary conditions of Desired Property 6.4, which can thus be restated as the following: 

▶ Desired Property 6.7 (Atom observance) . All atoms are observed by all nodes: for every node n, every explicit element ⟨n, e ⟩ ∈ E (n) and atom γ ∈ A, γ ∈ Γ(n) ⇐⇒ E(S), [x 7 → ⟨ n, z ⟩] |= γ.The rest of the steps of the recipe explain how to achieve this property while ensuring that E(S) |= α, where different parts of the construction address different atoms. Namely, we divide the atoms into several different kinds :

Element atoms Atoms containing no function applications, internally sub-divided into 

element relation atoms of the form P (x) where P ∈ Σ is a unary relation; element equivalence atoms of the form x = c where c ∈ Σ is a constant symbol; and element order atoms of the form x ≺ c and c ≺ x.

Image atoms Atoms of the form γ[x 7 → f (x)] , where γ is an element atom, and f ∈ Σ is a unary function symbol. For a given function symbol f , we use the name f -image atoms for the set of image atoms associated with it. 

Mixed order atoms Atoms of the form t ≺ t′, t = t′, where t, t ′ are non-ground terms in T.Then, for each kind of atom, a different part of the construction is important. Some are generic and others specific We shall see that for element and image atoms, Desired Property 6.7 can be proved independently of the order axioms and base theory in a generic fashion, while for the mixed order atoms the proof is more involved, and hinges on the specifics of the order and base theory. 

# 6.1.2 Observing Element Equivalence Atoms via Interpretation of Constants and Bound Formulas of Regular Nodes 

Recall that nodes n where x = c ∈ Γ (n) for some constant symbol c are regular nodes. The following lemma ensures that every constant symbol is associated with a unique regular node. 

▶ Lemma 6.8. For every constant symbol c ∈ Σ, there exists exactly one regular node n such that x = c ∈ Γ( n).Neta Elad and Sharon Shoham 35:13 

Accordingly, for every constant symbol c ∈ Σ, we define its interpretation cS ≜ ⟨n, t reg ⟩

where n is the regular node where x = c ∈ Γ (n), and treg is some ground term of the base theory. The choice of treg is theory-specific, and also depends on other theory-specific properties of the construction, in particular the interpretations of function symbols. We provide the specific term for each fragment in the corresponding sections. To ensure the well-definedness of constants we set the bound formula of every regular node n to be B(n) ≜ x = treg .From these definitions follows our first lemma towards proving Desired Property 6.7: 

▶ Lemma 6.9. All element equivalence atoms are observed. 

# 6.1.3 Observing Element Relation Atoms via Interpretation of Unary Relations 

Since the atoms inducing the equivalence classes include atomic formulas of unary relations, the interpretation of unary relation symbols is defined straightforwardly. For every node 

n ∈ D S and every unary relation P ∈ Σ, we define P S (n) as 

P S (n) ≜

{⊤ if P (x) ∈ Γ( n),

⊥ otherwise ,

and derive the following lemma: 

▶ Lemma 6.10. All element relation atoms are observed. 

# 6.1.4 Observing Element Order Atoms via Interpretation of ≺ for Semi-regular Pairs 

For pairs of nodes n, m where at least one of the nodes is regular (i.e., semi-regular pairs), we define the interpretation of the order ≺ by considering element order atoms, essentially inheriting the order interpretation from the order between elements and constants: 

≺S (n, m ) ≜



⊤ if x = c ∈ Γ( n) and c ≺ x ∈ Γ( m)

⊥ if x = c ∈ Γ( n) and c ≺ x / ∈ Γ( m)

⊤ if x = c ∈ Γ( m) and x ≺ c ∈ Γ( n)

⊥ if x = c ∈ Γ( m) and x ≺ c / ∈ Γ( n)

The correctness of this definition is given by the following two lemmas. 

▶ Lemma 6.11. At least one of the conditions above is true for semi-regular pairs, and when multiple conditions are true, they agree on the definition ( ⊤ or ⊥). 

▶ Lemma 6.12. All element order atoms are observed. 

# 6.1.5 Observing Image Atoms by Choosing Targets for Function Interpretations 

Recall that in symbolic structures function interpretations are defined by pairs of nodes and theory terms. In particular, for the unary functions of OSC ⋆ we have f S : DS →DS × Terms T ({x1}). When constructing S we can define the function interpretations in two phases: first defining the function target node, in a way that will ensure observance of 

L I C S 2 0 2 6 35:14 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

image atoms, then using the base theory to define the function terms, such that the functions are well-defined and observance of the mixed order atoms is ensured. Given a node n and function f , we define its interpretation as f S (n) ≜ 〈mfn, t fn

〉, where the target mfn is chosen such that all f -image atoms of n match the element atoms of m:

γ[x 7 → f (x)] ∈ Γ( n) ⇐⇒ γ ∈ Γ(mfn

).

When multiple candidates exist, we take the least according to the arbitrary order ≪ (to ensure consistency among different definitions). The following lemma ensures that such node always exists. 

▶ Lemma 6.13. For every node n and function symbol f , there exists some node mfn such that for every element atom γ, γ ∈ Γ(mfn

) ⇐⇒ γ[x 7 → f (x)] ∈ Γ( n).

▶ Lemma 6.14. All image atoms are observed. 

The function terms tfn will be defined later, with the exception that if mfn is a regular node, then tfn ≜ treg , as dictated by the well-definedness requirement of symbolic structures. 

# 6.1.6 Observing Mixed Order Atoms via Function Terms, Bound Formulas of Summary Nodes and Interpretation of ≺ for Summary Nodes 

Finally, the only atoms not yet handled by the previous steps are the mixed order atoms, whose handling is tightly coupled with the interpretation of the order symbol and the function terms, which are in turn entangled with the definition of bound formulas for summary nodes. Beyond the mixed order atoms, the interpretation of ≺ must also satisfy the order axiom α.Thus far, the order interpretation was only defined for pairs of nodes where at least one is regular, and only function targets were defined. Defining the function terms for function interpretations, bound formulas of summary nodes and the interpretation of ≺ for summary nodes is heavily dependent on the specific order axiom α and imposes requirements on the base theory. In Sections 6.2–6.5 we complete the details and present the chosen base theories for the order axioms we consider. The key idea for defining the remaining components is partitioning the summary nodes to segments according to their relation to constant symbols (i.e., according to the element order atoms). The exact criteria for segmenting is dependent on the specific order axiom α,and is chosen to allow us to split the remainder of the construction and the proof into two tasks: (i) the inter-segment task ; and (ii) the intra-segment task .

The inter-segment task. This task defines the order interpretation between nodes of different segments such that mixed order atoms involving functions whose target nodes reside in different segments are observed (regardless of the definition of function terms). 

The intra-segment task and embeddability. This task defines the order interpretation between nodes within the same segment, as well as the function terms and bound formulas, carefully examining the interaction between function terms and the order constraints to ensure that mixed order atoms also hold within a segment and that the resulting symbolic structure is well-defined. The base theory is key for this task. Namely, the base theory is used to define (i) an order interpretation within a summary node, which is expanded into an order between any two summary nodes within a segment by interleaving the nodes; (ii) function terms such that the required order on the non-ground terms T (as induced by the mixed order atoms) is maintained for every explicit element; and (iii) bound formulas that ensure Neta Elad and Sharon Shoham 35:15 

that function terms are well-defined, and that the order definition satisfies the order axioms. The combination of bound formulas and the order interpretation determines the internal shape of each summary node, i.e., the ordered set of explicit elements represented by the node. Defining the function terms in a way that observes the mixed order atoms requires the internal shape of every summary node (which may serve as the target for some function definition) to have the property of finite embeddability : the ability to embed within the node the set of non-ground terms T, ordered in any way that adheres to the order axioms. Moreover, to support mixed order atoms involving the term x, we need the stronger property of universal finite embeddability : the theory terms produced must be able to embed the order around every explicit element of the node. The lifting of the order by interleaving explicit elements of same-segment nodes ensures that the same terms still comply with the mixed order atoms even when functions target different summary nodes within the same segment. Figuring out how to achieve and encode the universal finite embeddability property within the base theory is one of the key insights required for completing the proof, and we shall see how it dictates the internal shape of summary nodes in the proofs of Sections 6.2–6.5. 

▶ Remark 6.15. As we shall see, the proofs given in Sections 6.2–6.5 actually show a stronger property: the fragments of OSC ⋆ we consider enjoy a bounded symbolic model property, where every satisfiable formula has a satisfying symbolic structure with bounded size, where all bound formulas, function terms and relation formulas are taken from a finite set of theory terms and formulas, dependent only on syntactic properties of the formula. 

# 6.2 Revisiting the Decidability Proof of the Totally-Ordered Self-Cycle Fragment 

In this section we restate the proof of the symbolic model property of OSC ⋆[tot ] from [ 15 ], using the more general proof framework we have introduced. This section is meant as a gentle introduction to the proof technique, using the simpler case of a total (linear) order. The construction for OSC ⋆[tot ] uses the theory of LIA as the decidable base theory for the constructed symbolic structure. Following the steps given in Section 6.1, we first define the regular node term to be 

treg = 0 , meaning regular nodes have bound formulas x = 0 , function terms are defined 

tfn ≜ 0 whenever mfn is a regular node, and each constant c is interpreted as ⟨n, 0⟩ where n is the regular node for which x = c ∈ Γ( n).Next we define the segments of summary nodes in a way that lets us easily resolve the inter-segment task, namely, define the inter-segment order interpretation. For every summary node 

n we denote the set of constant symbols “smaller” than n by C≺(n) ≜ { c ∈ Σ | c ≺ x ∈ Γ( n) },and we define a segment of summary nodes as a set of nodes with identical C≺(·). Using 

C≺(·) we define the inter-segment order as follows. Given two nodes n, m with segments 

C≺(n)̸ = C≺(m),

≺S (n, m ) ≜

{⊤ if C≺(n) ⊊ C≺(m),

⊥ otherwise .

This definition of the inter-segment order interpretation is dictated by the total order axiom tot . Intuitively, if there exists some constant symbol c such that c ≺ x ∈ Γ (m) and 

c ≺ x / ∈ Γ (n) then by totality, x ≺ c ∈ Γ (n) (n is a summary node, thus x = c / ∈ Γ (n)), and by transitivity, it must be the case that all explicit elements of n are smaller than those of m.Note that given that C≺(n)̸ = C≺(m), the total order axiom ensures that if C≺(n) ⊊ C≺(m)

does not hold, then the other direction must hold, i.e., C≺(m) ⊊ C≺(n).

L I C S 2 0 2 6 35:16 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property              

> · · · <f(x)<g(x)<x<h(x)<· · ·
> n
> Figure 3 Example of an order induced by mixed order atoms within a single summary node.
> c1

# · · ·       

> ⟨n1,0⟩
> ⟨n2,0⟩
> ⟨n3,0⟩
> ⟨n1,1⟩
> ⟨n2,1⟩
> ⟨n3,1⟩

# · · · c2 

> Figure 4 A segment with three interleaving summary nodes.

We have yet to define the interpretation of ≺ for same-segment summary nodes, thereby completing the definition of ≺. Yet, remarkably, due to the careful definition of the segments we can prove the correctness of the construction regarding the order axiom in a modular fashion, before defining the intra-segment order. 

▶ Lemma 6.16. For any definition of the intra-segment order that produces a strict total order within each segment, the interpretation ≺E(S) is a total order, i.e., E(S) |= tot .

Moreover, the inter-segment order definition ensures observance of mixed order atoms involving nodes of different segments: 

▶ Lemma 6.17. Let n be a summary node and let f, g ∈ Σ be function symbols such that 

mfn and mfn reside within different segments, then the mixed order atoms f (x) ≺ g(x) and 

g(x) ≺ f (x) are observed. Further, for f such that mfn and n reside in different segments, 

x ≺ f (x) and f (x) ≺ x are observed. 

Next we approach the intra-segment task. Namely, we define the intra-segment order, function terms and bound formulas for summary nodes, in a way that observes all mixed order atoms. As explained in Section 6.1, this is achieved by the universal embeddability property, which ensures that for any possible set of constraints over the non-ground terms, i.e., any possible set of mixed order atoms in some summary node, these constraints can be satisfied for every explicit element associated with the summary node. For example, consider a summary node n where the image atoms of n constrain the functions f, g and h to map back to n (i.e., n is the function target for f, g and h), and the mixed order atoms of n induce the following order: f (x) ≺ g(x) ≺ x ≺ h(x) (see Figure 3). We can see that for any explicit element of n, there must be two smaller explicit elements of 

n, which f (x) and g(x) will map to, and one larger explicit element of n, which h(x) will map to. Further note that all of these explicit elements must be linearly ordered. One natural shape for such a summary node is a bidirectionally-infinite line — the integers ordered by <. Notably, for the case of OSC ⋆[tot ], this shape is uniform for all summary nodes. (We shall see that for prefix orders, two internal shapes are required). Using the base theory of LIA, we define the bound formulas for all summary nodes as simply B(n) = ⊤.Back in our example, we can now define the function terms for f, g and h by considering the arrangement of the terms f (x), g (x), h (x) relative to x, according to the mixed order atoms. We choose function terms that realize this arrangement, for example, tfn = x1 − 2, t gn = x1 − 1

and thn = x1 + 1 .The discussion so far considered a single summary node. As explained earlier (Sec-tion 6.1.6), when a segment includes multiple summary nodes, we lift the order to nodes within the same segment by interleaving the explicit elements of the different summary nodes within the segment (which ensure function term compliance with mixed order atoms across different target summary nodes within the segment). An example is given in Figure 4. The Neta Elad and Sharon Shoham 35:17 

interleaving order is unimportant but should be consistent, and we use the arbitrary order 

≪ to deterministically define it. Finally, we provide the formal definition of the intra-segment order and the function terms. We define the intra-segment order between two nodes n, m as follows: 

n ≺S m ≜

{x1 < x 2 ∨ x1 = x2 if n ≪ m, x1 < x 2 otherwise .

To define the function terms for the cases where the target node of the function interpretation is a summary node (as illustrated above), we consider for every node its set of mixed order atoms. We order all non-ground terms T linearly, according to the order induced by the mixed order atoms. Note that two terms t, t ′ may occupy the same “spot” in the order, if 

t = t′ ∈ Γ(n) (e.g., f (x) = g(x) ∈ Γ(n)). Using the non-ground term x as an anchor, for each non-ground term f (x) we denote by kfn the distance between it and the anchor term x, and define tfn ≜ x1 + kfn.We formalize the correctness of the construction as follows: 

▶ Lemma 6.18. The intra-segment definition of the interpretation of ≺ produces a strict total order within each segment. 

▶ Lemma 6.19. The constructed symbolic structure is well-defined. 

▶ Lemma 6.20. All mixed order atoms are observed. 

This finishes the proof of Desired Property 6.7. Lemmas 6.16 and 6.18 imply that 

E(S) |= tot , and therefore: 

▶ Corollary 6.21. E(S) |= tot ∧ φ.

# 6.3 Decidability of the Prefix-Ordered Self-Cycle Fragment 

In this section we prove the main result of this paper: the decidability of the OSC[ pref ]

fragment. To that end, we prove the symbolic model property for OSC ⋆[pref ] using the STR 

base theory. As before, we follow the steps of Section 6.1. However, OSC ⋆[pref ] requires more involved handling by the base theory at every step. First, we fix the base theory to be STR ∆ over alphabet ∆ = {0, . . . , ℓ }, where ℓ = |T|, i.e., 

ℓ is the number of non-ground terms x, f (x), g (x), . . . that appear in atoms (and assumed to be at least 2). Intuitively, ℓ letters in the alphabet are sufficient to accommodate ℓ

non-ground terms that may be incomparable (for some element), and the letter 0 is used to represent elements that are smaller than all of them. The regular-node term is the empty string treg = ϵ, thereby defining bound formulas for regular nodes, interpretations of constants, and function terms for regular-node targets. Next we define the segments of summary nodes. Whereas in the total order, each segment represented some interval after a subset of the linearly ordered constants, for a prefix order, a finite set of constants may be ordered as a forest of rooted trees. Moreover, the space between a constant and its successor constants in a tree may itself be structured as a tree, inducing multiple segments. 

▶ Example 6.22. Figures 5a and 5b depict two possible sets of segments between 4 constant symbols that are ordered in a tree. We denote by ML, M R two structures for a signature 

{c0, c 1, c 2, c 3}, where for both we have M∗ |= c0 ≺ c1 ∧ c0 ≺ c2 ∧ c0 ≺ c3 and for any other pairs ci, c j , M∗̸ |= ci ≺ cj . However, ML and MR induce different segments, as in ML there 

L I C S 2 0 2 6 35:18 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property          

> c0
> c1c2c3
> s1
> s2
> s3
> (a) “Left-oriented” tree, ML.
> c0
> c1c2c3
> s1
> s′
> 2
> s3
> (b) “Right-oriented” tree, MR.
> Figure 5 Two possible sets of segments laid over 4 constants ordered in a tree. The arrows depict the transitive reduction.

exists an element that is smaller than c1 and c2 but incomparable to c3, and in MR there exists an element that is smaller than c2 and c3 but incomparable to c1.Formally, we define the segments in the construction as pairs of sets of constant symbols, recording for each node n both the set of constant symbols smaller than n, C≺(n) ≜ { c ∈ Σ |

c ≺ x ∈ Γ(n) } (as in OSC ⋆[tot ]), as well as the set of constant symbols larger than n,

C≻(n) ≜ { c ∈ Σ | x ≺ c ∈ Γ(n) }. We denote the segment of a node n by seg( n) ≜

(C≺(n), C ≻(n)) .Having defined the segments of summary nodes, we now address the inter-segment task. We start with an example. 

▶ Example 6.23. Going back to Example 6.22, let us focus on ML of Figure 5a. Formally, we have 3 summary nodes s1, s 2, s 3 where seg( s1) = ({c0}, {c1}), seg( s2) = ({c0}, {c1, c 2})

and seg( s3) = ({c0}, {c3}). The element order atoms imply that s1 is incomparable to s3,as otherwise, by transitivity we would expect x ≺ c3 ∈ Γ(s1) or x ≺ c1 ∈ Γ (s3). Similarly, 

s2 and s3 are incomparable. In contrast, since both s1 and s2 include the x ≺ c1 atom, by downwards totality they must be ordered. Moreover, s2 must come before s1, since otherwise, by transitivity, we would expect x ≺ c2 ∈ Γ( s1).The intuitive inter-segment order derived in Example 6.22 is generalized and formally defined in the following way. Given two nodes n, m such that seg( n)̸ = seg( m):

≺S (n, m ) ≜



⊤ if C≻(n) ∩ C≺(m)̸ = ∅,

⊤ if C≺(n) = C≺(m) and C≻(m) ⊊ C≻(n),

⊥ otherwise .

▶ Remark 6.24. Interestingly, these same definitions also work for OSC ⋆[tot ]. Though redundant, we could have defined the segments for OSC ⋆[tot ] to include both C≺(·) and 

C≻(·), and use the above definition for the inter-segment order. Note that the first case is equivalent to the first case in the definition of the inter-segment order for OSC ⋆[tot ], and the second case is void in OSC ⋆[tot ]. In a total order, if C≺(n) = C≺(m) then C≻(n) = C≻(m)

(and in particular it cannot be that C≻(m) ⊊ C≻(n)). This property can be understood intuitively, since a total order is a special case of a prefix order. Similarly to the construction for OSC ⋆[tot ], the definition of the inter-segment order obtains the following lemmas: Neta Elad and Sharon Shoham 35:19  

> 0
> ϵ02

# ·· ····

> 1

# ·· ···· 2

# ·· ····

Figure 6 The universal tree, a bidirectionally-infinite tree. The arrows show the transitive reduction of the order of the vertices. The ·· ···· symbol represents an infinite rooted tree, isomorphic to the tree represented by the language /(1| · · · |ℓ)∗/ with the standard prefix order, appended to the string of the node it is attached to. 

▶ Lemma 6.25. For any definition of the intra-segment order that produces a strict prefix order within each segment, the interpretation ≺E(S) is a strict prefix order, i.e., E(S) |= pref .

▶ Lemma 6.26. Let n be a summary node and let f, g ∈ Σ be function symbols such that 

mfn and mfn reside within different segments, then the mixed order atoms f (x) ≺ g(x) and 

g(x) ≺ f (x) are observed. Further, for f such that mfn and n reside in different segments, 

x ≺ f (x) and f (x) ≺ x are observed. 

Moving on to the intra-segment task, we first explain the internal shape of summary nodes, which is defined by the combination of bound formulas and order interpretation. We distinguish two kinds of summary nodes: linear nodes, any node n where C≻(n)̸ = ∅, and spanning nodes, where C≻(n) = ∅. Note that when C≻(n)̸ = ∅, there exists some constant c

such that n has atom x ≺ c ∈ Γ (n), thus all explicit elements of n must be smaller than c,and therefore, by downwards totality, totally ordered. The internal shape of a linear node n is isomorphic to the integer number line, which we express using a unary encoding: a non-negative integer z is represented by the string 1z (i.e., concatenating the letter ‘ 1’ z times), and a negative integer z is represented by the string 

0−z . The resulting bound formula B(n) is given by the regular expression /0+|1 ∗/(x) (recall that any regular expression is a relation symbol in the signature of the STR theory). For a spanning node m, a richer internal shape must be supported to ensure universal finite embeddability of any prefix order over ℓ elements, where ℓ is the number of non-ground terms in T, thereby facilitating observance of any combination of mixed order atoms. The general shape is shown in Figure 6, a bidirectionally-infinite tree with an out-degree of ℓ. It can be seen as an extension of the infinite line of linear segments, where each point becomes a root of an infinite tree. Each (rooted) infinite tree with out-degree ℓ can be represented by the regular expressions /(1| · · · |ℓ)∗/, and the entire universal tree is given by the bound formula B(m) = /(1| · · · |ℓ)∗|(0 +· ((2| · · · |ℓ)· (1| · · · |ℓ)∗)?) /(x).Having defined the bound formulas, it remains to define the order interpretation in a way that will induce the desired internal shape for each summary node. 

L I C S 2 0 2 6 35:20 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

# 6.3.1 Defining the Intra-segment Order by Encoding the Order of the Universal Tree 

Order between two vertices in the universal tree, represented by the words x1, x 2 is given by the formula β(x1, x 2), defined in the sequel. Conveniently, linear summary nodes are a special case of the universal tree (where the out-degree is 1 instead of ℓ), and so the same 

β(x1, x 2) is also suitable for them. As before, the intra-segment order is given by interleaving the summary nodes of each segment arbitrarily (using ≪), so for nodes n, m where seg( n) = seg( m) we define 

≺S (n, m ) ≜

{β(x1, x 2) ∨ x1 = x2 if n ≪ m, β(x1, x 2) otherwise .

In order to derive the order formula β(x1, x 2) between two vertices in the universal tree, we identify the three different parts of the universal tree, shown in Figure 6, and then define the order within and across each part. We start by identifying the three different parts: (i) the positive tree, the rooted, infinitely increasing tree, represented by all strings over sub-alphabet {1, . . . , ℓ }, (ii) the negative spine, the infinitely decreasing line, represented by the unary non-empty strings 0, 00 , . . . , and (iii) the negative branches, all trees that span out from vertices on the negative spine. These three parts are captured by the following regular expressions: 

pos( x) ≜ /(1| · · · |ℓ)∗/(x)neg-sp( x) ≜ /0+/(x)neg-br( x) ≜ /0+· (2| · · · |ℓ)· (1| · · · |ℓ)∗/(x)

We now consider all possible pairs of words x1, x 2 such that x1 should come before x2

according to the order within the universal tree. Immediately we can see that all vertices in the negative spine are smaller than vertices in the positive tree. Two other simple cases are when both words are in the positive tree or both are in the negative spine: in the first case they are ordered according to the prefix order, in the second according to the reverse prefix order. Lastly, we consider the cases where the second word x2 is on the negative branches. In these cases we need to consider the vertex on the negative spine from which the negative branch of x2 is spanning out. We identify the root of the negative branch for a word x

with the term neg-rt( x) ≜ pref 0(x), where pref 0(x) is the longest prefix of x containing only ‘0’s. This function is definable in STR , by, e.g., the following formula ∀x, y.y = pref 0(x) ↔(y ⊏ x ∧ /0∗/(y) ∧ ¬ (y · 0 ⊏ x)). If the first word x1 is on the negative spine and comes before the root of x2, then x1 is smaller than x2. If x1 is on the same negative branch as x2,then x1 and x2 are compared according to the regular prefix order. In all other cases, x1

and x2 are incomparable. Putting all of the above together, β(x1, x 2) is defined as the following if-then-else expres-sion: 

β(x1, x 2) ≜



⊤ if neg-sp( x1) ∧ pos( x2)

x1 ⊏ x2 if pos( x1) ∧ pos( x2)

x2 ⊏ x1 if neg-sp( x1) ∧ neg-sp( x2)neg-rt( x2) ⊏ x1 if neg-sp( x1) ∧ neg-br( x2)

x1 ⊏ x2 if neg-br( x1) ∧ neg-br( x2) ∧ neg-rt( x1) = neg-rt( x2)

⊥ otherwise Neta Elad and Sharon Shoham 35:21 

▶ Lemma 6.27. The intra-segment definition of the interpretation of ≺ produces a strict prefix order within each segment. 

# 6.3.2 Defining Function Terms by Encoding Finite Paths in the Universal Tree 

Finally we explain how to define function terms in a way that respects the mixed order atoms. Unlike the uniform segments used in the construction for OSC ⋆[tot ], the segments for OSC ⋆[pref ] are of two kinds: linear and spanning, with different internal shapes of nodes. (Note that all nodes in a segment are of the same kind; we thus refer to segments as linear or spanning accordingly.) While in OSC ⋆[tot ], the uniformity of segments allows defining all function terms without regard to the target nodes, a subtlety arises in OSC ⋆[pref ], since linear target nodes can only embed linear (total) orders, which means that we may fail to embed the set of all non-ground terms in T, ordered according to the mixed order atoms, in linear nodes. Therefore, when defining the function terms of a node n, instead of considering all function interpretations and mixed order atoms together, we first group functions according to the segments of their target nodes. Each such “function group” is associated with a unique segment of the corresponding target nodes, where the corresponding non-ground terms need to be embedded. We identify each group with the set of corresponding non-ground terms, and when n resides within the group’s segment, we also include x.For example, consider a node n and two function symbols f, g where the target node of f ,

mfn, is within the same segment as n, and the target node of g, mgn, is in a different segment. Then f and g have distinct function groups, where the function group of f includes the terms 

x, f (x) and is associated with a segment that includes both n and mfn, and the function group of g includes only g(x) and is associated with a distinct segment that includes mgn.For every function group G ⊆ T of non-ground terms, the function terms for all function symbols appearing in G are defined jointly, which is crucial to ensure the correct embedding of 

G, ordered according to the mixed order atoms. Importantly, the partitioning of non-ground terms T into groups of same-segment target nodes guarantees that the induced order can always be embedded in the corresponding segment. 

▶ Lemma 6.28. Given a node n ∈ D S and function group of non-ground terms G ⊆ T, if the mixed order atoms of n induce a non-linear order on G, then the segment of G is spanning. 

Generalizing the total order case, we define the function terms for the functions in each group relative to an anchor; if x is included in the group then it is the anchor, otherwise an arbitrary term f (x) within the group is used. Instead of using the distance between a non-ground term f (x) and the anchor term to produce function terms (as was possible in 

OSC ⋆[tot ]), we now think of the prefix order on a group’s non-ground terms as a forest, and consider paths from the anchor as a way to define the function terms. The paths are composed from “parent”, “child” and “sibling” steps, where different trees in the forest are connected by considering their roots as siblings. In the sequel we describe how to derive function terms according to the paths in this forest of non-ground terms. The paths in the forest of non-ground terms are finite, and since there are at most ℓ

vertices in the forest, the paths can be encoded by composing terms for the parent of a vertex, 

prnt( x), the j th child of a vertex, chld j (x) (for 1 ≤ j ≤ ℓ), and the j th sibling, sblng j (x).For prnt( x) we need to consider two cases: either x is on the non-positive spine, in which case its parent is obtained by appending 0, otherwise, its parent can be obtained from x by 

L I C S 2 0 2 6 35:22 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

trimming the last letter. This can be expressed by the if-then-else term 

prnt( x) ≜ ITE( x = ϵ ∨ neg-sp( x), x · 0, trim 1(x)) ,

where the trim 1(x) function can be defined by the formula ∀x, y.y = trim 1(x) ↔ (y =

x · 0 ∨ · · · ∨ y = x · ℓ).For chld j (x) where j ≥ 2 we simply append the letter j to x, and for chld 1(x) we need again to consider whether or not we are on the negative spine. If we are, the first child is obtained by trimming the last letter, otherwise, by appending 1:

chld j (x) ≜

{ITE(neg-sp( x), trim 1(x), w · 1) if j = 1 ,x · j otherwise .

We can see that there is a nice symmetry between prnt and chld 1, which reflects the fact that together they form the entire (positive and negative) spine of the tree. Finally, sblng j (x) requires additional considerations to ensure universal finite embed-dability and the observance of mixed order atoms. Consider a case where we have atoms 

f (x) ≺ x, f (x) ≺ g(x) but crucially no x ≺ g(x) or g(x) ≺ x: we need to ensure that x and 

g(x) will be incomparable, regardless of where x falls in the tree. For an explicit element where x represents a first child, we can take g(x) to be a second child of the same parent, which will always result in an incomparable vertex. For explicit elements where x represents a second child, we swap the roles and have g(x) to be a first child. In general, for siblings, we can compose prnt with an if-then-else term that replaces chld j with chld 1 according to whether or not the vertex in question is a jth child. For j = 1 this can be done by checking if the vertex is on the (positive or negative) spine. For other values of j, we check whether the last letter is j:

is-chld j (x) ≜

{/ (0|1) ∗/(x) if j = 1 ,x = trim 1(x) · j otherwise .

Finally, we define the terms sblng j as follows: 

sblng j (x) ≜ ITE(is-chld j (x), chld 1(prnt( x)) , chld j (prnt( x))) .

Given the above terms, for every node n and function group G, any path from the anchor to some non-ground term f (x) in G can be composed to produce some pfn(x), where the free variable x denotes the anchor. For the group where the anchor is x, we use the term 

tfn ≜ pfn(x1) (x1 representing an explicit element of the first argument of f , i.e., the explicit element of n), otherwise tfn ≜ pfn(ϵ).

▶ Lemma 6.29. The constructed symbolic structure is well-defined. 

▶ Lemma 6.30. All mixed order atoms are observed. 

This completes the proof and the construction of S, thus: 

▶ Corollary 6.31. E(S) |= pref ∧ φ.

▶ Remark 6.32 (Complexity analysis of OSC ⋆[pref ]). As previously explained, the construction of a symbolic model for OSC ⋆[pref ] provides a bound on the size of the model and identifies finite sets of bound formulas, function terms and relation formulas, which may be computed from the formula. Note however that the decision procedure induced by the symbolic model property reduces satisfiability of formulas in OSC ⋆[pref ] to validity of formulas in STR ,which is non-elementary. Thus the exact size bounds of symbolic structures are non-important for the complexity analysis. Neta Elad and Sharon Shoham 35:23 

# 6.4 Decidability of OSC ⋆[tot ∧ prosucc ] and OSC ⋆[tot ∧ regpred ]

One notable property of the symbolic structures constructed in Sections 6.2 and 6.3 is that some elements have neither successors nor predecessors. In particular, both in the case of a total order and of a prefix order, regular nodes — i.e., interpretations of constants — may be “disconnected”: since summary nodes stretch from negative infinity to positive infinity, regular nodes adjacent to summary nodes have no immediate neighbors. In this section and the next we consider two variants of total and prefix orders where the order relation ≺ is axiomatized such that there must exist a successor (in one variant) or a predecessor (in the other). To make these orders viable, we also restrict all functions to progressive or respectively regressive, so as to ensure mixed order atoms would not force an infinitely decreasing (respectively increasing) summary nodes. The induced internal shapes of summary nodes are then unidirectionally infinite, which ensures the existence of successors or predecessors. The adapted construction for OSC ⋆[tot ∧ prosucc ] (respectively OSC ⋆[tot ∧ regpred ])reuses most of the construction of Section 6.2. However, one key adjustment is needed. Namely, instead of having ⊤ as the bound formulas for summary nodes, we define them to 

B(n) = x ≥ 0 in the tot ∧prosucc variant, and B(n) = x ≤ 0 in the tot ∧regpred variant. This matches the desired internal shapes, and ensures that all elements have immediate neighbors, thus satisfying the successor/predecessor axioms. Importantly, well-definedness is preserved: in OSC ⋆[tot ∧ prosucc ], progressivity ensures that for any node n, the mixed order atoms will induce an order where the anchor x is minimal among all non-ground terms, and as a result, all function terms tfn will be of the form x1 + kfn for some non-negative integer 

kfn. Similarly in OSC ⋆[tot ∧ regpred ], regressivity ensures all function terms to be of the form x1 + kfn for some non-positive integer kfn.

▶ Lemma 6.33. The constructions for OSC ⋆[tot ∧ prosucc ] and OSC ⋆[tot ∧ regpred ]

are well-defined and correct. 

# 6.5 Decidability of OSC ⋆[pref ∧ prosucc ] and OSC ⋆[pref ∧ regpred ]

Similarly to the total order variants, the internal shapes of summary nodes become unidi-rectionally infinite for the variants of OSC ⋆[pref ], and we adapt the bound formulas and function terms to reflect this. For the successor variant OSC ⋆[pref ∧ prosucc ], the bound formulas for linear summary nodes are /1∗/(x), representing a positive line, and for spanning summary nodes they are /(1| · · · |ℓ)∗/(x), representing a positive rooted tree. When defining the function terms, in order to preserve well-definedness, we avoid prnt( x) and sblng j (x) terms. To that end, we always include the term x as the anchor for all function groups and construct paths from it. Due to progressivity, x will always be the minimum among the non-ground terms, and thus paths will only use chld j (x) terms. For the predecessor variant OSC ⋆[pref ∧ regpred ], the predecessor axiom requires all summary nodes to have the internal shape of a negative line, using the bound formula /0∗/(x) for all summary nodes. Similarly to pref ∧ prosucc , when defining function terms we always include the term x as the anchor for all function groups, but as a mirror-image of pref ∧ prosucc , here x will always be the maximum among the non-ground terms, and all paths will only use prnt( x) terms. In fact, in this case we can use the theory of LIA to express bound formulas and function terms. But notably, unlike in the symbolic structures constructed for OSC ⋆[tot ∧ regpred ], the overall order interpretation in the 

L I C S 2 0 2 6 35:24 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

symbolic structures for OSC ⋆[pref ∧ regpred ] may still be non-linear, due to the order between constants. 

▶ Lemma 6.34. The constructions for OSC ⋆[pref ∧ prosucc ] and OSC ⋆[pref ∧ regpred ]

are well-defined and correct. 

# 7 Implementation 

We implemented a tool for exploring theory-generic symbolic structures by building on the open-source FEST [ 15 ] Python library, which originally supported symbolic structures over LIA. We extended FEST to a generic framework, enabling model-checking of symbolic structures over different decidable theories. As a concrete instantiation beyond LIA, we added the STR theory of strings, whose decision procedure is implemented via a translation to Weak Monadic Second-Order Logic of One Successor (WS1S). The latest version of FEST 

is available at [14]. To support this translation, we developed Python bindings to the MONA [ 22 ] solver for WS1S and integrated them with the generic FEST infrastructure. Implementing the decision procedure for STR by translation to WS1S allows us to efficiently support common string operations via a specialized, optimized translation. For example, we support trim 1 and 

pref 0, as well as specific regular expressions like /0∗/ (all of which are used in the symbolic structures constructed in Section 6.3). Our implementation serves as a proof of concept, demonstrating the feasibility of our approach and providing a preliminary platform for experimenting with non-LIA symbolic structures. 

# 8 Conclusion and Future Work 

We have presented a generalization of symbolic structures to arbitrary base theories with a standard model, and have shown how this formalism can be used to establish decidability results for fragments of FOL that do not enjoy a finite model property. Our main technical contribution is a symbolic model property for several fragments in the Ordered Self-Cycle family, obtained via a generic construction of symbolic models from arbitrary models. This yields new decidability results, including for the Prefix-Ordered Self-Cycle fragment, and extends prior work that was limited to total orders and symbolic structures over LIA. The framework developed in this paper opens several directions for further inquiry. On the logical side, it would be interesting to identify additional decidable fragments using symbolic model properties, in particular fragments based on other kinds of order relations, such as partial orders or lattice-like structures. On the practical side, while our focus has been on foundational results, symbolic structures are intended as a means for counter-model generation in verification. An important direction for future work is to enhance the toy implementation of symbolic structures into a fully-fledged tool, and evaluate its effectiveness on realistic verification benchmarks. Understanding how symbolic counter-models can be presented to users and integrated into existing verification workflows remains an open and relevant challenge. 

References    

> 1Aharon Abadi, Alexander Rabinovich, and Mooly Sagiv. Decidable fragments of many-sorted logic. J. Symb. Comput. , 45(2):153–172, 2010. doi:10.1016/j.jsc.2009.03.003 .

Neta Elad and Sharon Shoham 35:25 

2 Wilhelm Ackermann. Über die erfüllbarkeit gewisser zählausdrücke. Mathematische Annalen ,100(1):638–649, 1928. doi:10.1007/BF01448869 .

3 Leo Bachmair and Harald Ganzinger. Resolution theorem proving. In Handbook of Automated Reasoning , pages 19–99. Elsevier and MIT Press, Amsterdam, 2001. 

4 Vince Bárány, Erich Grädel, and Sasha Rubin. Automata-based presentations of infinite structures. In Finite and Algorithmic Model Theory , volume 379 of London Mathematical Society Lecture Note Series , pages 1–76. Cambridge University Press, 2011. doi:10.1017/ CBO9780511974960.002 .

5 Vince Bárány, Balder ten Cate, and Luc Segoufin. Guarded negation. In Luca Aceto, Monika Henzinger, and Jirí Sgall, editors, Automata, Languages and Programming - 38th International Colloquium, ICALP 2011, Zurich, Switzerland, July 4-8, 2011, Proceedings, Part II , volume 6756 of Lecture Notes in Computer Science , pages 356–367, Berlin, Germany, 2011. Springer. 

doi:10.1007/978-3-642-22012-8\_28 .

6 Michael Benedikt, Leonid Libkin, Thomas Schwentick, and Luc Segoufin. Definable relations and first-order query languages over strings. J. ACM , 50(5):694–751, 2003. doi:10.1145/ 876638.876642 .

7 Achim Blumensath and Erich Grädel. Finite presentations of infinite structures: Automata and interpretations. Theory Comput. Syst. , 37(6):641–674, 2004. URL: https://doi.org/10. 1007/s00224-004-1133-y , doi:10.1007/S00224-004-1133-Y .

8 Achim Blumensath and Erich Grädel. Finite presentations of infinite structures: Au-tomata and interpretations. Theory Comput. Syst. , 37(6):641–674, 2004. doi:10.1007/ s00224-004-1133-y .

9 Egon Börger, Erich Grädel, and Yuri Gurevich. The Classical Decision Problem . Perspectives in Mathematical Logic. Springer, Berlin, 1997. 

10 J Richard Büchi. On a decision method in restricted second order arithmetic. In The Collected Works of J. Richard Büchi , pages 425–435. Springer New York, New York, NY, 1990. 

11 J. Richard Büchi and Lawrence H. Landweber. Definability in the monadic second-order theory of successor. J. Symb. Log. , 34(2):166–170, 1969. doi:10.2307/2271090 .

12 David C Cooper. Theorem proving in arithmetic without multiplication. Machine intelligence ,7(91-99):300, 1972. doi:10.1007/10930755_5 .

13 Daniel Danielski and Emanuel Kieronski. Finite satisfiability of unary negation fragment with transitivity. In MFCS , volume 138 of LIPIcs , pages 17:1–17:15, Dagstuhl, Germany, 2019. Schloss Dagstuhl - Leibniz-Zentrum für Informatik. doi:10.4230/LIPIcs.MFCS.2019.17 .

14 Neta Elad. Infinite models tools (FEST). https://www.cs.tau.ac.il/~netaelad/ #infinite-models-fest . Accessed: 2024-05-06. 

15 Neta Elad, Oded Padon, and Sharon Shoham. An infinite needle in a finite haystack: Finding infinite counter-models in deductive verification. Proc. ACM Program. Lang. , 8(POPL):970– 1000, 2024. doi:10.1145/3632875 .

16 Kurt Gödel. Ein spezialfall des entscheidungsproblems der theoretischen logik. Ergebnisse eines mathematischen Kolloquiums , 2:27–28, 1932. 

17 Erich Grädel, Wied Pakusa, Svenja Schalthöfer, and Lukasz Kaiser. Characterising choiceless polynomial time with first-order interpretations. In 30th Annual ACM/IEEE Symposium on Logic in Computer Science, LICS 2015, Kyoto, Japan, July 6-10, 2015 , pages 677–688. IEEE Computer Society, 2015. doi:10.1109/LICS.2015.68 .

18 Yuri Gurevich. The decision problem for standard classes. The Journal of Symbolic Logic ,41(2):460–464, 1976. doi:10.1017/S0022481200051513 .

19 Shachar Itzhaky, Anindya Banerjee, Neil Immerman, Aleksandar Nanevski, and Mooly Sagiv. Effectively-propositional reasoning about reachability in linked data structures. In CAV ,volume 8044 of Lecture Notes in Computer Science , pages 756–772, Berlin, Germany, 2013. Springer. doi:10.1007/978-3-642-39799-8\_53 .

20 László Kalmár. Über die erfüllbarkeit derjenigen zählausdrücke, welche in der normalform zwei benachbarte allzeichen enthalten. Mathematische Annalen , 108(1):466–484, 1933. 

L I C S 2 0 2 6 35:26 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

21 Bakhadyr Khoussainov and Anil Nerode. Automatic presentations of structures. In Daniel Leivant, editor, Logical and Computational Complexity. Selected Papers. Logic and Compu-tational Complexity, International Workshop LCC ’94, Indianapolis, Indiana, USA, 13-16 October 1994 , volume 960 of Lecture Notes in Computer Science , pages 367–392. Springer, 1994. doi:10.1007/3-540-60178-3\_93 .

22 Nils Klarlund and Anders Møller. MONA Version 1.4 User Manual . BRICS, Department of Computer Science, University of Aarhus, January 2001. Notes Series NS-01-1. Available from 

http://www.brics.dk/mona/ . URL: http://www.brics.dk/mona/ .

23 Harry R. Lewis. Complexity results for classes of quantificational formulas. J. Comput. Syst. Sci. , 21(3):317–353, 1980. doi:10.1016/0022-0000(80)90027-6 .

24 Martin Löb. Decidability of the monadic predicate calculus with unary function symbols. 

Journal of Symbolic Logic , 32(4):563, 1967. 

25 Giuliano Losa and Mike Dodds. On the formal verification of the stellar consensus protocol. In Bruno Bernardo and Diego Marmsoler, editors, 2nd Workshop on Formal Methods for Blockchains, FMBC@CAV 2020, Los Angeles, California, USA (Virtual Conference), July 20-21, 2020 , volume 84 of OASIcs , pages 9:1–9:9, Dagstuhl, Germany, 2020. Schloss Dagstuhl - Leibniz-Zentrum für Informatik. URL: https://doi.org/10.4230/OASIcs.FMBC.2020.9 ,

doi:10.4230/OASICS.FMBC.2020.9 .

26 Raz Lotan and Sharon Shoham. Implicit rankings for verifying liveness properties in first-order logic. In Arie Gurfinkel and Marijn Heule, editors, Tools and Algorithms for the Construction and Analysis of Systems - 31st International Conference, TACAS 2025, Held as Part of the International Joint Conferences on Theory and Practice of Software, ETAPS 2025, Hamilton, ON, Canada, May 3-8, 2025, Proceedings, Part I , volume 15696 of Lecture Notes in Computer Science , pages 375–395, Berlin, Germany, 2025. Springer. doi:10.1007/978-3-031-90643-5\ _20 .

27 Kenneth L. McMillan and Oded Padon. Deductive verification in decidable fragments with ivy. In Andreas Podelski, editor, Static Analysis - 25th International Symposium, SAS 2018, Freiburg, Germany, August 29-31, 2018, Proceedings , volume 11002 of Lec-ture Notes in Computer Science , pages 43–55, Berlin, Germany, 2018. Springer. doi: 10.1007/978-3-319-99725-4\_4 .

28 Michael Mortimer. On languages with two variables. Mathematical Logic Quarterly , 21(1):135– 140, 1975. doi:10.1002/malq.19750210118 .

29 Adithya Murali, Lucas Peña, Ranjit Jhala, and P. Madhusudan. Complete first-order reasoning for properties of functional programs. Proc. ACM Program. Lang. , 7(OOPSLA2):1063–1092, 2023. doi:10.1145/3622835 .

30 Oded Padon, Giuliano Losa, Mooly Sagiv, and Sharon Shoham. Paxos made EPR: decidable reasoning about distributed protocols. Proc. ACM Program. Lang. , 1(OOPSLA):108:1–108:31, 2017. doi:10.1145/3140568 .

31 Nicolas Peltier. Building infinite models for equational clause sets: Constructing non-ambiguous formulae. Log. J. IGPL , 11(1):97–129, 2003. doi:10.1093/jigpal/11.1.97 .

32 Michael O Rabin. Decidability of second-order theories and automata on infinite trees. 

Transactions of the american Mathematical Society , 141:1–35, 1969. doi:10.2307/1995086 .

33 Frank P Ramsey. On a problem of formal logic. Procedures of London Mathematical Society ,30:264–285, 1930. doi:10.1007/978-0-8176-4842-8\_1 .

34 Saharon Shelah. Decidability of a portion of the predicate calculus. Israel Journal of Mathematics , 28(1):32–44, 1977. doi:10.1007/BF02759780 .

35 Orr Tamir, Marcelo Taube, Kenneth L. McMillan, Sharon Shoham, Jon Howell, Guy Gueta, and Mooly Sagiv. Counterexample driven quantifier instantiations with applications to distributed protocols. Proc. ACM Program. Lang. , 7(OOPSLA2):1878–1904, 2023. doi:10.1145/3622864 .

36 Marcelo Taube, Giuliano Losa, Kenneth L. McMillan, Oded Padon, Mooly Sagiv, Sharon Shoham, James R. Wilcox, and Doug Woos. Modularity for decidability of deductive verification with applications to distributed systems. In Jeffrey S. Foster and Dan Grossman, editors, Neta Elad and Sharon Shoham 35:27    

> Proceedings of the 39th ACM SIGPLAN Conference on Programming Language Design and Implementation, PLDI 2018, Philadelphia, PA, USA, June 18-22, 2018 , pages 662–677, New York, NY, 2018. ACM. doi:10.1145/3192366.3192414 .
> 37 Marco Voigt. Decidable fragments of first-order logic and of first-order linear arithmetic with uninterpreted predicates . PhD thesis, Saarland University, Saarbrücken, Germany, 2019.

# A Proofs 

Proof of Theorem 4.4. Given a symbolic structure S and formula φ, we derive φS by the same transformation as in [ 15 ]. The theorem is proved by induction on the structure of φ,following [15]. ◀

Proof of Theorem 6.2. Let F be a fragment that admits a symbolic model property. Thus, there exists a semi-decision procedure for satisfiability in F: enumerating symbolic structures (by enumerating domain sizes, and terms and formulas in the base theory) and then model-checking each structure. Since FOL has a complete proof system, there exists a semi-decision procedure for unsatisfiability in F, and therefore F is decidable. ◀

Proof of Lemma 6.6. Follows immediately from the definition of Γ( ·). ◀

Proof of Lemma 6.8. Let d = cM , then M, [x 7 → d] |= x = c and by Lemma 6.6, x = c ∈

Γ(τ (d)) . For any other n′ = τ (d′), x = c ∈ Γ(n′) ⇐⇒ M, [x 7 → d′] |= x = c ⇐⇒ d′ = cM =

d ⇐⇒ n′ = τ (d). ◀

Proof of Lemma 6.9. Let x = c be some element equivalence atom. x = c ∈ Γ(n) ⇐⇒ 

cS = ⟨n, t reg ⟩. Since B(n) = x = treg , it follows that for the single explicit element 

⟨n, e ⟩ ∈ E (n), x = c ∈ Γ( n) ⇐⇒ E (S), [x 7 → ⟨ n, e ⟩] |= x = c. ◀

Proof of Lemma 6.10. For any node n and explicit element ⟨n, e ⟩, E(S), [⟨n, e ⟩ 7 → x] |=

P (x) ⇐⇒ | =T P S (n) ⇐⇒ P S (n) = ⊤ ⇐⇒ P (x) ∈ Γ( n). ◀

Proof of Lemma 6.11. Since at least one of n, m is regular, one of the above cases must hold. Moreover, as stated in Lemma 6.8, there is exactly one node per constant symbol c. Let 

n, m be nodes such that one is regular. If n = m then trivially the claim holds. Otherwise, let c1̸ = c2 be regular terms such that x = c1 ∈ Γ(n) and x = c2 ∈ Γ(m). If c1 ≺ x ∈ Γ(m),since x = c2 ∈ Γ(m), then it must be that c1 ≺ c2 is true in the model M , and therefore, since x = c1 ∈ Γ (n), we have x ≺ c2 ∈ Γ(n). Therefore, when two conditions above hold, they must agree. ◀

Proof of Lemma 6.12. For any element order atom x ≺ c or c ≺ x, the interpretation of c

is cS = ⟨n, t reg ⟩, for some regular node n. Thus, at least one of the cases in the definition of the semi-regular order will hold, and the atom will be observed appropriately. ◀

Proof of Lemma 6.13. Let n = [ d] be some node in S and let f be some function symbol. Let us denote by d′ = f M (d) the image of d in f in the model M , and let us denote the corresponding node by n′ = [ d′]. For every element atom γ, if γ ∈ Γ(n′) then, by Lemma 6.6, M, [x 7 → d′] |= γ and since d′ = f M (d) we have M, [x 7 → d] |= γ[x 7 → f (x)] ⇐⇒ 

γ[x 7 → f (x)] ∈ Γ( n). ◀

Proof of Lemma 6.14. Follows from the choice of mfn. ◀

Proof of Lemma 6.16. We prove irreflexivity, transitivity and totality separately. Proof by simple case analysis. ◀

L I C S 2 0 2 6 35:28 Decidability Results for Fragments of First-Order Logic via a Symbolic Model Property 

Proof of Lemma 6.17. Follows from the definition of the inter-segment order. ◀

Proof of Lemma 6.18. Follows from the properties of the < relation on the integers. ◀

Proof of Lemma 6.19. Function terms for regular target nodes are always treg , which satis-fies the bound formulas x = treg . The bound formulas for summary nodes are ⊤, which are always satisfied. ◀

Proof of Lemma 6.20. Proof by case analysis of non-ground terms and their segments. ◀

Proof of Lemma 6.25. Similarly to Lemma 6.16, proving irreflexivity, transitivity and downwards-totality separately, by case splitting. ◀

Proof of Lemma 6.26. Follows from the definition of the inter-segment order. ◀

Proof of Lemma 6.27. Case analysis of all possible “locations” in the infinite line (for linear nodes) or the universal tree (for spanning nodes). ◀

Proof of Lemma 6.29. Similarly to Lemma 6.19 for regular nodes. For functions with target summary nodes that are in linear segments, the mixed order atoms must always induce a linear order, which means all finite paths in the universal tree will only use the letter ‘0’ and ‘1’ and produce words within the regular language /1∗|0 +/. For target spanning nodes, the terms defined in Section 6.3.2 will always produce words that represent valid locations in the universal tree. ◀

Proof of Lemma 6.30. Follows by case analysis. ◀

Proof of Lemma 6.33. We give the main argument for prosucc , the reasoning for regpred 

is symmetrical. Since all functions are progressive, all function terms in the symbolic structure will be x + k for some non-negative k. Since for every node n we have B(n) |= x > = 0 

(including regular nodes), it follows that all terms will be in bounds. ◀

Proof of Lemma 6.34. Analogously to Lemma 6.33, the progressive/regressive axioms en-sure well-definedness, and observance of the mixed order atoms follows Lemma 6.30. ◀
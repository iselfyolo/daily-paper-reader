---
title: "PHAGE: Patent Heterogeneous Attention-Guided Graph Encoder for Representation Learning"
title_zh: PHAGE：用于表示学习的专利异构注意力引导图编码器
authors: "Yongmin Yoo, Qiongkai Xu, Zhangkai Wu, Longbing Cao"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10073v1"
tags: ["query:profile-3"]
score: 8.0
evidence: 在专利结构中编码法律引用和权利要求依赖关系
tldr: 针对专利权利要求中被现有线性编码器忽略的层级依赖结构，本文提出PHAGE模型。该模型通过确定性流水线构建包含法律引用和技术关系的异构图，并利用连接掩码与关系感知偏置将权利要求级拓扑引入Token级注意力机制。结合双粒度对比学习目标，PHAGE在分类、检索和聚类任务中表现优异，证明了文档内拓扑结构是极强的归纳偏置。
selection_source: fresh_fetch
motivation: 现有专利编码器通常将权利要求视为线性文本，忽略了其复杂的层级依赖结构以及不同类型的语义关系。
method: PHAGE通过构建异构图区分法律与技术关系，并利用关系感知偏置将权利要求拓扑结构映射到Token级注意力机制中。
result: 实验结果显示，PHAGE在专利分类、检索和聚类任务上全面超越了现有基准模型。
conclusion: 研究证明文档内部的权利要求拓扑结构提供了比文档间结构更强的归纳偏置，且这种偏置在训练后能有效保留在编码器中。
---

## 摘要
专利权利要求形成了一个有向依赖结构，其中从属权利要求继承并细化了先前权利要求的范围；然而，现有的专利编码器将权利要求线性化为文本，并丢弃了这种层级结构。将这种结构直接编码到自注意力机制中面临两个挑战：权利要求依赖关系混合了在语义和提取可靠性上各异的关系类型，且依赖图是定义在权利要求层级的，而 Transformer 则是在 token 层级进行注意力计算。PHAGE 通过一个确定性的图构建流水线解决了第一个挑战，该流水线将近乎确定性的法律引用与噪声较大的基于规则的技术关系区分开来，并将类型差异保留为异构边。它通过连通性掩码和可学习的关系感知偏置解决了第二个挑战，这些机制将权利要求层级的拓扑结构提升到 token 层级的注意力中，使编码器能够对每种关系类型进行差异化加权。随后，一种双粒度对比目标将表示与专利间的分类体系以及专利内的拓扑结构进行对齐。PHAGE 在分类、检索和聚类任务上均优于所有基准模型，表明文档内的权利要求拓扑结构是比文档间结构更强的归纳偏置，且这种偏置在训练后仍保留在编码器权重中。

## Abstract
Patent claims form a directed dependency structure in which dependent claims inherit and refine the scope of earlier claims; however, existing patent encoders linearize claims as text and discard this hierarchy. Directly encoding this structure into self-attention poses two challenges: claim dependencies mix relation types that differ in semantics and extraction reliability, and the dependency graph is defined over claims while Transformers attend over tokens. PHAGE addresses the first challenge through a deterministic graph construction pipeline that separates near-deterministic legal citations from noisier rule-based technical relations, preserving type distinctions as heterogeneous edges. It addresses the second through a connectivity mask and learnable relation-aware biases that lift claim-level topology into token-level attention, allowing the encoder to differentially weight each relation type. A dual-granularity contrastive objective then aligns representations with both inter-patent taxonomy and intra-patent topology. PHAGE outperforms all baselines on classification, retrieval, and clustering, showing that intra-document claim topology is a stronger inductive bias than inter-document structure and that this bias persists in the encoder weights after training.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **PHAGE**（Patent Heterogeneous Attention-Guided Graph Encoder），这是一种专门为专利表示学习设计的图引导编码器。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：专利权利要求（Claims）并非简单的线性文本，而是具有复杂的层级依赖结构（从属权利要求继承并细化前序权利要求）。现有的专利编码器（如 BERT-for-Patents）通常将权利要求视为扁平的序列，忽略了这种定义法律边界和技术逻辑的拓扑结构。
*   **研究动机**：
    *   **结构丢失**：线性编码无法捕捉远距离权利要求间的直接法律联系。
    *   **关系异构性**：权利要求间的依赖包含法律引用（确定性高）和技术耦合（噪声大）等不同类型，现有模型难以区分处理。
    *   **粒度错位**：依赖图定义在权利要求级别，而 Transformer 运行在 Token 级别，如何有效融合两者是技术难点。

### 2. 方法论
PHAGE 提出了一个三阶段的流水线，将权利要求拓扑结构内化到编码器权重中：
*   **异构权利要求依赖图 (CDG) 构建**：
    *   通过确定性规则提取四种边：**法律引用 (`rcite`)**、**技术术语流 (`rterm`)**、**功能耦合 (`rfunc`)** 以及**双重信号 (`rboth`)**。
    *   这种方法减少了对学习型边预测的依赖，保证了图结构的可靠性。
*   **关系感知注意力机制**：
    *   **连通性掩码 (Connectivity Mask)**：作为硬门控，限制 Token 只能关注其所属权利要求及其在 CDG 中有依赖关系的权利要求。
    *   **可学习关系偏置 (Relation-aware Bias)**：为每种关系类型在每一层引入可学习的标量偏置，允许模型差异化地加权不同类型的法律/技术联系。
*   **双粒度对比微调目标**：
    *   **文档级损失 ($L_{doc}$)**：利用 IPC 分类体系，拉近同类专利的距离。
    *   **权利要求级损失 ($L_{claim}$)**：利用 CDG 拓扑结构，确保具有依赖关系的权利要求在表示空间上更接近，并使用可学习的权重平衡不同关系的贡献。

### 3. 实验设计
*   **数据集**：
    *   **分类**：CLEF-IP, USPTO-70k。
    *   **检索**：DAPFAM（跨领域基准）。
    *   **聚类**：CLEF-IP。
*   **评估协议**：采用**冻结编码器 (Frozen-encoder)** 模式，即训练后提取特征进行线性探测或相似度计算，且**推理时不使用图结构**。
*   **对比方法 (Benchmarks)**：
    *   **序列模型**：BERT-base, SciBERT, BERT-for-Patents。
    *   **引用感知模型**：PatentSBERTa, PaECTER, PatEmbed。
    *   **图神经网络 (GNN) 变体**：在 BERT 输出上叠加 GCN, GAT, GraphSAGE。

### 4. 资源与算力
*   **训练数据**：使用 Harvard USPTO 数据集中的 131,653 篇专利（2013-2017年）。
*   **模型骨干**：BERT-for-Patents (Large)，24层，1024隐层维度。
*   **超参数**：Batch Size 为 512（通过梯度累积实现），训练 5 个 Epoch，学习率 2e-5。
*   **算力说明**：文中**未明确提及具体的 GPU 型号和数量**，但给出了详细的训练配置和收敛情况（通常此类规模需要 A100 或同级别显卡）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了分类、检索、聚类三大核心任务，并在多个数据集上进行了验证。
*   **消融实验**：非常详尽，分别验证了：
    1.  不同 CDG 边类型的影响（证明了即使是稀疏的 `rboth` 关系也很关键）。
    2.  注意力组件（掩码 vs 偏置）的作用。
    3.  对比损失函数的组合方式。
*   **客观性**：通过与多种 SOTA 模型（包括专门利用专利间引用关系的模型）对比，证明了其方法的优越性。实验设计遵循了严谨的统计原则（如 5 次运行取平均）。

### 6. 主要结论与发现
*   **内部拓扑胜过外部引用**：文档内部的权利要求拓扑结构提供的归纳偏置，比专利间的引用网络更强。
*   **结构偏置的内化**：PHAGE 在推理时无需构建图结构，仅靠训练时被“塑造”过的权重即可保持高性能，这证明模型成功学习了专利特有的逻辑路由。
*   **深度专业化**：模型低层倾向于处理技术术语流，而高层则专注于权利要求内部的细化。

### 7. 优点
*   **轻量化注入**：仅增加了极少量（每层 5 个）可学习参数，却带来了显著的性能提升。
*   **推理高效**：推理阶段不需要复杂的图构建或 GNN 计算，具有极强的实用性。
*   **可解释性**：通过对关系偏置的分析，揭示了模型如何处理法律引用与技术描述之间的差异。

### 8. 不足与局限
*   **语言依赖性**：术语流提取（`rterm`）依赖于英语的冠词用法（a/an vs the/said），迁移到无冠词语言（如中文、日文）时需要重新设计提取规则。
*   **训练-推理不对称**：虽然证明了权重可以保留偏置，但推理时完全丢弃图结构是否会导致上限受限仍有待探讨。
*   **长文本限制**：受限于 Transformer 的 512 token 长度，对于权利要求极多的超长专利，可能存在截断风险。

（完）

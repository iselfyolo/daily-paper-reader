---
title: "MAP-Law: Coverage-Driven Retrieval Control for Multi-Turn Legal Consultation"
title_zh: MAP-Law：面向多轮法律咨询的覆盖率驱动检索控制
authors: "Qinchuan Cheng, Ruixuan Xie, Jiaqi Liu, Xiaoya Yuan, Yuxin Liu"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01486v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 多轮法律咨询与检索控制
tldr: 针对多轮法律咨询中检索深度难以控制的问题，本文提出MAP-Law框架。该框架通过构建包含法律问题、要素和证据的结构化状态图，利用元素覆盖率、证据覆盖率和边际收益等指标，实现对检索过程的精准控制。实验表明，MAP-Law在保证高覆盖率的同时，显著减少了检索轮数和冗余信息，提升了咨询的准确性与效率。
selection_source: fresh_fetch
motivation: 现有的法律咨询智能体多采用固定检索深度，容易导致关键法律要素支撑不足或检索信息过载。
method: 提出一种覆盖率驱动的检索控制框架，通过结构化状态图建模咨询过程，并依据覆盖率信号动态决定检索策略。
result: "在劳动法场景下的实验显示，该方法仅需平均2.9轮检索即可达到0.860的要素覆盖率，比固定轮数基准减少了80%以上的证据量。"
conclusion: MAP-Law将检索停止机制从固定参数转变为可解释的决策过程，有效平衡了法律咨询的完备性与简洁性。
---

## 摘要
法律咨询是一项高风险、知识密集型的任务，要求智能体识别相关的法律问题，检索权威支持，并确定证据何时足以给出建议。尽管检索增强生成（RAG）提高了法律问答的可靠性，但许多多轮法律智能体仍依赖于固定的检索深度或粗粒度的启发式控制。这通常会导致关键法律要素的支持不足，或者过度检索增加了上下文负担并削弱了回答的焦点。我们提出了 MAP-Law，这是一个用于多轮法律咨询中检索控制的覆盖率驱动框架。MAP-Law 将咨询建模为一个在由问题节点、法律要素节点和证据节点组成的联合结构化状态上的受控检索过程。在每一轮检索后，智能体计算要素覆盖率（Element Coverage）、证据覆盖率（Evidence Coverage）和边际收益（Marginal Gain），并利用这些信号决定是继续检索、重定向搜索还是生成最终回复。通过这种方式，MAP-Law 将停止机制从固定的超参数转变为与法律论证结构一致的可解释且可审计的决策。在涵盖八个劳动法场景、包含 50 个案例的自建数据集上的实验表明，以 DeepSeek 作为动作选择器的 MAP-Law 平均仅需 2.9 轮检索和 5.8 条证据即可达到 0.860 的要素覆盖率。与固定的七轮基准相比，它减少了超过 80% 的证据量和 58% 的检索轮数。消融实验结果进一步证实了覆盖率驱动停止、联合图表示和基于大语言模型（LLM）的动作选择的独立贡献。

## Abstract
Legal consultation is a high-stakes, knowledge-intensive task that requires agents to identify relevant legal issues, retrieve authoritative support, and determine when evidence is sufficient for a recommendation. Although retrieval-augmented generation has improved grounding in legal question answering, many multi-turn legal agents still rely on fixed retrieval depth or coarse heuristic control. This often leads to either insufficient support for key legal elements or excessive retrieval that increases context burden and weakens answer focus.   We propose MAP-Law, a coverage-driven framework for retrieval control in multi-turn legal consultation. MAP-Law models consultation as a controlled retrieval process over a joint structured state consisting of issue nodes, legal element nodes, and evidence nodes. After each retrieval round, the agent computes Element Coverage, Evidence Coverage, and Marginal Gain, and uses these signals to decide whether to continue retrieval, redirect the search, or generate the final response. In this way, MAP-Law turns stopping from a fixed hyperparameter into an interpretable and auditable decision aligned with legal argumentative structure.   Experiments on a self-constructed dataset of 50 cases across eight labor-law scenarios show that MAP-Law with DeepSeek as the action selector achieves an Element Coverage of 0.860 using only 2.9 retrieval rounds and 5.8 evidence pieces on average. Compared with a fixed seven-round baseline, it reduces evidence volume by over 80% and retrieval rounds by 58%. Ablation results further confirm the independent contributions of coverage-driven stopping, joint graph representation, and LLM-based action selection.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAP-Law: Coverage-Driven Retrieval Control for Multi-Turn Legal Consultation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
法律咨询是一项高风险且知识密集型的任务。目前的法律大模型主要采用检索增强生成（RAG）技术来减少幻觉，但在多轮咨询中面临一个核心挑战：**检索深度难以平衡**。
*   **研究动机**：如果检索过早停止，关键法律要素（如证据、法条）支撑不足；如果机械地过度检索，则会引入冗余信息，增加上下文负担，甚至稀释回答的焦点。
*   **核心问题**：如何让智能体根据当前证据的“充分性”动态决定何时停止检索、何时重定向搜索，从而在保证法律论证完备性的同时提高效率。

### 2. 方法论：核心思想与技术细节
论文提出了 **MAP-Law**（Mindmap-Augmented Planning）框架，其核心是将检索控制建模为一种基于“覆盖率驱动”的规划过程。
*   **联合图表示（Joint Graph）**：构建一个包含“问题节点（Issues）”、“法律要素节点（Elements）”和“证据节点（Evidence）”的结构化状态图。
*   **三大核心指标**：
    1.  **要素覆盖率 (EC)**：已被充分证据支撑的法律要素比例。
    2.  **证据覆盖率 (EVC)**：检索到的证据中被最终采纳使用的比例（衡量冗余度）。
    3.  **边际收益 (MG)**：新一轮检索对未解决要素提供的增量支持。
*   **动作选择机制**：使用大语言模型（如 DeepSeek）作为决策器，基于上述结构化指标决定下一步动作（检索、补充、评估或生成结论）。
*   **停止规则**：当 EC 和 EVC 达到阈值，或者边际收益连续下降，或者达到最大轮数时，系统自动触发停止。

### 3. 实验设计
*   **数据集/场景**：自建了一个包含 **50 个法律咨询案例**的数据集，涵盖劳动法下的 8 个细分场景（如劳动关系认定、加班费争议、工伤赔偿等）。
*   **Benchmark**：由具备法律背景的人员标注了金标准法律要素、支撑法条和参考答案。
*   **对比方法**：
    *   **固定轮数基准**：Fixed-3、Fixed-5、Fixed-7（分别固定检索 3/5/7 轮）。
    *   **消融变体**：MAP-full (Rule)（使用规则而非 LLM 决策）、MAP-no-stopping（不设停止机制）、MAP-no-graph（不使用图结构）。

### 4. 资源与算力
*   **模型使用**：检索端使用了预训练的 `hfl/chinese-bert-wwm-ext` 作为编码器；决策端调用了 **DeepSeek API**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于推理阶段的控制框架而非模型预训练，其算力消耗主要集中在 API 调用和向量检索上。

### 5. 实验数量与充分性
*   **实验规模**：针对 50 个案例进行了多组对比实验和消融实验。
*   **充分性评价**：
    *   **消融实验充分**：分别验证了停止规则、图结构和 LLM 决策器的有效性，逻辑闭环完整。
    *   **客观性**：引入了法律专业人员进行标注，并使用了 Fleiss' κ 系数（0.82）证明标注的一致性。
    *   **局限性**：50 个案例的样本量相对较小，且仅限于劳动法领域，跨领域的泛化性验证略显不足。

### 6. 主要结论与发现
*   **高效平衡**：MAP-Law (DeepSeek) 在仅平均使用 **2.9 轮检索**和 **5.8 条证据**的情况下，达到了 **0.860** 的要素覆盖率。
*   **显著减负**：相比于固定 7 轮的基准，MAP-Law 减少了 **80% 以上**的证据冗余量，检索轮数缩减了 **58%**。
*   **控制优于堆砌**：实验证明，盲目增加检索轮数（如 MAP-no-stopping）不仅浪费算力，甚至可能因为引入噪声而导致覆盖率下降（从 0.86 降至 0.78）。

### 7. 优点与亮点
*   **可解释性强**：停止决策不再是黑盒，而是基于明确的法律要素支撑状态，符合法律实务中“证据充分性”的逻辑。
*   **结构化创新**：通过联合图将非结构化的对话转变为可追踪的推理状态，便于审计和纠错。
*   **自适应性**：能够根据案例的复杂程度动态调整检索深度，简单案例快处理，复杂案例深挖掘。

### 8. 不足与局限
*   **领域局限性**：目前仅在劳动法领域验证，不同法律部门（如刑法、民法典）的要素提取难度和逻辑结构可能存在差异。
*   **阈值敏感性**：EC、EVC 等停止阈值目前是基于经验设定的，缺乏针对不同风险等级任务的动态调整机制。
*   **依赖上游质量**：系统的表现高度依赖于初始的“法律要素提取”准确度，如果第一步漏掉关键要素，后续检索将无法弥补。
*   **规模风险**：50 个案例虽然深度足够，但难以覆盖法律咨询中可能出现的极端长尾情况。

（完）

---
title: "Accurate Legal Reasoning at Scale: Neuro-Symbolic Offloading and Structural Auditability for Robust Legal Adjudication"
title_zh: 大规模准确法律推理：面向稳健法律裁决的神经符号卸载与结构化可审计性
authors: "Stanisław Sójka, Witold Kowalczyk"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02472v1"
tags: ["query:profile-3"]
score: 10.0
evidence: 用于稳健法律裁决和自动推理的神经符号方法
tldr: "针对法律文本中复杂逻辑导致的推理错误和高昂成本，本文提出一种名为“摊销智能”的神经符号方法。该方法利用大模型将法律文本一次性转换为确定性自主合约语言（DACL）的图表示，通过确定性图执行实现法律裁决。该系统在保持高一致性的同时，将计算成本降低了90%以上，并满足了法律审计的严苛要求，为大规模法律推理提供了稳健的解决方案。"
selection_source: fresh_fetch
motivation: 现有大语言模型在处理复杂法律条款时存在推理错误、成本高昂且缺乏可审计性的问题。
method: 提出一种神经符号框架，利用大模型将法律文本转化为确定性自主合约语言（DACL）的图中间表示，并进行确定性执行。
result: "该方法在法律裁决中实现了近乎完美的逻辑一致性，消除了概率模型的推理缺陷，并降低了超过90%的计算成本。"
conclusion: 结合大模型的翻译能力与符号系统的确定性执行，是实现大规模、高精度且可审计法律推理的有效途径。
---

## 摘要
法律文本通常包含计算型法律条款——即理解这些条款需要复杂的逻辑。虽然前沿的大型推理模型（LRM）可以描述此类条款，但构建生产级系统仍受限于推理错误和高昂的推理成本。我们提出了“摊销智能”（Amortized Intelligence），这是一种神经符号方法，通过一次性使用大语言模型（LLM）将法律文本转换为确定性自主合约语言（DACL）：一种类型化的图中间表示。随后，裁决过程依赖于具有可视化可审计追踪的确定性图执行。与运行时 LRM 基准（包括 GPT-5.2 和 Gemini 3 Pro）相比，我们基于 DACL 的智能体实现了近乎完美的一致性，并缓解了在概率模型中观察到的“推理悬崖”现象。该系统在高吞吐量工作流中降低了 90% 以上的计算成本，同时满足了法律裁决中严格的可审计性要求。

## Abstract
Legal texts often contain computational legal clauses--provisions whose understanding requires complex logic. While frontier Large Reasoning Models (LRMs) can describe such clauses, building production-ready systems is limited by reasoning errors and the high cost of inference. We propose Amortized Intelligence, a neuro-symbolic approach where we use an LLM once to translate a legal text into Deterministic Autonomous Contract Language (DACL): a typed graph intermediate representation. Adjudication then relies on deterministic graph executions with a visually auditable trace. In comparison against runtime LRM baselines (including GPT-5.2 and Gemini 3 Pro), our DACL-based Agent achieves near-perfect consistency and mitigates the "reasoning cliff" observed in probabilistic models. The system reduces compute costs by over 90% in high-volume workflows while satisfying the strict auditability requirements of legal adjudication.

---

## 论文详细总结（自动生成）

这篇论文由 Delos AI 公司的研究人员撰写，提出了一种结合大语言模型（LLM）与符号逻辑的神经符号架构，旨在解决法律裁决在大规模应用中的准确性、成本和可审计性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管前沿的大型推理模型（LRM）在语义理解上表现出色，但在处理复杂的“计算型法律条款”（如税务公式、资格阈值、阶梯定价）时，面临两大障碍：
    1.  **可靠性风险（推理悬崖）**：概率模型在处理嵌套逻辑和算术时容易产生幻觉，且无法保证相同输入产生相同输出。
    2.  **经济成本**：对每一笔交易都使用高昂的思维链（CoT）推理会导致成本随业务量线性增长，在商业上不可持续。
*   **研究动机**：法律裁决需要确定性、可解释性和低成本。作者试图将 LLM 的语义能力与符号系统的严谨性结合，实现“摊销智能”（Amortized Intelligence）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 LLM 的角色从“运行时解释器”转变为“一次性编译器”。
    *   **编译阶段**：使用 LLM 将法律文本翻译成**确定性自主合约语言（DACL）**。DACL 是一种强类型的有向无环图（DAG）中间表示。
    *   **执行阶段**：运行时，轻量级智能体仅负责语义路由，核心逻辑由符号引擎根据 DACL 图确定性地执行。
*   **关键技术细节**：
    *   **DACL 四大原语**：
        1.  **程序（Procedure）**：顺序工作流。
        2.  **逻辑条款（Logical Clause）**：一阶命题逻辑。
        3.  **范围条款（Range Clause）**：处理连续变量到离散区间的映射（如阶梯电价）。
        4.  **定价公式（Pricing Formula）**：沙盒化的算术表达式。
    *   **智能体流水线**：语义映射（识别相关条款） -> 符号委派（执行 DACL 引擎） -> 结果合成（生成自然语言解释）。
    *   **版本控制**：支持条款级的时间戳标注，处理合同修订。

### 3. 实验设计
*   **数据集/场景**：选取了四个真实工业领域的协议：医疗（Health-PPO）、能源（Energy-Sup）、物流（Logistics-MSA）和市政（Muni-IFB）。其中物流协议复杂度最高，包含 76 个决策状态。
*   **基准测试（Benchmark）**：
    *   **对比方法**：GPT-5.2（无推理模式/中等推理模式）、Claude 4.5 Sonnet、Gemini 3 Pro。
    *   **评估流程**：使用事件生成器模拟 400 个真实交易事件，由独立开发的“黄金标准”确定性引擎生成标准答案，并引入 LLM-as-Judge 进行一致性评估。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）或训练时长。
*   **模型使用**：实验使用了 OpenAI、Anthropic 和 Google 的闭源 API。DACL 智能体在运行时使用了轻量级的 `gpt-5-mini`，而基准测试使用了完整版的 LRM。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同复杂度的领域，共进行了 400 组测试。
*   **充分性评估**：
    *   **覆盖度**：实验覆盖了从简单算术到大规模组合决策树的不同难度梯度。
    *   **错误分析**：进行了深入的错误分类（变量依赖错误、干扰幻觉、算术幻觉），揭示了 LRM 的主要失败模式。
    *   **客观性**：通过独立开发的“黄金标准”引擎和多模型对比，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能卓越**：DACL 智能体达到了 **99.5%** 的综合准确率，远超表现最好的 LRM 基准（82.8%）。
*   **成本骤降**：在高吞吐量任务中，DACL 架构比 GPT-5.2 推理模式降低了 **90% 以上**的计算成本，Token 消耗减少了约 10 倍。
*   **推理悬崖**：LRM 在处理高分支逻辑（如物流协议）时表现出断崖式下跌，而 DACL 表现出“复杂度不变性”。
*   **错误根源**：研究发现 LRM 的主要错误并非算术能力不足，而是**变量依赖（Variable Dependency）**错误（占 71%），即模型在复杂的布尔逻辑中迷失了状态追踪。

### 7. 优点
*   **架构创新**：提出的“摊销智能”概念将昂贵的推理过程转移到编译阶段，极具工程实践价值。
*   **结构化审计**：DACL 提供的执行追踪（Audit Trail）满足了法律领域对可解释性的刚需。
*   **生产验证**：该系统已在真实生产环境中运行超过 12 个月，处理了大量真实账单，证明了其稳健性。

### 8. 不足与局限
*   **表达力受限**：DACL 目前仅支持预定义的四类原语，无法处理“合理注意”等开放性法律标准或更高级的逻辑。
*   **标注成本**：为了验证准确性，需要人工编写“黄金标准”代码，这限制了大规模跨领域的快速评估。
*   **编译风险**：如果 LLM 在初始编译阶段误解了条款，该错误会变成确定性的重复错误（虽然可以通过人工审核缓解）。
*   **语言局限**：目前仅在英语商业合同上进行了验证，未测试非英语环境或非结构化协议。

（完）

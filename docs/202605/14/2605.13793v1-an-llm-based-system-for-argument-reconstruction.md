---
title: An LLM-Based System for Argument Reconstruction
title_zh: 一种基于大语言模型（LLM）的论证重构系统
authors: "Paulo Pirozelli, Victor Hugo Nascimento Rocha, Fabio G. Cozman, Douglas Aldred"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13793v1"
tags: ["query:q1"]
score: 9.0
evidence: 将论点重构为抽象论证图
tldr: 本研究针对人类推理中的核心环节——论证，开发了一个基于大语言模型（LLM）的端到端系统，用于将自然语言文本重构为抽象论证图。该系统采用多阶段流水线，逐步识别前提与结论，并确定支持、攻击和削弱三种逻辑关系。通过对论证理论教科书的案例进行人工评估以及在基准数据集上的定量测试，结果显示该系统能有效恢复复杂的论证结构，并在不同标注方案下表现良好，证明了LLM在可扩展论证重构领域的应用潜力。
selection_source: fresh_fetch
motivation: 旨在解决从自然语言文本中自动重构复杂论证结构并将其转化为抽象论证图的挑战。
method: 提出一种多阶段大语言模型流水线，通过识别论证组件、筛选相关元素并确定逻辑关系来构建有向无环图。
result: 实验表明该系统能有效恢复论证结构，并在多个基准数据集上通过适配标注方案达到了合理的性能水平。
conclusion: 该研究证明了基于大语言模型的流水线在实现可扩展的论证重构任务方面具有显著潜力。
---

## 摘要
论证是人类推理的一个基本方面，其中主张被支持、挑战并相互权衡。我们提出了一种基于端到端大语言模型（LLM）的系统，用于将自然语言文本中的论证重构为抽象论证图。该系统遵循多阶段流水线，逐步识别论证组件、选择相关元素并揭示其逻辑关系。这些元素被表示为由两种组件类型（前提和结论）和三种关系类型（支持、攻击和削弱）组成的有向无环图。我们进行了两项互补实验来评估该系统。首先，我们对取自论证理论教科书的论证进行人工评估，以评估系统恢复论证结构的能力。其次，我们在基准数据集上进行定量评估，通过将我们的输出映射到已建立的标注方案，从而实现与先前工作的比较。结果表明，该系统能够充分恢复论证结构，并且在适应不同的标注方案时，在各基准数据集上均取得了合理的性能。这些发现突显了基于 LLM 的流水线在可扩展论证重构方面的潜力。

## Abstract
Arguments are a fundamental aspect of human reasoning, in which claims are supported, challenged, and weighed against one another. We present an end-to-end large language model (LLM)-based system for reconstructing arguments from natural language text into abstract argument graphs. The system follows a multi-stage pipeline that progressively identifies argumentative components, selects relevant elements, and uncovers their logical relations. These elements are represented as directed acyclic graphs consisting of two component types (premises and conclusions) and three relation types (support, attack, and undercut). We conduct two complementary experiments to evaluate the system. First, we perform a manual evaluation on arguments drawn from an argumentation theory textbook to assess the system's ability to recover argumentative structure. Second, we conduct a quantitative evaluation on benchmark datasets, allowing comparison with prior work by mapping our outputs to established annotation schemes. Results show that the system can adequately recover argumentative structures and, when adapted to different annotation schemes, achieve reasonable performance across benchmark datasets. These findings highlight the potential of LLM-based pipelines for scalable argument reconstruction.

---

## 论文详细总结（自动生成）

以下是对论文《An LLM-Based System for Argument Reconstruction》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：论证（Argumentation）是人类推理的核心，但将自然语言文本转化为结构化的论证模型一直面临挑战。传统的**论证框架（AF）**虽然逻辑严密，但难以处理自然语言中的隐性前提、语境依赖和语义模糊；而**大语言模型（LLM）**虽具备强大的上下文理解能力，但直接生成的论证往往缺乏系统性的理论支撑和结构一致性。
*   **核心问题**：如何结合论证理论与 LLM 的优势，构建一个能够自动、准确且可扩展地将非结构化文本重构为符合逻辑规范的“抽象论证图”的系统。

### 2. 论文提出的方法论
该系统采用了一个**多阶段 LLM 流水线（Multi-stage Pipeline）**，将复杂的重构任务分解为多个可控的子任务：
*   **组件识别与处理**：
    1.  **识别组件**：提取文本中的主张（Claims）和前提（Premises），过滤无关信息。
    2.  **合并与重写**（可选）：消除冗余，将代词还原为具体对象，确保每个组件语义自洽。
*   **结构重构**：
    3.  **结论识别**：确定论证的核心结论。
    4.  **递归关系推理**：从结论出发，递归地寻找支持或攻击它的前提。通过维护一个已访问节点列表，强制生成**有向无环图（DAG）**。
    5.  **高级逻辑关系识别**：区分**收敛前提**（独立支持）与**联合前提**（共同起作用）；识别**隐性前提**（Enthymemes）；区分**反驳**（Rebuttal，攻击组件本身）与**削弱**（Undercut，攻击推理过程）。
*   **后处理与可视化**：
    6.  **传递归约（Transitive Reduction）**：移除冗余的直接边，简化图结构。
    7.  **图表生成**：将逻辑结构转化为可视化的论证图。

### 3. 实验设计
论文设计了两套互补的评估方案：
*   **内部评估（Internal Evaluation）**：
    *   **数据集**：基于 Sacrini (2023) 的论证理论教科书，手动标注了 42 个涉及哲学和常识的短论证。
    *   **指标**：组件跨度检测（F1）、结论检测（准确率）、关系检测（准确率），以及针对隐性前提、削弱关系和整体忠实度的人工评分（1-3 分量表）。
*   **外部评估（External Evaluation）**：
    *   **数据集**：使用两个标准基准数据集——**AAEC**（学生说服性论文）和 **AbstRCT**（生物医学摘要）。
    *   **对比方法**：与 Morio 等人 (2022) 的端到端论证挖掘 SOTA 模型进行对比。
    *   **模型版本**：实验使用了 GPT-4o、GPT-5 和 GPT-5-mini（注：根据论文标注的 2026 年发布日期，这些为当时的最先进模型）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该系统基于 LLM API 构建，其核心开销在于 API 调用。文中提到 GPT-5-mini 在输出质量与成本之间取得了较好的平衡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   内部评估涵盖了 42 个复杂论证案例，由两名独立标注者进行人工评分，并计算了标注者间的一致性（EA）。
    *   外部评估在两个不同领域（教育、医疗）的标准数据集上进行了定量测试。
*   **充分性评价**：实验设计较为全面，既有细粒度的逻辑结构定性分析，又有标准任务的定量对比。通过“金标准组件”实验，深入探讨了系统在不同阶段的瓶颈，实验结论较为客观、公平。

### 6. 论文的主要结论与发现
*   **结构恢复能力强**：系统在内部评估中表现优异，结论检测准确率达 92.5%，在处理隐性前提（2.86/3）和削弱关系（2.90/3）等高级任务上获得了高分。
*   **组件识别是瓶颈**：在外部基准测试中，由于 LLM 生成的文本边界与人工标注的精确跨度（Span）难以完全匹配，导致端到端 F1 分数较低。
*   **关系推理优势**：如果提供预先标注好的“金标准组件”，该系统在关系预测上的表现显著优于现有 SOTA 模型（在 AbstRCT 上 Link F1 提升了约 11.7%），证明了 LLM 在逻辑关联推理上的卓越能力。

### 7. 优点与亮点
*   **理论深度**：不同于简单的分类任务，该系统引入了论证理论中的高级概念（如 Undercut、Linked Premises），重构出的图结构更符合人类逻辑思维。
*   **多阶段控制**：通过分解步骤，降低了 LLM 处理长程依赖和全局一致性的难度，且每个阶段都可独立调试。
*   **递归 DAG 约束**：巧妙地解决了论证重构中常见的循环论证识别问题，确保了输出结构的合法性。

### 8. 不足与局限
*   **边界对齐问题**：在标准论证挖掘任务中，系统对文本跨度的精确识别（Exact Match）较差，这限制了其在现有 Benchmark 上的排名。
*   **计算成本**：多阶段流水线意味着需要多次调用 LLM API，对于大规模实时处理可能存在成本和延迟问题。
*   **幻觉风险**：在生成隐性前提时，LLM 可能会引入原文未暗示的、过于琐碎或无关的假设。
*   **长文本挑战**：虽然系统支持组件合并，但对于极长或结构极其松散的文档，重构的完整性仍有待验证。

（完）

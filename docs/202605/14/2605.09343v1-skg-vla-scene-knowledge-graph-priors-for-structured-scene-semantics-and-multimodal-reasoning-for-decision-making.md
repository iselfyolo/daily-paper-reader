---
title: "SKG-VLA: Scene Knowledge Graph Priors for Structured Scene Semantics and Multimodal Reasoning for Decision Making"
title_zh: SKG-VLA：用于结构化场景语义和决策多模态推理的场景知识图谱先验
authors: "Zeyu Li, Lei Li"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09343v1"
tags: ["query:q8"]
score: 9.0
evidence: 使用知识图谱进行投诉决策的多模态推理
tldr: 针对大规模投诉处理系统中多模态证据利用不足的问题，本文提出SKG-VLA框架。该框架通过构建场景知识图谱（SKG）来统一建模投诉实体、证据、政策及关系，并利用数据合成流水线构建大规模数据集。通过三阶段训练策略将结构化场景先验注入多模态模型，显著提升了政策推理能力、决策准确性及在复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的投诉理解系统主要依赖浅层分类或模板匹配，缺乏对显式场景结构、规则知识及跨证据依赖关系的深度利用。
method: 提出场景知识图谱（SKG）统一表示投诉语义，并结合数据合成流水线与三阶段训练策略，将结构化场景先验注入多模态决策模型。
result: 实验证明SKG-VLA在政策导向推理、决策准确率、长尾泛化以及处理不完整证据的鲁棒性方面均表现优异。
conclusion: 通过引入结构化场景知识图谱作为先验，可以有效增强多模态模型在复杂决策任务中的语义理解和推理能力。
---

## 摘要
大规模投诉处理系统中的决策日益依赖于异构证据，包括投诉叙述、屏幕截图、订单元数据、历史交互和平台政策。现有的投诉理解系统主要对孤立的模态进行浅层分类或模板匹配，而未能充分利用显式的场景结构、规则知识和跨证据依赖关系。为了解决这一局限性，我们提出了用于多模态投诉决策的 SKG-VLA。其核心思想是将每个案例建模为一个结构化的投诉场景，并使用场景知识图谱（SKG）来表示其与决策相关的语义。该图谱将投诉实体、证据项、政策条款、时间事件、交易状态和与行动相关的关系组织成一个统一的图。基于 SKG，我们构建了一个数据合成流水线，用于生成投诉场景描述、符合规则的图泛化、问答监督和决策建议。我们进一步构建了一个大规模投诉场景数据集，包含纯文本和多模态领域内基准。最后，我们采用了一种三阶段训练策略——领域自适应预训练、面向任务的指令微调和端到端多模态对齐——将结构化场景先验注入到多模态决策模型中。实验表明，SKG-VLA 持续提升了基于政策的推理能力、投诉决策准确性、长尾泛化能力以及在证据不完整情况下的鲁棒性。

## Abstract
Decision making in large-scale complaint handling systems increasingly relies on heterogeneous evidence, including complaint narratives, screenshots, order metadata, historical interactions, and platform policies. Existing complaint understanding systems mainly perform shallow classification or template matching over isolated modalities, while underutilizing explicit scene structure, rule knowledge, and cross-evidence dependencies. To address this limitation, we present SKG-VLA for multimodal complaint decision making. The core idea is to model each case as a structured complaint scene and represent its decision-relevant semantics with a \emph{Scene Knowledge Graph} (SKG), which organizes complaint entities, evidence items, policy clauses, temporal events, transactional states, and action-relevant relations into a unified graph. Based on SKG, we build a data synthesis pipeline that generates complaint scene descriptions, rule-consistent graph generalizations, question-answer supervision, and decision recommendations. We further construct a large-scale complaint scene dataset with both text-only and multimodal in-domain benchmarks. Finally, we adopt a three-stage training strategy -- domain-adaptive pre-training, task-oriented instruction fine-tuning, and end-to-end multimodal alignment -- to inject structured scene priors into a multimodal decision model. Experiments show that SKG-VLA consistently improves policy-grounded reasoning, complaint decision accuracy, long-tail generalization, and robustness under incomplete evidence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SKG-VLA** 的框架，旨在通过引入“场景知识图谱”（Scene Knowledge Graph, SKG）作为先验知识，提升多模态大模型在复杂投诉处理场景中的决策能力。

以下是对该论文的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代在线服务平台的投诉处理涉及极其复杂的异构证据（如投诉文本、截图、订单元数据、历史记录和平台政策）。现有的系统通常只进行浅层的文本分类或简单的模态匹配，忽视了投诉案例中内在的**结构化场景语义**、**时间逻辑**和**政策约束**。
*   **研究动机**：投诉决策不仅要回答“发生了什么”，还要根据规则决定“该做什么”。缺乏显式结构化推理的模型容易产生看似合理但违反政策的建议。因此，作者提出将投诉案例视为一个“结构化场景”，并利用知识图谱来增强模型的推理鲁棒性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将投诉案例表示为 **场景知识图谱 (SKG)**，作为连接原始多模态证据与最终决策建议的中间语义表示。
*   **关键技术细节**：
    *   **SKG 构建**：图谱节点包含投诉实体、证据项、政策条款、时间事件、交易状态和决策节点；边表示它们之间的逻辑关系。
    *   **数据合成流水线**：
        1.  **解析与生成**：利用 LLM 从原始证据中提取实体和关系，生成初始 SKG 和场景描述。
        2.  **迭代验证**：引入验证循环，确保生成的图谱与原始证据、政策保持一致。
        3.  **规则约束下的图泛化**：通过编辑“强耦合节点”（如订单状态、证据有效性）生成符合逻辑的变体，从而扩充长尾场景数据。
    *   **三阶段训练策略**：
        1.  **领域自适应预训练 (DAPT)**：在投诉场景描述和 QA 数据上训练语言模型，使其内化领域语义。
        2.  **任务导向指令微调 (SFT)**：训练模型根据场景描述进行政策推理和决策。
        3.  **端到端多模态对齐**：连接视觉编码器，使模型能直接处理原始截图和元数据。

### 3. 实验设计
*   **数据集与场景**：
    *   **种子数据**：来自 CFPB（金融投诉）、JDDC/JDDC 2.0（电商客服）、MMD、SIMMC 2.0 等公开数据集。
    *   **自建基准**：构建了包含 18.3 万个基础案例、143 万个 SKG 和 QA 对的大规模数据集。
    *   **Benchmark**：定义了两个领域内基准：`ComplaintScene-Text`（纯文本推理）和 `ComplaintScene-MM`（多模态决策）。
*   **对比方法**：对比了不同参数规模（0.5B, 1.5B, 3B）的 Qwen2.5 基座模型，以及官方的通用多模态模型 Qwen2.5-VL-3B-Instruct。

### 4. 资源与算力
*   **硬件配置**：使用了 **2 个计算节点**，每个节点配备 **8 张 AMD Instinct MI308X GPU**（每张显卡拥有 192GB HBM 显存），处理器为 Intel Xeon Platinum 8575C。
*   **训练细节**：采用了 Megatron-LM 风格的分布式训练，使用了 AdamW 优化器、BF16 混合精度和 FlashAttention-2 技术。文中未明确给出总训练时长，但详细列出了各阶段的超参数（如学习率、序列长度等）。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从 0.5B 到 3B 三个量级的模型，验证了方法的可扩展性。
*   **充分性与客观性**：
    *   **多维度评估**：不仅测试了平均准确率，还专门针对**长尾/罕见投诉类型**、**证据缺失/损坏（鲁棒性）**进行了测试。
    *   **消融实验**：详细分析了 SKG 图结构、政策节点、多轮验证、图泛化等组件对性能的贡献。
    *   **跨领域迁移**：在公共数据集（CFPB, SIMMC 2.0, DocVQA）上进行了测试，证明了模型在保持通用能力的同时增强了专业决策能力。实验设计较为全面、客观。

### 6. 主要结论与发现
*   **结构化先验至关重要**：引入 SKG 显著提升了模型在责任认定和解决方案预测方面的表现，远超浅层分类方法。
*   **鲁棒性增强**：在证据不完整（如截图损坏或元数据缺失）的情况下，SKG-VLA 的性能下降幅度远小于基座模型，证明了结构化推理的稳定性。
*   **政策一致性**：通过图谱约束，模型生成的决策更符合平台政策，减少了逻辑矛盾。
*   **小模型潜力**：经过 SKG 增强的 1.5B 模型在某些任务上甚至能媲美未经优化的 3B 通用模型。

### 7. 优点（亮点）
*   **创新的表示形式**：将复杂的投诉处理抽象为“场景知识图谱”，为多模态决策提供了一个清晰的中间逻辑层。
*   **高质量合成数据**：提出了一套严谨的“生成-验证-泛化”流程，解决了投诉领域高质量标注数据稀缺的问题。
*   **实战导向**：实验设计紧贴真实业务痛点（如证据不全、政策复杂、长尾案例），具有很强的应用参考价值。

### 8. 不足与局限
*   **对 LLM 生成质量的依赖**：初始 SKG 的构建依赖于高性能 LLM，如果初始解析存在偏差，可能会影响后续的训练质量。
*   **计算开销**：三阶段训练和复杂的图泛化流程对算力要求较高，且推理时可能涉及复杂的图解析。
*   **领域局限性**：虽然在投诉领域表现优异，但该框架向其他非规则约束型决策场景（如开放式创意生成）的迁移效果尚未验证。
*   **动态政策更新**：论文未详细讨论当平台政策频繁变动时，如何高效地更新模型中的 SKG 先验。

（完）

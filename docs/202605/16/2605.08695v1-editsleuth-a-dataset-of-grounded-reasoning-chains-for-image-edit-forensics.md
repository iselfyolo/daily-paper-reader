---
title: "EditSleuth: A Dataset of Grounded Reasoning Chains for Image-Edit Forensics"
title_zh: EditSleuth：用于图像编辑取证的有依据推理链数据集
authors: "Van-Loc Nguyen, AprilPyone MaungMaung, Minh-Triet Tran, Isao Echizen"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08695v1"
tags: ["query:ds-gen"]
score: 8.0
evidence: "从现有语料库构建了257,725个图像编辑三元组的数据集"
tldr: EditSleuth是一个包含25.7万个图像编辑三元组的大规模数据集，旨在提升AI编辑图像取证的可解释性。该数据集不仅提供编辑定位和分类标签，还引入了基于确定性算法生成的六步推理链，确保了推理过程的忠实度。通过在Qwen2-VL模型上的实验，证明了该数据集在维持分类精度的同时，能生成具有视觉依据的解释性文本，为图像取证研究提供了新的基准。
selection_source: fresh_fetch
motivation: 现有的图像取证方法往往只提供二元预测，缺乏对编辑类型、位置及视觉证据的深度语义理解和可验证的推理过程。
method: 构建了一个包含25.7万个样本的数据集，通过确定性流水线生成包含掩码、12类编辑分类、难度评分及六步推理链的图像三元组。
result: 实验显示，推理链监督在分类准确率上与仅标签监督持平，且能额外提供标签监督无法产生的、具有视觉依据的解释性描述。
conclusion: EditSleuth通过提供确定性且可落地的推理链，为开发具备解释能力的图像编辑取证系统奠定了基础。
---

## 摘要
AI 编辑图像的取证分析不仅需要二元的真伪预测：一个实用的系统应当能够定位编辑区域、识别其语义类型，并将其决策建立在视觉证据之上。现有的图像取证数据集通常侧重于检测或定位，而受推理监督的视觉语言数据集很少针对图像篡改，且往往依赖于忠实度难以验证的 LLM 生成的合理解释。我们推出了 EditSleuth，这是一个包含 257,725 个图像编辑三元组的数据集，由现有的图像编辑语料库构建而成，旨在用于有依据的图像编辑取证推理。每个样本包括一张编辑后的图像、其源图像、一个二元编辑掩码、一个 12 类的编辑分类标签、一个难度评分以及一个六步推理链。EditSleuth 推理链是根据基于三元组的上游产物确定性地生成的，每条陈述都与特定的可计算证据源相关联。我们的分析表明，一种朴素的四分量难度公式在量级特征中存在秩-2 相关性崩溃；而简化的三分量公式显著增加了 Pico-Banana 和 MagicBrush 上的得分离散度。难度在大多数编辑类别中也存在显著差异，这表明该得分并非编辑类型的替代指标。作为初步的学习研究，我们使用 LoRA 对 Qwen2-VL-2B 进行了微调，并发现以推理链作为目标的监督在可解析答案的分类准确率上与仅标签基线相当，同时还能产生仅标签监督无法生成的有依据的解释性文本。我们发布了该数据集、确定性构建流水线以及试点训练脚本。

## Abstract
Forensic analysis of AI-edited images requires more than binary real-versus-fake prediction: a useful system should localize the edit, identify its semantic type, and ground its decisions in visual evidence. Existing image-forensics datasets typically emphasize detection or localization, while reasoning-supervised vision-language datasets rarely target image manipulation and often rely on LLM-generated rationales whose faithfulness is difficult to verify. We introduce EditSleuth, a dataset of 257,725 image-edit triplets constructed from existing image-editing corpora for grounded image-edit forensic reasoning. Each example includes an edited image, its source image, a binary edit mask, a 12-class edit taxonomy label, a difficulty score, and a six-step reasoning chain. EditSleuth chains are generated deterministically from triplet-grounded upstream artifacts, with each statement tied to a specific computable source of evidence. Our analysis reveals that a naive four-component difficulty formulation suffers from a rank-2 correlation collapse among magnitude features; a simplified three-component formulation substantially increases score dispersion on both Pico-Banana and MagicBrush. Difficulty also varies meaningfully within most edit categories, indicating that the score is not a proxy for edit type. As an initial learning study, we fine-tune Qwen2-VL-2B with LoRA and find that chain-as-target supervision matches a label-only baseline on classification accuracy among parseable answers, while additionally yielding grounded explanatory prose that label-only supervision cannot produce. We release the dataset, the deterministic construction pipeline, and pilot training scripts.

---

## 论文详细总结（自动生成）

### EditSleuth：用于图像编辑取证的有依据推理链数据集

#### 1. 核心问题与整体含义（研究动机和背景）
随着生成式 AI 技术的飞速发展，图像编辑已从简单的像素级篡改演变为复杂的、受指令驱动的语义修改。现有的图像取证技术面临两个主要瓶颈：
*   **缺乏解释性**：大多数系统仅提供二元的“真/伪”预测，无法定位编辑区域、识别编辑类型或提供视觉证据支持。
*   **推理数据不可靠**：现有的视觉语言模型（VLM）推理数据集往往依赖于大语言模型（LLM）生成的解释，这些解释往往未直接观察视觉证据，存在“幻觉”和不可审计的问题。
**EditSleuth** 的核心目标是构建一个大规模、可审计且具有视觉依据（Grounded）的推理链数据集，将图像取证从简单的分类提升为具备逻辑支撑的取证分析。

#### 2. 核心方法论
论文提出了一个包含五个阶段的**确定性构建流水线**，将现有的图像编辑三元组（原图、编辑图、指令）转化为取证监督数据：
*   **阶段 A（数据摄入）**：适配不同来源的语料库（如 Pico-Banana, MagicBrush）。
*   **阶段 B（掩码生成）**：结合 L*a*b* 颜色距离、SSIM 结构失真和 LPIPS 感知距离，通过多信号差异计算生成二元编辑掩码，并自动判定编辑范围（局部或全局）。
*   **阶段 C（难度评分）**：提出 V2 难度公式，结合结构变化（$s_{struct}$）、掩码紧凑度（$s_{compact}$）和指令复杂度（$s_{instr}$），解决了 V1 版本中特征相关性崩溃的问题。
*   **阶段 D（类别分类）**：建立 12 类编辑分类体系（如对象添加、属性更改、风格迁移等），通过标签映射或基于正则规则的指令分类器进行标注。
*   **阶段 E（推理链合成）**：**核心创新点**。根据前序阶段生成的客观产物（掩码面积、得分、类别等），通过模板确定性地合成六步推理链。这种方法确保了推理链中的每一条陈述都可追溯到具体的计算结果，而非模型臆造。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：基于 Pico-Banana 构建，包含 257,725 个三元组。
    *   **评估集**：使用 MagicBrush dev 分支（528 个三元组）进行跨指令风格的泛化性评估。
*   **Benchmark 与对比方法**：
    *   以 **Qwen2-VL-2B** 为基础模型，使用 LoRA 进行微调。
    *   对比了两种监督模式：**仅标签（Label-only）**（输出 JSON 格式的类别、范围、难度）和 **推理链目标（Chain-as-target）**（输出完整的六步推理文本）。
*   **消融实验**：针对难度评分公式（V1 vs V2）和掩码生成的信号组合（LAB, SSIM, LPIPS）进行了详细对比。

#### 4. 资源与算力
*   **模型配置**：使用了 Qwen2-VL-2B-Instruct 模型，采用 LoRA 微调（秩 $r=16$，$\alpha=32$）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到在试点研究中使用了约 20,000 个样本的子集进行训练，并进行了 1-2 个 Epoch 的迭代。

#### 5. 实验数量与充分性
*   **实验规模**：数据集规模达到 25.7 万，在同类研究中属于大规模。
*   **充分性**：
    *   **统计分析充分**：对难度分布、类别覆盖、掩码质量进行了深入的定量分析。
    *   **消融实验客观**：通过相关性分析解释了难度公式改进的必要性。
    *   **局限性验证**：通过试点微调实验，诚实地反映了小参数模型在遵循复杂格式（推理链）时的召回率挑战。
*   **公平性**：在对比实验中保持了相同的模型底座、超参数和训练步数预算。

#### 6. 主要结论与发现
*   **推理链监督的有效性**：在可解析的输出中，推理链监督的分类准确率与仅标签监督持平，且能额外提供具有视觉依据的解释性文本。
*   **难度评分的科学性**：V2 公式显著提升了得分的离散度（在 MagicBrush 上提升 94%），且难度得分与编辑类别并非强耦合，证明其可作为独立的课程学习维度。
*   **格式遵循瓶颈**：2B 规模的模型在生成长推理链时，格式遵循率（Recall）低于简单的 JSON 输出，且增加训练轮数未能显著改善，暗示需要更大规模的模型或专门的格式对齐训练。

#### 7. 优点
*   **忠实度高**：推理链由确定性算法生成，避免了 LLM 生成解释时常见的“对不上图”的问题。
*   **多信号融合**：掩码生成结合了颜色、结构和感知三个维度，比单一信号更鲁棒。
*   **分类体系完善**：12 类分类法涵盖了从局部对象编辑到全局风格迁移的广泛场景。
*   **开源贡献**：提供了完整的构建流水线和试点脚本，具有很高的复用价值。

#### 8. 不足与局限
*   **掩码质量依赖**：掩码基于像素差异而非语义分割，在纹理复杂的区域可能存在噪声或边缘粗糙。
*   **模型规模限制**：试点实验仅在 2B 模型上进行，推理链的格式遵循率尚不理想。
*   **生成器偏差**：训练数据主要源自单一的编辑模型（Pico-Banana），可能无法完全覆盖其他生成器产生的特定伪影。
*   **推理链的实时性**：模型在推理时是“估计”这些数值，而非实时调用算法计算，其推理出的数值与真实物理特征之间的漂移程度尚待进一步验证。

（完）

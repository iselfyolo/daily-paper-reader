---
title: "Falkor-IRAC: Graph-Constrained Generation for Verified Legal Reasoning in Indian Judicial AI"
title_zh: Falkor-IRAC：用于印度司法人工智能中经验证法律推理的图约束生成
authors: Joy Bose
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14665v1"
tags: ["query:profile-3"]
score: 10.0
evidence: 司法人工智能中经过验证的法律推理图约束生成
tldr: 针对印度司法AI中大模型易产生虚假先例和推理链断裂的问题，本文提出Falkor-IRAC框架。该框架将法律判决转化为基于IRAC（争议、规则、分析、结论）结构的知识图谱，并利用FalkorDB进行低延迟遍历。通过“验证者代理”对生成内容进行图路径校验，确保推理过程有据可查，并能主动识别法律冲突，显著提升了法律推理的准确性与可靠性。
selection_source: fresh_fetch
motivation: 传统的向量检索增强生成（RAG）无法准确表达法律推理中的符号逻辑和程序状态转换，导致法律AI频繁出现幻觉。
method: 构建基于IRAC结构的法律知识图谱，并引入验证者代理（Verifier Agent）通过图路径追踪来校验LLM生成的推理链和引用。
result: 在51份最高法院判决书的测试中，验证者代理成功校验了正确引用并拒绝了虚假引用，有效降低了幻觉率。
conclusion: 结合图约束生成的法律AI框架比纯向量RAG更适合处理复杂的法律推理任务，为司法公正提供了更可靠的技术保障。
---

## 摘要
法律推理并非语义相似度搜索。法院判决编码了受约束的符号推理：先例传播、程序状态转换以及受法规约束的推断。这些属性是基于向量的检索增强生成（RAG）无法忠实表示的。幻觉先例、过时的法规引用以及缺乏支持的推理链仍然是基于大语言模型（LLM）的法律人工智能中持续存在的失效模式，这对于印度等案件量巨大的司法管辖区的司法公正具有实际影响。本文提出了 Falkor-IRAC，这是一个针对印度法律人工智能的图约束生成框架，它将生成过程建立在 IRAC（争议焦点、法律依据、论证分析、结论）知识图谱的结构化推理之上。印度最高法院和高等法院的判决被摄取为 IRAC 节点结构，并丰富了程序状态转换、先例关系和法律引用，存储在 FalkorDB 中以实现低延迟的智能体遍历。在推理阶段，只有当能够通过图谱追踪到有效的支持路径时，LLM 生成的答案才会被接受，这一检查由名为“验证者智能体”（Verifier Agent）的可证伪性预言机执行。该系统还将法理冲突作为一等输出进行检测，而不是默默地解决它们。Falkor-IRAC 使用图原生指标进行评估：引用落地准确率、路径有效率、幻觉先例率和冲突检测率。据论证，这些指标比 BLEU 和 ROUGE 更适合法律推理评估。在包含 51 份最高法院判决的概念验证语料库上，验证者智能体正确验证了已完成查询中的引用，并正确拒绝了虚假引用。与仅使用向量的 RAG 基准的对比评估，以及旨在解决当前 CPU 硬件超时率问题的 GPU 加速推理，均留待未来工作。

## Abstract
Legal reasoning is not semantic similarity search. A court judgment encodes constrained symbolic reasoning: precedent propagation, procedural state transitions, and statute-bound inference. These are properties that vector-based retrieval-augmented generation (RAG) cannot faithfully represent. Hallucinated precedents, outdated statute citations, and unsupported reasoning chains remain persistent failure modes in LLM-based legal AI, with real consequences for access to justice in high-caseload jurisdictions such as India. This paper presents Falkor-IRAC, a graph-constrained generation framework for Indian legal AI that grounds generation in structured reasoning over an IRAC (Issue, Rule, Analysis, Conclusion) knowledge graph. Judgments from the Supreme Court and High Courts of India are ingested as IRAC node structures enriched with procedural state transitions, precedent relationships, and statutory references, stored in FalkorDB for low-latency agentic traversal. At inference time, LLM-generated answers are accepted only if a valid supporting path can be traced through the graph, a check performed by a falsifiability oracle called the Verifier Agent. The system also detects doctrinal conflicts as a first-class output rather than silently resolving them. Falkor-IRAC is evaluated using graph-native metrics: citation grounding accuracy, path validity rate, hallucinated precedent rate, and conflict detection rate. These metrics are argued to be more appropriate for legal reasoning evaluation than BLEU and ROUGE. On a proof-of-concept corpus of 51 Supreme Court judgments, the Verifier Agent correctly validated citations on completed queries and correctly rejected fabricated citations. Evaluation against vector-only RAG baselines is left for future work, as is GPU-accelerated inference to address current timeout rates on CPU hardware.

---

## 论文详细总结（自动生成）

### 论文总结：Falkor-IRAC —— 印度司法 AI 中的图约束验证生成框架

#### 1. 核心问题与整体含义（研究动机和背景）
法律推理本质上是受约束的符号推理，而非简单的语义相似度搜索。传统的基于向量的检索增强生成（RAG）在法律领域面临三大结构性失效：
*   **语义不等于法律相关性**：关键先例可能与当前案件词汇迥异，其相关性在于结构而非词义。
*   **幻觉先例的职业风险**：LLM 生成的虚假引用在法律实践中具有严重的负面后果。
*   **法律推理的逻辑状态性**：法律过程涉及程序状态的转换（如保释申请的不同阶段），RAG 缺乏对这种状态的建模。
针对印度司法系统案件积压严重（约 5000 万件）且法律援助匮乏的现状，论文提出了 **Falkor-IRAC** 框架，旨在通过图约束生成技术，确保 AI 生成的法律建议有据可查、逻辑严密且无幻觉。

#### 2. 方法论：核心思想与技术细节
Falkor-IRAC 的核心思想是将生成过程与验证过程分离，利用知识图谱作为“硬约束”来校验 LLM 的输出。
*   **IRAC 知识图谱建模**：将判决书解析为 **IRAC**（Issue 争议、Rule 规则、Analysis 分析、Conclusion 结论）结构。图谱包含三层：
    1.  **IRAC 层**：捕捉判决的核心逻辑。
    2.  **程序层**：通过 `TRIGGERS` 和 `PRECEDES` 关系建模诉讼流程（如保释申请的状态机）。
    3.  **先例层**：包含 `CONFLICTS_WITH`（冲突）、`OVERRULES`（废止）等具有法理意义的边。
*   **图约束生成架构**：
    1.  **检索代理**：在 FalkorDB 中执行多策略图遍历，获取路径引导的上下文。
    2.  **LLM 生成**：基于检索到的路径生成初步答案。
    3.  **验证者代理（Verifier Agent）**：作为“可证伪性预言机”，检查生成答案中的引用路径在图谱中是否存在。若路径不存在或指向已废止（STALE）的法律，则触发重写或拒绝回答。
*   **数学定义**：有效性 $Valid(c)$ 定义为在图 $G$ 中存在一条支持路径 $P_c$ 能够支撑主张 $c$。若不存在，则判定为幻觉。

#### 3. 实验设计
*   **数据集**：包含 51 份印度最高法院的判决书，涵盖从 1949 年到 2026 年的多个领域（保释、宪法、劳工法等）。
*   **场景与查询**：设计了 10 个典型法律查询（如保释再申请、非法解雇后的复职条件等）。
*   **Benchmark 与对比**：
    *   **模型对比**：对比了 TinyLlama (1B) 和 Mistral 7B-Instruct。
    *   **指标体系**：放弃了传统的 BLEU/ROUGE，提出了**图原生指标**，包括引用落地准确率（CGA）、路径有效率（PVR）、幻觉先例率（HPR）和冲突检测率（CDR）。

#### 4. 资源与算力
*   **硬件环境**：实验明确指出在**消费级 CPU 硬件**上运行（通过 Ollama 部署）。
*   **算力细节**：文中未提及具体的 GPU 型号或数量。由于使用 CPU 推理，导致了明显的性能瓶颈：在 10 个查询中，有 7 个因为超过 300 秒的超时限制而失败。
*   **数据库**：使用了 FalkorDB，利用其低延迟特性支持智能体的实时图遍历。

#### 5. 实验数量与充分性
*   **实验规模**：目前处于**概念验证（Proof-of-Concept）阶段**。仅使用了 51 份判决书和 10 个查询。
*   **充分性评价**：
    *   **验证逻辑充分**：通过手动构造虚假引用测试了验证者代理的拦截能力，证明了“硬约束”机制的有效性。
    *   **统计意义不足**：由于样本量较小且缺乏与成熟 Vector RAG 或 GraphRAG 的大规模基准对比，其泛化性能尚未得到充分验证。
    *   **客观性**：作者诚实地记录了 CPU 推理的超时率和图谱覆盖范围的局限性。

#### 6. 主要结论与发现
*   **验证者机制有效**：验证者代理能以 100% 的准确率识别语料库内的真实引用，并成功拦截所有虚假（幻觉）引用。
*   **冲突检测的重要性**：系统能主动识别并输出法理冲突（如两个同级法院的矛盾判决），而非盲目给出单一答案，这更符合法律实务需求。
*   **硬约束优于软约束**：在法律等高风险领域，基于图路径的“一票否决制”比 Constitutional AI 等软性引导更能防止误导性信息的传播。

#### 7. 优点（亮点）
*   **架构创新**：将知识图谱从单纯的“检索增强”提升为“生成约束”，引入了硬性的验证环节。
*   **法理深度**：不仅建模了判决内容，还建模了诉讼的程序状态机和复杂的先例冲突关系。
*   **诚实的 AI 设计**：引入了“弃权”机制和冲突提示，优先保证准确性而非生成长度。

#### 8. 不足与局限
*   **图谱覆盖有限**：验证能力受限于已摄取的 51 份判决，无法对语料库之外的法律事实进行校验。
*   **推理延迟高**：在 CPU 上运行导致极高的超时率，实用性受限，亟需 GPU 加速。
*   **提取噪声**：利用 LLM 自动提取 IRAC 结构可能引入噪声，尤其是处理复杂的宪法法院判决时。
*   **语言限制**：虽然设计了多语言支持，但目前的 PoC 阶段主要基于英文，尚未对印度本土语言进行大规模测试。

（完）

---
title: "SynSQL: Synthesizing Relational Databases for Robust Evaluation of Text-to-SQL Systems"
authors: "Mohammadamin Habibollah, Davood Rafiei"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27261v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 合成关系数据库用于评估
tldr: SynSQL 通过自然语言合成具有语义意义的关系数据，用于压力测试文本转 SQL 系统。
selection_source: fresh_fetch
motivation: 合成关系数据库用于评估。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Evaluating text-to-SQL systems remains largely fragile: correctness is typically judged by executing predicted and gold SQL queries on a single static database, even though the same queries may behave differently under alternative database instances. This raises a broader language modeling question: Can large language models synthesize semantically meaningful, schema-consistent relational data directly from a natural language question? If so, such generation can serve as a controlled mechanism for stress-testing text-to-SQL systems beyond fixed benchmark databases. We introduce SynSQL, a framework that synthesizes test databases conditioned on question-schema alignment rather than gold SQL queries. SynSQL decomposes the task into three stages: (1) schema selection, (2) question-guided data synthesis, and (3) constraint-aware critique with iterative refinement, framing database construction as structured generation under semantic and relational constraints. Across ten text-to-SQL models on Spider, BIRD, and Spider 2.0, SynSQL-generated databases reveal performance drops of 3-14% compared to static evaluation, exposing errors masked by benchmark artifacts. We further analyze generation quality, constraint adherence, and failure modes, highlighting both the promise and limitations of LLMs in structured data synthesis. Our findings position synthetic database generation as a new lens for studying LLM reasoning, controllability, and robustness in structured environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SynSQL**，一个旨在通过合成关系数据库来增强 Text-to-SQL 系统评估鲁棒性的框架。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的 Text-to-SQL 评估高度依赖于单一、静态的基准数据库。这种评估方式具有脆弱性，因为预测的 SQL 查询在特定数据实例上可能因为“巧合”而产生正确结果（假阳性），或者因为数据库中缺失特定边界情况而无法暴露错误。
*   **核心问题**：能否直接根据自然语言（NL）问题和数据库模式（Schema），利用大语言模型（LLM）合成语义相关且符合约束的关系数据，从而对 Text-to-SQL 系统进行压力测试？
*   **整体含义**：论文将数据库构建视为一种受控的结构化生成任务，旨在超越固定基准，提供一种更严格的鲁棒性评估手段。

### 2. 论文提出的方法论：SynSQL 框架
SynSQL 将数据库合成任务分解为三个模块化阶段：
1.  **模式选择（Schema Selection）**：
    *   **核心思想**：减少搜索空间。由于真实数据库包含大量表和列，该模块识别与 NL 问题相关的子集。
    *   **技术细节**：采用“集成-扩展”策略。通过不同温度设置多次查询 LLM 以获取核心元素（集成），随后扩展语义或功能相关的列（扩展），以确保高召回率。
2.  **问题引导的数据合成（Question-Conditioned Data Synthesis）**：
    *   **核心思想**：直接根据问题和简化模式生成元组，而非依赖标准答案（Gold SQL）。
    *   **技术细节**：LLM 生成符合列类型、表结构和外键约束的记录。通过轻量级后处理强制执行基础结构正确性（如处理 NULL 值、归一化等）。
3.  **约束感知评论与迭代优化（Constraint-Aware Critique & Refinement）**：
    *   **核心思想**：通过自我修正机制解决完整性冲突。
    *   **技术细节**：Critic 模块从提示对齐、键完整性、模式覆盖、数据复杂性、多样性和相关性六个维度对生成的数据进行 1-10 分的评分。如果得分低于阈值（如 8.0），则将反馈送回合成器进行迭代优化（最多 3 次）。

### 3. 实验设计
*   **数据集**：Spider（简单模式）、BIRD（复杂查询与 Join）、Spider 2.0-SQLite（企业级复杂工作流）。
*   **被评估的 Text-to-SQL 系统**：共 10 个竞争模型，包括 OmniSQL-32B、RSL-SQL、Alpha-SQL、CSC-SQL、Gemini-SQL、DIN-SQL、DAIL-SQL、Graphix-3B、C3 以及 GPT-5.4。
*   **对比基准（Baseline）**：
    *   **Original**：原始基准测试中的静态数据库。
    *   **Vanilla**：不含模式选择和 Critic 反馈的单次 LLM 合成方法。
*   **评估指标**：
    *   **成功率 (SR)**：Gold SQL 在合成库上产生非空结果的比例。
    *   **执行准确率 (EX)**：标准指标。
    *   **复合执行准确率 ($EX_c$)**：要求模型在原始库和合成库上同时产生正确结果才算正确。

### 4. 资源与算力
*   **模型使用**：论文使用了多种 LLM 家族，包括 GPT-4o-mini、Gemini-1.5-Flash、Gemini-1.5-Pro 和 Qwen-2.5-7B。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要基于推理（In-context Learning）和迭代生成，其开销主要体现在 API 调用成本和推理延迟上，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上测试了 10 个不同的 Text-to-SQL 模型，涵盖了从轻量级到企业级的多种场景。
*   **消融实验**：详细分析了 Critic 模块对成功率的影响，以及模式选择策略（如 Oracle 模式 vs. 自动选择）的有效性。
*   **充分性评价**：实验设计较为充分且客观。通过引入 $EX_c$ 指标，有效地量化了数据变动带来的性能波动。对失败案例（如模式遗漏、语义误解）的深入分析增强了结论的可信度。

### 6. 论文的主要结论与发现
*   **性能大幅下降**：在 SynSQL 生成的数据库上，所有 Text-to-SQL 模型的准确率相比原始静态库下降了 **3-14%**。
*   **排名变动**：在某些情况下（如 Spider 数据集），模型的相对排名发生了变化，说明固定基准掩盖了系统的系统性错误。
*   **合成质量高**：SynSQL 在 BIRD 上达到了 82.07% 的成功率，在 Spider 上甚至超过了原始人工数据库的对齐度（93.04%），且结构有效性接近 99%。
*   **鲁棒性漏洞**：实验证明，当前的 Text-to-SQL 系统对数据值的微小变化（如大小写、数据粒度）非常敏感。

### 7. 优点
*   **无需 Gold SQL**：与以往依赖标准 SQL 变异的方法不同，SynSQL 仅需问题和模式，更符合真实评估场景。
*   **迭代优化机制**：引入 Critic 模块显著提升了合成数据的逻辑严密性和多样性。
*   **极简且真实**：生成的数据库体积小（如 20KB vs 原始 21MB），但语义丰富，便于人工检查和验证。

### 8. 不足与局限
*   **模式选择风险**：在处理极复杂模式时，模式选择器可能会遗漏关键列，导致 Gold SQL 无法执行（成功率受限）。
*   **语义对齐难题**：LLM 在生成数据时可能无法完全匹配 Gold SQL 中隐含的特定约定（如特定的日期格式或大小写敏感度）。
*   **应用限制**：目前主要针对 SQLite 场景，对于更复杂的分布式数据库或非关系型约束的覆盖尚不明确。

（完）

---
title: "SecGoal: A Benchmark for Security Goal Extraction and Formalization from Protocol Documents"
title_zh: SecGoal：一个用于从协议文档中提取和形式化安全目标的基准测试
authors: "Dawei Huang, Hui Li, Haonan Feng, Jingjing Guan, Yueshuang Jiao, Bo Jia"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27601v1"
tags: ["query:q9"]
score: 9.0
evidence: 协议文档安全目标的形式化
tldr: "本研究针对从自然语言协议文档中自动提取和形式化安全目标的难题，推出了首个专家标注的基准测试集 SecGoal，涵盖 5G-AKA 和 TLS 1.3 等 15 个主流协议。同时提出了 AIFG 框架，通过上下文感知提取和检索增强形式化来分解任务。实验表明，虽然顶级大模型在召回率上表现尚可，但精确率极低；而经过 SecGoal 微调的小型模型在 F1 分数上能超过 80%，显著优于通用大模型，为自动化协议分析奠定了基础。"
selection_source: fresh_fetch
motivation: 自动化从自然语言协议中提取和形式化安全目标面临专家标注资源匮乏及非结构化文本与符号逻辑衔接困难的挑战。
method: 提出了首个专家标注的基准 SecGoal 及 AIFG 框架，将任务分解为上下文感知的目标提取和检索增强的形式化过程。
result: "实验发现通用大模型精确率普遍低于 15%，而经过 SecGoal 指令微调的 7B/9B 参数模型 F1 分数可达 80% 以上。"
conclusion: 本研究通过提供高质量数据集和基准框架，证明了专用微调模型在自动化形式化协议分析领域的巨大潜力。
---

## 摘要
形式化验证为密码学安全提供了严格的保证，然而，从自然语言协议文档中自动提取和形式化安全目标仍然是一个主要瓶颈，专家标注资源的匮乏以及连接非结构化文本与符号逻辑的集成框架的缺失加剧了这一问题。我们推出了 SecGoal，这是首个专家标注的基准测试，涵盖了 15 个广泛部署的协议文档（包括 5G-AKA 和 TLS 1.3），以及 AIFG，一个将任务分解为上下文感知目标提取和检索增强形式化的 AI 辅助框架。我们进行了全面评估，以评估当代大语言模型（LLM）是否已准备好自动化这一流程。我们的结果揭示了显著的精确率-召回率失衡：前沿模型（如 Gemini 2.5-Pro）实现了高召回率，但精确率低于 15%，经常将操作性文本误分类为安全目标。相比之下，在 SecGoal 上进行指令微调使 7B/9B 参数的紧凑型模型能够实现 80% 以上的 F1 分数，显著优于更大的通用模型。我们的工作为自动化形式化协议分析建立了一个基础数据集和可复现的基准。

## Abstract
Formal verification provides rigorous guarantees for cryptographic security, yet automating the extraction and formalization of security goals from natural language protocol documents remains a major bottleneck, compounded by the scarcity of expert-annotated resources and integrated frameworks bridging unstructured text and symbolic logic. We introduce SecGoal, the first expert-annotated benchmark covering 15 widely deployed protocol documents, including 5G-AKA and TLS 1.3, and AIFG, an AI-assisted framework that decomposes the task into context-aware goal extraction and retrieval-augmented formalization. We conduct a comprehensive evaluation to assess whether contemporary LLMs are ready to automate this pipeline. Our results reveal a pronounced precision-recall imbalance: frontier models, such as Gemini 2.5-Pro, achieve high recall but precision below 15%, frequently misclassifying operational text as security goals. In contrast, instruction tuning on SecGoal enables compact models with 7B/9B parameters to achieve F1-scores above 80%, substantially outperforming larger general-purpose models. Our work establishes a foundational dataset and reproducible baseline for automated formal protocol analysis.

---

## 论文详细总结（自动生成）

这是一份关于论文《SecGoal: A Benchmark for Security Goal Extraction and Formalization from Protocol Documents》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：形式化验证（如使用 Tamarin 或 ProVerif 工具）是保障密码学协议安全的关键，但其核心瓶颈在于**“规范到模型的鸿沟”**。目前，从非结构化的自然语言协议文档（如 IETF RFC）中提取安全目标（如机密性、认证性）并将其转化为符号逻辑描述，主要依赖领域专家的手工劳动，效率低下且难以扩展。
*   **核心问题**：现有的 LLM 在代码生成和数学推理方面表现出色，但在处理长篇幅、高稀疏度（安全目标仅占文档内容的不到 9%）且逻辑严密的协议文档时，是否存在自动化提取和形式化的能力？目前缺乏高质量的专家标注数据集和统一的评估框架来回答这一问题。

### 2. 方法论：核心思想与技术细节
论文提出了 **AIFG (AI-assisted Framework for Goal extraction and formalization)** 框架，将任务解耦为两个阶段：
*   **阶段 I：安全目标提取（Mext）**
    *   **语义过滤**：针对协议文档中安全目标极其稀疏的特点，采用滑动窗口和上下文感知分段策略。
    *   **微调策略**：使用**随机负采样（Randomized Negative Downsampling）**对小型开源模型进行指令微调，通过 1:3 的正负样本比例强制模型学习精确的决策边界，以区分“安全目标”与“操作性描述”。
*   **阶段 II：安全目标形式化（Mform）**
    *   **检索增强生成（RAG）**：利用提取的目标作为查询，从文档中检索相关的实体定义（如 Nonce、密钥），解决自然语言中的指代模糊问题。
    *   **符号对齐**：结合预定义的协议消息流（Protocol Flow）和形式化模板（JSON Schema），将自然语言描述映射为精确的符号逻辑，确保变量命名与形式化模型一致。

### 3. 实验设计
*   **数据集 (SecGoal)**：首个专家标注的基准数据集，涵盖 15 个主流协议（如 5G-AKA、TLS 1.3、OAuth 2.0、FIDO2 等），包含约 100 个形式化属性和 300-400 个自然语言目标语句。
*   **对比方法**：
    *   **前沿闭源模型**：GPT-4o, Gemini 2.5-Pro, Gemini 2.5-Flash。
    *   **开源大模型**：Qwen3-235B, DeepSeek-V3.2 (含 Thinking 版本), GLM-4.6。
    *   **微调模型**：基于 SecGoal 微调的 Gemma2-9B-FT 和 Qwen2.5-7B-FT。
*   **评估指标**：提取阶段使用 Precision、Property-level Recall 和 F1-score；形式化阶段使用意图匹配 F1 和语义槽准确率（Acc slot）。

### 4. 资源与算力
*   **硬件环境**：实验在**华为昇腾（Ascend）NPU (snt9b3)** 节点上运行，配备 24 核 ARM 处理器和 192GB RAM。
*   **软件框架**：使用 LlamaFactory 进行 LoRA 微调，使用 RAGFlow 构建形式化流水线。
*   **训练细节**：微调周期为 20-30 个 Epoch，学习率在 8e-6 到 1.5e-5 之间，采用 bfloat16 精度。

### 5. 实验数量与充分性
*   **实验规模**：对 15 个协议进行了详尽的提取测试，并在两个典型协议（Kao Chow v1 和 NSSK）上验证了形式化生成能力。
*   **客观性与公平性**：
    *   采用了严格的**文档级划分（Document-level split）**，测试集协议（如 SPDM, OPC-UA）在训练阶段完全不可见，验证了模型的泛化能力。
    *   通过多轮独立运行（3次）并报告均值和标准差，确保了结果的可重复性。
    *   标注过程引入了两位形式化验证背景的博士进行独立标注，并由资深仲裁员解决分歧，Jaccard 一致性达 65.83%。

### 6. 主要结论与发现
*   **精确率-召回率悖论**：通用大模型（如 Gemini 2.5-Pro）在提取任务中表现出极高的召回率（>96%），但精确率极低（<15%），倾向于将大量操作性文本误判为安全目标。
*   **微调的优越性**：经过 SecGoal 微调的 7B/9B 小型模型在 F1 分数上突破了 80%，显著优于参数量大得多的通用模型（如 GPT-4o 的 F1 仅约 30%）。
*   **推理模型的局限**：带有思维链（CoT）的推理模型（如 DeepSeek-Thinking）在处理此类任务时容易“过度推理”，产生更多的假阳性（False Positives）。
*   **形式化能力**：AIFG 框架在已知协议流的情况下，能够以极高的稳定性（Recall=1.0）和准确率（Acc slot≈0.95）完成符号化转换。

### 7. 优点
*   **填补空白**：填补了 NLP 与形式化验证交叉领域缺乏专家级标注数据集的空白。
*   **任务解耦**：将复杂的端到端任务分解为提取和形式化两步，有效降低了幻觉风险并提高了符号一致性。
*   **实用性强**：证明了在特定领域，高质量的小规模专业数据比单纯增加模型参数量更有效。

### 8. 不足与局限
*   **依赖文档质量**：如果协议文档本身未明确描述某些安全目标，框架无法凭空生成（需结合外部专家知识）。
*   **未完全闭环**：目前仍需人工提供协议消息流（Protocol Flow），尚未实现从原始文本到可执行验证代码的完全端到端自动化。
*   **评估范围限制**：形式化生成的定量评估仅在两个较简单的协议上进行，对于超大规模协议（如 5G-AKA）的复杂逻辑转换仍需进一步验证。
*   **领域覆盖**：尚未涵盖后量子安全（Post-Compromise Security）等新兴领域的特殊逻辑结构。

（完）

---
title: "Naamah: A Large Scale Synthetic Sanskrit NER Corpus via DBpedia Seeding and LLM Generation"
title_zh: Naamah：通过 DBpedia 种子引导与大语言模型生成的大规模合成梵文命名实体识别语料库
authors: "Akhil Rajeev P, Annarao Kulkarni"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26456v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 利用大语言模型生成合成训练数据
tldr: 针对梵文命名实体识别标注资源匮乏的问题，本研究推出了包含10万余条句子的合成数据集Naamah。该方法结合DBpedia实体提取与24B参数推理模型生成技术，确保了古典语法的准确性与数据多样性。通过对XLM RoBERTa等模型的基准测试，证明了该数据集在推动梵文数字化方面的价值。
selection_source: fresh_fetch
motivation: 梵文命名实体识别面临标注数据稀缺以及现有大模型在处理复杂古典语法时准确性不足的挑战。
method: "结合DBpedia实体种子与24B参数混合推理模型，生成了包含102,942条句子的语法自然且多样化的合成训练数据。"
result: 成功构建了高质量的Naamah银标准数据集，并以此对XLM RoBERTa和IndicBERTv2两种架构进行了性能基准测试。
conclusion: Naamah数据集通过合成高质量的训练数据，有效解决了梵文NER资源匮乏问题，为古典文献的数字化处理提供了重要支持。
---

## 摘要
经典梵文文献的数字化受到标注资源匮乏的阻碍，特别是在命名实体识别（NER）方面。虽然最近的方法利用通用大语言模型（LLMs）进行数据增强，但这些方法仍然容易出错，且往往缺乏经典语法所需的推理深度。在这项工作中，我们介绍了 Naamah，这是一个包含 102,942 个句子的高质量银标准梵文 NER 数据集。我们提出了一种方法，将从 DBpedia 提取实体与 24B 参数混合推理模型的生成能力相结合，以创建语法自然且合成多样化的训练数据。我们利用该数据集对两种 Transformer 架构进行了基准测试：大规模多语言 XLM RoBERTa 和参数高效的 IndicBERTv2。

## Abstract
The digitisation of classical Sanskrit literature is impeded by a scarcity of annotated resources, particularly for Named Entity Recognition. While recent methodologies utilise generic Large Language Models (LLMs) for data augmentation, these approaches remain prone to error and often lack the reasoning depth required for classical grammar. In this work, we introduce Naamah, a high quality silver standard Sanskrit NER dataset comprising 102,942 sentences. We propose a methodology that combines entity extraction from DBpedia with the generative capabilities of a 24B parameter hybrid reasoning model to create grammatically natural and synthetically diverse training data. We utilize this dataset to benchmark two transformer architectures: the massive multilingual XLM RoBERTa and the parameter efficient IndicBERTv2.

---

## 论文详细总结（自动生成）

以下是对论文《Naamah: A Large Scale Synthetic Sanskrit NER Corpus via DBpedia Seeding and LLM Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：梵文（Sanskrit）作为一种具有深厚历史背景的语言，在自然语言处理（NLP）领域面临严重的**标注资源匮乏**（Low-resource）问题，特别是命名实体识别（NER）任务。
*   **研究背景**：
    *   **语言复杂性**：梵文具有极高的形态丰富性，包括复杂的变格（Vibhakti）系统（一个实体可能有超过24种形式）和连音（Sandhi）现象（词与词之间音韵融合），导致实体边界模糊。
    *   **标注成本高**：手动标注梵文需要极高的专业领域知识，导致现有数据集规模小、领域单一且类别不平衡。
    *   **现有方法局限**：跨语言投影（Cross-lingual projection）会引入对齐噪声；通用大模型（LLM）在处理古典语法时往往缺乏深度和准确性。

### 2. 方法论
论文提出了一种结合结构化知识库与生成式大模型的方法来构建“银标准”（Silver Standard）数据集：
*   **DBpedia 种子提取**：利用 SPARQL 查询从 DBpedia 中提取“人名（PER）”、“地名（LOC）”和“组织机构名（ORG）”。为了增强模型的泛化能力，不仅包含印度传统实体，还引入了转写为天城文的全球实体（如外国政要名），防止模型仅靠记忆词汇而非理解语法来识别实体。
*   **LLM 生成管线**：使用 **Sarvam-M**（一个针对印度语言优化的 24B 参数混合推理模型）。通过 Prompt 将提取的实体种子嵌入到语义连贯的梵文句子中。这种方法允许自然产生正确的变格和句法结构，比基于模板的方法更具多样性。
*   **启发式预处理**：通过 Python 脚本对生成的原始数据进行过滤，检查标签一致性、格式错误和边界歧义，最终保留了 102,942 个高质量句子。
*   **标注方案**：采用标准的 BIO 标注体系。

### 3. 实验设计
*   **数据集**：Naamah 数据集（102,942 条句子），划分为训练集（92,647 条）和验证集（10,295 条）。
*   **基准模型（Benchmarks）**：
    1.  **XLM-RoBERTa Base**：强大的多语言基线模型，拥有 250k 词汇量，但预训练数据中梵文占比极少。
    2.  **IndicBERTv2 (MLM Only)**：针对印度语言优化的轻量化模型（约 130 MB），具有专门针对印度语系脚本优化的分词器。
*   **评估指标**：精确率（Precision）、召回率（Recall）、F1 分数和准确率（Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 Hugging Face Trainer 进行微调。XLM-RoBERTa 训练了 3 个 Epoch，IndicBERTv2 训练了 4 个 Epoch；Batch Size 均为 16；学习率分别设为 $2 \times 10^{-5}$ 和 $3 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：基于超过 10 万条句子的规模进行训练和验证，这在梵文 NER 领域属于大规模实验。
*   **充分性评价**：实验对比了通用多语言模型与领域特定模型，并进行了定性的“失败模式分析”（Failure Mode Analysis）。虽然模型种类不算多，但针对分词器（Tokenizer）对形态学处理的影响进行了深入探讨，实验设计较为客观且具有针对性。

### 6. 主要结论与发现
*   **性能对比**：**IndicBERTv2 优于 XLM-RoBERTa**。IndicBERTv2 达到了 0.9615 的 F1 分数，而 XLM-RoBERTa 为 0.9506。
*   **分词器的关键作用**：研究发现，对于梵文这种高度合成的语言，**分词器的语言对齐比模型参数规模更重要**。
    *   XLM-RoBERTa 的通用分词器会将复杂的梵文词汇（如带后缀的实体）切碎，导致模型将后缀误识别为独立实体（如将地名的后缀识别为组织机构）。
    *   IndicBERTv2 的分词器能更好地保留语义完整性，处理连音和变格时更加稳健。

### 7. 优点
*   **创新的数据生成路径**：结合了结构化知识库（DBpedia）的准确性和推理模型（Sarvam-M）的灵活性，有效解决了低资源语言的数据瓶颈。
*   **高质量合成数据**：生成的句子语法自然，避开了僵硬的模板化问题。
*   **深入的定性分析**：不仅给出了量化指标，还通过具体案例解释了分词碎片化对 NER 任务的负面影响，为后续研究提供了指导。

### 8. 不足与局限
*   **缺乏金标准验证**：目前仅在合成的“银标准”数据上进行了验证，缺乏在真实历史文献（金标准）上的测试结果。
*   **合成数据偏差**：数据集可能继承了 LLM 的偏见或特定的生成风格，可能无法完全覆盖古典文献中极其复杂的连音（Sandhi）变体。
*   **实体类别有限**：目前仅涵盖 PER、LOC、ORG 三类，对于古典文献中重要的“作品名”、“神祇名”等细分类别尚未涉及。

（完）

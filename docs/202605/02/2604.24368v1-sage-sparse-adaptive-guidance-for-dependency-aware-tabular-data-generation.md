---
title: "SAGE: Sparse Adaptive Guidance for Dependency-Aware Tabular Data Generation"
title_zh: SAGE：用于依赖感知型表格数据生成的稀疏自适应引导
authors: "Shuo Yang, Zheyu Zhang, Bardh Prenkaj, Gjergji Kasneci"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24368v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 基于LLM的新型合成表格数据生成框架
tldr: 本研究针对大语言模型在生成表格数据时存在的伪相关和静态依赖问题，提出了SAGE框架。SAGE通过将特征离散化为值感知的伪特征，并构建基于互信息的稀疏依赖图，实现了动态且稀疏的生成引导。该方法通过上下文选择或逻辑值修正，使模型能聚焦于真正相关的特征，在提升合成数据保真度的同时显著增强了下游任务的实用性。
selection_source: fresh_fetch
motivation: 现有的LLM表格生成方法因密集建模和静态依赖假设，容易引入伪相关并忽略特征间随数值变化的动态关系。
method: SAGE通过离散化特征构建基于互信息的稀疏依赖图，并利用该图进行显式上下文选择或隐式逻辑值修正来引导生成。
result: "实验表明SAGE在多个数据集上将F1分数提升了10%，并有效减少了策略违规，显著优于现有的LLM生成方法。"
conclusion: 该研究强调了自适应结构在表格数据生成中的关键作用，并为实现LLM的上下文敏感控制提供了有效路径。
---

## 摘要
生成高保真合成表格数据仍然是提高隐私敏感和低资源领域数据可用性的关键挑战。最近的方法通过将表格行表示为序列来利用大语言模型（LLMs），但存在两个基本局限性：(1) 它们对特征依赖关系进行密集建模，从而引入了伪相关性；(2) 它们假设特征之间存在静态关系，忽略了这些依赖关系如何随特征值而变化。为了克服这些局限性，我们引入了 SAGE（稀疏自适应引导），这是一种新型的基于 LLM 的生成框架，它强制执行稀疏且动态的依赖引导。SAGE 将特征离散化为值感知的伪特征，并构建一个基于互信息的稀疏依赖图。该图通过显式上下文选择或隐式 Logit 修正自适应地引导生成，使 LLM 在合成过程中能够专注于真正相关的信息。我们在六个数据集和多个任务上进行的广泛实验表明，与之前的基于 LLM 的方法相比，SAGE 不仅提高了数据保真度和下游效用（将 F1 分数提升了 10%），还将策略违规减少了一个百分点。这些结果强调了自适应结构在表格数据生成中的重要性，并为 LLM 的上下文敏感控制提供了新的见解。

## Abstract
Generating high-fidelity synthetic tabular data remains a critical challenge for enhancing data availability in privacy-sensitive and low-resource domains. Recent approaches leverage LLMs by representing table rows as sequences, yet suffer from two fundamental limitations: (1) they model feature dependencies densely, introducing spurious correlations; and (2) they assume static relationships between features, ignoring how these dependencies vary with feature values. To overcome these limitations, we introduce SAGE (Sparse Adaptive Guidance), a novel LLM-based generation framework that enforces sparse and dynamic dependency guidance. SAGE discretizes features into value-aware pseudo-features and constructs a mutual information-based sparse dependency graph. This graph adaptively guides generation through explicit context selection or implicit logit correction, enabling LLMs to focus on truly relevant information during synthesis. Our extensive experiments across six datasets and multiple tasks reveal that SAGE not only improves data fidelity and downstream utility, boosting F1 scores by 10% compared to previous LLM-based methods, but also reduces policy violations by one point. These results highlight the importance of adaptive structure in tabular data generation and provide new insights into context-sensitive control of LLMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SAGE (Sparse Adaptive Guidance)** 的新型表格数据生成框架，旨在解决大语言模型（LLM）在处理表格数据时存在的结构性缺陷。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 LLM 的表格生成方法（如 GReaT）虽然利用了预训练知识，但存在两个根本局限：
    1.  **密集建模（Dense Modeling）**：LLM 的全注意力机制假设所有特征间都有关联，这在本质上是稀疏的表格数据中会引入“伪相关”。
    2.  **静态依赖假设**：现有方法假设特征间的关系是固定的，忽略了**值感知（Value-aware）**的动态依赖。例如，“贷款用途”为“教育”时，与“年龄”的相关性极强；而用途为“住房”时，则更依赖“收入”和“就业状态”。
*   **研究背景**：在医疗、金融等隐私敏感领域，高质量合成数据的需求巨大，但如何生成既符合逻辑又具备高保真度的表格数据仍是挑战。

### 2. 方法论：SAGE 框架
SAGE 的核心思想是通过**互信息（Mutual Information, MI）**构建动态、稀疏的依赖图来引导 LLM 生成。
*   **特征离散化（伪特征构建）**：
    *   将数值特征通过分箱（Binning）转化为二进制伪特征。
    *   将类别特征的每个类别视为独立的伪特征。
*   **互信息依赖矩阵**：在预处理阶段计算伪特征间的 MI，捕捉细粒度的值级相关性。
*   **两种引导策略**：
    1.  **特征选择器（Feature Selector - 显式引导）**：在生成目标特征时，根据 MI 阈值 $\tau$ 过滤掉不相关的上下文，只让模型关注真正相关的特征值。
    2.  **逻辑值修正（Logit Correction - 隐式引导）**：根据当前上下文的平均 MI 与全局平均 MI 的偏差 $\Delta$，动态调整输出层 Logit 的分布（平滑或锐化），从而调节生成置信度。
*   **工程优化**：
    *   **仅值损失（Value-only Loss）**：在微调时只计算特征值的 Loss，跳过模板文字（如 "feature is"），提升训练效率。
    *   **拒绝采样（Rejection Sampling）**：通过约束解码空间，确保生成的数值和类别在合法范围内。

### 3. 实验设计
*   **数据集**：涵盖 6 个代表性数据集，包括二分类（Adult, HELOC, MIC）、多分类（Iris, Diabetes）和回归任务（California Housing）。
*   **Benchmark 与对比方法**：
    *   **传统生成模型**：TVAE, CTGAN。
    *   **扩散模型**：Tab-Syn。
    *   **基于 LLM 的模型**：GReaT, GRADE, SPADA。
*   **评估指标**：下游任务实用性（F1, ACC, MAPE）、数据保真度（违规率）、真实性（SVM 判别器准确率）和隐私保护（DCR 距离）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练与推理时长**：附录中提供了时间对比。例如，在 Adult 数据集上，SAGE 的训练时间约为 15 分钟（远快于 GReaT 的 6 小时），单样本采样时间从 9 秒缩短至 0.2 秒。这主要归功于“仅值损失”优化和稀疏上下文。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个不同规模和领域的任务上进行了主实验。
    *   进行了消融实验，探讨了 MI 阈值 $\tau$ 对性能的影响。
    *   测试了不同 LLM 后端（GPT-2, Qwen-3, Llama-3）的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了从分类到回归、从低维到高维（MIC 数据集含 110 个特征）的场景。通过统计显著性检验（p < 0.05）证明了改进的有效性，对比基准包含了当前最先进的扩散模型和 LLM 模型，具有较高的客观性。

### 6. 主要结论与发现
*   **性能提升**：SAGE 在下游任务中表现优异，尤其在小数据集（如 Iris）上避免了 GReaT 的过拟合问题，F1 分数最高提升了 10%。
*   **保真度增强**：在地理边界约束（California Housing）和语义逻辑约束（Adult）下，SAGE 的违规率显著低于基线模型。
*   **动态引导的价值**：实验证明，显式特征选择适合噪声过滤，而隐式 Logit 修正更擅长处理复杂的空间依赖。

### 7. 优点与亮点
*   **动态性**：首次在 LLM 表格生成中引入了随特征值变化的动态依赖建模。
*   **效率高**：通过优化 Loss 计算和上下文稀疏化，大幅降低了 LLM 生成表格时的计算开销和延迟。
*   **可解释性**：利用互信息作为引导机制，比纯黑盒的注意力机制更具统计学依据。
*   **鲁棒性**：在不同规模的数据集和不同的 LLM 底座上均表现稳定。

### 8. 不足与局限
*   **高阶依赖缺失**：目前主要基于**成对（Pairwise）**互信息构建依赖图，可能无法完全捕捉多个特征共同作用的高阶复杂交互（尽管 LLM 自身可能在一定程度上弥补这一点）。
*   **预处理开销**：对于特征维度极高（如数万维）的数据集，计算全量伪特征 MI 矩阵的预处理成本可能会显著增加。
*   **超参数敏感性**：虽然论文给出了推荐值，但 MI 阈值 $\tau$ 和修正系数 $\lambda$ 在不同领域数据集上仍需微调以达到最优效果。

（完）

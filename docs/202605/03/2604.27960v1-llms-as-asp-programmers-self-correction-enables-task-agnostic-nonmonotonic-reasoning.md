---
title: "LLMs as ASP Programmers: Self-Correction Enables Task-Agnostic Nonmonotonic Reasoning"
title_zh: 大语言模型作为 ASP 程序员：自我修正实现任务无关的非单调推理
authors: "Adam Ishay, Joohyung Lee"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27960v1"
tags: ["query:profile-3"]
score: 8.0
evidence: 非单调推理与形式逻辑转换
tldr: 本研究针对大语言模型在复杂逻辑推理中的局限性，提出了一种通用的LLM+ASP框架。该框架将自然语言转化为回答集程序（ASP），利用非单调逻辑处理人类认知中的可撤销推理。通过引入基于求解器反馈的自动自我修正循环，该系统无需针对特定任务进行工程设计即可在多样化推理任务中表现出色。实验证明，该方法在非单调任务上显著优于基于SMT的方案，且紧凑的上下文参考比冗长文档更有效。
selection_source: fresh_fetch
motivation: 现有神经符号方法多依赖单调逻辑，难以处理人类认知中常见的非单调推理及高复杂度逻辑问题。
method: 提出一种无需特定领域工程的LLM+ASP框架，通过ASP求解器的结构化反馈实现自动迭代自我修正。
result: 在六个基准测试中，该框架在非单调推理任务上显著优于SMT方案，且自我修正机制有效替代了手工知识。
conclusion: 稳定模型语义使LLM能自然表达默认规则，且研究揭示了冗长上下文会导致约束遵循能力下降的“上下文腐烂”现象。
---

## 摘要
最近的大语言模型（LLMs）在推理方面取得了令人瞩目的里程碑，但在高计算成本、逻辑不一致以及高复杂度问题上的性能急剧下降方面仍面临挑战。虽然神经符号方法试图通过将 LLMs 与符号推理器结合来缓解这些问题，但现有方法通常依赖于单调逻辑（如 SMT），无法表示可废止推理（defeasible reasoning）——这是人类认知的核心组成部分。我们提出了“LLM+ASP”，这是一个将自然语言转换为回答集程序设计（ASP）的框架，ASP 是一种基于稳定模型语义的非单调形式化方法。与以往需要手动编写知识模块、特定领域提示或仅限于单一问题类别评估的“LLM+ASP”方法不同，我们的框架无需任何针对特定任务的工程设计，并可统一应用于各种推理任务。我们的系统利用自动化自我修正循环，其中来自 ASP 求解器的结构化反馈可实现迭代优化。通过在六个不同的基准测试中进行评估，我们证明了：(1) 稳定模型语义允许 LLMs 自然地表达默认规则和异常，在非单调任务上显著优于基于 SMT 的替代方案；(2) 迭代自我修正是性能提升的主要驱动力，有效地取代了对手工领域知识的需求；(3) 紧凑的上下文参考指南显著优于冗长的文档，揭示了“上下文腐烂”（context rot）现象，即过多的上下文会阻碍对约束的遵守。

## Abstract
Recent large language models (LLMs) have achieved impressive reasoning milestones but continue to struggle with high computational costs, logical inconsistencies, and sharp performance degradation on high-complexity problems. While neuro-symbolic methods attempt to mitigate these issues by coupling LLMs with symbolic reasoners, existing approaches typically rely on monotonic logics (e.g., SMT) that cannot represent defeasible reasoning -- essential components of human cognition. We present "LLM+ASP," a framework that translates natural language into Answer Set Programming (ASP), a nonmonotonic formalism based on stable model semantics. Unlike prior "LLM+ASP" approaches that require manually authored knowledge modules, domain-specific prompts, or evaluation restricted to single problem classes, our framework operates without any per-task engineering and applies uniformly across diverse reasoning tasks. Our system utilizes an automated self-correction loop where structured feedback from the ASP solver enables iterative refinement. Evaluating across six diverse benchmarks, we demonstrate that: (1) stable model semantics allow LLMs to naturally express default rules and exceptions, outperforming SMT-based alternatives by significant margins on nonmonotonic tasks; (2) iterative self-correction is the primary driver of performance, effectively replacing the need for handcrafted domain knowledge; (3) compact in-context reference guides substantially outperform verbose documentation, revealing a "context rot" phenomenon where excessive context hinders constraint adherence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LLM+ASP** 的神经符号框架，旨在通过结合大语言模型（LLM）的自然语言处理能力与回答集程序设计（ASP）的严谨逻辑推理能力，解决复杂且非单调的推理任务。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管当前的“推理型”LLM（如 OpenAI o1, DeepSeek R1）在多步推理上取得了进展，但仍面临计算成本高、逻辑不一致以及在极高复杂度问题上性能骤降的问题。
*   **核心挑战**：现有的神经符号方法大多依赖**单调逻辑**（如 SMT 或一阶逻辑），无法处理人类认知中常见的**非单调推理**（即新信息的加入可能导致之前的结论被撤销，例如“鸟会飞”但在已知“是企鹅”后撤销该结论）。
*   **整体含义**：论文提出了一种任务无关（Task-Agnostic）的框架，将 LLM 作为 ASP 程序员，通过自动化的反馈循环解决逻辑冲突和异常处理，无需为每个任务手动编写规则或提示词。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用 LLM 将自然语言描述转化为 ASP 代码，通过外部求解器（Clingo）执行，并根据求解器的反馈（错误信息或不合理的答案集）进行自我修正。
*   **关键技术细节**：
    *   **LLM+ASP 管道**：包含生成器（Generator）、ASP 求解器（Clingo）和格式化器。
    *   **自动化自我修正循环**：如果 ASP 代码语法错误或逻辑不符，求解器的反馈会传回给 LLM，指令其进行“UPDATE”；若 LLM 判断结果正确则执行“PASS”。
    *   **紧凑参考指南（Compact Reference）**：为了解决“上下文腐烂”（Context Rot）现象（即过长的文档反而降低模型遵循约束的能力），研究者将 2.2 万词的官方手册精简为 2.6 千词的精华指南。
    *   **非单调语义**：利用 ASP 的“稳定模型语义”自然地表达默认规则（Default Rules）和异常（Exceptions）。

### 3. 实验设计
*   **数据集/场景**：
    *   **约束满足**：ZebraLogic (XL 和自定义的更难的 XXL 版本)、SudokuBench（非常规数独变体）。
    *   **规划任务**：Mystery Blocksworld（混淆后的积木世界）。
    *   **非单调推理**：MultiLogicNMR（涵盖默认推理和规则冲突）、BoardgameQA（带有矛盾信息的推理）。
*   **对比方法（Baselines）**：
    *   **独立 LLM**：Gemini 2.5 Pro/Flash, OpenAI o4-mini, DeepSeek R1/V3。
    *   **LLM+SMT**：将框架中的 ASP 替换为 SMT 求解器进行对比。

### 4. 资源与算力
*   **硬件环境**：实验在配备 Intel Core i9-9900K CPU、NVIDIA RTX 2080 Ti GPU 和 32GB RAM 的机器上运行。
*   **算力消耗**：本地硬件仅用于运行 Clingo 求解器，LLM 推理通过 API 完成。
*   **成本说明**：核心实验的直接 API 费用约为 3,800 美元，总开发与测试支出估计为 4,200 美元。文中未明确说明具体的训练时长，因为主要依赖现成的预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个基准测试上进行了广泛评估，每个任务包含数百个问题，并进行了 3 次试验取平均值。
*   **充分性与客观性**：
    *   **消融实验**：对比了“无参考”、“标准参考”与“紧凑参考”的影响，以及“有无自我修正循环”的差异。
    *   **公平性**：在对比 LLM+SMT 时，确保两者都在相同的零样本（Zero-shot）设置下运行，不使用特定任务的工程优化。
    *   实验设计覆盖了从简单逻辑到 NP-Hard 问题的梯度，具有较高的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：LLM+ASP 的平均准确率达到 78.0%，比独立 LLM（51.3%）提升了 52.1%。在最难的 ZL-XXL 任务上，独立模型几乎崩溃（0%），而该框架仍能保持高准确率。
*   **自我修正是核心**：迭代修正使性能从 54.6% 提升至 78.0%，是框架成功的首要驱动力。
*   **ASP 的优越性**：在非单调推理任务上，ASP 显著优于 SMT（SMT 在此类任务上表现接近随机猜测），证明了稳定模型语义的必要性。
*   **上下文腐烂现象**：研究发现，提供 1/8 长度的精简参考指南比提供完整手册效果更好，因为冗长信息会干扰模型对核心约束的关注。

### 7. 优点
*   **任务无关性**：无需为不同领域设计特定的 Prompt 或知识模块，具有极强的通用性。
*   **非单调处理能力**：成功填补了神经符号推理在处理“可撤销逻辑”方面的空白。
*   **效率优势**：LLM 生成的是紧凑的代码而非直接搜索空间，随着问题复杂度增加，其 Token 利用效率远高于直接推理。

### 8. 不足与局限
*   **模型依赖性**：框架表现高度依赖底层 LLM 的基础能力，非推理模型（如 DeepSeek V3）在修正循环中表现较差。
*   **不可满足性（Unsat）调试难**：当生成的程序无解时，求解器提供的反馈极少，导致 LLM 难以定位逻辑矛盾。
*   **应用范围限制**：目前主要集中在形式化逻辑和规划任务，对于模糊、开放式或纯自然语言理解类的 NLP 任务适用性尚待验证。

（完）

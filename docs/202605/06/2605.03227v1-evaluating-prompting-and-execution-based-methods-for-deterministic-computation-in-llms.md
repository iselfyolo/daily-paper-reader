---
title: Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs
title_zh: 评估大语言模型中确定性计算的提示词与基于执行的方法
authors: Hongkun Yu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03227v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 包含自然语言指令的合成数据集，用于确定性计算
tldr: 本研究探讨了大语言模型在执行精确确定性计算任务（如二进制计数和算术运算）中的表现。通过在自建合成数据集上系统评估思维链（CoT）、程序思维（PoT）等多种提示策略，发现传统提示法在处理复杂逻辑时易出错，而 PoT 通过生成可执行代码并调用外部解释器实现了完美准确率。此外，微调的小型专用模型也能高效完成此类任务。研究表明，对于确定性计算，将 LLM 与外部工具结合或使用专用模型比单纯依赖提示词工程更可靠。
selection_source: fresh_fetch
motivation: 旨在系统评估大语言模型在处理需要零误差的精确确定性计算任务时的真实能力。
method: 引入合成数据集并对比了 CoT、由易到难分解、PoT 及自一致性等多种提示策略与模型微调的效果。
result: 传统提示策略受限于误差累积，而 PoT 方法和经过微调的小型 CodeT5 模型在所有测试任务中均达到了完美准确率。
conclusion: 大模型在符号计算上存在局限，通过外部代码执行工具或专用模型辅助是实现确定性计算的最优路径。
---

## 摘要
大语言模型（LLMs）在自然语言理解和推理方面展现出强大的能力。然而，它们执行精确、确定性计算的能力仍不明确。在这项工作中，我们系统地评估了多种提示策略，包括思维链（CoT）、由简入繁分解（Least-to-Most）、程序思维（PoT）和自洽性（SC），在需要精确且无误输出的任务上的表现，这些任务包括二进制计数、最长子串检测和算术求值。为了支持这项研究，我们引入了一个包含多样化自然语言指令的合成数据集，从而能够对跨多种任务类型的精确计算进行受控评估。我们的结果表明，标准的提示方法在基于序列的任务上仅达到中等准确率。CoT 提供的改进有限，而 Least-to-Most 则受到错误累积的影响。相比之下，PoT 通过生成可执行代码并将计算委托给外部解释器，实现了完美的准确率。自洽性通过多数投票提高了鲁棒性，但带来了巨大的计算开销。我们进一步训练了一个小型领域特定模型（CodeT5-small）来生成可执行程序，该模型在所有任务的留出合成测试数据上均以极低的训练成本实现了完美准确率。总的来说，我们的发现表明，LLMs 可能是在模拟推理模式，而非可靠地执行精确的符号计算。对于确定性任务，将 LLMs 与外部工具结合或使用专门模型提供了一种更可靠且高效的解决方案。

## Abstract
Large Language Models (LLMs) have demonstrated strong capabilities in natural language understanding and reasoning. However, their ability to perform exact, deterministic computation remains unclear. In this work, we systematically evaluate multiple prompting strategies, including Chain-of-Thought (CoT), Least-to-Most decomposition, Program-of-Thought (PoT), and Self-Consistency (SC), on tasks requiring precise and error-free outputs, including binary counting, longest substring detection, and arithmetic evaluation. To support this study, we introduce a synthetic dataset with diverse natural language instructions, enabling controlled evaluation of exact computation across multiple task types. Our results show that standard prompting methods achieve only moderate accuracy on sequence-based tasks. CoT provides limited improvement, while Least-to-Most suffers from error accumulation. In contrast, PoT achieves perfect accuracy by generating executable code and delegating computation to an external interpreter. Self-Consistency improves robustness through majority voting, but incurs substantial computational overhead. We further train a small domain-specific model (CodeT5-small) to generate executable programs, which achieves perfect accuracy on held-out synthetic test data across all tasks with minimal training cost. Overall, our findings suggest that LLMs may simulate reasoning patterns rather than reliably perform exact symbolic computation. For deterministic tasks, combining LLMs with external tools or using specialized models provides a more reliable and efficient solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Prompting and Execution-Based Methods for Deterministic Computation in LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在自然语言理解上表现卓越，但其在执行**精确、确定性计算**（如计数、符号操作、算术运算）方面的能力仍存疑。这类任务要求零误差，而 LLM 本质上是概率性的。
*   **核心问题**：现有的提示词策略（如 CoT、PoT 等）能否让 LLM 可靠地完成确定性计算？是依赖模型内部推理更好，还是将其作为代码生成器并调用外部工具更好？

### 2. 方法论
论文对比了五种提示策略和一种微调模型方法：
*   **提示策略（Prompting Strategies）**：
    *   **Plain Prompt**：直接要求模型给出答案。
    *   **Chain-of-Thought (CoT)**：引导模型生成中间推理步骤。
    *   **Least-to-Most**：将复杂问题分解为子问题（如分段计数再求和）并顺序解决。
    *   **Program-of-Thought (PoT)**：要求模型生成 Python 代码，通过外部解释器执行获取结果。
    *   **Self-Consistency (SC)**：对同一问题进行多次采样（5次），通过多数投票确定最终答案。
*   **微调模型方法**：
    *   **CodeT5-small**：训练一个小型专用模型，将自然语言指令直接映射为可执行的 Python 程序。
*   **执行机制**：对于 PoT 和 CodeT5，生成的代码在受限的 Python 环境中运行，禁止危险操作（如文件 I/O），仅捕获标准输出作为答案。

### 3. 实验设计
*   **数据集**：构建了一个包含 3000 个样本的合成数据集（每类任务 1000 个），包含多样化的自然语言指令。
*   **任务类型**：
    1.  **二进制计数**：统计 80-120 位二进制序列中 0 和 1 的数量。
    2.  **最长子串检测**：在包含字母、数字和符号的序列中寻找特定字符的最长连续长度。
    3.  **算术求值**：计算包含 3-6 个数字及四则运算的表达式（结果均为整数）。
*   **对比基准**：使用 `gpt-oss-120b` 模型作为提示词评估的基础模型，以 **Exact-match Accuracy（精确匹配准确率）** 为衡量指标。

### 4. 资源与算力
*   **微调模型**：使用 `CodeT5-small`，在单个 GPU 上训练。
*   **训练时长**：总训练时间约为 **3 分钟**（6 个 Epoch）。
*   **LLM 推理**：论文未明确说明运行 `gpt-oss-120b` 的具体 GPU 型号和数量，但提到 Self-Consistency 方法由于需要多次 API 调用，计算开销显著增加。

### 5. 实验数量与充分性
*   **实验规模**：
    *   单任务评估（二进制计数）：100 个样本（SC 为 50 个）。
    *   混合任务评估：150 个样本（每类 50 个；SC 为 90 个）。
    *   微调训练：使用了 3000 个样本的 90% 进行训练。
*   **充分性与公平性**：实验涵盖了从简单算术到复杂序列处理的不同难度梯度。通过使用多种自然语言指令变体，避免了模型对特定模板的记忆效应。实验设计较为客观，但在 LLM 提示词评估上样本量相对较小（受限于 API 成本）。

### 6. 主要结论与发现
*   **任务难度分级**：算术运算（100% 准确） $\ll$ 二进制计数 $<$ 最长子串。
*   **提示词局限性**：CoT 对确定性计算的提升微乎其微；Least-to-Most 表现最差，因为中间步骤的错误会产生累积效应。
*   **执行胜过推理**：**PoT 达到了 100% 的准确率**。这表明 LLM 擅长编写逻辑代码，但不擅长在脑中模拟执行这些逻辑。
*   **小模型优势**：经过微调的 CodeT5-small 同样达到了 100% 准确率，证明了在确定性任务上，小型专用模型比通用大模型更高效、更可靠。

### 7. 优点
*   **系统性对比**：全面覆盖了主流的推理增强提示策略。
*   **数据集设计**：合成数据集包含了边缘情况（如全 0 序列）和多样化指令，具有较好的鲁棒性测试价值。
*   **实用性建议**：明确了对于确定性任务，应优先选择“LLM 生成代码 + 外部执行”的架构。

### 8. 不足与局限
*   **任务复杂度有限**：实验任务虽然具有代表性，但仍属于定义明确的良构问题，未涉及现实世界中带有噪声或长程逻辑的复杂计算。
*   **样本量限制**：由于成本原因，LLM 提示词测试的样本量（150个）远小于数据集总量（3000个），可能存在一定的统计偏差。
*   **模型单一性**：主要评估了 `gpt-oss-120b`，未对比其他主流闭源模型（如 GPT-4）或更小规模的开源模型在提示词下的表现差异。

（完）

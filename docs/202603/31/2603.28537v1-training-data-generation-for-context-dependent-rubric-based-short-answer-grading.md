---
title: Training data generation for context-dependent rubric-based short answer grading
title_zh: 面向上下文相关的基于评分标准的短答题评分的训练数据生成
authors: "Pavel Šindelář, Dávid Slivka, Christopher Bouma, Filip Prášil, Ondřej Bojar"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28537v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 为自动评分创建大规模训练数据集
tldr: 本研究针对PISA等国际测试中自动评分系统对大规模领域特定数据的需求，提出了一种利用少量机密参考数据生成大规模训练集的方法。通过采用简单的派生文本格式来确保数据机密性，研究者成功构建了三个代理数据集。实验表明，这些数据集在特征上比纯提示生成的更接近真实数据，且其中一种方法能有效提升自动评分模型的训练效果，为解决教育评估中的数据稀缺问题提供了新思路。
selection_source: fresh_fetch
motivation: 自动评分系统需要大量领域特定数据，但获取大规模且符合机密性要求的真实学生答案数据非常困难。
method: 利用少量机密参考数据，通过简单的派生文本格式和生成技术，构建了三个大规模代理数据集以保护隐私。
result: 生成的代理数据集在表面特征上比纯提示生成的数据更接近参考数据集，且初步实验显示其能改善模型训练。
conclusion: 该数据生成方法为解决自动评分中的数据稀缺和隐私保护问题提供了有效途径，具有实际应用潜力。
---

## 摘要
每四年，经合组织（OECD）都会进行 PISA 测试，以评估全球青少年的知识水平，并实现教育系统间的比较。然而，由于必须避免语言差异和标注者偏见，对学生答案进行评分具有挑战性。出于这些原因，比较自动学生答案评分的方法将具有重要意义。为了训练其中一些需要机器学习的方法，或者为那些不需要机器学习的方法计算参数或选择超参数，需要大量的领域特定数据。在这项工作中，我们探索了几种仅以相对较小的机密数据集为参考来创建大规模训练数据集的方法，并利用一组非常简单的派生文本格式来保护机密性。利用这些方法，我们成功创建了三个代理数据集，至少从表面上看，它们比纯粹基于提示生成的结果更接近参考数据集。初步实验表明，其中一种方法可能还有助于改进模型训练。

## Abstract
Every 4 years, the PISA test is administered by the OECD to test the knowledge of teenage students worldwide and allow for comparisons of educational systems. However, having to avoid language differences and annotator bias makes the grading of student answers challenging. For these reasons, it would be interesting to compare methods of automatic student answer grading. To train some of these methods, which require machine learning, or to compute parameters or select hyperparameters for those that do not, a large amount of domain-specific data is needed. In this work, we explore a small number of methods for creating a large-scale training dataset using only a relatively small confidential dataset as a reference, leveraging a set of very simple derived text formats to preserve confidentiality. Using these methods, we successfully created three surrogate datasets that are, at the very least, superficially more similar to the reference dataset than purely the result of prompt-based generation. Early experiments suggest one of these approaches might also lead to improved model training.

---

## 论文详细总结（自动生成）

### 论文总结：面向上下文相关的基于评分标准的短答题评分训练数据生成

#### 1. 核心问题与研究背景
*   **研究动机**：自动短答题评分（Short Answer Grading, SAG）对于像 PISA 这样的国际大规模教育评估至关重要，但面临两大挑战：一是高质量、带评分标准（Rubric）的领域特定数据极度匮乏；二是现有的真实学生数据（如 OECD 的 PISA 数据）受非披露协议（NDA）和隐私法规保护，无法公开共享或直接用于大规模模型训练。
*   **核心问题**：如何在不违反机密性的前提下，利用少量受限的参考数据，生成大规模且在分布上接近真实数据的“代理数据集”（Surrogate Dataset），以支持自动评分模型的开发和优化。

#### 2. 方法论
论文提出了一套结合大语言模型（LLM）生成与特征匹配子集选择的方法：
*   **合成候选数据生成（多阶段流水线）**：
    *   **上下文提取**：从公开网站抓取文本，清洗并切分为 150-800 词的片段。
    *   **问题生成**：LLM 根据上下文生成 1-5 个开放式问题，并通过另一个 LLM 作为“法官”进行质量过滤（确保逻辑性、非主观性）。
    *   **评分标准生成**：LLM 生成“满分、部分得分、无分”三个等级的细则，并引入迭代反馈机制（Judge LLM 提供批评并修正，最多 10 次迭代）。
    *   **答案生成与评分**：模拟高中生在压力下的写作风格（含拼写错误），为每个等级生成 5 个候选答案。
*   **特征提取**：提取了 16 种语言和语义特征，包括 BGE-M3 嵌入余弦相似度、n-gram 召回率、答案长度、词汇密度、TF-IDF 相似度等。
*   **子集选择策略**：
    *   **方法一**：选择与机密数据特征均值最接近（L2 距离）的前 5% 样本。
    *   **方法二**：引入领域覆盖和 K-means 聚类（8 个中心），试图解决数据多样性不足的问题。
    *   **方法三（非还原特征法）**：不使用均值，而是计算合成样本在特征空间中与参考样本的邻近排名（Rank），选择最接近真实样本分布的数据。

#### 3. 实验设计
*   **数据集**：以 OECD 提供的机密 PISA 英语子集为参考，生成大规模合成数据集。
*   **评估场景**：
    *   **E1（少样本评估）**：使用 Qwen3-8B 模型，对比从全量合成集与子集选择后的数据中抽取 Few-shot 示例对机密数据的评分准确率。
    *   **E2（模型选择效用）**：测试子集是否能作为机密数据的可靠代理，用于超参数选择或模型架构筛选。
    *   **E3（模型微调）**：使用 ModernBERT-base-nli 进行微调，对比随机采样与方法三选择的子集在机密测试集上的表现。
*   **基准（Benchmark）**：全量合成数据集、随机采样的子集以及机密参考数据集的真实表现。

#### 4. 资源与算力
*   **生成模型**：使用了开源权重的 **GPT OSS 120b** 模型进行所有合成数据的生成和评审。
*   **微调模型**：ModernBERT-base-nli。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量，但提到使用了 e-INFRA CZ 项目提供的计算资源，并在微调过程中使用了 32 个样本的梯度累积。

#### 5. 实验数量与充分性
*   **实验规模**：针对三种选择方法分别进行了评估。方法三进行了多次重复实验（改变训练集顺序）以验证统计显著性。
*   **充分性评价**：初步实验较为全面，涵盖了 Few-shot、模型选择和全量微调三个维度。然而，作者也承认大多数评估仅基于单次运行，缺乏在多种不同规模模型和更多样化数据集上的广泛验证。

#### 6. 主要结论与发现
*   **方法三最有效**：只有利用“非还原特征”进行排名选择的方法三在统计上显著提升了模型性能（平均准确率提升约 0.0116 - 0.0206，p < 0.05）。
*   **简单均值匹配失效**：方法一和方法二（基于特征均值匹配）未能提升模型在机密数据上的表现，甚至在某些情况下导致性能下降，说明简单的统计平均不足以捕捉复杂的语言分布。
*   **表面相似性不等于实用性**：虽然生成的代理数据集在长度等表面特征上更接近真实数据，但要真正提升评分性能，需要更深层的语义对齐。

#### 7. 优点
*   **隐私保护**：提出了一种在不泄露原始机密文本的前提下，利用派生特征（DTFs）优化合成数据质量的框架。
*   **质量控制**：在生成流水线中引入了“生成-评审-修正”的多阶段循环，确保了合成评分标准的逻辑严密性。
*   **实用性强**：为教育评估领域在受限数据环境下的研究提供了一种可行的替代方案。

#### 8. 不足与局限
*   **特征表达能力有限**：目前的特征集（如长度、n-gram）可能仍过于简单，无法完全刻画复杂的评分逻辑。
*   **实验覆盖面窄**：主要针对英语数据和特定模型（ModernBERT, Qwen），尚未验证在多语言或更大规模模型上的泛化能力。
*   **计算成本**：使用 120B 规模的模型进行多阶段生成和评审，对算力有较高要求，且迭代修正过程可能较为耗时。

（完）

---
title: "BenchBench: Benchmarking Automated Benchmark Generation"
title_zh: BenchBench：自动化基准测试生成的基准测试
authors: "Yandan Zheng, Haoran Luo, Zhenghong Lin, Wenjin Liu, Luu Anh Tuan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20807v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 使用大语言模型自动生成基准测试数据集
tldr: 针对现有LLM基准测试易饱和、易污染且更新成本高的问题，本文提出BenchBench框架，旨在评估模型生成基准测试的能力。该框架包含提取领域卡片、生成配额受控套件及多模型验证三个阶段。通过在计算机、医学等九个领域生成1.5万个核心题目，研究发现模型的基准设计能力与其答题能力仅呈中度相关，并揭示了模型在设计过程中的偏见与质量差异，为自动化评估提供了新视角。
selection_source: fresh_fetch
motivation: 静态基准测试面临饱和、污染和高昂的更新成本，且LLM裁判存在偏见，因此需要评估模型设计基准的能力。
method: 提出包含领域卡片提取、多模型设计生成及多模型交叉验证的三个阶段流水线，构建了涵盖多学科、多模态的BenchBench数据集。
result: 生成了约1.5万个高质量题目，发现模型的设计能力与答题能力相关性较低（Spearman rho ~0.37），且题目无效性与区分度呈负相关。
conclusion: BenchBench实现了对自动化基准生成质量的规模化审计，证明了评估模型“出题”能力对于理解模型综合性能的重要性。
---

## 摘要
基准测试是追踪大语言模型（LLMs）进展的事实标准，但静态测试集会迅速饱和，容易受到数据污染，且更新成本高昂。对开放式题目的可扩展评估通常依赖 LLM 裁判，这引入了额外的偏见来源和提示词敏感性。我们认为，评估不应仅局限于模型回答基准测试的表现，还应扩展到模型设计基准测试的能力。我们推出了 BenchBench，这是一个用于评估自动化基准测试生成的阶段性流水线和数据集：(i) 从种子基准测试中提取结构化的领域卡片；(ii) 提示多个设计者 LLM 生成配额受控的测试套件；(iii) 利用多模型答题者小组对题目进行验证，尽可能使用精确/数值/符号验证器，否则使用基于评分细则的评判，从而产生带有题目级质量标记和心理测量诊断的设计者-答题者矩阵。在涵盖计算机科学、数学、医学和心智理论推理（包括多语言和多模态设置）的九个变体中，我们生成了 1.67 万个题目，过滤后保留了约 1.5 万个核心题目，并产生了约 15.2 万条经过评分的模型-题目响应。BenchBench 表明，基准测试设计能力与答题能力仅呈中度相关（Spearman rho ~0.37），无效性与区分度呈负相关（Pearson r ~ 0.62），且生成的设计者-答题者矩阵能够对格式/模态/语言保真度以及依赖于套件的自身/家族交互进行可扩展的审计。项目地址：https://github.com/koanatakiyo/BenchBench。

## Abstract
Benchmarks are the de facto standard for tracking progress in large language models (LLMs), yet static test sets can rapidly saturate, become vulnerable to contamination, and are costly to refresh. Scalable evaluation of open-ended items often relies on LLM judges, introducing additional sources of bias and prompt sensitivity. We argue that evaluation must extend beyond how well models answer benchmarks to how well models design them. We introduce BenchBench, a three-stage pipeline and dataset for benchmarking automated benchmark generation: (i) extract structured domain cards from seed benchmarks, (ii) prompt multiple designer LLMs to generate quota-controlled suites, and (iii) validate items with a multi-model answerer panel using exact/numeric/symbolic verifiers when possible and rubric-guided judging otherwise, yielding designer--answerer matrices with item-level quality flags and psychometric diagnostics. Across nine variants spanning computer science, mathematics, medicine, and theory-of-mind reasoning (including multilingual and multimodal settings), we generate 16.7K items, retain ~15K core items post-filtering, and produce ~152K graded model--item responses. BenchBench shows that benchmark-design ability is only moderately correlated with answer-time strength (Spearman rho ~0.37), invalidity is negatively associated with discrimination (Pearson r~0.62), and the resulting designer--answerer matrices enable scalable audits of format/modality/language fidelity and suite-dependent self/family interactions. The project is available at: https://github.com/koanatakiyo/BenchBench.

---

## 论文详细总结（自动生成）

以下是对论文《BenchBench: Benchmarking Automated Benchmark Generation》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的静态基准测试（Benchmarks）面临三大挑战：**饱和快**（模型性能迅速达到上限）、**易污染**（测试数据泄露到训练集）以及**更新成本高**。此外，依赖 LLM 作为裁判（LLM-as-a-judge）会引入偏见和提示词敏感性。
*   **研究动机**：作者认为评估不应只看模型“答题”好坏，更应看模型“出题”的能力。基准测试设计是一种**元能力（Meta-capability）**，要求模型理解知识的深度、控制难度、预判错误模式并确保题目具有区分度。
*   **整体含义**：提出 BenchBench 框架，通过评估“模型设计基准测试”的质量，实现自动化评估流程的规模化审计和元评估。

### 2. 论文提出的方法论
BenchBench 采用三阶段流水线：
1.  **阶段 1：领域卡片提取（Domain Card Extraction）**：从现有的种子基准测试中提取结构化的领域卡片（YAML 格式），包含本体结构（超类/子类）、核心术语、模态和语言约束。这确保了生成过程是受控的，而非盲目随机生成。
2.  **阶段 2：配额受控生成（Controlled Generation）**：
    *   **设计者角色**：多个 LLM 作为“出题人”。
    *   **控制变量**：通过配额控制题目的难度等级（L1-L5）、设计意图（标准 vs. 对抗性）、题目格式（单选、多选、开放式、结构化）。
3.  **阶段 3：面板验证与评分（Panel Validation & Scoring）**：
    *   **答题者面板**：由一组固定的模型对生成的题目进行解答。
    *   **客观优先评分**：建立评分层级，优先使用精确匹配、数值或符号验证器；仅在必要时（如开放式题目）使用基于评分细则（Rubric）的 LLM 裁判。
    *   **质量过滤**：通过静态和动态检查（如模型一致性、歧义检测）过滤掉无效题目，形成“核心题目集”。
*   **核心产出**：生成**设计者-答题者矩阵（Designer-Answerer Matrix）**，用于分析有效性、区分度、排名一致性及家族偏见。

### 3. 实验设计
*   **数据集/场景**：涵盖 4 大领域、9 个变体：
    *   计算机科学（CSBench EN/ZH）
    *   数学（WeMath 文本/多模态）
    *   医学（MedXpertQA 文本/多模态）
    *   心智理论（ToMBench EN/ZH）
*   **模型设置**：
    *   **设计者（Designers）**：GPT-5-mini, Gemini-2.5-flash, DeepSeek-chat, Qwen3-next, Doubao-seed, Llama-4-maverick 等。
    *   **答题者（Answerers）**：包含上述模型及其他变体共 10-12 个模型。
*   **对比方法**：通过计算生成基准与种子基准（Seed）的排名一致性（Kendall’s $\tau$）来验证生成质量。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要基于现有的商业和开源模型 API 进行推理、生成和评测，不涉及大规模模型预训练，因此算力消耗主要体现在 API 调用成本和推理时间上。

### 5. 实验数量与充分性
*   **实验规模**：生成了 **16,669** 个原始题目，过滤后保留约 **1.5 万** 个核心题目，产生了超过 **15.2 万** 条评分响应。
*   **充分性**：
    *   覆盖了多语言（中英）和多模态（文本/图像）。
    *   进行了人类审计（150 个样本），验证自动过滤机制的可靠性。
    *   分析了 8 个研究问题（RQ），涵盖了从设计能力相关性到家族偏见等多个维度。
*   **客观性**：采用了多模型面板交叉验证和客观评分优先原则，减少了单一模型作为裁判带来的偏见。

### 6. 论文的主要结论与发现
1.  **设计能力 $\neq$ 答题能力**：模型的设计能力与其答题强度仅呈中度相关（Spearman $\rho \approx 0.37$），说明“出题”是一项独立的元能力。
2.  **无效性与区分度负相关**：题目越容易出错（Broken% 高），其区分模型水平的能力就越差（Pearson $r \approx -0.62$）。
3.  **家族偏见存在但有限**：观察到模型在回答“自家”设计的题目时可能存在微弱优势，但并非普遍现象。
4.  **多模态挑战**：在医学等领域，引入视觉信息能显著改变题目的诊断效用，但也增加了生成无效题目的风险。
5.  **排名一致性**：高质量的设计者生成的题目能很好地保持已有的模型排名稳定性。

### 7. 优点
*   **视角新颖**：将 LLM 评估从“解题”转向“命题”，为解决基准测试饱和问题提供了可持续的路径。
*   **流程严谨**：通过领域卡片和配额控制，避免了 LLM 生成题目时常见的随机性和同质化问题。
*   **分析深入**：引入了心理测量学指标（如区分度、点二系列相关），使评估更具科学性。
*   **开源贡献**：提供了完整的流水线、领域卡片和生成的 15 万条响应数据。

### 8. 不足与局限
*   **种子依赖性**：领域卡片的提取仍依赖于现有的种子基准，可能继承原始数据的偏差或覆盖不足。
*   **裁判可靠性**：虽然优先使用客观评分，但仍有约 22% 的题目依赖 LLM 裁判，存在潜在的评分偏见。
*   **领域覆盖**：目前仅覆盖 4 个领域，法律、创意写作、物理等复杂领域尚未涉及。
*   **模型版本**：实验中使用的部分模型名称（如 GPT-5-mini, Llama-4）在论文发布时可能属于预测性命名或特定实验版本，其普适性需随模型更新进一步验证。

（完）

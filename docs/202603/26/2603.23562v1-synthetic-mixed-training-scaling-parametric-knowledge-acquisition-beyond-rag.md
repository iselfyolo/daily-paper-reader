---
title: "Synthetic Mixed Training: Scaling Parametric Knowledge Acquisition Beyond RAG"
title_zh: 合成混合训练：将参数化知识获取扩展至超越 RAG 的水平
authors: "Seungju Han, Konwoo Kim, Chanwoo Park, Benjamin Newman, Suhas Kotha, Jaehun Jung, James Zou, Yejin Choi"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23562v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 用于扩展大模型知识获取的合成数据增强
tldr: 针对合成数据在知识获取中效果受限且难以超越RAG的问题，本文提出“合成混合训练”框架。该方法通过结合合成问答对与合成文档，利用两者的互补信号，实现了随数据量和生成器强度增加的对数线性性能提升。此外，引入“焦点重写”技术增强文档多样性。实验表明，该方法在多个长文档基准测试中超越了RAG，为模型参数化知识获取提供了新路径。
selection_source: fresh_fetch
motivation: 现有的合成数据增强方法在扩展时收益递减，且性能通常无法突破检索增强生成（RAG）的上限。
method: 提出结合合成问答对与合成文档的混合训练方案，并利用焦点重写技术根据特定问题生成多样化的合成文档。
result: "在QuaLITY等多个基准测试中，该方法训练的Llama 8B模型性能超过了RAG，最高相对提升达4.4%。"
conclusion: 合成混合训练能有效提升模型的参数化知识获取能力，打破了RAG的性能天花板，具有良好的扩展性。
---

## 摘要
合成数据增强有助于语言模型在数据受限的领域学习新知识。然而，通过在更多合成 token 上训练或使用更强大的生成器来盲目扩展现有的合成数据方法，其收益会逐渐递减，且性能低于 RAG。为了突破 RAG 的上限，我们引入了合成混合训练（Synthetic Mixed Training），它结合了合成问答（QA）和合成文档。这利用了它们互补的训练信号，并使得随着合成数据量和生成器强度的增加，性能能够实现对数线性（log-linear）的提升。这使得模型在长文档阅读理解基准测试 QuaLITY 上比 RAG 取得了 2.6% 的相对增益。此外，我们还引入了焦点重写（Focal Rewriting），这是一种简单的合成文档生成技术，它显式地以特定问题为条件进行文档生成，从而提高了合成文档的多样性，并产生了更陡峭的对数线性扩展曲线。在 QuaLITY 上，我们的最终方案训练出的 Llama 8B 模型比 RAG 的性能高出 4.4%。在不同的模型和基准测试（QuaLITY、LongHealth、FinanceBench）中，我们的训练方法使模型在六个设置中的五个里击败了 RAG，平均性能提升 2.6%，在与 RAG 结合时则实现了 9.1% 的增益。

## Abstract
Synthetic data augmentation helps language models learn new knowledge in data-constrained domains. However, naively scaling existing synthetic data methods by training on more synthetic tokens or using stronger generators yields diminishing returns below the performance of RAG. To break the RAG ceiling, we introduce Synthetic Mixed Training, which combines synthetic QAs and synthetic documents. This leverages their complementary training signals, and enables log-linear improvements as both synthetic data volume and generator strength increase. This allows the model to outperform RAG by a 2.6\% relative gain on QuaLITY, a long-document reading comprehension benchmark. In addition, we introduce Focal Rewriting, a simple technique for synthetic document generation that explicitly conditions document generation on specific questions, improving the diversity of synthetic documents and yielding a steeper log-linear scaling curve. On QuaLITY, our final recipe trains a Llama 8B model that outperforms RAG by 4.4\% relatively. Across models and benchmarks (QuaLITY, LongHealth, FinanceBench), our training enables models to beat RAG in five of six settings, outperforms by 2.6\%, and achieves a 9.1\% gain when combined with RAG.

---

## 论文详细总结（自动生成）

以下是对论文《Synthetic Mixed Training: Scaling Parametric Knowledge Acquisition Beyond RAG》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在数据受限的特定领域，如何通过合成数据让语言模型（LM）内化新知识，并打破“检索增强生成（RAG）是性能天花板”的现状？
*   **研究背景**：
    *   尽管 RAG 是目前处理领域知识的事实标准，但将知识内化到模型参数中（参数化知识获取）在推理速度和长文本处理上更具优势。
    *   现有的合成数据增强方法（如重写、实体图等）在扩展规模时（增加 token 数或使用更强生成器）往往会遇到收益递减的瓶颈，且性能始终难以超越 RAG。
    *   论文旨在探索一种能够随数据量和计算量增加而实现“对数线性（log-linear）”持续增长的合成数据训练方案。

### 2. 论文提出的方法论
论文提出了两个核心技术：
*   **合成混合训练（Synthetic Mixed Training）**：
    *   **核心思想**：将“合成问答对（QA）”与“合成文档”按 1:1 的比例混合训练。
    *   **互补信号**：研究发现合成 QA 主要教授模型**检索行为**（如何通过思维链召回事实），而合成文档主要教授**事实知识**（领域特定的内容）。两者结合能产生协同效应，避免单一数据类型的饱和。
*   **焦点重写（Focal Rewriting）**：
    *   **关键技术**：在生成合成文档时，不再进行通用的重写，而是显式地以一个特定的合成问题为条件（Prompt 中加入“Focus on the question {{query}}”）。
    *   **作用**：这种方法显著提高了合成文档在词汇和语义上的多样性，从而使扩展曲线更加陡峭，延迟了性能饱和点的到来。

### 3. 实验设计
*   **数据集/场景**：
    *   **QuaLITY**：长文档阅读理解（虚构故事领域）。
    *   **LongHealth**：医疗临床文档问答。
    *   **FinanceBench**：财务报表问答（涉及 PDF 解析）。
*   **Benchmark（基准）**：以原始模型的 RAG 性能为主要对比目标。
*   **对比方法**：
    *   **WRAP**：文档重写。
    *   **EntiGraph (EG)**：基于实体的图生成。
    *   **Active Reading (AR)**：基于策略的重写。
    *   **纯合成 QA 训练**。
*   **模型**：使用 Llama 3.1 8B/70B 和 Qwen3 系列（1.7B 到 14B）作为实验对象。

### 4. 资源与算力
*   **算力消耗**：论文提供了详细的 FLOPs 估算。
    *   **训练规模**：最高训练了 700M（7亿）合成 token。
    *   **硬件与时长**：在 H100 GPU 上，训练一个由 70B 模型生成数据的 8B 模型（700M token），预计需要约 **18.5 个 H100 小时**（包含数据生成和模型训练）。
    *   **并行技术**：使用了 FSDP2 进行分布式训练，vLLM 进行高效推理生成。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。
    *   **扩展性实验**：测试了从 10M 到 700M token 的性能演变。
    *   **生成器缩放**：对比了 8B 和 70B 生成器对训练效果的影响。
    *   **模型通用性**：在 Llama 和 Qwen 两个模型家族、三个完全不同的领域（故事、医疗、金融）上验证了有效性。
    *   **消融实验**：包括混合比例测试、跨领域 QA 混合测试、元数据（Metadata）重要性测试等。
*   **客观性**：实验设计严谨，通过 8 次推理取平均值并标注置信区间，确保了结果的可靠性。

### 6. 主要结论与发现
*   **打破 RAG 天花板**：通过合成混合训练，8B 模型在 QuaLITY 上比 RAG 高出 4.4% 的相对准确率。
*   **对数线性扩展**：该方法在数据量达到原始文档 100 倍以上时仍保持增长，未出现明显饱和。
*   **生成器质量至关重要**：使用更强的生成器（如 70B）生成的文档对小模型（如 8B）的提升远大于 QA 的提升。
*   **与 RAG 互补**：在经过合成混合训练的模型上再叠加 RAG，性能可进一步提升 9.1%，说明参数化学习与外部检索并不冲突。
*   **模型规模效应**：模型越大，获取相同知识所需的合成 token 效率越高。

### 7. 优点
*   **简单高效**：不需要复杂的架构改动，仅通过优化数据配比和生成 Prompt 即可实现。
*   **揭示了机制**：深入分析了 QA 和文档在知识内化过程中的不同角色（行为 vs. 事实）。
*   **实用性强**：为特定领域（如医疗、金融）的模型定制化提供了清晰的可扩展路径。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 14B 以下的中小模型，尚未在大规模模型（如 70B+）上验证该扩展规律。
*   **灾难性遗忘**：虽然使用了 10% 的预训练数据回放（Replay），但论文未深入探讨在大规模知识注入后，模型通用能力的下降程度。
*   **生成成本**：虽然训练成本可控，但使用 70B 模型生成数亿 token 的合成数据在实际应用中仍是一笔不小的开支。

（完）

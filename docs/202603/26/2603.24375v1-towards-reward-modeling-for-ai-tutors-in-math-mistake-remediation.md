---
title: Towards Reward Modeling for AI Tutors in Math Mistake Remediation
title_zh: 迈向数学错误纠正中 AI 导师的奖励建模
authors: "Kseniia Petukhova, Ekaterina Kochmar"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24375v1"
tags: ["query:q13"]
score: 10.0
evidence: 利用合成数据为数学纠错AI导师进行奖励建模
tldr: 针对AI导师在数学纠错中难以评估教学质量的问题，本研究基于MRBench的人类偏好推导出教学维度层级，并合成对比响应对。通过在加权排名数据和合成数据上训练0.5B参数的Bradley-Terry奖励模型，实现了0.74的偏好预测准确率，在数学纠错场景下优于大型通用奖励模型，为AI教学评估提供了新思路。
selection_source: fresh_fetch
motivation: 传统的自然语言生成指标无法有效衡量AI导师在识别错误、引导推理及避免直接给出答案等关键教学维度的表现。
method: 从人类偏好中提取教学维度并合成对比响应对，利用自动生成的加权排名数据训练轻量级的Bradley-Terry奖励模型。
result: 该0.5B参数的模型在人类偏好测试中达到了0.74的准确率，性能优于参数量更大的通用奖励模型。
conclusion: 结合特定领域的教学维度和合成对比数据，能够显著提升轻量级模型在AI导师纠错任务中的奖励建模能力。
---

## 摘要
评估 AI 导师的教学质量仍然具有挑战性：标准的自然语言生成（NLG）指标无法判断回复是否识别了错误、引导了推理，或避免了直接给出答案。针对错误纠正任务，我们从 MRBench 上的用户成对偏好中推导出了教学维度的层级结构，并合成了在关键维度（如错误识别与定位、针对性、引导性、可操作性、清晰度和连贯性）上存在差异的极小对比回复对。我们开发并发布了 Bradley-Terry 偏好模型，该模型基于我们从 MRBench、合成对和数据组合中自动创建的加权和排名进行训练。仅使用合成数据，我们的最佳模型在人类偏好测试中达到了 0.69 的成对准确率；而将加权和数据与针对性合成组结合后，准确率提升至 0.74，在仅使用 0.5B 参数骨干网络的情况下，性能优于更大规模的通用奖励模型。

## Abstract
Evaluating the pedagogical quality of AI tutors remains challenging: standard NLG metrics do not determine whether responses identify mistakes, scaffold reasoning, or avoid revealing the answers. For the task of mistake remediation, we derive a hierarchy of pedagogical aspects from human pairwise preferences on MRBench, and synthesize minimally contrastive response pairs that differ along key aspects (e.g., mistake identification and location, targetedness, scaffolding, actionability, clarity, and coherence). We develop and release Bradley-Terry preference models trained on weighted-sum rankings that we automatically create from MRBench, synthetic pairs, and data combinations. Using only synthetic data, our best model reaches 0.69 pairwise accuracy on a human preference test, and combining weighted-sum data with targeted synthetic groups improves accuracy to 0.74, outperforming larger general-purpose reward models while using only a 0.5B-parameter backbone.

---

## 论文详细总结（自动生成）

### 论文总结：迈向数学错误纠正中 AI 导师的奖励建模

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管大语言模型（LLM）在教育领域展现出潜力，但评估 AI 导师的“教学质量”极具挑战。传统的 NLG 指标（如 BLEU、ROUGE）无法衡量导师是否有效地识别了学生错误、是否提供了支架式引导（Scaffolding），或者是否违规直接泄露了答案。
*   **核心问题**：现有的评估框架（如 MRBench）虽然定义了多个教学维度，但缺乏一个能够综合这些维度并符合人类偏好的统一奖励模型（Reward Model）。此外，简单的维度加权求和无法捕捉教学行为中的细微差别。

#### 2. 论文提出的方法论
*   **核心思想**：通过人类偏好实验确定教学维度的优先级层级，并利用该层级指导合成“极小对比响应对”（Minimally Contrastive Pairs），从而训练一个专门针对教学反馈的奖励模型。
*   **关键技术细节**：
    1.  **教学维度层级构建**：通过人类成对偏好标注，推导出优先级：**真实性/无矛盾 > 错误识别与定位 > 支架式引导与可操作性 > 针对性 > 不泄露最终答案 > 清晰度与连贯性**。
    2.  **合成数据增强（基于 CLAIR 范式）**：
        *   **改进子优回复**：针对 MRBench 中表现不佳的回复，利用 LLM（Claude-3.5-Sonnet）在特定维度（如错误定位、针对性）上进行单点改进。
        *   **降级优质回复**：故意削弱优质回复的某个维度（如泄露答案、降低事实性），制造负面样本。
        *   **全局非偏好处理**：确保事实错误或完全未识别错误的回复在排名中处于最低端。
    3.  **奖励模型训练**：采用 **Bradley-Terry 模型**架构，将偏好对输入骨干网络（Qwen2.5 或 Gemma-2），通过最小化负对数似然损失函数，学习对教学质量更高的回复赋予更高分。

#### 3. 实验设计
*   **数据集**：
    *   **MRBench**：包含 192 个对话上下文和 1,596 条标注了 8 个教学维度的回复。
    *   **Bridge 数据集**：用于验证模型在区分专家与新手教师回复上的泛化能力。
*   **Benchmark 与对比方法**：
    *   **人类偏好测试集**：由 414 个经过人类标注和传递性推理得到的偏好对组成。
    *   **对比模型**：包括通用奖励模型 **Skywork-Reward-V2 (8B)**、**RewardAnything (8B)** 以及前人针对教学任务训练的 **Qwen2.5-1.5B** 模型。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型规模**：使用了轻量级的骨干网络，包括 Qwen2.5-0.5B、Qwen2.5-1.5B 和 Gemma-2-2B。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   训练集包含 2,979 条基于加权和的偏好对和 11,346 条合成偏好对。
    *   进行了多组消融实验，测试了不同数据组合（如“加权和+针对性改进”、“加权和+错误定位改进”等）对模型性能的影响。
*   **充分性评价**：实验设计较为充分，不仅对比了不同规模的骨干网络，还跨家族对比了 Qwen 和 Gemma 模型，并与当前最强的通用奖励模型进行了基准测试。通过 McNemar 检验验证了结果的统计显著性，体现了实验的客观性。

#### 6. 论文的主要结论与发现
*   **小模型胜大模型**：经过特定教学数据微调的 **0.5B 参数模型**在人类偏好测试中的准确率（0.74）显著优于 **8B 参数的通用奖励模型**（0.64-0.68）。
*   **合成数据的价值**：仅使用合成数据训练的模型即可达到 0.69 的准确率；将原始标注数据与针对性合成数据（特别是改进“错误识别与定位”的数据）结合，性能提升最明显。
*   **维度互补性**：发现基于加权和训练的模型与基于合成数据训练的模型在错误类型上具有互补性，结合两者能显著提升鲁棒性。

#### 7. 优点
*   **方法论创新**：将复杂的教学评估拆解为结构化的维度层级，并利用“极小对比”思想生成高质量合成数据，解决了教育领域标注数据稀缺的问题。
*   **高效性**：证明了在特定垂直领域，极小规模的模型（0.5B）通过高质量数据也能实现超越通用大模型的性能。
*   **资源开源**：公开发布了标注数据、合成数据集、模型代码和训练好的奖励模型，对社区贡献较大。

#### 8. 不足与局限
*   **标注规模有限**：人类偏好测试集（414 对）规模较小，且部分标注由单人完成，可能存在一定的标注噪声。
*   **领域局限性**：实验主要集中在“数学错误纠正”任务，其推导出的教学层级是否能完美迁移到人文社科等其他学科尚待验证。
*   **合成偏差**：合成数据由单一模型（Claude-3.5-Sonnet）生成，可能引入该模型的特定风格偏见。
*   **静态评估**：奖励模型评估的是单轮回复质量，尚未涉及长期教学策略或多轮对话的动态优化。

（完）

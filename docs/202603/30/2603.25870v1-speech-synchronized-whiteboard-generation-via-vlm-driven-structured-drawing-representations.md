---
title: Speech-Synchronized Whiteboard Generation via VLM-Driven Structured Drawing Representations
title_zh: 基于视觉语言模型驱动的结构化绘图表示的语音同步白板生成
authors: "Suraj Prasad, Pinak Mahapatra"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25870v1"
tags: ["query:edu-llm"]
score: 9.0
evidence: 白板式教育视频与视觉语言模型驱动的绘图
tldr: 本研究针对教育视频中手绘插图与语音叙述精确同步的挑战，提出了首个包含24个STEM领域演示的Excalidraw数据集，记录了毫秒级绘图时间戳。通过对Qwen2-VL-7B模型进行LoRA微调，实现了基于语音预测同步绘图序列。实验证明，时间戳调节显著提升了时空对齐效果，且模型在未见过的STEM主题上具有良好的泛化能力，为自动化教育内容生成提供了新路径。
selection_source: fresh_fetch
motivation: 现有的教育视频制作缺乏能够将手绘插图与语音叙述进行结构化、高精度同步的自动化方法。
method: 构建了包含毫秒级时间戳的Excalidraw绘图与语音配对数据集，并利用LoRA微调Qwen2-VL-7B模型来预测与语音同步的结构化绘图序列。
result: 实验表明，引入时间戳调节显著改善了绘图与语音的同步性，且模型能够泛化至未见过的STEM学科领域。
conclusion: 该研究展示了利用视觉语言模型生成同步教学白板视频的可行性，并为未来自动化教育内容创作提供了数据和技术支持。
---

## 摘要
制作白板风格的教育视频需要手绘插图与口头叙述之间的精确协调，然而目前尚无现有方法通过结构化、可重现的绘图表示来解决这一多模态同步问题。我们提出了首个包含 24 个配对 Excalidraw 演示及叙述音频的数据集，其中每个绘图元素都带有毫秒级精度的创建时间戳，涵盖 8 个 STEM 领域。利用这些数据，我们研究了通过 LoRA 微调的视觉语言模型（Qwen2-VL-7B）是否能够仅凭 24 个演示就预测出与语音同步的完整笔画序列。我们的主题分层五折评估表明，与消融基准相比，时间戳调节显著提高了时间对齐性能，同时模型能够泛化到未见的 STEM 主题。我们讨论了其在真实课堂环境中的可迁移性，并发布了数据集和代码，以支持自动化教育内容生成领域的未来研究。

## Abstract
Creating whiteboard-style educational videos demands precise coordination between freehand illustrations and spoken narration, yet no existing method addresses this multimodal synchronization problem with structured, reproducible drawing representations. We present the first dataset of 24 paired Excalidraw demonstrations with narrated audio, where every drawing element carries millisecond-precision creation timestamps spanning 8 STEM domains. Using this data, we study whether a vision-language model (Qwen2-VL-7B), fine-tuned via LoRA, can predict full stroke sequences synchronized to speech from only 24 demonstrations. Our topic-stratified five-fold evaluation reveals that timestamp conditioning significantly improves temporal alignment over ablated baselines, while the model generalizes across unseen STEM topics. We discuss transferability to real classroom settings and release our dataset and code to support future research in automated educational content generation.

---

## 论文详细总结（自动生成）

### 论文总结：基于 VLM 驱动结构化绘图表示的语音同步白板生成

#### 1. 核心问题与研究动机
*   **核心问题**：如何自动化生成类似“可汗学院”风格的白板教学视频，实现手绘插图（Freehand Illustrations）与口头叙述（Spoken Narration）在时间与空间上的精确同步。
*   **研究背景**：制作此类视频极其耗时，教师需同时兼顾绘图时机、布局和叙述逻辑。现有方法（如 SketchAgent 或 VideoSketcher）虽能生成序列草图，但缺乏与语音的显式同步，且通常不输出可编辑的结构化格式。

#### 2. 方法论
*   **核心思想**：将白板生成任务建模为“时间步条件下的元素预测问题”，利用视觉语言模型（VLM）根据当前画布状态和语音转录文本，预测下一个绘图元素的类型、起始时间戳及几何坐标。
*   **关键技术细节**：
    *   **结构化表示**：采用 Excalidraw 的 JSON 格式，将每个元素（线条、箭头、矩形、自由绘图等）序列化为：`TYPE | ONSET_T | x0,y0 | x1,y1 | ...`。
    *   **模型架构**：以 **Qwen2-VL-7B** 为底座，通过 **LoRA** 进行参数高效微调。
    *   **输入模态**：包括当前画布的渲染图像（640x360 RGB）、文本历史记录以及带有单词级时间戳的语音转录文本。
    *   **自回归生成**：模型逐个预测元素，每预测一个元素后更新画布状态，再进行下一步预测，直至输出结束标记。

#### 3. 实验设计
*   **数据集**：**ExcaliTeach**。包含 24 个由讲师创作的演示，涵盖生物、化学、物理、数学等 8 个 STEM 领域，共 877 个绘图元素，均带有毫秒级时间戳。
*   **对比实验（E1-E5）**：
    *   **E1 (Full)**：完整模型。
    *   **E2 (No onset token)**：移除时间戳预测，改用后期均匀插值。
    *   **E3 (No canvas image)**：仅依赖文本历史，不输入画布图像。
    *   **E4 (One-shot)**：非自回归，一次性预测所有元素。
    *   **E5 (No transcript)**：移除语音转录文本，仅靠绘图历史预测。
*   **评估指标**：时间对齐误差（TAE）、Chamfer 距离（几何精度）、类型准确率、Gemini 评分及人类偏好调查。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：使用了 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 4（梯度累积至 32），采用 bf16 精度，训练 30 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：基于 24 个演示视频进行了**主题分层五折交叉验证（5-fold cross-validation）**，确保测试集主题在训练集中未出现过。
*   **充分性评价**：虽然原始数据样本量较小（24个），但通过五折交叉验证和针对性的消融实验（E1-E5），客观地验证了时间戳预测、视觉反馈和语音条件的重要性。实验设计较为严谨，能够支持其核心论点。

#### 6. 主要结论与发现
*   **时间戳调节至关重要**：显式预测起始时间（ONSET_T）显著降低了时间对齐误差（TAE）。
*   **多模态协同**：自回归视觉反馈有助于维持空间布局的一致性，而语音转录文本则是确定绘图时机的关键。
*   **泛化能力**：尽管数据量有限，模型仍能泛化到未见过的 STEM 学科主题。
*   **可编辑性**：生成的 Excalidraw 格式允许教师进行后期微调，具有极高的实用价值。

#### 7. 优点
*   **首创性**：提出了首个带有毫秒级对齐时间戳的结构化白板绘图数据集。
*   **实用性强**：输出结果非像素视频，而是可编辑的矢量 JSON，方便教师修改。
*   **低数据依赖**：证明了在 LoRA 微调下，VLM 可以在极小规模（24个样本）的专业领域数据上取得良好效果。

#### 8. 不足与局限
*   **数据规模限制**：24 个演示样本量仍然偏小，学科覆盖范围有待扩大。
*   **长序列漂移**：在长达数分钟的讲座中，自回归生成可能会累积几何误差，导致布局错乱。
*   **实时性挑战**：目前依赖事后语音转录（ASR），若要应用于直播或实时辅助，需解决低延迟流式处理问题。
*   **复杂交互缺失**：尚未处理教学中的停顿、回溯修改或复杂的肢体语言同步。

（完）

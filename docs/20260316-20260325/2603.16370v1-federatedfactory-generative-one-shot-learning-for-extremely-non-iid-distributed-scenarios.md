---
title: "FederatedFactory: Generative One-Shot Learning for Extremely Non-IID Distributed Scenarios"
title_zh: FederatedFactory：针对极端非独立同分布分布式场景的生成式单次学习
authors: "Andrea Moleri, Christian Internò, Ali Raza, Markus Olhofer, David Klindt, Fabio Stella, Barbara Hammer"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16370v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 支持从无到有合成全类别平衡的数据集
tldr: FederatedFactory 针对极端非独立同分布（Non-IID）的联邦学习场景，提出了一种无需预训练模型的生成式单轮学习框架。该方法通过交换生成模块而非判别参数，在本地合成类别平衡的数据集，有效解决了梯度冲突问题。实验表明，该框架在医学影像和标准数据集上均能达到接近中心化训练的性能，并支持精确的模块化遗忘。
selection_source: fresh_fetch
motivation: 传统的联邦学习在处理标签互斥的极端非独立同分布数据时，由于梯度冲突和对预训练模型的依赖，导致模型性能严重下降。
method: 该框架将联邦单元从判别参数转变为生成先验，通过单轮通信交换生成模块，在本地合成全局平衡的数据集进行训练。
result: "在 CIFAR-10 极端异构场景下将准确率从 11.36% 提升至 90.57%，并在 ISIC2019 等医学影像基准上恢复了接近中心化训练的性能。"
conclusion: FederatedFactory 证明了通过生成式先验交换可以有效克服极端数据异构挑战，并为联邦学习中的隐私保护和模块化遗忘提供了新路径。
---

## 摘要
联邦学习（FL）在不损害数据主权的前提下实现了分布式优化。然而，当局部标签分布互斥时，由于优化轨迹冲突，标准的权重聚合方法往往会失效。通常，联邦学习方法依赖于预训练的基础模型，这引入了不切实际的假设。我们提出了 FederatedFactory，这是一个零依赖框架，它将联邦单元从判别式参数反转为生成式先验。通过在单轮通信中交换生成模块，我们的架构支持从无到有地合成全局类别平衡的数据集，从而完全消除了梯度冲突和外部先验偏差。在包括 MedMNIST 和 ISIC2019 在内的多种医学影像基准测试上的评估表明，我们的方法恢复了中心化训练的性能上限。在病理性异构环境下，它将 CIFAR-10 的基准准确率从崩溃的 11.36% 提升至 90.57%，并将 ISIC2019 的 AUROC 恢复至 90.57%。此外，该框架通过确定性地删除特定的生成模块，实现了精确的模块化遗忘。

## Abstract
Federated Learning (FL) enables distributed optimization without compromising data sovereignty. Yet, where local label distributions are mutually exclusive, standard weight aggregation fails due to conflicting optimization trajectories. Often, FL methods rely on pretrained foundation models, introducing unrealistic assumptions. We introduce FederatedFactory, a zero-dependency framework that inverts the unit of federation from discriminative parameters to generative priors. By exchanging generative modules in a single communication round, our architecture supports ex nihilo synthesis of universally class balanced datasets, eliminating gradient conflict and external prior bias entirely. Evaluations across diverse medical imagery benchmarks, including MedMNIST and ISIC2019, demonstrate that our approach recovers centralized upper-bound performance. Under pathological heterogeneity, it lifts baseline accuracy from a collapsed 11.36% to 90.57% on CIFAR-10 and restores ISIC2019 AUROC to 90.57%. Additionally, this framework facilitates exact modular unlearning through the deterministic deletion of specific generative modules.

---

## 论文详细总结（自动生成）

以下是对论文《FederatedFactory: Generative One-Shot Learning for Extremely Non-IID Distributed Scenarios》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在现实世界的联邦学习（FL）场景中，数据往往呈现极端的非独立同分布（Non-IID）特性。最极端的情况是“单类别孤岛”（Single-Class Silo），即每个参与节点仅拥有一种类别的数据。
*   **核心痛点**：
    *   **梯度冲突**：传统的权重聚合方法（如 FedAvg）在标签互斥时，由于各节点优化轨迹完全背离，会导致全局模型性能崩溃。
    *   **外部依赖风险**：现有的单次联邦学习（OSFL）方案通常依赖预训练的基础模型（FM）作为先验，但在医疗影像等专业领域，FM 往往存在严重的领域偏移（Domain Shift）和语义幻觉。
*   **整体含义**：论文提出了一种名为 **FederatedFactory** 的新框架，旨在不依赖外部模型、仅通过单轮通信，在极端异构环境下恢复接近中心化训练的性能。

### 2. 方法论
*   **核心思想**：将联邦学习的单元从“判别式参数”（模型权重）转变为“生成式先验”。不再聚合分类器的梯度，而是交换本地训练的生成模型。
*   **关键技术细节**：
    1.  **本地生成工厂（Factory）**：每个客户端独立训练一个基于扩散模型（文中采用 EDM2 架构）的生成器，学习本地数据的流形分布。
    2.  **单次通信（One-Shot）**：客户端仅向服务器发送一次生成器的参数 $\theta_k$。
    3.  **从无到有合成（Ex Nihilo Synthesis）**：服务器（或 Peer 节点）将接收到的多个生成模块组合成一个“视觉记忆库”，从标准正态分布潜空间采样，合成一个类别平衡的全局数据集 $\hat{D}_{syn}$。
    4.  **全局训练**：在合成数据集上训练标准的判别式分类器（如 ResNet-50）。
*   **两种架构模式**：
    *   **中心化模式**：由受信任的服务器统一合成数据并训练模型。
    *   **去中心化（P2P）模式**：节点间广播生成器，本地合成混合数据集，通过“专家乘积”（Product of Experts, PoE）进行推理决策。
*   **模块化遗忘**：通过在生成矩阵 $\Gamma$ 中删除特定行列，实现对特定客户端或特定类别的精确数据擦除（Machine Unlearning）。

### 3. 实验设计
*   **数据集**：
    *   **标准基准**：CIFAR-10。
    *   **医疗影像基准**：MedMNIST（包含 BloodMNIST, RetinaMNIST, PathMNIST 子集）和 ISIC2019（高分辨率皮肤病变图像）。
*   **Benchmark（基准线）**：中心化训练上限（Centralized Upper Bound，即假设所有原始数据被集中在一起训练的结果）。
*   **对比方法**：FedAvg, FedDyn, FedProx, Scaffold。
*   **场景设置**：使用 Dirichlet 分布参数 $\alpha$ 控制异构程度，重点测试 $\alpha \to 0$ 的病理性单类别孤岛场景。

### 4. 资源与算力
*   **硬件环境**：
    *   **本地训练**：2 × NVIDIA H100 (80GB VRAM)，专门用于训练本地扩散模型。
    *   **合成与评估**：4 × NVIDIA L40 (48GB VRAM)，用于并行化图像合成及全局分类器训练。
*   **训练规模**：分类器训练 300 个 epoch；扩散模型采用轻量化配置（128 维度，32 采样步数）以适应分布式节点。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低分辨率自然图像到高分辨率专业医疗影像的 5 个主流数据集。
*   **充分性**：
    *   每组实验均运行 5 个独立随机种子并报告标准差，确保了结果的统计显著性。
    *   包含了 t-SNE 流形对齐分析、FID/KID 生成质量评估、通信与计算成本对比分析。
    *   **客观性**：实验不仅展示了性能提升，还诚实地讨论了计算开销（Generative Tax）的增加，对比设计较为公平。

### 6. 主要结论与发现
*   **性能恢复**：在极端异构下，传统 FL 方法准确率会跌至 10%-20%（接近随机猜测），而 FederatedFactory 能将其恢复至 90% 以上，几乎抹平了与中心化训练的差距。
*   **通信效率**：相比迭代式 FL，该方法将通信量降低了 **99.4%**（例如 CIFAR-10 从 358GB 降至 1.9GB）。
*   **理论证明**：定理 1 证明了全局风险严格受限于本地生成误差，验证了无需外部先验即可收敛。

### 7. 优点
*   **零外部依赖**：不依赖 CLIP 或 Stable Diffusion 等预训练模型，避免了外部先验带来的偏差和隐私泄露风险。
*   **解决梯度冲突**：通过数据空间合成而非参数空间聚合，从根本上避开了 Non-IID 导致的梯度抵消问题。
*   **原生支持遗忘权**：模块化的架构使得删除某个节点的数据贡献变得极其简单且精确，符合 GDPR 等隐私法规。

### 8. 不足与局限
*   **计算税（Generative Tax）**：将通信压力转嫁给了计算。本地训练扩散模型的算力需求（FLOPs）比传统 FL 高出约 1-2 个数量级。
*   **隐私保护深度**：虽然不传输原始数据，但生成模型参数仍可能受到成员推理攻击（MIA），论文尚未引入形式化的差分隐私（DP）保障。
*   **生成质量瓶颈**：如果本地数据量极度匮乏，生成模型可能难以捕捉完整的流形，从而限制最终分类器的泛化能力。

（完）

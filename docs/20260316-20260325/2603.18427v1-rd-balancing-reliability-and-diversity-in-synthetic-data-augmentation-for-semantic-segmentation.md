---
title: "R&D: Balancing Reliability and Diversity in Synthetic Data Augmentation for Semantic Segmentation"
title_zh: "R&D：语义分割合成数据增强中可靠性与多样性的平衡"
authors: "Huy Che, Dinh-Duy Phan, Duc-Khai Lam"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18427v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 使用可控扩散模型的合成数据增强流水线
tldr: "针对语义分割标注成本高且传统增强方法缺乏结构创新的问题，本文提出R&D框架，利用可控扩散模型生成合成数据。该方法通过类别感知提示和视觉先验融合，在保证生成图像多样性的同时，维持了与原始标签的像素级一致性（可靠性）。在PASCAL VOC和BDD100K上的实验证明，该方法显著提升了模型在数据稀缺场景下的性能和鲁棒性。"
selection_source: fresh_fetch
motivation: 传统数据增强难以生成新结构，而现有生成模型在语义分割任务中难以保持图像与标签的像素级一致性。
method: 提出一种结合可控扩散模型、类别感知提示和视觉先验融合的合成数据增强流水线，平衡数据的多样性与可靠性。
result: 在PASCAL VOC和BDD100K基准测试中，该方法显著提高了语义分割性能，特别是在小样本场景下表现优异。
conclusion: 通过平衡合成数据的多样性与可靠性，该方法有效弥补了合成数据与真实数据间的差距，增强了模型的泛化能力。
---

## 摘要
收集和标注用于像素级语义分割任务的数据集是极度耗费人力的。数据增强通过在不增加额外真实世界数据收集的情况下提高模型泛化能力，提供了一种可行的解决方案。传统的增强技术（如平移、缩放和颜色变换）虽然能产生几何变化，但无法生成新的结构。虽然生成模型已被用于扩展数据集的语义信息，但它们往往难以保持原始图像与生成图像之间的一致性，尤其是在像素级任务中。在这项工作中，我们提出了一种集成可控扩散模型的新型合成数据增强流水线。我们的方法平衡了数据的多样性和可靠性，有效地弥合了合成数据与真实数据之间的差距。我们利用类别感知提示和视觉先验融合来进一步提高图像质量，确保与分割标签的精确对齐。通过在 PASCAL VOC 和 BDD100K 等基准数据集上的评估，我们证明了该方法显著提升了语义分割性能，特别是在数据匮乏的场景下，同时提高了模型在实际应用中的鲁棒性。我们的代码可在 https://github.com/chequanghuy/Enhanced-Generative-Data-Augmentation-for-Semantic-Segmentation-via-Stronger-Guidance 获取。

## Abstract
Collecting and annotating datasets for pixel-level semantic segmentation tasks are highly labor-intensive. Data augmentation provides a viable solution by enhancing model generalization without additional real-world data collection. Traditional augmentation techniques, such as translation, scaling, and color transformations, create geometric variations but fail to generate new structures. While generative models have been employed to extend semantic information of datasets, they often struggle to maintain consistency between the original and generated images, particularly for pixel-level tasks. In this work, we propose a novel synthetic data augmentation pipeline that integrates controllable diffusion models. Our approach balances diversity and reliability data, effectively bridging the gap between synthetic and real data. We utilize class-aware prompting and visual prior blending to improve image quality further, ensuring precise alignment with segmentation labels. By evaluating benchmark datasets such as PASCAL VOC and BDD100K, we demonstrate that our method significantly enhances semantic segmentation performance, especially in data-scarce scenarios, while improving model robustness in real-world applications. Our code is available at \href{https://github.com/chequanghuy/Enhanced-Generative-Data-Augmentation-for-Semantic-Segmentation-via-Stronger-Guidance}{https://github.com/chequanghuy/Enhanced-Generative-Data-Augmentation-for-Semantic-Segmentation-via-Stronger-Guidance}.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **R&D**（Reliability and Diversity，可靠性与多样性）的合成数据增强框架，旨在解决语义分割任务中高质量标注数据匮乏的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：语义分割需要像素级的精确标注，人工成本极高。
*   **研究动机**：
    *   **传统增强局限性**：旋转、缩放等几何变换无法生成新的物体结构或纹理。
    *   **生成式增强挑战**：现有的生成模型（如 GAN 或基础扩散模型）在生成图像时，往往难以保证生成的图像内容与原始分割掩码（Mask）在像素级别上完全对齐（即“可靠性”不足）；同时，生成图像可能偏离原始数据分布（即“领域偏移”）。
    *   **平衡难题**：如何在增加数据集多样性（Diversity）的同时，确保合成数据对训练模型的有效性（Reliability）。

### 2. 方法论：R&D 框架
论文提出了一种集成两个可控扩散模型（Stable Diffusion + T2I-Adapter/ControlNet）的流水线：

*   **核心思想**：通过两种路径生成数据以实现平衡。
    *   **路径 A：Img2Img 可控扩散模型**。利用输入图像作为参考，结合视觉先验生成高度多样化的图像（改变背景和物体），产生数据集 $D_{gen1}$。
    *   **路径 B：可控修复（Inpainting）扩散模型**。仅修改标签区域内的物体，保持背景和非标签区域不变，确保数据分布的一致性，产生数据集 $D_{gen2}$。
*   **关键技术细节**：
    *   **类别感知提示（Class-aware Prompting）**：利用 BLIP 模型生成条件图像描述，并对标签类别词进行加权处理（如 `[class]++`），确保模型关注目标物体。
    *   **视觉先验融合（Visual Prior Blending）**：公式为 $V^* = \alpha V_I + V_S$。其中 $V_I$ 是原图边缘先验，$V_S$ 是分割掩码边缘先验，$\alpha$（设为 0.8）用于平衡全局结构与标签对齐度。
    *   **顺序生成与合并**：在修复模式下，针对不同类别物体顺序生成并根据掩码进行图像融合，避免小物体被背景覆盖。

### 3. 实验设计
*   **数据集**：
    *   **PASCAL VOC (2007/2012)**：用于通用的物体语义分割。
    *   **BDD100K**：用于自动驾驶场景下的可行驶区域和车道线分割，特别测试了雾天、隧道、加油站等特殊环境。
*   **Benchmark 与对比方法**：
    *   **模型架构**：DeepLabV3+ (ResNet50/101) 和 Mask2Former (Swin-B)。
    *   **对比方法**：原始数据训练（Baseline）、Stronger Guidance [11]、DiffuMask [10]、Dataset Diffusion [9] 等。
*   **实验场景**：特别设置了“数据稀缺”场景（如仅使用 VOC12 的 92、183、366 张图像进行训练）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了使用 MMSegmentation 框架，训练迭代次数为 30K，分辨率 512x512，Batch Size 为 16，使用 AdamW 优化器。
*   **生成模型**：使用了 SD-XL 作为基础生成模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 VOC 数据集做了 6 种不同数据规模（从 92 到 1464 张图）的对比实验。
    *   针对 BDD100K 做了 3 种特殊环境的泛化性实验。
    *   **消融实验**：涵盖了提示词选择（Simple vs. Class-aware）、数据合成策略（纯合成 vs. 混合 vs. 微调）、合成图像数量的影响等。
*   **评价**：实验设计较为充分，通过多尺度数据集验证了模型在小样本下的优越性，对比了多种主流的合成数据生成方法，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：在所有配置下，R&D 框架均优于原始数据训练。在 VOC12 全量数据集上，通过合成数据预训练再用真实数据微调，mIoU 提升了约 2.2%。
*   **小样本优势**：在数据极度匮乏（如仅 92 张图）时，合成数据带来的性能增益最为显著。
*   **平衡的重要性**：实验证明，单纯追求多样性会导致领域偏移，而 R&D 通过结合 Img2Img 和 Inpainting，在保持分布一致性的同时引入了必要的结构变化。
*   **微调策略**：先在大量合成数据上预训练，再用少量真实数据微调，是利用合成数据的最佳路径。

### 7. 优点（亮点）
*   **双路径设计**：巧妙地通过 Img2Img 保证多样性，通过 Inpainting 保证可靠性，解决了生成数据“虚假”或“跑偏”的问题。
*   **提示词优化**：提出的类别感知提示词方法比简单的类别堆砌或纯 Caption 更能引导模型生成准确的语义内容。
*   **视觉先验融合**：通过 $\alpha$ 系数融合原图和掩码的边缘信息，极大地提高了生成图像与标签的像素级对齐精度。

### 8. 不足与局限
*   **计算成本**：扩散模型的推理速度较慢，生成数千张高质量合成图像需要显著的时间和计算资源。
*   **依赖预训练模型**：合成质量高度依赖于底层 Stable Diffusion 模型的性能，若基础模型未见过某些特定类别的物体，生成效果会受限。
*   **算力信息缺失**：未提供具体的硬件开销和生成耗时，不便于其他研究者评估复现成本。
*   **隐私与特定领域验证**：虽然论文提到了隐私保护的潜力，但未在医疗或工业检测等更敏感、更专业的领域进行深入验证。

（完）

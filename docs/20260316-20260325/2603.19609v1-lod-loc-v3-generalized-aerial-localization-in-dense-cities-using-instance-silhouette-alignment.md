---
title: "LoD-Loc v3: Generalized Aerial Localization in Dense Cities using Instance Silhouette Alignment"
title_zh: LoD-Loc v3：利用实例轮廓对齐在密集城市中实现通用航空定位
authors: "Shuaibang Peng, Juelin Zhu, Xia Li, Kun Yang, Maojun Zhang, Yu Liu, Shen Yan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19609v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 航空图像合成数据生成流水线
tldr: 针对密集城市环境中的航空视觉定位问题，LoD-Loc v3 改进了前代模型在跨场景泛化和密集建筑场景下的不足。研究者构建了包含10万张图像的大规模实例分割数据集 InsLoD-Loc，并提出将定位范式从语义对齐转向实例轮廓对齐。该方法显著降低了位姿估计的歧义性，实现了卓越的零样本泛化能力，在密集城市定位任务中大幅领先现有技术。
selection_source: fresh_fetch
motivation: 解决现有航空定位方法在跨场景泛化能力差以及在密集建筑区域容易出现位姿估计歧义的问题。
method: 通过构建大规模合成数据集 InsLoD-Loc 并引入实例轮廓对齐技术，提升模型的泛化性与定位精度。
result: 实验表明 LoD-Loc v3 在跨场景和密集城市定位任务中均显著优于现有的最先进基准方法。
conclusion: 该研究证明了实例级信息和高质量合成数据对于提升复杂城市环境下航空视觉定位鲁棒性的关键作用。
---

## 摘要
我们提出了 LoD-Loc v3，这是一种在密集城市环境中进行通用航空视觉定位的新方法。虽然先前的工作 LoD-Loc v2 通过将语义建筑物轮廓与低细节城市模型对齐来实现定位，但它存在两个关键局限性：跨场景泛化能力差，以及在建筑密集场景中经常失败。我们的方法通过两项关键创新解决了这些挑战。首先，我们开发了一种新的合成数据生成流水线，产生了 InsLoD-Loc——这是迄今为止最大的航空图像实例分割数据集，包含 10 万张具有精确实例建筑标注的图像。这使得训练后的模型表现出卓越的零样本泛化能力。其次，我们通过从语义轮廓对齐转向实例轮廓对齐，重新构建了定位范式，这显著降低了密集场景中的位姿估计歧义。大量的实验表明，LoD-Loc v3 优于现有的最先进（SOTA）基准，在跨场景和密集城市场景中均以显著优势实现了卓越的性能。项目地址为 https://nudt-sawlab.github.io/LoD-Locv3/。

## Abstract
We present LoD-Loc v3, a novel method for generalized aerial visual localization in dense urban environments. While prior work LoD-Loc v2 achieves localization through semantic building silhouette alignment with low-detail city models, it suffers from two key limitations: poor cross-scene generalization and frequent failure in dense building scenes. Our method addresses these challenges through two key innovations. First, we develop a new synthetic data generation pipeline that produces InsLoD-Loc - the largest instance segmentation dataset for aerial imagery to date, comprising 100k images with precise instance building annotations. This enables trained models to exhibit remarkable zero-shot generalization capability. Second, we reformulate the localization paradigm by shifting from semantic to instance silhouette alignment, which significantly reduces pose estimation ambiguity in dense scenes. Extensive experiments demonstrate that LoD-Loc v3 outperforms existing state-of-the-art (SOTA) baselines, achieving superior performance in both cross-scene and dense urban scenarios with a large margin. The project is available at https://nudt-sawlab.github.io/LoD-Locv3/.

---

## 论文详细总结（自动生成）

以下是对论文《LoD-Loc v3: Generalized Aerial Localization in Dense Cities using Instance Silhouette Alignment》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：无人机（UAV）视觉定位通常依赖高精度3D地图（如SfM点云），但其维护成本高且存在隐私风险。基于城市细节水平（LoD）模型的定位方法（如LoD-Loc v2）利用建筑轮廓对齐，是一种更具扩展性的替代方案。
*   **核心问题**：
    1.  **泛化能力差**：现有模型在未见过的场景中表现不佳，严重依赖特定场景的训练数据。
    2.  **密集场景歧义性**：在建筑密集的城市中，多个建筑的语义轮廓会融合成一个整体，导致位姿估计出现严重歧义和定位失败。
*   **整体含义**：LoD-Loc v3 通过引入**实例级轮廓对齐**和**大规模合成数据集**，旨在实现一种在密集城市中具有强泛化能力的通用航空定位框架。

### 2. 方法论
核心思想是将定位范式从“语义轮廓匹配”转向“实例轮廓匹配”，并利用合成数据解决标注匮乏问题。
*   **InsLoD-Loc 数据集构建**：
    *   利用 Unreal Engine 5 (UE5) 结合 Google Earth 3D Tiles 渲染照片级真实的 RGB 图像。
    *   通过 OpenSceneGraph (OSG) 将地理参考的 LoD 模型与渲染视角对齐，自动生成精确的实例分割掩码（10万张图像，覆盖6国40个区域）。
*   **实例分割模块**：
    *   基于 SAM (Segment Anything Model) 构建，通过 LoRA 技术进行参数高效微调。
    *   引入 Prompter 模块生成候选框，使模型能自动提取查询图像中的单个建筑轮廓。
*   **实例对齐位姿评价（Cost Function）**：
    *   **模型实例化**：通过拓扑结构分析为 LoD 模型中的每个建筑分配唯一 ID。
    *   **非对称匹配**：计算查询图像实例与渲染实例之间的 Dice 系数。
    *   **加权策略**：提出基于置信度（Confidence-based）或面积（Area-based）的加权对齐得分，用于粗定位（网格搜索）和精定位（粒子滤波）阶段。

### 3. 实验设计
*   **数据集**：
    *   **UAVD4L-LoDv2**：2.5 $km^2$ 的公共基准。
    *   **Swiss-EPFLv2**：8.2 $km^2$ 的跨场景基准。
    *   **Tokyo-LoDv3**：论文自建的密集城市挑战集，包含5个极高密度的城场景。
*   **对比方法（Baselines）**：
    *   特征匹配类：CAD-Loc (结合 SIFT, SuperPoint, LoFTR, RoMa 等)。
    *   特征对齐类：MC-Loc (结合 DINOv2, RoMa)。
    *   LoD 对齐类：LoD-Loc (v1) 和 LoD-Loc v2。
*   **评价指标**：在 (2m, 2°), (3m, 3°), (5m, 5°) 阈值下的定位成功率，以及中值平移/旋转误差。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：
    *   在 InsLoD-Loc 的 88,493 张训练集上训练 20 个 epoch。
    *   使用 AdamW 优化器，基础学习率 $2 \times 10^{-4}$。
    *   输入图像尺寸为 $512 \times 512$。
*   **推理效率**：精定位阶段迭代 40 次，每步生成 52 个候选位姿。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个大型数据集上进行了详尽测试，涵盖了从郊区到极密集中心商务区的多种地貌。
*   **消融实验**：
    *   **轮廓表示实验**：对比了在相同数据集下，语义轮廓与实例轮廓的性能差异，证明了实例级信息的必要性。
    *   **对齐机制实验**：验证了强制合并实例掩码（退化为语义匹配）会导致性能大幅下降。
*   **客观性**：实验采用了**零样本（Zero-shot）**设置，即模型仅在合成数据上训练，直接在真实世界/不同城市的测试集上评估，这极大地增强了实验的说服力和公平性。

### 6. 主要结论与发现
*   **性能飞跃**：在密集城场景（Tokyo-LoDv3）中，LoD-Loc v3 在 (2m, 2°) 精度下比现有 SOTA 方法提升了 **2000%**（因为前代方法在密集区几乎完全失效）。
*   **强泛化性**：仅靠合成数据训练的模型，在真实世界数据集（Swiss-EPFLv2）上的表现超过了在真实数据上训练的 LoD-Loc v2。
*   **解决歧义**：实例对齐能有效区分紧邻的建筑，解决了语义轮廓在俯视视角下“粘连”导致的定位漂移问题。

### 7. 优点
*   **范式创新**：首次将实例分割引入 LoD 定位，从根本上解决了密集建筑群的匹配歧义。
*   **数据贡献**：发布了 InsLoD-Loc，这是目前最大的航空建筑实例分割数据集，对相关领域具有重要价值。
*   **实用性强**：实现了零样本跨场景定位，降低了无人机在实际部署时对目标区域标注数据的依赖。

### 8. 不足与局限
*   **环境依赖**：定位精度高度依赖实例分割的质量。在极端恶劣天气（如暴雨、浓雾）下，分割模型可能失效，进而导致定位崩溃。
*   **计算开销**：虽然使用了 LoRA，但基于 SAM 的推理和实时渲染对齐在嵌入式无人机平台上的实时运行仍面临挑战。
*   **地图精度风险**：如果源 LoD 模型本身存在严重的几何缺失或偏移（如文中提到的东京部分区域），会直接限制定位精度的上限。

（完）

---
title: "OrgForge: A Multi-Agent Simulation Framework for Verifiable Synthetic Corporate Corpora"
title_zh: OrgForge：用于可验证合成企业语料库的多智能体模拟框架
authors: Jeffrey Flynt
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14997v1"
tags: ["query:ds-gen"]
score: 10.0
evidence: 使用大语言模型生成可验证合成企业语料库的框架
tldr: 针对检索增强生成（RAG）评估中真实数据集法律风险高且合成数据逻辑不一致的问题，本文提出了OrgForge开源多智能体模拟框架。该框架通过确定性Python引擎维护底层事实，仅利用大模型生成文本，确保了跨文档的时间戳因果一致性和事实可验证性。它能模拟Slack、JIRA、Git等多种企业协作数据，为RAG系统提供具有结构化基准真相的高质量合成语料库。
selection_source: fresh_fetch
motivation: 现有的RAG评估语料库存在法律风险、人口统计偏差或合成数据内部逻辑矛盾等问题，缺乏可验证的结构化基准真相。
method: 提出一种将确定性物理引擎与大模型文本生成分离的架构，利用图动态子系统和参与者局部时钟来模拟复杂的企业组织行为。
result: 成功生成了包含Slack、JIRA、Confluence和邮件等多种相互关联、时间戳一致且可追溯至不可变事件日志的企业语料。
conclusion: OrgForge为评估RAG系统提供了一个可配置、可验证且无法律风险的多模态合成数据集生成方案。
---

## 摘要
评估检索增强生成（RAG）流水线需要具备可获知的地面真值（ground truth）、时间结构以及跨制品（cross-artifact）属性的语料库，而现实世界的数据集很少能清晰地提供这些特性。现有的资源（如 Enron 语料库）存在法律歧义、人口统计偏差且缺乏结构化的地面真值。纯大语言模型（LLM）生成的合成数据虽然解决了法律问题，但引入了一个更微妙的问题：无法防止生成模型在不同文档之间产生相互矛盾的事实幻觉。我们提出了 OrgForge，这是一个开源的多智能体模拟框架，它强制执行严格的“物理-认知”边界：一个确定性的 Python 引擎维护着一个 SimEvent 地面真值总线；大语言模型仅在受验证提案约束的情况下生成表面文本。执行者本地时钟（actor-local clock）确保了所有制品类型的因果时间戳正确性，消除了因每个文档独立采样时间戳而导致的时间线不一致问题。我们形式化了三个独立于 LLM 治理组织行为的图动态子系统：基于介数中心性（betweenness centrality）的压力传播、时间边权重衰减以及 Dijkstra 升级路由。通过运行可配置的 N 天模拟，OrgForge 生成交织的 Slack 线程、JIRA 工单、Confluence 页面、Git 拉取请求和电子邮件，所有这些都可以追溯到一个共享的、不可变的事件日志。此外，我们还描述了一个因果链跟踪子系统（为每个事件累积跨制品证据图）、一个用于识别重复故障类别的混合倒数排名融合（reciprocal-rank-fusion）循环检测器，以及一个通过带门控因果链和概率丢弃模拟来路由供应商警报、客户投诉和 HR 通信的入站/出站电子邮件引擎。OrgForge 采用 MIT 许可证发布。

## Abstract
Evaluating retrieval-augmented generation (RAG) pipelines requires corpora where ground truth is knowable, temporally structured, and cross-artifact properties that real-world datasets rarely provide cleanly. Existing resources such as the Enron corpus carry legal ambiguity, demographic skew, and no structured ground truth. Purely LLM-generated synthetic data solves the legal problem but introduces a subtler one: the generating model cannot be prevented from hallucinating facts that contradict themselves across documents.We present OrgForge, an open-source multi-agent simulation framework that enforces a strict physics-cognition boundary: a deterministic Python engine maintains a SimEvent ground truth bus; large language models generate only surface prose, constrained by validated proposals. An actor-local clock enforces causal timestamp correctness across all artifact types, eliminating the class of timeline inconsistencies that arise when timestamps are sampled independently per document. We formalize three graph-dynamic subsystems stress propagation via betweenness centrality, temporal edge-weight decay, and Dijkstra escalation routing that govern organizational behavior independently of any LLM. Running a configurable N-day simulation, OrgForge produces interleaved Slack threads, JIRA tickets, Confluence pages, Git pull requests, and emails, all traceable to a shared, immutable event log. We additionally describe a causal chain tracking subsystem that accumulates cross-artifact evidence graphs per incident, a hybrid reciprocal-rank-fusion recurrence detector for identifying repeated failure classes, and an inbound/outbound email engine that routes vendor alerts, customer complaints, and HR correspondence through gated causal chains with probabilistic drop simulation. OrgForge is available under the MIT license.

---

## 论文详细总结（自动生成）

### OrgForge：用于可验证合成企业语料库的多智能体模拟框架 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：评估企业级检索增强生成（RAG）系统需要高质量的语料库，这些语料库必须具备可获知的基准真相（Ground Truth）、时间结构和跨文档一致性。
*   **现有痛点**：
    *   **真实数据（如 Enron 语料库）**：存在法律风险、隐私问题、人口统计偏差，且缺乏结构化的事实记录来验证 RAG 的准确性。
    *   **纯 LLM 生成的合成数据**：虽然规避了法律风险，但 LLM 容易产生“事实幻觉”，导致不同文档间的事实相互矛盾（例如：Slack 消息说事故 3 点开始，而 JIRA 工单说 9 点开始），破坏了评估的严谨性。
*   **核心目标**：开发一个名为 **OrgForge** 的开源框架，通过模拟企业运作逻辑，生成在逻辑、时间和因果关系上完全一致且可验证的合成语料库。

#### 2. 方法论：核心思想与关键技术
OrgForge 的核心思想是**“物理-认知边界分离”**（Separating Fact Control from Prose Generation）：由确定性的引擎控制事实，LLM 仅负责润色文字。
*   **形式化架构 $M = (S, P, V, E)$**：
    *   **$S$ (状态)**：维护系统健康度、士气、员工压力等变量。
    *   **$P$ (规划者)**：LLM 智能体提出行动建议，但不能直接修改状态。
    *   **$V$ (验证器)**：确定性函数，根据预设规则（如：士气低落时不能举行庆祝活动）审核 LLM 的建议。
    *   **$E$ (事件日志)**：不可变的 SimEvent 总线，是所有文档的唯一事实来源。
*   **三大图动态机制**：
    1.  **压力传播**：基于社交图谱的介数中心性，模拟高压员工如何将压力传染给紧密协作的同事。
    2.  **时间边权重衰减**：模拟人际关系的动态变化，频繁协作会增强联系，缺乏互动则会导致关系疏远。
    3.  **Dijkstra 升级路由**：模拟事故升级流程，信息会沿着沟通成本最低（关系最强）的路径在组织中传递。
*   **参与者局部时钟（Actor-local Clock）**：为每个员工维护独立的时间指针，确保因果一致性（例如：回复消息的时间戳绝不会早于收到消息的时间）。
*   **多模态产出**：模拟生成 Slack 聊天、JIRA 工单、Confluence 文档、Git 拉取请求、外部邮件及服务器日志。

#### 3. 实验设计
*   **场景设置**：模拟了一个拥有 43 名员工、8 个部门的合成公司，运行了 22 个工作日（约 30 个自然日）。
*   **数据集规模**：生成了 1,079 份文档和 83 个评估问题。
*   **评估维度**：设计了 8 类问题，包括检索（Retrieval）、因果（Causal）、时间推理（Temporal）、缺口检测（Gap Detection）、路由（Routing）等。
*   **对比方法（Benchmark）**：
    *   **BM25**：基于关键词匹配的传统检索。
    *   **Dense Retrieval**：基于 Stella 1.5B 模型的向量嵌入检索。
    *   **评估指标**：MRR@10（平均倒数排名）和 Recall@10（召回率）。

#### 4. 资源与算力
*   **模型使用**：主要使用 `openai.gpt-oss-120b-1` 进行文本生成。
*   **算力消耗**：
    *   **总调用次数**：953 次 LLM 调用。
    *   **运行时间**：3 小时 4 分钟。
    *   **Token 消耗**：约 5.04 亿输入 Token，3.49 亿输出 Token。
    *   **经济成本**：总计约 **285.30 美元**。
*   **硬件说明**：文中未明确提及具体的 GPU 型号，但指出了使用了 Ollama 运行 1.5B 参数的本地模型进行向量检索基准测试。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了一次完整的大规模模拟运行结果。
*   **充分性评价**：
    *   **客观性**：通过确定性引擎生成基准真相，避免了人工标注的主观偏差。
    *   **覆盖度**：涵盖了企业沟通的多种主流渠道（Slack, JIRA, Email 等）。
    *   **局限性**：目前仅展示了一个公司的模拟案例，且尚未完成端到端的 RAG 智能体闭环评估（仅做了检索基准测试）。论文提到“跨语料库矛盾率”等消融实验仍在进行中。

#### 6. 主要结论与发现
*   **检索表现**：在企业级结构化语料中，**BM25 在大多数任务上优于向量检索（Dense）**。BM25 在因果类问题上表现最强（MRR=0.54），而向量检索仅在开放式检索上略有优势。
*   **推理难点**：所有基准方法在“计划（PLAN）”和“升级（ESCALATION）”类问题上得分几乎为零，说明多跳推理和时间上下文理解仍是当前 RAG 的核心挑战。
*   **因果一致性**：通过局部时钟机制，成功消除了合成数据中常见的时间线冲突问题。

#### 7. 优点与亮点
*   **架构创新**：严格分离事实控制与文本生成，从根本上解决了合成语料库的内部矛盾问题。
*   **高度可验证**：每个生成的文档都有唯一的 SimEvent ID 对应，支持自动化的精准评分。
*   **真实感噪声**：引入了“环境社交干扰”模型（如水冷机旁的闲聊），模拟了真实办公环境中低信号、高噪声的特征。
*   **开源贡献**：提供了完整的生成工具链、评估框架及 HuggingFace 数据集。

#### 8. 不足与局限
*   **多样性受限**：员工的人格特质（Persona）目前主要靠手动配置，缺乏自动化的多样性生成。
*   **成本较高**：生成一个高质量、长周期的语料库需要消耗大量的 LLM Token 费用（单次模拟近 300 美元）。
*   **单公司视角**：目前仅能模拟单一组织内部，对于跨公司（如供应链协作）的复杂场景支持不足。
*   **模型依赖**：语料库的文学质量和专业性高度依赖于底层 LLM 的水平。

（完）

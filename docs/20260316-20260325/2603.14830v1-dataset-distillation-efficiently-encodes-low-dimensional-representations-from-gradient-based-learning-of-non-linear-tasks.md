---
title: Dataset Distillation Efficiently Encodes Low-Dimensional Representations from Gradient-Based Learning of Non-Linear Tasks
title_zh: 数据集蒸馏从非线性任务的基于梯度学习中高效编码低维表示
authors: "Yuri Kinoshita, Naoki Nishikawa, Taro Toyoizumi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14830v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 数据集蒸馏将信息编码到合成数据点中
tldr: 本文针对数据集蒸馏（DD）缺乏理论解释的问题，研究了基于梯度的两层神经网络在非线性多指数模型任务下的蒸馏机制。研究证明，DD能有效将任务的低维结构编码进合成数据中。在输入维度为d、内在维度为r的情况下，仅需极低的存储复杂度即可实现高泛化性能。这是首批通过内在维度量化压缩率并分析梯度算法下DD机制的理论工作。
selection_source: fresh_fetch
motivation: 旨在揭示数据集蒸馏如何从训练过程中提取任务相关信息并将其高效编码为合成数据的内在理论机制。
method: 对应用于两层神经网络的梯度下降数据集蒸馏算法进行了理论分析，并重点研究了非线性的多指数模型任务结构。
result: 证明了蒸馏数据能有效捕获任务的低维结构，在内存复杂度为r的平方级别时即可实现模型的高泛化能力。
conclusion: 该研究首次利用任务内在维度量化了数据集蒸馏的压缩率，为理解基于梯度的蒸馏算法提供了理论支撑。
---

## 摘要
数据集蒸馏是一种感知训练的数据压缩技术，作为缓解优化和数据存储成本的有效工具，近期引起了越来越多的关注。然而，目前的进展在很大程度上仍是经验性的。从训练过程中提取任务相关信息并将此类信息高效编码到合成数据点中的底层机制仍不明确。在本文中，我们对应用于宽度为 $L$ 的两层神经网络基于梯度训练的数据集蒸馏实际算法进行了理论分析。通过关注一种被称为多指数模型（multi-index model）的非线性任务结构，我们证明了问题的低维结构被高效地编码到了生成的蒸馏数据中。该数据集以 $\tildeΘ(r^2d+L)$ 的内存复杂度复现了具有高泛化能力的模型，其中 $d$ 和 $r$ 分别是任务的输入维度和内在维度。据我们所知，这是首批包含特定任务结构、利用其内在维度来量化压缩率，并研究仅通过基于梯度的算法实现的数据集蒸馏的理论工作之一。

## Abstract
Dataset distillation, a training-aware data compression technique, has recently attracted increasing attention as an effective tool for mitigating costs of optimization and data storage. However, progress remains largely empirical. Mechanisms underlying the extraction of task-relevant information from the training process and the efficient encoding of such information into synthetic data points remain elusive. In this paper, we theoretically analyze practical algorithms of dataset distillation applied to the gradient-based training of two-layer neural networks with width $L$. By focusing on a non-linear task structure called multi-index model, we prove that the low-dimensional structure of the problem is efficiently encoded into the resulting distilled data. This dataset reproduces a model with high generalization ability for a required memory complexity of $\tildeΘ$$(r^2d+L)$, where $d$ and $r$ are the input and intrinsic dimensions of the task. To the best of our knowledge, this is one of the first theoretical works that include a specific task structure, leverage its intrinsic dimensionality to quantify the compression rate and study dataset distillation implemented solely via gradient-based algorithms.

---

## 论文详细总结（自动生成）

### 论文总结：数据集蒸馏从非线性任务的基于梯度学习中高效编码低维表示

#### 1. 核心问题与研究动机
*   **研究背景**：深度学习依赖大规模数据，导致了巨大的存储、传输和计算成本。数据集蒸馏（Dataset Distillation, DD）通过构建极小的合成数据集来捕捉原始数据的关键信息，使模型在合成数据上训练也能达到接近原始数据的性能。
*   **核心问题**：尽管 DD 在实证研究中取得了巨大成功，但其理论基础薄弱。目前尚不清楚 DD 如何从训练过程中提取任务相关信息，以及如何将这些信息高效编码到合成数据中，尤其是在非线性任务和基于梯度的优化动态下。
*   **研究动机**：旨在揭示 DD 在处理具有低维潜在结构（如流形假设）的非线性任务时的内在机制，并量化其压缩效率。

#### 2. 方法论：核心思想与技术细节
*   **任务模型**：采用**多指数模型（Multi-index Model）** $f^*(x) = \sigma^*(B^\top x)$，其中 $B$ 是一个 $d \times r$ 的矩阵，定义了一个 $r$ 维的潜在子空间（$r \ll d$）。
*   **模型架构**：使用宽度为 $L$ 的两层 ReLU 神经网络。
*   **核心思想**：采用**渐进式数据集蒸馏（Progressive DD）**，将训练分为两个阶段：
    *   **阶段 1（特征学习蒸馏）**：利用**梯度匹配（Gradient Matching, GM）**算法。证明了蒸馏过程能将任务的潜在子空间 $S^*$ 信息编码进合成样本的输入特征中。
    *   **阶段 2（输出层蒸馏）**：利用**性能匹配（Performance Matching, PM）**或 GM 算法。在特征层已确定的基础上，通过调整合成数据的标签来训练神经网络的输出层。
*   **关键结论**：证明了仅需 $\tilde{\Theta}(r^2d + L)$ 的存储复杂度即可复现高泛化性能，这远低于存储原始数据集或完整模型参数的成本。

#### 3. 实验设计
*   **实验场景**：基于合成数据的非线性回归任务。
*   **数据集/设置**：设定输入维度 $d=10$ 或 $100$，内在维度 $r=1, 3, 10$。
*   **Benchmark 与对比方法**：
    *   **Vanilla Training**：使用全部原始数据训练。
    *   **Random Baselines**：从原始数据中随机抽取同等数量的样本作为对照。
    *   **Teacher Training**：作为蒸馏目标的基准模型。
*   **应用实验**：**迁移学习**。验证在阶段 1 蒸馏出的特征是否可以辅助学习具有相同子空间但激活函数不同的新任务。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。由于该研究侧重于理论证明和合成数据的验证，其计算开销主要集中在蒸馏过程中的大批量随机初始化（$J$ 较大）上。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组针对不同数据量 $N$、初始化批次大小 $J$、以及不同内在维度 $r$ 的对比实验。
*   **充分性与公平性**：实验设计直接对应理论推导的边界条件（如 $N$ 和 $J$ 的缩放定律）。通过与随机基准的对比，客观地展示了蒸馏数据的优越性。虽然实验是在合成任务上进行的，但对于验证其理论框架而言是充分且公平的。

#### 6. 主要结论与发现
*   **低维结构编码**：DD 能够识别并提取任务的低维内在结构，并将其转化为设计精良的合成样本。
*   **压缩效率**：DD 的存储需求主要取决于任务的内在维度 $r$ 而非输入维度 $d$，实现了极高的压缩率。
*   **算法权衡**：PM 在蒸馏阶段计算复杂度较低，但在重训练阶段成本较高；GM 则相反。
*   **特征共享**：蒸馏出的合成样本捕获了任务的通用特征，具有很强的迁移学习潜力。

#### 7. 优点（亮点）
*   **理论突破**：这是首批在非线性任务、非线性模型及纯梯度优化框架下分析 DD 的理论工作，超越了以往局限于线性或核回归（KRR）的分析。
*   **解释力强**：解释了为什么 DD 在某些任务（如 MNIST）上比其他任务（如 CIFAR-10）更有效——这取决于任务内在维度的复杂性。
*   **渐进式框架**：提出的分阶段蒸馏分析方法为理解深度神经网络的特征学习提供了清晰的视角。

#### 8. 不足与局限
*   **任务局限性**：主要针对多指数模型，虽然具有代表性，但仍无法完全覆盖现实世界中极其复杂的非线性任务。
*   **计算开销**：为了获得高性能的蒸馏数据，蒸馏过程需要大量的随机初始化（$J$ 随 $d$ 缩放），这带来了显著的计算负担。
*   **架构泛化**：研究未深入探讨跨架构蒸馏（Cross-architecture generalization）的理论保证。
*   **初始化依赖**：蒸馏效果在一定程度上依赖于合成数据的初始化方式。

（完）

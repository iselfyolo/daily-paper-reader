---
title: "CompDiff: Hierarchical Compositional Diffusion for Fair and Zero-Shot Intersectional Medical Image Generation"
title_zh: CompDiff：用于公平且零样本交叉医学图像生成的层级组合式扩散模型
authors: "Mahmoud Ibrahim, Bart Elen, Chang Sun, Gokhan Ertaylan, Michel Dumontier"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16551v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于增强数据集的组合式扩散医学图像生成
tldr: 针对医疗影像生成中存在的子群体不平衡和交叉人口统计特征缺失问题，本文提出 CompDiff 框架。该方法通过分层条件网络（HCN）将人口统计特征进行结构化分解，并与 CLIP 嵌入结合，实现了跨子群体的参数共享。实验证明，CompDiff 在胸部 X 光和眼底图像生成中显著提升了图像质量、子群体公平性以及对未见交叉特征的零样本泛化能力，有效缓解了生成器的偏见。
selection_source: fresh_fetch
motivation: 现有生成模型在不平衡医疗数据上训练时，难以高质量生成稀有或未见的人口统计交叉特征子群体的图像。
method: 提出分层组合扩散框架 CompDiff，利用分层条件网络（HCN）对人口统计特征进行因子化分解，并将其作为交叉注意力的上下文。
result: 在 MIMIC-CXR 和 FairGenMed 数据集上，CompDiff 在 FID 指标、子群体公平性及零样本交叉泛化能力上均优于现有基准方法。
conclusion: 通过在架构层面优化人口统计特征的调节方式，可以有效提升医疗影像生成的公平性及其在下游分类任务中的表现。
---

## 摘要
生成模型越来越多地被用于增强医学影像数据集，以实现更公平的人工智能。然而，一个关键假设往往未被检验：即生成器本身在不同的人口统计群体中是否能产生同等高质量的图像。在不平衡数据上训练的模型可能会继承这些不平衡，导致稀有子群体的合成质量下降，并难以处理训练集中缺失的人口统计交叉项。我们将其称为“不平衡生成器问题”。现有的补救措施（如损失重加权）在优化层面起作用，但在某些组合的训练信号稀缺或缺失时，其效果有限。我们提出了 CompDiff，这是一个在表示层面解决该问题的层级组合式扩散框架。一个专门的层级条件网络（HCN）对人口统计条件进行分解，生成一个与 CLIP 嵌入拼接的人口统计标记，作为交叉注意力的上下文。这种结构化分解促进了子群体间的参数共享，并支持对稀有或未见的人口统计交叉项的组合泛化。在胸部 X 光片（MIMIC-CXR）和眼底图像（FairGenMed）上的实验表明，CompDiff 在图像质量（FID：64.3 对比 75.1）、子群体公平性（ES-FID）以及零样本交叉泛化（在预留交叉项上 FID 提升高达 21%）方面均优于标准微调和 FairDiffusion。在 CompDiff 生成的数据上训练的下游分类器也表现出更高的 AUROC 和更低的人口统计偏差，这表明人口统计条件的架构设计是公平医学图像生成中一个重要且尚未被充分探索的因素。代码可在 https://anonymous.4open.science/r/CompDiff-6FE6 获取。

## Abstract
Generative models are increasingly used to augment medical imaging datasets for fairer AI. Yet a key assumption often goes unexamined: that generators themselves produce equally high-quality images across demographic groups. Models trained on imbalanced data can inherit these imbalances, yielding degraded synthesis quality for rare subgroups and struggling with demographic intersections absent from training. We refer to this as the imbalanced generator problem. Existing remedies such as loss reweighting operate at the optimization level and provide limited benefit when training signal is scarce or absent for certain combinations. We propose CompDiff, a hierarchical compositional diffusion framework that addresses this problem at the representation level. A dedicated Hierarchical Conditioner Network (HCN) decomposes demographic conditioning, producing a demographic token concatenated with CLIP embeddings as cross-attention context. This structured factorization encourages parameter sharing across subgroups and supports compositional generalization to rare or unseen demographic intersections. Experiments on chest X-rays (MIMIC-CXR) and fundus images (FairGenMed) show that CompDiff compares favorably against both standard fine-tuning and FairDiffusion across image quality (FID: 64.3 vs. 75.1), subgroup equity (ES-FID), and zero-shot intersectional generalization (up to 21% FID improvement on held-out intersections). Downstream classifiers trained on CompDiff-generated data also show improved AUROC and reduced demographic bias, suggesting that architectural design of demographic conditioning is an important and underexplored factor in fair medical image generation. Code is available at https://anonymous.4open.science/r/CompDiff-6FE6.

---

## 论文详细总结（自动生成）

### 论文总结：CompDiff - 用于公平且零样本交叉医学图像生成的层级组合式扩散模型

#### 1. 核心问题与整体含义（研究动机和背景）
在医疗人工智能领域，生成模型常被用于扩充数据集以提升模型的公平性。然而，论文指出一个被忽视的**“不平衡生成器问题”**：如果生成模型本身是在不平衡的数据集上训练的，它会继承这些偏见，导致稀有子群体（如特定高龄、少数族裔女性的交叉组合）的图像生成质量低下。现有的方法（如 FairDiffusion）主要在优化层面通过损失重加权来解决，但这无法处理训练集中完全缺失的“人口统计交叉项”（Demographic Intersections）。本研究旨在从**表示学习**的角度，通过结构化的架构设计，使模型能够通过已知的单属性特征“组合”出未见的交叉特征图像。

#### 2. 核心方法论
CompDiff 的核心是**层级条件网络（Hierarchical Conditioner Network, HCN）**，其思想源于“组合性”（Compositionality）：

*   **核心思想**：将复杂的人口统计身份分解为层级结构。例如，“80岁亚洲女性”可以由“80岁”、“亚洲人”、“女性”这些基础属性以及它们之间的两两交互作用组合而成。
*   **关键技术细节**：
    *   **层级结构**：
        1.  **单属性嵌入（祖父母层）**：独立编码年龄、性别、种族。
        2.  **两两交互（父母层）**：通过 MLP 建模属性间的非线性关系（如年龄与性别的交互）。
        3.  **全组合（孩子层）**：融合所有交互信息生成最终的人口统计 Token。
    *   **上下文拼接**：HCN 输出的 Token 与 CLIP 文本嵌入拼接，共同作为扩散模型 UNet 交叉注意力的上下文。
    *   **损失函数**：总损失由四部分组成：
        1.  **扩散损失 ($L_{diff}$)**：标准的去噪损失。
        2.  **组合一致性损失 ($L_{comp}$)**：约束组合后的表示不要偏离简单加法基准太远，起到正则化作用。
        3.  **辅助分类损失 ($L_{aux}$)**：在投影后的 Token 上直接施加监督，确保人口统计信息不丢失。
        4.  **KL 散度 ($L_{KL}$)**：对变分潜在空间进行正则化。

#### 3. 实验设计
*   **数据集**：
    *   **MIMIC-CXR**：胸部 X 光片，包含年龄、性别、种族等元数据。
    *   **FairGenMed**：眼底图像，包含青光眼临床指标及人口统计信息。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准微调的 Stable Diffusion 2.1（人口统计信息放在文本提示词中）。
    *   **FairDiffusion**：目前的 SOTA 方法，采用贝叶斯扰动和损失重加权。
*   **评估指标**：FID（图像质量）、ES-FID（公平性缩放 FID）、疾病分类 AUROC、属性预测准确率、以及下游任务的 TSTR（在合成数据上训练，在真实数据上测试）表现。

#### 4. 资源与算力
*   **参数量**：HCN 架构非常轻量，仅比基准模型增加了 **0.19%** 的可训练参数。
*   **训练细节**：模型训练了 30,000 步。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号（如 A100 或 V100）及具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同模态（X 光和眼底图）上进行了大规模实验。
*   **消融实验**：非常充分。作者对比了“无人口统计信息”、“双文本分支”、“扁平 MLP 编码器”、“无辅助损失”等多种变体，证明了**层级架构**和**特定位置的辅助监督**是成功的关键。
*   **零样本验证**：专门设计了“留出实验”，将 5 个人口统计交叉组合从训练集中完全剔除，验证模型的零样本泛化能力。
*   **客观性**：使用了多种自动化评估指标和下游分类器验证，实验设计逻辑严密，对比公平。

#### 6. 主要结论与发现
*   **质量与公平双赢**：CompDiff 在提升整体图像质量（FID 显著下降）的同时，大幅降低了不同群体间的质量差异（ES-FID 表现最优）。
*   **零样本泛化**：在完全未见的交叉子群体上，CompDiff 的 FID 提升高达 21%，证明了组合式表示的有效性。
*   **下游增益**：使用 CompDiff 生成的数据训练分类器，不仅提高了疾病检测的 AUROC，还显著降低了针对特定群体的“漏诊率”（Underdiagnosis Rate）。

#### 7. 优点
*   **架构创新**：不同于以往只关注优化算法的研究，本文证明了**条件调节的架构设计**对公平性至关重要。
*   **零样本能力**：成功解决了训练数据中缺失特定交叉组合的难题。
*   **高效性**：参数增加极少，且不改变扩散模型的采样过程，易于集成。

#### 8. 不足与局限
*   **临床验证缺失**：评价主要依赖于定量指标（如 FID 和分类器准确率），缺乏放射科医生的主观视觉评估。
*   **属性限制**：目前的层级结构假设属性是离散且结构化的，对于连续属性或非结构化的人口统计特征支持有限。
*   **数据依赖**：虽然能处理未见的“组合”，但仍依赖于模型在单属性上的学习效果；如果某个单属性（如极少数族裔）在数据中彻底缺失，模型依然无法生成。

（完）

---
title: Data-efficient pre-training by scaling synthetic megadocs
title_zh: 通过扩展合成巨型文档实现高效数据预训练
authors: "Konwoo Kim, Suhas Kotha, Yejin Choi, Tatsunori Hashimoto, Nick Haber, Percy Liang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18534v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 通过缩放合成文档实现高效数据预训练
tldr: 本研究探讨了在预训练数据受限时，如何通过合成数据增强提升模型性能。研究发现，将同一原始文档的多个合成改写版本拼接成“超长文档（Megadocs）”，或通过插入推理过程拉长文档，比简单的改写数据增强更有效。这种方法不仅在验证损失和下游基准测试中表现更优，还显著提升了长上下文处理能力，在每篇文档生成32个版本时，数据效率提升达1.80倍，且优势随合成数据量增加而扩大。
selection_source: fresh_fetch
motivation: 旨在解决预训练数据受限时，如何设计合成数据算法以实现更好的损失缩放并提升计算效率。
method: 提出“超长文档（Megadocs）”概念，通过拼接同一文档的多个合成改写版本或插入推理过程来构建长文本。
result: Megadocs 将数据效率从 1.48 倍提升至 1.80 倍，并显著改善了长上下文损失和下游任务表现。
conclusion: 通过构建超长合成文档，模型能在数据受限的情况下更有效地从增加的计算资源中获益。
---

## 摘要
当预训练受限于数据而非计算资源时，合成数据增强已成为一种极具前景的解决方案。我们研究了如何设计合成数据算法以实现更好的损失缩放（loss scaling）：不仅在有限计算量下降低损失，特别是在计算量趋于无穷大时。我们首先证明，在网络数据中混合合成生成的改写（rephrases）进行预训练，可以改善网络数据上的独立同分布（i.i.d.）验证损失，尽管合成数据来自完全不同的分布。通过最优的混合和训练轮数（epoching）设置，随着合成生成数量的增加，损失和基准测试准确率在不发生过拟合的情况下得到提升，在每篇文档生成 32 个改写时，数据效率在 1.48 倍附近达到平台期。我们从一个新的视角发现了更好的损失缩放：来自同一文档的合成生成内容可以组成一个显著更长的单一“巨型文档”（megadocument），而不是许多短文档。我们展示了两种构建巨型文档的方法：拼接来自同一网络文档的合成改写，或者通过插入推理过程（rationales）来拉伸文档。与简单的改写相比，这两种方法都改善了 i.i.d. 损失、下游基准测试，尤其是长上下文损失，在每篇文档生成 32 个版本时，将数据效率从 1.48 倍提高到 1.80 倍。重要的是，随着合成数据生成量的增加，巨型文档相对于简单改写的改进效果会进一步扩大。我们的研究结果展示了在数据受限的情况下，如何设计能从增加的计算量中获益更多的合成数据算法。

## Abstract
Synthetic data augmentation has emerged as a promising solution when pre-training is constrained by data rather than compute. We study how to design synthetic data algorithms that achieve better loss scaling: not only lowering loss at finite compute but especially as compute approaches infinity. We first show that pre-training on web data mixed with synthetically generated rephrases improves i.i.d. validation loss on the web data, despite the synthetic data coming from an entirely different distribution. With optimal mixing and epoching, loss and benchmark accuracy improve without overfitting as the number of synthetic generations grows, plateauing near $1.48\times$ data efficiency at 32 rephrases per document. We find even better loss scaling under a new perspective: synthetic generations from the same document can form a single substantially longer megadocument instead of many short documents. We show two ways to construct megadocs: stitching synthetic rephrases from the same web document or stretching a document by inserting rationales. Both methods improve i.i.d. loss, downstream benchmarks, and especially long-context loss relative to simple rephrasing, increasing data efficiency from $1.48\times$ to $1.80\times$ at $32$ generations per document. Importantly, the improvement of megadocs over simple rephrasing widens as more synthetic data is generated. Our results show how to design synthetic data algorithms that benefit more from increasing compute when data-constrained.

---

## 论文详细总结（自动生成）

这篇论文由斯坦福大学的研究团队发表，探讨了在预训练数据受限的情况下，如何通过构建“合成巨型文档（Synthetic Megadocs）”来提升语言模型的预训练效率和性能。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前大模型预训练面临“数据荒”，计算资源的增长速度远超互联网可用文本的增长速度。
*   **核心问题**：如何设计合成数据算法，使其不仅在有限计算量下有效，且在计算量趋于无穷时仍能保持良好的损失缩放（Loss Scaling）特性？
*   **背景**：以往的合成数据研究多关注于提升数据质量或针对特定下游任务，而本研究关注的是如何利用合成数据更好地建模原始的网络数据分布（即预训练的核心任务）。

### 2. 论文提出的方法论
论文提出了**Megadocs（巨型文档）**的概念，认为与其将同一原始文档的多个合成版本视为独立短文档，不如将其整合为长文档。
*   **核心思想**：利用合成数据增加文档的“深度”和“长度”，而非仅仅增加文档的数量。
*   **关键技术细节**：
    *   **拼接改写（Stitched Rephrasing）**：将针对同一原始文档生成的 $G$ 个不同改写版本（Rephrases）拼接在一起，形成一个超长文档。实验发现，将原始文档放在拼接序列的末尾效果最好。
    *   **潜意识推理（Latent Thoughts）**：将原始文档切分为多个片段，在片段之间插入由生成器产生的推理过程（Rationales），从而“拉伸”文档长度。
    *   **混合与训练策略**：采用双流混合模式，即“真实数据流”和“合成+真实混合流”。通过精细调节混合比例（Mixing Fraction）和真实数据的训练轮数（Epochs），确保模型在不发生过拟合的情况下从合成数据中获益。

### 3. 实验设计
*   **数据集**：使用 DCLM 数据集中的 2 亿（200M）个真实 Token（约 16.4 万篇文档）。
*   **模型规模**：主要使用 3 亿（300M）参数的 Transformer，部分实验扩展至 15 亿（1.5B）参数。
*   **生成器**：使用 Llama 3.1 8B Instruct 作为外部合成数据生成器。
*   **Benchmark**：
    *   **i.i.d. 验证损失**：在原始网络数据分布上的损失。
    *   **长上下文损失**：在 arXiv 论文、维基百科长文章、Github 代码上的损失。
    *   **下游任务**：PIQA、SciQ、ARC Easy 等高信号基准测试。
*   **对比方法**：仅真实数据基准、简单改写（Simple Rephrasing，将合成文档打乱作为独立样本）。

### 4. 资源与算力
*   **硬件支持**：实验主要在 **Google TPU v4-64** 集群上运行，由 Google TPU 研究云（TRC）支持。
*   **训练细节**：使用了混合精度训练（fp32 参数，bf16 计算）。
*   **说明**：文中未明确给出总的 GPU/TPU 小时数，但提到所有超参数（学习率、权重衰减、轮数、混合比例）均经过了“局部最优”搜索，暗示了巨大的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了从每篇文档生成 1 个到 32 个合成版本的缩放实验。
*   **消融实验**：包括跨文档注意力掩码（Cross-document attention masking）的必要性、真实数据在合成流中的作用、学生模型规模缩放（300M vs 1.5B）等。
*   **客观性与公平性**：研究采用了“局部最优超参数搜索”方法，确保每种对比方法都在其最佳配置下运行，避免了因超参数未对齐导致的偏差。同时，所有代码和 WandB 运行记录均已开源。

### 6. 论文的主要结论与发现
*   **数据效率显著提升**：在每篇文档生成 32 个版本时，简单改写可实现 1.48 倍数据效率，而 Megadocs（尤其是 Latent Thoughts）可将其提升至 **1.80 倍**。
*   **缩放特性更优**：Megadocs 的性能随生成数量增加而持续提升，且相比简单改写，其平台期（Plateau）出现得更晚。
*   **长上下文优势**：Megadocs 显著降低了长文本上的验证损失，证明这种构造方式有助于模型学习长程依赖。
*   **可组合性**：合成数据增强与“模型集成（Ensembling）”方法具有良好的叠加效应，能够进一步降低损失下限。

### 7. 优点
*   **视角新颖**：将合成数据视为文档结构的扩展（Megadocs）而非单纯的样本扩充，为数据增强提供了新思路。
*   **严谨的缩放分析**：不仅关注单一指标提升，更关注损失随计算量和数据量增加的缩放规律。
*   **实战价值高**：提出的拼接策略和混合训练配方对于实际的大模型预训练具有直接的指导意义。

### 8. 不足与局限
*   **生成器依赖**：实验依赖于一个比学生模型更强大的外部生成器（Llama 3.1 8B），虽然作者通过消融实验论证了“自我改进”的可能性，但在完全从零开始的自循环场景下效果仍待验证。
*   **模型规模限制**：主要实验集中在 300M 和 1.5B 规模，尚未在百亿或千亿级参数模型上验证其缩放定律的稳定性。
*   **计算成本**：生成大量合成数据（如每篇文档 32 个版本）并进行多轮预训练，对推理和训练算力提出了极高要求。

（完）

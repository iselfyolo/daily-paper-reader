---
title: "DRTriton: Large-Scale Synthetic Data Reinforcement Learning for Triton Kernel Generation"
title_zh: DRTriton：用于 Triton Kernel 生成的大规模合成数据强化学习
authors: "Siqi Guo, Ming Lin, Tianbao Yang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21465v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 用于训练大语言模型的数据合成算法CSP-DAG
tldr: 针对生成式AI中CUDA算子开发难度大的问题，本文提出DRTriton框架，通过强化学习训练LLM将PyTorch代码转换为高性能Triton算子。该框架利用CSP-DAG算法生成大规模合成数据，结合解耦奖励的课程强化学习和推理时搜索，显著提升了算子转换的成功率和执行速度。实验表明，仅在合成数据上训练的DRTriton-7B在KernelBench Level 2上的表现远超GPT-5.2等顶尖模型。
selection_source: fresh_fetch
motivation: 现有大语言模型在将PyTorch代码自动转换为高效CUDA/Triton算子方面仍面临巨大挑战。
method: 提出包含CSP-DAG合成数据生成、解耦奖励的课程强化学习以及推理时搜索算法的扩展性学习框架。
result: "DRTriton-7B在KernelBench Level 2中实现了92%的加速优化，大幅领先于GPT-5.2和Claude-Sonnet-4.5。"
conclusion: 通过大规模合成数据和强化学习，可以有效训练出在真实世界复杂算子优化任务中超越人类专家水平的小型语言模型。
---

## 摘要
开发高效的 CUDA kernel 是生成式人工智能行业中一项基础且具有挑战性的任务。最近的研究利用大语言模型 (LLMs) 将 PyTorch 参考实现自动转换为 CUDA kernel，从而显著减少了工程工作量。然而，即使是目前最先进的 LLM（如 GPT-5.2 和 Claude-Sonnet-4.5）在这一特定任务上仍然面临困难。为了应对这一挑战，我们提出了 DRTriton，这是一个可扩展的学习框架，用于训练 LLM 将 PyTorch 代码转换为高度优化的 Triton kernel，这些 kernel 随后在运行时被编译为 CUDA kernel。DRTriton 由三个关键组件组成：(i) 一种名为 CSP-DAG 的数据合成算法，它保证了在难度可控的情况下，对算子空间进行全覆盖和无偏均匀采样；(ii) 一种具有解耦奖励的课程强化学习，可同时高效地优化转换成功率和推理速度；(iii) 一种测试时搜索算法，进一步提高了生成的 Triton kernel 的推理速度。值得注意的是，尽管完全在合成数据上进行训练，DRTriton 仍能有效地泛化到现实世界中即使对人类专家也具有挑战性的 CUDA kernel。实验结果表明，DRTriton-7B 在 KernelBench Level 2 的 92% 任务上实现了加速，相比之下，GPT-5.2 的加速比例为 23%，Claude-Sonnet-4.5 为 19%。

## Abstract
Developing efficient CUDA kernels is a fundamental yet challenging task in the generative AI industry. Recent researches leverage Large Language Models (LLMs) to automatically convert PyTorch reference implementations to CUDA kernels, significantly reducing the engineering efforts. State-of-the-art LLMs, such as GPT-5.2 and Claude-Sonnet-4.5, still struggle in this specific task. To address this challenge, we propose DRTriton, a scalable learning framework for training LLMs to convert PyTorch codes into highly optimized Triton kernels, which are then compiled to CUDA kernels at runtime. DRTriton consists of three key components: (i) a data synthetic algorithm CSP-DAG that guarantees full coverage and unbiased uniform sampling over the operator space with controlled difficulty; (ii) a curriculum reinforcement learning with decoupled reward efficiently optimizes conversion success rate and inference speed simultaneously; and (iii) a test-time search algorithm that further improves the inference speed of the generated Triton kernels. Notably, despite being trained exclusively on synthetic data, DRTriton generalizes effectively to real-world CUDA kernels that are challenging even for human experts. Experimental results show that DRTriton-7B achieves speedup on 92% of the KernelBench Level 2, compared to 23% for GPT-5.2 and 19% for Claude-Sonnet-4.5.

---

## 论文详细总结（自动生成）

### DRTriton: 用于 Triton Kernel 生成的大规模合成数据强化学习总结

#### 1. 核心问题与研究动机
*   **研究背景**：在生成式 AI 行业中，开发高效的 CUDA 算子（Kernel）是降低推理成本的关键，但其开发门槛极高，即使是专家也需要大量时间调试。
*   **核心挑战**：
    *   **现有模型能力不足**：尽管 GPT-5.2、Claude-Sonnet-4.5 等顶尖模型在通用代码生成上表现出色，但在将 PyTorch 转换为高性能 Triton/CUDA 算子时仍面临正确率低和性能优化差的问题。
    *   **训练数据匮乏**：现有的开源 PyTorch-Triton 对齐数据规模极小（仅万级），且缺乏对算子复杂度和质量的有效控制。
*   **研究动机**：探索是否能仅通过大规模、高质量的**合成数据**和**强化学习**，训练出一个在算子优化能力上超越商业大模型的小型专家模型。

#### 2. 方法论
DRTriton 框架由三个核心技术组件构成：
*   **CSP-DAG 合成算法**：
    *   **核心思想**：将 PyTorch 程序生成建模为有向无环图（DAG）上的约束满足问题（CSP）。
    *   **流程**：首先随机生成算子节点并连接成 DAG；然后利用 **CP-SAT 求解器** 自动计算满足算子语义（如矩阵乘法的维度匹配）的张量形状。
    *   **优势**：保证了生成的程序在语法和张量形状上的 100% 正确性，并实现了对算子空间的均匀采样。
*   **解耦奖励的课程强化学习 (DRPO)**：
    *   **课程设计**：采用从易到难的策略，分为单算子（Level 1）、双算子（Level 2）到五算子（Level 5）三个阶段进行强化学习。
    *   **解耦奖励**：将“正确性奖励”与“速度奖励”分离。在 DRPO 框架下，正确答案根据其相对于 PyTorch 的加速比进行加权，而错误答案则通过 Log-Sum-Exp 函数进行惩罚，解决了早期训练中奖励稀疏的问题。
*   **测试时搜索 (Test-time Search)**：
    *   针对复杂程序，将长代码分解为多个片段（Fragment），利用模型为每个片段生成 Kernel，并搜索最优的 Kernel 组合方案，以解决单次生成难以覆盖超长复杂逻辑的问题。

#### 3. 实验设计
*   **数据集/场景**：
    *   **合成基准测试 (Synthetic Benchmark)**：包含 406 个未见过的 PyTorch 程序，涵盖 1、2、5、20 个算子的不同难度等级。
    *   **真实世界基准测试 (KernelBench)**：包含 250 个真实的神经网络操作（如 MobileNet、VGG、MiniGPT 等），分为三个难度等级。
*   **对比方法**：
    *   **商业模型**：GPT-5.2、Claude-Sonnet-4.5。
    *   **开源模型**：DeepSeek-R1、Qwen-3-Coder-480B。
    *   **专用模型**：AutoTriton。
    *   **编译器底座**：Torch Eager、`torch.compile`。
*   **评估指标**：准确率 (Acc)、加速比例 (Faster1，即正确且比 PyTorch 快)、几何平均加速比。

#### 4. 资源与算力
*   **模型底座**：基于 Qwen-2.5-Coder-7B-Instruct 进行微调。
*   **训练规模**：
    *   **SFT 阶段**：使用 2,026 个由 DeepSeek-R1/GPT-5.2 生成并经过验证的单算子对。
    *   **RL 阶段**：在 100,000 个合成 PyTorch 程序上进行强化学习。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到 SFT 训练了 10 个 Epoch，RL 的每个阶段训练了 1 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的测试，包括 10 万量级的训练样本和跨越多个难度等级的测试集。
*   **消融实验**：
    *   对比了 **DRPO 与 GRPO** 算法的性能，证明了奖励解耦的优越性。
    *   测试了**不同的速度奖励函数**（对数函数 vs 幂函数）对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅在合成数据上验证，还通过自动重写工具在真实世界的 KernelBench 上进行了泛化性测试。对比对象涵盖了目前最强的闭源和开源模型，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **合成数据胜过真实数据**：尽管 DRTriton 仅在合成数据上训练，但在真实世界任务（KernelBench）上的表现远超在真实代码库上训练的模型。
*   **性能卓越**：DRTriton-7B 在 KernelBench Level 2 任务中，92% 的案例实现了加速，而 GPT-5.2 仅为 23%。
*   **课程学习的关键性**：通过从简单到复杂的逐步引导，模型能够学习到复杂的算子融合逻辑，这是直接在大规模复杂任务上训练难以实现的。
*   **测试时搜索的增益**：对于极复杂的程序（如 20 个算子），测试时搜索将准确率从 0% 提升到了 99%。

#### 7. 优点
*   **数据生成方案优雅**：CSP-DAG 解决了合成代码“不可运行”的痛点，提供了无限的高质量训练源。
*   **泛化能力强**：证明了在受控合成环境下的强化学习可以产生极强的领域泛化能力。
*   **实用性高**：提出的测试时搜索和代码重写工具，使其能够直接应用于现有的 PyTorch 项目优化。

#### 8. 不足与局限
*   **算子覆盖范围**：目前仅支持 53 种常用 PyTorch 算子，对于更冷门或自定义的算子支持有限。
*   **格式依赖**：模型高度依赖于“函数式”的 PyTorch 代码格式，对于复杂的类结构或非标准写法，需要依赖重写工具的转换质量。
*   **硬件局限**：目前主要针对 NVIDIA GPU 的 Triton 生态，尚未讨论在其他硬件架构（如昇腾、ROCm）上的迁移效果。

（完）

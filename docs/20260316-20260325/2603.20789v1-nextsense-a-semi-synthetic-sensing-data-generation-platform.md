---
title: "NextSense: A Semi-Synthetic Sensing Data generation Platform"
title_zh: NextSense：一种半合成感知数据生成平台
authors: "David Rico Menéndez, Pablo Picazo-Martínez, Antonio de la Oliva, Carlos Jesús Bernardos, Chathura Sarathchandra, Alain Mourad"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20789v1"
tags: ["query:ds-gen"]
score: 9.0
evidence: 半合成感知数据生成平台
tldr: 针对集成感知与通信（ISAC）应用中真实数据采集成本高、难度大的问题，本文推出了NextSense开源模块化半合成数据生成平台。该平台集成了5G协议栈、信道仿真器和用户设备（UE）仿真器，支持通过API和GUI对无线配置、信道、移动性及流量进行全面定制。NextSense能生成包含符号级IQ样本、协议轨迹和关键性能指标的多维度数据，为ISAC研究提供了高效的数据支撑。
selection_source: fresh_fetch
motivation: 现有的ISAC应用需要海量数据，但在真实网络中获取底层测量数据成本极高且难以实现。
method: 提出并构建了NextSense平台，通过集成5G协议栈与仿真组件，实现高度可定制的半合成数据生成。
result: 平台能够输出跨UE、RAN和核心网的多维度数据，包括IQ样本、协议追踪和KPI指标。
conclusion: 实验验证表明NextSense可以作为真实测量数据的可靠替代方案，有效支持感知用例的研究。
---

## 摘要
新兴的通感一体化（ISAC）应用需要海量数据，但在实际网络中收集此类数据集不仅成本高昂、耗时，且由于对底层测量数据的访问受限，往往难以实现。在本文中，我们提出了 NextSense，这是一个开放且模块化的半合成数据生成平台，由 5G 协议栈、信道仿真器和用户设备（UE）仿真器组成。该平台允许用户通过 API 和 GUI 对无线配置、信道与移动性以及流量特征进行全面定制，并生成结合了符号级 IQ 采样、协议跟踪以及跨 UE、无线接入网（RAN）和核心网（CN）的关键性能指标的多视角输出。本文描述了 NextSense 的架构，并验证了其在感知用例中作为真实测量数据可靠代理的能力。

## Abstract
Emerging integrated sensing and communication (ISAC) applications require large volumes of data, but collecting such datasets in real networks is costly, time consuming, and often infeasible due to limited access to low level measurements. In this paper we present NextSense, an open and modular semi-synthetic data generation platform that consists of a 5G stack, a channel emulator, and an UE emulator. The platform allows users full customization on radio configuration, channel and mobility, and traffic profiles through an API and GUI, and produces multi-perspective outputs that combine symbol-level IQ samples, protocol traces, and key performance indicators across UE, RAN, and CN. This paper describes the NextSense's architecture, and validates its ability to act as a faithful proxy for real measurements in sensing use cases.

---

## 论文详细总结（自动生成）

以下是对论文《NextSense: A Semi-Synthetic Sensing Data generation Platform》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着 6G 时代的到来，**通感一体化（ISAC）**应用对海量、高维度的底层数据（如原始 IQ 采样、细粒度信道状态信息 CSI）产生了巨大需求，以训练复杂的 AI/ML 模型。然而，获取这些数据面临三大障碍：
*   **获取难：** 运营商出于隐私和商业机密考虑，极少开放商用网络的 L1 层原始数据。
*   **成本高：** 在真实环境中部署大规模测试床进行数据采集既耗时又昂贵。
*   **灵活性差：** 现有的仿真工具往往过于理想化，而硬件测试床通常是封闭且难以自动化的。
**NextSense** 的提出旨在填补这一空白，通过构建一个**开放、模块化、自动化的半合成数据生成平台**，为研究人员提供可定制、可重复且具有高保真度的感知数据集。

### 2. 论文提出的方法论
NextSense 的核心思想是结合**真实的 5G 协议栈**与**硬件信道仿真器**，产生“半合成”数据。
*   **核心架构：** 平台由编排器（Orchestrator）统一管理，包含四个主要组件：
    1.  **OAI 5G Core & gNB：** 基于 OpenAirInterface 的开源 5G 核心网和基站，提供符合 3GPP R17 标准的协议栈。
    2.  **Keysight PROPSIM F64：** 硬件信道仿真器，负责模拟多径衰落、多普勒效应和复杂的空间信道模型（支持 3GPP TDL 或自定义 MATLAB 场景）。
    3.  **Amarisoft UE Emulator：** 软件定义的终端仿真器，支持多 UE 实例化及复杂的移动性轨迹。
    4.  **API/GUI 编排层：** 用户通过 JSON 配置文件或图形界面定义无线参数、移动路径和流量特征。
*   **关键技术细节：** 编排器通过 REST/SCPI 接口同步各组件，捕获从物理层（符号级 IQ 采样）到应用层（IP 包）的全堆栈日志。

### 3. 实验设计
为了验证半合成数据的有效性，论文设计了一个**室内存在检测（Indoor Presence Detection）**的验证实验：
*   **场景设置：** 对比了三种环境条件：空房间、有人存在、有移动机器人存在。
*   **基准（Benchmark）：** 使用在真实室内环境中采集的真实 5G SSB（同步信号块）信号作为基准数据集。
*   **对比方法：** 
    *   **统计分布对比：** 比较真实数据与 NextSense 生成数据的能量方差、概率密度函数（PDF）和累积分布函数（CDF）。
    *   **下游任务验证：** 使用支持向量机（SVM）进行二分类任务（检测机器人是否遮挡 LoS 路径）。
*   **验证指标：** 采用了 Kolmogorov-Smirnov (KS) 检验、Wasserstein 距离 (WD) 以及时间自相关性分析。

### 4. 资源与算力
*   **硬件平台：** 论文提到使用 **NVIDIA DGX Spark** 作为 AI 服务节点，用于处理、标注和存储生成的感知数据。
*   **具体算力细节：** 文中未明确给出 SVM 训练的具体时长或 GPU 占用率，但考虑到 SVM 处理的是提取后的特征，其算力需求相对较低。主要的资源消耗在于 Keysight PROPSIM 硬件仿真器和 OAI 协议栈的实时运行。

### 5. 实验数量与充分性
*   **实验规模：** 采集了包含 360 个子载波、4 个符号、100 个快照的数据张量进行统计分析。
*   **充分性评价：** 
    *   **统计学验证充分：** 通过 KS 检验（p > 0.05）和 WD 距离证明了合成数据与真实数据在统计分布上几乎不可区分。
    *   **跨域验证：** 实验不仅验证了信号的物理特性，还通过“合成数据训练、真实数据测试”的迁移学习实验（准确率达 98.33%）证明了数据的实用性。
    *   **局限性：** 实验场景目前集中在室内静态/低速场景，对于高速移动或大规模室外场景的验证尚不充分。

### 6. 论文的主要结论与发现
*   **高保真度：** NextSense 生成的半合成数据在能量动力学、相位统计和时间相干性方面与真实测量数据高度一致。
*   **模型可迁移性：** 在半合成数据上预训练的模型可以直接应用于真实环境，且表现优异，证明了该平台作为“数据代理”的可靠性。
*   **自动化优势：** 通过 API 驱动的编排，研究人员可以快速生成数千组不同配置的标注数据集，极大地提升了 ISAC 研究的效率。

### 7. 优点
*   **开放性与模块化：** 基于开源 OAI 栈，降低了研究门槛，且组件可灵活替换。
*   **全堆栈可见性：** 能够同步捕获从底层 IQ 采样到高层信令（如 RRC/NAS）的数据，这在商用设备中极难实现。
*   **硬件在环（HiL）：** 引入硬件信道仿真器，比纯软件仿真更接近物理现实，比真实测试床更具可控性和重复性。

### 8. 不足与局限
*   **硬件依赖：** 虽然协议栈开源，但核心的信道仿真（Keysight PROPSIM）依赖昂贵的专用硬件，普通研究机构可能难以负担。
*   **相干结构验证不足：** 论文承认目前主要验证了信号幅度和基本时间相关性，对于更复杂的相干信号结构（Coherent signal-level structure）的验证仍需进一步工作。
*   **场景覆盖面：** 目前仅展示了简单的室内检测用例，尚未涵盖 6G 愿景中的复杂多用户、超大规模 MIMO 或高频段（mmWave/THz）场景。

（完）

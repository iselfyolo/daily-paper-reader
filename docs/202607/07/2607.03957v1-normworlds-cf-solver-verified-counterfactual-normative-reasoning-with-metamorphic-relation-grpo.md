---
title: "NormWorlds-CF: Solver-Verified Counterfactual Normative Reasoning with Metamorphic-Relation GRPO"
title_zh: NormWorlds-CF：基于蜕变关系 GRPO 的求解器验证反事实规范推理
authors: Xinqi Zhang
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03957v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 可执行规则世界中的反事实规范推理
tldr: 针对语言模型在规范性推理中可能“因错得对”的问题，本文提出 NormWorlds-CF 环境，利用确定性求解器生成可验证的对偶世界推理路径。研究发现仅监督答案会导致模型无法处理证伪任务，为此提出了变态关系 GRPO（MR-GRPO）算法，通过类条件奖励强化模型对关系族和变化字段的理解。实验证明 MR-GRPO 在答案准确性与结构化推理一致性上均优于传统强化学习方法。
selection_source: fresh_fetch
motivation: 解决语言模型在规范性推理中缺乏逻辑一致性、容易因错误原因得出正确答案的问题。
method: 提出基于求解器验证的 NormWorlds-CF 环境，并引入 MR-GRPO 算法，利用变态关系提供细粒度的类条件奖励。
result: MR-GRPO 在 Qwen3-4B 等模型上实现了答案变化、支持集变化和关系族结构等多个维度的最强平衡性能。
conclusion: 验证的反事实结构能有效引导模型后训练，但精确的完整变更记录生成和分布外迁移仍是未来挑战。
---

## 摘要
语言模型可能会因为错误的理由而得出正确的规范性结论。我们介绍了 NormWorlds-CF，这是一个在可执行规则世界中进行反事实规范推理的求解器验证环境。其确定性求解器可生成最终答案、证明与证伪凭证、论点状态、支持集以及配对世界变化标签，从而在无需 LLM 评判员的情况下实现监督与评估。该基准测试包含阶段性 SFT 诊断和一个紧凑的配对世界任务，涉及 270 个根家族和 1080 个规范-变体对。SFT 诊断表明，仅针对最终答案的监督是一种不安全的代理方式：仅答案 SFT 在答案任务上达到了完美的准确率，但在证伪任务上得分却为零；而结合针对性回放的证明加证伪训练则在所有任务上都取得了较强的准确率。针对结构化变化任务，我们引入了蜕变关系 GRPO (MR-GRPO)，这是一种用于 GRPO 的类别条件奖励，它根据关系家族和求解器可见的变化字段给予部分奖励。在匹配的 1.7B 续写实验中，与稀疏奖励和仅答案 GRPO 相比，MR-GRPO 提高了留出关系准确率和关系家族正确性，并减少了错误家族误差。在 Qwen3-4B 的三种子验证中，仅答案奖励改善了答案变化字段，但削弱了关系家族结构；稀疏奖励对粗粒度关系标签的保留效果最好；而 MR-GRPO 在答案变化、支持变化、状态变化以及软根级蜕变关系指标上表现出最强的均衡性能。这些结果表明，经过验证的反事实结构可以在最终答案之外塑造训练后模型，而精确的全量变化记录生成、不变子类型识别以及分布外 (OOD) 迁移仍是待解决的问题。

## Abstract
Language models can reach the right normative verdict for the wrong reason. We introduce NormWorlds-CF, a solver-verified environment for counterfactual normative reasoning in executable rule worlds. Its deterministic solver produces final answers, proof and falsification certificates, argument statuses, support sets, and paired-world change labels, enabling supervision and evaluation without LLM judges. The benchmark contains staged SFT diagnostics and a compact paired-world task with 270 root families and 1080 canonical-to-variant pairs. The SFT diagnostics show that final-answer supervision is an unsafe proxy: answer-only SFT reaches perfect accuracy on answer tasks but scores zero on falsification, while proof-plus-falsification training with targeted replay reaches strong all-task accuracy. For the structured-change task, we introduce metamorphic-relation GRPO (MR-GRPO), a class-conditioned reward for GRPO that gives partial credit for relation families and solver-visible change fields. In matched 1.7B continuation experiments, MR-GRPO improves held-out relation accuracy and relation-family correctness, and reduces wrong-family error, compared to sparse and answer-only GRPO. In Qwen3-4B three-seed validation, answer-only reward improves answer-change fields but weakens relation-family structure, sparse reward preserves coarse relation labels best, and MR-GRPO delivers the strongest balanced performance across answer-change, support-change, status-change, and soft root-level metamorphic-relation metrics. These results show that verified counterfactual structure can shape post-training beyond final answers, while exact full change-record generation, invariant subtype recognition, and out-of-distribution (OOD) transfer remain open problems.

---

## 论文详细总结（自动生成）

以下是对论文《NormWorlds-CF: Solver-Verified Counterfactual Normative Reasoning with Metamorphic-Relation GRPO》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型语言模型（LLM）在处理法律、政策和伦理等规范性推理（Normative Reasoning）任务时，往往会出现“因错得对”的现象——即表面答案正确，但内部逻辑结构（如决定性规则、反论点处理、事实支持集）是错误的。
*   **研究动机**：现有的法律基准测试往往将法律知识、检索质量与推理能力混为一谈，且依赖 LLM 作为评判员（LLM-as-a-judge），缺乏客观的归因分析。
*   **整体含义**：本文旨在建立一个受控的“后训练实验室”，通过可执行的规则世界和确定性求解器，研究模型在反事实（Counterfactual）变换下的推理一致性，确保模型不仅给出正确答案，还能理解规则间的冲突与优先级。

### 2. 方法论
*   **NormWorlds-CF 环境**：
    *   **符号化世界**：将规范性场景定义为可执行程序，包含事实（Facts）、规则（Rules）、优先级（Priorities）和查询（Query）。
    *   **确定性求解器**：使用符号引擎自动生成“黄金标准”标签，包括最终答案、证明凭证（Proof）、证伪凭证（Falsification）、论点状态和最小支持集。
    *   **蜕变关系（Metamorphic Relations, MR）**：通过对原始世界进行受控编辑（如删除事实、改变优先级、添加干扰项），生成配对的世界，并定义它们之间的逻辑关系（如“答案应保持不变”或“支持集应发生变化”）。
*   **MR-GRPO 算法**：
    *   **核心思想**：在 DeepSeek 提出的 GRPO（群体相对策略优化）基础上，引入**类别条件奖励（Class-conditioned Reward）**。
    *   **技术细节**：奖励函数不再仅仅针对最终答案，而是分解为：
        1.  **关系奖励**：是否正确识别了变换的类型（如 `support_delta`）。
        2.  **架构奖励**：输出格式是否符合预定义的 JSON 模式。
        3.  **语义奖励**：针对答案变化、支持集变化、论点状态变化等字段给予部分信用（Partial Credit）。
        4.  **类别条件项**：根据变换类型（不变 vs. 改变）动态调整权重，例如在“不变”类中强化对空变化字段的奖励。

### 3. 实验设计
*   **数据集与场景**：
    *   包含 270 个根家族和 1080 个“规范-变体”对。
    *   任务分为五个阶段：仅答案、证明生成、证伪任务、论点状态跟踪、紧凑变化记录生成。
*   **Benchmark 与对比方法**：
    *   **SFT 阶段**：对比了“仅答案 SFT”、“仅证明 SFT”和“证明+证伪 SFT”。
    *   **RL 阶段**：在 1.7B 模型和 Qwen3-4B 模型上对比了三种奖励机制：
        1.  **Answer-only GRPO**：仅奖励答案变化的正确性。
        2.  **Sparse GRPO**：仅在所有字段完全匹配时给予奖励（稀疏奖励）。
        3.  **MR-GRPO**：本文提出的分解式、类别条件奖励。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：
    *   使用了 LoRA 适配器进行参数高效微调。
    *   1.7B 模型续写实验进行了 60 个 GRPO 步数。
    *   Qwen3-4B 验证实验进行了 40 个步数，组大小（Group Size）$G=4$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了阶段性的 SFT 诊断（4 个语料库）。
    *   在 1.7B 模型上进行了匹配的续写对比。
    *   在 Qwen3-4B 上进行了三个随机种子的验证实验。
    *   包含分布内（ID）和分布外（OOD）结构探针测试。
*   **充分性与客观性**：实验设计较为充分，采用了严格的控制变量法（相同的基座模型、提示词和更新预算）。由于评估完全由确定性求解器执行，排除了 LLM 评判员的主观偏差，结果具有较高的客观性。

### 6. 主要结论与发现
*   **仅答案监督是不安全的**：仅针对答案进行 SFT 的模型在证伪任务上的表现为 0，说明模型并未真正理解规范逻辑。
*   **奖励设计的权衡**：
    *   **仅答案奖励**虽然能提升答案准确率，但会严重破坏模型对关系家族结构的理解。
    *   **稀疏奖励**能较好保留粗粒度的关系标签，但由于信号过于微弱，难以优化具体的变更字段。
    *   **MR-GRPO** 实现了最佳的平衡，在答案、支持集和状态变化等多个结构化指标上均表现最优。
*   **结构化引导的有效性**：验证的反事实结构（而非仅仅是最终答案）可以有效引导后训练过程，提升模型的逻辑一致性。

### 7. 优点
*   **无需 LLM 评判**：通过符号求解器实现全自动、确定性的监督和评估，解决了推理任务中评估难的问题。
*   **诊断性强**：提出的“阶段性 SFT 阶梯”能清晰定位模型在哪个推理环节（证明、证伪或反事实比较）出现失效。
*   **方法创新**：将软件工程中的蜕变测试思想引入 RL 奖励设计，为结构化推理提供了更细粒度的信号。

### 8. 不足与局限
*   **合成环境限制**：NormWorlds-CF 是一个合成的、封闭的世界，虽然逻辑严密，但与真实法律文本的自然语言复杂性和歧义性仍有距离。
*   **OOD 迁移难题**：实验显示模型在面对未见过的结构化变换家族时，性能提升有限，说明跨结构的泛化仍是挑战。
*   **序列生成压力**：要求模型生成精确的完整变更记录（包含嵌套的论点攻击关系）对小参数模型而言依然非常困难，存在格式崩溃的风险。

（完）

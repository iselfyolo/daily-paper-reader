---
title: "Beyond Clean Text: Evaluating Encoder and Decoder Robustness for Bangla Event Detection in Noisy Text"
title_zh: 超越干净文本：评估孟加拉语噪声文本事件检测中编码器与解码器的鲁棒性
authors: "Tanvir Ahmed Sijan, S. M Golam Rifat, Nayeemul Islam, Md. Musfique Anwar"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30914v1"
tags: ["query:kgee"]
score: 9.0
evidence: 孟加拉语事件检测基准与评估
tldr: "本研究针对低资源语言孟加拉语，探讨了事件检测系统在现实噪声环境下的鲁棒性。作者构建了一个包含9,979条标注句子的基准数据集，涵盖干净新闻、语音识别转录及拼写错误文本。通过对比评估编码器（如BanglaBERT）与解码器（如Llama 3）模型，发现编码器在干净文本上表现更佳但在噪声下退化严重，而解码器模型在触发词受损时展现出显著的鲁棒性。研究还证明了混合训练和模型缩放对提升性能的有效性。"
selection_source: fresh_fetch
motivation: 现有的事件检测系统主要针对干净文本设计，在处理孟加拉语等低资源语言的现实噪声数据时鲁棒性不足。
method: 构建了包含40个事件子类型的孟加拉语新闻事件本体及基准数据集，并系统对比了微调编码器模型与指令微调解码器模型的表现。
result: 编码器模型在干净文本上性能更高但易受噪声影响，而解码器大语言模型在噪声环境下，尤其是触发词被破坏时，表现出更强的鲁棒性。
conclusion: 模型缩放能持续增强解码器的鲁棒性，而将干净与噪声数据结合训练是缩小编码器与解码器鲁棒性差距的有效策略。
---

## 摘要
事件检测 (ED) 系统通常在经过整理的干净文本上进行评估，这使得它们对现实世界噪声的鲁棒性在很大程度上尚未得到探索，特别是对于像孟加拉语这样的低资源语言。我们引入了一个通用的孟加拉语新闻事件本体，以及一个包含 40 个事件子类、9,979 条标注句子的基准测试集，涵盖了干净的新闻文本、现实世界的自动语音识别 (ASR) 转录文本以及拼写损坏的文本。我们系统地评估了微调后的仅编码器模型（BanglaBERT 和 XLM-R）以及经过指令微调的仅解码器大语言模型（Llama 3 和 Gemma 3）。我们的结果揭示了明显的架构权衡：编码器模型在干净文本上表现更好，但在噪声环境下性能大幅下降；而仅解码器 LLM 的鲁棒性显著更强，特别是在事件触发词被损坏的情况下。我们进一步表明，在指令微调过程中嵌入标注指南可以在噪声文本上建立更高的性能基准，但在不同噪声条件下，性能下降的缓解程度并不一致。最后，模型缩放持续提高了仅解码器 LLM 的鲁棒性，而干净数据与噪声数据的联合训练作为一种有效的正则化策略，对编码器架构的益处尤为显著，从而大幅缩小了鲁棒性差距。

## Abstract
Event detection (ED) systems are typically evaluated on clean, curated text, leaving their robustness to real-world noise largely unexplored, particularly for low-resource languages such as Bangla. We introduce a generalized Bangla news event ontology and a benchmark comprising 9,979 annotated sentences across 40 event subtypes, spanning clean news text, real-world Automatic Speech Recognition (ASR) transcripts, and orthographically corrupted text. We systematically evaluate fine-tuned encoder-only models (BanglaBERT and XLM-R) alongside instruction-tuned decoder-only large language models (Llama 3 and Gemma 3). Our results reveal a clear architectural trade-off: encoder models achieve higher performance on clean text but degrade substantially under noise, whereas decoder-only LLMs are markedly more robust, particularly when event triggers are corrupted. We further show that embedding annotation guidelines during instruction tuning establishes a higher performance baseline on noisy text but yields inconsistent reductions in performance degradation across noisy conditions. Finally, model scaling consistently improves the robustness of decoder-only LLMs, while combined training on clean and noisy data serves as an effective regularization strategy that disproportionately benefits encoder architectures, significantly narrowing the robustness gap.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Clean Text: Evaluating Encoder and Decoder Robustness for Bangla Event Detection in Noisy Text》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的事件检测（Event Detection, ED）研究大多集中在干净、规整的文本上，而现实应用（如应急监控、社交媒体分析）中的数据往往充满噪声。
*   **背景挑战**：对于孟加拉语（Bangla）这种低资源语言，缺乏通用的新闻事件本体，且现有研究多局限于特定领域（如犯罪或灾害）。
*   **核心问题**：在面对现实世界的语音识别（ASR）错误和拼写错误（正字法噪声）时，传统的“仅编码器”（Encoder-only）模型与新兴的“仅解码器”（Decoder-only）大语言模型（LLM）在鲁棒性上有何差异？

### 2. 核心方法论
*   **本体构建**：基于 ACE 2005 扩展了符合孟加拉国国情的 GDELT 事件类型，最终确定了 14 个主类和 40 个子类。
*   **建模范式对比**：
    *   **编码器模型**：采用传统的**令牌分类（Token Classification）**，使用 BIO 标注法。
    *   **解码器模型**：采用**结构化序列生成（Structured Sequence Generation）**，将任务转化为生成 Python 类代码的形式。
*   **指令微调与指南嵌入**：在 LLM 的 Prompt 中通过 Python docstrings 嵌入详细的标注指南（定义、触发词示例、边界约束），以增强模型对模式的理解。
*   **噪声模拟**：使用专门针对孟加拉语书写习惯的算法，模拟不同词错误率（WCR 10%-40%）的正字法噪声。

### 3. 实验设计
*   **数据集**：
    *   **Clean Test**：来自主流孟加拉语报纸的干净新闻。
    *   **ASR Test**：来自 YouTube 新闻频道的真实语音转录文本（包含口语、方言、转录错误）。
    *   **Ortho Test**：对干净文本进行人工合成的拼写损坏。
*   **对比方法**：
    *   **编码器**：BanglaBERT (Base/Large), XLM-RoBERTa (Base/Large)。
    *   **解码器**：Llama 3.1 (8B), Llama 3.2 (1B), Gemma 3 (1B/4B)。
*   **评估指标**：严格的 **Macro-F1**（要求触发词边界和类型完全匹配）。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA A100 GPU** 上完成。
*   **训练细节**：
    *   编码器模型进行全参数微调。
    *   解码器模型使用 **LoRA** 技术（Rank=16, Alpha=32），训练 5 个 Epoch，采用 AdamW 优化器。
    *   训练时长未明确给出具体小时数，但提到了早停机制（Early Stopping）。

### 5. 实验数量与充分性
*   **实验规模**：标注了 9,979 条句子（包含 7,813 个事件提及），规模在低资源语言研究中属于较大水平。
*   **充分性**：
    *   涵盖了从 1B 到 8B 不同参数规模的模型。
    *   对比了“仅干净数据训练”与“干净+噪声混合训练”两种策略。
    *   进行了 4 个等级的噪声强度测试（10% 到 40% WCR）。
    *   使用了 **McNemar 显著性检验** 来验证模型在触发词损坏 vs. 上下文损坏下的表现差异。
*   **客观性**：通过迭代多标签分层算法划分数据集，确保了长尾分布下的类别平衡，实验设计较为严谨。

### 6 主要结论与发现
*   **架构权衡**：编码器模型在干净文本上性能占优（最高 F1 达 70.6），但在噪声下退化严重（平均下降 11-14 点）；解码器 LLM 虽然上限略低，但极其稳健（平均仅下降 2-4 点）。
*   **触发词鲁棒性**：当事件触发词本身被损坏时，LLM 的表现显著优于编码器模型；而当仅上下文损坏时，两者差距缩小。
*   **标注指南的作用**：在 Prompt 中加入指南能显著提升 LLM 的性能基线，但并不能一致地消除噪声带来的性能跌落。
*   **缩放效应**：模型参数越大，解码器模型的鲁棒性越强；而编码器模型单纯增加参数量对鲁棒性的提升有限。
*   **混合训练**：将噪声数据加入训练集能显著缩小编码器与解码器之间的鲁棒性差距。

### 7. 优点与亮点
*   **填补空白**：首次针对孟加拉语事件检测进行了系统性的鲁棒性评估。
*   **数据质量**：结合了真实 ASR 数据和模拟噪声，比单纯使用合成噪声更具说服力。
*   **方法创新**：将标注指南转化为代码注释（docstrings）嵌入 Prompt，为 LLM 处理结构化抽取任务提供了有效参考。

### 8. 不足与局限
*   **评估偏见**：采用“严格边界匹配”的 Macro-F1 可能对生成式 LLM 不公平，因为 LLM 有时能识别语义但生成的边界可能多出或少掉一个虚词。
*   **领域局限**：研究仅限于新闻领域，其结论在社交媒体或医疗等其他领域的泛化性有待验证。
*   **任务范围**：仅关注事件检测（触发词识别），未涉及更复杂的事件论元抽取（Argument Extraction）。

（完）

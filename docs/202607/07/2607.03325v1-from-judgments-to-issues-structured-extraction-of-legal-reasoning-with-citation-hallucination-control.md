---
title: "From Judgments to Issues: Structured Extraction of Legal Reasoning with Citation-Hallucination Control"
title_zh: 从判决书到争议焦点：具有引用幻觉控制的法律推理结构化提取
authors: "Giovanni Piccioli, Alessia Fidelangeli, Piera Santin, Pierpaolo Vivo"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03325v1"
tags: ["query:profile-3"]
score: 10.0
evidence: 从判决书中结构化提取法律推理
tldr: 本研究针对意大利税务法院判决书，开发了一套自动化流水线，旨在将判决分解为独立的法律争议点，并提取基于IRAC框架的结构化XML表示。该系统采用DeepSeek V3模型以平衡性能与成本，并引入Linkoln解析器进行引用幻觉检测。通过专家验证，该流水线在争议点提取和引用准确性上表现优异，为法律检索和推理分析提供了大规模数据基础。
selection_source: fresh_fetch
motivation: 旨在解决法律判决书结构复杂且大语言模型在处理法律引用时存在幻觉的问题，实现对意大利税务判决的精细化结构提取。
method: 利用DeepSeek V3模型结合IRAC框架进行争议点分解，并配合Linkoln解析器建立引用幻觉检测过滤器，确保法律引用的真实性。
result: 经过法学博士的人工标注验证，该流水线在争议点提取和法律引用识别上与专家达成高度一致，有效控制了引用幻觉。
conclusion: 这是首个针对意大利税务判决、经过专家验证且具备幻觉控制的争议点级结构化提取系统，为下游法律应用奠定了基础。
---

## 摘要
我们提出了一种自动化流水线，将意大利税务法院的判决书分解为单个法律争议焦点，并针对每个争议焦点，提取基于 IRAC 框架和法律三段论的结构化 XML 表示。该流水线针对包含约 330,000 份意大利税务法院一审和二审裁决的语料库，并围绕一个功能强大且具有成本效益的通用模型（DeepSeek V3）构建，这一选择源于以可持续成本处理数十万份文档的需求。为了解决大语言模型在法律引用方面公认的不可靠性，我们将提取步骤与自动幻觉检测过滤器相结合，该过滤器将模型生成的引用与专用解析器（Linkoln）在判决文本中识别出的引用进行比较，并将其归一化为标准标识符（URN-NIR、ECLI、CELEX）。我们在由两名税法博士标注的 50 份判决书上验证了该流水线，计算了争议焦点提取和法律引用方面的标注者间一致性以及大模型与专家间的一致性，并对幻觉过滤器进行了独立评估。据我们所知，这是首个针对意大利税务法院裁决、经过专家验证且具有幻觉控制的争议焦点级结构化提取流水线，它为争议焦点级检索、引用网络分析以及大规模法律推理数据集的构建等下游应用提供了具体的起点。

## Abstract
We present an automated pipeline that decomposes Italian tax-court judgments into individual legal issues and extracts, for each issue, a structured XML representation grounded in the IRAC framework and the legal syllogism. The pipeline targets a corpus of approximately $330{,}000$ first- and second-instance decisions of the Italian tax courts and is built around a capable yet cost-efficient general-purpose model (DeepSeek V3), a choice driven by the need to process several hundred thousand documents at a sustainable cost. To address the well-documented unreliability of large language models on legal citations, we couple the extraction step with an automatic hallucination-detection filter that compares the references produced by the model with those identified in the judgment text by a dedicated parser (Linkoln), normalised to standard identifiers (URN-NIR, ECLI, CELEX). We validate the pipeline on $50$ judgments annotated by two PhDs in tax law, computing inter-annotator agreement and LLM-vs-expert agreement on both issue extraction and legal citations, together with a stand-alone evaluation of the hallucination filter. To the best of our knowledge, this is the first issue-level, expert-validated structured extraction pipeline with hallucination control for Italian tax-court decisions, and it provides a concrete starting point for downstream applications such as issue-level retrieval, citation-network analysis, and the construction of large-scale datasets of legal reasoning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为“从判决书到争议焦点”的自动化流水线，旨在对意大利税务法院的判决书进行深度结构化分析。以下是对该论文的详细总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：现代司法系统产生的判决书数量庞大（仅意大利税务法院每年就增加数十万份），法律专业人士难以高效处理。
*   **研究动机**：
    *   **粒度问题**：现有的法律NLP系统通常将整篇判决书视为一个整体，但一份判决往往包含多个独立的法律争议点（如管辖权、实体法适用、诉讼费分担）。
    *   **幻觉风险**：大语言模型（LLM）在处理法律引用时极易产生“幻觉”，虚构不存在的法律条文或案例。
    *   **成本挑战**：处理数十万份文档需要一种在高性能和低成本之间取得平衡的方案。

### 2. 论文提出的方法论
*   **核心思想**：以“法律争议焦点（Legal Issue）”为基本单位，利用LLM提取基于**IRAC框架**（争议、规则、应用、结论）和**法律三段论**的结构化XML表示。
*   **关键技术细节**：
    *   **结构化Schema**：定义了包含`<text>`（争议描述）、`<factual_premises>`（事实前提）、`<legal_references>`（法律引用）、`<citation_reason>`（引用理由）、`<judge_reasoning>`（法官推理）等字段的XML格式。
    *   **两阶段提示策略**：针对长文档，先识别所有争议点并生成摘要，再针对每个争议点分别提取详细的推理过程。
    *   **引用幻觉控制过滤器**：
        1.  使用**Linkoln解析器**从原始判决文本中提取所有真实的法律引用。
        2.  将LLM生成的引用与真实引用进行标准化匹配（使用URN-NIR、ECLI、CELEX等标识符）。
        3.  自动剔除LLM生成但在原文中未出现的引用。
*   **模型选择**：选用 **DeepSeek V3**，因其在保持高水平法律推理能力的同时，成本仅为顶级闭源模型（如Claude 3.5 Sonnet）的几十分之一。

### 3. 实验设计
*   **数据集**：从意大利税务法院官方数据库检索的约40万份判决书，经过预处理后保留328,605份。
*   **验证集（Benchmark）**：由两名税法博士（PhD）对随机抽取的50份判决书进行人工标注。
    *   **双盲标注**：其中20份由两人共同独立标注，用于计算标注者间一致性（IAA）。
    *   **评估指标**：精确率（P）、召回率（R）、F1分数，以及针对主观评分的Gwet’s AC2系数。
*   **对比方法**：对比了LLM提取结果与专家标注结果，并对幻觉过滤器进行了消融性质的独立评估。

### 4. 资源与算力
*   **算力资源**：论文未提及具体的本地GPU训练，主要通过API调用 **DeepSeek V3** 进行推理。
*   **成本核算**：处理全量语料库（约33万份文档）的总成本约为 **1,150美元**，平均每份判决书的处理成本仅为 **0.0035美元**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   全量处理了32.8万份文档。
    *   专家详细验证了50份判决书（包含78个LLM提取的争议点和数百条引用）。
    *   进行了**运行一致性分析**（Sensitivity Analysis），通过两次运行模型对比非确定性输出的影响。
*   **充分性评价**：实验设计较为客观、严谨。虽然50份验证集在数量上看似不多，但考虑到法律专家标注的高昂成本和深度，这在法律AI领域属于标准做法。论文还通过自助法（Bootstrap）计算了置信区间，增强了结果的可信度。

### 6. 主要结论与发现
*   **争议点提取**：LLM表现出**高精确率（>93%）但中等召回率（60%-80%）**。这意味着模型提取的内容几乎都是正确的，但会漏掉一些次要争议点。
*   **幻觉控制效果显著**：过滤器将法律引用的幻觉率从 **11.7% 降低至 0.9%**，且仅误删了3%的正确引用。
*   **法律原则（Principles）是重灾区**：LLM在提取抽象法律原则时幻觉率最高（达69%），而对具体法条和案例的提取相对准确。
*   **质量评分**：专家对LLM生成的摘要、推理和结论的评分均在4.6/5分以上，证明了其高度的实用性。

### 7. 优点
*   **理论结合实践**：将法律界的IRAC框架直接转化为机器可读的XML Schema，具有很强的法学理论支撑。
*   **成本效益极高**：证明了使用中端价格的通用大模型配合特定的过滤机制，可以大规模处理复杂的法律任务。
*   **幻觉控制创新**：引入外部解析器（Linkoln）进行闭环验证，有效解决了LLM在法律严肃场景下的信任问题。

### 8. 不足与局限
*   **领域局限性**：目前仅在意大利税法领域验证，其Schema和解析器对其他司法管辖区或法律分支的迁移性有待观察。
*   **召回率不足**：模型倾向于只提取“主要”争议点，对于复杂案件中的细微法律问题存在漏检。
*   **非确定性风险**：即使在Temperature=0的情况下，LLM的输出仍存在波动，这可能影响法律应用的稳定性。
*   **解析器依赖**：幻觉过滤器的效果高度依赖于Linkoln解析器的准确性，如果解析器漏掉原件中的引用，过滤器也会将其误判为幻觉。

（完）

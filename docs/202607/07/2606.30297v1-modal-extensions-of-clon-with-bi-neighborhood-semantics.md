---
title: Modal Extensions of CLoN with Bi-neighborhood Semantics
title_zh: 具有双邻域语义的 CLoN 模态扩张
authors: "Mahan Vaz, Daniel Skurt"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30297v1"
tags: ["query:q5"]
score: 9.0
evidence: 容纳道德困境的规范逻辑基础
tldr: 本研究探讨了CLoN（FDE的一个子逻辑）的非正规模态扩展，并为其提出了双邻域语义。通过为每个模态算子引入两个不同的邻域函数，即使在否定算子极弱的情况下，也能验证包含弱否定的非平凡公理。该框架旨在为处理道德困境的义务逻辑奠定基础，确保系统在面对冲突时不会平凡化，从而在逻辑层面兼容规范性原则与道德冲突。
selection_source: fresh_fetch
motivation: 旨在构建一种能够处理道德困境且不会导致系统平凡化的义务逻辑框架。
method: 基于FDE逻辑，为CLoN的非正规模态扩展开发了双邻域语义，并为每个模态算子定义了特定的拒绝集。
result: 成功验证了包含弱否定算子的非平凡公理，证明了该逻辑在极弱否定环境下依然具有表达力。
conclusion: 该研究为开发能够容纳冲突义务且保持逻辑一致性的义务逻辑提供了重要的理论基础。
---

## 摘要
本文将为 CLoN 的非正规模态扩张提供邻域语义，CLoN 是 FDE 的一个子逻辑。我们的框架建立在早期关于基于 FDE 的非正规模态逻辑的研究之上，并为每个模态算子采用了两个不同的邻域函数。尽管该逻辑具有非常弱的否定算子，我们将证明，通过对模态算子的拒绝集（rejection sets）进行正确定义，我们可以验证包含该弱否定算子的非平凡公理。我们方法的哲学目标是为义务逻辑构建基础，使其能够同时容纳通常的义务原则和道德困境，而不会导致系统的平凡化。

## Abstract
In this paper we will present neighborhood semantics for non-normal modal extensions of $\clon$, which is a sublogic of {\sf FDE}. Our framework is built upon earlier work on {\sf FDE}-based non-normal modal logics and employs two different neighborhood functions for each modal operator. Despite being a logic with a very weak negation operator, we will show that with the right definition of the rejection sets of the modal operators, we can validate non-trivial axioms that contain the weak negation operator. The philosophical aim of our approach is to construct the basis for deontic logics that are able to accommodate both the usual deontic principles and moral dilemmas, without resulting in trivialization of the system.

---

## 论文详细总结（自动生成）

这篇论文题为《具有双邻域语义的 CLoN 模态扩张》（Modal Extensions of CLoN with Bi-neighborhood Semantics），是一篇关于非经典逻辑（特别是模态逻辑与协调逻辑）的理论研究论文。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：CLoN 是一种极弱的逻辑系统，它是 FDE（一阶蕴涵逻辑）的子逻辑。CLoN 的特点是其否定算子既允许“真值缺失”（gappy）又允许“真值重叠”（glutty），但在基础系统中没有任何关于否定的公理。
*   **核心问题**：如何在 CLoN 这种否定算子极弱的逻辑基础上，构建非正规模态逻辑（Non-normal Modal Logics）的语义框架。
*   **研究动机**：
    *   **哲学需求**：现有的义务逻辑（Deontic Logics）在处理“道德困境”（即同时存在 $O\phi$ 和 $O\neg\phi$ 的情况）时，往往会导致系统平凡化（即推导出任何结论）。
    *   **理论空白**：目前缺乏对 CLoN 家族模态扩张的深入研究，尤其是能够灵活处理非正规模态算子（不满足 K 公理或聚合规则）的语义工具。

### 2. 方法论：核心思想与关键技术细节
*   **核心思想**：引入**双邻域语义（Bi-neighborhood Semantics）**。传统的邻域语义为每个世界分配一个命题集合，而双邻域语义为每个模态算子分配两个独立的邻域函数：一个用于处理“验证”（Truth/Verification），另一个用于处理“拒绝”（Falsity/Rejection）。
*   **关键技术细节**：
    *   **逻辑系统 CLoNE**：在 CLoN 基础上增加模态算子 $\Box$ 和 $\Diamond$，并引入外延性规则（Extensionality Rules），即如果 $\phi \leftrightarrow \psi$，则 $\Box\phi \leftrightarrow \Box\psi$。
    *   **语义构造**：
        *   定义验证集 $[\phi]^+$ 和拒绝集 $[\phi]^-$。
        *   对于模态算子 $\# \in \{\Box, \Diamond\}$，其验证条件为：$w \in [\#\phi]^+$ 当且仅当 $[\phi]^+ \in N^+_\#(w)$。
        *   其拒绝条件为：$w \in [\#\phi]^-$ 当且仅当 $[\phi]^- \in N^-_\#(w)$。
    *   **独立性**：由于 CLoN 的否定非常弱，$\Box$ 和 $\Diamond$ 不再互为对偶（Duals），必须分别独立定义。
*   **证明方法**：通过构造“规范模型”（Canonical Model）证明了该逻辑系统相对于双邻域框架的**可靠性（Soundness）**和**完备性（Completeness）**。

### 3. 实验设计
*   **研究性质**：本文属于**纯理论数学逻辑研究**，不涉及实证实验、数据集或软件基准测试（Benchmark）。
*   **对比方法**：论文在理论层面对比了：
    *   基于 FDE 的模态逻辑。
    *   基于 LFI（形式不一致逻辑）的义务逻辑。
    *   传统的 Kripke 语义（证明了邻域语义在处理非正规模态逻辑时的优越性）。

### 4. 资源与算力
*   **说明**：文中未提及任何 GPU、算力资源或训练时长。所有的结论均通过数学推导和逻辑证明得出。

### 5. 实验数量与充分性
*   **评估**：虽然没有数值实验，但作者对多种模态公理（如 M, C, K, T, 4, D 等）及其对应的框架属性进行了详尽的分类讨论。
*   **充分性**：论文提供了完整的希尔伯特风格（Hilbert-style）演算，并针对每种扩张给出了相应的语义约束证明，从逻辑完备性的角度来看，论证是充分且严谨的。

### 6. 主要结论与发现
*   **语义可行性**：证明了即使在否定算子极弱的情况下，通过合理定义模态算子的“拒绝集”，依然可以验证包含弱否定的非平凡公理。
*   **道德困境的兼容性**：该系统是“义务协调的”（Deontically Paraconsistent），能够容纳相互冲突的义务而不导致逻辑崩溃，为解决道德困境提供了形式化工具。
*   **多模态扩展性**：该框架可以轻松扩展到包含多个独立模态算子的系统，并通过“桥接原则”（Bridge Principles）描述它们之间的相互作用。

### 7. 优点
*   **高度灵活性**：不强制要求 K 公理或聚合公理，允许构建非常弱的模态系统。
*   **哲学解释力**：为道德困境提供了自然的逻辑表达方式，避免了人工添加一致性算子的违和感。
*   **理论统一性**：将 CLoN 家族逻辑纳入了现代非正规模态逻辑的语义版图。

### 8. 不足与局限
*   **复杂性**：双邻域函数（$N^+$ 和 $N^-$）的引入增加了模型构造的复杂度和抽象程度。
*   **否定算子的局限**：由于 CLoN 否定过于微弱，许多经典的逻辑性质（如德·摩根定律、双重否定除去）在基础系统中均不成立，这可能限制其在某些传统推理场景中的应用。
*   **缺乏应用实例**：论文侧重于元逻辑证明，未展示该系统在具体人工智能推理或法律专家系统中的实际应用案例。

（完）

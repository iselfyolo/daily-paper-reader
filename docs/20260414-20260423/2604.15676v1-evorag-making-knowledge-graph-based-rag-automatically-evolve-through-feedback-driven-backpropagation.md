---
title: "EvoRAG: Making Knowledge Graph-based RAG Automatically Evolve through Feedback-driven Backpropagation"
title_zh: EvoRAG：通过反馈驱动的反向传播使基于知识图谱的 RAG 自动演进
authors: "Zhenbo Fu, Yuanzhe Zhang, Qiange Wang, Hao Yuan, Yuehao Xu, Enze Yi, Yanfeng Zhang, Ge Yu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15676v1"
tags: ["query:kgee"]
score: 9.0
evidence: 基于知识图谱的RAG与知识依赖演化
tldr: EvoRAG是一个自进化的知识图谱增强检索生成（KG-RAG）框架，旨在解决现有方法无法根据任务需求动态调整知识库的问题。它引入了反馈驱动的反向传播机制，将生成响应的反馈映射到具体的知识三元组，通过评估三元组的贡献度来持续优化知识图谱。该框架建立了反馈、大模型与图数据的闭环，显著提升了复杂推理任务的准确性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的KG-RAG框架难以适应特定任务需求，且缺乏根据生成反馈来过滤低贡献知识并更新知识图谱的有效机制。
method: 提出反馈驱动的反向传播机制，将响应级反馈归因至检索路径并传播到单个三元组，实现细粒度的知识图谱精炼与进化。
result: "实验结果显示，EvoRAG在推理准确性上比现有的最先进KG-RAG框架提升了7.34%。"
conclusion: 通过建立反馈驱动的闭环进化机制，EvoRAG证明了动态优化知识图谱能有效增强大模型在实际应用中的推理能力。
---

## 摘要
基于知识图谱的检索增强生成（KG-RAG）已成为一种极具前景的范式，通过从知识图谱（KG）中检索多跳路径来增强大语言模型（LLM）的推理能力。然而，现有的 KG-RAG 框架在现实场景中往往表现不佳，因为预先捕获的知识依赖关系并非针对下游任务或其不断变化的需求而定制。这些框架难以适应特定任务的需求，且在生成过程中缺乏过滤低贡献知识的机制。我们观察到，对生成响应的反馈为提高知识图谱质量提供了有效的监督，因为它直接反映了用户的预期，并提供了关于输出正确性和有用性的见解。然而，一个关键挑战在于如何有效地将响应层级的反馈与知识图谱中三元组层级的贡献评估及知识更新联系起来。在这项工作中，我们提出了 EvoRAG，这是一个自演进的 KG-RAG 框架，它利用对生成响应的反馈来持续优化知识图谱并提高推理准确性。EvoRAG 引入了一种反馈驱动的反向传播机制，通过衡量检索路径对响应的效用将反馈归因于这些路径，并将该效用回传至单个三元组，从而支持细粒度的知识图谱优化，实现更具适应性和准确性的推理。通过 EvoRAG，我们建立了一个耦合反馈、LLM 和图数据的闭环，持续增强在现实场景中的性能和鲁棒性。实验结果表明，EvoRAG 的推理准确率比最先进的 KG-RAG 框架提高了 7.34%。源代码已在 https://github.com/iDC-NEU/EvoRAG 发布。

## Abstract
Knowledge Graph-based Retrieval-Augmented Generation (KG-RAG) has emerged as a promising paradigm for enhancing LLM reasoning by retrieving multi-hop paths from KGs. However, existing KG-RAG frameworks often underperform in real-world scenarios because the pre-captured knowledge dependencies are not tailored to the downstream task or its evolving requirements. These frameworks struggle to adapt to task-specific requirements and lack mechanisms to filter low-contribution knowledge during generation. We observe that feedback on generated responses offers effective supervision for improving KG quality, as it directly reflects user expectations and provides insights into the correctness and usefulness of the output. However, a key challenge lies in effectively linking response-level feedback to triplet-level contribution evaluation and knowledge updates in the KG.   In this work, we propose EvoRAG, a self-evolving KG-RAG framework that leverages the feedback over generated responses to continuously refine the KG and enhance reasoning accuracy. EvoRAG introduces a feedback-driven backpropagation mechanism that attributes feedback to retrieved paths by measuring their utility for response and propagates this utility back to individual triplets, supporting fine-grained KG refinements towards more adaptive and accurate reasoning. Through EvoRAG, we establish a closed loop that couples feedback, LLM, and graph data, continuously enhancing the performance and robustness in real-world scenarios. Experimental results show that EvoRAG improves reasoning accuracy by $7.34\%$ over state-of-the-art KG-RAG frameworks. The source code has been made available at https://github.com/iDC-NEU/EvoRAG.

---

## 论文详细总结（自动生成）

这是一份关于论文《EvoRAG: Making Knowledge Graph-based RAG Automatically Evolve through Feedback-driven Backpropagation》的结构化深入分析报告。

---

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于知识图谱的检索增强生成（KG-RAG）框架通常依赖于**静态**的知识图谱。这导致了两个主要瓶颈：
    1.  **缺乏任务适应性**：预构建的 KG 往往包含大量与特定下游任务无关的噪声，或无法捕捉复杂的长程依赖。
    2.  **缺乏动态性**：无法及时识别和剔除过时或错误的知识。
*   **研究动机**：作者观察到，用户或 LLM 对生成响应的反馈（如正确性、满意度）包含了优化 KG 质量的宝贵信号。然而，**反馈（响应级）与 KG 更新（三元组级）之间存在粒度错配**。论文旨在建立一种机制，将宏观反馈转化为微调 KG 的微观指令。

### 2. 方法论：反馈驱动的自演进框架
EvoRAG 的核心思想是模拟神经网络的**反向传播（Backpropagation）**，将反馈信号逐层回传至知识单元。

*   **核心流程**：
    1.  **前向检索与生成**：结合语义相似度（$S_r$）和学习到的三元组贡献分（$S_c$）检索路径，LLM 生成响应。
    2.  **反馈获取**：通过 LLM 裁判、人工或 Ground Truth 获取响应级的评分（1-5分）。
    3.  **反馈驱动的反向传播**：
        *   **路径评估**：利用 LLM 从支持度（Supportiveness）、忠实度（Fidelity）和冲突度（Conflict）三个维度评估检索路径的效用（Utility）。
        *   **梯度回传**：定义损失函数为路径期望效用的负对数。计算损失函数对三元组贡献分（$S_c$）的梯度，并利用梯度下降更新每个三元组的得分。
    4.  **KG 演进（管理）**：
        *   **关系融合（Relation Fusion）**：将高贡献分的多跳路径抽象为“快捷边”（Shortcut），缩短推理深度。
        *   **关系抑制（Relation Suppression）**：降低低分三元组的检索权重，实现软性剪枝。
*   **关键公式**：三元组的选择概率 $P(t)$ 由语义相关性和贡献分加权决定：$P(t) = (1-\alpha)S_r(t) + \alpha S_c(t)$。

### 3. 实验设计
*   **数据集**：
    *   **RGB**：新闻文章构建，评估基础推理。
    *   **MultiHop (MTH)**：多文档新闻，侧重多跳查询。
    *   **HotpotQA (HPQ)**：维基百科多跳问答。
*   **Benchmark（对比方法）**：
    *   **KG-RAG 框架**：Microsoft GraphRAG (MRAG), LightRAG (LRAG), KRAG。
    *   **KG 精炼方法（集成至 KRAG）**：TransE, RotatE, CAGED, LLM_sim。
*   **实验场景**：模拟多用户在线服务环境，针对 KG 的热点区域进行连续查询和迭代优化。

### 4. 资源与算力
*   **硬件环境**：2x Intel Xeon Silver 4316 CPU, 503GB RAM, **2x NVIDIA RTX A6000 (48GB) GPU**。
*   **软件环境**：Ubuntu 20.04, CUDA 11.3, PyTorch 1.13.0。
*   **模型后端**：默认使用 **Qwen2.5-32B**，同时也测试了 Llama-3.1-70B 和 GPT-4o-mini。
*   **训练时长**：文中未明确给出总训练小时数，但提到在 20 Batch Size 下，反向传播仅占总运行时间的 23.9%，且性能在约 6 次迭代后趋于稳定。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集，总计超过 1700 个核心查询，并在扩展性实验中使用了 5000 个查询和百万级规模的 KG。
*   **实验维度**：
    *   **消融实验**：验证了反馈机制、交叉验证模块（Fidelity/Conflict）的必要性。
    *   **鲁棒性测试**：引入 10%-20% 的错误反馈（噪声），验证系统的容错能力。
    *   **效率分析**：对比了 Token 消耗、首字延迟（TTFT）和计算开销。
    *   **通用性测试**：更换了不同的 LLM 后端和反馈来源（人工、F1 分数）。
*   **评价**：实验设计非常充分且客观，不仅关注准确率，还深入分析了 KG 内部结构的变化和系统运行效率。

### 6. 主要结论与发现
*   **性能提升**：EvoRAG 在推理准确率上平均比 SOTA 框架提升了 **7.34%**。
*   **知识优化**：成功抑制了约 83% 的问题三元组（无关、过时信息），并引入了有效的快捷关系。
*   **效率优势**：由于过滤了无关知识，Prompt 长度平均缩短了 **4.6 倍**，显著降低了推理成本和延迟。
*   **收敛与鲁棒**：系统在 6 次迭代内收敛，且对 20% 的反馈噪声表现出极强的韧性。

### 7. 优点（亮点）
*   **闭环进化**：首次将“反向传播”概念完整引入 KG-RAG 的知识管理，实现了数据驱动的自动演进。
*   **细粒度归因**：通过路径效用作为中间桥梁，巧妙解决了响应级反馈到三元组级更新的映射难题。
*   **工程优化**：利用 vLLM 的 KV-cache 复用技术，大幅降低了引入反馈机制带来的额外计算开销。

### 8. 不足与局限
*   **实体更新缺失**：目前的演进主要集中在“关系”（边）的权重调整和融合，尚未涉及新实体的自动发现或实体属性的纠正。
*   **冷启动依赖**：系统性能仍受限于初始 KG 构建的质量，如果初始 KG 存在严重的知识断层，仅靠反馈可能难以完全修复。
*   **反馈稀疏性**：在实际应用中，用户反馈可能非常稀疏，论文主要基于 LLM 模拟反馈，真实场景下的收敛速度可能变慢。

（完）

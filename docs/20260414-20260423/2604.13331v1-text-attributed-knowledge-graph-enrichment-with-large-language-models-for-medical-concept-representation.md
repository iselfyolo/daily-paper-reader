---
title: Text-Attributed Knowledge Graph Enrichment with Large Language Models for Medical Concept Representation
title_zh: 利用大语言模型增强文本属性知识图谱以进行医疗概念表示
authors: "Mohsen Nayebi Kerdabadi, Arya Hadizadeh Moghaddam, Chen Chen, Dongjie Wang, Zijun Yao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13331v1"
tags: ["query:kgee"]
score: 9.0
evidence: 知识图谱增强与医学概念表示
tldr: 本研究针对电子健康档案（EHR）中医疗概念表示学习面临的跨类型依赖缺失和文本语义融合难的问题，提出了CoMed框架。该框架利用大语言模型（LLM）从EHR关联和语义推理中构建并增强知识图谱，生成节点描述和边解释，并通过LoRA微调的LLaMA与异构图神经网络（GNN）联合训练，实现了文本语义与图结构的深度融合，显著提升了临床预测性能。
selection_source: fresh_fetch
motivation: 现有医疗本体资源缺乏跨类型依赖且难以整合丰富的临床文本语义，限制了医疗概念表示的质量。
method: 提出CoMed框架，结合EHR统计关联与LLM推理构建知识图谱，并利用LLM生成文本属性，最后通过LoRA微调的LLaMA与异构GNN进行联合表征学习。
result: 在MIMIC-III和MIMIC-IV数据集上的实验表明，CoMed显著提升了临床预测任务的性能，并可作为标准EHR流程的有效插件。
conclusion: 通过LLM增强的文本属性知识图谱能有效融合结构与语义信息，为医疗概念表示学习提供了一种强大的新范式。
---

## 摘要
在电子健康记录（EHR）挖掘中，学习医疗概念（如标准化的诊断、药物和手术代码）的高质量表示是下游临床预测的基础。然而，鲁棒的概念表示学习面临两个关键挑战：(i) 临床上重要的跨类型依赖关系（如诊断-药物和药物-手术关系）在现有本体资源中往往缺失或不完整，限制了建模复杂 EHR 模式的能力；(ii) 结构化资源中往往缺乏丰富的临床语义，即使以文本形式存在，也难以与知识图谱（KG）结构整合以进行表示学习。为了应对这些挑战，我们提出了 CoMed，这是一个用于医疗概念表示的、由大语言模型（LLM）赋能的图学习框架。CoMed 首先通过结合从 EHR 中挖掘的统计可靠关联以及类型约束的 LLM 提示来推断语义关系，从而构建一个覆盖医疗代码的全局知识图谱。随后，它利用 LLM 生成节点描述和边解释，将知识图谱丰富为文本属性图，为概念及其关系提供语义信号。最后，CoMed 将经过 LoRA 微调的 LLaMA 文本编码器与异构图神经网络（GNN）进行联合训练，将文本语义和图结构融合为统一的概念嵌入。在 MIMIC-III 和 MIMIC-IV 上的广泛实验表明，CoMed 持续提升了预测性能，并可作为标准 EHR 流水线的有效插件式概念编码器。

## Abstract
In electronic health record (EHR) mining, learning high-quality representations of medical concepts (e.g., standardized diagnosis, medication, and procedure codes) is fundamental for downstream clinical prediction. However, robust concept representation learning is hindered by two key challenges: (i) clinically important cross-type dependencies (e.g., diagnosis-medication and medication-procedure relations) are often missing or incomplete in existing ontology resources, limiting the ability to model complex EHR patterns; and (ii) rich clinical semantics are often missing from structured resources, and even when available as text, are difficult to integrate with KG structure for representation learning. To address these challenges, we present CoMed, an LLM-empowered graph learning framework for medical concept representation. CoMed first builds a global knowledge graph (KG) over medical codes by combining statistically reliable associations mined from EHRs with type-constrained LLM prompting to infer semantic relations. It then utilizes LLMs to enrich the KG into a text-attributed graph by generating node descriptions and edge rationales, providing semantic signals for both concepts and their relationships. Finally, CoMed jointly trains a LoRA-tuned LLaMA text encoder with a heterogeneous GNN, fusing text semantics and graph structure into unified concept embeddings. Extensive experiments on MIMIC-III and MIMIC-IV show that CoMed consistently improves prediction performance and serves as an effective plug-in concept encoder for standard EHR pipelines.

---

## 论文详细总结（自动生成）

### 论文总结：CoMed —— 利用大语言模型增强文本属性知识图谱以进行医疗概念表示

#### 1. 核心问题与研究动机
在电子健康档案（EHR）挖掘中，学习高质量的医疗概念（诊断、药物、手术代码）表示是临床预测任务的关键。然而，现有方法面临两大挑战：
*   **跨类型依赖缺失**：现有的医学本体（如ICD、UMLS）主要关注同类层级关系，缺乏临床上重要的跨类型关联（如某种诊断与特定药物或手术的因果/治疗关系）。
*   **语义融合困难**：结构化资源缺乏丰富的临床语义，而文本形式的医学知识难以与知识图谱（KG）的结构化信息有效整合。
*   **幻觉与一致性风险**：直接使用大语言模型（LLM）生成关系可能产生不符合实际临床观察的“幻觉”连接。

#### 2. 方法论
论文提出了 **CoMed** 框架，通过四个阶段实现医疗概念的深度表征：
1.  **证据提取（Evidence Extraction）**：从原始EHR数据中计算代码间的共现频率、条件概率、互信息（PMI）和卡方检验等统计指标，筛选出统计学显著的代码对，确保关系构建基于真实临床证据。
2.  **知识图谱归纳（KG Induction）**：设计类型约束的提示词（Prompt），将统计证据输入LLM，引导其在预定义的28种临床关系库中分配关系类型、置信分数和临床解释（Rationale）。
3.  **知识图谱增强（KG Enrichment）**：利用LLM为每个节点生成详细的临床描述，并为每条边整合统计证据与文本解释，构建一个**文本属性图（Text-Attributed Graph）**。
4.  **协同学习（Co-Learning）**：
    *   **文本编码**：使用经过 LoRA 微调的 LLaMA 模型对节点文本描述进行编码。
    *   **结构编码**：使用异构图神经网络（GNN）在KG上进行消息传递，融合邻域结构和边属性。
    *   **训练策略**：采用“最少更新优先”的采样调度策略，在平衡计算成本的同时确保长尾（罕见）代码也能获得充分的LLM微调。

#### 3. 实验设计
*   **数据集**：使用了两个公开的基准数据集：**MIMIC-III** 和 **MIMIC-IV**。
*   **任务场景**：下一阶段诊断预测（Next-visit Diagnosis Prediction），这是一个多标签分类任务。
*   **基准模型（Benchmarks）**：
    *   **基础模型**：Transformer, AdaCare, RETAIN, TCN。
    *   **对比方法**：包括基于层级的本体方法（GRAM, KAME, HAP）、基于KG的方法（ADORE, GraphCare）以及最新的跨类型关联方法（LINKO, KAMPNet）。
*   **评估指标**：AUPRC（按频率分层评估）、F1分数、Acc@k。

#### 4. 资源与算力
论文明确列出了实验环境与资源消耗：
*   **硬件**：Intel Xeon Silver 4214R CPU (24核), 256 GiB RAM, **1张 NVIDIA RTX A6000 GPU (48 GB)**。
*   **模型**：主实验使用 Llama-3.2-1B，LoRA秩 $r=8$。
*   **耗时与内存**：在 MIMIC-IV 上，联合训练（K=10）每轮（epoch）约需 275.4 秒，峰值训练显存约 24.2 GB。推理阶段由于移除了LLM（使用缓存嵌入），每轮仅需 3.19 秒，显存仅 518 MB。

#### 5. 实验数量与充分性
实验设计非常详尽且具有说服力：
*   **多模型插件测试**：将CoMed集成到4种不同的EHR骨干网络中，均观察到性能提升。
*   **消融实验**：逐步验证了KG结构、边特征、LLM描述及LoRA微调对性能的贡献。
*   **敏感性分析**：测试了不同LLM底座（Gemma-2, Qwen2.5, SmolLM2）的影响，证明框架具有模型无关性。
*   **临床专家审计**：邀请两位临床医生对生成的50条边进行盲审，平均得分4.84/5，验证了KG的临床准确性。
*   **数据稀缺性分析**：在不同训练集比例（25%~100%）下测试，证明了模型在小样本下的鲁棒性。

#### 6. 主要结论与发现
*   **性能领先**：CoMed 在所有指标上均显著优于现有最先进（SOTA）模型。
*   **长尾效应改善**：在罕见诊断代码（0-25%分位数）上的提升尤为显著，说明引入外部语义知识能有效弥补数据稀疏问题。
*   **推理高效**：通过预计算和缓存技术，CoMed 在保持高性能的同时，推理成本与普通GNN相当，具备实际部署可行性。

#### 7. 优点
*   **证据驱动**：结合了EHR统计数据与LLM的先验知识，有效解决了LLM在医疗领域的幻觉问题。
*   **深度融合**：通过协同学习框架，实现了文本语义（LLM）与图结构（GNN）的端到端对齐。
*   **临床可解释性**：生成的边带有文本解释和置信度，且经过了临床专家的定性验证。
*   **灵活性**：作为插件式编码器，能轻松集成到现有的各种预测流水线中。

#### 8. 不足与局限
*   **训练成本**：尽管使用了LoRA和采样策略，联合训练LLM和GNN的计算开销仍高于纯结构化模型。
*   **词汇量扩展性**：对于拥有数万个代码的超大规模医疗词汇表，维护和更新所有节点的LLM表示可能面临扩展性挑战。
*   **静态图限制**：目前构建的是全局静态KG，尚未充分考虑医疗关系随时间演变的动态特性。

（完）

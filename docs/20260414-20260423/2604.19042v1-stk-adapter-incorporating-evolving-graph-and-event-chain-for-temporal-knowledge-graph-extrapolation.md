---
title: "STK-Adapter: Incorporating Evolving Graph and Event Chain for Temporal Knowledge Graph Extrapolation"
title_zh: STK-Adapter：结合演化图与事件链的时序知识图谱外推
authors: "Shuyuan Zhao, Wei Chen, Weijie Zhang, Xinrui Hou, Junfeng Shen, Boyan Shi, Shengnan Guo, Youfang Lin, Huaiyu Wan"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19042v1"
tags: ["query:kgee"]
score: 9.0
evidence: 时序知识图谱外推与事件链集成
tldr: 针对时序知识图谱（TKG）外推任务中图结构与大语言模型（LLM）对齐浅层及特征稀释的问题，本文提出STK-Adapter。该方法通过空间-时间混合专家（MoE）、事件感知MoE及跨模态对齐MoE，深度融合演化图编码器与LLM，有效捕捉时空模式与语义依赖。实验证明其在多个基准数据集上显著优于现有方法，并具有极强的跨数据集泛化能力。
selection_source: fresh_fetch
motivation: 现有方法在将TKG演化结构与LLM结合时，存在时空信息丢失和微调过程中结构特征稀释的挑战。
method: 提出STK-Adapter，利用空间-时间MoE、事件感知MoE和跨模态对齐MoE来深度整合演化图特征与事件链语义。
result: 在多个基准数据集上的实验结果显示，STK-Adapter显著超越了当前最先进的方法，并展现出优异的泛化性能。
conclusion: STK-Adapter通过多层级的MoE架构实现了图结构与文本语义的深度对齐，为时序知识图谱推理提供了更有效的解决方案。
---

## 摘要
时序知识图谱（TKG）外推旨在基于历史事实预测未来事件。近期的研究尝试通过将 TKG 的演化结构表示和文本事件链集成到大语言模型（LLM）中来增强 TKG 外推。然而，这些方法受到两个主要挑战的限制：(1) 由于 TKG 的图演化结构表示与 LLM 的语义空间之间仅存在浅层对齐，导致关键的时空信息丢失；(2) 在 LLM 微调过程中，TKG 的演化结构特征会逐渐稀释。为了应对这些挑战，我们提出了时空知识适配器（STK-Adapter），它灵活地集成了演化图编码器和 LLM，以促进 TKG 推理。在 STK-Adapter 中，设计了一个时空混合专家模型（Spatial-Temporal MoE）来捕捉 TKG 固有的空间结构和时间模式。采用事件感知混合专家模型（Event-Aware MoE）来建模事件链中复杂的时序语义依赖关系。此外，还提出了一种跨模态对齐混合专家模型（Cross-Modality Alignment MoE），通过 TKG 引导的注意力专家促进深层跨模态对齐。在基准数据集上的广泛实验表明，STK-Adapter 显著优于现有最先进的方法，并在跨数据集任务中表现出强大的泛化能力。代码可在 https://github.com/Zhaoshuyuan0246/STK-Adapter 获取。

## Abstract
Temporal Knowledge Graph (TKG) extrapolation aims to predict future events based on historical facts. Recent studies have attempted to enhance TKG extrapolation by integrating TKG's evolving structural representations and textual event chains into Large Language Models (LLMs). Yet, two main challenges limit these approaches: (1) The loss of essential spatial-temporal information due to shallow alignment between TKG's graph evolving structural representation and the LLM's semantic space, and (2) the progressive dilution of the TKG's evolving structural features during LLM fine-tuning. To address these challenges, we propose the Spatial-Temporal Knowledge Adapter (STK-Adapter), which flexibly integrates the evolving graph encoder and the LLM to facilitate TKG reasoning. In STK-Adapter, a Spatial-Temporal MoE is designed to capture spatial structures and temporal patterns inherent in TKGs. An Event-Aware MoE is employed to model intricate temporal semantics dependencies within event chains. In addition, a Cross-Modality Alignment MoE is proposed to facilitate deep cross-modality alignment by TKG-guided attention experts. Extensive experiments on benchmark datasets demonstrate that STK-Adapter significantly outperforms state-of-the-art methods and exhibits strong generalization capabilities in cross-dataset task. The code is available at https://github.com/Zhaoshuyuan0246/STK-Adapter.

---

## 论文详细总结（自动生成）

### STK-Adapter：结合演化图与事件链的时序知识图谱外推总结

#### 1. 核心问题与研究动机
时序知识图谱（TKG）外推旨在根据历史事实预测未来事件。目前将大语言模型（LLM）引入该领域的研究主要面临两大挑战：
*   **浅层对齐导致信息丢失**：现有方法通常仅使用简单的投影层（如 MLP）将图结构嵌入映射到 LLM 的语义空间，这种“一刀切”的浅层对齐无法充分保留复杂的时空信息。
*   **特征稀释问题**：在 LLM 微调过程中，由于模型天生偏向文本语义，TKG 的演化结构特征在经过多层 LLM 处理后会逐渐稀释，限制了模型对时序图结构的建模能力。

#### 2. 方法论
论文提出了 **STK-Adapter**，通过在 LLM 的每一层中集成三个专门的混合专家（MoE）模块，实现图结构与文本语义的深度融合：
*   **空间-时间混合专家（ST-MoE）**：利用预训练的演化图编码器（如 LogCL）提取实体和关系的演化表示。通过稀疏激活的专家网络，在 LLM 的每一层中持续捕捉和维护 TKG 的空间结构和时间模式，防止特征稀释。
*   **事件感知混合专家（EA-MoE）**：针对线性化的事件链，设计了“事件感知路由”。它确保属于同一事件的所有 Token 共享相同的路由信号，从而引导专家网络专门建模事件间的复杂时序依赖和语义重复模式。
*   **跨模态对齐混合专家（CMA-MoE）**：采用 TKG 引导的注意力专家。以文本表示为 Query，以图结构表示为 Key 和 Value，通过注意力机制在每一层将时空知识注入文本语义，实现深层对齐。
*   **自适应融合与混合评分**：通过门控网络融合各模块输出。在推理阶段，结合 LLM 的解码概率分数与图模型的拓扑评分进行最终排名。

#### 3. 实验设计
*   **数据集**：使用了四个主流 TKG 基准数据集：**ICE14、ICE18、ICE15**（政治事件数据）和 **WIKI**（维基百科历史数据）。
*   **对比方法**：
    *   **嵌入/图神经网络方法**：REGCN, TiRGN, LogCL, CognTKE。
    *   **纯 LLM/微调方法**：GPT-NeoX, GenTKG, CoH。
    *   **混合/前沿方法**：LLM-DA, MESH。
*   **评估指标**：Hit@1, Hit@3, Hit@10。

#### 4. 资源与算力
*   **硬件**：使用了服务器集群，配备 **8 张 NVIDIA A40 GPU**（每张 48GB 显存）。
*   **模型配置**：以 **Llama3-8B** 为冻结的主干模型（也测试了 Qwen2.5-7B 和 Mistral-7B），微调 STK-Adapter 模块。
*   **训练细节**：采用 BF16 精度，微调 2 个 Epoch，学习率为 2e-5，使用 AdamW 优化器。

#### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多维度对比**：在四个数据集上进行了全面测试，STK-Adapter 在所有指标上均达到 SOTA。
*   **消融实验**：逐一验证了 ST-MoE、EA-MoE、CMA-MoE 以及混合评分机制的有效性。
*   **兼容性测试**：验证了该方法对不同图编码器（REGCN, TiRGN 等）和不同 LLM 主干的适配能力。
*   **泛化性实验**：进行了跨数据集的零样本（Zero-shot）迁移实验，证明了其强大的知识迁移能力。
*   **效率分析**：对比了参数量、推理时间和显存占用，证明了 MoE 架构在性能与效率间的平衡。

#### 6. 主要结论与发现
*   **深度集成至关重要**：逐层注入图特征比仅在输入端进行一次性投影能更有效地保留时空信息。
*   **MoE 的优越性**：MoE 架构允许不同专家处理不同类型的异构数据（图 vs 文本），通过专家分工解决了特征稀释问题。
*   **泛化能力强**：相比于依赖规则更新的方法（如 LLM-DA），STK-Adapter 在未见过的分布上表现更稳健。
*   **混合评分的辅助作用**：虽然 STK-Adapter 自身推理能力已很强，但结合图模型的拓扑评分能进一步微调排名。

#### 7. 优点
*   **架构创新**：将 MoE 引入 TKG-LLM 适配器，解决了多模态对齐中的特征稀释难题。
*   **即插即用**：对图编码器具有不可知性（Encoder-agnostic），可以灵活更换底层的图模型。
*   **性能卓越**：在复杂数据集（如 ICE15/18）上相比现有方法有显著提升。
*   **分析深入**：通过专家路由可视化，直观展示了模型如何学习不同层级的时空特征。

#### 8. 不足与局限
*   **计算开销**：尽管使用了参数高效的 MoE，但逐层注入和 Beam Search 解码相比传统嵌入模型仍有较高的推理延迟和内存消耗。
*   **依赖检索质量**：实验表明，模型性能对历史事件检索（TLR 策略）的质量有一定依赖性。
*   **长序列限制**：受限于 LLM 的上下文窗口和计算成本，处理极长历史链条时可能存在瓶颈。

（完）

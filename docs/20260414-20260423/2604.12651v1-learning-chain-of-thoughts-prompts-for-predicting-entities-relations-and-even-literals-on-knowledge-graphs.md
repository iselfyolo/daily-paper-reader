---
title: "Learning Chain Of Thoughts Prompts for Predicting Entities, Relations, and even Literals on Knowledge Graphs"
title_zh: 学习用于预测知识图谱上实体、关系甚至字面量的思维链提示
authors: "Alkid Baci, Luke Friedrichs, Caglar Demir, N'Dah Jean Kouagou, Axel-Cyrille Ngonga Ngomo"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12651v1"
tags: ["query:kgee"]
score: 9.0
evidence: 使用大语言模型进行知识图谱嵌入和链接预测
tldr: 针对知识图谱嵌入（KGE）模型在处理未知实体、关系及字面量时的局限性，本文提出RALP框架。该框架将链接预测重新定义为提示学习问题，利用大语言模型的泛化能力，通过贝叶斯优化（MIPRO算法）在极少样本下学习基于字符串的思维链（CoT）提示。RALP不仅能预测缺失实体和关系，还能处理复杂的OWL推理任务，在多个基准测试中显著优于传统KGE模型，为动态异构图谱推理提供了新思路。
selection_source: fresh_fetch
motivation: 传统知识图谱嵌入模型难以应对动态图谱中的未知元素及字面量，而大语言模型具备强大的泛化和推理潜力。
method: 提出RALP框架，通过贝叶斯优化在少量样本下学习思维链提示，将其作为三元组评分函数进行链接预测和推理。
result: "在多个基准测试中，RALP将MRR提升了5%以上，并在复杂的OWL类表达式推理任务中达到了超过88%的Jaccard相似度。"
conclusion: 基于提示学习的大语言模型推理是传统嵌入方法的灵活替代方案，能有效提升知识图谱在复杂场景下的预测性能。
---

## 摘要
知识图谱嵌入 (KGE) 模型在链接预测方面表现良好，但在处理未见过的实体、关系，尤其是字面量 (literals) 时面临困难，这限制了它们在动态异构图中的应用。相比之下，预训练大语言模型 (LLMs) 通过提示 (prompting) 能够有效地进行泛化。我们将链接预测重新表述为一个提示学习问题，并引入了 RALP，它学习基于字符串的思维链 (CoT) 提示，作为三元组的评分函数。通过 MIPRO 算法利用贝叶斯优化，RALP 在无需梯度访问的情况下，仅需不到 30 个训练样本即可识别出有效的提示。在推理阶段，RALP 预测缺失的实体、关系或整个三元组，并根据学习到的提示分配置信度分数。我们在转导式、数值型和 OWL 实例检索基准上进行了评估。RALP 在各数据集上将最先进的 KGE 模型性能提升了超过 5% 的 MRR，并通过高质量的推断三元组增强了泛化能力。在具有复杂类表达式（例如 $\exists hasChild.Female$, $\geq 5 \; hasChild.Female$）的 OWL 推理任务中，它实现了超过 88% 的 Jaccard 相似度。这些结果突显了基于提示的 LLM 推理是嵌入式方法的一种灵活替代方案。我们将实现、训练和评估流水线开源：https://github.com/dice-group/RALP 。

## Abstract
Knowledge graph embedding (KGE) models perform well on link prediction but struggle with unseen entities, relations, and especially literals, limiting their use in dynamic, heterogeneous graphs. In contrast, pretrained large language models (LLMs) generalize effectively through prompting. We reformulate link prediction as a prompt learning problem and introduce RALP, which learns string-based chain-of-thought (CoT) prompts as scoring functions for triples. Using Bayesian Optimization through MIPRO algorithm, RALP identifies effective prompts from fewer than 30 training examples without gradient access. At inference, RALP predicts missing entities, relations or whole triples and assigns confidence scores based on the learned prompt. We evaluate on transductive, numerical, and OWL instance retrieval benchmarks. RALP improves state-of-the-art KGE models by over 5% MRR across datasets and enhances generalization via high-quality inferred triples. On OWL reasoning tasks with complex class expressions (e.g., $\exists hasChild.Female$, $\geq 5 \; hasChild.Female$), it achieves over 88% Jaccard similarity. These results highlight prompt-based LLM reasoning as a flexible alternative to embedding-based methods. We release our implementation, training, and evaluation pipeline as open source: https://github.com/dice-group/RALP .

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RALP**（Reasoning with Automated LLM Prompts）的新框架，旨在利用大语言模型（LLM）解决知识图谱（KG）中的链接预测和推理问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：传统的知识图谱嵌入（KGE）模型（如 TransE, DistMult）将实体和关系映射为连续向量。虽然在标准链接预测上表现良好，但它们难以处理：1) 训练中未见过的实体或关系（归纳推理能力差）；2) 数值型字面量（如人口、GDP等数值）；3) 复杂的逻辑推理（如 OWL 类表达式）。
*   **核心问题**：能否将链接预测从“学习连续向量参数”转变为“学习离散的字符串提示（Prompt）”，从而利用 LLM 的泛化能力和推理能力来补全知识图谱？

### 2. 方法论
*   **核心思想**：将链接预测建模为提示学习问题。传统模型学习评分函数 $\phi_\Theta(h, r, t)$ 的向量参数 $\Theta$，而 RALP 学习字符串参数 $\beta$（即提示词），通过 LLM 对三元组的真实性进行评分或直接预测缺失部分。
*   **关键技术细节**：
    *   **RALP 与 RALP†**：RALP 直接使用默认提示词进行上下文学习（ICL）；RALP† 则使用 **MIPRO 算法**（基于贝叶斯优化的提示优化器）在无需梯度的情况下，从极少量样本（<30个）中自动搜索最优的思维链（CoT）提示词。
    *   **思维链（CoT）评分**：引导 LLM 生成推理步骤，解释为什么某个实体是合理的候选者，并给出置信度分数。
    *   **数值预测**：将数值预测视为回归任务，LLM 不仅预测具体数值，还预测置信区间 $[y_{min}, y_{max}]$。
    *   **图增强算法**：提出一种算法，利用 RALP 发现训练集中缺失的潜在真实三元组，并将其反馈给传统 KGE 模型进行数据增强。

### 3. 实验设计
*   **数据集**：
    *   **Countries (S1, S2, S3)**：用于测试地理关系的转导式链接预测。
    *   **LitWD1K**：从 Wikidata 提取，包含大量数值字面量（如人口、经纬度）。
    *   **Father**：包含 130 个复杂的 OWL 描述逻辑（ALCQHI）表达式，用于测试逻辑推理。
*   **对比方法**：对比了 7 种主流 KGE 模型（DistMult, Keci, DeCaL, MuRE, ComplEx, QMult, QuatE）。
*   **评价指标**：MRR、Hits@N（链接预测）；ICR（区间覆盖率）、IW（区间宽度）（数值预测）；Jaccard 相似度（OWL 推理）。

### 4. 资源与算力
*   **硬件环境**：使用了一台 Ubuntu 服务器，配置为 **Intel Xeon Platinum 处理器** 和 **4 张 NVIDIA H100 80GB GPU**。
*   **模型使用**：通过 vLLM 框架本地部署了 **Qwen2.5-32B-Instruct** 模型，并采用 4 路张量并行（Tensor Parallelism）。
*   **训练细节**：传统 KGE 模型训练了 256 个 epoch；RALP† 的提示优化过程仅需极少量的 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了转导式链接预测、数值回归、OWL 实例检索三大类任务。
*   **充分性评价**：实验设计较为全面，不仅对比了多种基准模型，还进行了消融实验（如 OWL 任务中 Manchester 语法 vs. DL 语法的对比）。
*   **局限性**：所选数据集规模相对较小（如 Countries 和 LitWD1K 子集），这主要是受限于 LLM 的上下文窗口长度和推理成本，尚未在大规模图谱（如完整版 FB15k-237）上验证。

### 6. 主要结论与发现
*   **性能突破**：RALP† 在 Countries-S1 数据集上达到了 **1.000 的 MRR**，这是传统 KGE 模型难以企及的。
*   **数据增强有效性**：使用 RALP 生成的高质量三元组增强训练集后，传统 KGE 模型的性能普遍提升了 5% 以上。
*   **数值推理能力**：LLM 能够很好地处理数值字面量，预测值与真实值非常接近，且置信区间校准良好（覆盖率 >75%）。
*   **语法偏好**：在 OWL 推理中，LLM 对 **Manchester 语法**（更接近自然语言）的理解远好于传统的描述逻辑（DL）符号。

### 7. 优点
*   **零样本/少样本能力**：无需大规模重新训练，仅需少量示例即可适应新图谱。
*   **处理字面量**：填补了传统 KGE 模型在处理数值、文本等字面量信息上的短板。
*   **可解释性**：通过 CoT 提示，模型能够给出预测背后的逻辑依据，而非仅仅是一个分数。
*   **灵活性**：能够处理 OWL 中复杂的逻辑约束（如“至少有 5 个孩子”）。

### 8. 不足与局限
*   **可扩展性瓶颈**：受 LLM 上下文窗口限制，无法一次性处理超大规模的邻域信息，需要复杂的采样策略。
*   **语义依赖**：高度依赖实体和关系的名称是否具有语义含义（如果实体是无意义的 ID，模型将失效）。
*   **异常值敏感**：在数值预测中，如果检索到的上下文包含极端异常值，会显著干扰 LLM 的判断。
*   **推理成本**：相比于向量点积，LLM 的推理开销（时间、算力）极其昂贵。

（完）

---
title: "Automatic Ontology Construction Using LLMs as an External Layer of Memory, Verification, and Planning for Hybrid Intelligent Systems"
title_zh: 利用大语言模型作为混合智能系统的外部记忆、验证与规划层进行自动本体构建
authors: "Pavel Salovskii, Iuliia Gorshkova"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20795v1"
tags: ["query:kgee"]
score: 10.0
evidence: 自动化的本体构建和结构化知识图谱维护流水线
tldr: 本研究提出一种混合智能系统架构，通过将大语言模型与外部本体记忆层（RDF/OWL）结合，克服了传统RAG在结构化理解和长期记忆方面的局限。该系统通过自动化流水线从异构数据中构建知识图谱，并利用SHACL进行形式化验证。实验表明，该架构在多步推理和规划任务中显著提升了性能，为构建具备可解释性和可靠决策能力的智能体提供了新方案。
selection_source: fresh_fetch
motivation: 针对当前大语言模型缺乏长期记忆、结构化理解弱及推理能力有限等问题，寻求一种结合参数化知识与形式化本体推理的混合架构。
method: 开发了一套自动化本体构建流水线，利用LLM进行实体关系提取并生成RDF三元组，同时引入SHACL约束验证和图增强推理。
result: 在汉诺塔等规划任务中的实验显示，本体增强显著提升了多步推理性能，并实现了生成-验证-修正的闭环。
conclusion: 该架构通过引入外部本体层，有效增强了LLM系统的持久性知识管理、逻辑验证和复杂任务规划能力。
---

## 摘要
本文提出了一种用于智能系统的混合架构，其中大语言模型（LLMs）通过外部本体记忆层得到了扩展。该方法不单纯依赖参数化知识和基于向量的检索（RAG），而是利用 RDF/OWL 表示构建并维护结构化知识图谱，从而实现持久、可验证且具有语义基础的推理。核心贡献是一个从异构数据源（包括文档、API 和对话日志）自动构建本体的自动化流水线。系统执行实体识别、关系抽取、规范化和三元组生成，随后利用 SHACL 和 OWL 约束进行验证，并进行持续的图谱更新。在推理过程中，LLMs 在整合了基于向量的检索、基于图的推理以及外部工具交互的组合上下文中运行。在包括汉诺塔基准测试在内的规划任务上的实验观察表明，与基准 LLM 系统相比，本体增强提高了多步推理场景下的性能。此外，本体层实现了对生成输出的形式化验证，将系统转变为“生成-验证-修正”流水线。所提出的架构解决了当前基于 LLM 的系统的关键局限性，包括缺乏长期记忆、结构化理解能力弱以及推理能力有限。它为构建需要持久知识、可解释性和可靠决策的智能体系统、机器人应用和企业级 AI 解决方案奠定了基础。

## Abstract
This paper presents a hybrid architecture for intelligent systems in which large language models (LLMs) are extended with an external ontological memory layer. Instead of relying solely on parametric knowledge and vector-based retrieval (RAG), the proposed approach constructs and maintains a structured knowledge graph using RDF/OWL representations, enabling persistent, verifiable, and semantically grounded reasoning.   The core contribution is an automated pipeline for ontology construction from heterogeneous data sources, including documents, APIs, and dialogue logs. The system performs entity recognition, relation extraction, normalization, and triple generation, followed by validation using SHACL and OWL constraints, and continuous graph updates. During inference, LLMs operate over a combined context that integrates vector-based retrieval with graph-based reasoning and external tool interaction.   Experimental observations on planning tasks, including the Tower of Hanoi benchmark, indicate that ontology augmentation improves performance in multi-step reasoning scenarios compared to baseline LLM systems. In addition, the ontology layer enables formal validation of generated outputs, transforming the system into a generation-verification-correction pipeline.   The proposed architecture addresses key limitations of current LLM-based systems, including lack of long-term memory, weak structural understanding, and limited reasoning capabilities. It provides a foundation for building agent-based systems, robotics applications, and enterprise AI solutions that require persistent knowledge, explainability, and reliable decision-making.

---

## 论文详细总结（自动生成）

这是一份关于论文《Automatic Ontology Construction Using LLMs as an External Layer of Memory, Verification, and Planning for Hybrid Intelligent Systems》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）虽然具备强大的生成和总结能力，但在处理需要**长期记忆、结构化逻辑理解、因果关系推理及形式化验证**的任务时存在显著局限。
*   **研究背景**：现有的检索增强生成（RAG）技术主要依赖向量相似度检索文本片段，这仅是一种“检索覆盖”，而非真正的“知识系统”。它无法重建实体间的复杂关系、规则和约束。
*   **整体含义**：论文提出了一种**神经符号（Neuro-symbolic）架构**，将 LLM 定位为解释和编排层，而将外部本体（Ontology）作为结构化、可验证的长期记忆层。这种转变旨在将 AI 系统从简单的“对话机器人”提升为具备“世界模型”的智能体。

### 2. 论文提出的方法论
论文的核心思想是构建一个**双重记忆系统**：向量存储（处理非结构化文本）+ RDF/OWL 本体图谱（处理结构化事实和规则）。
*   **核心架构**：利用 **MCP（Model Context Protocol）** 作为通用通信总线，连接 LLM、本体图谱、向量数据库和外部 API。
*   **Ontology Builder 流水线**：
    1.  **摄取与分段**：处理异构数据（PDF、API、对话等）。
    2.  **NER 与实体打标**：提取候选实体并分配初步类型。
    3.  **关系抽取**：识别谓词（如 `part-of`, `causes` 等）。
    4.  **规范化与对齐**：解决同义词、歧义及实体统一（URI 分配）。
    5.  **三元组构建**：将事实序列化为 `主语-谓词-宾语` 格式。
    6.  **形式化验证**：使用 **SHACL** 进行结构约束检查，使用 **OWL 推理机**（如 Pellet）进行逻辑一致性检查。
*   **闭环反馈**：系统从 LLM 的响应和对话中实时提取新事实，经过验证后更新本体，实现知识的持续累积。

### 3. 实验设计
论文采用了定量与定性相结合的评估方式：
*   **定量场景（汉诺塔基准测试）**：
    *   **任务**：测试系统在规划和逻辑推理方面的能力。
    *   **对比方法**：基准模型（Qwen3-Max） vs. 本体增强后的 Qwen3-Max。
    *   **指标**：不同盘数（3-6 盘）下的任务成功率。
*   **定性场景（Fact Analyzer 事实分析器）**：
    *   **案例**：针对复杂的临床试验监管法规（IND 提交后的 30 天期限规则）进行问答。
    *   **对比**：展示了纯 LLM 生成错误答案（被判定为 CONTRADICTED）与本体增强系统生成基于规则的正确答案（附带引用片段）的对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/运行的总时长。
*   **模型使用**：实验中提到了使用 Qwen3-Max 模型，但未披露其运行环境或推理成本。

### 5. 实验数量与充分性
*   **实验规模**：汉诺塔实验覆盖了 3 到 6 个盘子的难度梯度。
*   **充分性评价**：
    *   **初步验证性**：实验结果被作者称为“描述性证据”而非“最终统计证明”。
    *   **透明度不足**：文中未披露具体的运行次数、Prompt 模板、温度参数（Temperature）以及统计显著性分析（如置信区间）。
    *   **局限性**：实验组数相对较少，主要用于概念验证（Proof-of-Concept），在严谨的学术基准对比上尚显薄弱。

### 6. 论文的主要结论与发现
*   **性能提升**：在汉诺塔任务中，本体增强在 3 盘和 5 盘情况下分别提升了约 7% 和 12.2% 的成功率，但在极高难度（6 盘）下两者均告失败。
*   **中等复杂度红利**：本体层在任务超出简单局部搜索但尚未达到完全不可解的“中间复杂度区域”获益最明显。
*   **验证机制的价值**：本体不仅改善了检索，更提供了一个独立的验证通道，使系统能够识别并修正 LLM 的幻觉。
*   **架构转型**：成功的关键在于将 LLM 从“知识存储库”转变为“知识处理器”，利用符号逻辑确保可解释性和一致性。

### 7. 优点
*   **神经符号融合**：成功地将现代 LLM 的灵活性与传统本体工程的严谨性结合，解决了 RAG 无法处理逻辑约束的问题。
*   **标准兼容性**：全面采用了 W3C 标准（RDF, OWL, SPARQL, SHACL），保证了系统的互操作性和工业应用潜力。
*   **自动化流水线**：提出了一个闭环的本体构建流程，降低了传统本体维护中高昂的人工成本。

### 8. 不足与局限
*   **实验数据不透明**：缺乏大规模、多维度的基准测试数据，实验结果的统计效力有限。
*   **自动化风险**：虽然引入了验证，但自动构建过程中的“模式漂移”（Schema Drift）和初始错误提取仍可能导致知识图谱的质量下降。
*   **系统复杂性与延迟**：引入推理机和多步验证会显著增加系统的响应延迟，论文未对此进行量化分析。
*   **人工干预需求**：尽管目标是自动化，但在模式治理和冲突解决策略上，目前仍难以完全脱离人工监督。

（完）

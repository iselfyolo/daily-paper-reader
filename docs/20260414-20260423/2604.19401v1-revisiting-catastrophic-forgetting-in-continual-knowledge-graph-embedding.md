---
title: Revisiting Catastrophic Forgetting in Continual Knowledge Graph Embedding
title_zh: 重新审视持续知识图谱嵌入中的灾难性遗忘
authors: "Gerard Pons, Carlos Escolano, Besim Bilalli, Anna Queralt"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19401v1"
tags: ["query:kgee"]
score: 9.0
evidence: 持续知识图谱嵌入与实体干扰研究
tldr: "本研究重新审视了持续知识图谱嵌入（CKGE）中的灾难性遗忘问题。作者指出，现有方法主要关注限制旧嵌入的变化，却忽略了新实体引入带来的“实体干扰”现象，即新实体可能被错误地预测为旧问题的答案。研究通过提出修正的评估协议，揭示了现有评估对性能的高估（最高达25%），并引入了更完善的度量指标，为CKGE的准确评估提供了新标准。"
selection_source: fresh_fetch
motivation: 现有的CKGE评估协议忽略了新旧实体间的干扰，导致对灾难性遗忘程度的评估存在系统性偏差。
method: 提出了一种考虑实体干扰的修正评估协议，并设计了专门用于衡量CKGE灾难性遗忘的新指标。
result: "实验证明忽略实体干扰会使性能评估高估多达25%，且不同KGE模型受此影响程度各异。"
conclusion: 实体干扰是导致CKGE遗忘的关键因素，修正后的评估协议能更真实地反映模型在动态知识图谱上的表现。
---

## 摘要
知识图谱嵌入 (KGE) 支持知识图谱 (KG) 上的广泛下游任务。在实践中，随着新实体和事实的增加，知识图谱不断演化，这促使了随时间更新嵌入的持续知识图谱嵌入 (CKGE) 方法的发展。当前的 CKGE 方法主要通过限制对现有嵌入的修改来解决灾难性遗忘（即在先前学习的任务上性能下降的问题）。然而，我们表明这一观点是不完整的。当引入新实体时，它们的嵌入可能会干扰先前学习的嵌入，导致模型预测这些新实体而非先前正确的答案。这种现象被称为实体干扰 (entity interference)，在很大程度上被忽视了，且未被纳入当前的 CKGE 评估协议中。结果，对灾难性遗忘的评估变得具有误导性，CKGE 方法的性能被系统性地高估了。为了解决这个问题，我们引入了一种考虑实体干扰的修正 CKGE 评估协议。通过在多个基准测试上的实验，我们表明忽视这种效应会导致性能高估高达 25%，特别是在实体显著增长的场景中。我们进一步分析了不同的 CKGE 方法和 KGE 模型如何受到不同遗忘源的影响，并引入了专门为 CKGE 定制的灾难性遗忘指标。

## Abstract
Knowledge Graph Embeddings (KGEs) support a wide range of downstream tasks over Knowledge Graphs (KGs). In practice, KGs evolve as new entities and facts are added, motivating Continual Knowledge Graph Embedding (CKGE) methods that update embeddings over time. Current CKGE approaches address catastrophic forgetting (i.e., the performance degradation on previously learned tasks) primarily by limiting changes to existing embeddings.   However, we show that this view is incomplete. When new entities are introduced, their embeddings can interfere with previously learned ones, causing the model to predict them in place of previously correct answers. This phenomenon, which we call entity interference, has been largely overlooked and is not accounted for in current CKGE evaluation protocols. As a result, the assessment of catastrophic forgetting becomes misleading, and CKGE methods performance is systematically overestimated.   To address this issue, we introduce a corrected CKGE evaluation protocol that accounts for entity interference. Through experiments on multiple benchmarks, we show that ignoring this effect can lead to performance overestimation of up to 25%, particularly in scenarios with significant entity growth. We further analyze how different CKGE methods and KGE models are affected by the different sources of forgetting, and introduce a catastrophic forgetting metric tailored to CKGE.

---

## 论文详细总结（自动生成）

这是一份关于论文《Revisiting Catastrophic Forgetting in Continual Knowledge Graph Embedding》（重新审视持续知识图谱嵌入中的灾难性遗忘）的结构化分析报告：

### 1. 核心问题与研究动机
*   **研究背景**：知识图谱（KG）是动态演化的，新实体和事实不断涌现。持续知识图谱嵌入（CKGE）旨在增量更新嵌入模型，同时缓解“灾难性遗忘”。
*   **核心问题**：作者指出，当前学术界对 CKGE 中灾难性遗忘的理解是不完整的。现有的评估协议和方法主要关注**表示漂移（Representation Drift）**（即旧实体的向量发生了改变），却完全忽略了**实体干扰（Entity Interference）**。
*   **实体干扰定义**：即使旧实体的嵌入保持不变，新引入实体的嵌入也可能在向量空间中占据与旧实体相似的位置，导致模型在处理旧查询时错误地预测出新实体。
*   **研究动机**：由于现有评估协议在测试旧数据时排除了新实体，导致 CKGE 方法的性能被严重高估，掩盖了真实的遗忘程度。

### 2. 方法论
*   **遗忘源分解**：将灾难性遗忘分为两部分：
    1.  **表示漂移**：旧实体/关系嵌入的更新导致旧知识丢失。
    2.  **实体干扰**：新实体的嵌入进入了旧知识的决策区域。
*   **修正的评估协议**：
    *   **核心思想**：在评估任何历史快照（Snapshot）的测试集时，候选实体集应包含**当前时刻所有已知的实体**，而非仅包含该快照产生时的实体。
    *   **公式改进**：修正了排名（Rank）计算公式，确保新实体作为负采样候选者参与排序。
*   **统一的遗忘度量指标 (CF)**：
    *   提出一个综合指标，考虑了：(1) 性能的相对下降；(2) 训练周期的权重（早期快照经历更多更新，权重更高）；(3) 测试集的大小。
*   **缓解策略分类**：将现有技术归纳为数据层（重放、排序）、模型层（架构扩展、冻结）、损失层（正则化、重构、对齐）和权重更新层（掩码）。

### 3. 实验设计
*   **数据集**：使用了 8 个基准数据集，包括基于 FB15K-237 构建的 ENTITY、RELATION、FACT、HYBRID（考察不同增长类型），以及 GraphEqual/Higher/Lower（考察不同增长速率）和 PS-CKGE（考察模式偏移）。
*   **对比方法**：
    *   **基础基准**：Fine-tuning（微调）、Retraining（全量重训）、EWC（正则化）、EMR（重放）。
    *   **SOTA CKGE 方法**：LKGE, FMR, ETT-CKGE, DebiasedKGE, FastKGE, SAGE, incDE。
*   **底层模型**：主要使用 TransE，并在扩展实验中测试了 TransH, TransR, RotatE, BoxE, DistMult, ComplEx 等 10 种 KGE 模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **复现性**：作者提供了 GitHub 仓库地址，并提到对 SAGE 等模型进行了超参数网格搜索（如嵌入维度 $d \in [50, 200]$，学习率 $lr \in [10^{-2}, 10^{-5}]$），暗示了实验具有一定的计算工作量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 8 个不同特性的数据集。
    *   对比了 10 种不同的持续学习/CKGE 算法。
    *   横向对比了 10 种不同的底层 KGE 评分函数。
*   **充分性评价**：实验非常充分且具有说服力。作者不仅验证了评估协议的偏差，还深入探讨了“稳定性-塑性”权衡，并分析了不同模型架构（平移模型 vs 语义模型）对干扰的敏感度。
*   **客观性**：通过引入“全量重训（Retraining）”作为上限，客观揭示了现有 CKGE 方法在面对实体干扰时的无力感。

### 6. 主要结论与发现
*   **性能高估**：忽略实体干扰会导致性能评估高估多达 **25%**。在实体增长显著的场景下，某些号称“零遗忘”的方法（如冻结旧嵌入的方法）实际上存在严重的性能下降。
*   **干扰是主要矛盾**：在实体数量大幅增长的场景中，实体干扰对遗忘的贡献超过了表示漂移。
*   **模型差异**：
    *   **平移模型（如 TransR）**：对实体干扰最稳健，稳定性高但获取新知识的能力（塑性）较弱。
    *   **语义模型（如 QuatE, ComplEx）**：获取新知识快，但极易受到表示漂移和实体干扰的影响，遗忘最严重。
*   **重训优势**：全量重训得到的嵌入空间比任何 CKGE 方法都更能抵抗干扰，说明目前的 CKGE 策略尚未触及干扰问题的核心。

### 7. 优点
*   **视角独特**：首次系统性地定义并量化了 CKGE 中的“实体干扰”现象，纠正了长期以来的评估偏差。
*   **评估严谨**：提出的修正协议和 CF 指标为该领域提供了更科学、更公平的评价标准。
*   **分析深入**：不仅指出了问题，还通过多模型对比揭示了不同几何表示（平移、旋转、语义交互）在持续学习中的本质特性。

### 8. 不足与局限
*   **方法局限性**：大多数现有的 CKGE 专门方法（如 LKGE, FMR 等）在设计上高度绑定 TransE，难以直接迁移到更复杂的 KGE 模型（如 RotatE 或语义模型）中。
*   **缺乏新算法**：论文重点在于“重新审视”和“评估纠偏”，虽然指出了问题，但并未提出一种全新的、专门针对“实体干扰”的 CKGE 算法。
*   **应用限制**：实验主要基于 FB15K-237 的变体，对于更大规模（如数百万实体）或极度稀疏的动态知识图谱，其实体干扰的规律是否一致仍待验证。

（完）
